# 18.4 — Phase-18 hygiene status pass (0 Class A / ~12 Class B / Class C unchanged)

Fifth Phase-18 unit; docs-only. Mirrors Phase-13 Unit 13.4 +
Phase-14 Unit 14.6 + Phase-15 Unit 15.6 + Phase-16 Unit 16.6 +
Phase-17 Unit 17.5 hygiene catalog patterns. Catalogs in-flight
state after Phase 18's three code units (18.1 + 18.2 + 18.3) and
what survives heading into Units 18.5 + 18.6.

## THINK

### Class A — in-flight Phase-18 cleanup: **0 items**

Phase-18's three code units shipped without deferrals. All ADR-0018
D-G inheritance contract clauses code-realized at the second
exercise:
- **`renderReviewNotesMarkdown` sibling helper** + **`reviewNotesProcessor`
  singleton** + **`reviewNotesSchema` (identical-to-bioSchema Phase-18)**
  shipped Unit 18.1 with `bioProcessor` rename refactor (zero
  behavior change).
- **`/curator/challenges/[id]` reviewNotes markdown render** (FULL
  render — no clamp) shipped Unit 18.2.
- **`/profile` own-challenges listing reviewNotes markdown + CSS
  `line-clamp-3`** shipped Unit 18.3 with `truncateRationale()`
  removed for reviewNotes (markdown source-truncation
  incompatibility resolution).

**Phase-18 Class A count = 0** matches **Phase-12 + 13 + 14 + 15 +
16 + 17** well-scoped pattern — **seven consecutive phases** ship
with 0 Class A items.

### Class B — Phase-18 follow-ons: ~12 items in 3 categories

#### B.1 Markdown subset expansion (9; Phase-17 carryovers + Phase-18 new)

Phase-17 carryovers (unchanged):

- **Tables (GFM)** + **footnotes (GFM)** + **images in bio** +
  **syntax-highlighted code blocks** + **bio length expansion** +
  **`@mentions`** + **task-list interactivity** + **live preview
  client boundary** — 8 items per ADR-0018 D-H Phase-17 deferrals.

Phase-18 newly surfaced:

- **Q72 candidate**: `reviewNotesSchema` divergence from `bioSchema`
  — tables / footnotes / extended markdown subset for curator
  reviewNotes (4000-char field; longer content; tabular COI
  comparisons demand). Phase-19+ if curator demand surfaces. Adds
  surface-specific schema authoring per ADR-0018 D-G inheritance
  contract; current Phase-18 ships identical schemas with explicit
  shallow copy signaling "intentional parity Phase-18 / Phase-19+
  may diverge."

#### B.2 Surface expansion (3)

- **Markdown rating-action `rationale`** (Phase-18+; rationale is
  Phase-11 plain-text field; couples to existing `truncateRationale()`
  source-truncation; promotion would need analogous CSS line-clamp
  + markdown render swap; couples to per-challenge detail page).
- **Methodology page markdown unification** (long-term; Velite MDX
  pipeline vs `lib/markdown/` USER-STATE markdown rendering —
  unification is a larger architectural decision; Phase-19+ if
  signal demands).
- **Per-challenge detail page** (Phase-11 + 13 carryover; couples
  to Phase-18 Unit 18.3's `line-clamp-3` UX — user clicks through
  for full reviewNotes / rationale rendering). ~2-3 units when
  promoted.

#### B.3 Operational + infrastructure (5; unchanged from Phase 17)

- **Q69** (`BLOB_READ_WRITE_TOKEN` provisioning) + **Q54** +
  **Q55** operational gates carryover.
- **CI dummy `AUTH_SECRET`** (Phase-9 Class A item 3 carryover).
- **`pnpm db:migrate` doc** for new contributors — 6 migrations
  unchanged from Phase 17.
- **First LHCI run** validating Phase-14 through Phase-18
  auth-aware + markdown surfaces. 19 LHCI URLs unchanged.
- **Middleware-based auth-route protection** threshold stays at 2
  (Phase 18 added zero new protected routes).

### Class C — pre-existing carryovers (unchanged from Phase 17 Unit 17.5)

All ~25 Class C items persist unchanged. Notable items:

- HTML shell migration + Unit 8.4 STILL ON HOLD per parallel-session preservation.
- Abandoned-blob cleanup script (Phase-16 carryover; Phase 18 added zero blobs).
- External URL allowlist composability (ADR-0017 Option 2).
- SiteHeader avatar pill / avatar-dropdown (Phase-14 Class B).
- Q70 EXIF stripping (Phase-16 carryover) + Q68 expansion (content moderation on images).
- Mobile-nav variant + form-state preservation.
- Rate-limiting on review API + curator-of-record case-insensitive.

### Phase-17 → Phase-18 delta

| Metric | Phase 17 close (`ad951e4`) | Phase 18 close pre-this (HEAD `bf3acb4`) | Δ |
|---|---|---|---|
| Commits | 8 | 4 + this hygiene + 18.5 + 18.6 (anticipated) | +7 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | 18 | **18** | **0** (ADR-0018 D-G inheritance pre-documented Phase 17; no new ADR Phase 18) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (**second consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-17 stack) | (Phase-17 stack) | 0 |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 552 / 54 files | **562 / 54 files** | **+10 / 0** |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** (per ADR-0018 D-F invariant) |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS resolved | 24 | 24 (Q71 promotes in Unit 18.5 → 25) | 0 (anticipated +1) |
| OPEN_QUESTIONS open | 30 | 30 | 0 |
| OPEN_QUESTIONS lean | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 58 | 58 (anticipated +1 in 18.5) | 0 |
| i18n keys per locale | 127 | 127 | 0 |
| LHCI URLs | 19 | 19 | 0 |
| `lib/markdown/` files | 3 | 3 | 0 (extended; no file count change) |
| XSS-audit surfaces | 1 | **2** | **+1** (`renderReviewNotesMarkdown` pipeline sibling) |
| `dangerouslySetInnerHTML` surfaces | 2 | **4** | **+2** (`/curator/challenges/[id]` + `/profile` own-challenges) |
| Markdown render-consistency surfaces | 2 | **4** | **+2** |
| Schema-parity surfaces | 0 | **1** | **+1** (3 parity tests assert `reviewNotesSchema` ≡ `bioSchema` Phase-18) |
| Custom rehype plugins | 2 | 2 | 0 (`rehypeDemoteHeadings` + `rehypeStripUnsafeHrefs` shared across both processors) |

### Architectural firsts shipped in Phase 18

1. **First ADR-0018 D-G inheritance exercise** in project history —
   sibling helper + sibling schema pattern realized at second
   exercise of the contract Phase-17 documented. Validates the
   inheritance shape: each surface gets its own helper + schema;
   audit boundary stays explicit per surface.
2. **First reviewNotes markdown surface** in project history —
   Phase-12 reviewNotes plain-text rendering (`whitespace-pre-wrap`)
   upgraded to markdown render across both consumer surfaces
   (curator dashboard FULL + /profile own-challenges with
   line-clamp).
3. **Second consecutive 0-migration phase** (Phase 17 + Phase 18) —
   5 of 9 phases since DB landed (Phase 10 + 13 + 14 + 17 + 18 are
   0-migration; Phase 9 + 11 + 12 + 15 + 16 shipped 1 each; Phase
   18 ratio = 55.6% post-DB).
4. **First 4-surface markdown render consistency** in project
   history — single 14-class prose-styling vocabulary across:
   `/u/{handle}` bio (Phase 17) + `/profile` bio preview (Phase 17)
   + `/curator/challenges/[id]` reviewNotes (Phase 18) + `/profile`
   own-challenges reviewNotes (Phase 18).
5. **First CSS `line-clamp` visual truncation** in project history
   (Tailwind v4 built-in utility on `/profile` listing) — replaces
   Phase-12's `truncateRationale()` source-truncation pattern for
   markdown-incompatible reviewNotes. Establishes pattern: when
   source-truncation breaks rendering (markdown, HTML, etc.),
   visual-truncation via CSS is the correct approach.
6. **First XSS-audit schema-parity pattern** in project history —
   3 parity tests assert `reviewNotesSchema.tagNames` ≡
   `bioSchema.tagNames` (and `protocols` + `attributes`). Phase-19+
   divergence will need parity-test removal + audit comment per
   surface-specific schema.
7. **First "rename refactor" code unit** in project history —
   `bioProcessor` rename (Phase-18 D-8); zero behavior change;
   establishes pattern that disambiguating renames are valid Class A
   in-flight cleanup type even when no functional change.

### Phase-18+ Q-candidates flagged

Carryovers from Phase 17 (8 candidates carried unchanged):

- **Q59** (CLI emit-challenge-action) — carried Phase-12 through
  Phase-17/18 (now 7 phases).
- **Q60** (curator authz evolution).
- **Q61** (submitter anonymity option).
- **Q62** (rejection-rationale public visibility).
- **Q64** (per-user privacy opt-out).
- **Q65** (per-curator activity feed).
- **Q68 expansion** (content moderation on uploaded images).
- **Q70** (EXIF stripping on uploaded images).

Phase-18 newly surfaced:

- **Q72 candidate** (`reviewNotesSchema` divergence from `bioSchema`
  — markdown subset extensions for curator reviewNotes per Unit
  18.0 D-3 deferral). Phase-19+ if curator demand surfaces. Flagged
  in Unit 18.0 prep + Unit 18.1 schema-parity-by-explicit-shallow-
  copy decision.

Total **9 Phase-18+ Q-candidates** flagged for Phase-19+ kickoff.

### Phase-19+ ADR slot reservations

ADR-0018 D-G inheritance contract realized at second exercise in
Phase 18 without needing a new ADR. ADR-0019 candidate slot remains
open after **five-phase ADR cycle without claim** (Phase-15 ADR-0016
+ Phase-16 ADR-0017 + Phase-17 ADR-0018 + Phase-18 NO new ADR):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried 9 phases; strongest "patience signal" among architectural
  threads; previously ADR-0016 / 0017 / 0018 candidate — slots
  claimed by Phase 15 + 16 + 17 respectively; ADR-0019 candidate
  now).
- **Image-transcoding pipeline** (Q70 + Q68 expansion) — `sharp` vs
  external service.
- **Content moderation API** (Q68 + Q68 expansion).
- **Markdown subset extensions** (tables / footnotes / images /
  syntax-highlighting per Phase-18 D-H + Q72 candidate) — if
  Phase-17/18 deployment surfaces signal.
- **Subscriber-list email provider** (Phase-5 D-4 punt carried 13+
  phases — strongest overall "patience signal").

### Phase-17 single-session vs Phase-18 single-session pattern continuation

Phase 17 shipped as a single-session phase (primary shipped all 8
units 17.0 → 17.7). Phase 18 is shaping up the same way (primary
shipped all 4 code units 18.0 → 18.3 in single session; 18.4 +
18.5 + 18.6 anticipated single-session too).

The constitution + auto-memory + ADR ledger continues to converge
single-session work on the same outputs that parallel-session
work would produce; Phase 16's 8-of-9 parallel-session shipping
pattern + Phase 17's single-session pattern + Phase 18's continued
single-session pattern bracket the operational shape.

### Smoke gates

Docs-only — no source files modified beyond this THINK doc + the
hygiene CHANGELOG entry.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/18.4-phase-18-hygiene.md` (this).
- CHANGELOG: Unit 18.4 entry pinning the catalog + deltas.
- typecheck / test / build untouched.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 18.4 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-17 well-scoped pattern (seven
consecutive); ~12 Class B items in 3 categories (9 markdown subset
expansion incl. Phase-18 Q72 candidate + 3 surface expansion + 5
operational); Class C carryovers unchanged from Phase 17 Unit 17.5;
Phase 17 → 18 delta (18 ADRs unchanged / 0 migrations / 0 new deps
/ +10 tests / 0 bundle regression / +1 XSS-audit surface / +2
dangerouslySetInnerHTML surfaces / +1 schema-parity test surface);
7 Phase-18 architectural firsts (incl. ADR-0018 D-G first exercise,
first reviewNotes markdown, 2nd consecutive 0-migration phase,
first 4-surface markdown consistency, first CSS line-clamp visual
truncation, first XSS-audit schema-parity pattern, first rename
refactor); 9 Phase-19+ Q-candidates (Q59/Q60/Q61/Q62/Q64/Q65/Q68-
expansion/Q70/Q72); ADR-0019 candidate slot open after 5-phase
ADR cycle without multi-provider-OAuth claim.
