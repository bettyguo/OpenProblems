# 27.2 — Phase-27 hygiene status pass (0 Class A / ~5 Class B / Class C unchanged; sixteen consecutive well-scoped phases)

Third Phase-27 unit; docs-only. Mirrors Phase-13 through Phase-26
hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-27 cleanup: **0 items**

Phase-27's single code unit (27.1) shipped without deferrals.
F (markdown rationale) + J ("View details" links) bundled
realization across 7 files (3 markdown lib + 4 page routes + 2
i18n files + 1 test file) + 10 new vitest tests. **Phase-27
Class A count = 0** matches Phase-12 through Phase-26 well-
scoped pattern — **sixteen consecutive phases** ship with 0
Class A items.

### Class B — Phase-27 follow-ons: **~5 items in 11 categories**

#### B.1 Image-processing expansion (3 carried unchanged)

#### B.2 Phase-17 markdown subset expansion (8 carried unchanged)

#### B.3 Phase-18 surface expansion (3 + Q72 carried unchanged)

Phase 27 surfaces a **Q72-analogue for rationale** —
`rationaleSchema` could diverge from `bioSchema` Phase-28+ if
user demand surfaces (parallels Phase-18 Q72 for reviewNotes).
Tracked under B.3 alongside Q72 since the pattern is identical
(sibling schema divergence under ADR-0018 D-G).

#### B.4 Phase-20 backfill operational follow-ons (3 carried)

#### B.5 Phase-21 cleanup operational follow-ons (2 carried)

#### B.6 + B.7 (0 carried; closed Phase 25)

#### B.8 Phase-24 transcode operational follow-ons (1 carried)

AVATAR_SIZE / WEBP_QUALITY tuning.

#### B.9 Operational + infrastructure (9 carried)

#### B.10 Phase-26 detail-page follow-ons (**2 carried; 2 closed Phase 27**)

- ~~Markdown rationale~~ — **CLOSED Phase 27 Unit 27.1**.
- ~~Listing-page "View details" links~~ — **CLOSED Phase 27
  Unit 27.1**.
- acceptedActionId link target carried.
- Curator-translated FR review carried.

#### B.11 Operational + i18n maintenance (1 carried)

i18n key count audit (now 140 per-locale).

#### B.12 Phase-27 markdown-promotion follow-ons (Phase-27 newly surfaced)

- **Rating-action `rationale` markdown promotion** —
  separate from rating-challenge `rationale`. Rating-action
  YAMLs ship `dimensions.<dim>.rationale` as plain-text per
  Phase-0 schema. Promotion to markdown would extend the
  Phase-17/18/27 ADR-0018 D-G inheritance contract to a fourth
  call site. ~2-3 units; Phase-28+ if curator demand. (Phase
  27 promoted ratingChallenges.rationale ONLY — distinct
  schema field.)

### Class C — pre-existing carryovers (unchanged)

All ~25 Class C items persist unchanged.

### Phase-26 → Phase-27 delta

| Metric | Phase 26 close (`ddda758`) | Phase 27 close pre-this (HEAD `f842435`) | Δ |
|---|---|---|---|
| Commits | 5 | 2 + this hygiene + 27.3 + 27.4 (anticipated) | +5 anticipated total |
| ADRs | **19** | **19** | **0** (**ninth phase since Phase 3** — Phase 18 + 20-27; **first eight-phase no-new-ADR streak**) |
| Migrations | 6 | **6** | **0** (**eleventh consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-26 stack) | (Phase-26 stack) | 0 |
| Tests | 595 / 55 files | **605 / 55 files** | **+10 / 0** |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}/challenges` bundle | 108 kB | **108 kB** | **0** |
| `/u/{handle}/challenges/{id}` bundle | 108 kB | **108 kB** | **0** |
| `/problems/{slug}/challenges` bundle | 108 kB | **108 kB** | **0** |
| Dynamic page+API routes | 8 | 8 | 0 |
| OPEN_QUESTIONS resolved/open/lean/total | 26 / 29 / 4 / 59 | 26 / 29 / 4 / 59 | 0 (**eighth-consecutive Q-tally freeze anticipated** Phase 20-27) |
| i18n keys per locale | 139 | **140** | **+1** (new `public_challenges.view_details`) |
| `lib/markdown/` files | 3 (+ 1 .test.ts) | 3 (+ 1 .test.ts) | **0** (in-place edits) |
| Markdown render surfaces | 2 (bio + reviewNotes) | **3** (bio + reviewNotes + rationale) | **+1** (third sibling under ADR-0018 D-G) |
| Markdown render call sites | 5 (bio at `/u/{handle}` + `/profile`; reviewNotes at `/curator/challenges/[id]` + `/profile` + `/u/{handle}/challenges/{id}` Phase-26) | **9** (Phase-27 adds rationale at all 4 surfaces: 3 listings + detail page) | **+4** call sites |
| `dangerouslySetInnerHTML` surfaces | 5 | **9** | **+4** (same as above) |
| `truncateRationale()` helper usages | 3 (one per listing page; each private) | **0** (helpers removed Phase 27) | **-3** (dead code eliminated) |
| User-facing UX phases since Phase 18 | 1 (Phase 26) | **2** (Phase 27) | **+1** |

### Architectural firsts shipped in Phase 27

1. **First eight-phase no-new-ADR streak** (Phase 18 + 20-27).
2. **First eight-phase Q-tally freeze** anticipated (Phase
   20-27).
3. **Third sibling schema under ADR-0018 D-G** — bio (Phase 17)
   + reviewNotes (Phase 18) + **rationale (Phase 27)**.
   Validates the inheritance contract at 3 surfaces;
   `rationaleSchema` shape identical to `bioSchema` Phase-27
   per scope-cap discipline (mirrors `reviewNotesSchema`
   Phase-18 precedent).
4. **First markdown helper with `string → string` signature**
   — `renderRationaleMarkdown` differs from siblings
   (`string | null → string | null`) because rationale is
   NOT NULL per Phase-11 schema.
5. **First phase to close 2 items from the immediately-prior
   phase's B-class catalog in one bundle** — Phase 26 surfaced
   B.10 items 1+2; Phase 27 closed both in immediately-next
   phase. Tightest 2-item follow-on close cadence in project
   history.
6. **First phase to add markdown rendering at 4 new call sites
   in one code unit** — detail page + 3 listing pages all
   gain `renderRationaleMarkdown` invocation. Phase-17
   originally shipped 2 bio call sites; Phase-18 shipped 2
   reviewNotes call sites; Phase-26 added a third reviewNotes
   call site. Phase-27's 4-at-once is the largest single-phase
   call-site addition.
7. **First dead-code removal across multiple files** —
   `truncateRationale()` helper removed from 3 listing pages
   simultaneously (3 private function definitions deleted).
   Mirrors Phase-24's `inferExt()` removal but at greater
   scope (3 files vs 1).
8. **First listing-page source-truncation removal across 3
   pages simultaneously** — Phase-18 removed Phase-12
   source-truncation for reviewNotes on `/profile` only;
   Phase-27 extends the line-clamp-replacing-source-truncation
   pattern to all 3 listing pages (`/u/{handle}/challenges` +
   `/profile` + `/problems/{slug}/challenges`) for rationale.
9. **Eleventh consecutive 0-migration phase** (Phase 17-27; 14
   of 18 phases since DB landed).
10. **Eleventh consecutive single-session phase** (Phase 17-27).
11. **Second consecutive user-facing-UX phase after the
    operational-script-keystone streak break** (Phase 26
    broke the streak with the detail page; Phase 27 continues
    with markdown rationale + listing-to-detail links).

### Phase-27+ Q-candidates flagged

Carryovers from Phase 26 (7 candidates; 0 promoted Phase 27; 0
new Phase 27):

- **Q60** / **Q61** / **Q62** / **Q64** / **Q65** / **Q68
  expansion** / **Q72**.

### Phase-28+ ADR slot reservations

**ADR-0020 candidate slot remains open after 14-phase ADR cycle
without multi-provider-OAuth claim** (Phase-15 ADR-0016 +
Phase-16 ADR-0017 + Phase-17 ADR-0018 + Phase-18 NO-new-ADR +
Phase-19 ADR-0019 + Phase-20-27 NO-new-ADR):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried **17 phases** — strongest architectural patience
  signal growing each cycle).
- **Image-processing API content moderation** (Q68 + Q68
  expansion).
- **Markdown subset extensions ADR** (Phase-17 D-H + Q72; Phase
  27 surfaces a Q72-analogue for rationale).
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  **21+ phases** — strongest overall patience signal growing).

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/27.2-phase-27-hygiene.md` (this).
- CHANGELOG: Unit 27.2 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 27.2 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-26 well-scoped pattern
(**sixteen consecutive**); ~5 Class B items in 11 categories
(3 image-processing carried; 8 markdown carried; 3 Phase-18
carried + Q72 + rationaleSchema-divergence candidate
surfaced; 3 Phase-20 carried; 2 Phase-21 carried; 0 carried for
B.6 + B.7; 1 Phase-24 carried; 9 operational carried;
**B.10 2 of 4 CLOSED Phase 27** — markdown rationale + "View
details" links; 2 carried — acceptedActionId link target +
curator-translated FR; 1 Phase-26 i18n audit; **1 Phase-27
newly surfaced under B.12**: rating-action `rationale`
markdown promotion); Class C carryovers unchanged; Phase 26 →
27 delta (0 ADR Δ; 0 migration Δ; 0 dep Δ; 0 env var Δ; +10
tests / 0 file Δ; +1 i18n key per locale; 0 bundle Δ; +1
markdown render surface; +4 markdown render call sites; +4
`dangerouslySetInnerHTML` surfaces; -3 `truncateRationale`
helper usages; +1 user-facing UX phase since Phase 18 → 2);
**11 Phase-27 architectural firsts** (incl. **first eight-
phase no-new-ADR streak**, **first eight-phase Q-tally freeze**,
**third sibling schema under ADR-0018 D-G**, **first `string →
string` markdown helper signature**, **first 2-item-from-prior-
phase close in one bundle**, **first 4-new-call-site markdown
phase**, **first dead-code removal across 3 files**, **first
listing-page source-truncation removal across 3 pages**); 7
Phase-28+ Q-candidates carry; ADR-0020 candidate slot remains
open after **14-phase ADR cycle without multi-provider-OAuth
claim** (Phase-9 Class B item 8 carried **17 phases**; Phase-5
D-4 carried **21+ phases**).
