# 22.4 — Phase 22 acceptance gate (ADR-0014 D-D realization; 19 ADRs unchanged; third consecutive operational-script-keystone phase = pattern crystallized)

Fifth and final Phase-22 unit; docs-only. Closes Phase 22
(**thirteenth NON-§13 phase**; ADR-0014 D-D `emit-challenge-action`
CLI helper realized). Mirrors Phase-12 through Phase-21
acceptance-gate patterns.

> **§12 sign-off**: Phase 23 sign-off **pending** at end of unit.
> "Continue" override path available (**eighteenth invocation**
> opportunity).

## THINK

### Phase 22 ledger

**5 units shipped** (22.0 prep → 22.4 gate). **0 deferrals**;
**0 scope drift**. **Eleventh consecutive well-scoped phase**.
**Matches Phase 20 + 21 5-unit shape** — three consecutive
5-unit operational-script-keystone phases.

| Unit | Title | Type | Status |
|---|---|---|---|
| 22.0 | Phase 22 prep | docs | shipped `40bac81` |
| 22.1 | `emit-challenge-action` CLI + scaffold lib (+11 tests) | code | shipped `570e739` |
| 22.2 | Phase-22 hygiene status pass | docs | shipped `c20a76f` |
| 22.3 | ADR-0014 D-D Phase-22 implementation note | docs | shipped `8227525` |
| 22.4 | **Phase 22 acceptance gate** (this unit) | gate | this commit |

### Phase-22 closure summary

**ADR-0014 D-D `emit-challenge-action` CLI helper REALIZED** in
Unit 22.1 via `scripts/emit-challenge-action.ts` +
`lib/rating-challenges/scaffold.ts` + 11 vitest tests. **First
double-digit ADR-D-clause carryover closed** — ADR-0014 D-D
carried Phase-12 through Phase-21 = 10 phases. CLI helper writes
gitignored draft YAML at `drafts/ratings/<challenge-id>.yaml`
(drafts-first per Phase-5 `extract-leaderboard` precedent);
curator reviews + edits + moves to `content/` + commits manually
(per ADR-0014 D-D manual-commit-step requirement); returns to UI
to attach `acceptedActionId` filename.

Phase 22 is the **fourth phase since Phase 3 to close without
new ADR** (Phase 18 + 20 + 21 + 22; **first three-phase no-new-
ADR streak**) and the **third consecutive operational-script-
keystone phase** (Phase 20 + 21 + 22). **Pattern crystallized**:
well-scoped operational-script keystones now stack to a recurring
rhythm.

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`8227525`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **575/575 across 55 vitest files**. +11 / +1
  Δ from Phase 21 close (564/54); **first phase since Phase 19
  to add tests** (Phase 20 + 21 both shipped zero); **first
  operational-script-keystone phase to ship vitest tests**.
- **`pnpm build`** → ~659 prerendered pages + 7 dynamic page+API
  routes UNCHANGED. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-22 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` = 117 kB UNCHANGED. `/u/{handle}` = 108 kB
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32 baseline
  since Phase 2; unchanged through every Phase 3-22 unit).
- **`pnpm lint`** clean.

### Architectural firsts shipped in Phase 22

1. **First YAML-scaffolding surface** in project history —
   Phase 5 `extract-leaderboard` writes JSON diffs to `drafts/`;
   Phase 22 introduces YAML-scaffolding to `drafts/ratings/`
   targeting the content-tree shape directly.
2. **Third consecutive operational-script-keystone phase** —
   Phase 20 + 21 + 22. **Pattern crystallized**.
3. **Sixth consecutive 0-migration phase** (Phase 17 + 18 + 19 +
   20 + 21 + 22; 9 of 13 phases since DB landed are 0-migration).
4. **Sixth consecutive single-session phase** (Phase 17-22).
5. **Fourth phase since Phase 3 to close without new ADR** (Phase
   18 + 20 + 21 + 22; **first three-phase no-new-ADR streak**).
6. **First double-digit ADR-D-clause carryover closed** —
   ADR-0014 D-D carried 10 phases.
7. **First three-phase Q-tally freeze** in project history
   (Phase 20 + 21 + 22).
8. **First phase since Phase 19 to add tests** — Phase 20 + 21
   shipped zero new tests; Phase 22 ships +11 tests + 1 new
   `lib/` module because the YAML scaffolder is pure-function-
   testable.
9. **First operational-script-keystone phase to ship vitest
   tests** — Phase 20 + 21 scripts depended on live
   infrastructure (Vercel Blob list+del / Turso DB); Phase 22
   scaffolder is pure string-in / string-out.
10. **Pure-function-extraction precedent established** — pure
    helpers promoted to `lib/<domain>/<helper>.ts` + co-located
    `.test.ts` when testing requirement conflicts with script's
    top-level-await CLI shell. Future operational scripts with
    testable helpers follow this precedent.
11. **Implementation-note-on-ADR convention codified** — Phase
    20 Unit 20.3 annotated Q70 in `OPEN_QUESTIONS.md`; Phase 22
    Unit 22.3 annotates ADR-0014 D-D directly because
    Q59-candidate was never promoted to OQ. Annotation target =
    source-of-truth where deferral was originally pinned.

### Delta summary (Phase 21 → Phase 22)

| Metric | Phase 21 close (`a5ee9e1`) | Phase 22 close (HEAD `8227525`) | Δ |
|---|---|---|---|
| Commits | 5 | 4 + this gate | +5 |
| ADRs | **19** | **19** | **0** (1 amended with D-D Phase-22 implementation note; **third consecutive no-new-ADR phase** — first three-phase no-new-ADR streak; **fourth phase since Phase 3** to close without new ADR) |
| Migrations | 6 | **6** | **0** (**sixth consecutive 0-migration phase**) |
| Runtime deps | (Phase-21 stack) | (Phase-21 stack unchanged) | **0** |
| Env vars | 7 | 7 | **0** |
| Tests | 564 / 54 files | **575 / 55 files** | **+11 / +1** (**first phase since Phase 19 to add tests**) |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` + `/u/{handle}` bundles | 117 + 108 kB | 117 + 108 kB | 0 |
| OPEN_QUESTIONS resolved | 26 | **26** | **0** (**first three-phase Q-tally freeze** — Phase 20 + 21 + 22) |
| OPEN_QUESTIONS open | 29 | **29** | **0** |
| OPEN_QUESTIONS total | 59 | **59** | **0** |
| i18n keys per locale | 127 | 127 | 0 |
| `lib/rating-challenges/` files | 4 (+ 3 .test.ts) | **5 (+ 4 .test.ts)** | **+1 + 1 test** (`scaffold.ts` + `scaffold.test.ts`) |
| `scripts/` files | 8 | **9** | **+1** (`emit-challenge-action.ts`) |
| `package.json#scripts` entries | (+1: `cleanup-orphan-blobs`) | (+1: `emit-challenge-action`) | +1 |
| YAML-scaffolding surfaces | 0 | **1** | **+1** (first in project history) |
| Operational-script-keystone phases | 2 | **3** | **+1** (Phase 20 + 21 + 22 = **pattern crystallized**) |

### Surviving follow-ons

**Class B — ~10 items in 7 categories** (Phase-21 Class B carried
+ 4 Phase-22 scaffold operational follow-ons newly surfaced):

- **B.1 Image-processing expansion (4 carried unchanged)**:
  Q68 expansion; cropping UI; resizing; WebP/AVIF.
- **B.2 Phase-17 markdown subset expansion (8 carried unchanged)**.
- **B.3 Phase-18 surface expansion (3 carried + Q72)**.
- **B.4 Phase-20 backfill operational follow-ons (3 carried)**.
- **B.5 Phase-21 cleanup operational follow-ons (2 carried)**.
- **B.6 Phase-22 scaffold operational follow-ons (4 newly
  surfaced)**: prior-action auto-fill (~1 unit); signals_considered
  auto-gather (~1 unit; couples to prior-action); methodology-
  version dynamic read (~0.5 units); batch-emit-multiple
  (~0.5 units).
- **B.7 Operational + infrastructure (8; +1 from Phase 21)**:
  Q69 / Q54 / Q55 operational gates; CI dummy `AUTH_SECRET`;
  `pnpm db:migrate` doc; first LHCI run; middleware-based
  auth-route protection; `pnpm backfill-exif-strip` operational
  doc; `pnpm cleanup-orphan-blobs` operational doc;
  **`pnpm emit-challenge-action` operational doc newly surfaced**.

**Class C** — pre-existing carryovers unchanged.

### Phase-23+ Q-candidates flagged

**7 candidates carry** (was 8 at Phase 21 close; Q59-candidate
closed Phase 22 as ADR-0014 D-D realization without OQ-tally
affect):

- Q60 (curator authz evolution).
- Q61 (submitter anonymity).
- Q62 (rejection-rationale public visibility).
- Q64 (per-user privacy opt-out; ADR-0015 D-D).
- Q65 (per-curator activity feed; ADR-0015 D-E).
- Q68 expansion (content moderation; ADR-0019 D-F integration
  point + Phase-20 backfill + Phase-21 cleanup patterns
  reusable).
- Q72 (`reviewNotesSchema` divergence).

### Phase 23+ entry conditions

Per §12 cardinal rule. **Eighteenth "Continue" override
opportunity** in Unit 23.0 prep. Phase 23+ thread options (all
inferred-not-§13):

- **A. Q68 expansion content moderation** — ADR-0020 candidate
  for moderation API; ADR-0019 D-F inheritance contract; Phase-20
  backfill + Phase-21 cleanup patterns reusable; could be a
  **fourth consecutive operational-script-keystone phase** if
  scope tightens to moderation API integration + retroactive
  backfill scan. ~3-5 units.
- **B. Cropping UI** (per ADR-0019 D-F) — ~3-5 units; client dep.
- **C. Server-side resizing + WebP/AVIF** (per ADR-0019 D-F) —
  ~1-2 units; may be operational-script-keystone if scope =
  backfill existing avatars.
- **D. Q72 markdown subset extensions** — ~2-3 units.
- **E. Per-challenge detail page** — ~2-3 units.
- **F. Markdown rating-action `rationale`** — ~2-3 units;
  couples to (E).
- **G. Q64 per-user privacy opt-out** — ~3 units; breaks
  6-consecutive 0-migration streak.
- **H. Multi-provider OAuth expansion** — Phase-9 Class B item
  8 carried **12 phases**. **ADR-0020 candidate**. ~3-4 units.
  **Strongest architectural patience signal at right scope** —
  growing each cycle.
- **I. Subscriber-list email** — Phase-5 D-4 punt carried **16+
  phases**. ~6 units. Strongest overall patience signal.
- **K. Phase-22 scaffold operational follow-ons (B.6 items)** —
  4 items ~3 units combined; smallest reasonable scope. Would
  be a **fourth consecutive operational-script-keystone phase**
  if user picks. Prior-action auto-fill is the largest item.
- **L. Account-deletion blob cleanup** — Phase-16 carryover;
  conditional on Phase 23+ shipping account-delete path.
- **M. SiteHeader avatar pill** — Phase-14 Class B; UI minor.

**DB-migration trigger re-eval at Phase 23 kickoff**: trigger (a)
FIRED Unit 9.6; trigger (b) cold. Phase 23 migration count
depends on chosen thread.

### Risk surface at HEAD `8227525`

1. **Three operational gates** (Q54 / Q55 / Q69) pending curator.
2. **Scaffolder placeholders rely on curator review discipline**
   — the 4 OTHER dimensions are filled with placeholder
   grade/value/stars + TODO rationale. If curator commits the
   draft as-is without editing, `validate-content` + ADR-0005
   pre-commit hook still pass (placeholders satisfy schema
   shape). Mitigation: header comment + script stdout reminder
   + Phase-23+ prior-action-auto-fill enhancement (D-9 deferred
   to B.6).
3. **No content moderation on uploaded images** (Q68 expansion
   deferred).
4. **No live-preview in edit form** (Phase 17 + 18 deferral).
5. **Sharp cold-start cost** persists (Phase-19 carryover).
6. **Scaffolder live-tested only against placeholder fixtures**
   — vitest unit tests cover the pure function; live DB ↔ FS
   round-trip verified manually by curator on first invocation.

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token (Q69), NOT FR content backfill
(Q51 curator-track), NOT moderation / cropping / resizing /
WebP (Phase 23+), NOT live curator workflow run (curator
operational task; NOT a code-shipping unit), NOT destructive
cleanup of existing data. This unit is the **closure**, not the
**unblock**.

### Parallel-curator awareness

Phase 22 shipped as a **single-session phase** — primary shipped
all 5 units 22.0 → 22.4 in one coherent session. **Six
consecutive single-session phases** (Phase 17 + 18 + 19 + 20 +
21 + 22). Phase 16 remains the lone parallel-session phase.

### Three consecutive operational-script-keystone phases pattern

| Property | Phase 20 (backfill-exif-strip) | Phase 21 (cleanup-orphan-blobs) | Phase 22 (emit-challenge-action) |
|---|---|---|---|
| Units | 5 | 5 | 5 |
| Code units | 1 | 1 | 1 |
| New ADRs | 0 | 0 | 0 |
| New deps | 0 | 0 | 0 |
| New env vars | 0 | 0 | 0 |
| New vitest tests | 0 | 0 | **+11** |
| New migrations | 0 | 0 | 0 |
| New UI affordances | 0 | 0 | 0 |
| New i18n keys | 0 | 0 | 0 |
| New `scripts/` files | 1 | 1 | 1 |
| New `lib/` modules | 0 | 0 | **+1** |
| Closes carryover | ADR-0019 D-E | Phase-16 + Phase-20 (class-B) | ADR-0014 D-D |
| Carryover phase-count | 1 phase | 6 phases | **10 phases** |

**Pattern crystallized**. Phase-22 +11 tests + +1 `lib/` module
are scope-appropriate exceptions for pure-function-testable
surfaces, not departures from the pattern.

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/22.4-phase-22-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 22.4 entry.

### CHANGELOG entry shape

Unit 22.4 entry: declares Phase 22 closed (**thirteenth NON-§13
phase**; 5 units; 0 deferrals; 0 scope drift; eleventh
consecutive well-scoped phase); cites **ADR-0014 D-D
emit-challenge-action realization** via Unit 22.1 (**first
double-digit ADR-D-clause carryover closed** at 10 phases;
**first YAML-scaffolding surface**; **third consecutive
operational-script-keystone phase** — pattern crystallized;
**fourth phase since Phase 3 to close without new ADR** = **first
three-phase no-new-ADR streak**); 11 Phase-22 architectural
firsts; Phase 21 → 22 delta table (0 ADR Δ; 0 migration Δ; 0
runtime dep Δ; 0 env var Δ; +11 tests / +1 file = first phase
since Phase 19 to add tests; 0 bundle Δ; +1 `lib/` module + 1
test file; +1 `scripts/` file; +1 YAML-scaffolding surface; +1
operational-script-keystone phase counter); Class B follow-ons
(~10 items in 7 categories; **4 Phase-22 scaffold operational
newly surfaced**); 7 Phase-23+ Q-candidates (Q59-candidate
closed Phase 22 without OQ-tally affect); ADR-0020 candidate
slot open after **9-phase ADR cycle without multi-provider-OAuth
claim** (Phase-9 Class B item 8 carried **12 phases**); Phase
23+ thread options (strongest patience signals: Phase-9 Class B
item 8 = 12 phases multi-provider OAuth; Phase-5 D-4 punt = 16+
phases subscriber-list email); eighteenth "Continue" override
opportunity per §12; six consecutive single-session phases;
sixth consecutive 0-migration phase; three consecutive
operational-script-keystone phases pattern crystallized table.
