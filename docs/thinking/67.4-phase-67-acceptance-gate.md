# 67.4 — Phase 67 acceptance gate

Phase 67 closes at HEAD `794615f` (Unit 67.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-68-entry
conditions per §12.

## Phase 67 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 67.0 | Phase 67 prep (rank-1 render-time fallback for unresolved wikilinks via multi-className emit + isValidTarget? plugin-option + wikilinks-validated factory arm; D-1 framework decisions: multi-className emit + 3rd plugin-option-axis realization + build-time + render-time defense-in-depth) | docs | `fdddb66` |
| 67.1 | `ResolveWikilinksOptions.isValidTarget?: (slug, entityType?) => boolean` plugin-option (3rd plugin-option-axis realization) + plugin body multi-className emit (conditional `["wikilink", "wikilink-unresolved"]` when predicate returns false; backward-compat `["wikilink"]` when option absent/returns true) + `WikilinkExtensionRegistry` constructor extension (optional `isValidTarget?` field; tuple-form emit when either buildHref OR isValidTarget is set) + 11 NEW Phase-67 plugin-body / registry tests + ADR-0018 D-G APPEND-D-AY initial declaration | code+APPEND | `e371adb` |
| 67.2 | New `MARKDOWN_EXTENSIONS=wikilinks-validated` factory arm (11th single-value arm; first new arm since Phase 64 `tables-per-surface`; first 3-phase gap) + Velite-loaded `ValidWikilinkTargets` closure wrapping Phase-66 `isValidWikilinkTarget` helper + factory error message bumped to APPEND-D-AY + 4 NEW factory-dispatch tests + 6 NEW end-to-end integration tests | code | `bee834c` |
| 67.3 | Phase-67 hygiene + ADR-0018 APPEND-D-AY closure documentation (1-phase carryover; ties Phase-51 + Phase-59 fastest-closure record; first non-set-or-extend phase since Phase 60; 26-phase APPEND-deferral closure cadence — new record; 12 architectural firsts) | docs | `794615f` |
| 67.4 | **Phase 67 acceptance gate** (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-66 5-unit framework shape verbatim.

## First multi-className emit realization + first 2-realization for output-shape sub-pattern + 9th plugin-body axis realization + 3rd plugin-option-axis realization + first 3-realization for plugin-option axis + first state where a consumer has 6+ evolutions + first "build-time + render-time" defense-in-depth pattern + first state where Phase-66 validator helper has 2 consumers + first 3-phase gap between new-arm additions + first non-set-or-extend phase since Phase 60 + 26-phase APPEND-deferral closure cadence

- **Thread choice**: Phase-66 acceptance gate's Phase-67-entry table
  ranked **render-time fallback for unresolved wikilinks via multi-
  className emit** as rank 1 (would compose Phase-65 className with
  Phase-66 build-time validation; 2nd plugin-body output-shape
  realization + first multi-className emit realization). Phase 67
  shipped rank 1.
- **`ResolveWikilinksOptions.isValidTarget?`** plugin-option added
  (Unit 67.1). Mirrors Phase-62 `buildHref?` pattern verbatim —
  optional predicate with backward-compat default (Phase-65 single-
  class emit when absent or returns true; 2-class array emit when
  returns false).
- **Plugin body multi-className emit** (Unit 67.1): conditional
  `["wikilink", "wikilink-unresolved"]` vs `["wikilink"]` based on
  predicate. Multi-className emit composes with Phase-65 plugin-only
  emit pattern — the second class enters the post-sanitize tree
  under explicit framework intent and bypasses sanitize via
  APPEND-D-D plugin-order-after-default discipline.
- **`WikilinkExtensionRegistry` constructor extension** (Unit 67.1):
  optional `isValidTarget?` field alongside Phase-62 `buildHref?`.
  Tuple-form emit when EITHER option is set; bare-form preserved
  when BOTH undefined (Phase-62/65 byte-identical for the existing
  `MARKDOWN_EXTENSIONS=wikilinks` arm).
- **`MARKDOWN_EXTENSIONS=wikilinks-validated`** factory arm (Unit
  67.2): wires `isValidTarget` against `isValidWikilinkTarget(ref,
  validTargets)` from Phase-66 validator helper with module-init-
  time-lazy `ValidWikilinkTargets` set from `#site/content` Velite
  collections. 11th single-value arm; first new arm since Phase 64
  `tables-per-surface`; first 3-phase gap between new-arm additions.
- **`getValidWikilinkTargets()`** module-level lazy assembly +
  `__resetValidWikilinkTargetsCacheForTests()` test-only reset.
  Mirrors the `lib/content/cross-link-audit.ts` set assembly
  verbatim — drift-free between build-time + render-time consumers
  of the Phase-66 validator helper.
- **Factory error message** bumped to include `wikilinks-validated`
  in the recognized-values list AND reference `APPEND-D-AY` (was
  `APPEND-D-AV` at Phase 64). Stale Phase-64 D-AV regression-guard
  test forward-updated to D-AY.
- **11 NEW plugin-body / registry tests in `wikilinks.test.ts`**
  (Unit 67.1) + **4 NEW factory-dispatch tests in `lib/markdown/
  extensions/index.test.ts`** (Unit 67.2) + **6 NEW end-to-end
  integration tests in `lib/markdown/index.test.ts`** (Unit 67.2)
  = **21 NEW Phase-67 tests**.
- ADR-0018 D-G APPEND adds Phase-67 EXTENDED block + **APPEND-D-AY**
  (Unit 67.1; twenty-fifth two-letter slot).
- **Closes Phase-66 APPEND-D-AX render-time-fallback item at 1-phase
  carryover** (Phase 66 → 67). **Ties Phase-51 pubmed alias + Phase-
  59 bioRxiv cross-surface 1-phase fastest-closure record**. **First
  sub-2-phase closure since Phase 60**. **First single-phase-
  carryover closure of a deferral introduced in the immediately-
  prior phase's APPEND** in project history — establishes the
  "fresh-deferral fast-closure" sub-pattern within the APPEND-
  deferral closure cadence (prior 1-phase closures Phase 51 + 59
  were of older deferrals).
- **First non-set-or-extend phase since Phase 60** in project
  history. Phase 67's 1-phase carryover does NOT set, extend, OR
  tie the absolute-record carryover (which remains at 28-phase
  from Phase 66). Phase 67 is a "fresh-deferral fast-closure"
  phase shape. **Ends the 6-consecutive-phase set-or-extend
  streak** (Phase 61-66). The streak shifts to **6-phases-then-
  break** pattern.
- **First multi-className emit realization** in project history.
  Phase 65 single-class; Phase 67 first 2-class array conditionally.
- **Second plugin-body output-shape sub-pattern realization** in
  project history (extends Phase-65 sub-pattern to 2 realizations;
  **first 2-realization for output-shape sub-pattern**).
- **9th plugin-body axis realization** in project history (extends
  Phase-65 8-realization record to 9; **first 9-realization for
  plugin-body axis**).
- **Third plugin-option-axis realization** in project history
  (extends Phase-62/63 2-realization streak). **First 3-realization
  for plugin-option axis** + **first state where the plugin-option
  axis has 3+ realizations** in project history.
- **Wikilinks consumer gains 6th evolution post-first-ship** (Phase
  42 + 46 + 62 + 63 + 65 + 67). **First state where a consumer has
  6+ evolutions** in project history. Wikilinks remains the
  deepest-evolved consumer (first to 4 + first to 5 + first to 6).
- **First "build-time + render-time" defense-in-depth pattern** in
  project history. Phase 66 ships build-time validation; Phase 67
  ships render-time fallback. The Phase-66 validator helper is
  consumed at BOTH layers.
- **First state where the Phase-66 validator helper has 2
  consumers**: audit script (Phase 66) + factory arm closure
  (Phase 67); drift-free — both assemble `ValidWikilinkTargets`
  from the same 4 Velite collections (problems + papers + authors
  + institutions) with identical field-mappings.
- **First 3-phase gap between new-`MARKDOWN_EXTENSIONS`-arm
  additions** in project history (Phase 64 `tables-per-surface` →
  Phase 67 `wikilinks-validated`; Phases 65 + 66 added zero arms).
- **26-phase APPEND-deferral closure cadence sustained** (Phase
  42-67) — new longest sustained cadence in project history
  (extends Phase-66 25-phase record to 26). First 26-phase
  APPEND-deferral closure run.
- **`MARKDOWN_EXTENSIONS` single-value arms 10 → 11** — first new
  arm since Phase 64; 11th total arm. Curators opt into render-
  time fallback explicitly via env-var; the Phase-65 `wikilinks`
  baseline preserved on the default arm (no fallback emission).
- **34th APPEND on D-G** + **twenty-fifth two-letter slot D-AY**.
- **Fifty-eighth NON-§13 phase**; first 58-phase ledger-closure
  streak.
- **32nd consecutive no-new-ADR phase** (Phase 36-67).
- **37th consecutive phase without new B category** (Phase 31-67).
- **13-consecutive-phase post-half-century-Continue-override
  streak** (Phases 55-67); **62nd Continue override**; **12th past
  half-century threshold**.

## Phase 66 → Phase 67 delta

| Metric | Phase 66 close | Phase 67 close | Δ |
|---|---|---|---|
| Tests | 1552 / 76 files | **1573 / 76 files** | +21 / 0 (11 plugin-body + 4 factory-dispatch + 6 end-to-end integration) |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 33 | **34** (project record extends; twenty-fifth two-letter slot D-AY in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| **`MARKDOWN_EXTENSIONS` single-value arms** | **10** | **11 (+`wikilinks-validated`)** | **+1 (first new arm since Phase 64 `tables-per-surface`; first 3-phase gap between new-arm additions)** |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 22 | **22** | 0 (modifications only; no new file) |
| `AuditCheck` type count | 8 | **8** | 0 (Phase 67 doesn't add new audit type; reuses Phase-66 validator helper) |
| Phase-37-framework concrete consumers | 7 | **7** | 0 |
| Plugin-regex-extension realizations | 7 | **7** | 0 |
| **Plugin-body axis realizations** | **8** | **9 (+wikilinks render-time fallback multi-className emit)** | **+1 (9th plugin-body axis realization; first 9-realization)** |
| **Plugin-body output-shape sub-pattern realizations** | **1 (Phase 65)** | **2 (+Phase 67)** | **+1 (first 2-realization for output-shape sub-pattern)** |
| Plugin-body input-regex sub-pattern realizations | 7 | **7** | 0 |
| Constructor-arg-only zero-rework expansion realizations | 7 | **7** | 0 |
| Schema-extension realizations | 2 | **2** | 0 |
| **Plugin-option-axis realizations** | **2** | **3 (+wikilinks isValidTarget?)** | **+1 (first 3-realization for plugin-option axis; first state where plugin-option-axis has 3+ realizations)** |
| Schema-options-axis realizations | 1 | **1** | 0 |
| Build-time-validation realizations | 1 | **1** | 0 (validator helper gains 2nd CONSUMER but build-time-validation axis stays at 1 realization) |
| **Principal axes of zero-rework framework extension** | 4 | **4** | 0 |
| **Consumers with 3+ evolutions** | 3 (arxiv + tables + wikilinks) | **3** | 0 |
| **Consumers with 4+ evolutions** | 2 (wikilinks + tables) | **2** | 0 |
| **Consumers with 5+ evolutions** | 1 (wikilinks) | **1** | 0 (wikilinks evolution count moves from 5 → 6) |
| **Consumers with 6+ evolutions** | **0** | **1 (wikilinks)** | **+1 (first state where a consumer has 6+ evolutions)** |
| **D-clauses with BOTH items closed** | 4 | **4** | 0 |
| **D-clauses with 3+ items closed** | 2 (D-Q + D-L) | **2** | 0 |
| **D-clauses with 4+ items closed** | 2 (D-L + D-Q) | **2** | 0 |
| **D-clauses with 5+ items closed** | 1 (D-L) | **1** | 0 |
| **D-clauses with 6+ items closed** | 1 (D-L) | **1** | 0 |
| **D-clauses with ALL enumerated items closed** | 2 (D-Q + D-L) | **2** | 0 |
| **APPEND-deferral closure cadence (consecutive phases)** | 25 | **26 (Phase 42-67)** | **+1 (new record)** |
| **Longest absolute APPEND-deferral closure carryover** | 28-phase (Phase 66 D-L item 5) | **28-phase UNCHANGED** | 0 (Phase 67 1-phase fast-closure does not extend the absolute record) |
| **Set-or-tie/extend streak of absolute-record carryovers** | 6 (Phase 61-66) | **6 ENDED at Phase 66; Phase 67 is fresh-deferral fast-closure** | 0 (streak broken; new pattern begins) |
| **First sub-2-phase closure since Phase 60** | — | **YES (Phase 67 = 1-phase carryover)** | NEW |
| **First non-set-or-extend phase since Phase 60** | — | **YES (Phase 67)** | NEW |
| **Multi-className emit realizations** | **0** | **1 (Phase 67 wikilinks-unresolved)** | **+1 (first multi-className emit realization)** |
| **"Build-time + render-time" defense-in-depth patterns** | **0** | **1 (Phase 67 wikilinks)** | **+1 (first build-time + render-time defense-in-depth pattern)** |
| **Phase-66 validator helper consumer count** | 1 (audit script) | **2 (+factory arm closure)** | **+1 (first state where Phase-66 validator helper has 2 consumers)** |
| **Refactor-only phases** | 1 (Phase 62) | **1** | 0 |
| **Plugin-only emit pattern realizations** | 1 (Phase 65) | **1 (extended via multi-className composition; same plugin)** | 0 |
| **Non-regex plugin-body realizations** | 1 (Phase 65) | **2 (Phase 65 + 67)** | **+1 (extends output-shape sub-pattern)** |
| **Plugin-option-ready-before-consumer-demand realizations** | 1 (Phase 62) | **1** | 0 |
| **Schema-options-ready-before-curator-demand realizations** | 1 (Phase 64) | **1** | 0 |
| **Forward-compat-affordance prerequisite-fulfillment arcs completed** | 1 (Phase 62 → 63) | **1** | 0 (Phase 67 ships affordance + consumer in the same phase; no separate-phase arc) |
| **NON-§13 phase count** | 57 | **58** | **+1** |
| **"Continue" override invocations past half-century threshold** | 11 (+Phase 66 = 61st) | **12 (+Phase 67 = 62nd)** | **+1 (13-consecutive-phase post-half-century-Continue streak Phases 55-67)** |
| **No-new-ADR consecutive-phase streak** | 31 (Phase 36-66) | **32 (Phase 36-67)** | **+1** |
| **No-new-B-category consecutive-phase streak** | 36 (Phase 31-66) | **37 (Phase 31-67)** | **+1** |

## Architectural firsts in Phase 67 (12 enumerated)

1. **First multi-className emit realization** in project history.
   Phase 65 emitted single-class `["wikilink"]`. Phase 67 conditionally
   emits the 2-class array `["wikilink", "wikilink-unresolved"]` based
   on the `isValidTarget?` plugin-option. Establishes "multi-className
   emit" as a recognized framework technique for plugin-body output-
   shape extensions composing multiple classes via conditional emission.
2. **Second plugin-body output-shape sub-pattern realization** in
   project history (extends Phase-65 sub-pattern to 2 realizations).
   **First 2-realization for output-shape sub-pattern**. Plugin-body
   axis sub-pattern depth ranks post-Phase-67:
   - Input-regex sub-pattern: 7 realizations
   - **Output-shape sub-pattern: 2 realizations**
3. **9th plugin-body axis realization** in project history (extends
   Phase-65 8-realization record to 9; **first 9-realization for
   plugin-body axis**). Plugin-body axis remains the most-realized
   principal axis post-Phase-67.
4. **Third plugin-option-axis realization** in project history
   (extends Phase-62/63 2-realization streak). **First 3-realization
   for plugin-option axis** + **first state where the plugin-option
   axis has 3+ realizations** in project history. Axis depth ranking
   post-Phase-67:
   - Plugin-body: 9 realizations
   - Registry-state: 7 realizations
   - **Plugin-option: 3 realizations**
   - Schema-options: 1 realization
5. **Wikilinks consumer gains 6th evolution post-first-ship** (Phase
   42 + 46 + 62 + 63 + 65 + 67). **First state where a consumer has
   6+ evolutions** in project history.
6. **First "build-time + render-time" defense-in-depth pattern** in
   project history. Phase 66 ships build-time validation
   (`pnpm audit-content` fails CI on unresolved wikilinks); Phase 67
   ships render-time fallback (browsers show visually-distinct class
   when build-time was bypassed).
7. **First state where the Phase-66 validator helper has 2 consumers**
   in project history. Phase 66 ship: audit script
   (`lib/content/cross-link-audit.ts`). Phase 67 ship: factory arm
   closure (`lib/markdown/extensions/index.ts`). Drift-free: both
   assemble `ValidWikilinkTargets` from the same 4 Velite collections.
8. **First 3-phase gap between new-`MARKDOWN_EXTENSIONS`-arm additions**
   in project history. Phase 64 `tables-per-surface` was the previous
   arm; Phases 65 + 66 added zero arms; Phase 67 ships `wikilinks-
   validated` = 3-phase gap.
9. **Ties Phase-51 pubmed alias + Phase-59 bioRxiv cross-surface
   1-phase fastest-closure record**. Phase 67 closes Phase-66
   APPEND-D-AX render-time-fallback item at 1-phase carryover. **First
   single-phase-carryover closure of a deferral introduced in the
   immediately-prior phase's APPEND** in project history — establishes
   the "fresh-deferral fast-closure" sub-pattern within the APPEND-
   deferral closure cadence (prior 1-phase closures Phase 51 + 59 were
   of older deferrals).
10. **First non-set-or-extend phase since Phase 60** in project
    history. Phase 67's 1-phase carryover does NOT set, extend, OR
    tie the absolute-record carryover (which remains at 28-phase from
    Phase 66). Phase 67 is a "fresh-deferral fast-closure" phase
    shape. **Ends the 6-consecutive-phase set-or-extend streak**
    (Phase 61-66). Streak shifts to **6-phases-then-break** pattern.
11. **26-phase APPEND-deferral closure cadence sustained** (Phase
    42-67) — new longest sustained cadence in project history
    (extends Phase-66 25-phase record to 26). First 26-phase APPEND-
    deferral closure run.
12. **34th APPEND on ADR-0018 D-G** — extends the first-ADR-D-clause-
    with-most-APPENDs record from 33 → 34. **Twenty-fifth two-letter
    APPEND letter D-AY**. **Fifty-eighth NON-§13 phase** (extends
    Phase-66 milestone to 58); first 58-phase ledger-closure streak.
    **32nd consecutive no-new-ADR phase** (Phase 36-67). **37th
    consecutive phase without new B category** (Phase 31-67). **62nd
    "Continue" override invocation** — **twelfth invocation past the
    half-century threshold**; first **13-consecutive-phase post-half-
    century-Continue-override streak** (Phases 55-67). **192nd
    consecutive 103 kB First Load JS unit** (extends from 187 at
    Phase-66 close).

## Phase 67 → Phase 68 entry conditions

Per §12, Phase 68 entry requires **explicit human sign-off**.

**Framework state note**: Phase 67 introduced multi-className emit as
the second realization of the plugin-body output-shape sub-pattern;
the plugin-body axis now has 9 realizations (most-realized principal
axis). The plugin-option axis now has 3 realizations (first 3-
realization for plugin-option axis). The wikilinks consumer has 6
evolutions (first 6+-evolution consumer). The Phase-66 validator
helper has 2 consumers (audit script + render-time factory arm). The
"build-time + render-time defense-in-depth" pattern is established for
wikilinks. `MARKDOWN_EXTENSIONS` has 11 single-value arms.

**Recommended Phase 68 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| **1** | **2nd build-time-validation realization** — adds a second `AuditCheck` type that uses build-time validation (e.g., `markdown-link-target-fk` for non-wikilink markdown links pointing to local paths). **WOULD ELEVATE "build-time-validation" from Phase-66 sub-pattern to recognized PRINCIPAL AXIS** — first 5th principal axis in project history. **First state where the framework has 5 principal axes of zero-rework extension**. 2-3 units. | 2-3 | Autonomous-tractable; small-scope; principal-axis-introduction milestone |
| 2 | **Period-allowing slug character-class extension** — would let regex + validator + render-time fallback all match paper IDs like `[[paper:2109.07958]]`; plugin-body-axis realization lifting all three simultaneously; **first plugin-body extension that lifts validator + render-time fallback too** | 1-2 | Autonomous-tractable; XSS audit per attribute injection vector via dotted slugs |
| 3 | **Locale-prefixed cross-entity wikilink paths** — concrete `buildHref` variant producing `/{locale}/papers/${slug}` etc; **fourth plugin-option-axis realization** (if framed as separate buildHref variant) OR a new factory arm | 1-2 | Autonomous-tractable; small-scope follow-on |
| 4 | **Curator-configurable className value** via plugin-option axis (`ResolveWikilinksOptions.className?: string[]`) — would generalize Phase-67 hardcoded `["wikilink", "wikilink-unresolved"]` to a configurable array; **fourth plugin-option-axis realization** | 1-2 | Autonomous-tractable; XSS audit per CSS-class-injection vector |
| 5 | **Multi-className emit for entity-type-specific styling variants** (e.g., `<a class="wikilink wikilink-paper">`) — third plugin-body output-shape realization composing with Phase-67 multi-className pattern | 1-2 | Autonomous-tractable; depends on rank 4 decision |
| 6 | **Per-surface schema-options consumer beyond bio-restricted** — second schema-options-axis realization (extends Phase-64 axis to 2 realizations; first 2-realization for the FOURTH axis) | 1-2 | Autonomous-tractable |
| 7 | **Empty-alias fallback unification** across consumers (Phase-48 deferral) | 1 | Smallest-scope autonomous-tractable |
| 8 | **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 9 | **Legacy numeric-only bioRxiv IDs** (pre-2019 format) — 8th plugin-regex-extension | 1-2 | Autonomous-tractable; small-scope |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Most defensible autonomous-tractable next thread**: Rank 1 (**2nd
build-time-validation realization** — would elevate the Phase-66 sub-
pattern to a recognized PRINCIPAL AXIS; **first 5th principal axis in
project history**). Establishes the recurrence pattern across the
audit layer. Advances the framework taxonomy in a single phase.
**Alternative compelling thread**: Rank 4 (curator-configurable
className value) — would generalize Phase-67 hardcoded array; mirrors
Phase-62 buildHref pattern but on the className slot.

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 + Q69 +
Q73 + Q75 + Q77.

## Smoke gates

- `pnpm test` — **1573 / 76 files** (+21 / 0 vs Phase 66 close
  baseline 1552/76).
- `pnpm audit-content` — 0 errors / 6 warnings UNCHANGED + 0
  dangling wikilink refs (Phase-66 8th `AuditCheck` type still
  passes on production content; Phase 67 doesn't change audit
  behavior).
- First Load JS — **103 kB UNCHANGED** (192 consecutive units at
  Unit 67.4 ship; 187 at Phase 66 close + 4 Phase 67 units +
  this gate unit; **192-consecutive-unit streak**).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 68+ deferrals)

NOT 2nd build-time-validation realization (Phase 68+ rank 1; would
elevate to recognized PRINCIPAL AXIS — first 5th principal axis);
NOT period-allowing slug character-class extension (Phase 68+ rank 2);
NOT locale-prefixed cross-entity wikilink paths (Phase 68+ rank 3);
NOT curator-configurable className value via plugin-option axis
(Phase 68+ rank 4);
NOT multi-className emit for entity-type-specific styling variants
(Phase 68+ rank 5; third plugin-body output-shape realization);
NOT per-surface schema-options consumer beyond bio-restricted (Phase
68+ rank 6);
NOT empty-alias fallback unification (Phase 68+ rank 7);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix
(Phase 68+ rank 8);
NOT legacy numeric-only bioRxiv IDs (Phase 68+ rank 9);
NOT 2nd schema-options realization on a different consumer than
tables (Phase 68+);
NOT dx.doi.org legacy host (Phase 68+);
NOT stricter trailing-lookahead (Phase 68+);
NOT paper-card hover-preview (Phase 68+);
NOT auto-trim of alias display whitespace (Phase 68+);
NOT a future plugin that EMITS `<caption>` or `colSpan`/`rowSpan`/
`scope` (Phase 68+);
NOT `@mention` (Phase 68+);
NOT OSF preprint consumer (Phase 68+);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi +
pubmed + orcid + biorxiv (Phase 68+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 68+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 68+;
requires framework refactor);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 68+);
NOT cross-entity entity-type expansion beyond paper / author /
institution (Phase 68+);
NOT curator-facing builder configuration via DB or UI (Phase 68+);
NOT 2nd plugin using the plugin-only emit pattern (Phase 68+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck / safeAuth /
test backfill / account-deletion (each deferred);
all Phase-30-through-66 deferrals carry forward.

## Boundary statement

**Phase 67 CLOSED at HEAD `<this-unit>` after 5 numbered units (67.0
prep + 67.1 `ResolveWikilinksOptions.isValidTarget?` plugin-option +
`rehypeResolveWikilinks` plugin body multi-className emit
(`["wikilink", "wikilink-unresolved"]` when predicate returns false;
backward-compat `["wikilink"]` when option absent/returns true) +
`WikilinkExtensionRegistry` constructor extension + 11 NEW Phase-67
plugin-body / registry tests + APPEND-D-AY + 67.2 new
`MARKDOWN_EXTENSIONS=wikilinks-validated` factory arm + Velite-loaded
`ValidWikilinkTargets` closure wrapping Phase-66 validator helper +
factory error message bumped + 4 NEW factory-dispatch tests + 6 NEW
end-to-end integration tests + 67.3 hygiene + 67.4 gate). **First
multi-className emit realization** in project history. **Second
plugin-body output-shape sub-pattern realization** (extends Phase-65
sub-pattern to 2 realizations; first 2-realization for output-shape
sub-pattern). **9th plugin-body axis realization** (extends Phase-65
8-realization record to 9; first 9-realization for plugin-body axis).
**Third plugin-option-axis realization** (extends Phase-62/63 2-
realization streak; first 3-realization for plugin-option axis; first
state where plugin-option-axis has 3+ realizations). **Wikilinks
consumer gains 6th evolution post-first-ship** (Phase 42 + 46 + 62 +
63 + 65 + 67); **first state where a consumer has 6+ evolutions** in
project history. **First "build-time + render-time" defense-in-depth
pattern** in project history. **First state where the Phase-66
validator helper has 2 consumers** (audit script + factory arm
closure; drift-free). **Closes Phase-66 ADR-0018 APPEND-D-AX render-
time-fallback item at 1-phase carryover** (Phase 66 → 67) — **ties
Phase-51 pubmed alias + Phase-59 bioRxiv cross-surface 1-phase fastest-
closure record**. **First sub-2-phase closure since Phase 60**.
**First single-phase-carryover closure of a deferral introduced in the
immediately-prior phase's APPEND** in project history. **First non-
set-or-extend phase since Phase 60** — ends the 6-consecutive-phase
set-or-extend streak (Phase 61-66); the absolute-record carryover
remains at 28-phase from Phase 66. **First 3-phase gap between new-
`MARKDOWN_EXTENSIONS`-arm additions** in project history (Phase 64 →
67). **26-phase APPEND-deferral closure cadence sustained** — new
longest sustained cadence (extends Phase-66 record 25 → 26); first
26-phase APPEND-deferral closure run. **`MARKDOWN_EXTENSIONS` single-
value arms 10 → 11** (+`wikilinks-validated`); first new arm since
Phase 64 `tables-per-surface`. Plugin body conditional emit composes
Phase-65 plugin-only emit pattern with Phase-67 multi-className emit
pattern; XSS-safety contract preserved (both className strings
hardcoded literals; no curator input; no slug/entityType
interpolation; no new XSS surface). Existing `wikilinks` +
`wikilinks-cross-entity` arms emit single-class per Phase-65 byte-
identically (only the new `wikilinks-validated` arm activates the
predicate). 34th APPEND on D-G (record extends 33 → 34). Twenty-fifth
two-letter APPEND letter D-AY. **Fifty-eighth NON-§13 phase** (extends
Phase-66 milestone to 58); first 58-phase ledger-closure streak. 37th
consecutive phase without new B category — first 37-phase run
(extends Phase-66 record 36 → 37). 32nd consecutive no-new-ADR phase
(Phase 36-67; extends Phase-66 record 31 → 32; first 32-phase no-new-
ADR streak). 192nd consecutive 103 kB First Load JS unit (extends
from 187 at Phase-66 close). 62nd "Continue" override invocation —
twelfth invocation past the half-century threshold (Phase 55 = 50th;
…; Phase 67 = 62nd); **first 13-consecutive-phase post-half-century-
Continue-override streak** (Phases 55-67). 1573 tests across 76
vitest files (+21 / 0 vs Phase 66 close baseline 1552/76). 24 ADRs
UNCHANGED. 7 DB tables UNCHANGED. 9 migrations UNCHANGED. 14 env vars
UNCHANGED. 168 i18n keys per locale UNCHANGED. First Load JS 103 kB
UNCHANGED. Middleware 160 kB UNCHANGED. OPEN_QUESTIONS top-level Q-
count 66 UNCHANGED. Phase-37+ candidate count 8 UNCHANGED. 0 new
files. 5 modified files (`wikilinks.ts` + `wikilinks.test.ts` +
`extensions/index.ts` + `extensions/index.test.ts` + `index.test.ts`)
+ 1 ADR APPEND. Zero new ADRs / migrations / i18n keys / operational
gates / runtime deps. Phase 68 entry requires explicit human sign-off
per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 66 → 67 delta complete.
- [x] Phase 68 entry conditions documented (10 ranked candidates).
- [x] Framework-state-note explicitly flagged (Phase 67 introduced
      multi-className emit as 2nd output-shape sub-pattern realization;
      plugin-body axis at 9 realizations; plugin-option axis at 3
      realizations; first 6+-evolution consumer; build-time + render-
      time defense-in-depth pattern established; Phase-66 validator
      helper has 2 consumers; 11 MARKDOWN_EXTENSIONS arms).
- [x] Smoke gates pass.
- [x] STOP at Phase 68 boundary per §12.
