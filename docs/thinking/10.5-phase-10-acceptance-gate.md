# 10.5 — Phase 10 acceptance gate (Profile page + Phase-9 UI polish — first NON-§13 phase)

Phase-10 closing unit. Mirrors Units 1.12 / 2.13 / 3.13 / 4.13 / 5.13 /
6.10 / 7.11 / 8.9 / 9.9. Verifies every Phase-10 deliverable (the
**Profile page + Phase-9 UI polish** thread per Unit 10.0 D-1) is
operational locally at HEAD, emits the cross-phase roll-up, and lists
Phase-10 follow-ons that survive into Phase 11+.

**This is the first acceptance gate of a NON-§13 phase**, marking
the transition from §13-driven thread closure to follow-on-catalog-
driven thread selection. The §13 ledger closed at Unit 9.9; Phase 10
pulled from the Phase-9 Class B catalog (Unit 9.7 item 1 — profile
page).

## THINK

### Phase-10 thread scope

Per Unit 10.0 D-1: "Profile page + Phase-9 UI polish" —
**consolidation thread**. Lights up the auth-thread's unbuilt UI
surface (signed-in users had nowhere to see their state before this
phase) before pulling a bigger second-write-path expansion.

Tradeoff flagged in Unit 10.0: this is consolidation-first, NOT the
explicit Phase-9 Unit 9.0 D-5 deferral (rating-challenge submission;
~6-8 units). Rating-challenge stays deferred to Phase 11+.

### Phase-10 deliverable — local pass status

| Surface                                                                                  | Phase-10 unit | Status |
|------------------------------------------------------------------------------------------|---------------|--------|
| `lib/watchlist/` extension: `getWatchedSlugs(userId)` helper (Drizzle SELECT + ORDER BY createdAt DESC + LIMIT 50) | Unit 10.1    | ✓ Thin Drizzle wrapper; test deferred per Unit 10.1 CHANGELOG entry (Drizzle types prove SELECT shape). `WATCHLIST_LIMIT = 50` constant introduced for future-proofing. |
| `app/[locale]/profile/page.tsx` — **first protected route**                              | Unit 10.2    | ✓ Server-component check + redirect to `/api/auth/signin/github?callbackUrl=...` if no session. `dynamic = "force-dynamic"` overrides parent layout's SSG. |
| Profile page header (avatar + display name + GitHub login pill + sign-out)               | Unit 10.2    | ✓ Bare `<img>` for avatar (Unit 10.0 D-10 lean); display-name chain (`name → githubLogin → email → fallback`); separate Drizzle query for `users.githubLogin` (not in default Auth.js session shape); inline server-action sign-out form. |
| Profile page watchlist section (dense list reusing `<WatchlistToggle>`; empty-state CTA) | Unit 10.2    | ✓ One row per watched problem with linked title + domain/subdomain mono breadcrumb + `<StatusPill>` + reused `<WatchlistToggle>` (renders Watching ★ branch); dashed empty-state card with "Browse problems →" link. |
| `messages.profile.*` namespace (EN + FR; 6 keys each)                                   | Unit 10.2    | ✓ FR: "Connecté" / "Se déconnecter" / "Suivis" / "Problèmes que vous suivez" / "Vous ne suivez aucun problème pour l'instant." / "Parcourir les problèmes →". |
| Phase-10 hygiene status pass (Class A in-flight + Class B follow-ons + Class C carryovers) | Unit 10.3    | ✓ 2 Class A (profile-page Playwright smoke + LHCI enrolment for profile URLs) + 8 Class B + 14 Class C. |
| OPEN_QUESTIONS hygiene + ADR review (zero net surface)                                   | Unit 10.4    | ✓ 0 new Qs surfaced; 0 promotions; 0 status changes; 0 new ADRs; 0 prose edits; **first zero-architectural-surface phase** in the project's history. |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: 19 URLs enrolled
  in `lighthouserc.json` **UNCHANGED** through Phase 10 (no new
  locale-pilot surfaces in Phase 10; profile-page enrolment deferred
  as Class A item 2 — requires either signed-out 3xx path or
  CI-provisioned mock session).
- **W3C feed validator on `/api/v1/rss.xml` + `/api/v1/digest/<slug>`**:
  Phase-3/5/6/8/9/10 compound deferral; first preview deploy + Q47
  unblock.
- **Visual-regression baselines** for `/en/profile` + `/fr/profile`
  — Phase-10 deferred follow-on (Class B item 5; no Playwright spec
  changes this phase).
- **No auto-merge** (ADR-0009): Phase 10 added no LLM-translated
  content.
- **File-first / no DB held for CONTENT** per ADR-0004 + ADR-0013 D-F.
  Profile page exercises both surfaces cleanly: file-first `problems`
  from `#site/content` for problem metadata + USER-STATE DB
  (`watchlist` table read by `getWatchedSlugs`; `users.githubLogin`
  read for the GitHub-login pill).
- **First Load JS shared chunk = 103 kB**: held through every Phase-10
  unit. Profile page is server-rendered; sign-out form is
  server-action; `<WatchlistToggle>` reuse is server-rendered. Zero
  client-bundle delta.
- **Middleware bundle = 159 kB UNCHANGED**: route protection landed at
  the page layer in Phase 10, not the middleware layer.

### Phase-10 unit summary (6 commits — Profile page + Phase-9 UI polish)

| Unit | Commit    | Title                                                                                |
| ---- | --------- | ------------------------------------------------------------------------------------ |
| 10.0 | `d7745ce` | Phase 10 prep (Profile page + Phase-9 UI polish; first NON-§13 phase)                |
| 10.1 | `f36c4c5` | `lib/watchlist/` extension: `getWatchedSlugs` helper                                 |
| 10.2 | `6ea7a4f` | **Profile page** (`/[locale]/profile`; first protected route)                         |
| 10.3 | `35152b0` | Phase-10 hygiene status pass (Class A / B / C catalog)                                |
| 10.4 | `63ed3aa` | OPEN_QUESTIONS hygiene + ADR review (zero net surface)                                |
| 10.5 | _this_    | Phase 10 acceptance gate                                                              |

Total: **6 numbered units, all shipped**. No deferrals (matches Phase
9's 10/10 discipline; contrast with Phase 8's Unit 8.4 deferral).
Matches Unit 10.0 prep's 6-unit breakdown verbatim.

### State at HEAD (Unit 10.5)

- **Content** (no change vs Phase 9 close):
  - 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors / 14 institutions / 20 rating actions / 4 issue templates / methodology v1 (EN + FR) / contributing v1.0 (EN) + v1.1 (EN + FR) / 2 `entries.json` files.
  - **203 schema-validated content files + 36 raw MDX = 239 raw content files**. Both unchanged from Phase 9 close.
- **New Phase-10 surfaces** (relative to Phase 9 close at `9f8ff19`):
  - **`/[locale]/profile` route**: `app/[locale]/profile/page.tsx` — first protected route in the project.
  - **`lib/watchlist/` extension**: `getWatchedSlugs(userId)` helper + `WATCHLIST_LIMIT` constant.
  - **`messages/{en,fr}.json`**: `profile.*` namespace (6 keys per locale).
  - That's it. No new components; no new lib directories; no new ADRs; no new dependencies; no new migrations; no new env vars; no new tests; no new gitignore patterns.
- **Routes**: ~590 prerendered pages + **2 new SSG entries** (`/en/profile` + `/fr/profile`; rendered request-time via `dynamic = "force-dynamic"`) + 2 dynamic API routes from Phase 9 unchanged. **Page-route count nominally +1** (`[locale]/profile` shape).
- **Tests**: **394/394 across 45 vitest files UNCHANGED** through Phase 10. No test files added (Unit 10.1 helper test deferred per Drizzle-type-system rationale; Unit 10.2 Playwright smoke deferred per Class A item 1).
- **First Load JS shared chunk**: **103 kB UNCHANGED** through every Phase-10 unit.
- **Middleware bundle**: **159 kB UNCHANGED**. Route protection landed at page layer, not middleware.
- `pnpm validate-content` → **203 files** green (unchanged).
- `pnpm audit-content` → **0 errors / 6 warnings** (Q32-expected baseline since Phase 2; unchanged).
- `pnpm typecheck` clean. `pnpm build` clean compile (~4-6s; per-unit variance).
- **5 visualizations live** (unchanged from Phase 4).
- **13 ADRs** (unchanged from Phase 9; Phase 10 added zero new ADRs).
- **0 new dependencies in Phase 10** (was +5 in Phase 9).
- **`lighthouserc.json`** enrols **19 URLs** UNCHANGED.
- **OPEN_QUESTIONS state**: 19 resolved + 4 decided-as-lean + 28 open = **51 entries UNCHANGED from Phase 9 close**.

### Phase-10 follow-ons that survive the gate (non-blocking)

Per Unit 10.3 hygiene catalog:

**Class A (2 — in-flight Phase-10 items)**:
1. **Profile-page Playwright smoke test** — deferred from Unit 10.2.
2. **LHCI enrolment for `/en/profile` + `/fr/profile`** — deferred
   until first observed LHCI run motivates.

**Class B (8 — Phase-10-specific follow-ons)**: see Unit 10.3. Public
profile rendering at `/[locale]/u/[handle]`; user-editable fields;
per-user statistics surface; auth-aware middleware-based route
protection (Phase-9 Class B item 12 **PARTIALLY RESOLVED in Phase 10**
— server-component-level protection landed; middleware lift deferred
until 2+ protected routes exist); profile-page styling polish;
per-user discussion-activity surface; profile photo upload;
redundant `isWatched()` query inside reused `<WatchlistToggle>` on
profile.

**Class C (14 — carryovers from Phase 9 + earlier)**: see Unit 10.3.
Q54 / Q55 / CI `AUTH_SECRET` / `pnpm db:migrate` doc (all Phase-9
Class A operational); rating-challenge submission write-path; email
notifications; watchlist count on `/problems`; bulk-import/clear;
orphan-row cleanup; rate-limiting; multi-provider OAuth (ADR-0012
D-B forbidden); createUser vs linkAccount docs; first LHCI run
validating Phase-9; OAuth callback URL stability; HTML shell
migration **STILL ON HOLD**; chrome strings + FR backfill; sitemap
hints; trailing-slash normalization; etc.

Documented in detail in Unit 10.3's hygiene status pass.

### Phase 11 entry conditions

Per §12 cardinal rule: **explicit human sign-off required**.

**§13 ledger remains CLOSED** (closed at Unit 9.9; Phase 10 is the
first NON-§13 phase; Phase 11 will be the second NON-§13 phase).
Phase 11+ thread options stay inferred-not-§13:

1. **Rating-challenge submission write-path** — Phase-9 Unit 9.0 D-5
   explicit deferral; the most defensible next pick per the
   "honored-deferral" pattern. ~6-8 units. Second write-path beyond
   watchlist. Exercises auth pattern at a second surface; closes a
   §8.6 architectural concern (rating challenges feed curator-of-
   record workflow). Needs new `rating_challenges` Drizzle table +
   migration `0002_rating_challenges` + draft + curator review
   pipeline + form UI on problem detail page.
2. **Subscriber-list (third-party email provider)** — Phase-5 D-4
   punt completion. ~6 units. Decoupled from auth (third-party holds
   subscriber list); needs ADR-0014 (provider choice: Buttondown /
   Resend / ConvertKit / Loops) + `lib/email/` wrapper + subscribe
   form + unsubscribe link + digest delivery scheduler.
3. **Multi-provider OAuth expansion** (Google / GitLab / email-link)
   — ADR-0012 D-B forbidden in Phase 9; Phase-11+ follow-on ADR or
   explicit Q-promotion required. ~3-4 units.
4. **Public profile page at `/[locale]/u/[handle]`** (Phase-10 Class
   B item 1). ~3-4 units. Builds on Phase-10 surface; allows viewing
   another user's public profile.
5. **HTML shell migration + Unit 8.4 unblock** — STILL ON HOLD per
   parallel-session preservation signal. Re-attempt only with
   explicit curator authorization.
6. **Monetization thread** — premature without observed user traffic.
   Phase 12+ candidate.
7. **Q51 curator-track bulk FR content backfill** — orthogonal
   long-running thread.

**DB-migration trigger re-eval** at Phase 11 kickoff: procedural-only
formality (trigger (a) FIRED in Unit 9.6; the DB exists and is now in
even-broader active use as of Phase 10's profile-page reads). Trigger
(b) still cold (~1.6% of 5 MB; content count unchanged).

### Cross-phase milestone

This commit closes the **Profile page + Phase-9 UI polish thread** of
Phase 10 — the **first NON-§13 phase** and the **first
zero-architectural-surface phase** in the project's history.

The thread shipped at scope cap: **profile page + watchlist surface +
sign-out + GitHub identity display + first protected route**. Per
Unit 10.0 D-1: rating-challenge submission deferred to Phase 11+;
public profile page deferred; multi-provider OAuth still forbidden.
Scope discipline preserved.

6 commits (10.0-10.5; no deferrals) + 0 new ADRs + 0 new dependencies
+ 0 new tests + 0 OPEN_QUESTIONS movements + 0 client-bundle
regressions (103 kB First Load JS preserved end-to-end) + 0 middleware
regressions (159 kB UNCHANGED) + 1 new route surface (profile) + 1
new lib helper (`getWatchedSlugs`) + 1 new `messages.*` namespace
(profile, 6 keys EN + FR).

**Phase-10 firsts (project-wide)**:
- First NON-§13 phase.
- First zero-architectural-surface phase (0 new ADRs / 0 new Qs / 0
  prose edits).
- First protected route (`/[locale]/profile`).
- First inline-server-action-driven page (sign-out form alongside
  watchlist toggle reuse).
- First page that JOINs Auth.js v5 session state with a per-user
  Drizzle SELECT inside the page handler.

### Phase-10 over-vs-under against the 10.0 plan

Unit 10.0 prep scoped a 6-unit phase (10.0-10.5). Actual: **6 units
shipped + 0 deferred**. Scope drift this phase: **none**. Phase 10
stayed exactly within the 10.0 prep envelope.

One small landing-time correction (Unit 10.2): the originally-drafted
ESLint disable comments for `@next/next/no-img-element` referenced a
rule that isn't loaded in the project's flat ESLint config (per
OPEN_QUESTIONS Q19 / Phase-0 Unit 0.8 regression note). Removed the
disable comments; `alt=""` alone is valid per jsx-a11y/alt-text rule
for decorative images. Documented in Unit 10.2's CHANGELOG entry as
the only landing-time correction in the phase.

### Phase-10 vs Phase-9 contrast

Phase 9 was the **architectural keystone** phase: 10 units, +5
dependencies, +2 ADRs, +5 new code layers, +2 migrations, +5 DB
tables, +6 tests, ~+107 kB middleware bundle. Phase 9 broke the
Phase-4 "no user accounts" pact and fired §5.7 trigger (a).

Phase 10 was the **consolidation** phase: 6 units, +0 dependencies,
+0 ADRs, +1 new file + 1 helper extension, +0 migrations, +0 tests,
+0 middleware delta. Phase 10 lit up Phase-9's unbuilt UI surface
without expanding architectural commitments.

The contrast is by design (Unit 10.0 D-1 framing). Phase 11+ may
pick a thread that's closer to Phase 9's architectural-keystone shape
(rating-challenge submission, subscriber-list, multi-provider OAuth);
the rhythm pattern doesn't require alternation but the
zero-architectural-surface phase is a natural breathing point.

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 10.5 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 394/394 across 45 files unchanged (no test files
  touched in 10.5; or in any Phase 10 unit).
- `pnpm build` → ~590 prerendered pages + 2 dynamic API routes + 2
  profile route entries unchanged. First Load JS shared chunk = 103
  kB unchanged. Middleware bundle 159 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-10 close: marks Unit 10.5 as the acceptance gate; cites the
Phase-10 deliverable status table (all rows green; no DEFERRED rows);
emits the cross-phase state-at-HEAD; enumerates Phase-10 follow-ons
(Class A profile-Playwright + LHCI enrolment; Class B + Class C);
states **§13 ledger remains CLOSED** (Phase 10 was the first
NON-§13 phase); states Phase 11 entry condition (explicit sign-off
per §12; Phase 11+ thread options listed); notes DB-migration
trigger re-eval at Phase 11 kickoff is procedural-only formality.
