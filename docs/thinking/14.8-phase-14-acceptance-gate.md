# 14.8 — Phase 14 acceptance gate (public profile route; first per-USER read-side public surface; four-phase honored-deferral lineage closed)

Phase-14 closing unit. Mirrors Units 1.12 / 2.13 / 3.13 / 4.13 / 5.13 /
6.10 / 7.11 / 8.9 / 9.9 / 10.5 / 11.7 / 12.8 / 13.6. Verifies every
deliverable Phase 14 pulled (Public profile page at `/[locale]/u/[handle]`
per ADR-0015 + Unit 14.0 D-1 + four-phase honored-deferral lineage
closure) is operational locally at HEAD, emits the cross-phase
roll-up, and lists Phase-14 follow-ons that survive into Phase 15+.

## THINK

### Scope clarification

Phase 14 is the **fifth NON-§13 phase** (Phase 10 first; Phase 11
second; Phase 12 third; Phase 13 fourth; Phase 14 fifth). The §13
ledger has been **CLOSED since Unit 9.9**. Phase 14's "Public profile
page at `/[locale]/u/[handle]`" closes the **four-phase honored-deferral
lineage** (Phase-10 Class B item 1 + Phase-12 Class B item 12 +
Phase-13 Class B item 1 + Q58 lean #3 deferral) and **adds the first
per-USER read-side public surface** to the project:

- Phase 9: auth + write-path foundation (watchlist) + DB schema +
  curator-of-record join (`users.githubLogin` per ADR-0012 D-E).
- Phase 10: profile page (signed-in OWN-state surface; `/profile`).
- Phase 11: submission half of community feedback (USER-STATE writes).
- Phase 12: review half of community feedback (curator transitions).
- Phase 13: first read-side public surface for USER-STATE content
  (per-problem axis; Q58 closure).
- **Phase 14**: first per-USER read-side public surface (handle-
  routed shell + per-user challenges sub-route; ADR-0015).

Together: §3.1 "ratings are revisable" + §8.6 COI + community-feedback
loop + public per-user attribution are all operational at the code
layer, and the public attribution chain (per-problem listing
`@login` → public profile → per-user challenges → problem detail) is
end-to-end clickable.

### Phase-14 deliverables — local pass status

| Surface | Phase-14 unit | Status |
|---|---|---|
| Phase 14 prep THINK doc + 9-unit breakdown + procedural DB-trigger re-eval | Unit 14.0 | ✓ `docs/thinking/14.0-phase-14-prep.md`; first-thread = public profile route; ninth "Continue" override invocation. |
| ADR-0015 — per-user privacy model + public profile contract (D-A through D-F) | Unit 14.1 | ✓ first new ADR since Phase 12; pins 6 D-clauses; public-data invariant established; Q63 + Q64 + Q65 candidates flagged for Phase 15+. |
| `lib/users/` module (`getPublicProfileByHandle` + `getProfileActivity` + `getCuratorOfRecordSlugs`) + `lib/rating-challenges/getPublicChallengesByUser` extension + 13 tests | Unit 14.2 | ✓ pure + DB-mocked test coverage; case-insensitive handle lookup (D-B) + status-partition counts (D-A) + case-sensitive curator-of-record match (D-E) all exercised. |
| Public profile shell page at `/[locale]/u/[handle]` (`force-dynamic`; no auth gate; handle resolution + 404 + curator-of-record badge + activity counts + "Edit your profile" CTA) + `messages.public_profile.*` namespace (16 keys per locale; pre-added upfront for atomic discipline) | Unit 14.3 | ✓ first per-USER read-side public surface for the project; 1.92 kB / 108 kB First Load JS. |
| Per-user challenges sub-route at `/[locale]/u/[handle]/challenges` (`force-dynamic`; per-status visibility server-side filtered; mirrors Phase-13 per-problem listing row shape rotated by user axis) + Phase-13 per-problem listing `@login`-to-Link upgrade | Unit 14.4 | ✓ Q58 lean #3 closed; Phase-13 Unit 13.3 D-13 dangling-link-target wart resolved; 1.92 kB / 108 kB First Load JS. |
| SiteHeader "Your profile" link (signed-in only; avatar 16×16 + `@login` truncated 12 chars; hidden when `users.githubLogin === null` Phase-9 retrofit edge) + `safeLogin()` defensive wrapper | Unit 14.5 | ✓ chrome-level discoverability for the new route tree; ZERO new i18n keys (atomic-i18n pre-add discipline payoff). |
| Phase-14 hygiene status pass (Class A/B/C catalog) | Unit 14.6 | ✓ 4 carried Class A + 11 Phase-14-specific Class B + 19 Class C carryovers. |
| OPEN_QUESTIONS hygiene + ADR review (53 entries UNCHANGED; 15 ADRs all accepted; ADR-0015 D-A through D-F all unit-exercised) | Unit 14.7 | ✓ Q63 + Q64 + Q65 candidates flagged but NOT promoted (mirrors Phase-13 Q61 + Q62 precedent); 15 ADRs accepted. |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: 19 URLs UNCHANGED
  in `lighthouserc.json`. New `/[locale]/u/[handle]` + sub-route could
  be enrolled into LHCI in Phase 15+; deferred until first deploy +
  Q54 + Q55 ops unblock.
- **W3C feed validator on `/api/v1/rss.xml` + `/api/v1/digest/<slug>`**:
  Phase-3/5/6/8 compound deferral; first preview deploy + Q47 unblock.
- **Visual-regression baselines** for `/u/[handle]` + sub-route
  deferred Phase-14 follow-on; no Playwright spec changes this phase.
- **No auto-merge** (ADR-0009): Phase 14 added zero LLM-translated
  content; all surfaces are code or curator-typed translation strings.
- **File-first / no DB** (ADR-0004): REAFFIRMED. Phase 14 ships
  read-only per-user surface; zero new tables / columns / migrations.
  Content stays file-first; curator-of-record badge scans
  `#site/content` (Velite-built).
- **First Load JS shared chunk = 103 kB**: **HELD** through every
  Phase-14 unit. All Phase-14 surfaces server-rendered; zero client-
  bundle delta.
- **Middleware bundle = 160 kB**: **UNCHANGED** since Phase 12 close
  (Phase 12 was the last middleware-affecting change at +1 kB from
  Drizzle re-bundling; Phases 13 + 14 added zero middleware changes).

### Phase-14 unit summary (9 commits — fifth NON-§13 phase)

| Unit | Commit | Title |
|---|---|---|
| 14.0 | `761d001` | Phase 14 prep (public profile; ADR-0015 candidate) |
| 14.1 | `58223a4` | ADR-0015 per-user privacy model (15 ADRs total) |
| 14.2 | `0ac2a68` | lib/users module + getPublicChallengesByUser + 13 tests |
| 14.3 | `0fcbea7` | public profile shell page + messages.public_profile.* + badge |
| 14.4 | `00f76ef` | per-user challenges sub-route + Phase-13 @login Link upgrade |
| 14.5 | `26d9125` | SiteHeader 'Your profile' link (signed-in only) |
| 14.6 | `0faa3e4` | Phase-14 hygiene status pass (Class A/B/C catalog) |
| 14.7 | `c01962d` | OPEN_QUESTIONS hygiene + ADR review |
| 14.8 | _this_ | Phase 14 acceptance gate |

Total: **9 numbered units, all shipped**. Matches Unit 14.0 prep's
9-unit breakdown exactly (matches Phase 12's 9-unit shape; one unit
larger than Phase 13's 7 reflects new ADR + new lib module +
multi-surface route tree). **Zero deferrals**, **zero scope drift**
across the phase.

### State at HEAD (Unit 14.8)

- **Content** (no count change vs Phase-8 close; Phases 9 – 14 added
  no content):
  - 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors
    / 14 institutions / 20 rating actions / 4 issue templates /
    methodology v1 (EN + FR) / contributing v1.0 (EN) + v1.1 (EN + FR)
    / 2 `entries.json` files.
  - **203 schema-validated content files + 36 raw MDX = 239 total raw
    content files UNCHANGED**.
- **New Phase-14 code surfaces** (relative to Phase-13 close at
  `c3e3cbf`):
  - `lib/users/index.ts` (new; Unit 14.2) — 3 helpers + 2 interfaces.
  - `lib/users/index.test.ts` (new; Unit 14.2) — 13 tests.
  - `lib/rating-challenges/index.ts` (edit; Unit 14.2) — 1 new helper
    (`getPublicChallengesByUser`).
  - `app/[locale]/u/[handle]/page.tsx` (new; Unit 14.3) — public
    profile shell route.
  - `app/[locale]/u/[handle]/challenges/page.tsx` (new; Unit 14.4) —
    per-user challenges sub-route.
  - `app/[locale]/problems/[slug]/challenges/page.tsx` (edit; Unit
    14.4) — `@login` upgraded to `<Link href="/u/{login}">`.
  - `components/site-header/index.tsx` (edit; Unit 14.5) — "Your
    profile" link + `safeLogin()` defensive wrapper.
  - `messages/{en,fr}.json` (edits; Unit 14.3) — +16 keys per locale
    in `public_profile.*` namespace; +32 total across EN + FR.
  - `docs/adr/0015-per-user-privacy-model.md` (new; Unit 14.1).
  - `docs/adr/README.md` (edit; Unit 14.1) — ADR-0015 index row.
  - 9 new THINK docs (14.0 – 14.8).
- **Routes**: ~593 prerendered pages UNCHANGED + **7 dynamic page+API
  routes** (was 5 at Phase-13 close; +2 from Phase 14: `ƒ
  /[locale]/u/[handle]` + `ƒ /[locale]/u/[handle]/challenges`).
- **Tests**: **480/480 across 52 vitest files** (was 467/51 at
  Phase-13 close; +13 / +1 in Phase 14 from Unit 14.2's `lib/users/index.test.ts`).
- `pnpm validate-content` → **203 files** green (unchanged).
- `pnpm audit-content` → **0 errors / 6 warnings** (Q32 baseline since
  Phase 2; unchanged).
- `pnpm typecheck` clean.
- `pnpm build` clean; **First Load JS shared chunk = 103 kB UNCHANGED**;
  middleware bundle = **160 kB UNCHANGED**.
- **5 visualizations live** (unchanged from Phase 4).
- **15 ADRs** (was 14 at Phase-13 close; +1 ADR-0015 in Unit 14.1).
- **Dependencies**: **+0 net in Phase 14** (Phase-9 through Phase-13
  stack stable).
- **`lighthouserc.json`** enrols **19 URLs** UNCHANGED.
- **OPEN_QUESTIONS state**: 21 resolved + 4 decided-as-lean + 28 open
  = **53 total entries** (was 53 at Phase-13 close; +0 net; Q63 +
  Q64 + Q65 candidates flagged but NOT promoted).
- **DB schema tables**: **6 UNCHANGED**. **Columns UNCHANGED** (Phase
  14 read-only).
- **Migrations**: **4 UNCHANGED**.
- **Env contract**: **UNCHANGED** (no new env vars; `LOP_CURATOR_LOGINS`
  carried from Phase 12).
- **New code layers**: `lib/users/` + `app/[locale]/u/[handle]/` route
  tree.

### `lighthouserc.json` final state

Phase 14 changes: **none**. URL count holds at **19** at Phase-14
close. New public profile route + sub-route could be enrolled into
LHCI in Phase 15+; deferred until first preview deploy + Q54 + Q55
ops unblock (when real signed-in user accounts exist to render the
profile meaningfully).

### Phase-14 follow-ons that survive the gate (non-blocking)

Documented in detail in Unit 14.6's hygiene status pass (11 Class B
+ 4 Class A + 19 Class C items). Critical-path subset for Phase 15+
planning:

1. **User-editable profile fields (Q63 candidate)** — Class B item 1.
   ADR-0015 D-C deferral. Requires writes API + new DB columns +
   migration + XSS sanitization + possible image-upload pipeline
   ADR.
2. **Per-user privacy opt-out toggle (Q64 candidate)** — Class B
   item 2. ADR-0015 D-D deferral. Requires `users.profilePublic`
   boolean column + migration + writes surface.
3. **Per-curator activity feed / contribution timeline (Q65
   candidate)** — Class B item 3. ADR-0015 D-E expansion.
4. **Curator-of-record case-insensitive matching** — Class B item 4.
   Single-line `.toLowerCase()` enhancement.
5. **Per-user listing URL search-param sort + pagination** — Class B
   item 5 (Phase-13 Class B items 2 + 7 carryover).
6. **Per-row edit/withdraw on per-user listing when viewing own profile**
   — Class B item 6 (couples to Q63 promotion).
7. **Custom 404 page for `/u/[handle]`** — Class B item 7
   (pre-added `not_found_title` + `not_found_message` keys
   anticipate).
8. **`/about/privacy` explainer** — Class B item 8 (couples to Q64
   promotion).
9. **First LHCI run** on new public profile + sub-route — Class B
   item 9 (Phase-9 Class B item 10 carryover).
10. **Mobile-nav variant of "Your profile" link** — Class B item 10.
11. **SiteHeader avatar-dropdown consolidation** — Class B item 11.
12. **Q54 + Q55 operational unblocks** — Class A items 1 + 2 (carried
    since Phase 9).
13. **CI dummy `AUTH_SECRET`** — Class A item 3 (carried).
14. **`pnpm db:migrate` doc for new contributors** — Class A item 4
    (4 migrations).

### Pre-existing follow-ons that survived Phase 14 (carryover)

Unchanged from Unit 13.4 acceptance gate (19 Class C items per Unit
14.6 catalog). Notable:

- HTML shell migration + `<html lang={locale}>` + SiteHeader under
  `NextIntlClientProvider` — STILL ON HOLD per parallel-session
  preservation signal.
- W3C feed validator pass on `/api/v1/rss.xml` (Phase-3 carryover).
- `<managingEditor>` on RSS feeds (Q33/Q44; Q2 DNS gate).
- `pnpm clean-drafts` script.
- Phase-2 ROR-ID + InstaDeep orphan.
- Chrome strings + FR bulk backfill + StatusPill localization + nav
  labels (Q51 curator-track horizon).
- Trailing-slash normalization for `NEXT_PUBLIC_SITE_URL`.
- Per-entry sitemap hints.
- Watchlist count display on `/problems` index.
- Multi-provider OAuth expansion (Phase-9 Class B item 8; **ADR-0016
  candidate**).
- Real-API integration smoke for `lib/discussions/github-graphql.ts`
  (Q47 + `GITHUB_TOKEN`).
- CLI `pnpm emit-challenge-action <id>` (Q59 candidate).
- Form-state preservation on validation error.
- Orphan-row cleanup script for `watchlist` + `ratingChallenge`.
- Rate-limiting on `POST /api/v1/rating-challenges/[id]/review`.

### Phase 15+ entry conditions

Per §12 cardinal rule: **explicit human sign-off required** at this
acceptance gate. Phase 14 closes the four-phase honored-deferral
lineage + adds the first per-USER read-side public surface.

**Phase 15+ candidate threads** (each overridable):

- **User-editable profile fields (Q63 promotion)** (~4-5 units).
  Promotes Q63 candidate from ADR-0015 D-C deferral. Surfaces a new
  ADR for image-upload pipeline (Vercel Blob? S3? URL allowlist?)
  IF image override is in scope; otherwise just `users.displayName`
  + `users.bio` + writes API + new migration + XSS sanitization.
- **Per-user privacy opt-out (Q64 promotion)** (~3 units). Promotes
  Q64 candidate from ADR-0015 D-D deferral. `users.profilePublic`
  column + migration + writes surface + 404 on opt-out + `/about/privacy`
  explainer.
- **Per-curator activity feed (Q65 promotion)** (~4-5 units).
  Promotes Q65 candidate from ADR-0015 D-E expansion. New
  `getCuratorActions(handle)` helper + new `/u/{handle}/curated`
  sub-route + rating-action history join + per-curator badge link
  expansion.
- **Subscriber-list email** (~6 units). Phase-5 D-4 punt closure;
  opens `lib/email/` infrastructure. Surfaces **ADR-0016 candidate
  (alternative path)**.
- **CLI `pnpm emit-challenge-action <id>`** (~2-3 units; **Q59
  promotion** if usage demands).
- **Multi-provider OAuth expansion** (~3-4 units; **ADR-0016
  candidate**).
- **Full §8.6 24-mo collaborator COI check** (~3-5 units; **ADR-0017
  candidate** alongside `curatorRoles` DB table).
- **Q61 anonymity option / Q62 rejection-visibility** (Phase-13
  carryover candidates; ~2-3 units each).
- **HTML shell migration + Unit 8.4 unblock** — STILL ON HOLD per
  parallel-session preservation signal.
- **Monetization** — premature without observed user traffic. Phase
  16+ candidate.

DB-migration trigger re-eval mandatory at Phase 15 kickoff per Unit
14.0 D-2 cascade (procedural; trigger (a) already fired Phase 9;
trigger (b) cold).

### Cross-phase milestone

This commit closes the **four-phase honored-deferral lineage** for
the public profile route (Phase-10 Class B item 1 + Phase-12 Class B
item 12 + Phase-13 Class B item 1 + Q58 lean #3 deferral) + **adds
the first per-USER read-side public surface** to the project. 9
commits + 1 new ADR (ADR-0015) + 0 new dependencies + 0 client-bundle
regressions (103 kB First Load JS preserved end-to-end) + 0 test
regressions + 13 new tests + +0 OPEN_QUESTIONS promotion (Q63 + Q64
+ Q65 candidates flagged for Phase 15+) + 2 new dynamic page routes
+ 0 new DB columns / migrations / env vars + 32 new i18n keys + 1
SiteHeader edit + 1 Phase-13 per-problem-listing edit.

**§13 ledger remains CLOSED** (closed at Unit 9.9). Phase 14 is the
**fifth NON-§13 phase**.

**Four-phase honored-deferral lineage closed**. The public profile
route was first flagged in Phase 10 (signed-in own surface shipped;
public surface deferred); re-flagged at Phase 12 close; re-flagged
at Phase 13 close; Q58 lean #3 explicitly carved off for Phase 14.
Phase 14 closes the thread; the public attribution chain is now
end-to-end clickable: per-problem listing `@login` →
`/u/{login}` → `/u/{login}/challenges` → `/problems/{slug}` (back
loop).

**Three architectural patterns validated by Phase 14**:

1. **First per-USER read-side public surface** — sets the pattern
   Phase 15+ per-user surfaces (per-curator activity feed; per-user
   contribution timeline; per-user editable profile) will inherit.
2. **`/u/{login}` PUBLIC canonical vs `/profile` EDIT mode split**
   — establishes the "two surfaces per identity (public + edit)"
   pattern that Phase 15+ Q63 promotion will extend.
3. **Atomic-i18n pre-add discipline payoff** — Unit 14.3 pre-added
   all 16 keys upfront; Units 14.4 + 14.5 consumed subset without
   further messages edits. Mirrors Phase-11 Unit 11.3 + Phase-12 Unit
   12.4 + Phase-13 Unit 13.2 patterns; pre-add discipline reduces
   parallel-session collision risk on shared messages files.

### Phase-14 over-vs-under against the 14.0 plan

Unit 14.0 prep scoped a 9-unit phase (14.0 – 14.8). Actual: **9 units
shipped**, **zero deferrals**, **zero scope drift**. Per-unit landings
matched the prep table exactly. ADR-0015 D-A through D-F all
unit-level exercised across Units 14.2 – 14.5.

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 14.8 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 480/480 across 52 files unchanged (no test files
  touched in 14.8).
- `pnpm build` → ~593 prerendered pages + 7 dynamic page+API routes
  unchanged. First Load JS shared chunk = 103 kB unchanged. Middleware
  bundle = 160 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-14 close: marks Unit 14.8 as the acceptance gate; cites the
Phase-14 deliverable status table (every row ✓; zero deferrals); emits
the cross-phase state-at-HEAD (10 problems / etc.; ~593 prerendered
pages + 7 dynamic routes; 103 kB First Load JS; 480 tests across 52
files; 15 ADRs; 19 LHCI URLs; +0 new deps; 6 DB tables; 4 migrations;
53 OQ entries); enumerates Phase-14 follow-ons (Q63 / Q64 / Q65
candidates + curator-of-record case-insensitive enhancement +
per-user URL sort + pagination + per-row withdraw on own profile +
custom 404 + privacy explainer + first LHCI run on new routes +
mobile-nav variant + SiteHeader avatar-dropdown + Q54 + Q55 + CI
AUTH_SECRET + db:migrate doc); enumerates pre-existing carryovers;
states Phase 15+ entry condition (explicit sign-off per §12); notes
the **four-phase honored-deferral lineage closure** + **public
attribution chain end-to-end clickability**; flags Q63 + Q64 + Q65
candidates for Phase-15+ promotion alongside ADR-0016 (multi-provider
OAuth) + ADR-0017 (full §8.6 COI + `curatorRoles`) carryover
candidates.
