# 21.4 — Phase 21 acceptance gate (orphan-blob cleanup closure; 19 ADRs unchanged; second consecutive operational-script-keystone phase)

Fifth and final Phase-21 unit; docs-only. Closes Phase 21
(**twelfth NON-§13 phase**; orphan-blob cleanup realized via
`scripts/cleanup-orphan-blobs.ts`). Mirrors Phase-12 through
Phase-20 acceptance-gate patterns.

> **§12 sign-off**: Phase 22 sign-off **pending** at end of unit.
> "Continue" override path available (**seventeenth invocation**
> opportunity).

## THINK

### Phase 21 ledger

**5 units shipped** (21.0 prep → 21.4 gate). **0 deferrals**;
**0 scope drift**. **Tenth consecutive well-scoped phase**.
**Smallest phase shape tied with Phase 20** (5 units; was 6 in
Phase 19; 7 in Phase 18; 7 in Phase 13).

| Unit | Title | Type | Status |
|---|---|---|---|
| 21.0 | Phase 21 prep | docs | shipped `c15685c` |
| 21.1 | `scripts/cleanup-orphan-blobs.ts` (Vercel Blob list + DB join) | code | shipped `01fc7c3` |
| 21.2 | Phase-21 hygiene status pass | docs | shipped `cb3d0d2` |
| 21.3 | OPEN_QUESTIONS hygiene + ADR review | docs | shipped `23155ac` |
| 21.4 | **Phase 21 acceptance gate** (this unit) | gate | this commit |

### Phase-21 closure summary

**Phase-16 + Phase-20 carryover** (orphan-blob accumulation)
**CLOSED** in Unit 21.1 via `scripts/cleanup-orphan-blobs.ts`.
Per-run 5-phase pipeline reconciles DB-as-source-of-truth
(`users.imageOverride` rows with non-null URLs) against Vercel
Blob storage enumeration (paginated `list({ prefix: "avatars/" })`),
detects orphans (blobs not referenced by any DB row), and
deletes them with per-blob try/catch + `--dry-run` safety.

Phase 21 is the **third phase since Phase 3 to close without
new ADR** (Phase 18 + 20 + 21) and the **second consecutive
operational-script-keystone phase** (Phase 20 + 21). Together
with Phase 20, establishes the operational-script-keystone
pattern as recurring (not a one-off Phase-20 anomaly).

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`23155ac`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **564/564 across 54 vitest files**. 0 / 0
  Δ from Phase 20 close (564/54); **second consecutive
  zero-new-test phase** (Phase 20 + 21).
- **`pnpm build`** → ~659 prerendered pages + 7 dynamic page+API
  routes UNCHANGED. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-21 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` = 117 kB UNCHANGED. `/u/{handle}` = 108 kB
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32 baseline
  since Phase 2; unchanged through every Phase 3-21 unit).
- **`pnpm lint`** clean.

### Architectural firsts shipped in Phase 21

1. **First storage-reconciliation surface** in project history —
   Phase 16-20 trusted storage atomicity (delete-on-replace in
   `updateProfileImage`; best-effort `del()` in Phase-20
   backfill); Phase 21 introduces explicit DB-vs-storage
   reconciliation (DB is source of truth; storage is the
   side-effect to reconcile).
2. **Second operational-script-keystone phase** in project
   history — Phase 20 first via `backfill-exif-strip.ts`; Phase
   21 second via `cleanup-orphan-blobs.ts`. **Establishes the
   pattern as recurring**.
3. **Fifth consecutive 0-migration phase** (Phase 17 + 18 + 19 +
   20 + 21; 8 of 12 phases since DB landed are 0-migration).
4. **Fifth consecutive single-session phase** (Phase 17 + 18 +
   19 + 20 + 21; Phase 16 was the parallel-session phase).
5. **Third phase since Phase 3 to close without new ADR** (Phase
   18 + 20 + 21; **broadens the precedent** — ADR-pinned
   Phase-N+ contracts AND class-B-tracked operational follow-ons
   both qualify for no-new-ADR phases).
6. **Second consecutive zero-new-dep/test/ADR phase** (Phase 20
   + 21). Operational-script-keystone pattern correlates with
   zero-new-architectural-surface.
7. **First phase to combine `Promise.all`-parallel read-only
   operations** in an operational script (Unit 21.1's
   `enumerateAvatarBlobs()` + `snapshotInUseUrls()` run
   concurrently).
8. **First hygiene unit since Phase 13 Unit 13.5 to touch only
   ADR review** (no `OPEN_QUESTIONS.md` edit). Phase 21.3 is the
   second.
9. **First two-phase Q-tally freeze** in project history (Phase
   20 + 21; both shipped 0 Q-promotions / 0 status changes).
10. **Q59 hits double-digit phase carryover for the first time**
    (carried Phase-12 through Phase-21 = 10 phases).

### Delta summary (Phase 20 → Phase 21)

| Metric | Phase 20 close (`f4f498e`) | Phase 21 close (HEAD `23155ac`) | Δ |
|---|---|---|---|
| Commits | 5 | 4 + this gate | +5 |
| ADRs | **19** | **19** | **0** (**second consecutive no-new-ADR phase**; **third phase since Phase 3** to close without new ADR — Phase 18 + 20 + 21) |
| Migrations | 6 | **6** | **0** (**fifth consecutive 0-migration phase**) |
| Runtime deps | (Phase-20 stack) | (Phase-20 stack unchanged) | **0** (**zero new deps Phase 21**) |
| Env vars | 7 | 7 | **0** (**zero new env vars Phase 21**) |
| Tests | 564 / 54 files | **564 / 54 files** | **0** (**second consecutive zero-new-test phase**) |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` + `/u/{handle}` bundles | 117 + 108 kB | 117 + 108 kB | 0 |
| OPEN_QUESTIONS resolved | 26 | **26** | **0** (**first two-phase Q-tally freeze**) |
| OPEN_QUESTIONS open | 29 | **29** | **0** |
| OPEN_QUESTIONS total | 59 | **59** | **0** |
| i18n keys per locale | 127 | 127 | 0 |
| `scripts/` files | 7 | **8** | **+1** (`cleanup-orphan-blobs.ts`) |
| `package.json#scripts` entries | (+1: `backfill-exif-strip`) | (+1: `cleanup-orphan-blobs`) | +1 |
| Server-side image processing surfaces | 2 | 2 | 0 (cleanup script is storage-hygiene, not image-processing) |
| Privacy-by-default surfaces | 2 | 2 | 0 |
| Retroactive-privacy-correction surfaces | 1 | 1 | 0 |
| Storage-reconciliation surfaces | 0 | **1** | **+1** (first in project history) |
| Operational-script-keystone phases | 1 | **2** | **+1** (Phase 20 first; Phase 21 second) |

### Surviving follow-ons

**Class B — ~9 items in 6 categories** (Phase-20 Class B carried
+ 2 Phase-21 cleanup operational follow-ons newly surfaced):

- **B.1 Image-processing expansion (4 carried unchanged)**:
  Q68 expansion; cropping UI; resizing; WebP/AVIF.
- **B.2 Phase-17 markdown subset expansion (8 carried unchanged)**.
- **B.3 Phase-18 surface expansion (3 carried + Q72)**.
- **B.4 Phase-20 backfill operational follow-ons (3 carried
  unchanged)**: `--user-id` + `--verbose` + dry-run sentinel for
  `backfill-exif-strip`.
- **B.5 Phase-21 cleanup operational follow-ons (2 newly
  surfaced)**: `--prefix` flag (non-avatar blob types);
  `--verbose` flag (per-blob progress) for `cleanup-orphan-blobs`.
- **B.6 Operational + infrastructure (7; +1 from Phase 20)**:
  Q69 / Q54 / Q55 operational gates; CI dummy `AUTH_SECRET`;
  `pnpm db:migrate` doc; first LHCI run; middleware-based
  auth-route protection; `pnpm backfill-exif-strip` operational
  doc; **`pnpm cleanup-orphan-blobs` operational doc** newly
  surfaced.

**Class C** — pre-existing carryovers unchanged.

### Phase-22+ Q-candidates flagged

**8 candidates carry** (unchanged from Phase 19 and Phase 20;
Phase 21 added zero new and promoted zero):

- Q59 (CLI emit-challenge-action) — carried Phase-12 through
  Phase-21 = **10 phases**. **First double-digit phase
  carryover for any Q-candidate** in project history.
- Q60 / Q61 / Q62 / Q64 / Q65.
- Q68 expansion (content moderation on uploaded images;
  ADR-0019 D-F integration point + Phase-20 backfill pattern
  reusable for content-moderation backfill).
- Q72 (`reviewNotesSchema` divergence).

### Phase 22+ entry conditions

Per §12 cardinal rule. **Seventeenth "Continue" override
opportunity** in Unit 22.0 prep. Phase 22+ thread options (all
inferred-not-§13):

- **A. Q68 expansion content moderation** — ADR-0020 candidate
  for moderation API; ADR-0019 D-F inheritance contract; Phase-20
  backfill pattern reusable; Phase-21 cleanup pattern also
  reusable (after moderation backfill, run cleanup-orphan-blobs
  to garbage-collect quarantine-failed uploads). ~3-5 units.
- **B. Cropping UI** (per ADR-0019 D-F) — ~3-5 units; couples
  to lazy-loaded client dep.
- **C. Server-side resizing + WebP/AVIF** (per ADR-0019 D-F) —
  ~1-2 units; may be operational-script-keystone phase if
  scope = backfill existing avatars to new format.
- **D. Q72 markdown subset extensions** — ~2-3 units.
- **E. Per-challenge detail page** — ~2-3 units; may not need
  new ADR.
- **F. Markdown rating-action `rationale`** — ~2-3 units;
  couples to (E).
- **G. Q64 per-user privacy opt-out** — ~3 units; 4th ALTER
  migration breaks 5-consecutive-0-migration streak.
- **H. Multi-provider OAuth expansion** — Phase-9 Class B item
  8 carried **11 phases**. **ADR-0020 candidate**. ~3-4 units.
  **Strongest architectural patience signal at right scope** —
  growing each cycle without claim.
- **I. Subscriber-list email** — Phase-5 D-4 punt carried **15+
  phases**. ~6 units. Strongest overall patience signal.
- **J. Q59 CLI emit-challenge-action** — carried **10 phases**;
  smallest reasonable scope (~2-3 units).
- **K. Account-deletion blob cleanup** — Phase-16 carryover; if
  Phase 22+ ships account-deletion path, must integrate with
  `cleanup-orphan-blobs` or extend `deleteAccountAction` to
  delete owned blobs. ~1-2 units.

**DB-migration trigger re-eval at Phase 22 kickoff**: trigger (a)
FIRED Unit 9.6; trigger (b) cold. Phase 22 migration count
depends on chosen thread.

### Risk surface at HEAD `23155ac`

1. **Three operational gates** (Q54 / Q55 / Q69) pending curator.
2. **Cleanup script untested against live data Phase 21** —
   manual curator `--dry-run` verification required before
   first execution run. Script logic exercises Phase-16-tested
   Vercel Blob primitives + Phase-9-tested drizzle SELECT
   patterns; orchestration logic verified via typecheck + lint
   only.
3. **Storage-reconciliation snapshot-at-start posture** —
   `users.imageOverride` snapshot taken once at script start;
   concurrent user uploads during run window appear as orphans
   in the detection set. Mitigation: `--dry-run` first; low-
   traffic-window scheduling.
4. **No content moderation on uploaded images** (Q68 expansion
   deferred Phase 22+).
5. **No live-preview in edit form** (Phase 17 + 18 deferral).
6. **Sharp cold-start cost** persists (~100 ms; Phase-19+20
   carryover; not Phase-21 concern).

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token (Q69), NOT FR content backfill (Q51
curator-track), NOT moderation / cropping / resizing / WebP
(Phase 22+), NOT live cleanup run (curator operational task; NOT
a code-shipping unit), NOT destructive cleanup (Class C still
gated; cleanup-orphan-blobs is targeted reconciliation, not
arbitrary destruction). This unit is the **closure**, not the
**unblock**.

### Parallel-curator awareness

Phase 21 shipped as a **single-session phase** — primary shipped
all 5 units 21.0 → 21.4 in one coherent session. **Five
consecutive single-session phases** (Phase 17 + 18 + 19 + 20 +
21). Phase 16 remains the lone parallel-session phase.

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline unchanged).
- File added: `docs/thinking/21.4-phase-21-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 21.4 entry.

### CHANGELOG entry shape

Unit 21.4 entry: declares Phase 21 closed (**twelfth NON-§13
phase**; 5 units; 0 deferrals; 0 scope drift; tenth consecutive
well-scoped phase); cites orphan-blob cleanup closure via Unit
21.1 (**first storage-reconciliation surface** in project
history; **second operational-script-keystone phase**; **third
phase since Phase 3 to close without new ADR**); 10 Phase-21
architectural firsts; Phase 20 → 21 delta table (0 ADR Δ; 0
migration Δ; 0 runtime dep Δ; 0 env var Δ; 0 test Δ; 0 bundle Δ;
0 OQ Δ; +1 `scripts/` file; +1 storage-reconciliation surface;
+1 operational-script-keystone phase counter); Class B
follow-ons (~9 items in 6 categories); 8 Phase-22+ Q-candidates
(Q59 first double-digit carryover at 10 phases); ADR-0020
candidate slot open after **8-phase ADR cycle without multi-
provider-OAuth claim** (Phase-9 Class B item 8 carried 11
phases — strongest architectural patience signal growing);
Phase 22+ thread options (strongest patience signals: Phase-9
Class B item 8 = 11 phases multi-provider OAuth; Phase-5 D-4
punt = 15+ phases subscriber-list email; Phase-12 D-D = 10
phases Q59 CLI); seventeenth "Continue" override opportunity
per §12; five consecutive single-session phases (Phase 17 + 18
+ 19 + 20 + 21); fifth consecutive 0-migration phase; two
consecutive operational-script-keystone phases pattern
established as recurring.
