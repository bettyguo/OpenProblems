# 52.4 — Phase 52 acceptance gate

Phase 52 closes at HEAD `a1e964e` (Unit 52.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-53-entry
conditions per §12.

## Phase 52 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 52.0 | Phase 52 prep | docs | `4275717` |
| 52.1 | `PHASE_50_DEFAULT_ENABLED_SURFACES` cross-surface expansion + dispatch tests + decoupling + ADR-0018 D-G APPEND-D-AJ | code+APPEND | `88397b7` |
| 52.2 | 21 NEW end-to-end Phase-52 tests + first all-4-surfaces quadruple-alias + first all-4-surfaces 3-consumer same-slot composition | code | `c440e9a` |
| 52.3 | Phase-52 hygiene + APPEND-D-AH PubMed cross-surface item closure documentation | docs | `a1e964e` |
| 52.4 | Phase 52 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-51 5-unit framework shape
verbatim.

## Fifth realization of constructor-arg-only zero-rework expansion + first 5-realization property + first all-4-surfaces quadruple-alias + completes per-consumer all-4-surfaces arc + fastest-cross-surface-closure record

- `PHASE_50_DEFAULT_ENABLED_SURFACES` value evolved in-place
  from `new Set(["rationale"])` (Phase 50 ship through Phase 51
  close) to `new Set(["bio", "reviewNotes", "rationale", "actionRationale"])`
  (Phase 52 ship). **Fifth real-consumer-expansion realization**
  of the "constructor-arg-only zero-rework expansion" property
  (Phase 42 wikilinks; Phase 43 tables; Phase 44 arxiv; Phase
  49 doi; Phase 52 pubmed). **First 5-realization property in
  project history** (extends Phase-49 4-realization record to 5).
  **Completes the per-consumer all-4-surfaces arc for ALL 5
  Phase-37-framework consumers** — every concrete consumer is
  now default-enabled on every wired markdown surface.
- `PubmedExtensionRegistry` class + factory dispatch arm +
  plugin body + `PUBMED_PATTERN` regex **UNCHANGED**. Pure
  constructor-arg-only change.
- 3 dispatch tests in `lib/markdown/extensions/index.test.ts`
  flipped to reflect Phase-52 all-4-surfaces dispatch + first
  all-4-surfaces 3-consumer same-slot + first all-4-surfaces
  5-consumer composition.
- 4 baseline test blocks in `lib/markdown/index.test.ts`
  decoupled with explicit `new Set(["rationale"])` constructor
  args to preserve Phase-50/51 baseline coverage. **Decoupled-
  baseline-block pattern** (established Phase 49) applied
  verbatim — second application of the discipline.
- 21 NEW end-to-end tests in `lib/markdown/index.test.ts`
  across 3 describe blocks: default pubmed on all 4 surfaces
  (10); first all-4-surfaces quadruple-alias under 5-way
  composite (6); first all-4-surfaces 3-consumer same-slot
  composition (5).
- ADR-0018 D-G APPEND adds Phase-52 EXTENDED block + **APPEND-
  D-AJ** (Unit 52.1; tenth two-letter APPEND letter).
- ADR-0018 APPEND-D-AH PubMed PMID cross-surface item CLOSED at
  **2-phase carryover** (Phase 50 → Phase 52; Unit 52.3
  documents). **FASTEST CROSS-SURFACE-EXPANSION APPEND-DEFERRAL
  CLOSURE EVER OBSERVED** (beats prior 3-phase record from
  Phase-41 → 44 arxiv expansion). **First cross-surface-
  expansion closure under the 3-phase floor** in the cadence.
- **Second D-clause with BOTH items closed within the closure
  cadence**. APPEND-D-AH carries 2 deferrals (alias + cross-
  surface); both resolved (Phase 51 alias closed via D-AI;
  Phase 52 cross-surface closes via D-AJ). **D-AC was first
  (Phase 48 alias + Phase 49 cross-surface); D-AH is second**.
- **First "all 4 surfaces are quadruple-alias" state** in
  project history. Pre-Phase-52 only rationale was quadruple-
  alias (Phase 51 ship); post-Phase-52 every surface carries 4
  alias-syntax consumers under 5-way default. **First surface-
  with-4-alias-consumers cardinality of 4**.
- **First "all 4 surfaces have 3-consumer same-slot
  composition" state**. Pre-Phase-52 only rationale carried
  `[arxiv, doi, pubmed]` in `remarkPlugins`; post-Phase-52
  every surface does. **Regex-disjointness-as-sole-defense
  discipline at 3-consumer cardinality exercised on every
  surface in production default**.
- **First "all 4 surfaces with 5-consumer composition under
  default dispatch" state**. **Maximum-consumer-cardinality
  state generalized to all surfaces** (Phase 50 first-realized
  on rationale only).
- **Maximal multi-consumer all-surfaces composition extended
  to 5-consumer + 3-consumer-same-slot cardinality** — new
  upper bound for the Phase-37-framework's current
  capabilities.

## Phase 51 → Phase 52 delta

| Metric | Phase 51 close | Phase 52 close | Δ |
|---|---|---|---|
| Tests | 1174 / 73 files | **1195 / 73 files** | +21 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 18 | **19** (project record extends; tenth two-letter slot D-AJ in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 6 | 6 | 0 |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 17 | 17 | 0 |
| **Phase-37-framework concrete consumers** | **5** | **5** | 0 |
| **Constructor-arg-only zero-rework expansion realizations** | **4 (wikilinks + tables + arxiv + doi)** | **5 (+pubmed)** | **+1** |
| **Surfaces with 3-consumer same-slot composition** | **1 (rationale; arxiv+doi+pubmed)** | **4 (all surfaces)** | **+3** |
| **Surfaces with quadruple-alias (4+ alias-syntax consumers)** | **1 (rationale)** | **4 (all surfaces)** | **+3** |
| **Surfaces with 5-consumer composition under default dispatch** | **1 (rationale)** | **4 (all surfaces)** | **+3** |
| **Component-surface-slot active positions under 5-way default** | **17** (12 single + 4 arxiv-doi same-slot extras + 1 pubmed extra on rationale) | **20** (12 single + 4 arxiv-doi same-slot extras + 4 pubmed same-slot extras) | **+3** |
| **Fastest cross-surface-expansion closure (phases)** | 3 (Phase 41 → 44 arxiv) | **2 (Phase 50 → 52 pubmed)** | **-1 phase (new record)** |
| **APPEND-deferral closure cadence (consecutive phases)** | 10 (Phase 42-51) | **11 (Phase 42-52)** | **+1 (new record)** |
| **D-clauses with all items closed within the cadence** | **1 (D-AC)** | **2 (+D-AH)** | **+1** |

## Architectural firsts in Phase 52 (12 enumerated)

1. **Fifth realization of the "constructor-arg-only zero-
   rework expansion" property**. Phase 42 wikilinks; Phase 43
   tables; Phase 44 arxiv; Phase 49 doi; Phase 52 pubmed.
   **First 5-realization property in project history**
   (extends Phase-49 4-realization record to 5). **Completes
   the per-consumer all-4-surfaces arc for ALL 5 Phase-37-
   framework consumers**.
2. **First "all 4 surfaces are quadruple-alias" state** in
   project history. **First surface-with-4-alias-consumers
   cardinality of 4**.
3. **First "all 4 surfaces have 3-consumer same-slot
   composition" state**. Regex-disjointness-as-sole-defense
   discipline at 3-consumer cardinality exercised on every
   surface in production default.
4. **First "all 4 surfaces with 5-consumer composition under
   default dispatch" state**. Maximum-consumer-cardinality
   state generalized to all surfaces.
5. **Fastest cross-surface-expansion APPEND-deferral closure
   ever observed** at **2-phase carryover** (Phase 50 → 52).
   Beats prior 3-phase record from Phase-41 → 44. **First
   cross-surface-expansion closure under the 3-phase floor**.
6. **Fifth cross-surface-expansion APPEND-deferral closure**
   in the cadence (Phase 42 + 43 + 44 + 49 + 52). Completes
   the cross-surface-expansion arc for all 5 framework
   consumers.
7. **Second D-clause with BOTH items closed within the closure
   cadence**. D-AC was first (Phase 48 + 49); D-AH is second
   (Phase 51 + 52). Both-items closure window halved across
   the two examples (4 phases → 2 phases).
8. **Eleventh prep-/APPEND-doc-level deferral closed by a
   later phase**. APPEND-deferral closure cadence sustained
   11 phases — **new longest sustained cadence in project
   history** (extends Phase-51 record 10 → 11). **First 11-
   phase APPEND-deferral closure run**.
9. **19th APPEND on ADR-0018 D-G** — extends first-ADR-D-
   clause-with-most-APPENDs record 18 → 19.
10. **Tenth two-letter APPEND letter D-AJ** (Excel-spreadsheet
    column convention sustained).
11. **First "framework + 5 consumers + composition + 5
    expansions + 3-consumer same-slot composition × 4 + alias
    syntax × 4 + dual-form regex × 3 + 5-consumer composition
    × 4" 16-phase cluster** in project history (Phase 37-52).
    16-phase architectural arc. Plus **22nd consecutive phase
    without new B category** — Phase 31-52 = first 22-phase
    run (extends Phase-51 record from 21 → 22). Plus **forty-
    third NON-§13 phase**. Plus **120th consecutive 103 kB
    First Load JS unit** (extends from 116 at Phase-51 close).
12. **Maximal multi-consumer all-surfaces composition extended
    to 5-consumer + 3-consumer-same-slot cardinality** —
    defines the new upper bound for the Phase-37-framework's
    current capabilities. 5 consumers × 4 surfaces × 3 slots
    = 60 component-surface-slot positions; **20 active under
    5-way default** (12 single-occupancy + 4 arxiv-doi same-
    slot extras + 4 pubmed same-slot extras). Future expansions
    (6th consumer or 5th surface) extend the dimensions, not
    the cell density.

## Phase 52 → Phase 53 entry conditions

Per §12, Phase 53 entry requires **explicit human sign-off**.

**Recommended Phase 53 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **Older-style category-prefixed arxiv IDs** (APPEND-D-Y item 2; e.g., `arxiv:math/0211159`) — regex extension on existing `remarkLinkArxivIds`; closes APPEND-D-Y item 2 at **12-phase carryover** (Phase 41 → 53); longest open deferral in the closure cadence | 1-2 | Autonomous-tractable; second regex evolution on arxiv plugin |
| 2 | **ORCID auto-link consumer** (`orcid:NNNN-NNNN-NNNN-NNNN`) — **sixth concrete consumer** in the Phase-37 framework; **first 4th-`remarkPlugins` consumer**; tests whether regex-disjointness-as-sole-defense scales from 3 to 4 same-slot consumers | 2-3 | Autonomous-tractable; mirrors Phase-41/45/50 first-consumer shape |
| 3 | **Table-specific attributes** (`colspan` / `rowspan` / `scope`; APPEND-D-Q item 3) — schema-override extension on existing `TablesExtensionRegistry`; first regex/schema-extension equivalent on `schemaOverrides` slot | 1-2 | XSS-audit-required; schema-extension only |
| 4 | **bioRxiv preprint consumer** (`biorxiv:` or DOI-overlap with doi consumer) — sixth concrete consumer (alternative to ORCID); raises 4th-`remarkPlugins` consumer question | 2-3 | DOI overlap concern requires regex disambiguation |
| 5 | **Cross-entity wikilinks** (APPEND-D-L item 3) — entity-type disambiguation + plugin parameterization | 2-3 | Requires APPEND-D-L item 6 plugin parameterization to land first |
| 6 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — couples with `schemaOverride` for `class` attribute on `<a>`; first single-consumer-multi-slot case | 1-2 | XSS audit per attribute value |
| 7 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 8 | **Bare arxiv / DOI / PubMed IDs without prefix** (APPEND-D-Y item 3 + APPEND-D-AC + new D-AH carries) — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 9 | **Empty-alias fallback unification** across consumers (Phase-48 deferral carries) — refine arxiv/doi/pubmed empty-alias divergence | 1 | Smallest-scope autonomous-tractable |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1195 / 73 files**.
- `pnpm audit-content` — 0 errors / 6 warnings (48 consecutive
  phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (120 consecutive units
  at Unit 52.4 ship).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 53+ deferrals)

NOT older-style category-prefixed arxiv IDs (Phase 53+ rank 1);
NOT ORCID consumer (Phase 53+ rank 2);
NOT table-specific attributes (Phase 53+ rank 3);
NOT bioRxiv preprint consumer (Phase 53+ rank 4);
NOT cross-entity wikilinks (Phase 53+ rank 5);
NOT `<a class="wikilink">` styling (Phase 53+ rank 6);
NOT 404 handling (Phase 53+ rank 7);
NOT bare arxiv / DOI / PubMed IDs without prefix (Phase 53+
rank 8);
NOT empty-alias fallback unification (Phase 53+ rank 9);
NOT OSF preprint consumer (Phase 53+);
NOT dx.doi.org legacy host (Phase 53+);
NOT stricter trailing-lookahead (Phase 53+);
NOT paper-card hover-preview (Phase 53+);
NOT plugin parameterization (Phase 53+);
NOT auto-trim of alias display whitespace (Phase 53+);
NOT `<caption>` (Phase 53+);
NOT surface-specific table schemas (Phase 53+);
NOT `@mention` (Phase 53+);
NOT 4th-or-later `remarkPlugins` consumer beyond arxiv + doi +
pubmed (Phase 53+ rank 2 would ship ORCID; rank 4 would ship
bioRxiv);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 53+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 53+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-51 deferrals carry forward.

## Boundary statement

**Phase 52 CLOSED at HEAD `<this-unit>` after 5 numbered units
(52.0 prep + 52.1 cross-surface expansion + dispatch tests +
APPEND-D-AJ + 52.2 21 NEW end-to-end tests + first all-4-
surfaces quadruple-alias + first all-4-surfaces 3-consumer
same-slot composition + 52.3 hygiene + 52.4 gate). Fifth
realization of the "constructor-arg-only zero-rework expansion"
property — completes the per-consumer all-4-surfaces arc for
ALL 5 Phase-37-framework consumers (Phase 42 wikilinks; Phase
43 tables; Phase 44 arxiv; Phase 49 doi; Phase 52 pubmed).
First 5-realization property in project history (extends
Phase-49 4-realization record to 5). First "all 4 surfaces are
quadruple-alias" state — every surface carries wikilinks +
arxiv + doi + pubmed aliases simultaneously under 5-way
default. First surface-with-4-alias-consumers cardinality of 4
in project history. First "all 4 surfaces have 3-consumer
same-slot composition" state — the arxiv-doi-pubmed triple in
`remarkPlugins` now active on every surface under
`arxiv,doi,pubmed` composite default; regex-disjointness-as-
sole-defense discipline at 3-consumer cardinality (Phase 50
established) exercised everywhere in production default. First
"all 4 surfaces with 5-consumer composition under default
dispatch" state — maximum-consumer-cardinality state
generalized to all surfaces. Maximal multi-consumer all-
surfaces composition extended to 5-consumer + 3-consumer-same-
slot cardinality — 20 active component-surface-slot positions
under 5-way default (12 single-occupancy + 4 arxiv-doi same-
slot extras + 4 pubmed same-slot extras). FASTEST CROSS-
SURFACE-EXPANSION APPEND-DEFERRAL CLOSURE EVER OBSERVED at
2-phase carryover (Phase 50 → 52; beats prior 3-phase Phase-
41 → 44 record). First cross-surface-expansion closure under
the 3-phase floor. Cadence trajectory for the five cross-
surface-expansion realizations: 4 → 4 → 3 → 4 → 2 phases.
Fifth cross-surface-expansion APPEND-deferral closure in the
cadence (Phase 42 + 43 + 44 + 49 + 52). Closes ADR-0018 APPEND-
D-AH PubMed PMID cross-surface item at 2-phase carryover (Phase
50 → 52). Second D-clause with BOTH items closed within the
closure cadence — D-AH carries alias (closed Phase 51 via
D-AI) + cross-surface (closes Phase 52 via D-AJ); D-AC was
first (Phase 48 + 49); D-AH is second. Both-items closure
window halved across the two examples (4 phases → 2 phases).
APPEND-deferral closure cadence sustained 11 phases — new
longest sustained cadence in project history (extends Phase-51
record 10 → 11). First 11-phase APPEND-deferral closure run.
19th APPEND on ADR-0018 D-G (project record extends 18 → 19).
Tenth two-letter APPEND letter D-AJ. Forty-third NON-§13
phase. First "framework + 5 consumers + composition + 5
expansions + 3-consumer same-slot composition × 4 + alias
syntax × 4 + dual-form regex × 3 + 5-consumer composition × 4"
16-phase cluster in project history (Phase 37-52). 22nd
consecutive phase without new B category — first 22-phase run
(extends Phase-51 record 21 → 22). 120th consecutive 103 kB
First Load JS unit (extends from 116 at Phase-51 close). XSS
line of defense preserved. 1195 tests across 73 vitest files
(+21/0 vs Phase 51). 24 ADRs UNCHANGED. 7 DB tables UNCHANGED.
9 migrations UNCHANGED. 14 env vars UNCHANGED. 168 i18n keys
per locale UNCHANGED. First Load JS 103 kB UNCHANGED.
Middleware 160 kB UNCHANGED. OPEN_QUESTIONS top-level Q-count
66 UNCHANGED. Phase-37+ candidate count 8 UNCHANGED.
`MARKDOWN_EXTENSIONS` recognized single-value arms 6 UNCHANGED.
Zero new ADRs / migrations / i18n keys / env vars / operational
gates / runtime deps / files. Phase 53 entry requires explicit
human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 51 → 52 delta complete.
- [x] Phase 53 entry conditions documented (10 ranked
      candidates).
- [x] Smoke gates pass.
- [x] STOP at Phase 53 boundary per §12.
