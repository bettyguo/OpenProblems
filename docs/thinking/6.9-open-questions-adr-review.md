# 6.9 — OPEN_QUESTIONS hygiene + ADR review (Phase 6 close)

Eighth code-track unit of Phase 6. Docs-only. Reconciles the OPEN_QUESTIONS.md ledger against Phase-6 outcomes; refines status flags; surveys ADR-0001 through 0010 for drift introduced by Phase 6's surfaces. Mirrors the Phase-5 Unit 5.12 framing.

## THINK

### What changed in OPEN_QUESTIONS this unit

| Q-id | Before this unit          | After this unit                  | Reason                                                                                                                          |
|------|---------------------------|----------------------------------|---------------------------------------------------------------------------------------------------------------------------------|
| Q46  | decided (Unit 6.1)        | decided (unchanged)              | Already closed by ADR-0010 in Unit 6.1; no refinement needed.                                                                   |
| Q47  | open (operational)        | open (reaffirmed; graceful-degradation lineage cataloged) | Phase 6 added 4 read surfaces (Units 6.2 / 6.4 / 6.5 / 6.6); all gracefully degrade without Q47. Documented the unblock sequence: repo-settings → giscus.app config → `NEXT_PUBLIC_GISCUS_REPO_ID` env → `GITHUB_TOKEN` in CI. |
| Q48  | decided-as-lean           | **partially-resolved**           | Unit 6.3 + 6.5 closed the "linked from problem detail" half (with count upgrade). The sitemap half stays open — no sitemap surface exists at HEAD. Lean carried forward.    |
| Q49  | decided-as-lean           | **decided** (resolved via ADR-0010 D-F) | ADR-0010 D-F codifies the lean as the working contract; the OPEN_QUESTIONS entry is now the cross-reference rather than the locus. Matches the Q46 / ADR-0010 pattern.    |

### Pre-Phase-6 questions not touched by Phase 6

These remain in their prior status, unchanged by Phase 6's work:

- **Q2 / Q3** (DNS / domain registration) — still open. Phase 6 didn't add a deploy surface that needed a real domain.
- **Q6 – Q11 / Q14 – Q17 / Q19** — Phase-0/1 deferred decisions still open.
- **Q25** — JSON envelope shape: closed at the `/api/v1/ratings` level (Phase 3); broader question still open. Phase 6's RSS feeds are XML; doesn't affect Q25.
- **Q26 / Q28 – Q31** — Phase-1/2 deferred decisions still open. **Q31** (Velite + Zod 4 incompat) still survives in `velite.config.ts` — Phase 6 added no new Velite collections.
- **Q33** — RSS `dc:creator` framing — coupled to Q2. Phase 6's per-domain digest feeds inherit this gating (Unit 6.6 doesn't add `<dc:creator>` to discussion items either).
- **Q37** — Issue-template form-field schemas — Phase-4 deferred. Phase 6 didn't add new issue templates.
- **Q34 / Q35 / Q36 / Q38 / Q39 / Q42 / Q44** (decided-as-lean) — none touched by Phase 6.

### ADR review (0001 – 0010)

No drift introduced by Phase 6's surfaces. Verified relationships:

| ADR  | Title                                                        | Phase-6 interaction                                                                                                                                            |
|------|--------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 0001 | Next.js 15 App Router                                        | Held. Talk-page route (Unit 6.3) is a standard App Router dynamic segment + `generateStaticParams`; no convention drift.                                          |
| 0002 | Velite for MDX                                               | Held. Phase 6 added no new Velite collections.                                                                                                                  |
| 0003 | Zod 4                                                        | Held. Phase 6 added no new schemas (`IndexedProblem` interface extended is TS-only; no Zod schema lives alongside it).                                            |
| 0004 | File-first; no DB                                            | Held. `.github-cache/` is a build-time sidecar (matches the `.arxiv-cache/` / `.pdf-cache/` precedent); no first-party DB. Confirmed by Unit 6.0 D-2 trigger eval (1.434% of 5 MB). |
| 0005 | Rating-action immutability                                   | Held. Phase 6 added no rating actions. Discussions are community-authored, immutability doesn't apply.                                                            |
| 0006 | Saturation N/A encoding                                      | Held. Phase 6 didn't touch the rating-dimension schema.                                                                                                          |
| 0007 | DomainMap rendering + D3 import policy                       | Held. Phase 6 didn't touch any viz. `d3-selection` still installed but not client-imported.                                                                       |
| 0008 | LLM provider (Anthropic) + cost-governance pact              | Held. Phase 6 made no LLM calls; the `.llm-spend.log` is untouched by Phase 6 work.                                                                              |
| 0009 | Human-review diff format                                     | Held. Phase 6 didn't write any drafts; the `drafts/` directory is untouched by Phase 6 work.                                                                     |
| 0010 | Discussions backend (Giscus + GraphQL read-side)             | **New this phase.** Implementations in Units 6.2 / 6.3 / 6.4 / 6.5 / 6.6 all conform to the six D-A through D-F contracts; the env-safe wrappers (`tryGetDiscussionByPath`, `tryGetRecentDiscussionActivity`) added during Units 6.5 / 6.6 are mechanically faithful to D-D (fail-soft when `GITHUB_TOKEN` is unset). No supersession needed. |

### Cross-phase invariants reconfirmed

- **First Load JS shared chunk** = **103 kB** through every Phase-6 unit. Verified at the build smoke in Units 6.2 / 6.3 / 6.4 / 6.5 / 6.6. The Giscus React wrapper adds ~2 kB to the `/problems/[slug]/talk` route chunk only (per ADR-0010 D-A bundle isolation).
- **Test count** at HEAD = **323/323 across 38 files** (Phase-6 added 39 tests on top of the Phase-5-close baseline of 284). Confirmed at Units 6.7 / 6.8 smoke (test files untouched in those units).
- **Route count** at HEAD = **333 prerendered pages** (Phase-6 +10 from Unit 6.3 `/problems/<slug>/talk` SSG routes). Phase 5 closed at 322; Unit 5.13b added 1 for `/contributing/v1.1.0`; Unit 6.3 added 10. 322 + 1 + 10 = 333 ✓.
- **`pnpm audit-content`** = 0 errors / 6 warnings (Q32-expected `related-problems-symmetry` baseline since Phase 2; no change through Phase 6).

### Smoke gates (this unit)

Docs-only:
- File added: this THINK doc.
- File updated: OPEN_QUESTIONS.md (Q47 + Q48 + Q49 refined).
- CHANGELOG: Unit 6.9 entry.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline; unchanged).
- No code touched; typecheck / test / build untouched.

### CHANGELOG entry shape

Per-unit entry: surfaces Q46-Q49 final status at Phase-6 close (one resolved this unit: Q49 → ADR-0010 D-F; one partially-resolved: Q48 link-half done, sitemap-half open; two unchanged); confirms 7 ADRs unchanged + ADR-0010 conforming + no ADR supersession needed; reconfirms the cross-phase invariants (103 kB / 323 tests / 333 routes / 0 errors / 6 warnings) at HEAD; lists pre-Phase-6 questions not touched by this phase.
