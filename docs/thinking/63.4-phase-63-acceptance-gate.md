# 63.4 — Phase 63 acceptance gate

Phase 63 closes at HEAD `be5e7dc` (Unit 63.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-64-entry
conditions per §12.

## Phase 63 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 63.0 | Phase 63 prep (rank-1 cross-entity wikilinks; consume Phase-62 affordance) | docs | `6c8f645` |
| 63.1 | `WIKILINK_PATTERN` regex extension (`(?:(entity-type):)?` prefix group) + `ResolveWikilinksOptions.buildHref` signature extension `(slug, entityType?) => string` + new exported `CROSS_ENTITY_BUILD_HREF` constant + 11 NEW Phase-63 plugin-body tests + 1 updated Phase-62 spy test + ADR-0018 D-G APPEND-D-AU | code+APPEND | `515fb39` |
| 63.2 | `WikilinkExtensionRegistry` constructor evolution (optional `{ buildHref }` ctor arg + bare/tuple emit branching) + new factory dispatch arm `wikilinks-cross-entity` (8 → 9 single-value arms) + 6 NEW registry-tied tests in `wikilinks.test.ts` + 4 NEW factory-dispatch tests in `index.test.ts` | code | `0d7ea1d` |
| 63.3 | Phase-63 hygiene + ADR-0018 APPEND-D-L item 3 closure documentation (25-phase carryover; NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE RECORD; third consecutive phase to set absolute-record; first 3-consecutive-phase absolute-record-extension streak) | docs | `be5e7dc` |
| 63.4 | Phase 63 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-62 5-unit framework shape
verbatim.

## First consumer of a forward-compat plugin-option affordance shipped in a prior phase + first 2-phase forward-compat-affordance prerequisite-fulfillment arc completed + first registry-level realization of the plugin-option axis + first 2-realization for the plugin-option-axis + wikilinks 4th evolution post-first-ship + first state where a consumer has 4+ evolutions + D-L becomes first D-clause with 4-of-6 items closed + first D-clause with 4+ items closed + NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE RECORD at 25-phase carryover + third consecutive phase to set absolute-record + first 3-consecutive-phase absolute-record-extension streak + 22-phase APPEND-deferral closure cadence + first new MARKDOWN_EXTENSIONS single-value arm since Phase 58 bioRxiv + first 5-phase gap between single-value-arm additions

- **Thread choice**: Phase-62 acceptance gate's Phase-63-entry table
  ranked **cross-entity wikilinks** as rank 1 (the most-defensible
  autonomous-tractable next thread; consumes Phase-62 affordance;
  completes first 2-phase forward-compat-affordance arc). Phase 63
  ships rank 1. Alternative framings considered and rejected: (a)
  bundling cross-entity routing as a default-arm semantic change
  (would break Phase-62 `DEFAULT_BUILD_HREF` byte-identity
  contract); (b) rank 2 surface-specific table schemas (D-Q all-
  items-closed milestone; strong but does not complete the
  Phase-62 prerequisite arc, so deferred to Phase 64+).
- **`WIKILINK_PATTERN` regex extension** in
  `lib/markdown/extensions/wikilinks.ts`:
  - Before (Phase 46 → Phase-62 close): `/\[\[([a-z0-9-]+)(?:\|([^\]\n]+))?\]\]/g` — captures (1) slug, (2) optional alias.
  - After (Phase 63 ship): `/\[\[(?:([a-z0-9-]+):)?([a-z0-9-]+)(?:\|([^\]\n]+))?\]\]/g` — captures (1) optional entity-type prefix, (2) slug, (3) optional alias. Backwards-compat preserved: every Phase-38 / Phase-46 wikilink shape continues to match with entityType undefined.
- **`buildHref` signature extension**: `(slug: string) => string` → `(slug: string, entityType?: string) => string`. TypeScript optional-param widening preserves Phase-62 1-arg builder backwards-compat.
- **`DEFAULT_BUILD_HREF` UNCHANGED** (Phase-62 byte-identity contract preserved): 1-arg signature; entityType silently dropped when plugin invokes it. Under bare `WikilinkExtensionRegistry` default-call, `[[paper:x]]` routes to `/problems/x` (legacy fallback semantics for `wikilinks` arm).
- **New `CROSS_ENTITY_BUILD_HREF`** exported constant: routes `paper` → `/papers/{slug}`, `author` → `/authors/{slug}`, `institution` → `/institutions/{slug}`, fallback (undefined or unknown entityType) → `/problems/{slug}` (graceful degradation).
- **`WikilinkExtensionRegistry` constructor evolution** (Unit 63.2): adds optional second arg `options: { buildHref? }`. `buildHref === undefined` → bare-form emit (Phase 62 invariant preserved); `buildHref` set → tuple-form emit `[[rehypeResolveWikilinks, { buildHref }]]`.
- **New factory dispatch arm `wikilinks-cross-entity`** (Unit 63.2): wires `CROSS_ENTITY_BUILD_HREF`. `MARKDOWN_EXTENSIONS` single-value arms: 8 → **9**. First new arm since Phase 58 bioRxiv.
- 11 NEW tests in `wikilinks.test.ts` (Unit 63.1; plugin-body) + 1 updated Phase-62 spy test (signature evolution) + 6 NEW tests in `wikilinks.test.ts` (Unit 63.2; registry) + 4 NEW tests in `index.test.ts` (Unit 63.2; factory dispatch) = 22 NEW Phase-63 tests across two files (+ 1 updated Phase-62 test for forward-compat).
- ADR-0018 D-G APPEND adds Phase-63 EXTENDED block + **APPEND-D-AU**
  (Unit 63.1; twenty-first two-letter slot).
- **APPEND-D-L item 3 `Cross-entity wikilinks` CLOSED at 25-phase
  carryover** (Phase 38 → 63). **NEW LONGEST ABSOLUTE APPEND-
  DEFERRAL CLOSURE EVER OBSERVED** (extends Phase-62 24-phase
  record by 1 phase). **Third consecutive phase to set the
  absolute-record** — first 3-consecutive-phase absolute-record-
  extension streak (Phase 61 = 22-phase; Phase 62 = 24-phase;
  Phase 63 = 25-phase).
- **D-L becomes first D-clause with 4-of-6 enumerated items
  closed** (items 1 + 2 + 3 + 6 closed; items 4 + 5 remaining).
  **First D-clause with 4+ items closed** in project history.
- **Wikilinks consumer 4th evolution** post-first-ship (Phase 42
  + 46 + 62 + 63). **First state where a consumer has 4+
  evolutions** in project history.
- **First consumer of a forward-compat plugin-option affordance
  shipped in a prior phase** + **first 2-phase forward-compat-
  affordance prerequisite-fulfillment arc completed**.
- **First registry-level realization of the plugin-option axis**
  (Phase 62 introduced the axis as a signature affordance; Phase
  63 Unit 63.2 ships the first registry that emits tuple-form).
  **First 2-realization for the plugin-option-axis** (Phase 62
  affordance + Phase 63 registry consumer).
- **22-phase APPEND-deferral closure cadence sustained** (Phase
  42-63) — new longest sustained cadence (extends Phase-62
  record 21 → 22).
- **First new `MARKDOWN_EXTENSIONS` single-value arm since Phase
  58 bioRxiv** (`wikilinks-cross-entity`); first 5-phase gap
  between single-value-arm additions (Phase 58 → 63).
- **Fifty-fourth NON-§13 phase** (extends Phase-62 milestone to
  54); first 54-phase ledger-closure streak.

## Phase 62 → Phase 63 delta

| Metric | Phase 62 close | Phase 63 close | Δ |
|---|---|---|---|
| Tests | 1464 / 75 files | **1486 / 75 files** | +22 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 29 | **30** (project record extends; twenty-first two-letter slot D-AU in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| **`MARKDOWN_EXTENSIONS` single-value arms** | **8** | **9 (+wikilinks-cross-entity)** | **+1 (first new arm since Phase 58 bioRxiv; first 5-phase gap)** |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 21 | **21** | 0 |
| **Phase-37-framework concrete consumers** | **7** | **7** | 0 |
| **Plugin-regex-extension realizations** | **7** | **7** | 0 (Phase-63 regex extension counted under cross-entity phase-shape, not plugin-regex-extension axis) |
| **Constructor-arg-only zero-rework expansion realizations** | **7** | **7** | 0 |
| **Schema-extension realizations** | **2** | **2** | 0 |
| **Plugin-option-extension realizations** | **1** | **2 (+wikilinks cross-entity)** | **+1 (first 2-realization for plugin-option-axis; first registry-level realization)** |
| **Dual-form regex realizations** | **5** | **5** | 0 |
| **Principal axes of zero-rework framework extension** | **3** | **3** | 0 (count unchanged; plugin-option axis gains first registry-level realization) |
| **Consumers with 3+ evolutions** | **3 (arxiv + tables + wikilinks)** | **3** | 0 (wikilinks evolution count moves from 3 → 4) |
| **Consumers with 4+ evolutions** | **0** | **1 (wikilinks)** | **+1 (first state where a consumer has 4+ evolutions)** |
| **Surfaces with sextuple-alias** | **4** | **4** | 0 |
| **D-clauses with BOTH items closed** | **4** | **4** | 0 |
| **D-clauses with 3+ items closed** | **2 (D-Q + D-L)** | **2** | 0 (D-L count moves from 3 → 4 items closed) |
| **D-clauses with 4+ items closed** | **0** | **1 (D-L)** | **+1 (first D-clause with 4+ items closed)** |
| **APPEND-deferral closure cadence (consecutive phases)** | 21 | **22 (Phase 42-63)** | **+1 (new record)** |
| **Longest absolute APPEND-deferral closure carryover** | 24-phase (Phase 62 D-L item 6) | **25-phase (Phase 63 D-L item 3) — new record** | **+1 phase (third consecutive phase to set absolute-record; first 3-consecutive-phase absolute-record-extension streak)** |
| **Refactor-only phases** | 1 (Phase 62) | **1** | 0 (Phase 63 is forward-compat-affordance-consumer phase, not refactor-only) |
| **Plugin-option-ready-before-consumer-demand realizations** | 1 (Phase 62) | **1** | 0 |
| **Forward-compat-affordance prerequisite-fulfillment arcs completed** | 0 | **1 (Phase 62 → Phase 63)** | **+1 (first such arc completed)** |
| **NON-§13 phase count** | 53 | **54** | **+1** |
| **"Continue" override invocations past half-century threshold** | **7** | **8 (+Phase 63 = 58th)** | **+1** |

## Architectural firsts in Phase 63 (12 enumerated)

1. **First consumer of a forward-compat plugin-option affordance
   shipped in a prior phase** in project history. Phase 62
   shipped the `ResolveWikilinksOptions.buildHref` affordance as
   plugin-option-ready-before-consumer-demand realization
   (forward-compat for an unknown future consumer). Phase 63 IS
   the first consumer via `CROSS_ENTITY_BUILD_HREF` + new factory
   arm. **First 2-phase forward-compat-affordance prerequisite-
   fulfillment arc completed** in project history (Phase 62
   prerequisite ship → Phase 63 consumption ship). Validates the
   Phase-62 prediction.
2. **First registry-level realization of the plugin-option axis**
   in project history. Phase 62 introduced the axis as a plugin-
   signature affordance (the bare `WikilinkExtensionRegistry`
   continued to emit `rehypePlugins: [rehypeResolveWikilinks]`
   without options). Phase 63 Unit 63.2 ships the first registry
   that constructs with `{ buildHref: CROSS_ENTITY_BUILD_HREF }`
   and emits tuple-form. **First 2-realization for the plugin-
   option-axis** (Phase 62 affordance + Phase 63 registry
   consumer).
3. **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED**
   at **25-phase carryover** (Phase 38 → Phase 63). Extends
   Phase-62 24-phase record by 1 phase. APPEND-D-L closure
   trajectory: item 1 cross-surface 4-phase (Phase 42); item 2
   alias 8-phase (Phase 46); item 6 plugin parameterization
   24-phase (Phase 62); item 3 cross-entity **25-phase** (Phase
   63). **Third consecutive phase to set the absolute-record**
   — first 3-consecutive-phase absolute-record-extension streak
   in project history (Phase 61 22-phase → Phase 62 24-phase →
   Phase 63 25-phase).
4. **D-L becomes first D-clause with 4-of-6 enumerated items
   closed** (items 1 + 2 + 3 + 6 closed; items 4 + 5 remaining).
   **First D-clause with 4+ items closed** in project history.
   Prior multi-item D-clauses reached 3-of-N at Phase 61 (D-Q)
   and Phase 62 (D-L). D-L at 4-of-6 establishes a new depth
   for multi-item D-clause resolution.
5. **22-phase APPEND-deferral closure cadence sustained** (Phase
   42-63) — new longest sustained cadence in project history
   (extends Phase-62 record 21 → 22). **First 22-phase APPEND-
   deferral closure run** in project history.
6. **Wikilinks consumer gains 4th evolution post-first-ship**
   (Phase 42 cross-surface + Phase 46 alias + Phase 62 plugin
   parameterization + Phase 63 cross-entity). **First state
   where a consumer has 4+ evolutions** in project history.
   Wikilinks becomes the deepest-evolved consumer in the
   framework (arxiv + tables remain at 3 evolutions).
7. **Twenty-second prep-/APPEND-doc-level deferral closed by a
   later phase**: Phase 42 → 38 D-L item 1; …; Phase 62 → 38
   D-L item 6; **Phase 63 → 38 D-L item 3 Cross-entity
   wikilinks**.
8. **Thirtieth APPEND on ADR-0018 D-G** — extends the **first-
   ADR-D-clause-with-most-APPENDs record** from 29 → 30.
   **Twenty-first two-letter APPEND letter D-AU** (Excel-
   spreadsheet column convention sustained).
9. **Fifty-fourth NON-§13 phase** (extends Phase-62 half-
   century milestone to 54). The §13 ledger remains CLOSED
   across the entire 54-phase run (Phase 10-63). **First 54-
   phase ledger-closure streak**.
10. **First new `MARKDOWN_EXTENSIONS` single-value arm since
    Phase 58 bioRxiv** (`wikilinks-cross-entity`). 8 → 9 arms.
    **First 5-phase gap between single-value-arm additions**
    (Phase 58 → 63; longest gap to date).
11. **First "framework + 7 consumers + 7 expansions + 7
    plugin-regex-extensions + 2 schema-extensions + 2
    plugin-option-extensions + 9 `MARKDOWN_EXTENSIONS`
    single-value arms + 4-D-clauses-both-items-closed +
    D-clause-with-3-items-closed × 2 + D-clause-with-4-
    items-closed × 1 + consumer-with-4+-evolutions × 1 +
    first-forward-compat-affordance-arc-completed × 1 +
    first-registry-level-plugin-option-realization × 1 +
    three-consecutive-absolute-record-extension × 1 +
    plugin-option-ready-before-consumer-demand × 1 +
    refactor-only-phase × 1 + first-forward-compat-
    affordance-consumer × 1" 27-phase cluster** in project
    history (Phase 37-63). 27-phase architectural arc. Plus
    **33rd consecutive phase without new B category** (Phase
    31-63; extends Phase-62 record 32 → 33). Plus **173rd
    consecutive 103 kB First Load JS unit** (extends from
    169 at Phase-62 close). Plus **28th consecutive no-new-
    ADR phase** (Phase 36-63; extends Phase-62 record 27 →
    28).
12. **58th "Continue" override invocation** at Unit 63.0 —
    extends the **half-century-of-Continue-overrides
    milestone** (Phase 55 = 50th) to **58 over Phases
    6-63**. **Eighth invocation past the half-century
    threshold** — confirms the §12-cardinal-rule sign-off
    discipline remains durable post-milestone across 9
    successive phases.

## Phase 63 → Phase 64 entry conditions

Per §12, Phase 64 entry requires **explicit human sign-off**.

**Framework state note**: Phase 63 completed the first 2-phase
forward-compat-affordance prerequisite-fulfillment arc (Phase
62 plugin parameterization → Phase 63 cross-entity consumer).
The plugin-option axis is now exercised at both signature
(Phase 62) and registry (Phase 63) layers. Phase 64+
candidates can introduce new realizations of any existing
axis, complete D-L closure (items 4 + 5 still deferred), close
the last D-Q item (item 6 surface-specific table schemas),
introduce new consumers, or address pending operational
gates.

**Recommended Phase 64 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| **1** | **Surface-specific table schemas** (APPEND-D-Q item 6 — last remaining D-Q deferral) — closure would make D-Q the **first D-clause with ALL enumerated items closed** in project history | 1-2 | Autonomous-tractable; clean "architectural finishing move" for D-Q's multi-item cadence |
| 2 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — would advance D-L from 4-of-6 → 5-of-6; requires either framework refactor (multi-source schemaOverrides support) OR plugin-only emit pattern (no schemaOverride; rely on post-sanitize plugin-order discipline) | 1-2 | XSS audit per attribute value; framework decision required on multi-source schemaOverrides |
| 3 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — would close D-L (5-of-6 items if pursued after rank 2; 5-of-6 standalone otherwise); build-time validation against `content/problems/` + `content/papers/` + `content/authors/` + `content/institutions/` (cross-entity-aware validation per Phase 63 ship) + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 4 | **Empty-alias fallback unification** across consumers (Phase-48 deferral) | 1 | Smallest-scope autonomous-tractable |
| 5 | **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 6 | **Legacy numeric-only bioRxiv IDs** (pre-2019 format) | 1-2 | Autonomous-tractable; small-scope; would be 8th plugin-regex-extension |
| 7 | **OSF preprint consumer** — eighth concrete consumer | 2-3 | DOI overlap concern |
| 8 | **Locale-prefixed cross-entity wikilink paths** — concrete `buildHref` variant that produces `/{locale}/papers/${slug}` etc; demonstrates second cross-entity builder variant | 1-2 | Tractable; would extend Phase 63 affordance to a second builder realization |
| 9 | **Cross-entity entity-type expansion** (beyond paper / author / institution) — e.g., problem-tags or rating-actions; extends `CROSS_ENTITY_BUILD_HREF` routing table | 1 | Autonomous-tractable; small-scope |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Most defensible autonomous-tractable next thread**: Rank 1
(**surface-specific table schemas**) — would close the **last
remaining D-Q deferral** (item 6) and make D-Q the **first
D-clause with ALL enumerated items closed** in project history.
Rank 1 is the cleanest "architectural finishing move" for D-Q's
multi-item cadence, complementing the Phase-63 D-L 4-of-6
advance. After Phase 64+ ranks 1-3 (table schemas → wikilink-
class styling → 404 handling), all three of D-Q, D-L, and the
wikilinks consumer-evolution depth would reach plausible
"complete" states.

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1486 / 75 files** (+22 / 0 vs Phase 62
  close).
- `pnpm audit-content` — 0 errors / 6 warnings (59
  consecutive phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (173 consecutive units
  at Unit 63.4 ship; from 169 at Phase 62 close + 4 Phase-63
  units).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 64+ deferrals)

NOT surface-specific table schemas (Phase 64+ rank 1; D-Q
closure);
NOT `<a class="wikilink">` styling (Phase 64+ rank 2; framework
refactor pending);
NOT 404 handling for unresolved wikilinks (Phase 64+ rank 3);
NOT empty-alias fallback unification (Phase 64+ rank 4);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 64+ rank 5);
NOT legacy numeric-only bioRxiv IDs (Phase 64+ rank 6);
NOT OSF preprint consumer (Phase 64+ rank 7);
NOT locale-prefixed cross-entity wikilink paths (Phase 64+
rank 8);
NOT cross-entity entity-type expansion beyond paper / author /
institution (Phase 64+ rank 9);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi
+ pubmed + orcid + biorxiv (Phase 64+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 64+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 64+;
requires framework refactor per the composite-registry single-
source rule);
NOT dx.doi.org legacy host (Phase 64+);
NOT stricter trailing-lookahead (Phase 64+);
NOT paper-card hover-preview (Phase 64+);
NOT auto-trim of alias display whitespace (Phase 64+);
NOT a future plugin that EMITS `<caption>` or
`colSpan`/`rowSpan`/`scope` (Phase 64+);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 64+);
NOT curator-facing builder configuration (Phase 64+);
NOT build-time validation of captured entity-type against
content/papers / content/authors / content/institutions
(Phase 64+);
NOT relative-path wikilink variants (Phase 64+);
NOT `@mention` (Phase 64+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-62 deferrals carry forward.

## Boundary statement

**Phase 63 CLOSED at HEAD `<this-unit>` after 5 numbered units
(63.0 prep + 63.1 `WIKILINK_PATTERN` regex extension
(`(?:(entity-type):)?` prefix group) + `ResolveWikilinksOptions.buildHref`
signature extension `(slug, entityType?) => string` + new
exported `CROSS_ENTITY_BUILD_HREF` constant routing per entity-
type + 11 NEW plugin-body tests + 1 updated Phase-62 spy test
+ APPEND-D-AU + 63.2 `WikilinkExtensionRegistry` constructor
evolution (optional `{ buildHref }` ctor arg + bare/tuple emit
branching) + new factory dispatch arm `wikilinks-cross-entity`
(8 → 9 single-value arms) + 6 NEW registry-tied tests + 4 NEW
factory-dispatch tests + 63.3 hygiene + 63.4 gate). **First
consumer of a forward-compat plugin-option affordance shipped
in a prior phase** in project history — Phase 62 shipped the
`buildHref` affordance as plugin-option-ready-before-consumer-
demand realization; Phase 63 IS the first consumer via
`CROSS_ENTITY_BUILD_HREF` + new factory arm. **First 2-phase
forward-compat-affordance prerequisite-fulfillment arc
completed** in project history (Phase 62 prerequisite ship →
Phase 63 consumption ship). **First registry-level realization
of the plugin-option axis** — Phase 62 was a signature-only
ship (bare-form emit preserved); Phase 63 ships the first
registry that constructs with `{ buildHref: CROSS_ENTITY_BUILD_HREF }`
and emits tuple-form. **First 2-realization for the plugin-
option-axis** (Phase 62 affordance + Phase 63 registry
consumer). **Wikilinks consumer gains 4th evolution post-
first-ship** (Phase 42 cross-surface + Phase 46 alias + Phase
62 plugin parameterization + Phase 63 cross-entity); **first
state where a consumer has 4+ evolutions** in project
history. **Closes ADR-0018 APPEND-D-L item 3 `Cross-entity
wikilinks` at 25-phase carryover** (Phase 38 → Phase 63) —
**NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED**
(extends Phase-62 24-phase record by 1 phase). **Third
consecutive phase to set the absolute-record** — first 3-
consecutive-phase absolute-record-extension streak in project
history. APPEND-D-L closure trajectory: item 1 cross-surface
4-phase (Phase 42); item 2 alias 8-phase (Phase 46); item 6
plugin parameterization 24-phase (Phase 62); item 3 cross-
entity 25-phase (Phase 63); items 4 (`<a class="wikilink">`
styling) + 5 (404 handling) still deferred (Phase 64+). **D-L
becomes first D-clause with 4-of-6 enumerated items closed**
(items 1 + 2 + 3 + 6 closed); **first D-clause with 4+ items
closed** in project history. **APPEND-deferral closure cadence
sustained 22 phases** — new longest sustained cadence in
project history (extends Phase-62 record 21 → 22); first 22-
phase APPEND-deferral closure run. **New
`MARKDOWN_EXTENSIONS=wikilinks-cross-entity` single-value
arm** — first new arm since Phase 58 bioRxiv; first 5-phase
gap between single-value-arm additions (Phase 58 → 63); 8 → 9
arms. Composition preserved (plugin-option axis is per-plugin-
instance; existing `wikilinks` arm emits bare form, Phase 62
invariant preserved; new `wikilinks-cross-entity` arm emits
tuple form with `CROSS_ENTITY_BUILD_HREF`). XSS defenses
preserved (slug + entityType regex-validated to `[a-z0-9-]+`
per APPEND-D-I; both builders produce relative paths only; no
new XSS surface). 30th APPEND on ADR-0018 D-G (project record
extends 29 → 30). Twenty-first two-letter APPEND letter D-AU.
Fifty-fourth NON-§13 phase (extends Phase-62 half-century
milestone to 54); first 54-phase ledger-closure streak. 33rd
consecutive phase without new B category — first 33-phase run
(extends Phase-62 record 32 → 33). 28th consecutive no-new-ADR
phase (Phase 36-63; extends Phase-62 record 27 → 28). 173rd
consecutive 103 kB First Load JS unit (extends from 169 at
Phase-62 close). 58th "Continue" override invocation — eighth
invocation past the half-century threshold (Phase 55 = 50th;
…; Phase 63 = 58th). 1486 tests across 75 vitest files (+22 /
0 vs Phase 62). 24 ADRs UNCHANGED. 7 DB tables UNCHANGED. 9
migrations UNCHANGED. 14 env vars UNCHANGED. 168 i18n keys per
locale UNCHANGED. First Load JS 103 kB UNCHANGED. Middleware
160 kB UNCHANGED. OPEN_QUESTIONS top-level Q-count 66
UNCHANGED. Phase-37+ candidate count 8 UNCHANGED.
`MARKDOWN_EXTENSIONS` recognized single-value arms 8 → 9
(+`wikilinks-cross-entity`). 0 new files (in-place plugin-
body / registry / factory evolution; test additions in
existing test files). Zero new ADRs / migrations / i18n keys
/ operational gates / runtime deps. Phase 64 entry requires
explicit human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 62 → 63 delta complete.
- [x] Phase 64 entry conditions documented (10 ranked
      candidates).
- [x] Framework-state-note explicitly flagged (Phase 63
      completed the first 2-phase forward-compat-affordance
      prerequisite-fulfillment arc; plugin-option axis now
      exercised at both signature and registry layers).
- [x] Smoke gates pass.
- [x] STOP at Phase 64 boundary per §12.
