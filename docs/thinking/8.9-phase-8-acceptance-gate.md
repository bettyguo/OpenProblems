# 8.9 — Phase 8 acceptance gate (Bilingual rollout-completion thread)

Phase-8 closing unit. Mirrors Units 1.12 / 2.13 / 3.13 / 4.13 / 5.13 / 6.10 / 7.11. Verifies every §13 deliverable Phase 8 pulled (the **Bilingual rollout-completion** sub-thread per Unit 8.0 D-1) is operational locally at HEAD, emits the cross-phase roll-up, and lists Phase-8 follow-ons that survive into Phase 9+.

## THINK

### §13 Phase-6+ scope clarification

`MASTER_PROMPT.md` §13 names three Phase-6+ threads: Discussions, read+write API with token auth, bilingual rendering. Phase 6 (Unit 6.0 D-1) pulled **Discussions** as the first thread and closed it at Unit 6.10. Phase 7 (Unit 7.0 D-1) pulled **Bilingual** as the second thread and closed it at Unit 7.11 to "infrastructure-complete + pilot-validated." Phase 8 (Unit 8.0 D-1) pulled **Bilingual rollout completion** as a sub-thread of the same §13 entry to close the route-coverage half. Auth + read+write API stay deferred for future open-ended phases.

### §13 Bilingual sub-thread (rollout completion) — local pass status

| Surface                                                                                | Phase-8 unit | Status |
|----------------------------------------------------------------------------------------|--------------|--------|
| Bulk page migration: every bare `app/<route>/page.tsx` → `app/[locale]/<route>/page.tsx` (22 routes) | Unit 8.1 | ✓ Every route has a `[locale]/` shadow; bare routes deleted; cartesian generateStaticParams. |
| `middleware.ts` enforcing `localePrefix: "always"` per ADR-0011 D-B                    | Unit 8.1     | ✓ next-intl middleware; 51.8 kB server-side bundle; matcher excludes `/api`, `/_next`, `/_vercel`, file-extension paths. |
| `lib/i18n/navigation.ts` Link wrapper (next-intl `createSharedPathnamesNavigation`)    | Unit 8.1     | ✓ Used in 7 component files + 1 search-palette `useRouter`; locale-toggle + viz/MoversBoard + root not-found.tsx intentionally kept on `next/link`. |
| `TALK_PATHNAME_REGEX` extension for locale-aware Giscus paths                          | Unit 8.1     | ✓ Optional `(en|fr)/` capture; backward-compat preserved (pre-migration discussions still match). |
| `lighthouserc.json` URL prefix normalization (13 URLs prefixed with `/en/`)            | Unit 8.1     | ✓ URL count holds at 18 → 19 (Unit 8.6 added `/fr/contributing`). |
| Home-page FR hero translation + `messages.home.*` namespace (15 keys)                  | Unit 8.2     | ✓ `getTranslations("home")` server-side; hero h1, hero p, 2 CTA buttons, 3 section h2s, 2 "all X →" links, methodology blurb p, 3 aria-labels. |
| Explicit `NEXT_LOCALE` cookie config (name / maxAge / sameSite / path / secure)        | Unit 8.3     | ✓ Pinned in `middleware.ts`; 1-year maxAge; SameSite=Lax; production-only Secure; httpOnly intentionally false (preserves future client-side personalization). |
| `useTranslations` for LocaleToggle aria-label                                          | Unit 8.4     | **DEFERRED** — blocked on HTML shell migration; parallel session preserved existing `app/layout.tsx`-owns-`<html>` structure. |
| `lib/site-url.ts` extraction (5 SITE call sites consolidated)                          | Unit 8.5     | ✓ `SITE = process.env.NEXT_PUBLIC_SITE_URL ?? "https://llm-openproblems.org"`; preview deploys substitute their hostname; lib + pages unified. |
| Universal sitemap locale alternates (`entryWithAlternates` helper)                     | Unit 8.5     | ✓ Every sitemap entry carries `alternates.languages = { en, fr }`; post-Unit 8.1 every route has a `[locale]/` shadow, so the Unit 7.8 "no alternates without shadow" invariant flipped. |
| `/contributing` FR pilot (`content/contributing/v1.1.fr.mdx` + `resolveLocalized`)     | Unit 8.6     | ✓ Second sibling-file consumer; mirrors Unit 7.5's methodology pattern; `/fr/contributing` (default landing) renders FR content; `/fr/contributing/v1.0.0` falls back to EN per ADR-0011 D-D. |
| Phase-8 hygiene status pass (Class A/B/C catalog)                                      | Unit 8.7     | ✓ 1 Class A (Unit 8.4 deferred); 12 Class B follow-ons; 9 Class C carryovers from prior phases. |
| OPEN_QUESTIONS hygiene + ADR review (Q51 wording refined; no net promotions)           | Unit 8.8     | ✓ 18 resolved + 8 decided-as-lean + 19 still open (45 total; unchanged from Phase 7 close). |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: 19 URLs enrolled in `lighthouserc.json` (was 18 at Phase-7 close; +1 from Unit 8.6 `/fr/contributing`). 5 locale-pilot URLs (`/en/about`, `/fr/about`, `/en/methodology`, `/fr/methodology`, `/fr/contributing`) carry Phase-7 + Phase-8 freshness. **Documented `html-has-lang` axe-rule risk** on `/fr/...` pages survives Phase 8 (Unit 7.7 flag + Class B item 2); real LHCI run lands on first PR with the unit-8.x commits.
- **W3C feed validator on `/api/v1/rss.xml` + `/api/v1/digest/<slug>`**: Phase-3/5/6/8 compound deferral; first preview deploy + Q47 unblock.
- **Visual-regression baselines** for locale-pilot pages × 2 themes × N viewports — deferred Phase-8 follow-on; no Playwright spec changes this phase.
- **No auto-merge** (ADR-0009): Phase 8 added one LLM-translated MDX (`content/contributing/v1.1.fr.mdx`); marked `translation_source: "machine-assisted"` per ADR-0011 D-G; curator-reviewable; landed via standard git-apply flow, not LLM-CLI auto-write.
- **File-first / no DB** (ADR-0004): held. DB-migration trigger ~1.6% of 5 MB threshold at Phase-8 close (Unit 8.0 D-2 baseline 1.558%; +1 FR MDX adds ~6 KB; trigger ~1.6%). Still cold.
- **First Load JS shared chunk = 103 kB**: held through every Phase-8 unit. `next-intl` middleware bundles server-side only (51.8 kB; doesn't count against First Load JS); `lib/i18n/navigation` Link wrapper adds zero net bytes to the shared chunk.

### Phase-8 unit summary (9 commits — Bilingual rollout-completion sub-thread)

| Unit | Commit    | Title                                                                                            |
| ---- | --------- | ------------------------------------------------------------------------------------------------ |
| 8.0  | `0ee1ad9` | Phase 8 prep (Bilingual rollout-completion thread; 10-unit breakdown; DB-trigger 1.558%)         |
| 8.1  | `31ea2d5` | Bulk page migration + middleware (`localePrefix: "always"`)                                      |
| 8.2  | `defb122` | Home-page FR hero translation + `messages.home.*` keys                                            |
| 8.3  | `5e2b509` | Explicit `NEXT_LOCALE` cookie config on middleware                                                |
| 8.4  | —         | **DEFERRED** (blocked on HTML shell migration)                                                    |
| 8.5  | `90bd3c3` | `lib/site-url.ts` extraction + universal sitemap locale alternates                                |
| 8.6  | `bad59fa` | `/contributing` FR pilot (second sibling-file consumer)                                           |
| 8.7  | `c05e1ab` | Phase-8 hygiene status pass (Class A/B/C catalog)                                                 |
| 8.8  | `8ba25e0` | OPEN_QUESTIONS hygiene + ADR review (Q51 wording refined)                                         |
| 8.9  | _this_    | Phase 8 acceptance gate                                                                           |

Total: **9 numbered units + 1 deferral marker (8.4)**. Matches Unit 8.0 prep's 10-unit breakdown (8.0-8.9). The deferral is documented in CHANGELOG inline (Unit 8.4 entry) + Unit 8.7's Class A catalog + this gate's §13 deliverable table.

### State at HEAD (Unit 8.9)

- **Content** (no count change vs Phase-7 close at the schema-validated count; +1 raw MDX):
  - 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors / 14 institutions / 20 rating actions / 4 issue templates / methodology v1 (EN + FR from Unit 7.5) / contributing v1.0 (EN) + v1.1 (EN + **FR new from Unit 8.6**) / 2 `entries.json` files.
  - **203 schema-validated content files** (unchanged); **36 raw MDX files** (was 35; +1 from Unit 8.6).
- **New Phase-8 surfaces** (relative to Phase-7 close at `01862d2`):
  - 22 bare `app/<route>/page.tsx` migrated → `app/[locale]/<route>/page.tsx` (Unit 8.1).
  - `middleware.ts` (Unit 8.1; 51.8 kB server-side bundle).
  - `lib/i18n/navigation.ts` Link wrapper (Unit 8.1).
  - `lib/site-url.ts` SITE constant (Unit 8.5).
  - `content/contributing/v1.1.fr.mdx` (Unit 8.6).
  - 15 `messages.home.*` keys EN+FR (Unit 8.2).
- **~590 prerendered pages** (was 341 at Phase-7 close → ~590 after Unit 8.1's locale-doubling → +2 routes after Unit 8.6's `{en,fr} × {1.1.0}` contributing version expansion = ~590-592). Build summary at Unit 8.6 shipped 4 contributing version entries (was 2). First Load JS shared chunk = **103 kB UNCHANGED**.
- **388/388 vitest tests across 44 files** (was 383/44 at Phase-7 close); **+5 tests this phase**: +1 from Unit 8.1's TALK_PATHNAME_REGEX refactor + +4 from Unit 8.5's sitemap test rewrite. No new test files.
- `pnpm validate-content` → **203 files** green (unchanged; MDX isn't validated by validate-content — `v1.1.fr.mdx` flows through Velite refine).
- `pnpm audit-content` → **0 errors / 6 warnings** (Q32-expected baseline since Phase 2; unchanged).
- `pnpm typecheck` clean. `pnpm build` clean compile (~3-9s; per-unit variance).
- **5 visualizations live** (unchanged from Phase 4).
- **11 ADRs** (unchanged from Phase 7; Phase 8 added zero new ADRs).
- **0 new dependencies in Phase 8** (next-intl@3.x runtime from Phase 7 was the last addition).
- **`lighthouserc.json`** enrols **19 URLs** (was 18 at Phase-7 close; +1 from Unit 8.6 `/fr/contributing`).
- **OPEN_QUESTIONS state**: 18 resolved + 8 decided-as-lean + 19 still open = **45 total entries** (unchanged from Phase 7 close; Q51 wording refined in Unit 8.8 but stays decided-as-lean).

### `lighthouserc.json` final state

Phase 8 changes:
- **Unit 8.1** prefixed 13 non-locale-aware URLs with `/en/` (`/` → `/en`, `/problems/.../...` → `/en/problems/.../...`, etc.).
- **Unit 8.6** added `/fr/contributing` (`/en/contributing` was always enrolled per Phase-3 stub).

**19 URLs total** at Phase-8 close. Same cohort-strategy as prior phases — one representative URL per route-template per locale where the pilot pattern applies.

### Phase-8 follow-ons that survive the gate (non-blocking)

Same Q27-class pattern as prior phases — CI / deployed-URL artifacts must refresh on the first PR. Phase-8 specifics overlay the canonical list.

1. **Unit 8.4 — `useTranslations` for LocaleToggle aria-label**: deferred indefinitely. Depends on HTML shell migration. Documented in Unit 8.7 Class A.
2. **HTML shell migration into `app/[locale]/layout.tsx`** — Class B item 1. Closes Unit 7.7 axe-rule risk. Survives Phase 8 by parallel-session signal preservation; needs explicit curator authorization to land.
3. **Fallback-hint UI for `didFallback === true`** — Class B item 4. ADR-0011 D-D pinned the hint copy; deferred since Unit 7.5; survived Unit 8.6 (`/fr/contributing/v1.0.0` triggers `didFallback` but renders silently). Future i18n-UI unit.
4. **`content/contributing/v1.0.fr.mdx`** — Class B item 5. Curator-track per Q51.
5. **Bulk content backfill** (~200 EN files) — Class B item 6. Q51 horizon.
6. **`messages.{contributing,methodology,…}.*` chrome strings** — Class B item 7. Component-side string extraction.
7. **`StatusPill` localization** + **`components/recently-rated/` empty-state copy** + **Site-header nav labels via `useTranslations`** — Class B items 8/9/10.
8. **Trailing-slash normalization for `NEXT_PUBLIC_SITE_URL`** — Class B item 11. Defer until first observed footgun.
9. **Per-entry sitemap `lastModified` / `changeFrequency` / `priority`** — Class B item 12. Observation-driven.
10. **First LHCI run for the 5 new locale-pilot URLs** — Phase-7 carryover; CI signal on first PR with the unit-8.x commits.
11. **Visual-regression baselines** for the locale-pilot pages — Phase-7 carryover; Playwright `--update-snapshots` on a future PR.
12. **W3C feed validator pass** against `/api/v1/rss.xml` + `/api/v1/digest/<slug>` — Phase-3/5/6/8 compound deferral.

Documented in detail in Unit 8.7's hygiene status pass.

### Pre-existing follow-ons that survived Phase 8 (carryover from prior phases)

Unchanged from Unit 7.11 acceptance gate:

- Orphan `components/domain-tile-grid/` deletion (curator authorization).
- `entries.json` content backfill on 8 problems (editorial + API).
- `<managingEditor>` on RSS feeds (Q33 / Q44; Q2 DNS).
- W3C RSS validator pass on `/api/v1/rss.xml` (Phase-3 carryover).
- W3C validator pass on Phase-5 digest endpoints (Phase-5 carryover).
- `pnpm clean-drafts` script (operational signal not yet met).
- Phase-2 ROR-ID + InstaDeep orphan (curator hygiene).
- `NEXT_PUBLIC_GISCUS_REPO_ID` + repo-Discussions enablement (Phase-6 Q47 operational gate).
- Real-API integration smoke for `lib/discussions/github-graphql.ts` (Q47 + `GITHUB_TOKEN`).

Documented in detail in Unit 8.7 Class C carryover catalog.

### Phase 9 entry conditions

Per §12 cardinal rule: **explicit human sign-off required**. §13 Phase 6+ scope is open-ended; Phase 6 closed Discussions; Phase 7 + 8 together closed Bilingual (infrastructure + route coverage + 2 content surfaces translated; remaining curator-track per Q51). Phase 9+ candidate threads remain:

- **Auth + read+write API thread** (the remaining §13 thread): Clerk vs NextAuth.js v5 vs GitHub OAuth direct. **MANDATORY DB-migration trigger flip** on the first write-path unit per Units 4.12 / 5.10 / 6.0 / 7.0 / 8.0 D-2. ~14-unit phase. Breaks Phase-4 "no user accounts" pact at the chosen-provider-commits unit.
- **Subscriber-list thread** (Phase-5 D-4 punt completion): coupled to auth (write-half) OR third-party (Buttondown / Resend / ConvertKit) commitment ADR; can decouple from auth via third-party route.
- **Monetization thread**: still premature without auth + API + read-API maturity. Phase 10+ candidate.
- **Unit 8.4 + HTML shell migration**: could land as a tactical Phase-9 Unit 9.0 / 9.1 prefix if the chosen thread needs the SiteHeader-under-provider unblock; otherwise stays a Phase-8 follow-on indefinitely.

DB-migration trigger re-eval mandatory at Phase 9 kickoff per Unit 8.0 D-2. Stays cold throughout Phase 8 (+1 FR MDX added ~6 KB; trigger ~1.6% of 5 MB threshold). Flips immediately if Phase 9 picks the auth thread.

### Cross-phase milestone

This commit closes the **Bilingual rollout-completion sub-thread** of Phase 8 to the level Unit 8.0 D-1 + ADR-0011 scoped it: **route-complete + pilot-validated-with-2-content-surfaces** (every page route has a `[locale]/` shadow under `localePrefix: "always"`; FR hero on the home page; full FR translation of `/contributing/v1.1`; sitemap alternates universally; LHCI enrolment for `/fr/contributing`; explicit `NEXT_LOCALE` cookie config). Q51 stays decided-as-lean with refined wording capturing Phase 8's contribution.

9 commits (8.0-8.3, 8.5-8.9; 8.4 deferred) + 0 new ADRs + 0 new dependencies + 0 client-bundle regressions (103 kB First Load JS preserved end-to-end) + 0 test regressions + 5 new tests + 0 OPEN_QUESTIONS movements (Q51 wording refined; net entries unchanged at 45).

### Phase-8 over-vs-under against the 8.0 plan

Unit 8.0 prep scoped a 10-unit phase (8.0–8.9). Actual: 9 units shipped + 1 deferred = same unit-count budget. Scope drift this phase:

- **Dropped from scope mid-flight**: HTML shell migration (originally Unit 8.1) — preserved per parallel-session signal; surfaced into Unit 8.4 territory then deferred.
- **Added to scope mid-flight**: nothing. Phase 8 stayed within the 8.0 prep envelope.
- **Surface that emerged**: parallel-curator coordination at unprecedented intensity (Unit 8.1's `middleware.ts` + `lib/i18n/navigation.ts` + THINK doc deleted mid-unit by parallel session, then re-created on user "Continue"; Unit 8.6 FR translation authored by parallel session in parallel with the primary session's route refactor). Documented in unit THINK docs.

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 8.9 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 388/388 across 44 files unchanged (no test files touched in 8.9).
- `pnpm build` → ~590 prerendered pages unchanged. First Load JS shared chunk = 103 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-8 close: marks Unit 8.9 as the acceptance gate; cites the §13 Bilingual-rollout-completion deliverable status table (one row per Phase-8 surface; Unit 8.4 row marked DEFERRED); emits the cross-phase state-at-HEAD (10 problems / etc.; ~590 routes; 103 kB; 388 tests; 11 ADRs; 19 LHCI URLs; 0 new deps); enumerates Phase-8 follow-ons (Unit 8.4 + HTML shell migration; fallback-hint UI; v1.0 FR translation; bulk content backfill; component-side string extraction; trailing-slash normalization; per-entry sitemap hints; LHCI run; visual baselines; W3C validator); enumerates pre-existing carryovers from prior phases; states Phase 9 entry condition (explicit sign-off per §12); notes DB-migration trigger re-eval mandatory at Phase-9 kickoff (~1.6% cold; flips on first auth write-path).
