# 29.3 — ADR-0018 D-G Phase-29 enhancement note (third APPEND on same D-clause) + OPEN_QUESTIONS hygiene (0 Q changes; B.14 wikilink documented but not promoted)

Fourth Phase-29 unit; docs-only. ADR + OPEN_QUESTIONS hygiene
unit. Mirrors Phase-22/23/24/27 APPEND-not-EDIT phase-tagged
note patterns + Phase-18 + Phase-27 sibling-note shape.

## THINK

### ADR-0018 D-G Phase-29 enhancement note appended

Per the APPEND-not-EDIT phase-tagged convention codified Phase
22+23 + extended Phase 24 + extended retroactively Phase 27 +
extended to a fourth call site Phase 29: Unit 29.3 appends a
**third enhancement note** to ADR-0018 D-G documenting the
`actionRationaleSchema` + `renderActionRationaleMarkdown`
realization at the fourth sibling call site.

**Notes now on D-G** (chronological by realization):

1. **REALIZED Phase 18 Unit 18.2** — appended Phase 27 Unit 27.3
   retroactively. `renderReviewNotesMarkdown` + `reviewNotesSchema`
   sibling; second sibling under D-G.
2. **REALIZED Phase 27 Unit 27.1** — appended Phase 27 Unit 27.3
   in the same hygiene unit as the retroactive Phase-18 note.
   `renderRationaleMarkdown` + `rationaleSchema` sibling; third
   sibling under D-G.
3. **REALIZED Phase 29 Unit 29.1** — appended Phase 29 Unit 29.3
   (this unit). `renderActionRationaleMarkdown` +
   `actionRationaleSchema` sibling; **fourth sibling** under D-G.

**First ADR D-clause with three APPEND notes** in project
history. Prior longest: ADR-0014 D-D with 2 notes (Phase-22 +
Phase-23) + ADR-0019 D-F with 1 note (Phase-24) + ADR-0012 D-B
with 1 note (Phase-28 partial-supersession). ADR-0018 D-G now
stands alone at 3 APPEND notes.

### Note shape

The Phase-29 note opens with `**REALIZED Phase 29 Unit 29.1**`
(matches the convention) and documents:

- `renderActionRationaleMarkdown` + `actionRationaleSchema`
  fourth-sibling realization.
- Surface: rating-action `RatingAction.dimensions.<dim>.rationale`
  (content-side YAML; Velite-validated; required per Zod schema).
- Signature `string → string` mirrors Phase-27 (rating-action
  rationale required per `rating-action.ts` Zod schema).
- Single render surface (`DimensionCard` in
  `app/[locale]/problems/[slug]/ratings/page.tsx`) instantiated
  5× per rating action via `DimensionsBlock`.
- `actionRationaleSchema = { ...baseSchemaConfig }` — intentional
  parity with `bioSchema` Phase-29; Q72 family now expanded to
  3 sibling-divergence candidates at Phase 29 entry.
- 11 new vitest tests (8 helper + 3 schema parity); one
  additional test beyond Phase-27 documents wikilink
  preservation as literal text.
- Phase 29 did NOT need a new ADR — implementation realizes D-G
  inheritance at a fourth call site without architectural
  surface.
- **First content-side (Velite-validated YAML) markdown render
  call site** under D-G; establishes the convention that
  ADR-0018 D-G inheritance is **storage-layer-agnostic** (DB-
  backed and content-backed surfaces share the same sanitization
  audit boundary). The Phase-17 anticipation in the file's
  header comment is realized 12 phases later (Phase 17 → Phase
  29 anticipation-to-realization gap; load-bearing test of D-G
  inheritance-contract durability).
- **Phase-30+ Class B.14 (NOT promoted to Q absent demand)**:
  rating-action rationale wikilink resolution. Documents
  literal-text rendering of `[[problem-slug]]` syntax + the
  three preconditions for promotion + the natural coupling with
  Q72 family if either surfaces demand.

### OPEN_QUESTIONS hygiene

**No Q-tally changes this unit**. Phase 29 OPEN_QUESTIONS state
preserved at **26 resolved / 4 lean / 31 open / 61 total**
(carried unchanged from Phase 28 close at `c8db79a`).

**Class B.14 documented in CHANGELOG + ADR note, NOT promoted to
Q**:
- **Rationale for non-promotion**: wikilink rendering has no
  observed demand signal — content was authored with `[[slug]]`
  syntax as a curator-internal note convention, not as a
  user-facing affordance. Active resolution would couple
  challenge rationale + review notes + bio rendering decisions
  (Q72-family coupling) into one architectural scope. Better as
  a **deferred Class B item** than a premature Q-promotion that
  pre-commits the scope.
- **Promotion triggers** (Phase-30+ if any fire):
  1. Curator demand signal (curator explicitly asks for
     wikilink resolution in a rating action).
  2. User demand signal (submitter notes wikilinks not
     rendering as expected on a rating action they read).
  3. Q72 family promotion (if any sibling-divergence candidate
     promotes — e.g., curator demands tables in `reviewNotes`
     — wikilink resolution could couple into the same Q72
     ADR-0021 scope).
- **First-phase-since-Phase-19** to add a Class B item with
  explicit non-promotion rationale (Phase-19 added Q70 from
  Class B; Phase-29 surfaces B.14 deliberately as Class B).

### Phase 29 Q-tally freeze (1-phase; first since the Phase-20-27 8-phase freeze ended Phase 28)

Phase 29 ships **0 OPEN_QUESTIONS Δ**. Phase 28 broke the
8-phase Phase-20-27 Q-tally freeze by promoting Q73 + Q74
alongside ADR-0020. Phase 29 starts a new 1-phase freeze; the
B.14 candidate explicitly stays Class B to preserve the freeze.

### Smoke gates (docs-only)

`pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline since
Phase 2; unchanged through every Phase 3-28 unit). typecheck /
test / build untouched since no source files modified at this
unit.

### Architectural firsts shipped in this unit

- **First ADR D-clause with three APPEND notes** (ADR-0018 D-G;
  Phase-18 retroactive + Phase-27 + Phase-29).
- **Third APPEND note on ADR-0018 D-G** (extends APPEND-not-EDIT
  convention to its longest single-D-clause use; prior longest
  was 2 APPEND notes).
- **First Class B item surfaced with explicit non-promotion
  rationale since Phase 19** (B.14 wikilink rendering;
  documents the 3 promotion triggers as forward-compat).
- **First 1-phase Q-tally freeze re-start** after the
  Phase-20-27 8-phase freeze ended Phase 28.

### Parallel-curator awareness

This unit docs-only, no collision risk. No parallel-session
activity observed during Phase 29 work; working tree clean at
each unit boundary (HEAD `7f2fb2a` pre-this-unit).

### Files modified

- `docs/adr/0018-markdown-sanitization.md` (Phase-29 enhancement
  note appended to D-G section, after the existing Phase-27
  REALIZED note + before D-H heading).
- CHANGELOG.md (Unit 29.3 entry).
- `docs/thinking/29.3-adr-0018-dg-phase-29-note.md` (this file).
