# 6.8 — Phase-6 hygiene status pass (deferrals carried into Phase 7+)

Seventh code-track unit of Phase 6 (docs-only). Mirrors the Phase-5 Unit 5.11 framing — a status-pass on hygiene candidates that REMAIN deferred because they require either explicit human authorization (destructive actions per Units 4.4 / 5.11 policy) or curator-editorial work the harness can't perform unilaterally.

## THINK

### What this unit IS

A surfacing of remaining hygiene items at HEAD `d2c635e` (after Unit 6.7 LHCI enrolment). No code/content changes. Documents WHY each item is deferred and what would unlock it.

### What this unit IS NOT

- **Not the orphan `components/domain-tile-grid/` deletion**. The component remains at HEAD; the parallel-curator memory + destructive-action policy require an explicit "delete the orphan tile-grid" from the curator before any harness session removes pre-existing tracked files. Skipping the destructive action in this session is the correct behaviour per the documented Unit 4.4 / 5.11 precedent.
- **Not the `entries.json` backfill**. The Phase-5 `pnpm extract-leaderboard` toolchain is operational; running it against real papers requires `ANTHROPIC_API_KEY` + per-paper curator review (which papers, which benchmarks, which protocol notes). Editorial work that belongs to a curator-driven session.

### Inputs

- **Unit 5.11 (`f964ef4`)** — the Phase-5 hygiene status pass. Pattern: list each deferred item, name the unlock condition, mark gate type. This unit replays the same surface for Phase 6.
- **`docs/SESSION_HANDOFF_phase5_close.md`** — listed both deferred items as Phase-6 hygiene candidates.
- **State verification** (this unit):
  - `components/domain-tile-grid/index.tsx` exists; `grep -rn domain-tile-grid app components lib` finds only the self-reference. **Still orphan**.
  - 2 `entries.json` files across 10 problems (hallucination-reduction + long-horizon-agent-reliability). **8 problems still without entries** — unchanged through Phases 5 and 6.

### Hygiene status at HEAD `d2c635e`

| Item                                                          | Status        | Unlock condition                                                          | Gate type      |
|---------------------------------------------------------------|---------------|---------------------------------------------------------------------------|----------------|
| Orphan `components/domain-tile-grid/` deletion                | **Deferred**  | Explicit curator authorization: "delete the orphan tile-grid"             | Destructive-action policy |
| `entries.json` backfill on 8 problems                          | **Deferred**  | Curator session with `ANTHROPIC_API_KEY` + per-paper editorial review     | Editorial + API |
| `pnpm clean-drafts` script (op hygiene; surfaced in Unit 5.10) | **Deferred**  | `drafts/` accumulating > 100 stale files in any working tree              | Op signal (none today) |
| `<managingEditor>` on RSS feeds (Q44 / Q33)                    | **Deferred**  | Q2 (DNS / project email) resolution                                       | External (DNS) |
| Phase-2 ROR-ID + InstaDeep orphan                              | **Deferred**  | Curator-driven session (per Phase-4 hygiene list)                         | Editorial |
| W3C feed validator pass against deployed `/api/v1/digest/<slug>` URLs | **Deferred** | First preview deploy (Q27-class follow-on)                          | Deploy environment |
| `/digest` Playwright visual baselines (`chromium-win32`)       | **Deferred**  | `playwright test --update-snapshots` against `/digest`                    | Manual run |
| **NEW** — `/problems/[slug]/talk` Playwright visual baselines  | **Deferred**  | `playwright test --update-snapshots` against `/problems/<slug>/talk`      | Manual run |
| **NEW** — Real-API smoke for Phase-6 GraphQL client (Unit 6.2) | **Deferred**  | Q47 (repo Discussions enablement) resolution + `GITHUB_TOKEN` in CI       | Operational |
| **NEW** — `NEXT_PUBLIC_GISCUS_REPO_ID` env wiring              | **Deferred**  | Q47 resolution + curator runs giscus.app config UI                        | Operational |
| **NEW** — W3C feed validator pass against `/api/v1/digest/<slug>` with discussion items | **Deferred** | Unit 6.6 effects visible (Q47 resolved + at least 1 discussion with comments + first deploy) | Compound |

### Newly-surfaced operational gates from Phase 6

Phase 6 added two operational gates that aren't on the Phase-5 list:

1. **Q47 — GitHub Discussions enablement** on `bettyguo/OpenProblems`. Owner action; gates the GraphQL read-side (Unit 6.2) returning non-empty + the Giscus iframe rendering live (Unit 6.4) + the card badges populating (Unit 6.5) + discussion items appearing in the per-domain digest (Unit 6.6). Until resolved: every Phase-6 read surface gracefully renders empty / placeholder.
2. **`NEXT_PUBLIC_GISCUS_REPO_ID` env var**. Generated at giscus.app after Q47 resolves. Until set: Unit 6.4's component renders the "embed unavailable" curator message; talk-page shell otherwise functional.

Both flow through ADR-0010's graceful-degradation design (D-A + D-D) — Phase 6 ships as architecturally functional at HEAD; production unlock is two operator actions.

### Cross-phase deferral lineage

| Item                                | First surfaced | Phase 5 status | Phase 6 status | Reason for continued deferral |
|-------------------------------------|----------------|----------------|----------------|-------------------------------|
| Orphan `components/domain-tile-grid/` | Unit 4.4    | Deferred       | Deferred       | Destructive-action policy; needs explicit auth |
| `entries.json` backfill (8 problems) | Phase 2/4    | Deferred       | Deferred       | Editorial + API; needs curator session |
| `<managingEditor>` on RSS          | Phase 3 / Q33 | Deferred       | Deferred       | Q2 (DNS) gates Q33 + Q44 |
| W3C feed validator (Phase-3 RSS)   | Unit 3.13     | Deferred       | Deferred       | First preview deploy |
| W3C feed validator (Phase-5 digest) | Unit 5.13    | Deferred       | Deferred       | First preview deploy |
| `pnpm clean-drafts`                | Unit 5.10     | Deferred       | Deferred       | Operational signal not yet met |

Phase 6 did NOT close any prior-phase hygiene items. Phase 6 added: Q47 + `NEXT_PUBLIC_GISCUS_REPO_ID` + talk-page visual baselines + discussion-augmented W3C validator pass.

### What the curator could authorize in a future session

If a curator runs `claude-code` with explicit intent, the unlocks are:

- **"Delete the orphan tile-grid"** → harness session removes `components/domain-tile-grid/` + the smoke gates verify nothing references it. Likely a 1-commit hygiene unit.
- **"Backfill entries for problem X"** → curator session runs `pnpm extract-leaderboard <arxiv-id> --problem X` with `ANTHROPIC_API_KEY` set + reviews the diff + `git apply`s. Likely 1 commit per problem.
- **"Enable repo Discussions + paste the repoId"** → out-of-band on github.com + giscus.app, then a 1-commit env-setting unit (probably `.env.example` update + production deploy env).

None of these are blocking for Phase 6 acceptance. The phase ships with the surfaces in place + graceful degradation in the absence of the operational unlocks.

### Smoke gates (this unit)

Docs-only:
- File added: this THINK doc.
- CHANGELOG: Unit 6.8 entry.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline; unchanged).
- No code touched; typecheck / test / build untouched.

### CHANGELOG entry shape

Per-unit entry: surfaces remaining Phase-6 hygiene candidates + their gate types; documents two new Phase-6-specific gates (Q47 + giscus repoId); notes lineage of prior-phase deferrals continuing into Phase 7+; flags that the curator could authorize specific unlocks in future sessions but none are Phase-6-acceptance-blocking.
