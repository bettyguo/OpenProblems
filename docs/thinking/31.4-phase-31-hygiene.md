# 31.4 — Phase-31 hygiene + OPEN_QUESTIONS (+Q77 +Q78) + ADR-0021 D-F Phase-31 cross-ref note (**22 ADRs**; B.15 item 1 CLOSED at 1-phase carryover; first ADR D-clause to receive a Phase-N+1 APPEND at 1-phase age)

Docs-only hygiene unit per the Phase-31 prep doc (Unit 31.0). Mirrors
the Phase-29 Unit 29.2 + Phase-30 Unit 30.4 hygiene-unit shape verbatim.

## THINK

### Class A status (post-Phase-31 delta)

**0 Class A items** — twentieth consecutive phase ships with 0 Class A
items. Anticipated per prep doc Unit 31.0 D-7.

### Class B status (post-Phase-31 delta)

**~4 across 14 categories** (was ~5 across 14 categories at Phase 30 close).

| Category | Item | Status |
|---|---|---|
| B.15 (Phase-30 subscriber-list) | 1. Weekly digest scheduler + send template | **CLOSED Unit 31.3** (1-phase carryover; tightest follow-on cadence) |
| B.15 (Phase-30 subscriber-list) | 2. Digest-send email template | **CLOSED Unit 31.2** (consumed by 31.3 cron route) |
| B.15 (Phase-30 subscriber-list) | 3. DB-helper integration tests for `lib/subscribers/` | **partial close Unit 31.3** — full dep-injection coverage via `lib/cron/digest-send.test.ts` replaces the original "B.20 DB-helper deferred" scope for the digest-send path; the verify + unsubscribe + create routes' DB helpers remain deferred |
| B.15 (Phase-30 subscriber-list) | 4. Stale verification-token cleanup job | carried (Phase 32+; second cron entry in `vercel.json`) |
| B.15 (Phase-30 subscriber-list) | 5. Subscribe-endpoint rate limiting | carried (Phase 32+ if spam) |

Plus 8 Class B items carried unchanged from Phase 30 close (B.1 + B.2 +
B.3 + B.4 + B.5 + B.8 + B.9 + B.10 + B.11 + B.13 + B.14 — same set as
Phase 29 close after Phase-30 deltas).

**Net**: B.15 item count 5 → 4 (item 1 + item 2 closed; item 3 partial
close). Other 13 categories unchanged.

### OPEN_QUESTIONS updates (anticipated +2 net opens per Phase-31 prep D-9)

**+Q77** (operational; Vercel Cron production setup + `CRON_SECRET`
provisioning; mirrors Q54 + Q73 + Q75 operational-gate shape verbatim).

**+Q78** (architectural; digest-send analytics / observability Phase
32+; 6 candidate sub-threads enumerated; 3 resolution profiles
documented; Phase-31 lean = Profile A Resend-webhook-first).

**Q-tally Phase 31 → 31 close**: 26 resolved + 4 lean + 33 open →
26 resolved + 4 lean + **35 open** = **65 total** (+2 net). Mirrors
Phase-28's Q73 + Q74 + Phase-30's Q75 + Q76 ADR-coupled Q-promotion
pair shape — **third consecutive new-ADR phase to surface an
operational-plus-architectural Q-pair coupled to a single ADR**.

### ADR-0021 D-F Phase-31 cross-ref note

APPEND-not-EDIT note documenting the Phase-31 digest template
realization per ADR-0022 D-E. **First ADR D-clause to receive a
Phase-N+1 APPEND at 1-phase age** in project history (prior APPENDs
spanned 5+ phases between source and APPEND — ADR-0013 D-F Phase 13 →
APPEND Phase 11+30 = 5+19-phase gaps; ADR-0018 D-G Phase 17 → APPEND
Phase 22+27+29 = 5+10+12-phase gaps).

**Third APPEND-pattern ADR D-clause cluster** in project history
(ADR-0013 D-F = 2 APPENDs Phase 11 + Phase 30; ADR-0018 D-G = 3 APPENDs
Phase 22 + 27 + 29; ADR-0021 D-F = 1 APPEND Phase 31). Confirms the
APPEND-not-EDIT pattern as a recurring shape for inheritance-pattern
phase carry-forward.

### ADR count post-this-unit

**22 ADRs** (unchanged from Unit 31.1; Phase 31 = 1 new ADR via Unit
31.1; no second new ADR Phase 31). **ADR-0023 candidate slot opens**
(Phase 31 claimed ADR-0022). Strongest remaining Phase-32+ candidates:

- Q68 expansion content moderation (10+ phase carryover; ADR-0023 candidate).
- Markdown evolution ADR (Q72 + Phase-29 B.14 wikilink couple).
- Q76 expansion architectural follow-on (per-user-account subscriptions; Phase 31+ Q-tally carry).
- Account-merge UI (ADR-0020 D-E).
- Third+ provider expansion (ADR-0020 D-B amendment).
- Q78 architectural Phase-32+ (digest-send analytics — surfaced this unit).

### Phase-32+ Q-candidates flagged

**13 total** (was 11 at Phase 30 close):
- 7 carried from Phase 27 (Q60 / Q61 / Q62 / Q64 / Q65 / Q68 expansion / Q72).
- 2 carried from Phase 28 (Q73 + Q74).
- 2 carried from Phase 30 (Q75 + Q76).
- **2 NEW Phase 31 (Q77 + Q78)**.

**Q-promotion-pair shape recurring**: Phase 28 ADR-0020 → Q73 + Q74;
Phase 30 ADR-0021 → Q75 + Q76; Phase 31 ADR-0022 → Q77 + Q78. **Third
consecutive new-ADR phase to surface an operational-plus-architectural
Q-pair coupled to a single ADR** — confirms the ADR-cycle-coupled
Q-promotion-pair shape as a stable recurring pattern in project history
(not a Phase-28/30 anomaly).

### Architectural firsts shipped in this unit

- **+Q77 + Q78 promotion pair** confirms the ADR-cycle-coupled
  Q-promotion-pair shape as stable across 3 consecutive new-ADR phases
  (Phase 28 + 30 + 31).
- **First ADR D-clause to receive a Phase-N+1 APPEND at 1-phase age**
  (ADR-0021 D-F → Phase-31 APPEND; tightest source-to-APPEND gap in
  project history).
- **Third APPEND-pattern ADR D-clause cluster** in project history.
- **B.15 item 1 CLOSED at 1-phase carryover** — mirrors the Phase
  21/23/25/27/29 tightest-follow-on cadence.
- **B.15 item 2 CLOSED at 1-phase carryover** (digest-send template
  realized Unit 31.2).
- **B.15 item 3 partial close** via Unit 31.3's dep-injection coverage
  (replaces the B.20 DB-helper-deferred scope for the digest-send
  path).

### Smoke gates (this unit)

Docs-only — no source files modified.

- File modified: `OPEN_QUESTIONS.md` (+Q77 + Q78; ~50 lines appended
  after Q76).
- File modified: `docs/adr/0021-subscriber-list-email.md` (Phase-31
  cross-ref note appended to D-F section; ~5 lines).
- File modified: `CHANGELOG.md` (Unit 31.4 entry under Phase 31).
- File added: `docs/thinking/31.4-phase-31-hygiene.md` (this file).
- `audit-content`: 0 errors / 6 warnings (Q32 baseline unchanged).
- `typecheck` / `test` / `build`: untouched since no source files
  modified.

### Parallel-curator awareness

This unit is docs-only. Touches:

- MODIFIED `OPEN_QUESTIONS.md` — append-only Q77 + Q78 entries; per
  CURATION_PROMPT.md parallel sessions route new questions to
  `docs/open-questions-inbox/<RUN-ID>.md` not here. **OPEN_QUESTIONS
  is a shared singleton — appending in a serial session is safe**;
  parallel sessions go through the inbox + merge pass. No parallel
  session activity observed during Phase 31; safe to append directly.
- MODIFIED `docs/adr/0021-subscriber-list-email.md` — APPEND-only note
  to D-F section; low collision risk (ADR-0021 is the Phase-30 ADR;
  no Phase-31 parallel ADR thread observed).
- MODIFIED `CHANGELOG.md` — append-only Unit 31.4 entry under `### Phase
  31` section opened Unit 31.0; low collision risk.
- NEW `docs/thinking/31.4-phase-31-hygiene.md` — fresh file; no
  collision risk.

### Phase-31 anti-scope (boundary statement preview for Unit 31.5)

NOT: production cron operation (Q77 operational); NOT digest-send
analytics / Resend webhook integration (Q78 architectural; Phase 32+);
NOT retry queue for transient send failures (Phase 32+ per ADR-0022
D-H); NOT per-subscriber preferred send-day (Phase 32+); NOT digest
content customization (Phase 32+); NOT React Email / MJML template
adoption (Phase 32+ if template count grows); NOT FR translation of
digest content (Phase 32+ curator-track); NOT per-problem subscription
(Phase 32+ Q76 expansion); NOT authenticated `users.id` link on
subscriber rows (Phase 32+ Q76); NOT cross-domain aggregated weekly
summary (Phase 32+); NOT admin / curator UI for digest analytics
(Phase 32+); NOT manual / curator-triggered "send now" override
(Phase 32+); NOT Resend tier upgrade automation (Phase 32+); NOT
row-level lock for concurrent-cron-invocation race (Phase 32+; documented
as known limitation in ADR-0022 Negative consequences); NOT bounce /
complaint webhook handling (Phase 32+); NOT stale verification-token
cleanup job (Phase 32+ B.15 item 4). Phase 31 is the **production
send-loop realization** for the Phase-30 architectural foundation —
the cron route ships, the template ships, the migration ships, the
auth contract ships; production cron operation awaits Q77 operational
provisioning.
