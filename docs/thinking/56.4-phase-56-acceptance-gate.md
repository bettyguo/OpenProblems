# 56.4 — Phase 56 acceptance gate

Phase 56 closes at HEAD `21ab9b7` (Unit 56.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-57-entry
conditions per §12.

## Phase 56 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 56.0 | Phase 56 prep | docs | `2cb15e4` |
| 56.1 | `PHASE_54_DEFAULT_ENABLED_SURFACES` value change to all 4 surfaces + 5 baseline blocks decoupled + 3 dispatch tests flipped + ADR-0018 D-G APPEND-D-AN | code+APPEND | `117d0f9` |
| 56.2 | 21 NEW end-to-end Phase-56 tests + first all-4-surfaces quintuple-alias state + first all-4-surfaces 4-consumer same-slot composition | code | `dc120ba` |
| 56.3 | Phase-56 hygiene + APPEND-D-AL ORCID cross-surface item closure documentation | docs | `21ab9b7` |
| 56.4 | Phase 56 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-55 5-unit framework shape
verbatim.

## Sixth realization of constructor-arg-only expansion + first 6-realization for that pattern + first TWO coexisting 6-realization framework patterns + first all-4-surfaces quintuple-alias + first all-4-surfaces 4-consumer same-slot + first all-surfaces saturated at max-consumer-cardinality + ties Phase-52 fastest cross-surface closure record

- `PHASE_54_DEFAULT_ENABLED_SURFACES` evolved from
  `new Set(["rationale"])` (Phase 54 ship; Phase 55 alias-
  syntax preserved size 1) to `new Set(["bio", "reviewNotes", "rationale", "actionRationale"])`
  (Phase 56 ship). Constructor-arg value-only change.
- `OrcidExtensionRegistry` class + factory dispatch arm +
  plugin body + `ORCID_PATTERN` regex **UNCHANGED**.
- **5 baseline test blocks in `lib/markdown/index.test.ts`
  decoupled** with explicit `new Set(["rationale"])`
  constructor args (D-12 decoupled-baseline-block pattern;
  third application of the discipline after Phase 49 doi +
  Phase 52 pubmed).
- **3 dispatch tests flipped** in
  `lib/markdown/extensions/index.test.ts` to assert all-4-
  surfaces behavior.
- **1 registry assertion flipped** in
  `lib/markdown/extensions/orcid.test.ts` from size-1 to
  size-4.
- **21 NEW end-to-end tests** in
  `lib/markdown/index.test.ts` (Unit 56.2) across 3 describe
  blocks (mirrors Phase 52 Unit 52.2 +21 delta verbatim).
- ADR-0018 D-G APPEND adds Phase-56 EXTENDED block + **APPEND-
  D-AN** (Unit 56.1; fourteenth two-letter slot).
- APPEND-D-AL ORCID cross-surface item CLOSED at **2-phase
  carryover** — **ties Phase-52 pubmed-cross-surface fastest
  closure record**.
- **Sixth realization of the "constructor-arg-only zero-rework
  expansion" property** — **first 6-realization for that
  pattern in project history**.
- **First state with TWO coexisting 6-realization framework
  patterns** — plugin-regex-extension (Phase 46 + 47 + 48 +
  51 + 53 + 55; reached 6 at Phase 55) AND constructor-arg-
  only-expansion (Phase 42 + 43 + 44 + 49 + 52 + 56; reaches
  6 at Phase 56).
- **First "all 4 surfaces are quintuple-alias" state**.
- **First "all 4 surfaces have 4-consumer same-slot
  composition" state**.
- **First "all 4 surfaces with 6-consumer composition under
  default dispatch" state** — **first "all-surfaces saturated
  at maximum-consumer-cardinality" state** in project history.
- Cross-surface-expansion cadence trajectory: 4 → 4 → 3 → 4
  → 2 → 2 phases. **Second cross-surface-expansion closure
  at the 2-phase floor**; sustained-floor confirmed.

## Phase 55 → Phase 56 delta

| Metric | Phase 55 close | Phase 56 close | Δ |
|---|---|---|---|
| Tests | 1303 / 74 files | **1324 / 74 files** | +21 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 22 | **23** (project record extends; fourteenth two-letter slot D-AN in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 7 | 7 | 0 |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 19 | 19 | 0 |
| **Phase-37-framework concrete consumers** | **6** | **6** | 0 |
| **Plugin-regex-extension realizations** | **6** | **6** | 0 |
| **Constructor-arg-only zero-rework expansion realizations** | **5 (Phase 42+43+44+49+52)** | **6 (+orcid Phase 56)** | **+1 (new record)** |
| **Coexisting 6-realization framework patterns** | **1 (plugin-regex-extension)** | **2 (+constructor-arg-only-expansion)** | **+1 (first state)** |
| **Dual-form regex realizations** | **4** | **4** | 0 |
| **Surfaces with N+ alias-syntax consumers (max cardinality)** | **5 (quintuple-alias; rationale at Phase 55)** | **5 (quintuple-alias; all 4 surfaces at Phase 56)** | **+3 surfaces (first all-4-surfaces quintuple-alias)** |
| **Surfaces with 4-consumer same-slot composition** | **1 (rationale)** | **4** | **+3 (first all-4-surfaces 4-consumer same-slot)** |
| **Surfaces with 6-consumer composition under default dispatch** | **1 (rationale)** | **4** | **+3 (first all-surfaces-saturated-at-max-cardinality)** |
| **Cross-surface-expansion APPEND-deferral closures** | **5** | **6** | **+1** |
| **2-phase APPEND-deferral closures (cross-surface)** | **1 (Phase 52 pubmed)** | **2 (+Phase 56 orcid)** | **+1 (first tied state)** |
| **D-clauses with BOTH items closed within cadence** | **2 (D-AC + D-AH)** | **3 (+D-AL)** | **+1 (first 3-D-clauses-with-both-items state)** |
| **APPEND-deferral closure cadence (consecutive phases)** | 14 | **15 (Phase 42-56)** | **+1 (new record)** |

## Architectural firsts in Phase 56 (12 enumerated)

1. **Sixth realization of the "constructor-arg-only zero-
   rework expansion" property** (Phase 42 wikilinks + Phase 43
   tables + Phase 44 arxiv + Phase 49 doi + Phase 52 pubmed +
   Phase 56 orcid). **First 6-realization for the constructor-
   arg-only-expansion pattern in project history** (extends
   Phase-52 5-realization record 5 → 6). **Generalizes the
   per-consumer all-4-surfaces arc to ALL 6 Phase-37-framework
   consumers** — every concrete consumer is now default-
   enabled on every wired markdown surface.
2. **First state with TWO coexisting 6-realization framework
   patterns**. Plugin-regex-extension (Phase 46 + 47 + 48 +
   51 + 53 + 55) reached 6 at Phase 55. Constructor-arg-only-
   zero-rework-expansion (Phase 42 + 43 + 44 + 49 + 52 + 56)
   reaches 6 at Phase 56. **First state where the framework
   exhibits two patterns each with 6 realizations**. The two
   patterns differ in dimension: plugin-body axis vs registry-
   state axis. Together they cover the two principal axes of
   zero-rework framework extension at 6 realizations each
   spanning the full 6-consumer roster.
3. **First "all 4 surfaces are quintuple-alias" state** in
   project history. Pre-Phase-56 only rationale was quintuple-
   alias (Phase 55 ship: wikilinks + arxiv + doi + pubmed +
   orcid aliases simultaneously); post-Phase-56 every surface
   carries the 5-alias-syntax-consumer composition under 6-
   way default. **First surface-with-5-alias-consumers
   cardinality of 4**.
4. **First "all 4 surfaces have 4-consumer same-slot
   composition" state** in project history. Pre-Phase-56 only
   rationale carried `[arxiv, doi, pubmed, orcid]` in
   `remarkPlugins`; post-Phase-56 every surface does. **First
   state where regex-disjointness-as-sole-defense discipline
   at 4-consumer cardinality (Phase 54 established) is
   exercised on every surface in production default**.
5. **First "all 4 surfaces with 6-consumer composition under
   default dispatch" state** in project history. Pre-Phase-56
   only rationale carried 6-consumer composition (Phase 54
   maximum-consumer-cardinality state); post-Phase-56 every
   surface does. **Maximum-consumer-cardinality state
   generalized to all surfaces** — Phase-54 asymmetric
   `[rationale=6, others=5]` becomes symmetric `[all=6]`.
   **First "all-surfaces saturated at maximum-consumer-
   cardinality" state** in project history.
6. **Ties Phase-52 fastest cross-surface-expansion APPEND-
   deferral closure record** at **2-phase carryover** (Phase
   54 → 56). **Second cross-surface-expansion closure at the
   2-phase floor**. Cadence trajectory: 4 → 4 → 3 → 4 → 2 →
   2 phases (Phase 42 + 43 + 44 + 49 + 52 + 56). **First state
   where two cross-surface-expansion closures tie at 2-phase
   carryover** — the 2-phase carryover is now the sustained-
   floor for cross-surface-expansion APPEND-deferral closure.
7. **Sixth cross-surface-expansion APPEND-deferral closure**
   in the cadence (Phase 42 + 43 + 44 + 49 + 52 + 56).
   Cross-surface-expansion cadence now established at 6
   realizations — one closure per Phase-37-framework consumer,
   mirroring the constructor-arg-only-expansion property's 6
   realizations.
8. **Third D-clause with BOTH items closed within the closure
   cadence**. APPEND-D-AL (Phase 54 ORCID first-ship APPEND)
   carries 2 deferrals: alias (closed Phase 55 at 1-phase
   carryover via D-AM) + cross-surface (closes Phase 56 at
   2-phase carryover via D-AN). **D-AC was first (Phase 48
   alias item 2 + Phase 49 cross-surface); D-AH was second
   (Phase 51 alias + Phase 52 cross-surface); D-AL is third**.
   **First state where THREE D-clauses have BOTH their
   enumerated deferrals resolved through the cadence** —
   confirms the 2-item-both-closed pattern as the framework's
   standard shape for N-th-consumer first-ship APPENDs.
9. **Fifteenth prep-/APPEND-doc-level deferral closed by a
   later phase**. **APPEND-deferral closure cadence sustained
   15 phases — new longest sustained cadence in project
   history** (extends Phase-55 record 14 → 15). **First 15-
   phase APPEND-deferral closure run**.
10. **23rd APPEND on ADR-0018 D-G** — extends first-ADR-D-
    clause-with-most-APPENDs record 22 → 23. **Fourteenth
    two-letter APPEND letter D-AN** (Excel-spreadsheet column
    convention sustained).
11. **First "framework + 6 consumers + 6 expansions + 6
    plugin-regex-extensions + 4-consumer same-slot × 4
    surfaces + 6-consumer composition × 4 surfaces + alias
    syntax × 5 + dual-form regex × 4 + 5-consumer composition
    × 4 + inner-class disjunction × 1 + 2-evolutions on
    arxiv + quintuple-alias × 4" 20-phase cluster** in
    project history (Phase 37-56). 20-phase architectural arc.
    Plus **26th consecutive phase without new B category** —
    Phase 31-56 = first 26-phase run (extends Phase-55 record
    25 → 26). Plus **forty-seventh NON-§13 phase**. Plus
    **139th consecutive 103 kB First Load JS unit** (extends
    from 135 at Phase-55 close). Plus **21st consecutive no-
    new-ADR phase** (Phase 36-56; extends Phase-55 record
    20 → 21).
12. **51st "Continue" override invocation** at Unit 56.0 —
    extends the **half-century-of-Continue-overrides
    milestone** (Phase 55 = 50th) to **51 over Phases
    6-56**. **First override invocation past the half-century
    threshold** — marks the durability of the §12-cardinal-
    rule sign-off discipline across the project's multi-month
    rhythm.

## Phase 56 → Phase 57 entry conditions

Per §12, Phase 57 entry requires **explicit human sign-off**.

**Recommended Phase 57 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **Table-specific attributes** (`colspan` / `rowspan` / `scope`; APPEND-D-Q item 3) — schema-override extension on existing `TablesExtensionRegistry`; **first regex/schema-extension equivalent on `schemaOverrides` slot** (would generalize plugin-regex-extension pattern off remarkPlugins to a new slot kind) | 1-2 | XSS-audit-required; schema-extension only |
| 2 | **bioRxiv preprint consumer** (`biorxiv:` / DOI-overlap with doi consumer) — **seventh concrete consumer**; first 5th-`remarkPlugins` consumer; tests whether regex-disjointness scales from 4 to 5 same-slot consumers | 2-3 | DOI overlap concern requires regex disambiguation |
| 3 | **Cross-entity wikilinks** (APPEND-D-L item 3) — entity-type disambiguation + plugin parameterization | 2-3 | Requires APPEND-D-L item 6 plugin parameterization to land first |
| 4 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — couples with `schemaOverride` for `class` attribute on `<a>`; first single-consumer-multi-slot case | 1-2 | XSS audit per attribute value |
| 5 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 6 | **Bare arxiv / DOI / PubMed / ORCID IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 7 | **Empty-alias fallback unification** across consumers (Phase-48 deferral carries) — refine arxiv/doi/pubmed/orcid empty-alias divergence | 1 | Smallest-scope autonomous-tractable |
| 8 | **OSF preprint consumer** — seventh or later concrete consumer alternative to bioRxiv | 2-3 | DOI overlap concern |
| 9 | **Plugin parameterization for wikilink-href-builder** (APPEND-D-L item 6) — enables Rank 3 cross-entity-wikilinks downstream | 1-2 | Mechanical refactor; autonomous-tractable |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1324 / 74 files** (+21 / 0 vs Phase 55
  close).
- `pnpm audit-content` — 0 errors / 6 warnings (52 consecutive
  phases at Q32 baseline; longest stable-warnings streak
  extends 51 → 52).
- First Load JS — **103 kB UNCHANGED** (139 consecutive units
  at Unit 56.4 ship).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 57+ deferrals)

NOT table-specific attributes (Phase 57+ rank 1);
NOT bioRxiv preprint consumer (Phase 57+ rank 2);
NOT cross-entity wikilinks (Phase 57+ rank 3);
NOT `<a class="wikilink">` styling (Phase 57+ rank 4);
NOT 404 handling (Phase 57+ rank 5);
NOT bare arxiv / DOI / PubMed / ORCID IDs without prefix (Phase
57+ rank 6);
NOT empty-alias fallback unification (Phase 57+ rank 7);
NOT OSF preprint consumer (Phase 57+ rank 8);
NOT plugin parameterization for wikilink-href-builder (Phase
57+ rank 9);
NOT dx.doi.org legacy host (Phase 57+);
NOT stricter trailing-lookahead (Phase 57+);
NOT paper-card hover-preview (Phase 57+);
NOT auto-trim of alias display whitespace (Phase 57+);
NOT `<caption>` (Phase 57+);
NOT surface-specific table schemas (Phase 57+);
NOT `@mention` (Phase 57+);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 57+);
NOT 5th-or-later `remarkPlugins` consumer beyond arxiv + doi +
pubmed + orcid (Phase 57+ rank 2 ships bioRxiv as 5th
candidate);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 57+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 57+
rank 1 ships table-specific attributes as a regex/schema-
extension on the existing tables consumer, not a 2nd consumer);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-55 deferrals carry forward.

## Boundary statement

**Phase 56 CLOSED at HEAD `<this-unit>` after 5 numbered units
(56.0 prep + 56.1 constant-value flip + 5 baseline blocks
decoupled + 3 dispatch tests flipped + 1 registry assertion
flipped + APPEND-D-AN + 56.2 21 NEW end-to-end tests + 3 new
describe blocks + 56.3 hygiene + 56.4 gate). Sixth realization
of the "constructor-arg-only zero-rework expansion" property:
ORCID cross-surface expansion from `rationale`-only to all 4
markdown surfaces via constructor-arg value-only change in
`PHASE_54_DEFAULT_ENABLED_SURFACES`. First 6-realization for
the constructor-arg-only-expansion pattern in project history
(Phase 42 wikilinks + Phase 43 tables + Phase 44 arxiv + Phase
49 doi + Phase 52 pubmed + Phase 56 orcid; extends Phase-52
5-realization record to 6). First state with TWO coexisting
6-realization framework patterns — plugin-regex-extension
(Phase 46 + 47 + 48 + 51 + 53 + 55; reached 6 at Phase 55) AND
constructor-arg-only-zero-rework-expansion (Phase 42 + 43 +
44 + 49 + 52 + 56; reaches 6 at Phase 56). First state where
the framework exhibits two patterns each with 6 realizations;
together they cover the two principal axes of zero-rework
framework extension (plugin-body axis + registry-state axis)
at 6 realizations each spanning the full 6-consumer roster.
Generalizes the per-consumer all-4-surfaces arc to ALL 6
Phase-37-framework consumers — every concrete consumer is now
default-enabled on every wired markdown surface. Closes APPEND-
D-AL ORCID cross-surface item at 2-phase carryover (Phase 54
→ 56). TIES PHASE-52 PUBMED-CROSS-SURFACE 2-PHASE FASTEST-
CLOSURE RECORD. Second cross-surface-expansion closure at the
2-phase floor; first state where two cross-surface-expansion
closures tie at 2-phase carryover — the 2-phase carryover is
now the sustained-floor for cross-surface-expansion APPEND-
deferral closure. Cadence trajectory: 4 → 4 → 3 → 4 → 2 → 2
phases. First "all 4 surfaces are quintuple-alias" state in
project history — every surface carries wikilinks + arxiv +
doi + pubmed + orcid aliases simultaneously under 6-way
default. First surface-with-5-alias-consumers cardinality of
4. First "all 4 surfaces have 4-consumer same-slot
composition" state — regex-disjointness-as-sole-defense
discipline at 4-consumer cardinality (Phase 54 established)
exercised on every surface in production default. First "all
4 surfaces with 6-consumer composition under default dispatch"
state — maximum-consumer-cardinality state generalized to all
surfaces; Phase-54 asymmetric `[rationale=6, others=5]`
becomes symmetric `[all=6]`. First "all-surfaces saturated at
maximum-consumer-cardinality" state in project history. Third
D-clause with BOTH items closed within the closure cadence
(D-AL alias Phase 55 + cross-surface Phase 56); first state
where THREE D-clauses have BOTH their enumerated deferrals
resolved through the cadence (D-AC + D-AH + D-AL). Sixth
cross-surface-expansion APPEND-deferral closure in the cadence;
cross-surface-expansion arc now spans all 6 framework consumers.
APPEND-deferral closure cadence sustained 15 phases — new
longest sustained cadence in project history (extends Phase-55
record 14 → 15). First 15-phase APPEND-deferral closure run.
Maximal multi-consumer all-surfaces composition extended to
6-consumer + 4-consumer-same-slot cardinality — defines the
new upper bound for the Phase-37-framework's current
capabilities. 23rd APPEND on ADR-0018 D-G (project record
extends 22 → 23). Fourteenth two-letter APPEND letter D-AN.
Forty-seventh NON-§13 phase. First "framework + 6 consumers +
6 expansions + 6 plugin-regex-extensions + 4-consumer same-
slot × 4 surfaces + 6-consumer composition × 4 surfaces +
alias syntax × 5 + dual-form regex × 4 + 5-consumer composition
× 4 + inner-class disjunction × 1 + 2-evolutions on arxiv +
quintuple-alias × 4" 20-phase cluster in project history
(Phase 37-56). 26th consecutive phase without new B category —
first 26-phase run (extends Phase-55 record 25 → 26). 21st
consecutive no-new-ADR phase (Phase 36-56; extends Phase-55
record 20 → 21). 139th consecutive 103 kB First Load JS unit
(extends from 135 at Phase-55 close). 51st "Continue" override
invocation — first override invocation past the half-century
threshold (over Phases 6-56). XSS line of defense preserved.
1324 tests across 74 vitest files (+21/0 vs Phase 55). 24 ADRs
UNCHANGED. 7 DB tables UNCHANGED. 9 migrations UNCHANGED. 14
env vars UNCHANGED. 168 i18n keys per locale UNCHANGED. First
Load JS 103 kB UNCHANGED. Middleware 160 kB UNCHANGED.
OPEN_QUESTIONS top-level Q-count 66 UNCHANGED. Phase-37+
candidate count 8 UNCHANGED. `MARKDOWN_EXTENSIONS` recognized
single-value arms 7 UNCHANGED. Zero new ADRs / migrations /
i18n keys / env vars / operational gates / runtime deps /
files. Phase 57 entry requires explicit human sign-off per
§12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 55 → 56 delta complete.
- [x] Phase 57 entry conditions documented (10 ranked
      candidates).
- [x] Smoke gates pass.
- [x] STOP at Phase 57 boundary per §12.
