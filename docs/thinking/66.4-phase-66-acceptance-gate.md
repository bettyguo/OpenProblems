# 66.4 — Phase 66 acceptance gate

Phase 66 closes at HEAD `b3baa84` (Unit 66.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-67-entry
conditions per §12.

## Phase 66 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 66.0 | Phase 66 prep (rank-1 404 handling for unresolved wikilinks via build-time validator; D-1 framework decisions: build-time validation as 404-handling mechanism + audit-layer extension framing) | docs | `4b65e43` |
| 66.1 | `lib/markdown/extensions/wikilinks-validator.ts` build-time validator helper (extractWikilinkReferences + isValidWikilinkTarget + WikilinkReference + ValidWikilinkTargets; cross-entity routing mirrors Phase-63 CROSS_ENTITY_BUILD_HREF; WIKILINK_PATTERN exported from `./wikilinks.ts` for cross-file re-use) + 17 NEW validator unit tests + ADR-0018 D-G APPEND-D-AX initial declaration | code+APPEND | `188f186` |
| 66.2 | `lib/content/cross-link-audit.ts` Phase-66 evolution (8th `AuditCheck` type `wikilink-target-fk`; rating-action YAML walker; new `summary.danglingWikilinkRefs` field; `scripts/cross-link-audit.ts` SUMMARY line evolves) + 7 NEW audit-integration tests + new `test/fixtures/audit-wikilink-dangling/` fixture | code | `08252a4` |
| 66.3 | Phase-66 hygiene + ADR-0018 APPEND-D-L item 5 closure documentation (28-phase carryover — NEW LONGEST ABSOLUTE; second post-tie absolute-record extension; D-L becomes SECOND D-clause with ALL enumerated items closed; first state where 2 D-clauses have ALL enumerated items closed; first D-clause with 6+ items closed; first D-clause to traverse 0-of-N → ALL-N closed under Phase-37 framework era; first build-time-validation realization) | docs | `b3baa84` |
| 66.4 | **Phase 66 acceptance gate** (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-65 5-unit framework shape
verbatim.

## First build-time-validation realization + D-L FULL CLOSURE + first state where 2 D-clauses have ALL enumerated items closed + first D-clause with 6+ items closed + first D-clause to traverse 0-of-N → ALL-N closed under Phase-37 framework era + 28-phase APPEND-deferral carryover (NEW LONGEST ABSOLUTE) + second post-tie absolute-record extension + first 6-consecutive-phase set-or-extend streak + 25-phase APPEND-deferral closure cadence

- **Thread choice**: Phase-65 acceptance gate's Phase-66-entry table
  ranked **404 handling for unresolved wikilinks** as rank 1 (would
  advance D-L from 5-of-6 → 6-of-6 = ALL-CLOSED; the framework
  decision was "build-time validation vs render-time fallback").
  Phase 66 shipped rank 1 via build-time validation. Alternative
  framings considered and rejected per the Phase-66-prep doc D-1:
  (a) **render-time fallback bundled into Phase 66** — redundant
  under build-time validation; defer to Phase 67+ as belt-and-
  suspenders defense against CI-bypass; (b) **rank 2 locale-
  prefixed paths** — does not advance D-L closure; defer to Phase
  67+; (c) **rank 3 curator-configurable className** — XSS audit
  required + does not advance D-L closure; defer to Phase 67+;
  (d) **rank 4 per-surface schema-options** — tables-only follow-
  on; defer to Phase 67+.
- **`lib/markdown/extensions/wikilinks-validator.ts` first-ship**
  (Unit 66.1): new sibling helper to `wikilinks.ts`. Exports
  `extractWikilinkReferences(markdown, surface): WikilinkReference[]`
  (reuses Phase-63 `WIKILINK_PATTERN` regex re-exported from
  `./wikilinks.ts`) + `isValidWikilinkTarget(ref, validTargets):
  boolean` (cross-entity-aware routing mirrors Phase-63
  `CROSS_ENTITY_BUILD_HREF`) + `WikilinkReference` interface +
  `ValidWikilinkTargets` interface.
- **`lib/content/cross-link-audit.ts` Phase-66 evolution** (Unit
  66.2): 8th `AuditCheck` type `wikilink-target-fk` joins the
  existing 7 (`paper-problem-fk` + `paper-author-fk` + `paper-
  institution-fk` + `author-institution-fk` + `related-problems-
  fk` + `related-problems-symmetry` + `entries-contributions-
  agreement`). New rating-action YAML walker scans
  `content/problems/*/ratings/*.yaml` `dimensions.<dim>.rationale`
  strings; each captured wikilink ref validated; ERROR-severity
  finding emitted on miss with cross-entity-aware label
  (`no such {paper|author|institution|problem} target "{slug}"`).
  New `summary.danglingWikilinkRefs` field.
- **`scripts/cross-link-audit.ts`** (Unit 66.2): SUMMARY line
  evolves — `problems:` row now reports `(X asymmetric related-
  problems edges, Y dangling wikilink refs)`. Second statistic
  in the problems summary tuple.
- **`test/fixtures/audit-wikilink-dangling/`** (Unit 66.2): NEW
  3-file fixture (`taxonomy.yaml` + `problems/real/problem.yaml`
  + `problems/real/ratings/2026-05-14-initial.yaml`) exercising
  4 distinct dangling wikilink shapes (1 bare + 3 cross-entity)
  + 1 valid bare reference as a regression guard.
- **17 NEW validator unit tests in `wikilinks-validator.test.ts`**
  (Unit 66.1) + **7 NEW audit-integration tests in
  `cross-link-audit.test.ts`** (Unit 66.2) = **24 NEW Phase-66
  tests** ÷ 2 layers (validator + audit).
- ADR-0018 D-G APPEND adds Phase-66 EXTENDED block + **APPEND-D-AX**
  (Unit 66.1; twenty-fourth two-letter slot).
- **APPEND-D-L item 5 `404 handling for unresolved wikilinks`
  CLOSED at 28-phase carryover** (Phase 38 → 66). **NEW LONGEST
  ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED** (extends
  Phase-65 27-phase record by 1 phase). **Second post-tie
  absolute-record extension** in project history (Phase 65 was
  first post-tie extension; Phase 66 confirms post-tie-EXTENSION
  pattern as sustained trajectory).
- **D-L FULLY CLOSED at Phase 66** — all 6 enumerated items
  closed (items 1 + 2 + 3 + 4 + 6 + 5 across Phases 42 + 46 +
  62 + 63 + 65 + 66).
- **D-L becomes SECOND D-clause with ALL enumerated items
  closed** in project history (D-Q was first at Phase 64).
  **First state where 2 D-clauses have ALL enumerated items
  closed**. Establishes the D-clause-completion pattern as
  REPEATABLE across the framework — not D-Q-specific.
- **First D-clause with 6+ items closed** (D-L at 6-of-6; D-Q
  has only 4 enumerated items — D-L is the largest enumerated
  D-clause set by item count in project history).
- **First D-clause to traverse from 0-of-N closed → ALL-N closed
  entirely under the Phase-37 framework era** (D-L items first
  enumerated at Phase 38 first-ship; all 6 items closed by
  Phase 66 — a 28-phase arc from creation to completion).
- **First build-time-validation realization** in project history.
  Phase 66 introduces build-time-validation as a sub-pattern
  within the existing `lib/content/cross-link-audit.ts` audit-
  layer machinery. Audit-layer extension framing per D-1
  framework decision — no new principal axis declared at Phase
  66. Phase 67+ could elevate "build-time-validation" to a
  recognized principal axis if a second realization ships.
- **First 6-consecutive-phase set-or-extend streak** in project
  history (Phase 61 new + 62 extend + 63 extend + 64 tie + 65
  extend + 66 extend). Extends Phase-65 5-consecutive-phase
  streak.
- **25-phase APPEND-deferral closure cadence sustained** (Phase
  42-66) — new longest sustained cadence in project history
  (extends Phase-65 record 24 → 25). First 25-phase APPEND-
  deferral closure run.
- **Fifty-seventh NON-§13 phase** (extends Phase-65 milestone to
  57); first 57-phase ledger-closure streak.

## Phase 65 → Phase 66 delta

| Metric | Phase 65 close | Phase 66 close | Δ |
|---|---|---|---|
| Tests | 1524 / 75 files (+ 4 viz hotfix = 1528 / 75 at session start) | **1552 / 76 files** | +24 tests / +1 file (17 validator + 7 audit-integration; new wikilinks-validator.test.ts file) |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 32 | **33** (project record extends; twenty-fourth two-letter slot D-AX in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 10 | **10 UNCHANGED** | 0 (Phase 66 is entirely build-time; no render-time pipeline change; no new factory arm) |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 21 | **22** (+`wikilinks-validator.ts`) | +1 |
| `lib/content/` files | 19 | **19** (modifications only) | 0 |
| `AuditCheck` type count | 7 | **8 (+`wikilink-target-fk`)** | **+1 (first 8-AuditCheck state; first new AuditCheck type since Phase 2 baseline ship)** |
| Phase-37-framework concrete consumers | 7 | **7** | 0 (validator is a sibling build-time helper, not a Phase-37-framework consumer) |
| Plugin-regex-extension realizations | 7 | **7** | 0 |
| Plugin-body axis realizations | 8 | **8** | 0 (validator is build-time; plugin body UNCHANGED) |
| Constructor-arg-only zero-rework expansion realizations | 7 | **7** | 0 |
| Schema-extension realizations | 2 | **2** | 0 |
| Plugin-option-extension realizations | 2 | **2** | 0 |
| Schema-options-axis realizations | 1 | **1** | 0 |
| Dual-form regex realizations | 5 | **5** | 0 |
| **Principal axes of zero-rework framework extension** | 4 | **4** | 0 (Phase 66 is audit-layer extension; no new principal axis declared per D-1 lean) |
| **Build-time-validation realizations** | **0** | **1 (Phase 66 wikilink-target-fk)** | **+1 (first build-time-validation realization)** |
| **Consumers with 3+ evolutions** | 3 (arxiv + tables + wikilinks) | **3** | 0 |
| **Consumers with 4+ evolutions** | 2 (wikilinks + tables) | **2** | 0 |
| **Consumers with 5+ evolutions** | 1 (wikilinks) | **1** | 0 (wikilinks evolution count UNCHANGED at 5; validator is sibling, not 6th evolution) |
| **D-clauses with BOTH items closed** | 4 | **4** | 0 |
| **D-clauses with 3+ items closed** | 2 (D-Q + D-L) | **2** | 0 |
| **D-clauses with 4+ items closed** | 2 (D-L + D-Q) | **2** | 0 (D-L count moves 5 → 6) |
| **D-clauses with 5+ items closed** | 1 (D-L) | **1** | 0 (D-L at 6-of-6) |
| **D-clauses with 6+ items closed** | **0** | **1 (D-L)** | **+1 (first D-clause with 6+ items closed)** |
| **D-clauses with ALL enumerated items closed** | 1 (D-Q) | **2 (D-Q + D-L)** | **+1 (first state where 2 D-clauses have ALL items closed)** |
| **APPEND-deferral closure cadence (consecutive phases)** | 24 | **25 (Phase 42-66)** | **+1 (new record)** |
| **Longest absolute APPEND-deferral closure carryover** | 27-phase (Phase 65 D-L item 4) | **28-phase (Phase 66 D-L item 5) — new record** | **+1 phase (second post-tie absolute-record extension; confirms post-tie-EXTENSION pattern)** |
| **Set-or-tie/extend streak of absolute-record carryovers** | 5 (Phase 61-65) | **6 (Phase 61-66)** | **+1 (first 6-consecutive-phase set-or-extend streak)** |
| **Refactor-only phases** | 1 (Phase 62) | **1** | 0 |
| **Plugin-only emit pattern realizations** | 1 (Phase 65) | **1** | 0 |
| **Non-regex plugin-body realizations** | 1 (Phase 65) | **1** | 0 |
| **Plugin-option-ready-before-consumer-demand realizations** | 1 (Phase 62) | **1** | 0 |
| **Schema-options-ready-before-curator-demand realizations** | 1 (Phase 64) | **1** | 0 |
| **Forward-compat-affordance prerequisite-fulfillment arcs completed** | 1 (Phase 62 → 63) | **1** | 0 |
| **NON-§13 phase count** | 56 | **57** | **+1** |
| **"Continue" override invocations past half-century threshold** | 10 (+Phase 65 = 60th) | **11 (+Phase 66 = 61st)** | **+1 (first phase of the second post-half-century-Continue-decade)** |
| **No-new-ADR consecutive-phase streak** | 30 (Phase 36-65) | **31 (Phase 36-66)** | **+1** |
| **No-new-B-category consecutive-phase streak** | 35 (Phase 31-65) | **36 (Phase 31-66)** | **+1** |

## Architectural firsts in Phase 66 (12 enumerated)

1. **First build-time-validation realization** in project history.
   Phase 66 introduces build-time-validation as a sub-pattern
   within the existing `lib/content/cross-link-audit.ts` audit-
   layer machinery. Audit-layer extension framing per Unit-66.0
   D-1 framework decision — no new principal axis declared at
   Phase 66; Phase 67+ could elevate "build-time-validation" to a
   recognized principal axis if a second realization ships. The 4
   principal axes UNCHANGED at their Phase-65-close counts:
   plugin-body 8 + registry-state 7 + plugin-option 2 + schema-
   options 1.
2. **`wikilink-target-fk` becomes the 8th `AuditCheck` type** in
   `lib/content/cross-link-audit.ts` (was 7 at Phase 65 close).
   **First state where the audit machinery has 8 `AuditCheck`
   types**. **First new `AuditCheck` type since Phase 2 baseline
   ship** (the original 7 checks all date from Unit 2.11 in 2026-
   01; Phase 66 is the first audit-layer evolution since).
3. **D-L becomes SECOND D-clause with ALL enumerated items
   closed** in project history (D-Q was first at Phase 64).
   **First state where 2 D-clauses have ALL enumerated items
   closed**. Establishes the D-clause-completion pattern as
   REPEATABLE across the framework — not D-Q-specific.
4. **First D-clause with 6+ items closed** in project history
   (D-L at 6-of-6; D-Q has only 4 enumerated items). D-L is the
   largest enumerated D-clause set by item count in project
   history.
5. **First D-clause to traverse from 0-of-N closed → ALL-N closed
   entirely under the Phase-37 framework era** (D-L items first
   enumerated Phase 38 first-ship; all 6 items closed by Phase 66
   — a 28-phase arc from creation to completion).
6. **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED**
   at **28-phase carryover** (Phase 38 → Phase 66). Extends
   Phase-65 27-phase record by 1 phase. **Second post-tie
   absolute-record extension** in project history (Phase 65 was
   first post-tie extension; Phase 66 confirms the post-tie-
   EXTENSION pattern as a sustained trajectory). The absolute-
   record trajectory:
   - Phase 61 = 22-phase (new record).
   - Phase 62 = 24-phase (extend by 2).
   - Phase 63 = 25-phase (extend by 1).
   - Phase 64 = 25-phase (TIE; first tie).
   - Phase 65 = 27-phase (extend by 2; first post-tie extension).
   - **Phase 66 = 28-phase (extend by 1; second post-tie
     extension; confirms post-tie-EXTENSION pattern).**
7. **First 6-consecutive-phase set-or-extend streak** in project
   history. Extends Phase-65 5-consecutive-phase streak. Streak
   shape: 1 new + 3 extends + 1 tie + 2 extends within the run
   (the only tie was Phase 64; subsequent phases extend). Streak
   trajectory:
   - Phase 61: 22-phase (new record).
   - Phase 62: 24-phase (extend by 2).
   - Phase 63: 25-phase (extend by 1).
   - Phase 64: 25-phase (TIE).
   - Phase 65: 27-phase (extend by 2).
   - **Phase 66: 28-phase (extend by 1).**
8. **25-phase APPEND-deferral closure cadence sustained** (Phase
   42-66) — new longest sustained cadence in project history
   (extends Phase-65 24-phase record to 25). First 25-phase
   APPEND-deferral closure run.
9. **Twenty-fifth prep-/APPEND-doc-level deferral closed by a
   later phase**: Phase 42 → 38 D-L item 1; …; Phase 65 → 38 D-L
   item 4; **Phase 66 → 38 D-L item 5 (404 handling for
   unresolved wikilinks)**.
10. **Thirty-third APPEND on ADR-0018 D-G** — extends the **first-
    ADR-D-clause-with-most-APPENDs record** from 32 → 33.
    **Twenty-fourth two-letter APPEND letter D-AX** (Excel-
    spreadsheet column convention sustained). First 33-APPEND
    D-clause state.
11. **Fifty-seventh NON-§13 phase** (extends Phase-65 milestone
    to 57). The §13 ledger remains CLOSED across the entire 57-
    phase run (Phase 10-66). **First 57-phase ledger-closure
    streak**.
12. **61st "Continue" override invocation** at Unit 66.0 —
    **eleventh invocation past the half-century threshold**
    (Phase 55 = 50th; …; Phase 65 = 60th; **Phase 66 = 61st**).
    **First phase of the second post-half-century-Continue-
    decade** (Phase 55 closed the original decade; Phases 56-65
    were the first decade-of-post-half-century-Continue; Phase
    66 opens the second decade). **First 12-consecutive-phase
    post-half-century-Continue-override streak** (Phases 55-66).

## Phase 66 → Phase 67 entry conditions

Per §12, Phase 67 entry requires **explicit human sign-off**.

**Framework state note**: Phase 66 introduced build-time
validation as a sub-pattern within the existing audit-layer
machinery; the audit machinery now has 8 `AuditCheck` types. The
4 principal axes of zero-rework framework extension UNCHANGED.
D-L has ALL 6 enumerated items closed (FULL CLOSURE at Phase
66); D-L is the SECOND D-clause to reach ALL-CLOSED state after
D-Q at Phase 64. The D-clause-completion pattern is established
as REPEATABLE.

**Recommended Phase 67 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| **1** | **Render-time fallback for unresolved wikilinks** — would compose Phase-65 className with Phase-66 build-time validation via multi-className emit (`["wikilink", "wikilink-unresolved"]`); **second plugin-body output-shape realization** (extends Phase-65 sub-pattern to 2 realizations) + **first multi-className emit realization** + possible **3rd plugin-option-axis realization** if scoped that way. Belt-and-suspenders defense against CI-bypass. | 2-3 | Autonomous-tractable; depends on plugin signature evolution to accept isValidTarget option OR composes with existing validator helper via build-time injection of the valid-targets set |
| 2 | **Period-allowing slug character-class extension** — would let regex + validator match paper IDs like `[[paper:2109.07958]]`; plugin-body-axis realization lifting both plugin + validator simultaneously; **first plugin-body extension that lifts the validator helper too** | 1-2 | Autonomous-tractable; small scope; but XSS audit per attribute injection vector via dotted slugs |
| 3 | **Locale-prefixed cross-entity wikilink paths** — concrete `buildHref` variant producing `/{locale}/papers/${slug}` etc; **third plugin-option-axis realization** at the registry layer | 1-2 | Autonomous-tractable; small-scope follow-on to Phase-63 ship |
| 4 | **Curator-configurable className value** via plugin-option axis (`ResolveWikilinksOptions.className?: string`) — would generalize Phase-65 hardcoded `"wikilink"` to configurable; **third plugin-option-axis realization** OR **second non-regex plugin-body realization** | 1-2 | Autonomous-tractable; XSS audit per CSS-class-injection vector |
| 5 | **2nd build-time-validation realization** — e.g., link-target-fk for non-wikilink markdown links in curator content; **would elevate "build-time-validation" from sub-pattern to recognized PRINCIPAL AXIS** of zero-rework framework extension; **first 5th principal axis in project history**. Phase 66 introduced the sub-pattern; Phase 67+ second realization would establish the recurrence pattern across the audit layer. | 2-3 | Autonomous-tractable; small-scope follow-on to Phase-66 ship; principal-axis-introduction milestone |
| 6 | **Per-surface schema-options consumer beyond bio-restricted** — second schema-options-axis realization (extends Phase-64 axis to 2 realizations; first 2-realization for the FOURTH axis) | 1-2 | Autonomous-tractable |
| 7 | **Empty-alias fallback unification** across consumers (Phase-48 deferral) | 1 | Smallest-scope autonomous-tractable |
| 8 | **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 9 | **Legacy numeric-only bioRxiv IDs** (pre-2019 format) — 8th plugin-regex-extension | 1-2 | Autonomous-tractable; small-scope |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Most defensible autonomous-tractable next thread**: Rank 1
(**Render-time fallback for unresolved wikilinks**) — would
compose Phase-65 className with Phase-66 build-time validation
via multi-className emit; second plugin-body output-shape
realization; first multi-className emit realization. Establishes
the "build-time + render-time" defense-in-depth pattern for the
wikilinks plugin. **Alternative compelling thread**: Rank 5 (2nd
build-time-validation realization) — would elevate Phase-66 sub-
pattern to recognized PRINCIPAL AXIS = first 5th principal axis
in project history. Both are autonomous-tractable; rank 1 closes
the wikilinks story arc tighter; rank 5 advances the framework
taxonomy.

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm test` — **1552 / 76 files** (+24 tests / +1 file vs
  Phase 65 close).
- `pnpm audit-content` — 0 errors / 6 warnings UNCHANGED + **0
  dangling wikilink refs** (the wikilink-target-fk check is
  ACTIVE on production content; all existing wikilinks resolve;
  63 consecutive phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (187 consecutive units at
  Unit 66.4 ship; 183 at Phase 65 close + viz hotfix + docs
  refresh + 4 Phase 66 units; **187-consecutive-unit streak**).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 67+ deferrals)

NOT render-time fallback for unresolved wikilinks (Phase 67+
rank 1; belt-and-suspenders defense; redundant under build-time
validation);
NOT period-allowing slug character-class extension (Phase 67+
rank 2; would lift regex + validator simultaneously);
NOT locale-prefixed cross-entity wikilink paths (Phase 67+
rank 3);
NOT curator-configurable className value via plugin-option axis
(Phase 67+ rank 4);
NOT 2nd build-time-validation realization (Phase 67+ rank 5;
would elevate sub-pattern to recognized principal axis);
NOT per-surface schema-options consumer beyond bio-restricted
(Phase 67+ rank 6);
NOT empty-alias fallback unification (Phase 67+ rank 7);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 67+ rank 8);
NOT legacy numeric-only bioRxiv IDs (Phase 67+ rank 9);
NOT dx.doi.org legacy host (Phase 67+);
NOT stricter trailing-lookahead (Phase 67+);
NOT paper-card hover-preview (Phase 67+);
NOT auto-trim of alias display whitespace (Phase 67+);
NOT a future plugin that EMITS `<caption>` or
`colSpan`/`rowSpan`/`scope` (Phase 67+);
NOT `@mention` (Phase 67+);
NOT OSF preprint consumer (Phase 67+);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi +
pubmed + orcid + biorxiv (Phase 67+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 67+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 67+;
requires framework refactor);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 67+);
NOT cross-entity entity-type expansion beyond paper / author /
institution (Phase 67+);
NOT curator-facing builder configuration via DB or UI (Phase
67+);
NOT curator-facing per-surface schema configuration via DB or
UI (Phase 67+);
NOT multi-className emit (entity-type-specific styling variants)
(Phase 67+);
NOT 2nd plugin using the plugin-only emit pattern (Phase 67+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-65 deferrals carry forward.

## Boundary statement

**Phase 66 CLOSED at HEAD `<this-unit>` after 5 numbered units
(66.0 prep + 66.1 `lib/markdown/extensions/wikilinks-validator.ts`
new sibling helper exporting `extractWikilinkReferences` +
`isValidWikilinkTarget` + `WikilinkReference` +
`ValidWikilinkTargets` (cross-entity routing mirrors Phase-63
`CROSS_ENTITY_BUILD_HREF`; reuses `WIKILINK_PATTERN` regex re-
exported from `./wikilinks.ts`) + 17 NEW Phase-66 validator unit
tests in `wikilinks-validator.test.ts` + APPEND-D-AX + 66.2
`lib/content/cross-link-audit.ts` Phase-66 evolution (8th
`AuditCheck` type `wikilink-target-fk`; rating-action YAML
walker; new `summary.danglingWikilinkRefs` field;
`scripts/cross-link-audit.ts` SUMMARY line evolves) + 7 NEW
Phase-66 audit-integration tests in `cross-link-audit.test.ts` +
new `test/fixtures/audit-wikilink-dangling/` fixture + 66.3
hygiene + 66.4 gate). **First build-time-validation realization**
in project history — establishes build-time-validation as a sub-
pattern within the existing `lib/content/cross-link-audit.ts`
audit-layer machinery. Audit-layer extension framing per Unit-
66.0 D-1 framework decision — no new principal axis of zero-
rework framework extension declared at Phase 66; the 4 principal
axes UNCHANGED at their Phase-65-close counts (plugin-body 8 +
registry-state 7 + plugin-option 2 + schema-options 1). Phase
67+ could elevate "build-time-validation" to a recognized
PRINCIPAL AXIS if a second realization ships. **`wikilink-
target-fk` becomes the 8th `AuditCheck` type** in
`lib/content/cross-link-audit.ts` (was 7 at Phase 65 close;
first new `AuditCheck` type since the Phase-2 baseline ship in
Unit 2.11). Cross-entity-aware routing mirrors Phase-63
`CROSS_ENTITY_BUILD_HREF` exactly — drift-free. **Closes ADR-
0018 APPEND-D-L item 5 `404 handling for unresolved wikilinks`
at 28-phase carryover** (Phase 38 → Phase 66) — **NEW LONGEST
ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED** (extends Phase-
65 27-phase record by 1 phase). **Second post-tie absolute-
record extension** in project history (Phase 65 was first post-
tie extension; Phase 66 confirms post-tie-EXTENSION pattern as
sustained trajectory). APPEND-D-L closure trajectory: item 1
cross-surface 4-phase (Phase 42); item 2 alias 8-phase (Phase
46); item 6 plugin parameterization 24-phase (Phase 62); item 3
cross-entity 25-phase (Phase 63); item 4 className styling 27-
phase (Phase 65); **item 5 404 handling 28-phase (Phase 66; new
record; LAST remaining D-L item)**. **D-L FULLY CLOSED at Phase
66** — all 6 enumerated items closed. **D-L becomes SECOND
D-clause with ALL enumerated items closed** in project history
(D-Q was first at Phase 64). **First state where 2 D-clauses
have ALL enumerated items closed**. **First D-clause with 6+
items closed** (D-L at 6-of-6; D-Q has only 4 enumerated items
— D-L is the largest enumerated D-clause set by item count in
project history). **First D-clause to traverse from 0-of-N
closed → ALL-N closed entirely under the Phase-37 framework
era** (D-L items first enumerated Phase 38 first-ship; all 6
items closed by Phase 66 — a 28-phase arc from creation to
completion). **First 6-consecutive-phase set-or-extend streak**
in project history (Phase 61 new + 62 extend + 63 extend + 64
tie + 65 extend + 66 extend). **25-phase APPEND-deferral closure
cadence sustained** — new longest sustained cadence (extends
Phase-65 record 24 → 25); first 25-phase APPEND-deferral closure
run. **`MARKDOWN_EXTENSIONS` single-value arms UNCHANGED at
10** — Phase 66 is entirely build-time; no render-time pipeline
change; no new factory arm. Plugin body UNCHANGED; render-time
pipeline UNCHANGED; wikilinks consumer evolution count UNCHANGED
at 5 (validator is a sibling build-time consumer, not a 6th
plugin evolution). External markdown links UNCHANGED. Composite
registry composition rules preserved. XSS defenses preserved
(validator inherits the regex-validated character-class
constraint slug + entityType both `[a-z0-9-]+`; no rendering,
no HTML interpolation; no new XSS surface). 33rd APPEND on ADR-
0018 D-G (project record extends 32 → 33). Twenty-fourth two-
letter APPEND letter D-AX. Fifty-seventh NON-§13 phase (extends
Phase-65 milestone to 57); first 57-phase ledger-closure streak.
36th consecutive phase without new B category — first 36-phase
run (extends Phase-65 record 35 → 36). 31st consecutive no-new-
ADR phase (Phase 36-66; extends Phase-65 record 30 → 31; first
31-phase no-new-ADR streak). 187th consecutive 103 kB First Load
JS unit (extends from 183 at Phase-65 close). 61st "Continue"
override invocation — eleventh invocation past the half-century
threshold (Phase 55 = 50th; …; Phase 66 = 61st); **first phase
of the second post-half-century-Continue-decade** (Phase 55
closed the original decade; Phases 56-65 were the first decade-
of-post-half-century-Continue; Phase 66 opens the second
decade); **first 12-consecutive-phase post-half-century-
Continue-override streak** (Phases 55-66). 1552 tests across 76
vitest files (+24 tests / +1 file vs Phase 65 close baseline
1528/75). 24 ADRs UNCHANGED. 7 DB tables UNCHANGED. 9 migrations
UNCHANGED. 14 env vars UNCHANGED. 168 i18n keys per locale
UNCHANGED. First Load JS 103 kB UNCHANGED. Middleware 160 kB
UNCHANGED. OPEN_QUESTIONS top-level Q-count 66 UNCHANGED. Phase-
37+ candidate count 8 UNCHANGED. `MARKDOWN_EXTENSIONS`
recognized single-value arms 10 UNCHANGED. 2 new files (sibling
validator helper + validator test file) + 3 new fixture files
(taxonomy + problem + rating) + 4 modified files (`wikilinks.ts`
+ `cross-link-audit.ts` + `cross-link-audit.test.ts` +
`scripts/cross-link-audit.ts`) + ADR-0018 APPEND. Zero new ADRs
/ migrations / i18n keys / operational gates / runtime deps.
Phase 67 entry requires explicit human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 65 → 66 delta complete.
- [x] Phase 67 entry conditions documented (10 ranked
      candidates).
- [x] Framework-state-note explicitly flagged (Phase 66
      introduced build-time-validation as a sub-pattern within
      the existing audit-layer machinery; audit machinery now
      has 8 AuditCheck types; D-L FULLY CLOSED; SECOND D-clause
      with ALL items closed; FIRST D-clause with 6+ items closed).
- [x] Smoke gates pass.
- [x] STOP at Phase 67 boundary per §12.
