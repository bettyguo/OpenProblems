# 64.0 — Phase 64 prep (**surface-specific table schemas** — last remaining APPEND-D-Q deferral (item 6); adds optional second constructor arg `options: { surfaceSchemaOverrides?: ReadonlyMap<MarkdownSurface, Partial<Schema>> }` to `TablesExtensionRegistry` so curators can supply DIFFERENT schemas per surface (Phase 39-61 invariant preserved byte-identical when `surfaceSchemaOverrides` is undefined); adds new exported `PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES` constant demonstrating a concrete differentiation (bio gets a restricted basic-tables variant per APPEND-D-Q item 6 example; reviewNotes + rationale + actionRationale get the full Phase-61 schema); adds new exported `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` constant for the bio variant; adds new `MARKDOWN_EXTENSIONS=tables-per-surface` single-value arm wiring the per-surface-aware registry; **first realization of a FOURTH principal axis of zero-rework framework extension** — schema-options axis joins registry-state (Phase 38+; 7 realizations), plugin-body (Phase 46+; 7 realizations), and plugin-option (Phase 62+; 2 realizations) axes; **first state where the framework has FOUR principal axes of zero-rework extension in project history**; **first schema-options-ready-before-curator-demand realization** (generalizes Phase-62 plugin-option-ready-before-consumer-demand to the schema slot); **tables consumer gains 4th evolution post-first-ship** (Phase 43 cross-surface + Phase 57 attributes + Phase 61 caption + Phase 64 per-surface) — **first state where TWO consumers have 4+ evolutions** (wikilinks at 4 + tables joins); closes ADR-0018 APPEND-D-Q item 6 `Surface-specific table schemas` at **25-phase carryover** (Phase 39 → Phase 64) — **TIES Phase-63 absolute APPEND-deferral closure record** without extending (first absolute-record-TIE in project history; ends the 3-consecutive-phase absolute-record-EXTENSION streak but begins a 4-consecutive-phase set-or-tie streak — Phase 61 new + 62 extend + 63 extend + 64 tie); **D-Q becomes first D-clause with ALL enumerated items closed within the Phase-39-tables-cap subset** (items 2 + 3 + 4 + 6 — the four items deferred at Phase-39 first-ship — all closed) — **first D-clause with ALL items closed** in project history; APPEND-D-AV twenty-second two-letter slot; anticipated 5 units; **59th "Continue" override** — ninth invocation past half-century threshold; **fifty-fifth NON-§13 phase**; new env-var arm `tables-per-surface` brings `MARKDOWN_EXTENSIONS` single-value arms from 9 to 10 (second new arm in 2 consecutive phases — **first 2-consecutive-phase new-arm-addition streak** in project history))

> **Thread choice note (Phase-63 gate rank 1)**: Phase 63 acceptance
> gate's Phase-64-entry table ranked **surface-specific table
> schemas** as rank 1 (the most-defensible autonomous-tractable
> next thread; closure makes D-Q the first D-clause with ALL
> enumerated items closed in project history). Phase 64 ships rank
> 1. Alternative framings considered and rejected: (a) rank 2
> `<a class="wikilink">` styling — still blocked on framework
> decision regarding multi-source schemaOverrides (would require
> either ADR APPEND violating APPEND-D-C no-deep-merge contract OR
> plugin-only emit pattern requiring further framework
> reconnaissance); (b) rank 3 404 handling for unresolved
> wikilinks — would close D-L items 4+5 over Phase 64-66 but
> introduces build-time validation step before D-Q closure (which
> is in-hand); (c) bundling per-surface schemas WITH a Phase-65+
> bio-restricted plugin demand signal — premature without curator
> data; the schema-options-ready discipline established Phase 64
> enables that future demand to land as a 1-unit value-only ship.

Phase 64 kickoff per `MASTER_PROMPT.md` §12 cardinal rule. Phase 63
closed at HEAD `e36d597` (post-Unit-63.4 README + PROGRESS_SUMMARY
hygiene; gate at HEAD `fad1328`; fifty-fourth NON-§13 phase; 5
numbered units; **first consumer of a forward-compat plugin-option
affordance shipped in a prior phase**; **first 2-phase forward-
compat-affordance prerequisite-fulfillment arc completed**; **first
registry-level realization of the plugin-option axis**; **first 2-
realization for the plugin-option-axis**; **wikilinks consumer
gains 4th evolution post-first-ship**; **first state where a
consumer has 4+ evolutions**; **D-L becomes first D-clause with
4-of-6 enumerated items closed**; **first D-clause with 4+ items
closed**; **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER
OBSERVED** at 25-phase carryover; **third consecutive phase to set
the absolute-record**; **first 3-consecutive-phase absolute-
record-extension streak**; 22-phase APPEND-deferral closure
cadence; 30th D-G APPEND record extends; twenty-first two-letter
APPEND letter D-AU; **first new `MARKDOWN_EXTENSIONS` single-
value arm since Phase 58 bioRxiv**; **first 5-phase gap between
single-value-arm additions**; 58th "Continue" override). This
unit is docs-only.

> **Sign-off note**: §12 phase-boundary sign-off granted via the
> **59th "Continue" override** invocation (over Phases 6-64).
> **Ninth invocation past the half-century threshold** (Phase
> 55 = 50th; …; Phase 63 = 58th; Phase 64 = 59th). User selected
> Rank 1 via `AskUserQuestion` AND issued a literal "Continue"
> message, per Phase-45-boundary discipline. First-thread D-1
> recommendation overridable into Unit 64.1.

> **§13 ledger status (carried)**: still **CLOSED**. Phase 64 is
> the **fifty-fifth NON-§13 phase** (extends Phase-63 half-
> century milestone to 55).

## THINK

### Phase 63 → Phase 64 baseline (HEAD = `e36d597`; clean tree)

- **Tests**: **1486 / 75 vitest files** (+22 / 0 vs Phase 62
  close).
- **ADRs**: **24** (Phase 63 added 0 new ADRs; 28 consecutive
  no-new-ADR phases since Phase 36 — Phase 64 anticipates
  **29**).
- **ADR-0018 D-G**: **30 APPENDs** (project record). Two-letter
  slots IN USE since Phase 43 D-AA … Phase 63 D-AU; Phase 64
  takes **D-AV** (twenty-second two-letter slot).
- **First Load JS shared chunk**: **103 kB UNCHANGED** through
  every Phase 9-63 unit (**173 consecutive units** at Unit 63.4
  ship; 100-unit threshold crossed at Phase 48 Unit 48.3; Phase
  64 anticipates **178+ at gate**).
- **Middleware bundle**: **160 kB UNCHANGED** since Phase 12.
- **DB tables**: 7 unchanged; **Migrations**: 9 unchanged.
- **Env vars**: **14** (`MARKDOWN_EXTENSIONS` 9 single-value
  arms + compositions). Phase 64 anticipates **14 unchanged**
  count for env-var COUNT but **10 single-value arms** (+1
  `tables-per-surface`) — **second new arm in 2 consecutive
  phases** (Phase 63 added `wikilinks-cross-entity`; Phase 64
  adds `tables-per-surface`). **First 2-consecutive-phase
  new-arm-addition streak** in project history. Prior Phase
  59-62 each evolved existing consumers without new arm;
  Phase 63 broke the 5-phase no-new-arm gap; Phase 64 makes it
  consecutive.
- **`lib/markdown/extensions/` files**: **21** UNCHANGED.
  Phase 64 evolves existing `tables.ts` constructor signature
  + adds factory arm in existing `index.ts` + extends
  `tables.test.ts`; no new file.
- **Phase-37-framework concrete consumers**: **7**. Phase 64
  keeps count at **7** (per-surface schemas is schema-options-
  axis evolution on existing tables consumer + new factory arm;
  no new consumer file).
- **Plugin-regex-extension realizations**: **7** UNCHANGED
  (Phase 60 ship). Phase 64 anticipates **7 UNCHANGED** (per-
  surface schemas does not touch any plugin body).
- **Constructor-arg-only zero-rework expansion realizations**:
  **7** UNCHANGED (Phase 59 ship). Phase 64 anticipates **7
  UNCHANGED** (per-surface schemas adds a NEW optional
  constructor arg — the SHAPE differs from constructor-arg-
  ONLY-value-change realizations; counted under schema-options
  axis instead).
- **Schema-extension realizations**: **2** UNCHANGED (Phase 57
  attributes + Phase 61 caption). Phase 64 anticipates **2
  UNCHANGED** (per-surface schemas does NOT extend the schema
  CONTENT — `GFM_TABLE_SCHEMA_OVERRIDES` is unchanged; the
  NEW `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` is a
  SUBSET of the existing schema, not an extension; Phase 64
  counted under schema-options axis).
- **Plugin-option-extension realizations**: **2** UNCHANGED
  (Phase 62 affordance + Phase 63 registry consumer). Phase
  64 anticipates **2 UNCHANGED** (Phase 64 is a NEW AXIS
  realization, not a plugin-option realization — distinct
  framework slot).
- **Schema-options-axis realizations**: **0** at Phase 63
  close. Phase 64 anticipates **1** — **FIRST realization of
  schema-options axis as a named framework-extension axis in
  project history**. Schema-options axis is the FOURTH
  principal axis of zero-rework framework extension; parallels
  the Phase-62 plugin-option axis but operates on the
  schemaOverrides slot instead of plugin invocation.
- **Dual-form regex realizations**: **5** UNCHANGED.
- **Tables consumer evolutions**: **3** (Phase 43 cross-surface;
  Phase 57 attributes; Phase 61 caption). Phase 64 anticipates
  **4** (+per-surface) — **first 4-evolution-on-tables state**
  in project history; tables joins wikilinks at depth-4 — **first
  state where TWO consumers have 4+ evolutions** in project
  history (wikilinks was first at Phase 63; tables joins at
  Phase 64). Other consumers: arxiv at 3 evolutions (Phase 44
  + 47 + 53); doi/pubmed/orcid/biorxiv at 1-2 evolutions each.
- **Principal axes of zero-rework framework extension**: **3**
  at Phase 63 close. Phase 64 anticipates **4** — **schema-
  options axis** joins registry-state axis (Phase 38+; 7
  realizations), plugin-body axis (Phase 46+; 7 realizations),
  and plugin-option axis (Phase 62+; 2 realizations). **First
  state where the framework has FOUR principal axes of zero-
  rework extension** in project history (Phase 62 introduced
  3 axes; Phase 64 introduces the 4th).
- **`MARKDOWN_EXTENSIONS` single-value arms**: **9** (Phase
  63 added `wikilinks-cross-entity`). Phase 64 anticipates
  **10** (+`tables-per-surface`) — **second new arm in 2
  consecutive phases**; **first 2-consecutive-phase new-arm-
  addition streak** in project history (Phase 59-62 each
  evolved without new arm; Phase 63 broke the 5-phase no-
  new-arm gap; Phase 64 makes consecutive). Per Phase-63 close
  the gap pattern was: Phase 38 first arm → … → Phase 58
  bioRxiv (8 arms) → Phase 63 wikilinks-cross-entity (5-phase
  gap to 9 arms) → Phase 64 tables-per-surface (1-phase gap
  to 10 arms; first 1-phase gap since Phase 57-58 sequential
  attributes-then-bioRxiv arms — actually Phase 58 was the
  bioRxiv arm; pre-Phase-58 the gap pattern was Phase 50
  pubmed → Phase 54 orcid (4-phase gap) → Phase 58 bioRxiv
  (4-phase gap) → Phase 63 wikilinks-cross-entity (5-phase
  gap) → Phase 64 tables-per-surface (1-phase gap; shortest
  gap since Phase 49-50 doi-then-pubmed sequential addition
  pattern).
- **D-clauses with ALL items closed (within enumerated
  subset)**: **0** at Phase 63 close. Phase 64 anticipates
  **1** (D-Q at 4-of-4 items within Phase-39-tables-cap
  subset 2+3+4+6) — **FIRST D-clause with ALL enumerated
  items closed** in project history.
- **OPEN_QUESTIONS state**: 28 resolved + 4 lean + 34 open =
  **66 total** UNCHANGED since Phase 36.
- **Phase-37+ candidate count**: **8** UNCHANGED.
- **APPEND-deferral closure cadence pattern**: sustained 22
  phases (Phase 42-63) — longest in project history. Phase 64
  continues with **Phase-39 APPEND-D-Q item 6 (Surface-specific
  table schemas)** at **25-phase carryover**.
- **Absolute-record carryover trajectory**: Phase 61 = 22-phase
  (new); Phase 62 = 24-phase (extend by 2); Phase 63 = 25-phase
  (extend by 1); Phase 64 = 25-phase (TIE; first absolute-
  record-TIE in project history). **Phase 64 BREAKS the
  3-consecutive-phase absolute-record-EXTENSION streak** but
  **BEGINS a 4-consecutive-phase set-or-tie streak** (61 set +
  62 extend + 63 extend + 64 tie).
- **Working tree at session start**: clean (Phase 63 close +
  hygiene refresh committed at `e36d597`).

### D-1. First-thread recommendation: **surface-specific table schemas**

Closes:

1. **ADR-0018 APPEND-D-Q item 6** (Surface-specific table
   schemas — Phase 39 enumerated as a deferral at first-ship).
   Phase 64 closes at **25-phase carryover** (Phase 39 → Phase
   64). **TIES Phase-63 absolute APPEND-deferral closure
   record** (25-phase) without extending — first absolute-
   record-TIE in project history.
2. **Phase-63-gate Phase-64-candidate rank 1** ("Surface-
   specific table schemas (APPEND-D-Q item 6 — last remaining
   D-Q deferral) — closure would make D-Q the first D-clause
   with ALL enumerated items closed in project history").

**Why surface-specific table schemas now** (D-Q finishing-move +
fourth principal axis introduction + tables consumer reaches
depth-4):

- **D-Q becomes first D-clause with ALL enumerated items
  closed** within the Phase-39-tables-cap subset (items 2 + 3
  + 4 + 6 — the four items deferred at Phase-39 first-ship —
  ALL closed). **First D-clause with ALL items closed** in
  project history. Prior multi-item D-clauses reached 4-of-6
  at Phase 63 (D-L). D-Q closure trajectory:
  - Item 1 (multi-value `MARKDOWN_EXTENSIONS=wikilinks,tables`
    composition) → closed Phase 40 (1-phase carryover; pre-
    cadence).
  - Item 2 (cross-surface table expansion) → closed Phase 43
    (4-phase carryover; first cadence closure).
  - Item 3 (table-specific attributes `colspan`/`rowspan`/
    `scope`) → closed Phase 57 (18-phase carryover; first
    NEW LONGEST ABSOLUTE RECORD).
  - Item 4 (`<caption>` element) → closed Phase 61 (22-phase
    carryover; new NEW LONGEST ABSOLUTE RECORD).
  - Item 5 (`remarkPlugins` slot consumer) → closed Phase 41
    (2-phase carryover via arxiv first-ship; pre-cadence
    closure).
  - Item 6 (surface-specific table schemas) → Phase 64 = **25-
    phase carryover** (TIES Phase-63 absolute record).
- **First state where the framework has FOUR principal axes of
  zero-rework extension** in project history. Phase 62
  introduced the 3rd axis (plugin-option); Phase 64 introduces
  the 4th (schema-options). Axes catalog post-Phase-64:
  - Registry-state axis (Phase 38+; 7 realizations): enabled-
    surface-set value-change without code change.
  - Plugin-body axis (Phase 46+; 7 realizations): regex /
    plugin-body extension without registry change.
  - Plugin-option axis (Phase 62+; 2 realizations): plugin-
    signature options flow through registry to plugin
    invocation.
  - Schema-options axis (Phase 64+; 1 realization): per-
    surface schema differentiation map flows through registry
    to per-surface schemaOverrides emission.
- **Tables consumer gains 4th evolution post-first-ship** (Phase
  43 cross-surface + Phase 57 attributes + Phase 61 caption +
  Phase 64 per-surface). **First state where TWO consumers
  have 4+ evolutions** in project history (wikilinks at
  Phase 63 was first; tables joins at Phase 64). Consumer-
  evolution-depth distribution post-Phase-64:
  - wikilinks: 4 evolutions (Phase 42 + 46 + 62 + 63)
  - tables: 4 evolutions (Phase 43 + 57 + 61 + 64) — NEW
  - arxiv: 3 evolutions (Phase 44 + 47 + 53)
  - doi: 2 evolutions (Phase 48 + 49)
  - pubmed: 2 evolutions (Phase 51 + 52)
  - orcid: 2 evolutions (Phase 55 + 56)
  - biorxiv: 2 evolutions (Phase 59 + 60)
- **First schema-options-ready-before-curator-demand
  realization** — generalizes Phase-62 plugin-option-ready-
  before-consumer-demand to the schema slot. The
  `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` constant + the
  new `tables-per-surface` arm together ship the differentiation
  capability AHEAD of explicit curator demand for a bio-only
  basic-table policy; the demand-anticipation discipline is now
  observed in BOTH plugin-option (Phase 62) and schema-options
  (Phase 64) axes.
- **TIES Phase-63 absolute APPEND-deferral closure record** at
  25-phase carryover (Phase 39 → Phase 64) — **first absolute-
  record-TIE in project history**. The 3-consecutive-phase
  absolute-record-EXTENSION streak (Phase 61 22 → Phase 62 24
  → Phase 63 25) ENDS at Phase 64, but a 4-consecutive-phase
  set-or-tie streak BEGINS (Phase 61 new + 62 extend + 63
  extend + 64 tie). This is the first phase shape where an
  APPEND-deferral closes AT the absolute record without
  extending it.
- **23-phase APPEND-deferral closure cadence sustained**
  (extends Phase-63 22-phase record to 23 — new longest
  sustained cadence in project history).
- **Closure of D-Q completes the framework's first
  fully-resolved D-clause**: D-Q (Phase 39 tables-consumer
  deferrals) becomes the first D-clause where every item
  enumerated at first-ship has reached closure. Prior multi-
  item D-clauses (D-Y arxiv, D-AC doi, D-AH pubmed, D-AL
  orcid, D-AP bioRxiv, D-L wikilinks) all carry residual
  unresolved items. D-Q closure establishes the "fully-
  resolved D-clause" as a recognized closure shape.
- **Demand-anticipatory composition matrix evolution**: the
  `tables-per-surface` arm demonstrates per-surface
  differentiation as an opt-in capability. The existing
  `tables` arm preserves byte-identity (all 4 enabled surfaces
  get `GFM_TABLE_SCHEMA_OVERRIDES` uniformly per Phase-43
  default). Curators who want per-surface differentiation opt
  INTO the new arm; the framework supports both modes through
  the same `TablesExtensionRegistry` class.
- **Concrete differentiation example**: per APPEND-D-Q item 6
  ("reviewNotes allows colspan; bio does not"), the Phase-64
  `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` constant
  encodes a defensible per-surface differentiation:
  - **bio** (short user-facing profile content): restricted
    schema — Phase-17 base + basic GFM table tags + `align`
    attribute only; NO `colspan` / `rowspan` / `scope` / no
    `<caption>` (basic tabular layout but no rich data
    constructs).
  - **reviewNotes + rationale + actionRationale** (curator
    editorial content): full Phase-61 schema with `colspan`
    / `rowspan` / `scope` / `<caption>` (rich data tables
    for multi-criterion scoring, COI matrices, structured
    editorial reasoning).

**Path A** (no new ADR; APPEND ADR-0018 D-G) chosen per Phase-
37 through 63 D-9 precedent. ADR-0018 D-G gains its **31st
APPEND** — extends the record from 30 → 31. **Twenty-second
two-letter APPEND letter D-AV** (after Phase-43 D-AA …
Phase-63 D-AU).

### D-2. DB-migration trigger re-evaluation

Phase 64 anticipates **0 new migrations**. Pure constructor-
signature evolution + per-surface schema-override map +
factory dispatch arm addition + bio-restricted schema variant
constant.

### D-3. Surface-specific table schemas shape anticipated (Unit 64.1 + 64.2)

D-A. **`TablesExtensionRegistry` constructor evolution** (Unit
64.1):

```ts
// Before (Phase 39 ship through Phase-63 close):
export class TablesExtensionRegistry implements MarkdownExtensionRegistry {
  private readonly enabledSurfaces: ReadonlySet<MarkdownSurface>;

  constructor(enabledSurfaces: ReadonlySet<MarkdownSurface>) {
    this.enabledSurfaces = enabledSurfaces;
  }

  getExtensions(surface: MarkdownSurface): MarkdownExtensionSet {
    if (this.enabledSurfaces.has(surface)) {
      return { schemaOverrides: GFM_TABLE_SCHEMA_OVERRIDES };
    }
    return {};
  }
}

// After (Phase 64 ship):
export interface TablesExtensionOptions {
  /**
   * Optional per-surface schema map. When a surface is in
   * `enabledSurfaces` AND present in this map, the map's
   * `Partial<Schema>` is emitted instead of the default
   * `GFM_TABLE_SCHEMA_OVERRIDES`. When a surface is in
   * `enabledSurfaces` but NOT in this map, falls back to
   * `GFM_TABLE_SCHEMA_OVERRIDES` (Phase 39-61 invariant).
   * When undefined (Phase 39-63 default), all enabled
   * surfaces receive `GFM_TABLE_SCHEMA_OVERRIDES` uniformly.
   */
  surfaceSchemaOverrides?: ReadonlyMap<MarkdownSurface, Partial<Schema>>;
}

export class TablesExtensionRegistry implements MarkdownExtensionRegistry {
  private readonly enabledSurfaces: ReadonlySet<MarkdownSurface>;
  private readonly surfaceSchemaOverrides: TablesExtensionOptions["surfaceSchemaOverrides"];

  constructor(
    enabledSurfaces: ReadonlySet<MarkdownSurface>,
    options: TablesExtensionOptions = {},
  ) {
    this.enabledSurfaces = enabledSurfaces;
    this.surfaceSchemaOverrides = options.surfaceSchemaOverrides;
  }

  getExtensions(surface: MarkdownSurface): MarkdownExtensionSet {
    if (!this.enabledSurfaces.has(surface)) return {};
    const perSurface = this.surfaceSchemaOverrides?.get(surface);
    if (perSurface !== undefined) {
      return { schemaOverrides: perSurface };
    }
    return { schemaOverrides: GFM_TABLE_SCHEMA_OVERRIDES };
  }
}
```

**Backwards-compat preserved**: every Phase-39 / Phase-43 /
Phase-57 / Phase-61 call site that constructs
`new TablesExtensionRegistry(enabledSurfaces)` continues to
work byte-identically (options defaults to `{}`,
`surfaceSchemaOverrides` defaults to `undefined`, getExtensions
returns `GFM_TABLE_SCHEMA_OVERRIDES` for enabled surfaces).
The `MARKDOWN_EXTENSIONS=tables` arm behavior is byte-
identical to Phase 63.

D-B. **`GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` NEW** (Unit
64.1) — bio-restricted variant per APPEND-D-Q item 6 example:

```ts
/**
 * Phase-64 bio-restricted schema variant per APPEND-D-Q item
 * 6 example ("bio does not allow colspan"). Subset of
 * `GFM_TABLE_SCHEMA_OVERRIDES`: omits `colspan` / `rowspan`
 * / `scope` on th + td (Phase-57 attribute additions) AND
 * omits `caption` from tagNames (Phase-61 tag addition).
 * Retains Phase-39 baseline (basic GFM table tags +
 * `align` attribute). Suitable for short user-facing
 * profile content where rich data tables are
 * inappropriate.
 *
 * Phase-17 base allow-list verbatim per APPEND-D-C
 * override-replace contract.
 */
export const GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED: Partial<Schema> = {
  tagNames: [
    // Phase-17 base allow-list (verbatim copy per APPEND-D-C)
    "p", "strong", "em", "code", "pre", "a", "ul", "ol", "li",
    "h3", "h4", "h5", "h6", "blockquote", "hr", "del", "br", "input",
    // Phase-39 basic GFM table tags (NO caption per bio-restricted policy)
    "table", "thead", "tbody", "tr", "th", "td",
  ],
  attributes: {
    a: ["href"],
    input: [["type", "checkbox"], "checked", "disabled"],
    // Phase-39 align ONLY (NO Phase-57 colSpan/rowSpan/scope per bio-restricted policy)
    th: [["align", "left", "center", "right"]],
    td: [["align", "left", "center", "right"]],
    "*": [],
  },
};
```

D-C. **`PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES` NEW**
(Unit 64.1) — concrete per-surface map for the new arm:

```ts
/**
 * Phase-64 default per-surface schema-override map for the
 * `MARKDOWN_EXTENSIONS=tables-per-surface` arm. Demonstrates
 * the schema-options axis with a concrete differentiation
 * per APPEND-D-Q item 6 example:
 *
 *   - bio: restricted variant (no spans/scope/caption)
 *   - reviewNotes + rationale + actionRationale: full
 *     Phase-61 schema (everything)
 *
 * Surface enumeration follows `MarkdownSurface` type-union
 * order from `./types.ts`.
 */
export const PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES: ReadonlyMap<
  MarkdownSurface,
  Partial<Schema>
> = new Map([
  ["bio", GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED],
  ["reviewNotes", GFM_TABLE_SCHEMA_OVERRIDES],
  ["rationale", GFM_TABLE_SCHEMA_OVERRIDES],
  ["actionRationale", GFM_TABLE_SCHEMA_OVERRIDES],
]);
```

D-D. **New factory arm `tables-per-surface`** (Unit 64.2):

```ts
// In lib/markdown/extensions/index.ts:
case "tables":
  return new TablesExtensionRegistry(PHASE_39_DEFAULT_ENABLED_SURFACES);
case "tables-per-surface":
  // Phase 64 Unit 64.2 — first realization of schema-options
  // axis (fourth principal axis of zero-rework framework
  // extension). Wires PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES
  // map per ADR-0018 D-G APPEND-D-AV.
  return new TablesExtensionRegistry(
    PHASE_39_DEFAULT_ENABLED_SURFACES,
    { surfaceSchemaOverrides: PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES },
  );
```

**`MARKDOWN_EXTENSIONS` single-value arms**: 9 → **10**
(+`tables-per-surface`). Second new arm in 2 consecutive
phases — first 2-consecutive-phase new-arm-addition streak in
project history.

D-E. **Composition matrix evolution at Phase 64 default** —
UNCHANGED for the `tables` arm (uniform GFM_TABLE_SCHEMA_OVERRIDES
on all 4 enabled surfaces; Phase 39-63 invariant preserved
byte-identical); NEW row for the `tables-per-surface` arm:

| Surface | tables arm schemaOverrides | tables-per-surface arm schemaOverrides | rehypePlugins | remarkPlugins |
|---|---|---|---|---|
| `bio` | GFM_TABLE_SCHEMA_OVERRIDES (full Phase-61) | **GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED** (no spans/scope/caption) | wikilinks default-call (per arm choice) | [arxiv, doi, pubmed, orcid, biorxiv] |
| `reviewNotes` | GFM_TABLE_SCHEMA_OVERRIDES | GFM_TABLE_SCHEMA_OVERRIDES (full Phase-61) | wikilinks default-call | [arxiv, doi, pubmed, orcid, biorxiv] |
| `rationale` | GFM_TABLE_SCHEMA_OVERRIDES | GFM_TABLE_SCHEMA_OVERRIDES | wikilinks default-call | [arxiv, doi, pubmed, orcid, biorxiv] |
| `actionRationale` | GFM_TABLE_SCHEMA_OVERRIDES | GFM_TABLE_SCHEMA_OVERRIDES | wikilinks default-call | [arxiv, doi, pubmed, orcid, biorxiv] |

The two table arms are mutually exclusive single-value choices;
curators pick one. `tables-per-surface` is the schema-options-
axis-aware arm that demonstrates per-surface differentiation.

D-F. **Composite registry composition note**: the existing
`CompositeExtensionRegistry` single-source schemaOverrides rule
(Phase 40 APPEND-D-R; lib/markdown/extensions/composite.ts:71-77)
holds. Per-surface schemaOverrides from `TablesExtensionRegistry`
compose conflict-free with `WikilinkExtensionRegistry`
(rehypePlugins) and the five `remarkPlugins` consumers (arxiv +
doi + pubmed + orcid + biorxiv). The composite's
schemaOverrides slot allows AT MOST ONE component per surface;
since tables is still the only schemaOverrides-providing
consumer at Phase 64, no new conflict surface introduced.
**Phase 64 does NOT trigger the composite's single-source
rule**.

D-G. **Tests to add** (Unit 64.1 — ~8 NEW tests in
`tables.test.ts`):

1. **Constructor accepts optional second arg
   `surfaceSchemaOverrides`** (signature change verified via
   `new TablesExtensionRegistry(enabled, { surfaceSchemaOverrides: map })`
   compiles + runs).
2. **`surfaceSchemaOverrides === undefined` preserves Phase
   39-63 byte-identical behavior** (regression guard;
   `getExtensions` returns `GFM_TABLE_SCHEMA_OVERRIDES` on
   enabled surfaces).
3. **`surfaceSchemaOverrides` with bio-restricted variant
   returns the restricted schema on bio** (Phase 64 NEW
   behavior).
4. **`surfaceSchemaOverrides` with bio-restricted variant
   falls back to `GFM_TABLE_SCHEMA_OVERRIDES` on surfaces NOT
   in the map** (graceful fallback per D-A spec).
5. **`surfaceSchemaOverrides` map respects `enabledSurfaces`
   gate** (surface NOT in enabledSurfaces returns `{}` even
   if present in the map).
6. **`GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` content**:
   includes basic GFM table tags (table/thead/tbody/tr/th/td)
   + Phase-17 base + NO caption.
7. **`GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` attribute
   content**: includes Phase-39 align ONLY on th + td (no
   colspan/rowspan/scope).
8. **`PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES` map
   content**: bio → restricted; other 3 surfaces → full
   Phase-61 schema.
9. **Schema-isolation: bio-restricted schema strips
   `<caption>`** (Phase-61 tag not in bio-restricted
   tagNames; sanitize strips it on bio surface).
10. **Schema-isolation: bio-restricted schema strips
    `colspan`/`rowspan`/`scope`** (Phase-57 attributes not in
    bio-restricted; sanitize strips them on bio surface).
11. **Schema-isolation: reviewNotes under per-surface map
    still survives `<caption>` + spans + scope** (full
    schema preserved on non-bio surfaces).

Unit 64.2 — ~5 NEW integration-level tests in
`tables.test.ts` + `index.test.ts`:

1. **TablesExtensionRegistry with map emits per-surface
   differentiated schemas across all 4 surfaces** (single
   composite assertion over all 4 surfaces).
2. **Factory `tables-per-surface` arm constructs per-surface-
   aware registry** (env-var dispatch test).
3. **End-to-end `MARKDOWN_EXTENSIONS=tables-per-surface`
   returns bio-restricted on bio** (factory + getExtensions
   composition).
4. **End-to-end `MARKDOWN_EXTENSIONS=tables` (default) still
   returns uniform GFM_TABLE_SCHEMA_OVERRIDES on bio** (Phase
   39-63 invariant preserved end-to-end through factory
   pathway).
5. **Factory error message lists `tables-per-surface` in
   recognized values** (updated unknown-arm error message).
6. **Composite registry: `wikilinks,tables-per-surface`
   composes conflict-free** (single-source schemaOverrides
   rule unaffected; tables is still the only schemaOverrides-
   providing consumer).

D-H. **APPEND letter sequence: D-AV (twenty-second two-letter
slot)**. Phase 43 D-AA … Phase 63 D-AU; Phase 64 D-AV.

D-I. **Phase 65+ deferrals** (Phase-64 per-surface scope cap):

- **`<a class="wikilink">` styling** (APPEND-D-L item 4
  carries) — Phase 65+; still blocked pending framework
  decision on multi-source schemaOverrides.
- **404 handling for unresolved wikilinks** (APPEND-D-L item
  5 carries) — Phase 65+; would close D-L if pursued after
  item 4.
- **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
  prefix** — Phase 65+.
- **Legacy numeric-only bioRxiv IDs** (pre-2019 format) —
  Phase 65+.
- **dx.doi.org legacy host parsing** (APPEND-D-AC carries) —
  Phase 65+.
- **Stricter trailing-lookahead for trailing-period DOIs**
  (APPEND-D-AC carries) — Phase 65+.
- **Paper-card hover-preview** (APPEND-D-Y item 6 carries) —
  Phase 65+.
- **Auto-trim of alias display whitespace** — Phase 65+.
- **Empty-alias fallback unification** across consumers —
  Phase 65+.
- **A future plugin that EMITS `<caption>` or
  `colSpan`/`rowSpan`/`scope`** to realize the Phase-57 /
  Phase-61 schema-ready-before-plugin states — Phase 65+.
- **Curator-facing per-surface schema configuration via DB or
  UI** — Phase 65+; would require new env-var arm
  consumption from DB-backed state.
- **Locale-prefixed cross-entity routing** — Phase 65+.
- **OSF preprint consumer** — eighth concrete consumer —
  Phase 65+.
- **6th-or-later `remarkPlugins` consumer beyond arxiv + doi
  + pubmed + orcid + biorxiv** — Phase 65+.
- **2nd `rehypePlugins` consumer beyond wikilinks** — Phase
  65+.
- **2nd `schemaOverrides` consumer beyond tables** — Phase
  65+ (requires framework refactor per the composite-registry
  single-source rule).
- **3rd regex evolution on `remarkLinkArxivIds`** — Phase
  65+.
- **Cross-entity entity-type expansion beyond paper / author
  / institution** (e.g., problem-tags or rating-actions) —
  Phase 65+.
- **Validate captured entity-type against content/papers /
  content/authors / content/institutions at build time** —
  Phase 65+.
- **Per-surface schema-options consumer beyond bio-restricted**
  (e.g., a `reviewNotes-extended` schema variant with footnote
  support) — Phase 65+; would extend Phase-64 schema-options
  axis to additional surface variants.
- **ADR-0025 concrete content-moderation provider** — Phase
  65+; not autonomous-tractable.

### Provisional unit breakdown (64.0 … 64.4)

| Unit | Title | Type |
|---|---|---|
| 64.0 | **Phase 64 prep** (this unit) | docs |
| 64.1 | `TablesExtensionRegistry` constructor evolution (optional 2nd ctor arg `options: { surfaceSchemaOverrides?: ReadonlyMap<MarkdownSurface, Partial<Schema>> }` + fallback to `GFM_TABLE_SCHEMA_OVERRIDES` for surfaces not in map) + new exported `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` constant + new exported `PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES` map + 10+ NEW unit tests in `tables.test.ts` + ADR-0018 D-G APPEND-D-AV | code+APPEND |
| 64.2 | New factory dispatch arm `tables-per-surface` (10th `MARKDOWN_EXTENSIONS` single-value arm; first 2-consecutive-phase new-arm-addition streak) + factory error-message update + 5+ NEW integration-level tests (`tables.test.ts` + `index.test.ts`) | code |
| 64.3 | Phase-64 hygiene + ADR-0018 APPEND-D-Q item 6 closure documentation (25-phase carryover — TIES Phase-63 absolute APPEND-deferral closure record; first absolute-record-TIE in project history; first D-clause with ALL enumerated items closed; first state where TWO consumers have 4+ evolutions; first realization of FOURTH principal axis of zero-rework framework extension; first state where framework has FOUR principal axes; first schema-options-ready-before-curator-demand realization; 23-phase APPEND-deferral closure cadence; tables consumer 4th evolution; second new arm in 2 consecutive phases; first 2-consecutive-phase new-arm-addition streak; 4-consecutive-phase set-or-tie streak begins) | docs |
| 64.4 | **Phase 64 acceptance gate** | gate |

Total: **5 units**. Mirrors Phase 41-63 5-unit framework shape
verbatim.

### Phase-64-blocking decisions deferred to per-unit implementation

- **D-8** (Unit 64.1): Constructor signature shape — second
  arg as `options: TablesExtensionOptions` object (extensible
  for future Phase-65+ options). Lean = **options object
  mirroring Phase-62 plugin-option pattern**.
- **D-9** (Unit 64.1): Per-surface fallback semantics —
  surface in `enabledSurfaces` but NOT in map → falls back to
  `GFM_TABLE_SCHEMA_OVERRIDES` (NOT empty). Lean = **graceful
  fallback to default schema**.
- **D-10** (Unit 64.1): Bio-restricted schema CONTENT — Phase-17
  base + basic GFM table tags + align attribute only; NO
  colspan/rowspan/scope/caption. Lean = **subset matching
  APPEND-D-Q item 6 example verbatim**.
- **D-11** (Unit 64.1): Constants placement — colocate
  `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` +
  `PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES` with
  `GFM_TABLE_SCHEMA_OVERRIDES` in `tables.ts`. Lean = **colocate**.
- **D-12** (Unit 64.1): Map type — `ReadonlyMap<MarkdownSurface,
  Partial<Schema>>` mirroring the `ReadonlySet<MarkdownSurface>`
  pattern for `enabledSurfaces`. Lean = **ReadonlyMap for type-
  safety parity**.
- **D-13** (Unit 64.2): New env-var arm naming —
  `tables-per-surface` (hyphenated; matches the multi-word
  pattern of Phase-63 `wikilinks-cross-entity`). Lean =
  **`tables-per-surface`**.
- **D-14** (Unit 64.1): APPEND-D-AV naming — Excel convention
  continues (D-AU → D-AV → D-AW … D-AZ → D-BA). Lean =
  **continue Excel**.
- **D-15** (Unit 64.1): Per-surface map semantics — overlay
  pattern (map entries OVERRIDE per-surface, missing entries
  fall back to default schema). Alternative was strict-mode
  (map entries REPLACE the enabled-surface gate entirely;
  missing entries → disabled). Lean = **overlay pattern**
  (preserves Phase-39 enabled-surface-set as the primary gate).
- **D-16** (Unit 64.1): Axis naming — call the new axis
  "schema-options axis" (parallel to plugin-option axis name).
  Alternative: "schema-differentiation axis" or "per-surface-
  options axis" or "consumer-options axis" (generalized).
  Lean = **schema-options axis** (preserves Phase-62 plugin-
  option axis name verbatim without retconning; introduces 4th
  axis as a clean sibling).

### Smoke gates (this unit)

Docs-only — no source files modified.

- File added: `docs/thinking/64.0-phase-64-prep.md`.
- CHANGELOG: open `### Phase 64` section + Unit 64.0 entry.
- `pnpm audit-content`: 0 errors / 6 warnings UNCHANGED.
- `pnpm test`: 1486/75 UNCHANGED.
- First Load JS / middleware: UNCHANGED.

### Parallel-curator awareness

This unit docs-only; no collision. Phase 64 collision risk:

- 64.1: MODIFIED `lib/markdown/extensions/tables.ts`
  (constructor evolution + 2 NEW exported constants) +
  MODIFIED `lib/markdown/extensions/tables.test.ts` (~10
  NEW tests) + MODIFIED
  `docs/adr/0018-markdown-sanitization.md` (APPEND-D-AV).
- 64.2: MODIFIED `lib/markdown/extensions/index.ts` (factory
  arm addition + error-message update for new arm) +
  MODIFIED `lib/markdown/extensions/tables.test.ts` (~3 NEW
  integration tests) + MODIFIED
  `lib/markdown/extensions/index.test.ts` (~3 NEW factory-
  dispatch tests).
- 64.3 / 64.4: docs hygiene + gate; parallel-safe.

### Alternative threads (overridable into Unit 64.1)

| Thread | Why tempting | Why deferred | Units |
|---|---|---|---|
| **`<a class="wikilink">` styling** (APPEND-D-L item 4) | Phase-63 gate rank 2; would advance D-L from 4-of-6 → 5-of-6 | Requires framework refactor (multi-source schemaOverrides; violates APPEND-D-C); Phase 65+ | 1-2 |
| **404 handling for unresolved wikilinks** (APPEND-D-L item 5) | Phase-63 gate rank 3 | Build-time validation step; deferred until after rank 2 framework decision | 2-3 |
| **Empty-alias fallback unification** (Phase-48 deferral) | Smallest-scope; Phase-63 gate rank 4 | Phase 65+ | 1 |
| **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** | Phase-63 gate rank 5 | Ambiguity-sensitive; Phase 65+ | 1-2 |
| **Legacy numeric-only bioRxiv IDs** (pre-2019 format) | Phase-63 gate rank 6; would be 8th plugin-regex-extension | Phase 65+ | 1-2 |
| **OSF preprint consumer** | eighth concrete consumer; Phase-63 gate rank 7 | DOI overlap concern; Phase 65+ | 2-3 |
| **Locale-prefixed cross-entity wikilink paths** | Phase-63 gate rank 8 | Sub-realization of Phase 63 buildHref affordance; defer to demand signal | 1-2 |
| **Cross-entity entity-type expansion** beyond paper/author/institution | Phase-63 gate rank 9 | Small-scope follow-on; Phase 65+ | 1 |
| **ADR-0025 concrete moderation provider** | Strongest patience signal; Phase-63 gate rank 10 | Not autonomous-tractable; Phase 65+ | 3-5 |
| **Q78 / Q79 / v2 methodology / safeAuth / test backfill / account-delete / pre-commit typecheck** | Various follow-ons | Each deferred | 1-5 each |

### Phase-64 anti-scope (boundary statement preview for Unit 64.4)

NOT `<a class="wikilink">` styling (Phase 65+; deferred pending
framework decision on multi-source schemaOverrides);
NOT 404 handling for unresolved wikilinks (Phase 65+);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 65+);
NOT legacy numeric-only bioRxiv IDs (Phase 65+);
NOT dx.doi.org legacy host (Phase 65+);
NOT stricter trailing-lookahead (Phase 65+);
NOT paper-card hover-preview (Phase 65+);
NOT auto-trim of alias display whitespace (Phase 65+);
NOT empty-alias fallback unification (Phase 65+);
NOT a future plugin that EMITS `<caption>` or
`colSpan`/`rowSpan`/`scope` (Phase 65+);
NOT `@mention` resolution (Phase 65+);
NOT OSF preprint consumer (Phase 65+);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi
+ pubmed + orcid + biorxiv (Phase 65+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 65+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 65+;
requires framework refactor);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 65+);
NOT cross-entity entity-type expansion beyond paper / author /
institution (Phase 65+);
NOT build-time validation of captured entity-type against
content/papers / content/authors / content/institutions (Phase
65+);
NOT locale-prefixed cross-entity routing (Phase 65+);
NOT relative-path wikilink variants (Phase 65+);
NOT curator-facing per-surface schema configuration via DB or
UI (Phase 65+);
NOT per-surface schema-options consumer beyond bio-restricted
(Phase 65+);
NOT 2nd schema-options realization on a different consumer
than tables (Phase 65+; tables is the only schemaOverrides
consumer Phase 64);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-63 deferrals carry forward.

**This phase is the first realization of a FOURTH principal
axis of zero-rework framework extension** + **first state
where the framework has FOUR principal axes of zero-rework
extension** + **first D-clause with ALL enumerated items
closed in project history** + **first absolute APPEND-
deferral-closure record TIE in project history** + **first
state where TWO consumers have 4+ evolutions** + **second
new `MARKDOWN_EXTENSIONS` single-value arm in 2 consecutive
phases — first 2-consecutive-phase new-arm-addition streak**.

### Anticipated architectural firsts in Phase 64 (12)

1. **First realization of a FOURTH principal axis of zero-
   rework framework extension** in project history. Phase 62
   introduced the 3rd axis (plugin-option); Phase 64
   introduces the 4th axis (schema-options). Schema-options
   axis: per-surface schema differentiation map flows through
   `TablesExtensionRegistry` constructor to per-surface
   schemaOverrides emission. Mechanistically parallel to
   Phase-62 plugin-option axis (constructor evolution + flow-
   through to consumer-specific configuration) but targets a
   distinct framework slot (schemaOverrides vs plugin
   invocation). Schema-options axis joins:
   - Registry-state axis (Phase 38+; 7 realizations)
   - Plugin-body axis (Phase 46+; 7 realizations)
   - Plugin-option axis (Phase 62+; 2 realizations)
   - **Schema-options axis (Phase 64+; 1 realization)**
2. **First state where the framework has FOUR principal axes
   of zero-rework extension** in project history. Phase 62
   established 3 axes (first 3-axis state); Phase 64
   establishes 4 axes. Each axis enables independent zero-
   rework extension paths; the framework now supports
   evolution along 4 orthogonal vectors without registry-
   reshape or core-pipeline change.
3. **First D-clause with ALL enumerated items closed** in
   project history (D-Q at 4-of-4 items within the Phase-39-
   tables-cap subset: items 2 + 3 + 4 + 6). Prior multi-item
   D-clauses reached 4-of-6 at Phase 63 (D-L). D-Q ALL-items-
   closed establishes the "fully-resolved D-clause" as a
   recognized closure shape. D-clauses with deferred items
   post-Phase-64: D-Y (arxiv, 1 remaining); D-AC (doi, 3
   remaining); D-AH (pubmed, 1 remaining); D-AL (orcid, 0
   remaining — Phase 56 closed all; need to re-check);
   D-AP (bioRxiv, 0 remaining — Phase 60 closed all; need to
   re-check); D-L (wikilinks, 2 remaining — items 4 + 5).
4. **First absolute APPEND-deferral closure record TIE** in
   project history. Phase 64 closes D-Q item 6 at 25-phase
   carryover (Phase 39 → Phase 64) — TIES Phase-63 record
   without extending. The 3-consecutive-phase absolute-record-
   EXTENSION streak (Phase 61 22 → Phase 62 24 → Phase 63
   25) ends at Phase 64, but a 4-consecutive-phase set-or-tie
   streak BEGINS (Phase 61 new + 62 extend + 63 extend + 64
   tie). First absolute-record-TIE phase shape recognized.
5. **First state where TWO consumers have 4+ evolutions** in
   project history. Wikilinks reached 4 evolutions at Phase
   63 (4 + 6 + 62 + 63); tables joins at Phase 64 (43 + 57 +
   61 + 64). Other consumers remain at ≤3 evolutions (arxiv
   at 3; doi/pubmed/orcid/biorxiv at 2 each).
6. **First schema-options-ready-before-curator-demand
   realization** — generalizes Phase-62 plugin-option-ready-
   before-consumer-demand to the schema slot. The
   demand-anticipation discipline is now observed in BOTH
   plugin-option (Phase 62) and schema-options (Phase 64)
   axes. The discipline progression:
   - Phase 57 = schema-ready-before-plugin (attributes layer)
   - Phase 61 = schema-ready-before-plugin (tags layer)
   - Phase 62 = plugin-option-ready-before-consumer-demand
     (one layer up from schema-ready-before-plugin)
   - **Phase 64 = schema-options-ready-before-curator-demand**
     (parallel to plugin-option-ready but in the schema slot)
7. **Twenty-third prep-/APPEND-doc-level deferral closed by a
   later phase**: Phase 42 → 38 D-L item 1; …; Phase 63 → 38
   D-L item 3; **Phase 64 → 39 D-Q item 6 Surface-specific
   table schemas**. **APPEND-deferral closure cadence
   sustained 23 phases** — new longest sustained cadence in
   project history (extends Phase-63 record 22 → 23). First
   23-phase APPEND-deferral closure run.
8. **Thirty-first APPEND on ADR-0018 D-G** — extends the
   **first-ADR-D-clause-with-most-APPENDs record** from 30 →
   31. **Twenty-second two-letter APPEND letter D-AV** (Excel-
   spreadsheet column convention sustained). First 31-APPEND
   D-clause state.
9. **Fifty-fifth NON-§13 phase** (extends Phase-63 half-
   century milestone to 55). The §13 ledger remains CLOSED
   across the entire 55-phase run (Phase 10-64). **First 55-
   phase ledger-closure streak**.
10. **Second new `MARKDOWN_EXTENSIONS` single-value arm in
    2 consecutive phases** (`tables-per-surface`). 9 → 10
    single-value arms. **First 2-consecutive-phase new-arm-
    addition streak** in project history (Phase 63
    `wikilinks-cross-entity` + Phase 64 `tables-per-surface`).
    Prior pattern: Phase 50 pubmed → Phase 54 orcid (4-phase
    gap) → Phase 58 bioRxiv (4-phase gap) → Phase 63
    `wikilinks-cross-entity` (5-phase gap) → Phase 64
    `tables-per-surface` (1-phase gap). First 1-phase gap
    between single-value arms since Phase 49-50 doi-then-
    pubmed sequential addition.
11. **First "framework + 7 consumers + 7 expansions + 7
    plugin-regex-extensions + 2 schema-extensions + 2
    plugin-option-extensions + 1 schema-options-extension +
    10 `MARKDOWN_EXTENSIONS` single-value arms + 4-D-clauses-
    both-items-closed + D-clause-with-3-items-closed × 2 +
    D-clause-with-4-items-closed × 1 + D-clause-with-ALL-
    items-closed × 1 + consumer-with-4+-evolutions × 2 +
    first-forward-compat-affordance-arc-completed × 1 +
    first-registry-level-plugin-option-realization × 1 +
    three-consecutive-absolute-record-extension × 1 +
    four-consecutive-absolute-record-set-or-tie × 1 +
    plugin-option-ready-before-consumer-demand × 1 +
    schema-options-ready-before-curator-demand × 1 +
    refactor-only-phase × 1 + first-forward-compat-
    affordance-consumer × 1 + first-fourth-principal-axis-
    realization × 1" 28-phase cluster** in project history
    (Phase 37-64). 28-phase architectural arc. Plus **34th
    consecutive phase without new B category** (Phase 31-
    64; extends Phase-63 record 33 → 34). Plus **fifty-
    fifth NON-§13 phase** (extends Phase-63 milestone to
    55). Plus **178th consecutive 103 kB First Load JS
    unit** (extends from 173 at Phase-63 close). Plus
    **29th consecutive no-new-ADR phase** (Phase 36-64;
    extends Phase-63 record 28 → 29).
12. **59th "Continue" override invocation** at Unit 64.0 —
    extends the **half-century-of-Continue-overrides
    milestone** (Phase 55 = 50th) to **59 over Phases
    6-64**. **Ninth invocation past the half-century
    threshold** — confirms the §12-cardinal-rule sign-off
    discipline remains durable post-milestone across 10
    successive phases (Phases 55-64). First 10-consecutive-
    phase post-half-century-Continue-override streak.

### Open questions newly surfacing

Anticipated: 0 new opens. ADR-0018 APPEND-D-Q item 6 is the
LOCUS of Phase 64 closure.

## DESIGN — not applicable

This unit is docs-only.

## CODE — not applicable

Same.

## ITERATE — gates

- [x] Phase 63 close baseline documented.
- [x] D-1 first-thread recommendation framed (rank 1 from
      Phase-63 gate; surface-specific table schemas).
- [x] Per-surface schemas design sketched (D-3 A-H).
- [x] Backwards-compatibility contract documented (default-
      fallback preserves Phase 39-61 verbatim; existing
      `tables` arm unchanged; new opt-in `tables-per-surface`
      arm).
- [x] Anti-scope enumerated.
- [x] Anticipated architectural firsts enumerated (12).
- [x] Provisional unit breakdown (5 units).
- [x] Parallel-curator awareness restated.
- [x] Per-unit decisions D-8..D-16 lean-noted.
- [x] No new ADRs (Path A per Phase-37-through-63 precedent).
- [x] No new OPEN_QUESTIONS entries.
- [x] Smoke gates green (docs-only).
- [ ] Unit 64.0 ships at commit `<this unit>`; Unit 64.1
      authors the `TablesExtensionRegistry` constructor
      evolution + `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED`
      + `PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES` +
      10+ NEW unit tests + APPEND-D-AV.
