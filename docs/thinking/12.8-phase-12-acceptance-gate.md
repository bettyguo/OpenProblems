# 12.8 — Phase 12 acceptance gate (Curator review pipeline; Q57 closure; first new ADR since Phase 9)

Phase-12 closing unit. Mirrors Units 1.12 / 2.13 / 3.13 / 4.13 / 5.13 /
6.10 / 7.11 / 8.9 / 9.9 / 10.5 / 11.7. Verifies every deliverable
Phase 12 pulled (the **Curator review pipeline** — Q57 keystone closure
per ADR-0014) is operational locally at HEAD, emits the cross-phase
roll-up, and lists Phase-12 follow-ons that survive into Phase 13+.

## THINK

### Scope clarification

Phase 12 is the **third NON-§13 phase** (Phase 10 was first; Phase 11
second; Phase 12 third). The §13 ledger has been **CLOSED since Unit
9.9**. Phase 12's "Curator review pipeline" closes Q57 — the Phase-11-
surfaced architectural question — and lands the project's **first
new ADR since Phase 9** (ADR-0014), **first ALTER migration**
(`0003_rating_challenge_review`), **first authorization tier beyond
signed-in** (curator allowlist via env var), and **first multi-state
DB state machine** (the 7-transition graph).

This completes the community-feedback loop: Phase 11 shipped the
**submission** half; Phase 12 ships the **review** half. Editorial
governance is now operational at the code layer.

### Phase-12 deliverables — local pass status

| Surface | Phase-12 unit | Status |
|---|---|---|
| Phase 12 prep THINK doc + 9-unit breakdown + procedural DB-trigger re-eval | Unit 12.0 | ✓ `docs/thinking/12.0-phase-12-prep.md`; first-thread = curator review pipeline (Q57 keystone); seventh "Continue" override invocation. |
| ADR-0014 curator review pipeline (state machine + env-var authz + simplified COI + manual YAML emission + ALTER discipline + page-local auth) | Unit 12.1 | ✓ MADR-3.0 short-form ADR; 14 ADRs at HEAD; first new ADR since Phase 9. |
| `0003_rating_challenge_review` ALTER migration (4 nullable columns on `ratingChallenge`: `reviewedAt` + `reviewerId` FK with `ON DELETE SET NULL` + `reviewNotes` + `acceptedActionId`) + schema edit | Unit 12.2 | ✓ first ALTER migration in project; drizzle-kit-generated SQL corrected in-place for FK action clause per ADR-0014 D-E anticipation; snapshot + journal landed atomically. |
| `lib/auth/curator.ts` (`isCurator` env-var allowlist) + `lib/auth/login.ts` (`getLoginById` shared helper) + `lib/rating-challenges/coi.ts` (`getCoIStatus` simplified §8.6) + `lib/rating-challenges/` extensions (`reviewChallenge` + `withdrawChallenge` + state-machine predicates + `getPendingChallenges` + `getChallengeById` + `attachAcceptedAction`) + 43 tests | Unit 12.3 | ✓ all pure-function half tested (43 tests: 11 isCurator + 7 COI + 25 state-machine); DB-helper half exercised at request time. |
| Curator dashboard route at `/[locale]/curator/challenges` (list view; `force-dynamic`; ASC fairness queue) + `/[locale]/curator/challenges/[id]` (detail view; full rationale + COI surface + review form + attach-action-YAML form) + `POST /api/v1/rating-challenges/[id]/review` API + `messages.curator.*` namespace (47 keys per locale) + 10 tests | Unit 12.4 | ✓ second protected page route + sub-route detail; third dynamic API route in `/api/v1/`; 10 review-API tests; First Load JS shared chunk = 103 kB UNCHANGED. |
| Withdraw-own-challenge UI on profile page (`withdrawChallengeAction` server-action; isAllowedWithdrawal gating) + profile-page status-aware rendering (color-coded status pills + curator-notes preview block + acceptedActionId reference line) + `messages.profile.challenges_*` extensions (8 new keys per locale across Units 12.4 + 12.5) | Unit 12.5 | ✓ submitter-side community-feedback loop closure (pull-based notification per ADR-0014 D-9); zero new tests required (state-machine + helpers exercised by Unit 12.3). |
| Phase-12 hygiene status pass (Class A/B/C catalog) | Unit 12.6 | ✓ 4 carried Class A + 14 Phase-12-specific Class B + 19 Class C carryovers; risk-surface enumeration; boundary statement. |
| OPEN_QUESTIONS hygiene + ADR review (Q57 → resolved; 14 ADRs at HEAD; zero prose-shift reconciliations) | Unit 12.7 | ✓ Q57 promoted; Q58 stays open; Q59 + Q60 candidates flagged but NOT promoted; 13 prior ADRs unchanged in body; ADRs 0004 / 0005 / 0011 / 0012 / 0013 reaffirmed or extended by ADR-0014. |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: 19 URLs UNCHANGED
  in `lighthouserc.json` (curator dashboard is auth-gated + curator-authz-
  gated; LHCI as unauthenticated would redirect — not a meaningful smoke
  surface yet). First LHCI run validating auth-aware SiteHeader on `/en/`
  still pending (Phase-9 Class B item 10; carried).
- **W3C feed validator on `/api/v1/rss.xml` + `/api/v1/digest/<slug>`**:
  Phase-3/5/6/8 compound deferral; first preview deploy + Q47 unblock.
- **Visual-regression baselines** for `/curator/challenges` +
  `/curator/challenges/[id]` deferred Phase-12 follow-on; no Playwright
  spec changes this phase.
- **No auto-merge** (ADR-0009): Phase 12 added zero LLM-translated
  content; all surfaces are code or curator-typed translation strings.
- **File-first / no DB** (ADR-0004): held for content. The
  `ratingChallenge` table's new review columns sit under ADR-0013 D-F
  (REVIEW-STATE is USER-STATE-adjacent on the same row); content
  (`content/problems/*`) stays file-first. Acceptance of a challenge
  emits a rating-action YAML per ADR-0014 D-D (manual emission preserves
  ADR-0004 + ADR-0005 immutability).
- **First Load JS shared chunk = 103 kB**: **HELD** through every
  Phase-12 unit. All new surfaces server-rendered; review form +
  withdraw button + attach-action-YAML form are server-actions; zero
  client-bundle delta.
- **Middleware bundle**: 159 kB → **160 kB** (+1 kB Drizzle re-bundling;
  well within ~16% of Vercel Edge's 1 MB limit; not a regression).

### Phase-12 unit summary (9 commits — third NON-§13 phase)

| Unit | Commit | Title |
|---|---|---|
| 12.0 | `7e60b88` | Phase 12 prep (Curator review pipeline; Q57 keystone) |
| 12.1 | `dfcbec5` | ADR-0014 curator review pipeline |
| 12.2 | `ebcf9c8` | DB scaffold: 0003_rating_challenge_review ALTER migration |
| 12.3 | `9f160e9` | Review helpers + curator authz + COI |
| 12.4 | `dbabcb9` | Curator dashboard + review API + messages.curator.* |
| 12.5 | `15b343b` | Withdraw UI + profile-page status awareness |
| 12.6 | `cfd8b52` | Phase-12 hygiene status pass (Class A/B/C catalog) |
| 12.7 | `b84c542` | OPEN_QUESTIONS hygiene + ADR review |
| 12.8 | _this_ | Phase 12 acceptance gate |

Total: **9 numbered units, all shipped**. Matches Unit 12.0 prep's
9-unit breakdown exactly. **Zero deferrals**, **zero scope drift**
across the phase (vs. Phase 8's mid-flight Unit 8.4 deferral — Phase
12 ships clean like Phases 9 + 11).

### State at HEAD (Unit 12.8)

- **Content** (no count change vs Phase-8 close; Phases 9 + 10 + 11 +
  12 added no content):
  - 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors
    / 14 institutions / 20 rating actions / 4 issue templates /
    methodology v1 (EN + FR) / contributing v1.0 (EN) + v1.1 (EN + FR)
    / 2 `entries.json` files.
  - **203 schema-validated content files + 36 raw MDX = 239 total raw
    content files UNCHANGED**.
- **New Phase-12 code surfaces** (relative to Phase-11 close at
  `2df4290`):
  - `lib/auth/curator.ts` + `lib/auth/curator.test.ts` (Unit 12.3) —
    `isCurator` env-var allowlist + 11 tests.
  - `lib/auth/login.ts` (Unit 12.4) — shared `getLoginById(userId)`
    helper.
  - `lib/rating-challenges/coi.ts` + `lib/rating-challenges/coi.test.ts`
    (Unit 12.3) — `getCoIStatus` Phase-12 simplified §8.6 + 7 tests.
  - `lib/rating-challenges/index.ts` (edit; Unit 12.3) — extended with
    state-machine predicates + `reviewChallenge` + `withdrawChallenge` +
    `getPendingChallenges` + `getChallengeById` + `attachAcceptedAction`
    + `getOpenChallengeCountByProblem`.
  - `lib/rating-challenges/state-machine.test.ts` (Unit 12.3) — 25 tests
    on pure predicates.
  - `app/[locale]/curator/challenges/page.tsx` (Unit 12.4) — list view.
  - `app/[locale]/curator/challenges/[id]/page.tsx` (Unit 12.4) — detail
    view with review form + attach-action-YAML form.
  - `app/api/v1/rating-challenges/[id]/review/route.ts` +
    `route.test.ts` (Unit 12.4) — REST surface + 10 tests.
  - `app/[locale]/profile/page.tsx` (edit; Unit 12.5) — withdraw UI +
    status-aware rendering.
  - `lib/db/schema.ts` (edit; Unit 12.2) — 4 new columns on
    `ratingChallenge` per ADR-0014 D-A.
  - `lib/db/migrations/0003_rating_challenge_review.sql` (Unit 12.2) —
    4 ALTER TABLE ADD COLUMN statements; in-place FK clause correction.
  - `lib/db/migrations/meta/0003_snapshot.json` + `_journal.json`
    (Unit 12.2) — drizzle-kit snapshot + journal.
  - `messages/{en,fr}.json` (edits; Units 12.4 + 12.5) — +55 keys per
    locale (47 `curator.*` + 8 `profile.challenges_*` extensions); +110
    keys total across EN + FR.
  - `docs/adr/0014-curator-review-pipeline.md` + `docs/adr/README.md`
    (edit) (Unit 12.1) — ADR + index update.
  - 4 new THINK docs (12.0 + 12.6 + 12.7 + 12.8).
- **Routes**: ~590 → **~592 prerendered pages** (+2: curator list both
  locale variants) + **4 dynamic API routes** (`ƒ /api/auth/[...nextauth]`
  + `ƒ /api/v1/watchlist/[slug]` + `ƒ /api/v1/rating-challenges` + **`ƒ
  /api/v1/rating-challenges/[id]/review`**).
- **Tests**: **456/456 across 50 vitest files** (was 403/46 at Phase-11
  close; +53 tests / +4 files in Phase 12 = 11 isCurator + 7 COI + 25
  state-machine + 10 review API).
- `pnpm validate-content` → **203 files** green (unchanged).
- `pnpm audit-content` → **0 errors / 6 warnings** (Q32 baseline since
  Phase 2; unchanged).
- `pnpm typecheck` clean.
- `pnpm build` clean; ~592 prerendered pages + 4 dynamic API routes;
  **First Load JS shared chunk = 103 kB UNCHANGED**; **middleware bundle
  = 160 kB** (+1 kB tolerance Drizzle re-bundling).
- **5 visualizations live** (unchanged from Phase 4).
- **14 ADRs** (was 13 at Phase-11 close; +1: **ADR-0014** in Unit 12.1).
  First new ADR since Phase 9.
- **Dependencies**: **+0 net in Phase 12** (Phase-9/10/11 stack stable).
- **`lighthouserc.json`** enrols **19 URLs** (unchanged from Phase-8
  close).
- **OPEN_QUESTIONS state**: 20 resolved + 4 decided-as-lean + 29 open
  = **53 total entries** (was 53 at Phase-11 close; +1 promotion Q57 →
  resolved; -1 open net 0 size change).
- **DB schema tables**: **6 UNCHANGED** (`user`, `account`, `session`,
  `verificationToken`, `watchlist`, `ratingChallenge`). Phase 12 ALTERed
  `ratingChallenge` (no CREATE).
- **DB schema columns added to `ratingChallenge`**: +4 (`reviewedAt`,
  `reviewerId`, `reviewNotes`, `acceptedActionId`).
- **Migrations**: 3 → **4 cumulative** (drizzle-kit 0-indexed monotonic
  sequence: `0000_initial_auth` + `0001_watchlist` + `0002_rating_challenges`
  + **`0003_rating_challenge_review`** — first ALTER).
- **Env contract**: +1 env var (`LOP_CURATOR_LOGINS` per ADR-0014 D-B).
- **New code layers**: `lib/auth/curator.ts` + `lib/auth/login.ts` +
  `lib/rating-challenges/coi.ts` + `app/[locale]/curator/` tree +
  `app/api/v1/rating-challenges/[id]/review/` route.

### `lighthouserc.json` final state

Phase 12 changes: **none**. URL count holds at **19** at Phase-12 close.
Curator dashboard is auth-gated + curator-authz-gated; LHCI as
unauthenticated would redirect. First LHCI run validating auth-aware
SiteHeader on `/en/` is a Phase-9 Class B item 10 carryover.

### Phase-12 follow-ons that survive the gate (non-blocking)

Documented in detail in Unit 12.6's hygiene status pass (14 Class B + 4
Class A + 19 Class C items). Critical-path subset for Phase 13+
planning:

1. **CLI helper `pnpm emit-challenge-action <id>`** (Class B item 1).
   Automates ADR-0014 D-D YAML scaffolding step. Phase 13+. Potential
   **Q59 candidate** if Phase-12 acceptance smoke signals manual
   emission too cumbersome.
2. **Q58 public visibility surface** (Class B item 11). Counter / per-
   problem listing / public-profile-of-challenges. Phase 13+ explicit.
3. **Public profile page at `/[locale]/u/[handle]`** (Class B item 12 +
   Phase-10 Class B item 1). Couples to Q58.
4. **Full §8.6 24-month collaborator COI check** (Class B item 6).
   Phase 13+ when usage justifies multi-hop DB ↔ file-system traversal.
5. **Email notifications when challenge is reviewed** (Class B item 7).
   Phase 13+ alongside subscriber-list `lib/email/` (Phase-5 D-4 punt).
6. **Multi-stage curator approval** (Class B item 3). Phase 14+
   alongside editorial-board Q7.
7. **Per-problem ACLs** (Class B item 4) + **`curatorRoles` DB table +
   admin UI** (Class B item 5). Phase 13+ / 14+.
8. **Middleware-based auth-route protection** (Class B item 13). Phase-9
   Class B item 12 follow-on. Threshold = 3+ protected page routes;
   Phase 12 brings count to 2 (profile + curator dashboard). Lift fires
   Phase 13+ if a third protected page route lands.
9. **Q54 + Q55 operational unblocks** (Class A items 1 + 2; carried
   since Phase 9). Phase-12 surfaces architecturally complete; only
   operational env config blocks end-to-end Vercel smoke.
10. **CI dummy `AUTH_SECRET`** (Class A item 3; carried).
11. **`pnpm db:migrate` doc for new contributors** (Class A item 4) —
    DB now has 4 migrations to run on first setup.

### Pre-existing follow-ons that survived Phase 12 (carryover)

Unchanged from Unit 11.7 acceptance gate (19 Class C items per Unit
12.6 catalog). Notable:

- HTML shell migration + `<html lang={locale}>` + SiteHeader under
  `NextIntlClientProvider` — STILL ON HOLD per parallel-session
  preservation signal.
- W3C feed validator pass on `/api/v1/rss.xml` (Phase-3 carryover).
- `<managingEditor>` on RSS feeds (Q33/Q44; Q2 DNS gate).
- `pnpm clean-drafts` script.
- Phase-2 ROR-ID + InstaDeep orphan.
- `messages.{contributing,methodology,…}.*` chrome strings + FR bulk
  backfill + StatusPill localization + nav labels via `useTranslations`
  (Q51 curator-track horizon).
- Trailing-slash normalization for `NEXT_PUBLIC_SITE_URL`.
- Per-entry sitemap `lastModified` / `changeFrequency` / `priority`.
- Watchlist count display on `/problems` index (Phase-9 Class B item 4).
- Multi-provider OAuth expansion (Phase-9 Class B item 8).
- Real-API integration smoke for `lib/discussions/github-graphql.ts`
  (Q47 + `GITHUB_TOKEN`).

### Phase 13+ entry conditions

Per §12 cardinal rule: **explicit human sign-off required** at this
acceptance gate. Phase 12 closes the curator review pipeline keystone
(Q57) + lands the first new ADR since Phase 9 (ADR-0014) + lands the
first ALTER migration + lands the first authorization tier beyond
signed-in.

**Phase 13+ candidate threads** (each overridable):

- **Q58 public visibility surface** (~3-5 units). Smallest reasonable
  next phase; couples cleanly to Phase 12's `accepted` / `submitted` /
  `under_review` / `rejected` / `withdrawn` status transitions. Counter
  on problem detail + per-problem listing + (eventually) public-profile-
  of-challenges. Phase-12 hygiene Class B item 11; Unit 11.6 Q58 lean
  documented.
- **Subscriber-list (third-party email)** (~6 units). Phase-5 D-4 punt
  closure; opens `lib/email/` infrastructure + ADR-0015 (provider
  choice). Couples to Phase-12 Class B item 7 (email notifications when
  challenge is reviewed). **Potential ADR-0015** candidate.
- **Public profile page at `/[locale]/u/[handle]`** (~3-4 units).
  Phase-10 Class B item 1 + Phase-12 Class B item 12. Couples to Q58
  (challenges public-visibility). Phase 13+ candidate alongside Q58.
- **CLI helper `pnpm emit-challenge-action <id>`** (~2-3 units; Q59
  promotion if usage demands). Automates ADR-0014 D-D YAML scaffolding;
  removes the "write YAML by hand" friction at acceptance time.
- **Multi-provider OAuth expansion** (~3-4 units; **potential ADR-0016**).
  Lifts ADR-0012 D-B's single-provider restriction. Lower priority — no
  observed non-GitHub user-segment ask yet.
- **HTML shell migration + Unit 8.4 unblock** — STILL ON HOLD per
  parallel-session preservation signal.

DB-migration trigger re-eval mandatory at Phase 13 kickoff per Unit
12.0 D-2 cascade (procedural; trigger (a) already fired Phase 9; trigger
(b) cold at ~1.656% of 5 MB).

### Cross-phase milestone

This commit closes the **Curator review pipeline** — the keystone Phase-
12 thread + the strongest claim by sequential thread-closure precedent
(Phase 11 introduced submission; Phase 12 closes review). 9 commits + 1
new ADR (ADR-0014) + 0 new dependencies + 0 client-bundle regressions
(103 kB First Load JS preserved end-to-end) + 0 test regressions + 53
new tests + +1 OPEN_QUESTIONS promotion (Q57 resolved) + 1 new
authorization tier (curator allowlist via `LOP_CURATOR_LOGINS` env var)
+ 1 new ALTER migration (`0003_rating_challenge_review` — first ALTER
in project) + 4 new DB columns on `ratingChallenge` (no new tables) +
55 new i18n keys per locale + 1 new dynamic API route + 2 new page
routes.

**§13 ledger remains CLOSED** (closed at Unit 9.9 with Phase 9
acceptance gate). Phase 12 is the **third NON-§13 phase**.

**Community-feedback loop closed**. Phase 11 shipped the submission
half (any signed-in user can challenge any rating dimension); Phase 12
shipped the review half (curators can transition challenges through
the 7-state machine; submitters can withdraw; acceptance emits a rating-
action YAML manually per ADR-0014 D-D). The §3.1 "ratings are revisable"
mandate is now operational at the code layer. §8.6 COI is simplified
for Phase 12 + flagged for Phase 13+ full enforcement.

### Phase-12 over-vs-under against the 12.0 plan

Unit 12.0 prep scoped a 9-unit phase (12.0–12.8). Actual: **9 units
shipped**, **zero deferrals**, **zero scope drift**. Per-unit landings
matched the prep table exactly. ADR-0014 absorbed the architectural
surface that Unit 12.0 D-12 had pinned in advance; D-A through D-F all
realized in Units 12.2–12.5.

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 12.8 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 456/456 across 50 files unchanged (no test files touched
  in 12.8).
- `pnpm build` → ~592 prerendered pages + 4 dynamic API routes
  unchanged. First Load JS shared chunk = 103 kB unchanged. Middleware
  bundle = 160 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-12 close: marks Unit 12.8 as the acceptance gate; cites the
Phase-12 deliverable status table (every row ✓; zero deferrals); emits
the cross-phase state-at-HEAD (10 problems / etc.; ~592 prerendered
pages + 4 dynamic API routes; 103 kB First Load JS; 456 tests across 50
files; 14 ADRs; 19 LHCI URLs; +0 new deps; 6 DB tables; 4 migrations;
53 OQ entries); enumerates Phase-12 follow-ons (CLI emission helper /
Q58 visibility / public profile / full §8.6 COI / email notifications /
multi-stage approval / per-problem ACLs / curatorRoles table /
middleware lift threshold 2/3 / Q54+Q55+CI AUTH_SECRET+db:migrate doc);
enumerates pre-existing carryovers; states Phase 13+ entry condition
(explicit sign-off per §12); notes the **community-feedback loop
closure** (Phase 11 submission + Phase 12 review = §3.1 + §8.6
architectural mandate operational); flags ADR-0015 (subscriber-list) +
ADR-0016 (multi-provider OAuth) + ADR-0017 (full §8.6 COI +
`curatorRoles`) as Phase-13+ candidates.
