# 44.0 — Phase 44 prep (third cross-surface expansion of a Phase-37-framework consumer: arxiv expands from `rationale`-only to all 4 markdown surfaces via constructor-arg change in `PHASE_41_DEFAULT_ENABLED_SURFACES`; mirrors Phase 42 + 43 expansion pattern verbatim; **completes per-consumer expansion arc — all 3 consumers expanded to all 4 surfaces**; closes ADR-0018 APPEND-D-Y item 1 deferral at 3-phase carryover; APPEND-D-AB second two-letter slot; anticipated 4-5 units; **39th "Continue" override**; **thirty-fifth NON-§13 phase**)

Phase 44 kickoff per `MASTER_PROMPT.md` §12 cardinal rule. Phase 43
closed at HEAD `eb32444` (Unit 43.4 acceptance gate; **thirty-
fourth NON-§13 phase**; 5 units; second cross-surface expansion
realized via tables; first two-letter APPEND-D-AA; first
"schemaOverrides on bio + rationale + actionRationale"
production state; first "all 4 surfaces with same-surface
different-slot composition" state; first "all 3 framework slots
on same surface" case validated end-to-end). This unit is
docs-only.

> **Sign-off note**: §12 requires explicit human sign-off at every
> phase boundary. Phase 44 sign-off was granted **via "Continue"
> override** (**thirty-ninth invocation** of this pattern). The
> first-thread D-1 recommendation is overridable into Unit 44.1.

> **§13 ledger status (carried)**: still **CLOSED**. Phase 44 is
> the **thirty-fifth NON-§13 phase**.

## THINK

### Phase 43 → Phase 44 baseline (HEAD = `eb32444`)

- **Tests**: **908/908 across 71 vitest files**.
- **ADRs**: **24** (Phase 43 added 0 new ADRs).
- **ADR-0018 D-G**: **10 APPENDs** (project record; Phase 18 +
  27 + 29 + 37 + 38 + 39 + 40 + 41 + 42 + 43). Single-letter
  slot CONSUMED through Z (Phase 42 D-Z); two-letter slot
  IN USE since Phase 43 D-AA; Phase 44 takes **D-AB**.
- **First Load JS shared chunk**: **103 kB UNCHANGED** through
  every Phase 9-43 unit (**80 consecutive units**).
- **Middleware bundle**: **160 kB UNCHANGED** since Phase 12.
- **DB tables**: 7 unchanged; **Migrations**: 9 unchanged.
- **Env vars**: **14** (`MARKDOWN_EXTENSIONS` 4 single-value
  arms + compositions including 3-way).
- **`lib/markdown/extensions/` files**: **13** UNCHANGED.
- **Phase-37-framework concrete consumers**: **3** exercising
  **3 of 3 slots**.
- **Markdown surfaces enabled by ≥1 consumer**: **4 of 4**
  (Phase 42 + 43 expansions; Phase 41 baseline).
- **Per-consumer default-enabled-surfaces** (Phase 44 starting
  state):
  - `PHASE_38_DEFAULT_ENABLED_SURFACES = Set(["bio", "reviewNotes", "rationale", "actionRationale"])` — wikilinks (4; Phase 42).
  - `PHASE_39_DEFAULT_ENABLED_SURFACES = Set(["bio", "reviewNotes", "rationale", "actionRationale"])` — tables (4; Phase 43).
  - `PHASE_41_DEFAULT_ENABLED_SURFACES = Set(["rationale"])` — arxiv (1; Phase 41 ship).
- **Surfaces with all 3 framework slots active under 3-way
  default**: **1 (`rationale`; Phase 43 ship)**.
- **OPEN_QUESTIONS state**: 28 resolved + 4 lean + 34 open =
  **66 total** UNCHANGED since Phase 36.
- **Phase-37+ candidate count**: **8** UNCHANGED.
- **APPEND-deferral closure cadence pattern**: Phase 42 closed
  Phase-38 D-L item 1; Phase 43 closed Phase-39 D-Q item 2;
  oldest-first at one-deferral-per-phase. Phase 44 continues
  the cadence with Phase-41 D-Y item 1 (skips Phase 40 because
  composition is not a per-consumer cross-surface deferral).
- **Working tree at session start**: clean (Phase 43 close at
  `eb32444`).

### D-1. First-thread recommendation: **arxiv cross-surface expansion to all 4 markdown surfaces via constructor-arg change in `PHASE_41_DEFAULT_ENABLED_SURFACES`**

Closes:

1. **ADR-0018 APPEND-D-Y item 1 deferral** (Phase 41 documented
   "Cross-surface arxiv expansion: bio + reviewNotes +
   actionRationale autolinks; no current content evidence in
   user-prose columns; demand-signal-first; Phase 42+
   constructor-arg change with zero plugin or registry rework").
   Phase 44 is the demand-signal-relaxed realization mirroring
   the Phase 42 + 43 patterns verbatim. **3-phase gap** from
   Phase-41 intent to Phase-44 realization (faster than the
   4-phase Phase 38 → 42 and Phase 39 → 43 gaps because
   APPEND-D-Y was created 2 phases later than D-L and 2
   phases later than D-Q).
2. **Phase-43-gate Phase-44-candidate rank 1** ("Arxiv cross-
   surface expansion; completes per-consumer expansion arc").

**Why arxiv now** (the last remaining cross-surface-expansion
candidate after Phase 42 wikilinks + Phase 43 tables):

- **Completes the per-consumer expansion arc**: after Phase
  44, **all 3 Phase-37-framework consumers** (wikilinks +
  tables + arxiv) have been **expanded to all 4 markdown
  surfaces** via constructor-arg value-only change. The
  "constructor-arg-only zero-rework expansion" property each
  Phase 38/39/41 consumer documented in its APPEND will
  have **three real-consumer-expansion realizations**.
- **First "all 3 framework slots on all 4 surfaces" state**:
  pre-Phase-44, only `rationale` surface has all 3 slots
  active under 3-way default. Post-Phase-44, ALL 4 surfaces
  have all 3 slots active under 3-way `wikilinks,tables,arxiv`
  default. **Maximal multi-consumer all-surfaces composition**
  becomes the new default.
- **Closes the oldest remaining cross-surface-expansion
  APPEND deferral**: D-L Phase 38 closed Phase 42; D-Q
  Phase 39 closed Phase 43; D-Y Phase 41 is the next oldest
  (skipping Phase 40 which had no per-consumer deferral).
  Cadence preserved.

**Path A** (no new ADR; APPEND ADR-0018 D-G) chosen per
Phase-37 through 43 D-9 precedent. ADR-0018 D-G gains its
**11th APPEND** — extends the record from 10 → 11. **Second
two-letter APPEND letter D-AB** (after Phase-43 D-AA).

### D-2. DB-migration trigger re-evaluation

Phase 44 anticipates **0 new migrations**. Pure constant-value
change in a TypeScript module. §5.7 trigger (a) FIRED Unit 9.6
carried; trigger (b) cold Phase 44.

### D-3. Cross-surface arxiv expansion shape anticipated (Unit 44.1)

D-A. **`PHASE_41_DEFAULT_ENABLED_SURFACES` value change** (mirrors
Phase 42 + 43 verbatim):

```ts
// Before (Phase 41 ship through Phase-43 close):
export const PHASE_41_DEFAULT_ENABLED_SURFACES: ReadonlySet<MarkdownSurface> =
  new Set(["rationale"]);

// After (Phase 44 ship):
export const PHASE_41_DEFAULT_ENABLED_SURFACES: ReadonlySet<MarkdownSurface> =
  new Set(["bio", "reviewNotes", "rationale", "actionRationale"]);
```

**Name discipline preserved** per Phase-42 D-8 precedent.
Surface enumeration follows `MarkdownSurface` type-union order
per Phase-42 D-9.

D-B. **`ArxivExtensionRegistry` class + factory dispatch arm —
UNCHANGED**. The class accepts a `ReadonlySet<MarkdownSurface>`
constructor arg; expansion is constructor-arg-only. **Third
real-consumer-expansion realization** of the property.

D-C. **Tests to update / add** (mirrors Phase 42/43 Unit 1
shape):

1. `arxiv.test.ts` — update the registry assertion from
   `Set(["rationale"])` + size 1 → 4 surfaces + size 4.
2. `extensions/index.test.ts` — update the dispatch test
   "arxiv on rationale only Phase 41" → "all 4 surfaces
   Phase 44" assertions.
3. `extensions/index.test.ts` — update composite-dispatch
   tests (the 3-way `wikilinks,tables,arxiv` test gains
   arxiv-on-all-4 assertions; the 2-way `wikilinks,arxiv`
   and `tables,arxiv` arguments updated analogously).

D-D. **NEW end-to-end tests** (Unit 44.2):

1. `describe("Phase-44 arxiv default — all 4 surfaces via PHASE_41_DEFAULT_ENABLED_SURFACES")`
   block — arxiv autolinks render on bio + reviewNotes +
   rationale (Phase-41 baseline) + actionRationale via the
   factory-driven default constant.
2. `describe("Phase-44 all-3-slots-on-all-4-surfaces under 3-way default")`
   block — under 3-way `wikilinks,tables,arxiv` Phase-44
   defaults, every surface has all 3 slots active:
   - bio: wikilinks(rehype) + tables(schema) + arxiv(remark).
   - reviewNotes: wikilinks(rehype) + tables(schema) + arxiv(remark).
   - rationale: wikilinks(rehype) + tables(schema) + arxiv(remark) — Phase-43 baseline preserved.
   - actionRationale: wikilinks(rehype) + tables(schema) + arxiv(remark).
   **First "all 3 framework slots on all 4 surfaces under
   default dispatch" state**. Conflict-free per APPEND-D-R.

D-E. **Composition matrix Phase 44 expansion** (Phase-44
ADR-0018 D-G APPEND-D-AB documents):

| Surface | `wikilinks,tables,arxiv` (Phase 44 3-way default) |
|---|---|
| `bio` | wikilinks(rehype) + tables(schema) + arxiv(remark) |
| `reviewNotes` | wikilinks(rehype) + tables(schema) + arxiv(remark) |
| `rationale` | wikilinks(rehype) + tables(schema) + arxiv(remark) |
| `actionRationale` | wikilinks(rehype) + tables(schema) + arxiv(remark) |

**All 4 surfaces have all 3 slots active** post-Phase-44.
Conflict-free per APPEND-D-R (each surface has at most one
component per slot; 3 distinct slots × 4 surfaces × 3
consumers = 12 component-surface-slot triples, all distinct).
**Maximal multi-consumer all-surfaces composition** under
default dispatch.

D-F. **APPEND letter sequence: D-AB (second two-letter slot)**.
Phase 43 used D-AA; Phase 44 uses D-AB. Subsequent
single-APPEND phases continue with D-AC, D-AD, ... D-AZ; then
D-BA + ... .

D-G. **Phase 45+ deferrals** (Phase-44 arxiv-consumer scope
cap; mirrors the Phase-42 + 43 deferral structure):

- **Older-style category-prefixed arXiv IDs** (`math/0211159`)
  — APPEND-D-Y item 2 deferral carries forward; Phase 45+ if
  curator content surfaces older citations.
- **Bare arXiv IDs without `arxiv:` prefix** — APPEND-D-Y
  item 3 deferral; Phase 45+ if a stricter context-aware
  match becomes warranted.
- **DOI sibling consumer** — APPEND-D-Y item 4 deferral; first
  compositional same-slot case under composition (two
  consumers both using `remarkPlugins`); Phase 45+.
- **arXiv display-text alias syntax** `[[arxiv:NNNN.NNNNN|display]]`
  — APPEND-D-Y item 5 deferral; Phase 45+.
- **Paper-card hover-preview** — APPEND-D-Y item 6 deferral;
  Phase 45+ couples to UI thread.
- **Multi-anchor wikilink alias `[[slug|display]]`** —
  APPEND-D-L item 2 deferral; Phase 45+.
- **Cross-entity wikilinks** — APPEND-D-L item 3 deferral;
  Phase 45+.
- **Table-specific attributes** (`colspan` / `rowspan` /
  `scope`) — APPEND-D-Q item 3 deferral; Phase 45+.
- **`<a class="wikilink">` styling** — APPEND-D-L item 4
  deferral; Phase 45+.
- **404 handling for unresolved wikilinks** — APPEND-D-L
  item 5 deferral; Phase 45+.

### Provisional unit breakdown (44.0 … 44.4)

| Unit | Title | Type |
|---|---|---|
| 44.0 | **Phase 44 prep** (this unit) | docs |
| 44.1 | `PHASE_41_DEFAULT_ENABLED_SURFACES` expansion + updated `arxiv.test.ts` + updated `extensions/index.test.ts` + ADR-0018 D-G APPEND-D-AB | code+APPEND |
| 44.2 | NEW end-to-end tests for arxiv on bio + reviewNotes + actionRationale + first all-3-slots-on-all-4-surfaces tests | code |
| 44.3 | Phase-44 hygiene + ADR-0018 APPEND-D-Y item 1 closure documentation | docs |
| 44.4 | **Phase 44 acceptance gate** | gate |

Total: **5 units**. Mirrors Phase 42 + 43 unit shape verbatim.

### Phase-44-blocking decisions deferred to per-unit implementation

- **D-8** (Unit 44.1): Constant naming — keep
  `PHASE_41_DEFAULT_ENABLED_SURFACES` per Phase-42 D-8 precedent. Lean = **keep**.
- **D-9** (Unit 44.1): Surface enumeration order — `MarkdownSurface`
  type-union order per Phase-42 D-9 precedent. Lean = **type-union order**.
- **D-10** (Unit 44.2): Test-block placement — new describe blocks per Phase-42/43 D-10 precedent. Lean = **new blocks**.
- **D-11** (Unit 44.1): APPEND-D-AB naming — Excel convention. Lean = **continue Excel**.

### Smoke gates (this unit)

Docs-only — no source files modified.

- File added: `docs/thinking/44.0-phase-44-prep.md`.
- CHANGELOG: open `### Phase 44` section + Unit 44.0 entry.
- `pnpm audit-content`: 0 errors / 6 warnings UNCHANGED.
- `pnpm test`: 908/908 across 71 files UNCHANGED.
- First Load JS / middleware: UNCHANGED.

### Parallel-curator awareness

This unit docs-only; no collision. Phase 44 collision risk
mirrors Phase 42/43:

- 44.1: MODIFIED `lib/markdown/extensions/arxiv.ts` +
  `arxiv.test.ts` + `extensions/index.test.ts` +
  ADR-0018 (APPEND-D-AB).
- 44.2: MODIFIED `lib/markdown/index.test.ts` (new describe
  blocks).
- 44.3 / 44.4: docs hygiene + gate; parallel-safe.

### Alternative threads (overridable into Unit 44.1)

| Thread | Why tempting | Why deferred | Units |
|---|---|---|---|
| **DOI sibling consumer** in `remarkPlugins` slot | First compositional same-slot case (two consumers using `remarkPlugins`); architecturally novel | Higher complexity than cross-surface expansion; Phase 45+ closes APPEND-D-Y item 4. | 2-3 |
| **Multi-anchor wikilink alias syntax** | APPEND-D-L item 2 deferral | Less architecturally novel than completing the expansion arc; Phase 45+. | 1-2 |
| **Cross-entity wikilinks** | APPEND-D-L item 3 deferral | Requires plugin parameterization; Phase 45+. | 2-3 |
| **Table-specific attributes** (`colspan` / `rowspan` / `scope`) | APPEND-D-Q item 3 deferral; XSS audit | XSS-audit-required; Phase 45+. | 1-2 |
| **ADR-0025 concrete moderation provider** | Strongest patience signal | Not autonomous-tractable. | 3-5 |
| **`@mention` consumer** | APPEND-D-Y item 7 | Conditional on Q73 gate. | 2-3 |
| **Pre-commit typecheck hook** | Closes Unit-36.1 gap | Tiny. | 1 |
| **Q78 / Q79 / v2 methodology / safeAuth / test backfill / account-delete** | Various follow-ons | Each deferred. | 1-5 each |

### Phase-44 anti-scope (boundary statement preview for Unit 44.4)

NOT DOI sibling consumer (Phase 45+);
NOT multi-anchor wikilink alias (Phase 45+);
NOT cross-entity wikilinks (Phase 45+);
NOT table-specific attributes (Phase 45+);
NOT plugin parameterization for wikilink-href-builder (Phase
45+);
NOT `<caption>` element (Phase 45+);
NOT surface-specific table schemas (Phase 45+);
NOT older-style arxiv IDs (Phase 45+);
NOT bare arxiv IDs without prefix (Phase 45+);
NOT arxiv display-text alias syntax (Phase 45+);
NOT paper-card hover-preview (Phase 45+);
NOT `<a class="wikilink">` styling (Phase 45+);
NOT 404 handling for unresolved wikilinks (Phase 45+);
NOT `@mention` resolution (Phase 45+);
NOT same-slot composition with two consumers (Phase 45+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-43 deferrals carry forward.

**This phase is the third cross-surface expansion of a
Phase-37-framework consumer**. Completes the per-consumer
expansion arc — all 3 consumers expanded to all 4 surfaces.
First "all 3 framework slots on all 4 surfaces under default
dispatch" state in project history.

### Anticipated architectural firsts in Phase 44 (12)

1. **Third cross-surface expansion of a Phase-37-framework
   consumer** — arxiv → all 4 surfaces. **Completes the
   per-consumer expansion arc**: wikilinks (Phase 42) +
   tables (Phase 43) + arxiv (Phase 44) all now ship default-
   enabled on all 4 surfaces. The "constructor-arg-only zero-
   rework expansion" property has **three real-consumer-
   expansion realizations**.
2. **First "all 3 framework slots on all 4 surfaces under
   default dispatch" state** in project history. Pre-Phase-44:
   only `rationale` had all 3 slots active under 3-way
   default. Post-Phase-44: ALL 4 surfaces have all 3 slots
   active. **Maximal multi-consumer all-surfaces composition**
   becomes the new default.
3. **APPEND-D-Y item 1 deferral CLOSED at 3-phase carryover**
   (Phase 41 → Phase 44). **Third prep-/APPEND-doc-level
   deferral closed by value-only change in a later phase**
   (Phase 42 → 38 at 4-phase gap; Phase 43 → 39 at 4-phase
   gap; Phase 44 → 41 at 3-phase gap; the 3-phase gap is
   faster because APPEND-D-Y was created 2 phases later
   than D-Q; the per-phase cadence is the same).
4. **Eleventh APPEND on ADR-0018 D-G** — extends the **first-
   ADR-D-clause-with-most-APPENDs record** from 10 → 11
   (Phase 18 + 27 + 29 + 37 + 38 + 39 + 40 + 41 + 42 + 43 +
   **44**).
5. **Second two-letter APPEND letter D-AB** (after Phase-43
   D-AA). Excel-spreadsheet column convention continues.
6. **First "framework + 3 consumers + composition + 3
   expansions" 8-phase cluster** in project history (Phase
   37-44). Eight-phase architectural arc.
7. **14th consecutive phase without new B category** —
   Phase 31-44 = **first 14-phase run** in project history
   (extends Phase-43 record from 13 → 14).
8. **81st-through-84th consecutive 103 kB First Load JS
   units** (Phase 44 has 5 units; 44.0 prep doesn't
   increment; 44.1-44.4 each increment).
9. **Thirty-fifth NON-§13 phase**.
10. **Third "constructor-arg-only zero-rework expansion"
    property realization** — pattern matured. **Framework's
    plugin + class + factory code remains stable** since
    Phase 37/38/39/40/41 ship commits; cross-surface
    expansions touch only constant + tests + ADR APPEND.
11. **Third consecutive APPEND-deferral closure** (Phase 42
    closed D-L item 1; Phase 43 closed D-Q item 2; Phase 44
    closes D-Y item 1). Cadence sustained 3 phases.
12. **All 3 consumers + all 4 surfaces + all 3 slots = full
    framework activation under default dispatch**. Post-Phase-
    44 state is the framework's **maximal activation**: no
    further default-enabling-of-existing-consumers is
    possible. Phase 45+ candidates necessarily explore
    consumer behavior (alias syntax, cross-entity), schema
    behavior (table-attributes, class-styling), validation
    (404-handling), or new consumers (DOI, @mention).

### Open questions newly surfacing

Anticipated: 0 new opens. ADR-0018 APPEND-D-Y item 1
(cross-surface arxiv expansion deferral) is the LOCUS of
Phase 44 closure.
