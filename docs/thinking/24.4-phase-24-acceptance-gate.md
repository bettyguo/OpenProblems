# 24.4 — Phase 24 acceptance gate (resize+WebP realization; 19 ADRs unchanged; fifth consecutive operational-script-keystone phase; first concrete user-value-at-deploy)

Fifth and final Phase-24 unit; docs-only. Closes Phase 24
(**fifteenth NON-§13 phase**; ADR-0019 D-F resize+WebP path
realized via forward `putAvatar` extension + retroactive
backfill script). Mirrors Phase-12 through Phase-23
acceptance-gate patterns.

> **§12 sign-off**: Phase 25 sign-off **pending** at end of unit.
> "Continue" override path available (**twentieth invocation**
> opportunity).

## THINK

### Phase 24 ledger

**5 units shipped** (24.0 prep → 24.4 gate). **0 deferrals**;
**0 scope drift**. **Thirteenth consecutive well-scoped phase**.
**Matches Phase 20-23 5-unit shape** — five consecutive 5-unit
operational-script-keystone phases.

| Unit | Title | Type | Status |
|---|---|---|---|
| 24.0 | Phase 24 prep | docs | shipped `20c14d6` |
| 24.1 | putAvatar resize+WebP + backfill-resize-webp script | code | shipped `dbab227` |
| 24.2 | Phase-24 hygiene status pass | docs | shipped `9b06f2b` |
| 24.3 | ADR-0019 D-F Phase-24 enhancement note | docs | shipped `f13e738` |
| 24.4 | **Phase 24 acceptance gate** (this unit) | gate | this commit |

### Phase-24 closure summary

**ADR-0019 D-F image-processing inheritance contract** REALIZED
in Unit 24.1 for the resizing + WebP format-conversion paths
(rows 3 + 4 of the D-F example pipeline). Forward path:
`sharp(buffer).rotate().resize({ width: 512, height: 512, fit: "cover" }).webp({ quality: 85 }).toBuffer()`
in `lib/storage/putAvatar`. Retroactive path:
`scripts/backfill-resize-webp.ts` with `-resize.webp` storage-
key suffix (distinguishes from Phase-20's `-backfill.<ext>`).
Storage-key extension + content-type always `.webp` / `image/webp`
post-Phase-24 regardless of input MIME; `inferExt()` helper
removed as dead code; 5 existing storage tests modified in-place
(0 net new tests).

Phase 24 is the **sixth phase since Phase 3 to close without
new ADR** (Phase 18 + 20 + 21 + 22 + 23 + 24; **first
five-phase no-new-ADR streak**) and the **fifth consecutive
operational-script-keystone phase** (Phase 20-24). Pattern
stable at 5 phases.

**First concrete user-value-at-deploy** in the operational-
script-keystone streak — Phase 20-23 closures shipped curator-
workflow improvements (EXIF privacy backfill / orphan storage
hygiene / curator scaffold tooling / scaffold auto-fill);
Phase 24 ships measurable post-deploy effect on shipped
content (smaller avatar files + bandwidth + storage + render
savings).

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`f13e738`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **585/585 across 55 vitest files**. 0 net
  test Δ from Phase 23 close (585/55); **5 existing tests
  modified in-place** for new sharp `.resize()` + `.webp()`
  chain + `.webp` extension + `image/webp` content-type
  assertions. **First operational-script-keystone phase to
  ship 0 net new tests**.
- **`pnpm build`** → ~659 prerendered pages + 7 dynamic page+API
  routes UNCHANGED. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-24 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` = 117 kB UNCHANGED. `/u/{handle}` = 108 kB
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32
  baseline since Phase 2; unchanged through every Phase 3-24
  unit).
- **`pnpm lint`** clean.

### Architectural firsts shipped in Phase 24

1. **First bandwidth-optimization surface** in project history.
2. **Fifth consecutive operational-script-keystone phase**
   (Phase 20-24); pattern stable at 5 phases.
3. **Eighth consecutive 0-migration phase** (Phase 17-24; 11
   of 15 phases since DB landed are 0-migration).
4. **Eighth consecutive single-session phase** (Phase 17-24).
5. **Sixth phase since Phase 3 to close without new ADR**
   (**first five-phase no-new-ADR streak**).
6. **First concrete user-value-at-deploy** in the operational-
   script-keystone streak.
7. **First edit to `lib/storage/` since Phase 19** — first-edit-
   after-five-phases of the storage layer.
8. **First operational-script-keystone phase to ship 0 net new
   tests** — Phase 20+21 = 0 new; Phase 22+23 = +11/+10 new;
   Phase 24 = 0 net via in-place modification of 5 existing
   tests.
9. **First phase to ship two operational scripts producing
   different storage-key suffixes** — Phase-20 `-backfill.<ext>`
   + Phase-24 `-resize.webp` coexist + distinguish in Vercel
   Blob console.
10. **First phase to remove dead code** — `inferExt()` helper
    in `lib/storage/index.ts` becomes dead after Phase 24
    (output is always `.webp`); removed in same code unit.
11. **First five-phase Q-tally freeze** in project history
    (Phase 20-24).
12. **Implementation-note convention extends to a second ADR** —
    Phase 22+23 codified APPEND-not-EDIT on ADR-0014 D-D;
    Phase 24 applies verbatim to ADR-0019 D-F.

### Delta summary (Phase 23 → Phase 24)

| Metric | Phase 23 close (`225d97f`) | Phase 24 close (HEAD `f13e738`) | Δ |
|---|---|---|---|
| Commits | 5 | 4 + this gate | +5 |
| ADRs | **19** | **19** | **0** (1 amended with Phase-24 note on D-F; **fifth consecutive no-new-ADR phase** = first five-phase streak; **sixth phase since Phase 3** to close without new ADR) |
| Migrations | 6 | **6** | **0** (**eighth consecutive 0-migration phase**) |
| Runtime deps | (Phase-23 stack) | (Phase-23 stack unchanged) | **0** |
| Env vars | 7 | 7 | **0** |
| Tests | 585 / 55 files | **585 / 55 files** | **0 net** (5 existing modified in-place) |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` + `/u/{handle}` bundles | 117 + 108 kB | 117 + 108 kB | 0 |
| OPEN_QUESTIONS resolved | 26 | **26** | **0** (**first five-phase Q-tally freeze** — Phase 20-24) |
| OPEN_QUESTIONS open | 29 | **29** | **0** |
| OPEN_QUESTIONS total | 59 | **59** | **0** |
| i18n keys per locale | 127 | 127 | 0 |
| `lib/storage/` files | 2 | 2 | **0** (in-place edit; both files modified) |
| `scripts/` files | 9 | **10** | **+1** (`backfill-resize-webp.ts`) |
| `package.json#scripts` entries | (+1: `emit-challenge-action`) | (+1: `backfill-resize-webp`) | +1 |
| Server-side image processing surfaces | 2 | **3** | **+1** |
| Retroactive-privacy-correction surfaces | 1 | **2** | **+1** (Phase-20 EXIF + Phase-24 resize-WebP) |
| Bandwidth-optimization surfaces | 0 | **1** | **+1** (first in project history) |
| Operational-script-keystone phases | 4 | **5** | **+1** (Phase 20-24) |

### Surviving follow-ons

**Class B — ~9 items in 9 categories** (Phase-23 Class B
unchanged minus 2 B.1 items closed Phase 24 + 2 Phase-24 B.8
newly surfaced):

- **B.1 Image-processing expansion (2 carried; 2 CLOSED Phase
  24)**: Q68 expansion content moderation; cropping UI; AVIF
  format conversion. (~~Server-side resizing~~ + ~~WebP format
  conversion (WebP half)~~ CLOSED.)
- **B.2 Phase-17 markdown subset expansion (8 carried)**.
- **B.3 Phase-18 surface expansion (3 + Q72 carried)**.
- **B.4 Phase-20 backfill operational follow-ons (3 carried)**.
- **B.5 Phase-21 cleanup operational follow-ons (2 carried)**.
- **B.6 Phase-22 scaffold operational follow-ons (2 carried)**.
- **B.7 Phase-23 scaffold operational follow-ons (1 carried)**.
- **B.8 Phase-24 transcode operational follow-ons (2 newly
  surfaced)**: `--user-id` flag for `backfill-resize-webp`
  (~0.5 units); AVATAR_SIZE / WEBP_QUALITY tuning (~0.5 units).
- **B.9 Operational + infrastructure (9; +1 from Phase 23 for
  `pnpm backfill-resize-webp` operational doc)**.

**Class C** — pre-existing carryovers unchanged.

### Phase-25+ Q-candidates flagged

**7 candidates carry** (unchanged from Phase 23 + Phase 24):

- Q60 (curator authz evolution).
- Q61 (submitter anonymity).
- Q62 (rejection-rationale public visibility).
- Q64 (per-user privacy opt-out; ADR-0015 D-D).
- Q65 (per-curator activity feed; ADR-0015 D-E).
- Q68 expansion (content moderation; ADR-0019 D-F row 1 of
  example pipeline; Phase-25+ ADR-0020 candidate).
- Q72 (`reviewNotesSchema` divergence).

### Phase 25+ entry conditions

Per §12 cardinal rule. **Twentieth "Continue" override
opportunity** in Unit 25.0 prep. Phase 25+ thread options (all
inferred-not-§13):

- **A. Q68 expansion content moderation** — ADR-0020 candidate;
  could be **sixth consecutive operational-script-keystone
  phase** if scope = moderation API + retroactive backfill
  scan. ADR-0019 D-F row 1 of example pipeline. ~3-5 units.
- **B. Cropping UI** (per ADR-0019 D-F row 2) — ~3-5 units;
  client dep.
- **C-other. AVIF format conversion** (per ADR-0019 D-F row 4
  half) — Phase 24 shipped WebP only; AVIF support adds ~5-10%
  storage savings over WebP on modern decoders. ~1-2 units;
  could be **sixth consecutive operational-script-keystone**
  if scope = forward+backfill. Phase 25+ if storage cost
  signal demands.
- **D. Q72 markdown subset extensions** — ~2-3 units.
- **E. Per-challenge detail page** — ~2-3 units; may not need
  new ADR.
- **F. Markdown rating-action `rationale`** — ~2-3 units;
  couples to (E).
- **G. Q64 per-user privacy opt-out** — ~3 units; breaks
  8-consecutive 0-migration streak.
- **H. Multi-provider OAuth expansion** — Phase-9 Class B item
  8 carried **14 phases**. **ADR-0020 candidate**. ~3-4 units.
  Strongest architectural patience signal at right scope.
  Pre-emptive for non-existent user base given Q54/Q55/Q69
  operational gates still pending.
- **I. Subscriber-list email** — Phase-5 D-4 punt carried
  **18+ phases**. ~6 units. Strongest overall patience signal.
- **K-other. Phase-22+23 scaffold operational follow-ons
  bundle** (methodology-version dynamic read + batch-emit-
  multiple + `--no-prior-fill`; ~1.5 units combined) +
  Phase-24 B.8 items (`--user-id` for backfill-resize-webp +
  AVATAR_SIZE/WEBP_QUALITY tuning; ~1 unit combined) — total
  ~2.5 units bundled.
- **L. Account-deletion blob cleanup** — Phase-16 carryover;
  conditional on Phase 25+ shipping account-delete path.
- **M. SiteHeader avatar pill** — Phase-14 Class B; UI minor.

**DB-migration trigger re-eval at Phase 25 kickoff**: trigger (a)
FIRED Unit 9.6; trigger (b) cold. Phase 25 migration count
depends on chosen thread.

### Risk surface at HEAD `f13e738`

1. **Three operational gates** (Q54 / Q55 / Q69) pending
   curator.
2. **`AVATAR_SIZE` + `WEBP_QUALITY` are global constants** —
   any change to these affects all future uploads + requires
   a new backfill cycle to re-render existing avatars. Phase 25+
   tuning surfaces this risk.
3. **WebP-only output post-Phase-24** — no AVIF fallback;
   universal browser support but locks the format choice.
   Phase 25+ AVIF support if storage cost signal.
4. **Backfill script live-tested only against operator dry-run
   inspection** — Phase-24 unit tests cover forward-path sharp
   pipeline shape; backfill script depends on live infrastructure
   (Phase 20+21 stance carried).
5. **No content moderation on uploaded images** (Q68 expansion
   deferred; ADR-0019 D-F row 1 of example pipeline).
6. **No live-preview in edit form** (Phase 17 + 18 deferral).

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token (Q69), NOT FR content backfill
(Q51 curator-track), NOT moderation / cropping / AVIF (Phase
25+), NOT live curator workflow run (curator operational task;
NOT a code-shipping unit), NOT destructive cleanup. This unit
is the **closure**, not the **unblock**.

### Parallel-curator awareness

Phase 24 shipped as a **single-session phase** — primary
shipped all 5 units 24.0 → 24.4 in one coherent session.
**Eight consecutive single-session phases** (Phase 17-24).
Phase 16 remains the lone parallel-session phase.

### Five consecutive operational-script-keystone phases pattern

| Property | P20 | P21 | P22 | P23 | P24 |
|---|---|---|---|---|---|
| Units | 5 | 5 | 5 | 5 | 5 |
| Code units | 1 | 1 | 1 | 1 | 1 |
| New ADRs | 0 | 0 | 0 | 0 | 0 |
| New deps | 0 | 0 | 0 | 0 | 0 |
| New env vars | 0 | 0 | 0 | 0 | 0 |
| New vitest tests | 0 | 0 | +11 | +10 | **0 net** |
| New test files | 0 | 0 | +1 | 0 | 0 |
| New `scripts/` files | 1 | 1 | 1 | 0 | 1 |
| New `lib/` modules | 0 | 0 | +1 | 0 | 0 |
| Dead-code removed | 0 | 0 | 0 | 0 | **+1 (`inferExt`)** |
| Closes carryover | ADR-0019 D-E | Phase-16+20 (class-B) | ADR-0014 D-D | Phase-22 B.6 1+2 | **ADR-0019 D-F (resize+WebP)** |
| Carryover phase-count | 1 | 6 | 10 | 1 | 5 |
| User-value-at-deploy | privacy backfill | storage hygiene | curator scaffolder | scaffolder auto-fill | **bandwidth + storage + render** |

**Pattern stable at 5 phases**. Phase 24 contributes three new
dimensions to the pattern: **0 net test Δ** (alongside Phase
20+21's "0 new tests" — vs Phase 22+23's "+10/+11 new");
**dead-code removal** (refactoring opportunity inside an
operational-script-keystone phase); and the **first concrete
user-value-at-deploy** (extends the pattern beyond curator-
workflow tooling).

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/24.4-phase-24-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 24.4 entry.

### CHANGELOG entry shape

Unit 24.4 entry: declares Phase 24 closed (**fifteenth NON-§13
phase**; 5 units; 0 deferrals; 0 scope drift; thirteenth
consecutive well-scoped phase); cites **ADR-0019 D-F resize +
WebP realization** via Unit 24.1; **first bandwidth-optimization
surface**; **fifth consecutive operational-script-keystone
phase**; **first concrete user-value-at-deploy** in the
operational-script-keystone streak; **sixth phase since Phase 3
to close without new ADR** = **first five-phase no-new-ADR
streak**; 12 Phase-24 architectural firsts; Phase 23 → 24
delta table (0 ADR Δ; 0 migration Δ; 0 runtime dep Δ; 0 env
var Δ; 0 net test Δ = first operational-script-keystone phase
with no net test Δ; 0 bundle Δ; 0 `lib/` file Δ — in-place
edit; +1 `scripts/` file; +1 server-side image processing
surface; +1 retroactive-privacy-correction surface; +1
bandwidth-optimization surface; +1 operational-script-keystone
phase counter); Class B follow-ons (~9 items in 9 categories;
B.1 server-resizing + WebP-half CLOSED + 2 B.8 newly surfaced);
7 Phase-25+ Q-candidates carry unchanged; ADR-0020 candidate
slot open after **11-phase ADR cycle without multi-provider-
OAuth claim** (Phase-9 Class B item 8 carried **14 phases**);
Phase 25+ thread options (strongest patience signals: Phase-9
Class B item 8 = 14 phases multi-provider OAuth; Phase-5 D-4
punt = 18+ phases subscriber-list email); twentieth "Continue"
override opportunity per §12; eight consecutive single-session
phases; eighth consecutive 0-migration phase; five consecutive
operational-script-keystone phases pattern table shows Phase
24 contributes 0-net-test-Δ + dead-code-removal +
user-value-at-deploy as new dimensions.
