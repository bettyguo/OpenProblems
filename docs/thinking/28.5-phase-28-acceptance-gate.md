# 28.5 — Phase 28 acceptance gate (multi-provider OAuth realized via ADR-0020; lifts ADR-0012 D-B; 20 ADRs; first new ADR since Phase 19; closes Phase-9 Class B item 8 at 17-phase carryover; eight-phase no-new-ADR streak ends; 6-unit shape matches Phase 19's new-ADR rhythm)

Sixth and final Phase-28 unit; docs-only. Closes Phase 28
(**nineteenth NON-§13 phase**; multi-provider OAuth expansion
realized via new ADR-0020 + Google provider integration in
`lib/auth/` + per-provider sign-in UI in `<AuthControl>` +
Q73 operational gate + Q74 architectural candidate). Mirrors
Phase-12 through Phase-27 acceptance-gate patterns; first
**6-unit** acceptance gate since Phase 19 (which was the
previous new-ADR phase shape).

> **§12 sign-off**: Phase 29 sign-off **pending** at end of unit.
> "Continue" override path available (**twenty-fourth invocation**
> opportunity).

## THINK

### Phase 28 ledger

**6 units shipped** (28.0 prep → 28.5 gate). **0 deferrals**;
**0 scope drift**. **Seventeenth consecutive well-scoped phase**.
**Breaks the 8-phase 5-unit streak** (Phase 20-27 = 8 phases at 5
units each) cleanly via the new-ADR shape; **first 6-unit phase
since Phase 19** (the prior new-ADR phase = same 6-unit shape).

| Unit | Title | Type | Status |
|---|---|---|---|
| 28.0 | Phase 28 prep | docs | shipped `4591e5a` |
| 28.1 | ADR-0020 multi-provider OAuth (docs-only) | docs | shipped `4720ffd` |
| 28.2 | Google provider + per-provider sign-in UI + i18n + 11 tests | code | shipped `6219ccc` |
| 28.3 | Phase-28 hygiene status pass | docs | shipped `dc69763` |
| 28.4 | ADR-0012 D-B partial-supersession + Q73 + Q74 | docs | shipped `c8db79a` |
| 28.5 | **Phase 28 acceptance gate** (this unit) | gate | this commit |

### Phase-28 closure summary

**Phase-9 Class B item 8 CLOSED** at **17-phase carryover** — the
**strongest non-multi-phase architectural patience signal** in
project history at Phase 28 entry. Closed via:

- **ADR-0020 multi-provider OAuth** (Unit 28.1; 240 lines): six
  D-clauses pin multi-provider runtime contract (D-A); Google as
  second provider via `next-auth/providers/google` (D-B); Auth.js
  v5 canonical env-var convention `AUTH_GOOGLE_ID` +
  `AUTH_GOOGLE_SECRET` (D-C); curator-of-record gate stays
  GitHub-only — Q74 surfaces non-GitHub-curator-eligibility for
  Phase 29+ (D-D); Auth.js default `allowDangerousEmailAccountLinking:
  false` — same-email-different-provider yields two `users.id`
  rows (D-E); per-provider sign-in UX buttons stacked mobile +
  `sm:` side-by-side, no chooser modal (D-F).
- **`lib/auth/index.ts` extension** (Unit 28.2): `Google` import +
  `providers: [GitHub, Google]` array; `extractGithubLogin` helper
  delegation factored out to new pure-function module.
- **`lib/auth/link-account.ts`** (Unit 28.2; 73 lines NEW): pure
  helpers `PROVIDER_IDS = ["github", "google"]` constant +
  `extractGithubLogin(account, profile, user)` encoding ADR-0020
  D-D's GitHub-only curator-of-record gate. **First pure-function-
  extraction from `lib/auth/index.ts`** (Phase-22+23 `scaffold.ts`
  precedent applied to a Phase-9 surface).
- **`lib/auth/link-account.test.ts`** (Unit 28.2; 11 tests NEW):
  3 `PROVIDER_IDS` regression guards (length, ordering,
  ProviderId union) + 8 `extractGithubLogin` cases (happy path
  for github; null for google + unknown future provider + missing
  profile.login + non-string login + null profile + non-object
  profile + missing user.id). **First new test file in `lib/auth/`
  since Phase-12 Unit 12.3** = 16-phase gap.
- **`components/auth-control/index.tsx`** (Unit 28.2; 122 lines
  was 95): signed-out branch renders `PROVIDER_IDS.map(...)` →
  one `<ProviderSignInForm>` per provider; per-provider i18n
  labels via `providerLabel(t, provider)` switch; stacked mobile
  + `sm:` side-by-side layout. **First multi-button signed-out UI
  in `<AuthControl>`**. Signed-in branch unchanged (sign-out is
  provider-agnostic per Auth.js).
- **`.env.example`** (Unit 28.2): Phase-28 Auth multi-provider
  block added; `AUTH_GOOGLE_ID` + `AUTH_GOOGLE_SECRET`. Env count
  **7 → 9** (first env-var-count change since Phase 16 Q69 = 12
  phases gap).
- **i18n keys** (Unit 28.2): `auth.sign_in_with_github` +
  `auth.sign_in_with_google` in en.json + fr.json. **140 → 142
  per locale** (first `auth` namespace addition since Phase 9 Unit
  9.5 = 16-phase gap).
- **ADR-0012 D-B partial-supersession note** (Unit 28.4): APPEND-
  not-EDIT phase-tagged note appended documenting Phase-28's
  partial supersession via ADR-0020. **First time an ADR D-clause
  is partially superseded by a follow-on ADR** rather than amended
  via own-D-clause implementation note. ADR-0012 D-A/C/D/E remain
  authoritative; only D-B's exclusivity claim is lifted.
- **OPEN_QUESTIONS Q73 + Q74** (Unit 28.4): Q73 operational
  (Google OAuth app registration; mirrors Q54 shape); Q74
  architectural (non-GitHub-users-as-curators; Phase 29+ if
  curator demand widens; three resolution options documented).
  **First Q-tally change since Phase 19** (Q70 promotion). Q-tally:
  26 resolved / 4 lean / 29 open → 26 / 4 / **31** = **61 total**
  (+2 net). **Eight-phase Q-tally freeze ends** (Phase 20-27 =
  8 phases).

**First architectural patience-signal phase since Phase 19**
(10 phases since the last patience-signal closure — Phase 19's
Q70 EXIF stripping; Phase 20-27 closed operational + UX follow-
ons). **First new ADR since Phase 19** = **eight-phase no-new-ADR
streak ends** (Phase 18 + 20-27).

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`c8db79a`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **616/616 across 56 vitest files**. +11 / +1
  Δ from Phase 27 close (605/55).
- **`pnpm build`** → ~659 prerendered pages + 8 dynamic page+API
  routes UNCHANGED (Google sign-in reuses `/api/auth/[...nextauth]`).
  **First Load JS shared chunk = 103 kB UNCHANGED** end-to-end
  through every Phase 9-28 unit. **Middleware bundle = 160 kB
  UNCHANGED** since Phase 12. `/profile` = 117 kB; `/u/{handle}` /
  `/u/{handle}/challenges` / `/u/{handle}/challenges/{id}` /
  `/problems/{slug}/challenges` = 108 kB each — all UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32 baseline
  since Phase 2; unchanged through every Phase 3-28 unit).
- **`pnpm lint`** clean.

### Architectural firsts shipped in Phase 28

1. **First new ADR since Phase 19** — ADR-0019 was the last new
   ADR; Phase 28 adds ADR-0020. 10-phase no-new-ADR gap closes.
2. **First architectural patience-signal phase since Phase 19** —
   Phase 20-27 closed operational + UX follow-ons; Phase 28
   returns to architectural-commitment shape.
3. **First time an ADR D-clause is partially superseded by a
   follow-on ADR** rather than amended via own-D-clause
   implementation note (ADR-0020 supersedes ADR-0012 D-B; ADR-0012
   D-A/C/D/E preserved). Establishes the convention for future
   inter-ADR supersession-without-full-replacement.
4. **Eight-phase no-new-ADR streak ends** (Phase 18 + 20-27 = 8
   phases). Prior longest streak = 5 phases at Phase 23 close.
5. **Eight-phase Q-tally freeze ends** (Phase 20-27 = 8 phases).
   Phase 28 = +Q73 + +Q74 = +2 open.
6. **First multi-provider state in project history** — Phase 9-27
   shipped GitHub-only; Phase 28 = GitHub + Google. ADR-0012 D-B
   single-provider restriction lifted.
7. **First env-var-count change since Phase 16** (Q69
   `BLOB_READ_WRITE_TOKEN` added Phase 16; 12 phases gap). Phase
   28: 7 → 9.
8. **First pure-function-extraction from `lib/auth/index.ts`** —
   `link-account.ts` extracts the `linkAccount` event helper +
   `PROVIDER_IDS` constant. Mirrors Phase-22+23 `scaffold.ts`
   pure-function-extraction precedent applied to a Phase-9
   surface.
9. **First multi-button signed-out UI in `<AuthControl>`** —
   Phase-9 single-button shape preserved through 18 NON-§13
   phases; Phase 28 introduces per-provider buttons.
10. **First new test file in `lib/auth/` since Phase-12** — 16
    phases gap. `link-account.test.ts` joins `curator.test.ts`
    as a pure-function-helper test sibling.
11. **First `auth` namespace i18n-key addition since Phase 9** —
    16 phases unchanged; +2 keys (`sign_in_with_github` +
    `sign_in_with_google`).
12. **`PROVIDER_IDS` constant** establishes single-source-of-
    truth for provider count + ordering. Sign-in UI iterates the
    constant directly; ADR-0020 D-B/D-F invariants enforced
    through the data structure rather than parallel
    documentation.
13. **First 6-unit acceptance gate since Phase 19** — Phase 20-27
    all shipped at 5 units; Phase 28's new-ADR shape matches
    Phase 19's 6-unit rhythm verbatim.
14. **Twelfth consecutive 0-migration phase** (Phase 17-28; 15
    of 19 phases since DB landed are 0-migration).
15. **Twelfth consecutive single-session phase** (Phase 17-28).
16. **Phase 28 closes the strongest non-multi-phase architectural
    patience signal** — Phase-9 Class B item 8 17-phase
    carryover; closure shifts the patience-signal hierarchy:
    Phase-5 D-4 subscriber-list email (21+ phases) is now the
    sole strongest patience signal at Phase 29 entry.

### Delta summary (Phase 27 → Phase 28)

| Metric | Phase 27 close (`a34a38d`) | Phase 28 close (HEAD `c8db79a`) | Δ |
|---|---|---|---|
| Commits | 5 | 5 + this gate | +6 |
| ADRs | **19** | **20** | **+1** (**first new ADR since Phase 19**; **eight-phase no-new-ADR streak ends**) |
| Migrations | 6 | **6** | **0** (**twelfth consecutive 0-migration phase**) |
| Runtime deps | (Phase-27 stack) | (Phase-27 stack) | 0 (`next-auth/providers/google` from existing `next-auth@5.0.0-beta.31`) |
| Env vars | 7 | **9** | **+2** (`AUTH_GOOGLE_ID` + `AUTH_GOOGLE_SECRET`; **first env-var-count change since Phase 16** = 12-phase gap) |
| Tests | 605 / 55 files | **616 / 56 files** | **+11 / +1** (**first new `lib/auth/` test file since Phase-12** = 16-phase gap) |
| First Load JS (shared) | 103 kB | **103 kB** | **0** (37th consecutive unit at 103 kB) |
| Middleware bundle | 160 kB | **160 kB** | **0** (since Phase 12) |
| All 4 affected page bundles | 117 / 108 / 108 / 108 kB | 117 / 108 / 108 / 108 kB | 0 |
| Dynamic page+API routes | 8 | 8 | 0 (Google sign-in reuses `/api/auth/[...nextauth]`) |
| OPEN_QUESTIONS resolved | 26 | **26** | **0** |
| OPEN_QUESTIONS open | 29 | **31** | **+2** (Q73 + Q74; **first Q-tally change since Phase 19**) |
| OPEN_QUESTIONS total | 59 | **61** | **+2** |
| i18n keys per locale | 140 | **142** | **+2** (**first `auth` namespace addition since Phase 9** = 16-phase gap) |
| `lib/auth/` files | 3 (`index.ts` + `curator.ts` + `curator.test.ts`) | **5** (+ `link-account.ts` + `link-account.test.ts`) | **+2** |
| OAuth providers configured | 1 (GitHub) | **2** (GitHub + Google) | **+1** (**first multi-provider state**) |
| `.env.example` blocks | 4 | **5** | **+1** |
| Patience-signal-closing phases since Phase 19 | 0 | **1** | **+1** (**first such phase since Phase 19** = 10-phase gap) |

### Surviving follow-ons

**Class B — ~5 items in 13 categories** (Phase-27 Class B unchanged
except Phase-9 item 8 closure under B.9; + 4 Phase-28 newly
surfaced under B.13):

- **B.1 Image-processing expansion** (3 carried unchanged): Q68
  expansion content moderation; cropping UI; AVIF format
  conversion.
- **B.2 Phase-17 markdown subset expansion** (8 carried).
- **B.3 Phase-18 surface expansion** (3 + Q72 + rationale-Q72-
  analogue carried).
- **B.4 Phase-20 backfill operational follow-ons** (3 carried).
- **B.5 Phase-21 cleanup operational follow-ons** (2 carried).
- **B.6 + B.7** (0 carried; closed Phase 25).
- **B.8 Phase-24 transcode operational follow-ons** (1 carried):
  AVATAR_SIZE / WEBP_QUALITY tuning.
- **B.9 Operational + infrastructure** (8 of 9 carried; **1 CLOSED
  Phase 28**: multi-provider OAuth expansion = Phase-9 Class B
  item 8 17-phase carryover). 8 carried: Q54/Q55/Q69 operational
  gates; CI dummy `AUTH_SECRET`; `pnpm db:migrate` doc; first LHCI
  run; middleware-based auth-route protection; per-script
  operational docs.
- **B.10 Phase-26 detail-page follow-ons** (2 carried):
  acceptedActionId link target; curator-translated FR review.
- **B.11 Operational + i18n maintenance** (1 carried; updated to
  142 per-locale Phase 28).
- **B.12 Phase-27 markdown-promotion follow-ons** (1 carried):
  rating-action `rationale` markdown promotion.
- **B.13 Phase-28 multi-provider follow-ons (NEWLY SURFACED;
  4 items)**: Q73 Google OAuth registration operational; Q74
  non-GitHub-users-as-curators architectural; account-merge UI
  for same-email cross-provider users (ADR-0020 D-E Phase 29+);
  third+ provider expansion (each requires ADR-0020 amendment or
  new ADR per D-B).

**Class C** — pre-existing carryovers unchanged.

### Phase-29+ Q-candidates flagged

**9 candidates carry** (7 carried from Phase 27 + 2 NEW Phase 28):

- Q60 (curator authz evolution).
- Q61 (submitter anonymity).
- Q62 (rejection-rationale public visibility).
- Q64 (per-user privacy opt-out; ADR-0015 D-D).
- Q65 (per-curator activity feed; ADR-0015 D-E).
- Q68 expansion (content moderation on uploaded images;
  ADR-0019 D-F integration point pre-documented).
- Q72 (`reviewNotesSchema` divergence; Phase-18 carryover).
- **Q73 (NEW Phase 28; operational)** — Google OAuth app
  registration.
- **Q74 (NEW Phase 28; architectural)** — non-GitHub users as
  curators.

### Phase 29+ entry conditions

Per §12 cardinal rule. **Twenty-fourth "Continue" override
opportunity** in Unit 29.0 prep. Phase 29+ thread options (all
inferred-not-§13):

- **A. Q68 expansion content moderation** — ADR-0021 candidate;
  ADR-0019 D-F integration point pre-documented. ~3-5 units.
- **B. Cropping UI** (per ADR-0019 D-F) — ~3-5 units; client dep
  regression risk.
- **C-other. AVIF format conversion** (per ADR-0019 D-F row 4
  half) — ~2-3 units; needs ADR-0019 amendment.
- **D. Q72 markdown subset extensions** — ~2-3 units.
- **F-other. Rating-action `rationale` markdown promotion** —
  Phase-27 B.12 carryover; couples to ADR-0018 D-G inheritance
  contract. ~2-3 units; would be 4th sibling under D-G.
- **G. Q64 per-user privacy opt-out** — ~3 units; breaks 12-
  consecutive 0-migration streak.
- **I. Subscriber-list email** — Phase-5 D-4 punt carried **21+
  phases** — **now the SINGLE strongest patience signal** in
  project history after Phase 28 closed Phase-9 Class B item 8.
  Multi-phase scope better as 2-3 phase rollout (foundation →
  templates → scheduler). ~6 units in foundation alone.
- **J-other. Account-merge UI** (ADR-0020 D-E Phase 29+
  candidate) — ~2-3 units; conditional on observed cross-provider
  user confusion.
- **K-other. Third+ provider expansion** (ADR-0020 D-B Phase 29+
  amendment) — GitLab / email-link / passkeys candidates.
  ~3-4 units; requires new ADR or ADR-0020 amendment.
- **L. Account-deletion blob cleanup** — Phase-16 carryover;
  conditional on Phase 29+ shipping account-delete path.
- **M. SiteHeader avatar pill** — Phase-14 Class B; UI minor.
- **N. v2 methodology authoring** — curator-track.

**DB-migration trigger re-eval at Phase 29 kickoff**: trigger (a)
FIRED Unit 9.6 carried; trigger (b) cold. Phase 29 migration
count depends on chosen thread (G breaks the 12-consecutive
0-migration streak; all other threads preserve it).

### Risk surface at HEAD `c8db79a`

1. **Three+ operational gates pending** (Q54 / Q55 / Q69 + new
   Q73). All curator-track.
2. **Google sign-in untested live** — Phase 28 ships the code
   ready to consume `AUTH_GOOGLE_ID` + `AUTH_GOOGLE_SECRET`;
   end-to-end live smoke deferred to Q73 closure.
3. **Same-email cross-provider users get two `users.id` rows**
   per ADR-0020 D-E (Auth.js default conservatism). If curator
   observes confusion in production, account-merge UI is the
   Phase 29+ unblock candidate.
4. **Non-GitHub users blocked from curator-of-record** per
   ADR-0020 D-D. Q74 surfaces this for Phase 29+ resolution.
5. **`next-auth@5.0.0-beta.31` exact-version pin** carries from
   Phase 9 (per ADR-0012 D-A beta-tag convention). Phase 29+ may
   bump to `^5.x` after Auth.js v5 GA.

### Boundary statement

NOT the GitHub OAuth app registration (Q54), NOT Turso DB
provisioning (Q55), NOT Vercel Blob token (Q69), NOT Google
OAuth app registration (Q73 — Phase 28+ operational), NOT FR
content backfill (Q51 curator-track), NOT account-merge UI (Phase
29+; ADR-0020 D-E candidate), NOT third+ provider expansion
(Phase 29+; ADR-0020 D-B amendment candidate), NOT non-GitHub-
curator widening (Q74; Phase 29+ architectural). This unit is
the **closure**, not the **unblock**.

### Parallel-curator awareness

Phase 28 shipped as a **single-session phase** — primary shipped
all 6 units 28.0 → 28.5 in one coherent session. **Twelve
consecutive single-session phases** (Phase 17-28). Phase 16
remains the lone parallel-session phase. Working tree clean at
each unit boundary; no parallel-session activity observed during
Phase 28 work.

### Phase 28 = new-ADR rhythm + 6-unit shape table (vs prior new-ADR phases)

| Property | Phase 12 (ADR-0014) | Phase 14 (ADR-0015) | Phase 15 (ADR-0016) | Phase 16 (ADR-0017) | Phase 17 (ADR-0018) | Phase 19 (ADR-0019) | **Phase 28 (ADR-0020)** |
|---|---|---|---|---|---|---|---|
| Units | 9 | 9 | 9 | 9 | 8 | 6 | **6** |
| Code units | 4 | 4 | 4 | 4 | 4 | 1 | **1** |
| New ADRs | 1 | 1 | 1 | 1 | 1 | 1 | **1** |
| Migrations added | 1 | 0 | 1 | 1 | 0 | 0 | **0** |
| Schema-altering | yes | no | yes | yes | no | no | **no** |
| Closes Q-candidate | Q57 | Q58 lean #3 | Q63 | Q67 | Q66 | Q70 | **Phase-9 Class B item 8 (not a Q)** |
| Adds Q-candidates | 0 | Q63/64/65 | Q66/67/68 | Q69/70 | Q71/72 | 0 | **Q73/74** |
| Single-session | yes | yes | yes | no (parallel) | yes | yes | **yes** |
| User-facing | yes (curator UI) | yes (`/u/{handle}`) | yes (`/profile`) | yes (avatar upload) | yes (bio markdown) | yes (EXIF) | **yes (multi-provider sign-in)** |

**Phase 28 = the lightest new-ADR phase by unit count tied with
Phase 19**, both at 6 units. Each new-ADR phase has shipped 1
new ADR; Phase 19 + Phase 28 share the no-migration shape; Phase
28 alone adds Q-candidates WITHOUT a primary Q-promotion (Q73 +
Q74 are NEW Qs, not promotions of pre-existing candidates).

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline unchanged).
- File added: `docs/thinking/28.5-phase-28-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 28.5 entry.

### CHANGELOG entry shape

Unit 28.5 entry: declares Phase 28 closed (**nineteenth NON-§13
phase**; 6 units; 0 deferrals; 0 scope drift; seventeenth
consecutive well-scoped phase); cites **multi-provider OAuth
expansion via ADR-0020 closing Phase-9 Class B item 8 17-phase
carryover** = strongest non-multi-phase architectural patience
signal at entry; **first new ADR since Phase 19** = **10-phase
no-new-ADR gap closes** + **first architectural patience-signal
phase since Phase 19**; **first time an ADR D-clause is
partially superseded by a follow-on ADR** (ADR-0020 supersedes
ADR-0012 D-B; ADR-0012 D-A/C/D/E preserved); **eight-phase
no-new-ADR streak ends** (Phase 18 + 20-27); **eight-phase
Q-tally freeze ends** (Phase 20-27; Phase 28 = +Q73 + +Q74 = +2
open); 16 Phase-28 architectural firsts; Phase 27 → 28 delta
table (+1 ADR Δ; 0 migration Δ; 0 runtime dep Δ; +2 env vars Δ;
+11 tests / +1 test file Δ; 0 bundle Δ; +2 i18n keys per locale;
+2 `lib/auth/` source files; +1 OAuth provider; +1 `.env.example`
block; +1 patience-signal-closing phase since Phase 19); Class B
follow-ons (~5 items in 13 categories; **B.9 1 of 9 CLOSED
Phase 28** = multi-provider OAuth expansion; **B.13 4 NEW Phase
28 surfaced** = Q73 + Q74 + account-merge UI + third+ provider
expansion); 9 Phase-29+ Q-candidates (7 carried from Phase 27 +
Q73 + Q74 new Phase 28); ADR-0021 candidate slot opens (Phase 28
claimed ADR-0020); strongest remaining patience signals
post-Phase-28: **Phase-5 D-4 subscriber-list email** (21+ phases
— **now the SINGLE strongest patience signal** in project
history after Phase 28 closed Phase-9 Class B item 8); 6-unit
phase shape matches Phase 19's new-ADR rhythm (the lightest
new-ADR phase tied with Phase 19); **twenty-fourth "Continue"
override opportunity** per §12; twelve consecutive single-session
phases; twelfth consecutive 0-migration phase; new-ADR-rhythm
retrospective table shows Phase 28 = tied-lightest new-ADR phase
(6 units, 1 code unit, 1 new ADR, 0 migrations) with Phase 19,
both single-session, both user-facing.
