# 65.4 — Phase 65 acceptance gate

Phase 65 closes at HEAD `f1a226f` (Unit 65.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-66-entry
conditions per §12.

## Phase 65 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 65.0 | Phase 65 prep (rank-1 `<a class="wikilink">` styling via plugin-only emit pattern; D-1 framework decision: plugin-only emit chosen — preserves APPEND-D-C + APPEND-D-R contracts) | docs | `71fedde` |
| 65.1 | `rehypeResolveWikilinks` plugin-body className emission (emit `className: ["wikilink"]` on every resolved `<a>` element; HAST array form; rehype-stringify renders as `class="wikilink"`; plugin-only emit pattern bypasses sanitize allow-list via APPEND-D-D plugin-order-after-default discipline) + 7 NEW Phase-65 className tests + 44 existing `wikilinks.test.ts` assertion updates + 88 existing `index.test.ts` assertion updates + ADR-0018 D-G APPEND-D-AW | code+APPEND | `b9c7efa` |
| 65.2 | 5 NEW Phase-65 end-to-end className integration tests in `lib/markdown/index.test.ts` (end-to-end through full sanitize pipeline + cross-entity arm composition + external-markdown-link UNCLASSED negative-scope + 7-way default composite cross-surface + default-registry no-className regression guard) + `CROSS_ENTITY_BUILD_HREF` import | code | `bd573d7` |
| 65.3 | Phase-65 hygiene + ADR-0018 APPEND-D-L item 4 closure documentation (27-phase carryover — NEW LONGEST ABSOLUTE; first post-tie absolute-record extension; D-L becomes first D-clause with 5-of-6 enumerated items closed; first D-clause with 5+ items closed) | docs | `f1a226f` |
| 65.4 | Phase 65 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-64 5-unit framework shape
verbatim.

## First non-regex plugin-body realization + first 8-realization for plugin-body axis + first state where plugin-body axis exceeds registry-state axis + first state where a consumer has 5+ evolutions + first D-clause with 5+ items closed + first post-tie absolute-record extension + first 5-consecutive-phase set-or-extend streak + 24-phase APPEND-deferral closure cadence + plugin-only emit pattern established as recognized framework technique

- **Thread choice**: Phase-64 acceptance gate's Phase-65-entry table
  ranked **`<a class="wikilink">` styling** as rank 1 (would advance
  D-L from 4-of-6 → 5-of-6; the framework decision was "plugin-only
  emit pattern vs multi-source schemaOverrides refactor"). Phase 65
  shipped rank 1 via the plugin-only emit pattern. Alternative
  framings considered and rejected per the Phase-65-prep doc D-1:
  (a) multi-source schemaOverrides refactor — would require new ADR
  APPEND violating APPEND-D-C; not autonomously tractable; (b) rank
  2 404 handling bundled into Phase 65 — would exceed canonical
  5-unit phase shape; defer to Phase 66+ rank 1.
- **`rehypeResolveWikilinks` plugin body evolution** in
  `lib/markdown/extensions/wikilinks.ts`:
  - Before (Phase 63 ship through Phase-64 close):
    `properties: { href: buildHref(slug, entityType) }`.
  - After (Phase 65 ship):
    `properties: { className: ["wikilink"], href: buildHref(slug, entityType) }`.
  - HAST uses array form for className per `property-information`
    convention; rehype-stringify renders as `class="wikilink"` at
    compile time.
- **Plugin-only emit pattern** (first realization): the className
  addition does NOT require a `schemaOverrides` change. Plugin runs
  AFTER `rehype-sanitize` per APPEND-D-D plugin-order-after-default
  discipline (lib/markdown/index.ts:213-215); class attribute enters
  the post-sanitize tree under explicit framework intent and
  bypasses the sanitize allow-list filter. Establishes the plugin-
  only emit pattern as the recognized framework technique for
  plugin-body output-shape extensions emitting attributes NOT in
  the base schema allow-list.
- **No factory arm change**: className inherited automatically by
  BOTH existing `wikilinks` and `wikilinks-cross-entity` arms.
  `MARKDOWN_EXTENSIONS` single-value arms UNCHANGED at 10. Phase 65
  establishes the convention "opt-in for SEMANTIC changes (Phase 63
  routing; Phase 64 schema content); default for VISUAL/ANNOTATIVE
  additions (Phase 65 className)."
- **External markdown links UNCHANGED**: standard markdown
  `[text](url)` continues to emit unclassed `<a href="url">text</a>`
  via `remark-rehype`'s default transformation. Only wikilinks-
  plugin-resolved anchors receive the className. This is the
  desired behavior — curators can style `a.wikilink` distinct from
  unclassed external `a` links.
- **7 NEW plugin-body tests in `wikilinks.test.ts`** (Unit 65.1;
  className behavior in plugin isolation) + **5 NEW integration
  tests in `index.test.ts`** (Unit 65.2; end-to-end through full
  sanitize pipeline + cross-entity + external-link discrimination +
  7-way composite + default-registry regression guard) + **44
  existing `wikilinks.test.ts` assertion updates + 88 existing
  `index.test.ts` assertion updates** to include `class="wikilink"`
  on wikilink-emitted anchors = **12 NEW Phase-65 tests + 132
  implicit assertions across existing tests**.
- ADR-0018 D-G APPEND adds Phase-65 EXTENDED block + **APPEND-D-AW**
  (Unit 65.1; twenty-third two-letter slot).
- **APPEND-D-L item 4 `<a class="wikilink">` styling CLOSED at
  27-phase carryover** (Phase 38 → 65). **NEW LONGEST ABSOLUTE
  APPEND-DEFERRAL CLOSURE EVER OBSERVED** (extends Phase-63/Phase-
  64 25-phase TIED record by 2 phases). **First post-tie absolute-
  record extension** in project history. Resumes the absolute-
  record-EXTENSION pattern after Phase-64's tie.
- **First non-regex plugin-body realization** in project history.
  All 7 prior plugin-body axis realizations are regex/extraction
  extensions evolving the plugin's INPUT pattern. Phase 65 is the
  FIRST plugin-body realization to evolve the plugin's OUTPUT shape
  (className emission). Establishes the OUTPUT-SHAPE extension as a
  recognized sub-pattern within the plugin-body axis.
- **First 8-realization for plugin-body axis** (extends Phase-60 7-
  realization record).
- **First state where plugin-body axis exceeds registry-state axis
  in realization count** in project history (plugin-body 8 +
  registry-state 7; first asymmetric state for the two original
  principal axes; plugin-body becomes the most-realized principal
  axis).
- **Wikilinks consumer 5th evolution** post-first-ship (Phase 42 +
  46 + 62 + 63 + 65). **First state where a consumer has 5+
  evolutions** in project history.
- **D-L becomes first D-clause with 5-of-6 enumerated items
  closed** (items 1 + 2 + 3 + 4 + 6 closed; only item 5 404
  handling remaining). **First D-clause with 5+ items closed** in
  project history.
- **First 5-consecutive-phase set-or-extend streak** in project
  history (Phase 61 new + 62 extend + 63 extend + 64 tie + 65
  extend).
- **24-phase APPEND-deferral closure cadence sustained** (Phase
  42-65) — new longest sustained cadence (extends Phase-64 record
  23 → 24). First 24-phase APPEND-deferral closure run.
- **Fifty-sixth NON-§13 phase** (extends Phase-64 milestone to 56);
  first 56-phase ledger-closure streak.

## Phase 64 → Phase 65 delta

| Metric | Phase 64 close | Phase 65 close | Δ |
|---|---|---|---|
| Tests | 1512 / 75 files | **1524 / 75 files** | +12 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 31 | **32** (project record extends; twenty-third two-letter slot D-AW in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| **`MARKDOWN_EXTENSIONS` single-value arms** | **10** | **10 UNCHANGED** | **0** (Phase 65 is a plugin-body output-shape extension; className inherited automatically by BOTH existing wikilinks + wikilinks-cross-entity arms; no new factory arm; convention "default for VISUAL/ANNOTATIVE additions" established) |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 21 | **21** | 0 |
| **Phase-37-framework concrete consumers** | **7** | **7** | 0 |
| **Plugin-regex-extension realizations** | **7** | **7** | 0 (Phase 65 is OUTPUT-shape, not regex/input-pattern extension) |
| **Plugin-body axis realizations** | **7** | **8 (+wikilinks className output-shape)** | **+1 (first 8-realization for plugin-body axis; first non-regex plugin-body realization; first state where plugin-body axis exceeds registry-state axis)** |
| **Constructor-arg-only zero-rework expansion realizations** | **7** | **7** | 0 |
| **Schema-extension realizations** | **2** | **2** | 0 |
| **Plugin-option-extension realizations** | **2** | **2** | 0 |
| **Schema-options-axis realizations** | **1** | **1** | 0 |
| **Dual-form regex realizations** | **5** | **5** | 0 |
| **Principal axes of zero-rework framework extension** | **4** | **4** | 0 (Phase 65 is plugin-body axis 8th realization; no new axis) |
| **Consumers with 3+ evolutions** | **3 (arxiv + tables + wikilinks)** | **3** | 0 (wikilinks evolution count moves from 4 → 5) |
| **Consumers with 4+ evolutions** | **2 (wikilinks + tables)** | **2** | 0 |
| **Consumers with 5+ evolutions** | **0** | **1 (wikilinks)** | **+1 (first state where a consumer has 5+ evolutions)** |
| **D-clauses with BOTH items closed** | **4** | **4** | 0 |
| **D-clauses with 3+ items closed** | **2 (D-Q + D-L)** | **2** | 0 |
| **D-clauses with 4+ items closed** | **2 (D-L + D-Q)** | **2** | 0 (D-L count moves from 4 → 5) |
| **D-clauses with 5+ items closed** | **0** | **1 (D-L)** | **+1 (first D-clause with 5+ items closed)** |
| **D-clauses with ALL enumerated items closed** | **1 (D-Q)** | **1** | 0 (D-L at 5-of-6; item 5 remaining) |
| **APPEND-deferral closure cadence (consecutive phases)** | 23 | **24 (Phase 42-65)** | **+1 (new record)** |
| **Longest absolute APPEND-deferral closure carryover** | 25-phase (Phase 63 + 64 TIED) | **27-phase (Phase 65 D-L item 4) — new record** | **+2 phases (first post-tie absolute-record extension in project history)** |
| **Set-or-tie/extend streak of absolute-record carryovers** | 4 (Phase 61 + 62 + 63 + 64) | **5 (Phase 61 + 62 + 63 + 64 + 65)** | **+1 (first 5-consecutive-phase set-or-extend streak)** |
| **Plugin-only emit pattern realizations** | **0** | **1 (Phase 65 wikilinks className)** | **+1 (first realization of plugin-only emit pattern as recognized framework technique)** |
| **Non-regex plugin-body realizations** | **0** | **1 (Phase 65 wikilinks className)** | **+1 (first non-regex plugin-body realization)** |
| **Refactor-only phases** | 1 (Phase 62) | **1** | 0 |
| **Plugin-option-ready-before-consumer-demand realizations** | 1 (Phase 62) | **1** | 0 |
| **Schema-options-ready-before-curator-demand realizations** | 1 (Phase 64) | **1** | 0 |
| **Forward-compat-affordance prerequisite-fulfillment arcs completed** | 1 (Phase 62 → 63) | **1** | 0 |
| **NON-§13 phase count** | 55 | **56** | **+1** |
| **"Continue" override invocations past half-century threshold** | **9** | **10 (+Phase 65 = 60th)** | **+1 (first decade-of-post-half-century-Continue milestone reached)** |

## Architectural firsts in Phase 65 (12 enumerated)

1. **First non-regex plugin-body realization** in project history.
   All 7 prior plugin-body axis realizations (Phase 46 wikilinks
   alias + Phase 47 arxiv alias + Phase 48 doi alias + Phase 51
   pubmed alias + Phase 53 arxiv legacy ID-class + Phase 55 orcid
   alias + Phase 60 biorxiv alias) are regex/extraction extensions
   evolving the plugin's INPUT pattern. Phase 65 is the FIRST
   plugin-body realization to evolve the plugin's OUTPUT shape
   (className emission). Establishes the OUTPUT-SHAPE extension as
   a recognized sub-pattern within the plugin-body axis. The
   plugin-body axis now has two recognized sub-patterns:
   - Input-regex sub-pattern (Phase 46-60; 7 realizations).
   - **Output-shape sub-pattern (Phase 65+; 1 realization).**
2. **First 8-realization for plugin-body axis** in project history
   (extends Phase-60 7-realization record). The plugin-body axis
   is the most-realized principal axis post-Phase-65; Phase 65
   establishes it at depth 8.
3. **First state where plugin-body axis exceeds registry-state
   axis in realization count** in project history. Pre-Phase-65:
   both axes at 7 realizations (registry-state from Phase 38+;
   plugin-body from Phase 46+). Post-Phase-65: plugin-body at 8 +
   registry-state at 7 — **first asymmetric state for the two
   original principal axes**. Plugin-body becomes the most-
   realized principal axis. Axis depth ranking post-Phase-65:
   - Plugin-body: **8 realizations**
   - Registry-state: 7 realizations
   - Plugin-option: 2 realizations
   - Schema-options: 1 realization
4. **Wikilinks consumer gains 5th evolution post-first-ship**
   (Phase 42 cross-surface + Phase 46 alias + Phase 62 plugin
   parameterization + Phase 63 cross-entity + Phase 65 className).
   **First state where a consumer has 5+ evolutions** in project
   history. Wikilinks remains the deepest-evolved consumer.
5. **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED**
   at **27-phase carryover** (Phase 38 → Phase 65). Extends
   Phase-63/Phase-64 25-phase TIED record by 2 phases. **First
   post-tie absolute-record extension** in project history.
   Resumes the absolute-record-EXTENSION pattern after Phase-64's
   tie. The absolute-record trajectory:
   - Phase 61 = 22-phase (new record).
   - Phase 62 = 24-phase (extend by 2).
   - Phase 63 = 25-phase (extend by 1).
   - Phase 64 = 25-phase (TIE; first tie).
   - **Phase 65 = 27-phase (extend by 2; first post-tie
     extension).**
6. **First 5-consecutive-phase set-or-extend streak** in project
   history. Extends Phase-64 4-consecutive-phase set-or-tie
   streak. Streak shape evolves from "set-or-tie" to
   "set-or-extend" (no new ties in the 5-phase run; Phase 64 was
   the only tie within the streak; Phase 65 extends after the
   tie).
7. **D-L becomes first D-clause with 5-of-6 enumerated items
   closed** within the cadence (items 1 + 2 + 3 + 4 + 6 closed;
   only item 5 404 handling remaining). **First D-clause with 5+
   items closed** in project history. Prior multi-item D-clauses:
   D-L at 4-of-6 (Phase 63); D-Q at 4-of-4 ALL-CLOSED (Phase 64;
   first ALL-CLOSED D-clause). Phase 66+ closure of item 5 would
   make D-L the second ALL-items-closed D-clause.
8. **Plugin-only emit pattern established as recognized framework
   technique**. Phase 65 ships the first realization; the pattern
   is now available for future plugin-body output-shape extensions
   that need to emit attributes NOT in the base schema allow-list.
   The pattern exploits the post-sanitize plugin-order discipline
   (APPEND-D-D); preserves APPEND-D-C / APPEND-D-R contracts; no
   schemaOverrides change required.
9. **24-phase APPEND-deferral closure cadence sustained** (Phase
   42-65) — new longest sustained cadence in project history
   (extends Phase-64 record 23 → 24). First 24-phase APPEND-
   deferral closure run.
10. **Thirty-second APPEND on ADR-0018 D-G** — extends the **first-
    ADR-D-clause-with-most-APPENDs record** from 31 → 32.
    **Twenty-third two-letter APPEND letter D-AW** (Excel-
    spreadsheet column convention sustained). First 32-APPEND
    D-clause state.
11. **First "framework + 7 consumers + 7 expansions + 7 plugin-
    regex-extensions + 1 plugin-body-output-shape + 2 schema-
    extensions + 2 plugin-option-extensions + 1 schema-options-
    extension + 10 `MARKDOWN_EXTENSIONS` single-value arms +
    D-clause-with-ALL-items-closed × 1 + D-clause-with-5+-items-
    closed × 1 + consumer-with-5+-evolutions × 1 + first-non-
    regex-plugin-body-realization × 1 + first-plugin-body-axis-
    exceeds-registry-state-axis × 1 + four-consecutive-absolute-
    record-set-or-tie × 1 + five-consecutive-absolute-record-set-
    or-extend × 1 + first-post-tie-absolute-record-extension × 1
    + plugin-only-emit-pattern × 1" 29-phase cluster** in project
    history (Phase 37-65). 29-phase architectural arc. Plus
    **35th consecutive phase without new B category** (Phase
    31-65; extends Phase-64 record 34 → 35). Plus **fifty-sixth
    NON-§13 phase** (extends Phase-64 milestone to 56). Plus
    **181st consecutive 103 kB First Load JS unit** (extends
    from 178 at Phase-64 close). Plus **30th consecutive no-new-
    ADR phase** (Phase 36-65; extends Phase-64 record 29 → 30;
    first 30-phase no-new-ADR streak).
12. **60th "Continue" override invocation** at Unit 65.0 —
    extends the **half-century-of-Continue-overrides milestone**
    (Phase 55 = 50th) to **60 over Phases 6-65**. **Tenth
    invocation past the half-century threshold** — confirms the
    §12-cardinal-rule sign-off discipline remains durable post-
    milestone across 11 successive phases (Phases 55-65). **First
    decade-of-post-half-century-Continue milestone** reached
    (Phases 55-65 = 11 phases for the 10 overrides past 50th;
    effective decade-of-Continue milestone).

## Phase 65 → Phase 66 entry conditions

Per §12, Phase 66 entry requires **explicit human sign-off**.

**Framework state note**: Phase 65 introduced the OUTPUT-SHAPE
sub-pattern within the plugin-body axis (first non-regex plugin-
body realization); the plugin-body axis now has 8 realizations
and is the most-realized principal axis. The plugin-only emit
pattern is established as a recognized framework technique. D-L
has 5 of 6 items closed; item 5 (404 handling for unresolved
wikilinks) is the only remaining D-L item — closure would make
D-L the second D-clause with ALL items closed in project history
(D-Q was first at Phase 64). Phase 66+ candidates can complete
D-L closure, introduce realizations along any existing axis, or
introduce new consumers.

**Recommended Phase 66 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| **1** | **404 handling for unresolved wikilinks** (APPEND-D-L item 5 — last remaining D-L deferral) — closure would make D-L the SECOND D-clause with ALL enumerated items closed in project history (D-Q was first at Phase 64) | 2-3 | Autonomous-tractable; introduces build-time validation step + render-time fallback strategy; requires content/* traversal to enumerate valid problem/paper/author/institution slugs |
| 2 | **Locale-prefixed cross-entity wikilink paths** — concrete `buildHref` variant producing `/{locale}/papers/${slug}` etc; **third plugin-option-axis realization** (Phase 62 affordance + Phase 63 registry consumer + Phase 66 second registry consumer) | 1-2 | Autonomous-tractable; small-scope follow-on to Phase-63 ship |
| 3 | **Curator-configurable className value** via plugin-option axis (`ResolveWikilinksOptions.className?: string`) — would generalize Phase-65 hardcoded `"wikilink"` to a configurable value; **third plugin-option-axis realization** OR **second non-regex plugin-body realization** depending on framing | 1-2 | Autonomous-tractable; XSS audit per CSS-class-injection vector |
| 4 | **Per-surface schema-options consumer beyond bio-restricted** — e.g., `reviewNotes-extended` variant with a hypothetical additional tag; **second schema-options-axis realization** (extends Phase 64 axis to 2 realizations; first 2-realization for the FOURTH axis) | 1-2 | Autonomous-tractable |
| 5 | **Multi-className emit** — e.g., `<a class="wikilink wikilink-paper">` for entity-type-specific styling variants; would be **second plugin-body output-shape realization** (extends Phase-65 sub-pattern to 2 realizations) | 1-2 | Autonomous-tractable; depends on Phase 66 rank 3 decision (configurable className first vs multi-className first) |
| 6 | **Empty-alias fallback unification** across consumers (Phase-48 deferral) | 1 | Smallest-scope autonomous-tractable |
| 7 | **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 8 | **Legacy numeric-only bioRxiv IDs** (pre-2019 format) — would be 8th plugin-regex-extension | 1-2 | Autonomous-tractable; small-scope |
| 9 | **OSF preprint consumer** — eighth concrete consumer | 2-3 | DOI overlap concern; introduces NEW consumer |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Most defensible autonomous-tractable next thread**: Rank 1
(**404 handling for unresolved wikilinks** — APPEND-D-L item 5)
— closure would make **D-L the SECOND D-clause with ALL
enumerated items closed** in project history (D-Q was first at
Phase 64). Establishes the "2 D-clauses with ALL items closed"
state. Rank 1 is the cleanest "architectural finishing move" for
D-L's multi-item cadence; rank 1 + rank 4 (per-surface schema-
options) together would advance the framework along 2 distinct
axes within the next few phases.

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1524 / 75 files** (+12 / 0 vs Phase 64 close).
- `pnpm audit-content` — 0 errors / 6 warnings (61 consecutive
  phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (182 consecutive units at
  Unit 65.4 ship; from 178 at Phase 64 close + 4 Phase-65 units).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 66+ deferrals)

NOT 404 handling for unresolved wikilinks (Phase 66+ rank 1;
build-time validation step);
NOT locale-prefixed cross-entity wikilink paths (Phase 66+ rank 2;
3rd plugin-option-axis realization);
NOT curator-configurable className value via plugin-option axis
(Phase 66+ rank 3);
NOT per-surface schema-options consumer beyond bio-restricted
(Phase 66+ rank 4; 2nd schema-options-axis realization);
NOT multi-className emit beyond `"wikilink"` (Phase 66+ rank 5;
2nd plugin-body output-shape realization);
NOT empty-alias fallback unification (Phase 66+ rank 6);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 66+ rank 7);
NOT legacy numeric-only bioRxiv IDs (Phase 66+ rank 8);
NOT OSF preprint consumer (Phase 66+ rank 9);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi
+ pubmed + orcid + biorxiv (Phase 66+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 66+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 66+;
requires framework refactor per the composite-registry single-
source rule);
NOT dx.doi.org legacy host (Phase 66+);
NOT stricter trailing-lookahead (Phase 66+);
NOT paper-card hover-preview (Phase 66+);
NOT auto-trim of alias display whitespace (Phase 66+);
NOT a future plugin that EMITS `<caption>` or
`colSpan`/`rowSpan`/`scope` (Phase 66+);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 66+);
NOT cross-entity entity-type expansion beyond paper / author /
institution (Phase 66+);
NOT build-time validation of captured entity-type against
content/papers / content/authors / content/institutions (Phase
66+);
NOT curator-facing builder configuration via DB or UI (Phase
66+);
NOT curator-facing per-surface schema configuration via DB or
UI (Phase 66+);
NOT 2nd plugin using the plugin-only emit pattern (Phase 66+;
first reuse of the Phase-65-established pattern);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-64 deferrals carry forward.

## Boundary statement

**Phase 65 CLOSED at HEAD `<this-unit>` after 5 numbered units
(65.0 prep + 65.1 `rehypeResolveWikilinks` plugin-body className
emission (emit `className: ["wikilink"]` on every resolved `<a>`
element via the plugin-only emit pattern; HAST array form per
property-information convention; rehype-stringify renders as
`class="wikilink"`) + 7 NEW Phase-65 className tests + 44
existing `wikilinks.test.ts` assertion updates + 88 existing
`index.test.ts` assertion updates + APPEND-D-AW + 65.2 5 NEW
Phase-65 end-to-end integration tests in `index.test.ts` + 65.3
hygiene + 65.4 gate). **First non-regex plugin-body realization**
in project history — all 7 prior plugin-body axis realizations
(Phase 46-60) are regex/extraction extensions evolving the
plugin's INPUT pattern; Phase 65 is the FIRST plugin-body
realization to evolve the plugin's OUTPUT shape (className
emission). Establishes the OUTPUT-SHAPE extension as a recognized
sub-pattern within the plugin-body axis. **First 8-realization
for plugin-body axis** (extends Phase-60 7-realization record).
**First state where plugin-body axis exceeds registry-state axis
in realization count** in project history (plugin-body 8 +
registry-state 7; first asymmetric state for the two original
principal axes; plugin-body becomes the most-realized principal
axis). **Plugin-only emit pattern** established as recognized
framework technique for plugin-body output-shape extensions
emitting attributes NOT in the base schema allow-list — exploits
the post-sanitize plugin-order discipline (APPEND-D-D); preserves
APPEND-D-C / APPEND-D-R contracts; no schemaOverrides change
required. **Wikilinks consumer gains 5th evolution post-first-
ship** (Phase 42 cross-surface + Phase 46 alias + Phase 62
plugin parameterization + Phase 63 cross-entity + Phase 65
className); **first state where a consumer has 5+ evolutions**
in project history. **Closes ADR-0018 APPEND-D-L item 4
`<a class="wikilink">` styling at 27-phase carryover** (Phase 38
→ Phase 65) — **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE
EVER OBSERVED** (extends Phase-63/Phase-64 25-phase TIED record
by 2 phases). **First post-tie absolute-record extension** in
project history; resumes the absolute-record-EXTENSION pattern
after Phase-64's tie. APPEND-D-L closure trajectory: item 1
cross-surface 4-phase (Phase 42); item 2 alias 8-phase (Phase
46); item 6 plugin parameterization 24-phase (Phase 62); item 3
cross-entity 25-phase (Phase 63); item 4 className styling 27-
phase (Phase 65); item 5 404 handling for unresolved wikilinks
still deferred (Phase 66+ rank 1 candidate; closure would make
D-L the SECOND D-clause with ALL enumerated items closed). **D-L
becomes first D-clause with 5-of-6 enumerated items closed**
(items 1 + 2 + 3 + 4 + 6 closed); **first D-clause with 5+ items
closed** in project history. **First 5-consecutive-phase set-or-
extend streak** in project history (Phase 61 new + 62 extend +
63 extend + 64 tie + 65 extend). **24-phase APPEND-deferral
closure cadence sustained** — new longest sustained cadence
(extends Phase-64 record 23 → 24); first 24-phase APPEND-deferral
closure run. **`MARKDOWN_EXTENSIONS` single-value arms UNCHANGED
at 10** — className inherited automatically by BOTH existing
`wikilinks` and `wikilinks-cross-entity` arms; no new factory
arm; Phase 65 establishes the convention "opt-in for SEMANTIC
changes (Phase 63 routing; Phase 64 schema content); default for
VISUAL/ANNOTATIVE additions (Phase 65 className)." External
markdown links UNCHANGED — only wikilinks-plugin-resolved
anchors receive the className. Composite registry composition
rules preserved — the 7-way default composite
`wikilinks,tables,arxiv,doi,pubmed,orcid,biorxiv` composes
conflict-free with the Phase-65 plugin-body className addition
(no schemaOverrides change in Phase 65; composite single-source
rule not triggered). XSS defenses preserved (className value is
hardcoded literal `"wikilink"`; no curator input; no opportunity
for attribute injection). 32nd APPEND on ADR-0018 D-G (project
record extends 31 → 32). Twenty-third two-letter APPEND letter
D-AW. Fifty-sixth NON-§13 phase (extends Phase-64 half-century
milestone to 56); first 56-phase ledger-closure streak. 35th
consecutive phase without new B category — first 35-phase run
(extends Phase-64 record 34 → 35). 30th consecutive no-new-ADR
phase (Phase 36-65; extends Phase-64 record 29 → 30; first 30-
phase no-new-ADR streak). 182nd consecutive 103 kB First Load JS
unit (extends from 178 at Phase-64 close). 60th "Continue"
override invocation — tenth invocation past the half-century
threshold (Phase 55 = 50th; …; Phase 65 = 60th); **first decade-
of-post-half-century-Continue milestone** reached (Phases 55-65
= 11 phases for the 10 overrides past 50th). 1524 tests across
75 vitest files (+12 / 0 vs Phase 64). 24 ADRs UNCHANGED. 7 DB
tables UNCHANGED. 9 migrations UNCHANGED. 14 env vars UNCHANGED.
168 i18n keys per locale UNCHANGED. First Load JS 103 kB
UNCHANGED. Middleware 160 kB UNCHANGED. OPEN_QUESTIONS top-level
Q-count 66 UNCHANGED. Phase-37+ candidate count 8 UNCHANGED.
`MARKDOWN_EXTENSIONS` recognized single-value arms 10 UNCHANGED.
0 new files (in-place wikilinks.ts plugin body className
addition; test additions in existing test files; bulk assertion
updates in 2 existing test files). Zero new ADRs / migrations /
i18n keys / operational gates / runtime deps. Phase 66 entry
requires explicit human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 64 → 65 delta complete.
- [x] Phase 66 entry conditions documented (10 ranked
      candidates).
- [x] Framework-state-note explicitly flagged (Phase 65
      introduced the OUTPUT-SHAPE sub-pattern within the plugin-
      body axis; plugin-body axis now has 8 realizations and is
      the most-realized principal axis; plugin-only emit pattern
      established as recognized framework technique).
- [x] Smoke gates pass.
- [x] STOP at Phase 66 boundary per §12.
