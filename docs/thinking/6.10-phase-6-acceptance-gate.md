# 6.10 — Phase 6 acceptance gate (Discussions thread)

Phase-6 closing unit. Mirrors Unit 1.12 / 2.13 / 3.13 / 4.13 / 5.13. Verifies every §13 Phase-6+ thread-deliverable that this phase pulled (the **Discussions** thread per Unit 6.0 D-1) is operational locally at HEAD, emits the cross-phase roll-up, and lists Phase-6 follow-ons that survive into Phase 7+.

## THINK

### §13 Phase-6+ scope clarification

`MASTER_PROMPT.md` §13 names three threads — Discussions, read+write API with token auth, bilingual rendering — without enumerated cross-phase contract. Unit 6.0 D-1 picked **Discussions** as the first thread; the other two remain available threads for future open-ended phases. This acceptance gate verifies the **Discussions** deliverable surface; auth + bilingual stay deferred.

### §13 Discussions thread — local pass status

| Surface                                                            | Phase-6 unit | Status |
|---------------------------------------------------------------------|-------------|--------|
| GitHub Discussions read-side metadata client (`lib/discussions/github-graphql.ts`) | Unit 6.2  | ✓ Operational; 21 tests; env-safe wrappers `tryGetDiscussionByPath` + `tryGetRecentDiscussionActivity`. |
| `/problems/[slug]/talk` SSG route shell (10 pages)                  | Unit 6.3  | ✓ All 10 SSG routes generated; breadcrumb + h1 + section landmark + noscript fallback + back-link. |
| Giscus iframe embed wrapper (`components/discussions/GiscusEmbed.tsx`) | Unit 6.4 | ✓ `"use client"`; hydration-safe placeholder; theme sync via `useTheme`; env-driven `repoId` with graceful "unavailable" fallback; 7 component tests. |
| Activity badge on problem cards + Discuss-link count upgrade        | Unit 6.5  | ✓ Server-side fetched at SSG; cards show "N comments" link when threads with comments exist; problem detail link upgrades to `(N)`. |
| Discussion threads in per-domain RSS digest                         | Unit 6.6  | ✓ `buildDigest` extended with `kind: "discussion"`; one repo-global fetch; title-regex parse against `TALK_PATHNAME_REGEX`; domain + window filtering; channel description mentions discussion threads when present. |
| `/talk` LHCI enrolment                                              | Unit 6.7  | ✓ One representative URL added to `lighthouserc.json` (14 URLs total). |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: `/problems/<slug>/talk` enrolled (Unit 6.7). The 5 per-domain RSS endpoints unchanged; `/digest` hub unchanged. Real LHCI run lands on the first PR with this commit.
- **W3C feed validator on the per-domain digest** (with discussion items now possible): deferred to first preview deploy + Q47 resolution — same Q27-class pattern as prior phases. Same applies to `/api/v1/rss.xml` from Phase 3.
- **Visual-regression baselines** for `/problems/<slug>/talk` × 2 themes × N viewports — deferred Phase-6 follow-on. `chromium-win32` baselines not captured this unit (no Playwright spec changes); a follow-on PR can run `playwright test --update-snapshots`.
- **No auto-merge** (ADR-0009): Phase 6 did NOT add any LLM-drafting paths. Discussions are community-authored via the Giscus iframe (auth-via-GitHub inside the iframe; we never see writes).
- **File-first / no DB** (ADR-0004): held. Build-time `.github-cache/<query-hash>.json` is a sidecar; no first-party storage of user identity or comment content.
- **First Load JS shared chunk = 103 kB**: held throughout Phase 6. Giscus wrapper adds ~2 kB to the `/talk` route chunk only (per ADR-0010 D-A bundle isolation).

### Phase-6 unit summary (10 units shipped — Discussions thread)

| Unit | Commit    | Title                                                                                            |
| ---- | --------- | ------------------------------------------------------------------------------------------------ |
| 6.0  | `ada448b` | Phase 6 prep (Discussions-first thread; 11-unit breakdown + DB-trigger 1.434% re-eval)            |
| 6.1  | `300cde3` | ADR-0010 — Discussions backend (Giscus embed + first-party GraphQL read-side)                     |
| 6.2  | `93230a1` | `lib/discussions/github-graphql.ts` (ADR-0010 D-B / D-D / D-E read-side client)                   |
| 6.3  | `f0dbff1` | `app/problems/[slug]/talk/page.tsx` (route shell + Giscus embed slot; +10 SSG routes)             |
| 6.4  | `f9a465e` | `components/discussions/GiscusEmbed.tsx` (iframe wrapper + theme sync)                            |
| 6.5  | `d7f17f7` | Problem-card activity badge + detail-page Discuss-link count upgrade                              |
| 6.6  | `0ac77e0` | `lib/digest/build-digest.ts` extension (Discussion threads in per-domain RSS)                     |
| 6.7  | `d2c635e` | `lighthouserc.json` enrols `/problems/<slug>/talk` (14 URLs)                                      |
| 6.8  | `e1be7e1` | Phase-6 hygiene status pass (deferrals to Phase 7+)                                               |
| 6.9  | `23b3ee3` | OPEN_QUESTIONS hygiene + ADR review (Phase 6 close)                                               |
| 6.10 | _this_    | Phase 6 acceptance gate                                                                           |

Total: **11 units** (6.0 – 6.10). Tighter than the 14-unit Phase 0-5 cadence — predicted by Unit 6.0 (Discussions thread is smaller-scope than prior phases' bundles).

### State at HEAD (Unit 6.10)

- 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors / 14 institutions / 20 rating actions / 4 issue templates / 1 methodology MDX / **2 contributing MDX** (v1.0 + v1.1) / 2 entries.json files (hallucination-reduction + long-horizon-agent-reliability). **Content unchanged this phase** — Phase 6 added code + scripts + docs, not content.
- **New Phase-6 surfaces**: 10 talk-page SSG routes (`/problems/<slug>/talk`); 1 first-party GitHub GraphQL read-side client (`lib/discussions/github-graphql.ts`); 1 Giscus iframe wrapper (`components/discussions/GiscusEmbed.tsx`); discussion-activity badge + count upgrade on problem cards and detail page; `kind: "discussion"` items in the per-domain RSS digests.
- **333 prerendered pages** (was 322 at Phase-4 close → 322 at Phase-5 close → 323 after Unit 5.13b /contributing v1.1.0 → 333 after Unit 6.3 +10 talk pages). Audit: 322 + 1 + 10 = 333 ✓.
- **First Load JS shared chunk = 103 kB UNCHANGED** through every Phase-6 unit. The Giscus wrapper bundles into the `/problems/[slug]/talk` route chunk only (~2 kB delta → 108 kB First Load for that route).
- **323/323 vitest tests across 38 files** (was 284/36 at Phase-5 close); **+39 tests this phase**: `github-graphql` (18 in Unit 6.2 + 3 from Unit 6.5 + 5 from Unit 6.6 = **26 total**) + `GiscusEmbed` (7) + `build-digest` (6). The exact breakdown matches the per-unit smoke counts: 18 + 7 + 3 + 11 = 39.
- `pnpm validate-content` → 203 files green.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32-expected baseline since Phase 2).
- `pnpm typecheck` clean. `pnpm build` clean compile.
- **5 visualizations live** (unchanged from Phase 4): RatingRadar, SaturationCurve, MoversBoard, RatingHistoryStream, DomainMap.
- **10 ADRs** (added ADR-0010 in Phase 6): 0001-0009 unchanged + **0010 (Discussions backend)** new.
- **2 new dependencies in Phase 6**: `@octokit/graphql@^9.0.3` (Unit 6.2), `@giscus/react@^3.1.0` (Unit 6.4). Both pure JS; no `pnpm-workspace.yaml` `allowBuilds` change needed.
- **`lighthouserc.json`** enrols **14 URLs** (was 13 at Phase-5 close; added `/problems/hallucination-reduction/talk` in Unit 6.7).
- **OPEN_QUESTIONS state**: 4 new Phase-6 questions surfaced (Q46-Q49). Q46 + Q49 **closed** (Q46 via ADR-0010 in Unit 6.1; Q49 via ADR-0010 D-F codified in Unit 6.9). Q47 **open operational** (repo Discussions enablement; not architectural). Q48 **partially-resolved** (link-half done in Units 6.3 + 6.5; sitemap-half open).

### `lighthouserc.json` final state

Phase 6 added: `/problems/hallucination-reduction/talk` (Unit 6.7). 14 URLs total. Same cohort-strategy as prior phases — one representative URL per route-template.

### Phase-6 follow-ons that survive the gate (non-blocking)

Same Q27-class pattern as prior phases — CI / deployed-URL artifacts must refresh on the first PR.

1. **Q47 operational unblock**: GitHub Discussions enablement on `bettyguo/OpenProblems` + giscus.app config UI to generate `NEXT_PUBLIC_GISCUS_REPO_ID` + add `GITHUB_TOKEN` to CI. Sequence documented in Unit 6.9's Q47 entry. Until resolved: every Phase-6 read surface gracefully renders empty/placeholder.
2. **W3C feed validator pass against `/api/v1/digest/<slug>` URLs** with discussion items — same Q27-class pattern as Phase-3's `/api/v1/rss.xml` validation deferral. Compound gate: Q47 + ≥ 1 discussion with comments + first preview deploy.
3. **Visual-regression baselines** for `/problems/<slug>/talk` × 2 themes × N viewports — `chromium-win32` baselines not captured in this unit (no Playwright spec changes). Future PR can pass `playwright test --update-snapshots`.
4. **Real-API integration smoke for Unit 6.2's GraphQL client** — gated on Q47 + `GITHUB_TOKEN` in CI. Unit 6.10 acceptance is local correctness via mocks (323 tests pass); production smoke is deferred.
5. **LHCI a11y ≥ 95 enumeration for `/problems/<slug>/talk`** — Unit 6.7 enrolled the URL; the canonical run lands on the first PR with this commit.
6. **`app/sitemap.ts`** + Q48 sitemap-half closure — no sitemap surface at HEAD. Phase-7+ candidate.

### Pre-existing follow-ons that survived Phase 6 (carryover from prior phases)

- Orphan `components/domain-tile-grid/` deletion — gated on explicit "delete the orphan tile-grid" curator authorization.
- `entries.json` content backfill on 8 problems — gated on curator-driven session with `ANTHROPIC_API_KEY`.
- `<managingEditor>` on RSS feeds (Q33 / Q44) — gated on Q2 (DNS / project email).
- W3C RSS validator pass on `/api/v1/rss.xml` — Phase-3 carryover; first preview deploy.
- W3C validator pass on Phase-5 digest endpoints (without discussion items) — Phase-5 carryover; first preview deploy.
- `pnpm clean-drafts` script — operational signal not yet met (`drafts/` < 100 stale files locally).
- Phase-2 ROR-ID + InstaDeep orphan — curator-driven hygiene; unchanged.

Documented in detail in Unit 6.8's hygiene status pass.

### Phase 7 entry conditions

Per §12 cardinal rule: **explicit human sign-off required**. §13 Phase 6+ scope is open-ended; Phase 6 pulled the **Discussions** thread; Phase 7+ candidate threads remain:

- **Auth thread**: Clerk vs NextAuth.js v5 vs GitHub OAuth direct. **MANDATORY DB-migration trigger flip** on the first write-path unit per Units 4.12 / 5.10 / 6.0 D-2. Unit 6.0 alternative-thread table sketches this as a 14+ unit phase.
- **Bilingual thread** (FR primary): pure content-side; no DB; estimated 14+ units infra + ongoing translation backfill.
- **Subscriber-list thread**: completes the Phase-5 D-4 punt; coupled to auth OR third-party (Buttondown / Resend / ConvertKit).
- **Monetization thread**: premature without auth + API + read-API maturity.

DB-migration trigger re-eval mandatory at Phase 7 kickoff per Unit 6.0 D-2 (currently 1.434% of 5 MB threshold). Stays cold throughout Phase 6; flips immediately if Phase 7 picks the auth thread.

### Cross-phase milestone

This commit closes the **Discussions thread** of Phase 6 in its entirety as authored in Unit 6.0. The 11-unit breakdown shipped end-to-end with one structural refactor (Unit 6.5's sync/async split on `getIndexedProblems`) documented at the time. The other §13 Phase-6+ threads (auth, bilingual, subscriber list, monetization) remain available for future open-ended phases.

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 6.10 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 323/323 across 38 files unchanged (no test files touched in 6.10).
- `pnpm build` → 333 prerendered pages unchanged. First Load JS shared chunk = 103 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-6 close: marks unit 6.10 as the acceptance gate; cites the §13 deliverable status table (one row per Discussions surface); emits the cross-phase state-at-HEAD (10 problems / 30 papers / etc.; 333 routes; 103 kB; 323 tests; 10 ADRs; 14 LHCI URLs); enumerates Phase-6 follow-ons (Q47-unblock sequence, W3C validator, visual baselines, real-API smoke, LHCI runs, sitemap); enumerates pre-existing carryovers from prior phases; states Phase-7 entry condition (explicit sign-off per §12); notes the DB-migration trigger re-eval status at HEAD (1.434%).
