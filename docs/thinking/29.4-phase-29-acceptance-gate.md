# 29.4 — Phase 29 acceptance gate (rating-action `dimensions.<dim>.rationale` markdown promotion realized via ADR-0018 D-G fourth sibling; 20 ADRs; tenth no-new-ADR phase since Phase 3; thirteenth consecutive 0-migration phase; 5-unit shape matches Phase 20-27)

Fifth and final Phase-29 unit; docs-only. Closes Phase 29
(**twentieth NON-§13 phase**; rating-action rationale markdown
promotion realized via fourth sibling under ADR-0018 D-G;
Phase-27 B.12 carryover closed at 2-phase age; first content-
side — Velite-validated — markdown render call site;
APPEND-not-EDIT convention extended to first three-APPEND ADR
D-clause). Mirrors Phase-12 through Phase-28 acceptance-gate
patterns; resumes the **5-unit no-new-ADR shape** (Phase 20-27 =
8 phases at 5 units; Phase 28 = 6-unit new-ADR shape; Phase 29 =
back to 5).

> **§12 sign-off**: Phase 30 sign-off **pending** at end of unit.
> "Continue" override path available (**twenty-fifth invocation**
> opportunity).

## THINK

### Phase 29 ledger

**5 units shipped** (29.0 prep → 29.4 gate). **0 deferrals**;
**0 scope drift**. **Eighteenth consecutive well-scoped phase**.
**Resumes the 5-unit no-new-ADR phase shape** after Phase 28's
6-unit new-ADR shape (Phase 19 was the prior 6-unit new-ADR
phase; both shapes are stable). **Ninth 5-unit phase in the last
9 no-new-ADR phases** (Phase 20-27 + 29 all 5 units; Phase 28
was 6 units due to new ADR).

| Unit | Title | Type | Status |
|---|---|---|---|
| 29.0 | Phase 29 prep | docs | shipped `63ce147` |
| 29.1 | `actionRationaleSchema` + `renderActionRationaleMarkdown` + `actionRationaleProcessor` + `DimensionCard` edit + 11 new tests | code | shipped `11c2fa4` |
| 29.2 | Phase-29 hygiene status pass | docs | shipped `7f2fb2a` |
| 29.3 | ADR-0018 D-G Phase-29 enhancement note (third APPEND) + OPEN_QUESTIONS hygiene | docs | shipped `3f34840` |
| 29.4 | **Phase 29 acceptance gate** (this unit) | gate | this commit |

### Phase-29 closure summary

**Phase-27 Class B.12 CLOSED at 2-phase carryover** — first
Phase-27-Class-B follow-on closed; resumes the Phase 21/23/25/27
tight-follow-on-close pattern after Phase 28's patience-signal
thread took the slot. Closed via:

- **`lib/markdown/sanitize-schema.ts` extension** (Unit 29.1):
  `actionRationaleSchema = { ...baseSchemaConfig }` fourth
  sibling export under ADR-0018 D-G. Docstring documents the
  first content-side markdown surface + 12-phase Phase-17
  anticipation → Phase-29 realization gap + Q72-family
  divergence-candidate position.
- **`lib/markdown/index.ts` extension** (Unit 29.1):
  `actionRationaleProcessor` singleton (fourth processor) +
  `renderActionRationaleMarkdown(text: string): string` (fourth
  render helper; signature mirrors Phase-27).
- **`lib/markdown/index.test.ts` extension** (Unit 29.1):
  **+11 new tests** (8 helper + 3 schema parity) across 2 new
  `describe` blocks. Mirrors Phase-18 + Phase-27 sibling-test
  pattern + adds wikilink-preservation regression-guard test
  documenting Class B.14.
- **`app/[locale]/problems/[slug]/ratings/page.tsx` edit**
  (Unit 29.1; Phase-3 surface long-untouched since Phase 3
  Unit 3.x): adds `renderActionRationaleMarkdown` + `cn` imports;
  defines `ACTION_RATIONALE_PROSE_CLASSES` local constant
  (mirrors Phase-27 `RATIONALE_PROSE_CLASSES` shape; tuned to
  tight `text-xs` scale + compact rhythm for 2-column dimension
  grid); replaces `DimensionCard`'s `<p>` rationale render with
  `<div dangerouslySetInnerHTML>`. `DimensionCard` instantiated
  5× per rating action via `DimensionsBlock`; all static-
  generated.
- **ADR-0018 D-G Phase-29 enhancement note appended** (Unit
  29.3): **third APPEND note on D-G**; **first ADR D-clause with
  three APPEND notes** in project history. Documents fourth-
  sibling realization + content-side surface + storage-layer-
  agnostic inheritance establishment + Phase-30+ Class B.14
  wikilink-resolution candidate.

### Final smoke gates (all green at HEAD pre-this-unit `3f34840`)

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **627/627 across 56 vitest files** (+11 / 0
  file Δ from Phase 28 close 616/56; vitest file count
  UNCHANGED — tests extend existing `lib/markdown/index.test.ts`).
- **`pnpm build`** → ~659 prerendered pages + **8 dynamic
  page+API routes UNCHANGED** (Phase-29 ratings-page promotion
  adds no dynamism; route was already SSG-prerendered).
  **First Load JS shared chunk = 103 kB UNCHANGED** end-to-end
  through every Phase 9-29 unit (38 consecutive units; Phase
  9-29 invariant preserved). **Middleware bundle = 160 kB
  UNCHANGED** since Phase 12. **`/[locale]/problems/[slug]/ratings`
  bundle = 1.92 kB / 108 kB UNCHANGED**. All other page bundles
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32
  baseline since Phase 2; unchanged through every Phase 3-29
  unit).
- **`pnpm lint`** clean.

### 13 Phase-29 architectural firsts

1. **Fourth sibling schema under ADR-0018 D-G** (bio + reviewNotes
   + challenge rationale + action rationale).
2. **First content-side (Velite-validated YAML) markdown render
   call site under ADR-0018 D-G** — establishes storage-layer-
   agnostic inheritance.
3. **Realizes the 12-phase Phase-17 anticipation** in
   `sanitize-schema.ts:22-24` (load-bearing test of D-G
   inheritance contract durability).
4. **First ADR D-clause with three APPEND notes** (ADR-0018 D-G:
   Phase-18 retroactive + Phase-27 + Phase-29).
5. **Third APPEND note on ADR-0018 D-G** (extends APPEND-not-EDIT
   convention to its longest single-D-clause use).
6. **First post-ADR-streak-break no-new-ADR phase** (Phase 28
   broke the 8-phase Phase-18 + 20-27 streak; Phase 29 starts a
   new 1-phase streak immediately).
7. **Tenth phase since Phase 3 to close without new ADR**
   (Phase 18 + 20-27 + 29).
8. **Thirteenth consecutive 0-migration phase** (Phase 17-29;
   16 of 20 phases since DB landed).
9. **Thirteenth consecutive single-session phase** (Phase 17-29).
10. **First Phase-27-Class-B follow-on closed at 2-phase age**
    (B.12; tight cadence resumed after Phase 28's patience-
    signal thread).
11. **First non-zero test-count delta + zero-file-count delta
    since Phase 22** (+11 tests added to existing file; 7-phase
    gap).
12. **Q72 family expanded to 3 sibling-divergence candidates**
    at Phase 29 entry (`reviewNotesSchema` + `rationaleSchema` +
    `actionRationaleSchema` could each diverge from `bioSchema`).
13. **First Class B item surfaced with explicit non-promotion
    rationale since Phase 19** (B.14 wikilink rendering;
    documents 3 promotion triggers as forward-compat).

### Delta summary (Phase 28 → Phase 29)

- +5 commits (4 + this gate).
- **+0 ADR** (20 → 20; tenth no-new-ADR phase since Phase 3;
  ADR-0021 candidate slot stays open).
- 0 migration Δ (6 → 6; thirteenth consecutive 0-migration
  phase).
- 0 runtime dep Δ.
- 0 env-var Δ (9 → 9; first env-var-stable phase since Phase 28).
- **+11 / 0 file test Δ** (616/56 → 627/56).
- 0 First Load JS Δ (103 kB UNCHANGED; 38th consecutive unit).
- 0 middleware Δ (160 kB UNCHANGED).
- 0 page-bundle Δ.
- 0 dynamic-route Δ.
- 0 i18n keys Δ (142 → 142; first i18n-stable phase since
  Phase 28).
- 0 source-file Δ in `lib/markdown/` (3 → 3; helpers added in-
  place).
- **+1 markdown render surface** (3 → 4).
- **+1 markdown render call site** (9 → 10).
- **+1 `dangerouslySetInnerHTML` surface** (9 → 10).
- **+1 XSS-audit surface** (3 → 4).
- **+1 content-side markdown call site** (0 → 1; first non-DB-
  backed call site under ADR-0018 D-G).
- **+1 APPEND note on ADR-0018 D-G** (2 → 3; first three-APPEND
  ADR D-clause).
- OPEN_QUESTIONS Δ = 26 / 4 / 31 / 61 → 26 / 4 / 31 / 61
  (UNCHANGED; 1-phase Q-tally freeze re-started after Phase
  28's break of the 8-phase Phase-20-27 freeze).
- +1 Class B category (B.14 wikilink rendering; documented-not-
  promoted).
- **+1 Class-B follow-on closed** (Phase-27 B.12 rating-action
  rationale markdown).

### Surviving follow-ons (Class B; ~5 items across 13 categories)

**B.12 1 of 1 CLOSED Phase 29** = rating-action `dimensions.<dim>.rationale`
markdown promotion (Phase-27 B.12 carryover; 2-phase age).

**B.14 1 NEW Phase 29 surfaced**: rating-action rationale
wikilink resolution (NOT promoted to Q; 3 promotion triggers
documented).

8 carried unchanged from Phase 28 close: Q54 / Q55 / Q69
operational gates; CI dummy `AUTH_SECRET`; `pnpm db:migrate`
doc; first LHCI run; middleware-based auth-route protection;
per-script operational docs (backfill-exif-strip +
cleanup-orphan-blobs + emit-challenge-action).

All other Class B categories carried unchanged from Phase 28
close (B.1 / B.2 / B.3 [Q72 family expanded to 3] / B.4 / B.5 /
B.8 / B.9 / B.10 / B.11 / B.13).

### Phase-30+ Q-candidates flagged (9 total)

7 carried from Phase 27 (Q60 / Q61 / Q62 / Q64 / Q65 / Q68
expansion / Q72) + 2 carried from Phase 28 (Q73 + Q74). **9
total Phase-30+ Q-candidates** (unchanged Phase 29).

### Phase-30+ ADR slot reservations

**ADR-0021 candidate slot stays open** (opened Phase 28 by
ADR-0020 landing; Phase 29 did not claim).

Strongest remaining candidates at Phase 30 entry:

- **Phase-5 D-4 subscriber-list email** (carried **22+ phases** —
  **sole strongest patience signal**; Phase 28 closed the prior
  strongest non-multi-phase signal — Phase-9 Class B item 8 at
  17 phases; subscriber-list email is uncontested longest
  carryover). Multi-phase scope (foundation + provider +
  verification + unsubscribe + templates + scheduler); better as
  2-3 phase rollout, not a clean single-phase candidate.
- **Q68 expansion content moderation** (uploaded images;
  ADR-0019 D-F row 1; ~3-5 units).
- **Markdown evolution ADR** (Phase-17 D-H + Q72 family-expanded
  to 3 + Phase-29 wikilink B.14 could couple into one ADR scope;
  ~3-5 units if coupled).
- **Account-merge UI** (ADR-0020 D-E candidate; 1-phase age;
  ~2-3 units).
- **Third+ provider expansion** (ADR-0020 D-B amendment; 1-phase
  age; ~2-3 units).

### Risk surface at Phase 29 close

- 4 operational gates pending (Q54 / Q55 / Q69 / Q73; unchanged
  Phase 29).
- `next-auth@5.0.0-beta.31` exact-version pin (carries from
  Phase 9 + Phase 28 multi-provider; unchanged Phase 29).
- All other Phase-28 risk-surface items unchanged.
- **New Phase-29 risk**: heading-demotion `<h3>` clamp coexists
  with page-level `<h3>` "Signals considered" in
  `/problems/[slug]/ratings`; ADR-0018 D-C permits same-level
  coexistence; risk is theoretical at Phase-29 close (no
  current rating-action rationale uses headings).

### Boundary statement

NOT the new ADR-0021 (slot stays open); NOT the ADR-0018 D-G
amendment (third APPEND note only — APPEND-not-EDIT convention);
NOT new content; NOT new routes; NOT new env vars; NOT new
i18n keys; NOT new dynamic routes; NOT new migrations; NOT new
dependencies; NOT wikilink resolution (B.14, documented-not-
promoted; deferred); NOT challenge rationale changes (Phase 27
surface untouched); NOT reviewNotes / bio changes (Phase 17-18
surfaces untouched); NOT rating-action SCHEMA change
(`rating-action.ts` Zod schema unchanged; content-side data
shape preserved); NOT new content moderation (Q68 expansion;
deferred); NOT account-merge UI (Phase-28 follow-on; deferred);
NOT subscriber-list email infrastructure (Phase-5 D-4 punt;
deferred). This phase is the **render-layer promotion** of an
existing content-side data field, **realizing the 12-phase
Phase-17 anticipation** without architectural surface change.

### Parallel-curator awareness

Phase 29 shipped as a **single-session phase** — primary shipped
all 5 units 29.0 → 29.4 in one coherent session. **Thirteen
consecutive single-session phases** (Phase 17-29). Phase 16
remains the lone parallel-session phase. Working tree clean at
each unit boundary; no parallel-session activity observed during
Phase 29 work.

### Phase 29 vs prior no-new-ADR phases

Phase 29 = tied-lightest no-new-ADR phase rhythm at 5 units
(matches Phase 20-27; mirrors Phase 18's 7-unit shape only in
the no-new-ADR rhythm — Phase 18's heavier scope reflected
broader feature work). The **5-unit shape is the stable
lightest no-new-ADR phase rhythm** when:
- No schema migration.
- No new env vars.
- No new i18n keys.
- No new ADR.
- Single keystone thread realized in one code unit (1 prep + 1
  code + 1 hygiene + 1 ADR-note-or-OQ-hygiene + 1 gate).

**Ninth 5-unit phase in the last 9 no-new-ADR phases** (Phase
20-27 + 29 all 5 units; Phase 28 was 6 units due to new ADR).

### Phase 30 entry conditions

Per §12 cardinal rule, **explicit human sign-off required**.
**Twenty-fifth "Continue" override opportunity** in Unit 30.0
prep. Thread options enumerated above; **I. Subscriber-list
email** is the sole strongest remaining patience signal (22+
phases at Phase 30 entry).

### Files modified

- CHANGELOG.md (Unit 29.4 entry).
- `docs/thinking/29.4-phase-29-acceptance-gate.md` (this file).
