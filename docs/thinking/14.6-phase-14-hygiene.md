# 14.6 — Phase-14 hygiene status pass (Class A / B / C catalog)

Seventh Phase-14 unit; docs-only. Mirrors Phase-5/6/7/8/9/10/11/12/13
hygiene precedents. Catalogs **Class A** in-flight Phase-14 items (4
carried; 0 newly surfaced), **Class B** Phase-14-specific follow-ons
(11), and **Class C** carryovers from prior phases (19; mostly
unchanged from Unit 13.4). Lands ahead of Unit 14.7 (OPEN_QUESTIONS
hygiene) and Unit 14.8 (acceptance gate).

## Class A — 4 carried operational items (unchanged from Unit 13.4)

Phase 14 did not surface any new operational gates. All Phase-14
surfaces are read-only on existing Phase-9 / Phase-11 / Phase-12 DB
data + Velite `#site/content`; no new DB columns, no new env vars,
no new ops coordination required.

1. **Q54 GitHub OAuth app registration** (operational; carried since
   Phase 9). Public profile route (Unit 14.3) + sub-route (Unit 14.4)
   do NOT require auth, so they render to unauthenticated visitors
   even with Q54 unresolved. The SiteHeader "Your profile" link (Unit
   14.5) only renders when signed-in; Q54 unresolved → no sign-in
   path → no SiteHeader profile link → no broken state.
2. **Q55 Turso production DB provisioning** (operational; carried since
   Phase 9). Unit 14.3 shell + Unit 14.4 sub-route both DB-read on
   every request; production needs Turso provisioned + reachable. CI
   + dev `local.db` already exercises the helpers via Unit 14.2's 13
   DB-mocked tests.
3. **CI dummy `AUTH_SECRET`** (Phase-9 Class A item 3; carried).
4. **`pnpm db:migrate` doc for new contributors** in `/contributing`
   v1.2 (Phase-9 Class A item 4; carried). 4 migrations (unchanged
   from Phase 13 — Phase 14 added zero migrations).

## Class B — 11 Phase-14-specific follow-ons

Architectural follow-ons surfaced by Phase 14's surface. Each is
overridable as a Phase-15+ thread.

1. **User-editable profile fields** (Q63 candidate; flagged in
   ADR-0015 D-C). Editable `users.name` (display name) + `users.bio`
   (new column; new migration) + possible image override (new
   column OR Vercel-Blob upload pipeline + new ADR). Phase 15+;
   requires writes API + new DB columns + XSS sanitization.
2. **Per-user privacy opt-out toggle** (Q64 candidate; flagged in
   ADR-0015 D-D). `users.profilePublic` boolean column + migration
   + writes surface + 404 response on `/u/{handle}` when opted-out.
   Phase 15+; promote if usage signals demand.
3. **Per-curator activity feed / contribution timeline** (Q65
   candidate; flagged in ADR-0015 D-E expansion). New
   `getCuratorActions(handle)` helper + new `/u/{handle}/curated`
   sub-route + rating-action history join. Phase 15+; couples to
   curator-of-record badge expansion.
4. **Curator-of-record badge case-insensitive matching** (ADR-0015
   D-E enhancement; single-line `.toLowerCase()` change). Lift if
   observed YAML/GitHub case mismatches accumulate. Phase 15+ pure
   server-side logic; no schema change.
5. **Per-user listing URL search-param sort + pagination**
   (Phase-13 Class B items 2 + 7 carryover, now also applicable to
   per-user listing). LIMIT 50 silent cap is sufficient for current
   scale; Phase 15+ via URL `?sort=...&offset=N`.
6. **Per-row "edit / withdraw" affordances on per-user listing when
   viewing own profile**. Today the per-user sub-route renders
   read-only rows; signed-in own-profile viewer cannot withdraw a
   pending challenge from this surface (must navigate to `/profile`).
   Phase 15+ alongside Q63 promotion.
7. **Custom 404 page for `/u/[handle]`**. Currently `notFound()`
   invokes Next.js's default 404 surface. Pre-added `not_found_title`
   + `not_found_message` keys in Unit 14.3 anticipate a custom 404
   page; Phase 15+ implementation.
8. **`/about/privacy` explainer** (ADR-0015 D-D pre-emptive
   mitigation). Today the public profile footer link points to
   `/about` placeholder; Phase 15+ expands to dedicated privacy
   explainer documenting the public-data invariant + opt-out path
   (depends on Q64 promotion).
9. **First LHCI run validating new public profile route + sub-route**
   (Phase-9 Class B item 10 + Phase-13 Class B carryover; now
   applicable to 7 new URLs: `/en/u/[any-handle]` + `/fr/u/[any-handle]`
   + sub-routes + 404). 19 LHCI URLs unchanged in `lighthouserc.json`;
   first preview-deploy PR with Phase-14 commits triggers.
10. **Mobile-nav variant of "Your profile" link**. Current
    `hidden sm:inline-flex` desktop-first pattern hides the SiteHeader
    profile link on mobile. Phase 15+ if mobile signed-in usage
    surfaces.
11. **Avatar-dropdown** on SiteHeader (profile + sign-out + future
    settings). Current pattern is "avatar+`@login` Link → public
    profile; separate AuthControl button for sign-out". Phase 15+
    consolidation candidate if SiteHeader gains 3+ user-specific
    affordances.

## Class C — 19 carryovers (mostly unchanged from Unit 13.4)

1. Orphan `components/domain-tile-grid/` deletion (curator
   authorization).
2. `entries.json` content backfill on 8 problems.
3. `<managingEditor>` on RSS feeds (Q33/Q44; Q2 DNS gate).
4. `pnpm clean-drafts` script.
5. Phase-2 ROR-ID + InstaDeep orphan.
6. **Q47 Discussions enablement** (Phase-6 operational; gated on
   `GITHUB_TOKEN` + `GISCUS_REPO_ID`).
7. **Unit 8.4 HTML shell migration** — STILL ON HOLD per parallel-
   session preservation signal. Closes Phase-7 `html-has-lang`
   axe-rule risk on `/fr/...` pages.
8. Fallback-hint UI for `didFallback === true` (Phase-8 Class B
   item 4).
9. `messages.{contributing,methodology,…}.*` chrome strings + FR
   bulk backfill + StatusPill localization + nav labels via
   `useTranslations` (Q51 curator-track horizon).
10. Trailing-slash normalization for `NEXT_PUBLIC_SITE_URL`.
11. Per-entry sitemap `lastModified` / `changeFrequency` /
    `priority` hints.
12. Watchlist count display on `/problems` index (Phase-9 Class B
    item 4).
13. Multi-provider OAuth expansion (Phase-9 Class B item 8;
    **ADR-0016 candidate**).
14. Real-API integration smoke for `lib/discussions/github-graphql.ts`
    (Q47 + `GITHUB_TOKEN`).
15. **CLI helper `pnpm emit-challenge-action <id>`** (Q59 candidate;
    Phase-12 Class B item 1; Phase-13 Class B item 8 carryover;
    Phase 15+ candidate).
16. Form-state preservation on validation error (Phase-11 Unit 11.3
    tradeoff; Phase-12 Class B item 3; Phase-13 Class B item ?
    carryover).
17. Orphan-row cleanup script for `watchlist` + `ratingChallenge`
    tables (Phase-9 Class B item 6 + Phase-11 Class B item 11; ADR-
    0014 D-? `reviewerId` `ON DELETE SET NULL` orphan nuance).
18. W3C feed validator passes against deployed `/api/v1/rss.xml` +
    `/api/v1/digest/<slug>` (Phase-3/5/6/8 compound; first deploy
    gate).
19. Rate-limiting on `POST /api/v1/rating-challenges/[id]/review`
    (Phase-12 Class B item 14; per-curator surface; defer until
    broader unauthenticated surfaces land).

## Phase-14 surface delta vs Phase-13 close

- **Routes**: +2 new dynamic page routes (`ƒ /[locale]/u/[handle]`
  + `ƒ /[locale]/u/[handle]/challenges`). Total dynamic page+API
  routes: **5 → 7** (+2).
- **Tests**: 467 → **480** (+13 net; all from Unit 14.2's
  `lib/users/index.test.ts`). Test files: 51 → **52** (+1).
- **First Load JS shared chunk**: **103 kB UNCHANGED** through every
  Phase-14 unit. All Phase-14 surfaces server-rendered; zero client-
  bundle delta.
- **Middleware bundle**: **160 kB UNCHANGED** (no middleware changes).
- **ADRs**: 14 → **15** (+1: ADR-0015 — per-user privacy model).
- **Dependencies**: **+0 net** (Phase-9/10/11/12/13 stack stable).
- **New code layers**: `lib/users/` (Phase-14 net-new) +
  `app/[locale]/u/[handle]/` route tree (Phase-14 net-new).
- **Migrations**: **4 UNCHANGED** (Phase 14 added zero migrations).
- **DB schema tables**: **6 UNCHANGED**. **Columns UNCHANGED**
  (Phase 14 read-only).
- **Env contract**: **UNCHANGED** (no new env vars).
- **i18n**: +16 `public_profile.*` keys per locale (+32 total across
  EN + FR; ALL pre-added in Unit 14.3 per atomic-i18n discipline;
  Units 14.4 + 14.5 consumed subset without further edits).
- **OPEN_QUESTIONS state**: 53 entries at Phase-13 close; **53
  unchanged** at Phase-14 close (Q63 / Q64 / Q65 candidates flagged
  but NOT promoted; promotion deferred to Phase 15+ kickoff). To be
  reconfirmed in Unit 14.7's mechanical tally.

## Parallel-curator activity log (Phase 14)

- Primary session shipped 14.0 / 14.1 / 14.2 / 14.3 / 14.4 / 14.5 /
  14.6 (this) sequentially without collisions.
- ADR-0015 + Unit 14.0 prep gave each unit a tight enough scope that
  parallel divergence didn't surface.
- Atomic-i18n pre-add discipline (Unit 14.3 pre-adding all 16 keys
  upfront) prevented messages.json collisions on Units 14.4 + 14.5.

## Risk surface at HEAD `26d9125`

1. **`AUTH_SECRET` must be set in CI / Vercel / preview** (Class A
   item 3; surfaces as Auth.js init throw under `NODE_ENV=production`;
   `safeAuth()` doesn't catch).
2. **End-to-end public profile exercise blocked on Q54 + Q55**
   (architectural surface complete; operational unblock pending).
3. **Orphan watchlist + ratingChallenge rows tolerated** until
   cleanup script lands (Class C item 17; ADR-0013 D-F + ADR-0014
   D-? intentional).
4. **`html-has-lang` axe-rule mismatch on `/fr/...`** (Class C item
   7; Unit 8.4 deferral keeps it indefinite).
5. **No opt-out for `/u/{handle}` Phase 14** (ADR-0015 D-D). Q64
   candidate addresses if signal appears.

## Boundary statement

NOT the OAuth app registration (Q54 operational), NOT the Turso
provisioning (Q55 operational), NOT writes for editable profile
fields (Phase 15+; Q63 candidate), NOT the per-user privacy opt-out
toggle (Phase 15+; Q64 candidate), NOT the per-curator activity feed
(Phase 15+; Q65 candidate), NOT destructive cleanup (Class C item
1 still gated). This unit is the **catalog**, not the **resolution**.

## Smoke gates (this unit)

Docs-only — no `validate-content` / `test` / `build` changes
anticipated.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).
- typecheck / test / build untouched since no source files modified.

## CHANGELOG entry shape

Unit 14.6 entry: catalogs 4 Class A + 11 Class B + 19 Class C items;
notes 0 newly surfaced operational gates; emits Phase-14 surface
delta (+2 dynamic routes / +13 tests / +1 vitest file / +1 ADR /
+0 deps / +32 i18n keys / +0 migrations / +0 DB columns / +0 env
vars / +0 First Load JS regression / +0 middleware regression);
states Q63 / Q64 / Q65 candidates remain flagged (Unit 14.7
mechanical tally will confirm); enumerates risk surface at HEAD.
