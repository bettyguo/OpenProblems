# 61.4 — Phase 61 acceptance gate

Phase 61 closes at HEAD `2ab34b7` (Unit 61.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-62-entry
conditions per §12.

## Phase 61 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 61.0 | Phase 61 prep (redirect from rank 2 to rank 7) | docs | `24e0076` |
| 61.1 | `GFM_TABLE_SCHEMA_OVERRIDES.tagNames` extension to add `"caption"` + 6 NEW/updated `tables.test.ts` schema-isolation tests + ADR-0018 D-G APPEND-D-AS | code+APPEND | `f5a4c9d` |
| 61.2 | 6 NEW Phase-61 caption edge-case + cumulative-schema-extension-state schema-isolation tests in `tables.test.ts` | code | `189deef` |
| 61.3 | Phase-61 hygiene + ADR-0018 APPEND-D-Q item 4 closure documentation (22-phase carryover; NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED) | docs | `2ab34b7` |
| 61.4 | Phase 61 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-60 5-unit framework shape
verbatim.

## Second realization of Phase-57-derived schema-extension phase-shape pattern + first state where schema-extension is observed twice within same consumer + tables consumer gains 3rd evolution + first state where TWO consumers have 3+ evolutions each + first phase to advance framework beyond Phase-60 capability ceiling + schema-ready-before-plugin extended from attributes to tags + NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE record at 22-phase carryover + D-Q becomes first D-clause with 3-of-4 items closed + 20-phase APPEND-deferral closure cadence

- **Thread choice**: Phase-60 gate ranked rank 2 (`<a class="wikilink">`
  styling) as "second schema-extension realization sibling to Phase
  57". Reconnaissance revealed `CompositeExtensionRegistry`
  (`lib/markdown/extensions/composite.ts:71-77`) throws on
  schemaOverrides conflict per APPEND-D-C no-deep-merge contract.
  Adding schemaOverrides to wikilinks would break `wikilinks,tables`
  composition. Phase 61 **redirects to rank 7 (`<caption>` element)**
  — clean 2nd schema-extension realization on the existing tables-
  owned schemaOverrides slot; no composition conflict.
- **`GFM_TABLE_SCHEMA_OVERRIDES.tagNames` evolution** in
  `lib/markdown/extensions/tables.ts`:
  - Before (Phase 39 ship through Phase-60 close; Phase-57 attributes
    baseline): 18 Phase-17 base tags + 6 GFM table tags (`table`,
    `thead`, `tbody`, `tr`, `th`, `td`) = 24 entries.
  - After (Phase 61 ship): 18 Phase-17 base tags + 7 GFM table tags
    (+`caption`) = 25 entries.
- `attributes` UNCHANGED — HTML5 `<caption>` has NO caption-specific
  attributes; minimal XSS surface.
- `TablesExtensionRegistry` class + factory dispatch arm +
  `PHASE_39_DEFAULT_ENABLED_SURFACES` + `remark-gfm` parser all
  UNCHANGED.
- 6 NEW/updated tests in `tables.test.ts` (Unit 61.1) + 6 NEW
  caption edge-case tests in `tables.test.ts` (Unit 61.2) = 12
  NEW caption-related tests in `tables.test.ts`.
- ADR-0018 D-G APPEND adds Phase-61 EXTENDED block + **APPEND-D-AS**
  (Unit 61.1; nineteenth two-letter slot).
- **APPEND-D-Q item 4 `<caption>` CLOSED at 22-phase carryover**
  (Phase 39 → 61). **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE
  EVER OBSERVED** in project history (extends Phase-57 18-phase
  record by 4 phases).
- **D-Q becomes first D-clause with 3-of-4 enumerated items closed**
  in project history (items 1 + 3 + 4 closed; only item 6 surface-
  specific table schemas remaining). First D-clause with 3+ items
  closed.
- **Second realization of the Phase-57-derived schema-extension
  phase-shape pattern** in project history — first 2-realization
  for that pattern (extends Phase-57 record 1 → 2). **First state
  where the schema-extension pattern is observed twice within the
  same consumer** (tables).
- **Tables consumer gains 3rd evolution post-first-ship** (Phase
  43 + Phase 57 + Phase 61). **First state where TWO consumers
  have 3+ evolutions each** in project history (arxiv was first
  via Phase 44 + 47 + 53; tables joins).
- **First phase to advance the framework beyond its Phase-60
  capability ceiling** via new realization of an existing phase-
  shape pattern. **First post-capability-ceiling phase to ship a
  new realization** in project history.
- **Schema-ready-before-plugin state extended from attributes
  (Phase 57) to tags (Phase 61)** — first TAG-addition schema-
  ready-before-plugin in project history.
- **20-phase APPEND-deferral closure cadence** (extends Phase-60
  record 19 → 20).
- **Fifty-second NON-§13 phase** (extends Phase-60 half-century
  milestone to 52); first 52-phase ledger-closure streak.

## Phase 60 → Phase 61 delta

| Metric | Phase 60 close | Phase 61 close | Δ |
|---|---|---|---|
| Tests | 1440 / 75 files | **1452 / 75 files** | +12 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 27 | **28** (project record extends; nineteenth two-letter slot D-AS in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 8 | 8 | 0 |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 21 | **21** | 0 |
| **Phase-37-framework concrete consumers** | **7** | **7** | 0 |
| **Plugin-regex-extension realizations** | **7** | **7** | 0 |
| **Constructor-arg-only zero-rework expansion realizations** | **7** | **7** | 0 |
| **Schema-extension realizations** | **1** | **2 (+caption)** | **+1 (first 2-realization)** |
| **Dual-form regex realizations** | **5** | **5** | 0 |
| **Consumers with 3+ evolutions** | **1 (arxiv)** | **2 (+tables)** | **+1 (first state where TWO consumers have 3+ evolutions each)** |
| **Surfaces with sextuple-alias** | **4** | **4** | 0 |
| **D-clauses with BOTH items closed** | **4** (D-AC + D-AH + D-AL + D-AP) | **4** | 0 |
| **D-clauses with 3+ items closed** | **0** | **1 (+D-Q)** | **+1 (first D-clause with 3+ items closed)** |
| **APPEND-deferral closure cadence (consecutive phases)** | 19 | **20 (Phase 42-61)** | **+1 (new record)** |
| **Longest absolute APPEND-deferral closure carryover** | 18-phase (Phase 57 D-Q item 3) | **22-phase (Phase 61 D-Q item 4) — new record** | **+4 phases (sub-18-phase record beat)** |
| **NON-§13 phase count** | 51 | **52** | **+1** |
| **"Continue" override invocations past half-century threshold** | **5** | **6 (+Phase 61 = 56th)** | **+1** |

## Architectural firsts in Phase 61 (12 enumerated)

1. **Second realization of the Phase-57-derived schema-extension
   phase-shape pattern** in project history. Phase 57 tables
   attributes (`colspan`/`rowspan`/`scope` on `<th>`/`<td>`);
   Phase 61 tables caption (`caption` tag added to `tagNames`).
   **First 2-realization for the schema-extension phase-shape
   pattern in project history** — extends Phase-57 record 1 → 2.
   **First state where the schema-extension pattern is observed
   twice within the same consumer** (tables); both realizations
   within tables; both schema-ready-before-plugin.
2. **Tables consumer gains 3rd evolution post-first-ship** (Phase
   43 cross-surface + Phase 57 attributes + Phase 61 caption).
   **First state where TWO consumers have 3+ evolutions each** in
   project history (arxiv was first via Phase 44 cross-surface +
   Phase 47 alias + Phase 53 legacy ID-class = 3 evolutions;
   tables joins at Phase 61). Confirms the 3-evolution depth as
   a recognized framework-evolution shape for consumers with
   sustained demand signal.
3. **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED**
   at **22-phase carryover** (Phase 39 → Phase 61). Extends
   Phase-57 18-phase record (Phase 39 → 57 D-Q item 3 table-
   specific attributes) by 4 phases. APPEND-D-Q closure
   trajectory: item 1 cross-surface 4-phase (Phase 43); item 3
   attributes 18-phase (Phase 57); item 4 caption **22-phase**
   (Phase 61).
4. **D-Q becomes first D-clause with 3-of-4 enumerated items
   closed** within the cadence in project history (items 1 + 3
   + 4 closed; only item 6 surface-specific table schemas
   remaining). **First D-clause with 3+ items closed**. Prior
   multi-item D-clauses had at most 2 items closed (D-AC + D-AH
   + D-AL + D-AP each reached 2-item-both-closed). D-Q at 3-of-
   4 establishes a new depth for multi-item D-clause resolution.
5. **First phase to advance the framework beyond its Phase-60
   capability ceiling** via new realization of an existing
   phase-shape pattern. Phase 60 closed at "framework's current
   capability ceiling for the 7-consumer roster" — all 7
   concrete consumers had had ALL their applicable extensions
   resolved along both principal axes. Phase 61 advances by
   extending the schema-extension phase-shape pattern from 1 →
   2 realizations within the existing tables consumer + existing
   schemaOverrides slot. **First post-capability-ceiling phase
   to ship a new realization** in project history.
6. **Schema-ready-before-plugin state extended from attributes
   (Phase 57) to tags (Phase 61)** in project history. Phase 57
   introduced schema-ready-before-plugin for attributes
   (`colspan`/`rowspan`/`scope` allow-listed ahead of any
   pipeline that emits them); Phase 61 extends to tag additions
   (`caption` tag allow-listed ahead of any pipeline that emits
   it). **First state where schema-ready-before-plugin applies
   to a TAG addition** (vs Phase-57 ATTRIBUTE additions).
   Confirms the schema-ready-before-plugin discipline as the
   framework's forward-compatibility mechanism for sanitize-tag-
   allow-list expansion ahead of plugin emit.
7. **20-phase APPEND-deferral closure cadence sustained** (Phase
   42-61) — new longest sustained cadence in project history
   (extends Phase-60 record 19 → 20). **First 20-phase APPEND-
   deferral closure run** in project history.
8. **Twentieth prep-/APPEND-doc-level deferral closed by a later
   phase**: Phase 42 → 38 D-L item 1; …; Phase 60 → 58 D-AP
   bioRxiv alias item; **Phase 61 → 39 D-Q item 4 `<caption>`
   element**.
9. **Twenty-eighth APPEND on ADR-0018 D-G** — extends the
   **first-ADR-D-clause-with-most-APPENDs record** from 27 →
   28. **Nineteenth two-letter APPEND letter D-AS** (Excel-
   spreadsheet column convention sustained).
10. **Fifty-second NON-§13 phase** (extends Phase-60 half-
    century milestone to 52). The §13 ledger remains CLOSED
    across the entire 52-phase run (Phase 10-61) — **first
    52-phase ledger-closure streak**.
11. **First "framework + 7 consumers + 7 expansions + 7
    plugin-regex-extensions + 2 schema-extensions + 5-consumer
    same-slot × 4 surfaces + 7-consumer composition × 4
    surfaces + alias × 6 + dual-form × 5 + sextuple-alias × 4
    + cross-surface-first × 1 + axis-asymmetry-resolved × 1 +
    ALL-7-consumers-fully-extended × 1 + schema-ready-before-
    plugin × 2 (attributes + tags) + 4-D-clauses-both-items-
    closed + D-clause-with-3-items-closed × 1 + 2-consumers-
    with-3+-evolutions × 1" 25-phase cluster** in project
    history (Phase 37-61). 25-phase architectural arc. Plus
    **31st consecutive phase without new B category** — Phase
    31-61 = first 31-phase run (extends Phase-60 record 30 →
    31). Plus **163rd consecutive 103 kB First Load JS unit**
    (extends from 159 at Phase-60 close). Plus **26th
    consecutive no-new-ADR phase** (Phase 36-61; extends
    Phase-60 record 25 → 26).
12. **56th "Continue" override invocation** at Unit 61.0 —
    extends the **half-century-of-Continue-overrides
    milestone** (Phase 55 = 50th) to **56 over Phases 6-61**.
    **Sixth invocation past the half-century threshold** —
    confirms the §12-cardinal-rule sign-off discipline remains
    durable post-milestone across 7 successive phases.

## Phase 61 → Phase 62 entry conditions

Per §12, Phase 62 entry requires **explicit human sign-off**.

**Framework state note**: Phase 61 advanced the framework
beyond its Phase-60 capability ceiling via a new realization of
the schema-extension phase-shape pattern (1 → 2 realizations).
The framework has now demonstrated that **existing phase-shape
patterns can be extended with new realizations on different
slot kinds or consumers**, opening Phase 62+ candidates that
introduce new realizations of existing patterns OR introduce
genuinely new phase-shape patterns.

**Recommended Phase 62 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **Plugin parameterization for wikilink-href-builder** (APPEND-D-L item 6) — refactor `rehypeResolveWikilinks` plugin to accept entity-type parameter; prerequisite for cross-entity wikilinks | 1-2 | Autonomous-tractable refactor; standalone unit |
| 2 | **Cross-entity wikilinks** (APPEND-D-L item 3) — entity-type disambiguation `[[paper-id\|display]]` / `[[author-slug\|display]]` / `[[institution-slug\|display]]`; **depends on plugin parameterization (rank 1) landing first or same-phase** | 2-3 | Autonomous-tractable; depends on rank 1 |
| 3 | **Surface-specific table schemas** (APPEND-D-Q item 6 — last remaining D-Q deferral) — closure would make D-Q the first D-clause with ALL enumerated items closed | 1-2 | Autonomous-tractable; would complete D-Q closure |
| 4 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — requires either framework refactor (multi-source schemaOverrides support) OR plugin-only emit pattern (no schemaOverride; rely on post-sanitize plugin-order discipline) | 1-2 | XSS audit per attribute value; defer until framework decision on multi-source schemaOverrides |
| 5 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 6 | **Empty-alias fallback unification** across consumers (Phase-48 deferral) — refine arxiv/doi/pubmed/orcid/biorxiv empty-alias divergence | 1 | Smallest-scope autonomous-tractable |
| 7 | **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 8 | **Legacy numeric-only bioRxiv IDs** (pre-2019 format) — regex evolution mirrors Phase-53 arxiv legacy ID-class shape | 1-2 | Autonomous-tractable; small-scope; would be 8th plugin-regex-extension |
| 9 | **OSF preprint consumer** — eighth concrete consumer | 2-3 | DOI overlap concern; introduces NEW consumer |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Most defensible autonomous-tractable next thread**: Rank 1
(plugin parameterization for wikilink-href-builder) as
prerequisite for rank 2 (cross-entity wikilinks). The two
together form a natural Phase 62-63 sequence: Phase 62 plugin
parameterization (refactor; ~1-2 units) → Phase 63 cross-entity
wikilinks (~2-3 units). OR rank 3 (**surface-specific table
schemas**) — would close the **last remaining D-Q deferral**
and make D-Q the **first D-clause with ALL enumerated items
closed** in project history. Rank 3 is the cleanest
"architectural finishing move" for D-Q's multi-item cadence.

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1452 / 75 files** (+12 / 0 vs Phase 60
  close).
- `pnpm audit-content` — 0 errors / 6 warnings (57
  consecutive phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (164 consecutive units
  at Unit 61.4 ship).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 62+ deferrals)

NOT plugin parameterization (Phase 62+ rank 1);
NOT cross-entity wikilinks (Phase 62+ rank 2; depends on plugin
parameterization);
NOT surface-specific table schemas (Phase 62+ rank 3; closure
would complete D-Q);
NOT `<a class="wikilink">` styling (Phase 62+ rank 4; deferred
pending framework decision on multi-source schemaOverrides);
NOT 404 handling (Phase 62+ rank 5);
NOT empty-alias fallback unification (Phase 62+ rank 6);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 62+ rank 7);
NOT legacy numeric-only bioRxiv IDs (Phase 62+ rank 8);
NOT OSF preprint consumer (Phase 62+ rank 9);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi +
pubmed + orcid + biorxiv (Phase 62+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 62+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 62+;
requires framework refactor per the composite-registry single-
source rule);
NOT dx.doi.org legacy host (Phase 62+);
NOT stricter trailing-lookahead (Phase 62+);
NOT paper-card hover-preview (Phase 62+);
NOT auto-trim of alias display whitespace (Phase 62+);
NOT a future plugin that EMITS `<caption>` or
`colSpan`/`rowSpan`/`scope` (Phase 62+);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 62+);
NOT `@mention` (Phase 62+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-60 deferrals carry forward.

## Boundary statement

**Phase 61 CLOSED at HEAD `<this-unit>` after 5 numbered units
(61.0 prep with rank-2-to-rank-7 redirect + 61.1
`GFM_TABLE_SCHEMA_OVERRIDES.tagNames` extension adding
`"caption"` + 6 NEW/updated schema-isolation tests + APPEND-
D-AS + 61.2 6 NEW caption edge-case + cumulative-schema-
extension-state tests + 61.3 hygiene + 61.4 gate). Second
realization of the Phase-57-derived schema-extension phase-
shape pattern in project history: `<caption>` element schema-
extension on `GFM_TABLE_SCHEMA_OVERRIDES` adding `"caption"`
to `tagNames` allow-list. Class + factory dispatch arm +
`PHASE_39_DEFAULT_ENABLED_SURFACES` constant + `remark-gfm`
parser all UNCHANGED. Pure schema-internal value evolution on
the existing slot. Mirrors Phase-57 shape verbatim with
`tagNames` evolution (vs Phase-57 `attributes` evolution).
First 2-realization for the schema-extension phase-shape
pattern in project history (extends Phase-57 record 1 → 2).
First state where the schema-extension pattern is observed
twice within the same consumer (tables); both realizations
within tables; both schema-ready-before-plugin. Tables
consumer gains 3rd evolution post-first-ship (Phase 43 cross-
surface + Phase 57 attributes + Phase 61 caption); first
state where TWO consumers have 3+ evolutions each (arxiv was
first via Phase 44 + 47 + 53; tables joins). First phase to
advance the framework beyond its Phase-60 capability ceiling
via new realization of an existing phase-shape pattern; first
post-capability-ceiling phase to ship a new realization.
Schema-ready-before-plugin state extended from attributes
(Phase 57) to tags (Phase 61) — first TAG-addition schema-
ready-before-plugin in project history. Closes ADR-0018
APPEND-D-Q item 4 `<caption>` at 22-phase carryover (Phase
39 → Phase 61) — NEW LONGEST ABSOLUTE APPEND-DEFERRAL
CLOSURE EVER OBSERVED (extends Phase-57 18-phase record by 4
phases). APPEND-D-Q closure trajectory: item 1 cross-surface
4-phase (Phase 43); item 3 attributes 18-phase (Phase 57);
item 4 caption 22-phase (Phase 61). D-Q becomes first D-
clause with 3-of-4 enumerated items closed within the
cadence (items 1 + 3 + 4 closed; only item 6 surface-
specific table schemas remaining). First D-clause with 3+
items closed in project history. APPEND-deferral closure
cadence sustained 20 phases — new longest sustained cadence
in project history (extends Phase-60 record 19 → 20). First
20-phase APPEND-deferral closure run. Composition preserved
(`CompositeExtensionRegistry` single-source schemaOverrides
rule per APPEND-D-C maintained). XSS defenses preserved
(`<caption>` is text-only; no caption-specific attributes
allowed; existing global `attributes["*"] = []` rule strips
any caption attribute including XSS-vector `onclick`). 28th
APPEND on ADR-0018 D-G (project record extends 27 → 28).
Nineteenth two-letter APPEND letter D-AS. Fifty-second NON-
§13 phase (extends Phase-60 half-century milestone to 52);
first 52-phase ledger-closure streak. 31st consecutive phase
without new B category — first 31-phase run (extends Phase-60
record 30 → 31). 26th consecutive no-new-ADR phase (Phase
36-61; extends Phase-60 record 25 → 26). 164th consecutive
103 kB First Load JS unit (extends from 159 at Phase-60
close). 56th "Continue" override invocation — sixth invocation
past the half-century threshold (Phase 55 = 50th; Phase 56 =
51st; Phase 57 = 52nd; Phase 58 = 53rd; Phase 59 = 54th;
Phase 60 = 55th; Phase 61 = 56th). 1452 tests across 75
vitest files (+12 / 0 vs Phase 60). 24 ADRs UNCHANGED. 7 DB
tables UNCHANGED. 9 migrations UNCHANGED. 14 env vars
UNCHANGED. 168 i18n keys per locale UNCHANGED. First Load JS
103 kB UNCHANGED. Middleware 160 kB UNCHANGED. OPEN_QUESTIONS
top-level Q-count 66 UNCHANGED. Phase-37+ candidate count 8
UNCHANGED. `MARKDOWN_EXTENSIONS` recognized single-value
arms 8 UNCHANGED (schema-extension within existing tables
consumer; no new arm). 0 new files (in-place schema-value
evolution in existing `tables.ts` + test additions). Zero
new ADRs / migrations / i18n keys / operational gates /
runtime deps. Phase 62 entry requires explicit human sign-
off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 60 → 61 delta complete.
- [x] Phase 62 entry conditions documented (10 ranked
      candidates).
- [x] Framework-state-note explicitly flagged (Phase 61
      advanced beyond Phase-60 capability ceiling via new
      realization of existing phase-shape pattern).
- [x] Smoke gates pass.
- [x] STOP at Phase 62 boundary per §12.
