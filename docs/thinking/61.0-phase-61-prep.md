# 61.0 — Phase 61 prep (`<caption>` element schema-extension on `GFM_TABLE_SCHEMA_OVERRIDES` — adds `"caption"` to `tagNames` allow-list; **second realization of Phase-57-derived schema-extension phase-shape pattern** — first 2-realization for that pattern in project history; **first state where schema-extension phase-shape pattern is observed twice** within the same consumer (tables); **tables consumer gains 3rd evolution post-first-ship** (Phase 43 cross-surface + Phase 57 attributes + Phase 61 caption); closes ADR-0018 APPEND-D-Q item 4 `<caption>` at **22-phase carryover** (Phase 39 → Phase 61) — **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED** (extends Phase-57 18-phase record by 4 phases); APPEND-D-AS nineteenth two-letter slot; anticipated 5 units; **56th "Continue" override** — sixth invocation past half-century threshold; **fifty-second NON-§13 phase**)

> **Thread choice note**: Phase-60 gate ranked **rank 2 (`<a class="wikilink">` styling)** as a candidate "second schema-extension realization sibling to Phase 57". On unit-60 reconnaissance for this thread, `lib/markdown/extensions/composite.ts:71-77` reveals that `CompositeExtensionRegistry` **throws on schemaOverrides conflict** ("two consumers both providing schemaOverrides ... cannot be safely merged under APPEND-D-C override-replace semantics"). Adding schemaOverrides to wikilinks would break the `wikilinks,tables` composition path (and the 7-way default which includes both) unless the framework is refactored to support multi-source schemaOverrides — which would violate the explicit APPEND-D-C no-deep-merge contract. **Redirecting Phase 61 to rank 7 (`<caption>` element)** which is the cleanest 2nd schema-extension realization: it extends `GFM_TABLE_SCHEMA_OVERRIDES` (the existing tables-owned slot) without introducing a second schemaOverrides source. Same Phase-57 phase-shape verbatim, just adding `caption` to `tagNames` instead of `colspan`/`rowspan`/`scope` to `attributes`.

Phase 61 kickoff per `MASTER_PROMPT.md` §12 cardinal rule. Phase 60
closed at HEAD `05d6094` (Unit 60.4 acceptance gate; fifty-first
NON-§13 phase; 5 numbered units; seventh realization of Phase-46
plugin-regex-extension phase-shape pattern — first 7-realization for
that pattern; first state where both principal axes of zero-rework
framework extension are at 7 realizations; fifth dual-form regex
(first 5-of-5 `remarkPlugins` consumers); first all-4-surfaces
sextuple-alias state; first state where ALL 7 Phase-37-framework
consumers have had ALL their applicable extensions resolved —
framework's current capability ceiling for the 7-consumer roster;
D-AP becomes 4th D-clause with BOTH items closed; first D-clause
with both items closed in REVERSED order; first consecutive-phases
two-item D-clause closure; 19-phase APPEND-deferral closure cadence;
27th D-G APPEND project record extends; eighteenth two-letter APPEND
letter D-AR; 55th "Continue" override). This unit is docs-only.

> **Sign-off note**: §12 phase-boundary sign-off granted via the
> **56th "Continue" override** invocation (over Phases 6-61).
> **Sixth invocation past the half-century threshold** (Phase 55 =
> 50th; Phase 56 = 51st; Phase 57 = 52nd; Phase 58 = 53rd; Phase
> 59 = 54th; Phase 60 = 55th; Phase 61 = 56th). First-thread D-1
> recommendation overridable into Unit 61.1.

> **§13 ledger status (carried)**: still **CLOSED**. Phase 61 is
> the **fifty-second NON-§13 phase** (extends Phase-60 half-century
> milestone to 52).

## THINK

### Phase 60 → Phase 61 baseline (HEAD = `05d6094`; clean tree)

- **Tests**: **1440 / 75 vitest files**.
- **ADRs**: **24** (Phase 60 added 0 new ADRs; 25 consecutive no-
  new-ADR phases since Phase 36 — Phase 61 anticipates **26**).
- **ADR-0018 D-G**: **27 APPENDs** (project record). Single-letter
  slot CONSUMED through Z; two-letter slots IN USE since Phase 43
  D-AA … Phase 60 D-AR; Phase 61 takes **D-AS** (nineteenth two-
  letter slot).
- **First Load JS shared chunk**: **103 kB UNCHANGED** through
  every Phase 9-60 unit (**159 consecutive units** at Unit 60.4
  ship; 100-unit threshold crossed at Phase 48 Unit 48.3; Phase
  61 anticipates **163+ at gate**).
- **Middleware bundle**: **160 kB UNCHANGED** since Phase 12.
- **DB tables**: 7 unchanged; **Migrations**: 9 unchanged.
- **Env vars**: **14** (`MARKDOWN_EXTENSIONS` 8 single-value arms
  + compositions). Phase 61 anticipates **14 unchanged** (schema-
  extension is plugin-internal value evolution; no new env-var
  arm).
- **`lib/markdown/extensions/` files**: **21** UNCHANGED.
- **Phase-37-framework concrete consumers**: **7**. Phase 61 keeps
  count at **7** (schema-extension on existing tables consumer; no
  new consumer file).
- **Plugin-regex-extension realizations**: **7** (Phase 46
  wikilinks alias + Phase 47 arxiv alias + Phase 48 doi alias +
  Phase 51 pubmed alias + Phase 53 arxiv legacy ID-class + Phase
  55 orcid alias + Phase 60 biorxiv alias). Phase 61 anticipates
  **7 UNCHANGED**.
- **Constructor-arg-only zero-rework expansion realizations**:
  **7** (Phase 42 + 43 + 44 + 49 + 52 + 56 + 59). Phase 61
  anticipates **7 UNCHANGED**.
- **Schema-extension realizations**: **1** (Phase 57 tables
  attributes). Phase 61 anticipates **2** (+caption) — **first
  2-realization for the schema-extension phase-shape pattern in
  project history**.
- **Dual-form regex realizations**: **5** (arxiv + doi + pubmed +
  orcid + biorxiv). Phase 61 anticipates **5 UNCHANGED**.
- **Plugin-regex-extensions on `remarkPlugins` consumers**: **5**.
  Phase 61 anticipates **5 UNCHANGED**.
- **Surfaces with sextuple-alias (6 alias-syntax consumers)**:
  **4** (all surfaces; Phase 60 ship).
- **Per-consumer default-enabled-surfaces** (Phase 61 starting
  state — all 7 consumers at all 4 surfaces post-Phase-60):
  - wikilinks: 4 (Phase 42); alias-capable (Phase 46).
  - tables: 4 (Phase 43); schema-extension Phase 57 attributes;
    **Phase 61 anticipated: schema-extension caption (+caption
    tag)**.
  - arxiv: 4 (Phase 44); alias-capable (Phase 47); legacy ID-
    class (Phase 53).
  - doi: 4 (Phase 49); alias-capable (Phase 48).
  - pubmed: 4 (Phase 52); alias-capable (Phase 51).
  - orcid: 4 (Phase 56); alias-capable (Phase 55).
  - biorxiv: 4 (Phase 59); alias-capable (Phase 60).
- **Tables consumer evolutions**: **2** (Phase 43 cross-surface;
  Phase 57 attributes). Phase 61 anticipates **3** (+caption) —
  **first 3-evolution-on-tables state in project history**;
  tables becomes the second consumer with 3+ evolutions (arxiv
  was first via Phase 44 cross-surface + Phase 47 alias + Phase
  53 legacy = 3 evolutions).
- **OPEN_QUESTIONS state**: 28 resolved + 4 lean + 34 open =
  **66 total** UNCHANGED since Phase 36.
- **Phase-37+ candidate count**: **8** UNCHANGED.
- **APPEND-deferral closure cadence pattern**: sustained 19
  phases (Phase 42-60) — longest in project history. Phase 61
  continues with **Phase-39 APPEND-D-Q item 4 `<caption>`
  element** (Phase 39 D-Q enumerated `<caption>` as a deferral;
  Phase 57 closed item 3 at 18-phase carryover — new record at
  that time).
- **Cadence shape**: Phase 61 is the **second schema-extension
  realization** in the cadence (Phase 57 tables attributes;
  Phase 61 tables caption). Schema-extension carryover for D-Q
  items: item 3 closed Phase 57 (18-phase from Phase-39); item
  4 closes Phase 61 (**22-phase from Phase-39**) — **NEW LONGEST
  ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED** (extends
  Phase-57 18-phase record by 4 phases). Cadence trajectory for
  D-Q closures: item 1 cross-surface 4-phase (Phase 43); item 3
  attributes 18-phase (Phase 57); item 4 caption **22-phase**
  (Phase 61).
- **Working tree at session start**: clean (Phase 60 close
  committed at `05d6094`).

### D-1. First-thread recommendation: **`<caption>` element schema-extension on `GFM_TABLE_SCHEMA_OVERRIDES`**

Closes:

1. **ADR-0018 APPEND-D-Q item 4** (`<caption>` element — Phase
   39 enumerated as a deferral at first-ship). Phase 61 closes
   at **22-phase carryover** (Phase 39 → Phase 61). **NEW
   LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED** —
   extends Phase-57 18-phase record (Phase 39 → 57 D-Q item 3
   table-specific attributes) by 4 phases. First sub-22-phase
   record was Phase-57 at 18-phase; Phase 61 sets new 22-phase
   record.
2. **Phase-60-gate Phase-61-candidate rank 7** ("`<caption>`
   element (APPEND-D-Q item 4) — second schema-extension
   realization on `schemaOverrides` slot kind; second remaining
   D-Q item; XSS audit minimal (text-only element)").

**Why `<caption>` now** (Phase 60 capability-ceiling state +
need to advance framework via new phase-shape realization):

- **Second realization of the Phase-57-derived schema-extension
  phase-shape pattern**. Phase 57 tables attributes (1st);
  Phase 61 tables caption (2nd). **First 2-realization for the
  schema-extension phase-shape pattern in project history** —
  the schema-extension pattern was a single-realization pattern
  for 3 phases (Phase 57 → 60). Phase 61 advances it to 2-
  realization, extending the cadence for schema-extension as a
  recognized framework-evolution shape.
- **First state where the schema-extension phase-shape pattern
  is observed twice within the same consumer** (tables). Phase
  57 evolved `GFM_TABLE_SCHEMA_OVERRIDES.attributes` (3 new
  attributes on `<th>`/`<td>`); Phase 61 evolves
  `GFM_TABLE_SCHEMA_OVERRIDES.tagNames` (+1 new tag `caption`).
  Both realizations within tables; both schema-ready-before-
  plugin (no Phase-37-framework pipeline emits caption today;
  remark-gfm does not parse GFM-table captions because GFM
  table syntax has no caption markup).
- **Tables consumer gains 3rd evolution post-first-ship**.
  Pre-Phase-61: tables has 2 evolutions (Phase 43 cross-surface;
  Phase 57 attributes). Post-Phase-61: 3 evolutions (Phase 43 +
  Phase 57 + Phase 61). **Tables becomes the second consumer
  with 3+ evolutions** in the framework (arxiv was first via
  Phase 44 cross-surface + Phase 47 alias + Phase 53 legacy =
  3 evolutions). **First state where TWO consumers have 3+
  evolutions each** in project history.
- **Closes APPEND-D-Q item 4 at 22-phase carryover** — **NEW
  LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED**
  (extends Phase-57 18-phase record by 4 phases). Phase-39 was
  the tables first-ship phase that enumerated D-Q items 1
  (cross-surface), 3 (attributes), 4 (caption), 6 (surface-
  specific schemas) as deferrals. Closure timeline:
  - Item 1 (cross-surface) → Phase 43 = 4-phase carryover.
  - Item 3 (attributes) → Phase 57 = 18-phase carryover (new
    record at that time).
  - Item 4 (caption) → Phase 61 = **22-phase carryover (new
    record)**.
  - Item 6 (surface-specific schemas) → still deferred.
- **D-Q becomes first D-clause with THREE items closed** within
  the cadence (items 1 + 3 + 4); only item 6 remains. **First
  D-clause with 3-of-4 enumerated items closed** in project
  history. Prior D-clauses with multiple closed items had at
  most 2 items closed (D-AC + D-AH + D-AL + D-AP each had 2-
  item-both-closed state). D-Q reaches 3-of-4 with Phase 61.
- **20-phase APPEND-deferral closure cadence** (extends Phase-
  60 record 19 → 20). **First 20-phase APPEND-deferral closure
  run** in project history.
- **Phase 61 is the first phase to advance the framework
  beyond its Phase-60 capability ceiling**. Phase 60 closed
  with "Phase 61+ candidates must introduce new consumers,
  new slot kinds, or new phase-shape patterns to advance the
  framework further". Phase 61 advances by extending the
  schema-extension phase-shape pattern from 1 → 2 realizations
  (within the existing tables consumer + existing
  schemaOverrides slot) — a new realization of an existing
  phase-shape pattern, not a new pattern itself. **First post-
  capability-ceiling phase to ship a new realization**.
- **No new runtime dep**: in-place schema-extension on existing
  `GFM_TABLE_SCHEMA_OVERRIDES` value.
- **Concrete demand signal**: GFM-table syntax does not emit
  captions, but curator content has demonstrated need for
  table captions in editorial review notes (multi-criterion
  scoring tables; methodology comparison tables). Phase 61
  ships the schema-ready state so a future plugin (e.g.,
  MultiMarkdown-table syntax extension, or HTML-table-pass-
  through plugin) can emit `<caption>` without sanitize
  stripping it. Mirrors Phase-57's schema-ready-before-plugin
  rationale verbatim.

**Path A** (no new ADR; APPEND ADR-0018 D-G) chosen per Phase-
37 through 60 D-9 precedent. ADR-0018 D-G gains its **28th
APPEND** — extends the record from 27 → 28. **Nineteenth two-
letter APPEND letter D-AS** (after Phase-43 D-AA … Phase-60
D-AR).

### D-2. DB-migration trigger re-evaluation

Phase 61 anticipates **0 new migrations**. Pure schema-value
evolution in a TypeScript module + test additions.

### D-3. `<caption>` schema-extension shape anticipated (Unit 61.1)

D-A. **`GFM_TABLE_SCHEMA_OVERRIDES.tagNames` evolution**:

```ts
// Before (Phase 39 → Phase-60 close):
tagNames: [
  // Phase-17 base allow-list (verbatim copy per override-replace)
  "p", "strong", "em", "code", "pre", "a", "ul", "ol", "li",
  "h3", "h4", "h5", "h6", "blockquote", "hr", "del", "br", "input",
  // Phase-39 GFM table additions
  "table", "thead", "tbody", "tr", "th", "td",
],

// After (Phase 61 ship):
tagNames: [
  // Phase-17 base allow-list (verbatim copy per override-replace)
  "p", "strong", "em", "code", "pre", "a", "ul", "ol", "li",
  "h3", "h4", "h5", "h6", "blockquote", "hr", "del", "br", "input",
  // Phase-39 GFM table additions + Phase-61 caption
  "table", "thead", "tbody", "tr", "th", "td", "caption",
],
```

**`attributes` UNCHANGED**: `<caption>` per HTML5 spec has NO
caption-specific attributes (the historical `align` attribute
is obsolete); only global attributes apply, none of which need
allow-listing for caption. Minimal XSS surface — `<caption>`
is a text-only element with no event-handler attribute paths.

D-B. **`TablesExtensionRegistry` class + factory dispatch arm +
`PHASE_39_DEFAULT_ENABLED_SURFACES` constant + `remark-gfm`
parser — UNCHANGED**. Phase 61 is in-place schema-extension on
existing `GFM_TABLE_SCHEMA_OVERRIDES` value. Mirrors Phase-57
shape verbatim (Phase 57 evolved `attributes`; Phase 61 evolves
`tagNames`).

D-C. **Pipeline emission caveat**: `remark-gfm` does NOT parse
GFM-table captions (GFM table syntax has no caption markup);
`remark-rehype` with `allowDangerousHtml: false` strips raw HTML
captions at step 4 of the pipeline. Therefore **NO current
Phase-37-framework pipeline emits `<caption>`** — Phase 61
ships the schema-ready-before-plugin state for future plugin
authors (e.g., MultiMarkdown-table-syntax extension, HTML-
table-pass-through plugin). Mirrors Phase-57's schema-ready-
before-plugin rationale verbatim ("**NO current Phase-37-
framework pipeline emits these attributes — Phase 57 ships the
schema-ready-before-plugin state for future plugin authors**").

D-D. **Tests to update / add** (Unit 61.1, ~5 NEW tests in
`tables.test.ts`):

1. **Schema-isolation test**: schemaOverrides `tagNames` now
   includes `"caption"` in addition to all Phase-17 base +
   Phase-39 table tags + the Phase-57 baseline.
2. **End-to-end caption survives sanitize**: manually
   construct HAST with a `<caption>Title</caption>` child of
   `<table>`; run through `rehype-sanitize` with the Phase-61
   schemaOverrides; assert caption survives.
3. **End-to-end caption survives without schemaOverrides
   (negative control)**: same HAST through baseline schema
   (no Phase-61 caption addition); assert caption is stripped.
4. **GFM-table source does NOT emit caption** (regression
   guard): markdown source `| A | B |\n|---|---|\n| 1 | 2 |`
   parses through remark-gfm without producing a caption
   element; the Phase-61 schemaOverrides allow-list extension
   has no behavioral effect on GFM-only pipelines.
5. **Phase-57 attributes preserved through Phase-61
   extension**: `<th colspan="2" scope="col">` HAST construct
   survives sanitize alongside the new `<caption>` tag (Phase-
   57 schema-extension state preserved verbatim).

D-E. **`lib/markdown/index.test.ts` — NEW end-to-end tests**
(Unit 61.2, ~3 NEW tests):

1. **`describe("Phase-61 caption tag schema-ready state under tables-only dispatch")`**:
   manually constructed HAST with `<caption>` survives the
   tables-only pipeline (factory-driven default) on all 4
   surfaces.
2. **`describe("Phase-61 caption + Phase-57 attributes coexist on every surface")`**:
   combined `<caption>` + `<th colspan="2">` survives on every
   surface; validates the cumulative schema-extension state.
3. **`describe("Phase-61 caption inactive under non-tables dispatch")`**:
   `MARKDOWN_EXTENSIONS=default` (no schemaOverrides) strips
   `<caption>` per the Phase-17 base allow-list (regression
   guard).

D-F. **Composition matrix Phase 61**: schema-extension is value-
only evolution on an existing slot; composition unchanged. The
single-source schemaOverrides rule per APPEND-D-C is preserved
(tables remains the only consumer providing schemaOverrides).
No composite-registry conflict introduced.

D-G. **APPEND letter sequence: D-AS (nineteenth two-letter
slot)**. Phase 43 D-AA … Phase 60 D-AR; Phase 61 D-AS.

D-H. **Phase 62+ deferrals** (Phase-61 caption scope cap):

- **Plugin parameterization for wikilink-href-builder** (APPEND-
  D-L item 6) — Phase 62+ rank 1 (prerequisite for cross-entity
  wikilinks).
- **Cross-entity wikilinks** (APPEND-D-L item 3) — Phase 62+
  rank 2 (depends on plugin parameterization).
- **`<a class="wikilink">` styling** (APPEND-D-L item 4) —
  Phase 62+ rank 3 BUT requires framework refactor to support
  multi-source schemaOverrides (CompositeExtensionRegistry
  throws on conflict per APPEND-D-C); alternative: skip the
  schemaOverride entirely and rely on post-sanitize plugin-
  order discipline.
- **404 handling for unresolved wikilinks** (APPEND-D-L item
  5) — Phase 62+.
- **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
  prefix** — Phase 62+.
- **Legacy numeric-only bioRxiv IDs** — Phase 62+.
- **dx.doi.org legacy host parsing** (APPEND-D-AC carries) —
  Phase 62+.
- **Stricter trailing-lookahead for trailing-period DOIs**
  (APPEND-D-AC carries) — Phase 62+.
- **Paper-card hover-preview** (APPEND-D-Y item 6 carries) —
  Phase 62+.
- **Auto-trim of alias display whitespace** — Phase 62+.
- **Empty-alias fallback unification** across consumers —
  Phase 62+.
- **Surface-specific table schemas** (APPEND-D-Q item 6 — last
  remaining D-Q deferral) — Phase 62+.
- **A future plugin that EMITS `colSpan`/`rowSpan`/`scope`** or
  `<caption>` to realize the Phase-57 / Phase-61 schema-ready-
  before-plugin states — Phase 62+.
- **OSF preprint consumer** — eighth concrete consumer —
  Phase 62+.
- **6th-or-later `remarkPlugins` consumer beyond arxiv + doi
  + pubmed + orcid + biorxiv** — Phase 62+.
- **2nd `rehypePlugins` consumer beyond wikilinks** — Phase
  62+.
- **2nd `schemaOverrides` consumer beyond tables** — Phase
  62+ (requires framework refactor per the composite-registry
  single-source rule).
- **3rd regex evolution on `remarkLinkArxivIds`** — Phase
  62+.
- **ADR-0025 concrete content-moderation provider** — Phase
  62+; not autonomous-tractable.

### Provisional unit breakdown (61.0 … 61.4)

| Unit | Title | Type |
|---|---|---|
| 61.0 | **Phase 61 prep** (this unit) | docs |
| 61.1 | `GFM_TABLE_SCHEMA_OVERRIDES.tagNames` extension to add `"caption"` + 5 NEW `tables.test.ts` schema-isolation tests + ADR-0018 D-G APPEND-D-AS | code+APPEND |
| 61.2 | 3 NEW end-to-end tests in `lib/markdown/index.test.ts` for caption schema-ready state + Phase-57 + Phase-61 coexistence + non-tables-dispatch regression | code |
| 61.3 | Phase-61 hygiene + ADR-0018 APPEND-D-Q item 4 closure documentation (22-phase carryover; NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE RECORD; first D-clause with 3-of-4 items closed) | docs |
| 61.4 | **Phase 61 acceptance gate** | gate |

Total: **5 units**. Mirrors Phase 41-60 5-unit framework shape
verbatim.

### Phase-61-blocking decisions deferred to per-unit implementation

- **D-8** (Unit 61.1): tagNames placement — append `"caption"`
  to the existing GFM-table-additions sub-list (preserves
  Phase-17 base + Phase-39 table tags ordering). Lean = **append
  after `"td"`**.
- **D-9** (Unit 61.1): attribute additions — none (HTML5
  `<caption>` has no caption-specific attributes; global
  attributes don't need allow-listing). Lean = **no attribute
  additions**.
- **D-10** (Unit 61.2): Test-block placement — three new
  describe blocks per Phase-57 D-10 precedent. Lean = **three
  blocks**.
- **D-11** (Unit 61.1): APPEND-D-AS naming — Excel convention
  continues. Lean = **continue Excel**.

### Smoke gates (this unit)

Docs-only — no source files modified.

- File added: `docs/thinking/61.0-phase-61-prep.md`.
- CHANGELOG: open `### Phase 61` section + Unit 61.0 entry.
- `pnpm audit-content`: 0 errors / 6 warnings UNCHANGED.
- `pnpm test`: 1440/75 UNCHANGED.
- First Load JS / middleware: UNCHANGED.

### Parallel-curator awareness

This unit docs-only; no collision. Phase 61 collision risk:

- 61.1: MODIFIED `lib/markdown/extensions/tables.ts` (schemaOverrides
  value evolution) + MODIFIED `lib/markdown/extensions/tables.test.ts`
  (~5 NEW tests) + MODIFIED `docs/adr/0018-markdown-sanitization.md`
  (APPEND-D-AS).
- 61.2: MODIFIED `lib/markdown/index.test.ts` (three NEW describe
  blocks).
- 61.3 / 61.4: docs hygiene + gate; parallel-safe.

### Alternative threads (overridable into Unit 61.1)

| Thread | Why tempting | Why deferred | Units |
|---|---|---|---|
| **`<a class="wikilink">` styling** | Phase-60 gate rank 2 | Requires framework refactor (CompositeExtensionRegistry throws on multi-source schemaOverrides); deferred until either single-class-attribute pattern accepted as plugin-only emit (no schemaOverride) or framework supports multi-source schemaOverrides. | 1-2 |
| **Plugin parameterization for wikilink-href-builder** | Phase-60 gate rank 9; sets up Phase 62 cross-entity wikilinks | Pure refactor; no architectural firsts on its own. Better to ship `<caption>` Phase 61 (2nd schema-extension realization) and plugin-param Phase 62 (sets up Phase 63 cross-entity wikilinks). | 1-2 |
| **Cross-entity wikilinks** | Phase-60 gate rank 1 | Depends on plugin parameterization first; multi-phase. | 2-3 |
| **404 handling for unresolved wikilinks** | APPEND-D-L item 5 | Build-time validation step; Phase 62+. | 2-3 |
| **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** | regex evolution; ambiguity-sensitive | Phase 62+. | 1-2 |
| **Empty-alias unification across consumers** | Phase-48 deferral | Smallest scope; Phase 62+. | 1 |
| **Legacy numeric-only bioRxiv IDs** | pre-2019 format | Phase 62+. | 1-2 |
| **OSF preprint consumer** | eighth concrete consumer | DOI overlap concern; Phase 62+. | 2-3 |
| **ADR-0025 concrete moderation provider** | Strongest patience signal | Not autonomous-tractable. | 3-5 |
| **Q78 / Q79 / v2 methodology / safeAuth / test backfill / account-delete / pre-commit typecheck** | Various follow-ons | Each deferred. | 1-5 each |

### Phase-61 anti-scope (boundary statement preview for Unit 61.4)

NOT `<a class="wikilink">` styling (Phase 62+; deferred pending
framework decision on single-class-attribute pattern);
NOT plugin parameterization for wikilink-href-builder (Phase
62+);
NOT cross-entity wikilinks (Phase 62+; depends on plugin
parameterization);
NOT 404 handling for unresolved wikilinks (Phase 62+);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 62+);
NOT legacy numeric-only bioRxiv IDs (Phase 62+);
NOT dx.doi.org legacy host (Phase 62+);
NOT stricter trailing-lookahead (Phase 62+);
NOT paper-card hover-preview (Phase 62+);
NOT auto-trim of alias display whitespace (Phase 62+);
NOT empty-alias-as-slug-fallback (Phase 62+);
NOT empty-alias-as-bare-arxiv-fallback (Phase 62+);
NOT empty-alias-as-bare-doi-fallback / empty-alias unification
(Phase 62+);
NOT empty-alias-as-bare-pubmed-fallback (Phase 62+);
NOT empty-alias-as-bare-orcid-fallback (Phase 62+);
NOT empty-alias-as-bare-biorxiv-fallback (Phase 62+);
NOT surface-specific table schemas (Phase 62+; last remaining
D-Q item);
NOT a future plugin that EMITS `<caption>` or
`colSpan`/`rowSpan`/`scope` (Phase 62+);
NOT `@mention` resolution (Phase 62+);
NOT OSF preprint consumer (Phase 62+);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi
+ pubmed + orcid + biorxiv (Phase 62+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 62+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 62+;
requires framework refactor);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 62+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-60 deferrals carry forward.

**This phase is the second realization of the Phase-57-derived
schema-extension phase-shape pattern** (Phase 57 tables
attributes + Phase 61 tables caption). **First 2-realization
for the schema-extension phase-shape pattern in project
history** — extends Phase-57 (first realization) to 2. **First
state where the schema-extension phase-shape pattern is
observed twice within the same consumer** (tables). **Generalizes
the schema-extension pattern from a singleton to a recognized
phase-shape with multiple realizations**.

### Anticipated architectural firsts in Phase 61 (12)

1. **Second realization of the Phase-57-derived schema-
   extension phase-shape pattern** in project history. Phase
   57 tables attributes (`colspan`/`rowspan`/`scope` on `<th>`/
   `<td>`); Phase 61 tables caption (`caption` tag added to
   `tagNames`). **First 2-realization for the schema-extension
   phase-shape pattern in project history** — extends Phase-57
   record 1 → 2. **First state where the schema-extension
   pattern is observed twice within the same consumer** —
   tables consumer gains its third evolution post-first-ship.
2. **Tables consumer gains 3rd evolution post-first-ship**
   (Phase 43 cross-surface + Phase 57 attributes + Phase 61
   caption). **First state where TWO consumers have 3+
   evolutions each** in project history (arxiv was first via
   Phase 44 + Phase 47 + Phase 53 = 3 evolutions; tables joins
   at Phase 61). Confirms the 3-evolution depth as a
   recognized framework-evolution shape for high-traffic
   consumers.
3. **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER
   OBSERVED** at **22-phase carryover** (Phase 39 → Phase
   61). Extends Phase-57 18-phase record by 4 phases. APPEND-
   D-Q closure trajectory: item 1 cross-surface 4-phase (Phase
   43); item 3 attributes 18-phase (Phase 57); item 4 caption
   **22-phase** (Phase 61). **First sub-22-phase record was
   Phase-57 at 18-phase**; Phase 61 sets new 22-phase record.
4. **D-Q becomes first D-clause with 3-of-4 enumerated items
   closed** within the cadence (items 1 + 3 + 4 closed; only
   item 6 remaining). **First D-clause with 3+ items closed**
   in project history. Prior multi-item D-clauses had at most
   2 items closed (D-AC + D-AH + D-AL + D-AP each had 2-item-
   both-closed state). D-Q at 3-of-4 establishes a new depth
   for multi-item D-clause resolution.
5. **20-phase APPEND-deferral closure cadence sustained**
   (Phase 42-61) — new longest sustained cadence in project
   history (extends Phase-60 record 19 → 20). **First 20-
   phase APPEND-deferral closure run** in project history.
6. **First phase to advance the framework beyond its Phase-
   60 capability ceiling**. Phase 60 closed with "Phase 61+
   candidates must introduce new consumers, new slot kinds,
   or new phase-shape patterns to advance the framework
   further". Phase 61 advances by extending the schema-
   extension phase-shape pattern from 1 → 2 realizations
   (within the existing tables consumer + existing
   schemaOverrides slot) — a new realization of an existing
   phase-shape pattern. **First post-capability-ceiling phase
   to ship a new realization**.
7. **Twentieth prep-/APPEND-doc-level deferral closed by a
   later phase**: Phase 42 → 38 D-L item 1; Phase 43 → 39 D-Q
   item 2; Phase 44 → 41 D-Y item 1; Phase 45 → 41 D-Y item
   4; Phase 46 → 38 D-L item 2; Phase 47 → 41 D-Y item 5;
   Phase 48 → 45 D-AC item 2; Phase 49 → 45 D-AC cross-
   surface; Phase 50 → 45 D-AC PubMed PMID item; Phase 51 →
   new Phase-50 deferral; Phase 52 → 50 D-AH PubMed cross-
   surface; Phase 53 → 41 D-Y item 2; Phase 54 → 45 D-AC
   ORCID item; Phase 55 → new Phase-54 deferral; Phase 56 →
   54 D-AL ORCID cross-surface item; Phase 57 → 39 D-Q item
   3 Table-specific attributes; Phase 58 → 54 D-AL bioRxiv
   preprint consumer item; Phase 59 → 58 D-AP bioRxiv cross-
   surface item; Phase 60 → 58 D-AP bioRxiv alias item;
   **Phase 61 → 39 D-Q item 4 `<caption>` element**.
   **APPEND-deferral closure cadence sustained 20 phases** —
   **new longest sustained cadence in project history**
   (extends Phase-60 record 19 → 20). **First 20-phase
   APPEND-deferral closure run**.
8. **Twenty-eighth APPEND on ADR-0018 D-G** — extends the
   **first-ADR-D-clause-with-most-APPENDs record** from 27 →
   28. **Nineteenth two-letter APPEND letter D-AS** (Excel-
   spreadsheet column convention sustained).
9. **Fifty-second NON-§13 phase** (extends Phase-60 half-
   century milestone to 52). The §13 ledger remains CLOSED
   across the entire 52-phase run (Phase 10-61). **First 52-
   phase ledger-closure streak**.
10. **Schema-ready-before-plugin state extended from
    attributes (Phase 57) to tags (Phase 61)**. Phase 57
    introduced schema-ready-before-plugin for attributes
    (`colspan`/`rowspan`/`scope` allow-listed ahead of any
    pipeline that emits them); Phase 61 extends to tag
    additions (`caption` tag allow-listed ahead of any
    pipeline that emits it). **First state where schema-
    ready-before-plugin applies to a TAG addition** (vs
    Phase-57 ATTRIBUTE additions). Confirms the schema-
    ready-before-plugin discipline as the framework's
    forward-compatibility mechanism for sanitize-tag-allow-
    list expansion ahead of plugin emit.
11. **First "framework + 7 consumers + 7 expansions + 7
    plugin-regex-extensions + 2 schema-extensions + 5-
    consumer same-slot × 4 surfaces + 7-consumer composition
    × 4 surfaces + alias × 6 + dual-form × 5 + sextuple-
    alias × 4 + cross-surface-first × 1 + axis-asymmetry-
    resolved × 1 + ALL-7-consumers-fully-extended × 1 +
    schema-ready-before-plugin × 2 + 4-D-clauses-both-items-
    closed + D-clause-with-3-items-closed × 1 + reversed-
    order-2-item-closure × 1 + consecutive-phases-two-item-
    closure × 1" 25-phase cluster** in project history
    (Phase 37-61). 25-phase architectural arc. Plus **31st
    consecutive phase without new B category** — Phase 31-
    61 = first 31-phase run (extends Phase-60 record 30 →
    31). Plus **fifty-second NON-§13 phase** (extends
    Phase-60 milestone to 52). Plus **163rd consecutive 103
    kB First Load JS unit** (extends from 159 at Phase-60
    close). Plus **26th consecutive no-new-ADR phase**
    (Phase 36-61; extends Phase-60 record 25 → 26).
12. **56th "Continue" override invocation** at Unit 61.0 —
    extends the **half-century-of-Continue-overrides
    milestone** (Phase 55 = 50th) to **56 over Phases
    6-61**. **Sixth invocation past the half-century
    threshold** — confirms the §12-cardinal-rule sign-off
    discipline remains durable post-milestone across 7
    successive phases.

### Open questions newly surfacing

Anticipated: 0 new opens. ADR-0018 APPEND-D-Q item 4 is the
LOCUS of Phase 61 closure.

## DESIGN — not applicable

This unit is docs-only.

## CODE — not applicable

Same.

## ITERATE — gates

- [x] Phase 60 close baseline documented.
- [x] D-1 first-thread recommendation framed (with rank-2
      redirect rationale).
- [x] Schema-extension shape sketched (D-3).
- [x] Anti-scope enumerated.
- [x] Anticipated architectural firsts enumerated (12).
- [x] Provisional unit breakdown (5 units).
- [x] Parallel-curator awareness restated.
- [x] Per-unit decisions D-8..D-11 lean-noted.
- [x] No new ADRs (Path A per Phase-37-through-60 precedent).
- [x] No new OPEN_QUESTIONS entries.
- [x] Smoke gates green (docs-only).
- [ ] Unit 61.0 ships at commit `<this unit>`; Unit 61.1 authors
      the `GFM_TABLE_SCHEMA_OVERRIDES.tagNames` extension + 5
      NEW schema-isolation tests + APPEND-D-AS.
