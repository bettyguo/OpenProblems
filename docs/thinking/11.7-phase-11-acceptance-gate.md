# 11.7 — Phase 11 acceptance gate (Rating-challenge submission write-path)

Phase-11 closing unit. Mirrors Units 1.12 / 2.13 / 3.13 / 4.13 / 5.13 /
6.10 / 7.11 / 8.9 / 9.9 / 10.5. Verifies every deliverable Phase 11
pulled (the **Rating-challenge submission write-path** — honored-deferral
pick per Unit 11.0 D-1 + Phase-9 Unit 9.0 D-5) is operational locally at
HEAD, emits the cross-phase roll-up, and lists Phase-11 follow-ons that
survive into Phase 12+.

## THINK

### Scope clarification

Phase 11 is the **second NON-§13 phase** (Phase 10 was the first; both
pull from the Phase-9 follow-on catalog + Phase-9 Unit 9.0 D-5 explicit
deferral). The §13 ledger has been **CLOSED since Unit 9.9** — Phase 6
shipped Discussions; Phase 7 + 8 shipped Bilingual; Phase 9 shipped
Auth + read+write API. Phase 11's "Rating-challenge submission write-path"
is the **second write-path** in the project (first was `watchlist` in
Phase 9 Unit 9.6); it validates the architectural pattern Phase 9
introduced.

### Phase-11 deliverables — local pass status

| Surface                                                                                  | Phase-11 unit | Status |
|------------------------------------------------------------------------------------------|---------------|--------|
| `ratingChallenges` Drizzle table (UUID PK; userId FK; problemSlug plain text; dimension/proposedValue/rationale TEXT; status default "submitted"; createdAt timestamp_ms) | Unit 11.1     | ✓ schema landed; `0002_rating_challenges` migration generated; FK with cascade; per Q56 lean / ADR-0013 D-F. |
| `lib/rating-challenges/` helpers (`DIMENSIONS` const + `Dimension` type + `isValidDimension` + `validateProposedValue` + `validateRationale` + `submitChallenge` + `getUserChallenges` + `UserChallenge` type) | Unit 11.2     | ✓ pure-function half exercised by Unit 11.2's API route tests; DB-helper half exercised at request time. |
| `POST /api/v1/rating-challenges` route (401 / 400 field-specific / 201) | Unit 11.2     | ✓ 9 tests covering each exit shape per validation gate; mocked `@/lib/auth` + partially-mocked `@/lib/rating-challenges` via `importOriginal` to keep validation real. |
| Inline collapsible submission form on `/[locale]/problems/[slug]` (server-rendered; server-action driven; HTML5 + server-side validation; redirect-with-search-param error reporting) | Unit 11.3 | ✓ `<RatingChallengeForm>` server component; three render branches (signed-out CTA / signed-in `<details>` / signed-in with error open); `submitAction` server-action; injected as section 8a on problem detail page. |
| `messages.rating_challenge.*` (22 keys per locale: heading + description + sign-in prompt + open-form summary + submitted banner + dimension/proposedValue/rationale form labels + submit + error-field translations) | Unit 11.3     | ✓ EN + FR; FR uses "Contester cette notation" + "Argumentaire" register. |
| Profile page extension: "Your rating challenges" section with dense-list rendering of user's submissions | Unit 11.4     | ✓ injected below watching section; reuses `rating_challenge.dim_*` keys via second `getTranslations` call; truncates rationale at 200 chars; per-row date stamp + status pill + dimension label + proposed value. |
| `messages.profile.*` extension (5 new keys per locale) | Unit 11.4     | ✓ `challenges_heading` / `challenges_aria_label` / `challenges_empty_message` / `challenges_empty_cta` / `challenges_status_submitted`. |
| Phase-11 hygiene status pass (Class A/B/C catalog) | Unit 11.5     | ✓ 4 carried Class A items (Q54 / Q55 / CI AUTH_SECRET / db:migrate doc); 12 Class B follow-ons; 19 Class C carryovers. |
| OPEN_QUESTIONS hygiene + ADR review (Q57 + Q58 surfaced; zero net promotions; 13 ADRs unchanged) | Unit 11.6     | ✓ Q57 (curator review pipeline shape) + Q58 (challenge visibility) added as `open` with detailed leans; ADRs 0001-0013 unchanged in body. |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: 19 URLs unchanged
  in `lighthouserc.json`. The new `RatingChallengeForm` injected on
  problem detail page is server-rendered (no client bundle delta;
  shared chunk holds at 103 kB). LHCI run lands on first PR with the
  unit-11.x commits.
- **W3C feed validator on `/api/v1/rss.xml` + `/api/v1/digest/<slug>`**:
  Phase-3/5/6/8 compound deferral; first preview deploy + Q47 unblock.
- **Visual-regression baselines** for `/profile` + problem-detail-page
  with form expanded — deferred Phase-11 follow-on; no Playwright spec
  changes this phase.
- **No auto-merge** (ADR-0009): Phase 11 added zero LLM-translated
  content; all surfaces are code or curator-typed translation strings.
- **File-first / no DB** (ADR-0004): held for content. The new
  `ratingChallenge` table sits under ADR-0013 D-F (USER-STATE only);
  content (`content/problems/*`) stays file-first. Acceptance of a
  challenge in Phase 12+ Q57 lean WILL emit a new rating-action YAML
  file under `content/problems/<slug>/ratings/` — preserving ADR-0004
  + ADR-0005 immutability.
- **First Load JS shared chunk = 103 kB**: **HELD** through every
  Phase-11 unit. The new server-rendered surfaces add zero client
  bundle weight; the form posts via server-action; the profile-page
  extension is a server-side list render.
- **Middleware bundle = 159 kB**: HELD since Phase 9 close. Phase 11
  added no middleware composition changes.

### Phase-11 unit summary (8 commits — second NON-§13 phase)

| Unit | Commit    | Title                                                                                            |
| ---- | --------- | ------------------------------------------------------------------------------------------------ |
| 11.0 | `bb6fc3f` | Phase 11 prep (Rating-challenge submission; honored-deferral pick)                              |
| 11.1 | `4cf6016` | DB scaffold: `ratingChallenges` table + `0002_rating_challenges` migration                       |
| 11.2 | `98502d3` | `lib/rating-challenges/` helpers + `POST /api/v1/rating-challenges` route + tests                |
| 11.3 | `23197ca` | Submission form on problem detail page + `messages.rating_challenge.*` (EN + FR)                  |
| 11.4 | `68d13ac` | Profile page extension: "Your rating challenges" section                                          |
| 11.5 | `da0b1a5` | Phase-11 hygiene status pass (Class A/B/C catalog)                                                |
| 11.6 | `b0f0af2` | OPEN_QUESTIONS hygiene + ADR review (Q57 + Q58 surfaced)                                          |
| 11.7 | _this_    | Phase 11 acceptance gate                                                                          |

Total: **8 numbered units, all shipped**. Matches Unit 11.0 prep's
8-unit breakdown exactly. **Zero deferrals**, **zero scope drift**
across the phase.

### State at HEAD (Unit 11.7)

- **Content** (no count change vs Phase-8 close; Phases 9 + 10 + 11
  added no content):
  - 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors
    / 14 institutions / 20 rating actions / 4 issue templates /
    methodology v1 (EN + FR) / contributing v1.0 (EN) + v1.1 (EN + FR)
    / 2 `entries.json` files.
  - **203 schema-validated content files + 36 raw MDX = 239 total raw
    content files** (UNCHANGED).
- **New Phase-11 code surfaces** (relative to Phase-10 close at
  `0a55bfd`):
  - `lib/rating-challenges/index.ts` (Unit 11.2) — 5 named exports +
    type.
  - `app/api/v1/rating-challenges/route.ts` + `route.test.ts` (Unit
    11.2) — POST endpoint + 9 tests.
  - `components/rating-challenge-form/index.tsx` (Unit 11.3) — server
    component + inline server-action.
  - `app/[locale]/problems/[slug]/page.tsx` (edit; Unit 11.3) — accepts
    `searchParams` + injects `<RatingChallengeForm>` as section 8a.
  - `app/[locale]/profile/page.tsx` (edit; Unit 11.4) — adds "Your
    rating challenges" section below watching section.
  - `lib/db/schema.ts` (edit; Unit 11.1) — adds `ratingChallenges`
    table export.
  - `lib/db/migrations/0002_rating_challenges.sql` (Unit 11.1) — 1
    table, 1 FK with cascade, 0 indexes.
  - `lib/db/migrations/meta/0002_snapshot.json` (Unit 11.1) — Drizzle
    snapshot.
  - `messages/{en,fr}.json` (edits; Units 11.3 + 11.4) — +27 keys per
    locale; +54 keys total.
- **~590 prerendered pages + 3 dynamic API routes** (+1 dynamic API
  route `ƒ /api/v1/rating-challenges`). Page-route count unchanged from
  Phase-10 close. First Load JS shared chunk = **103 kB UNCHANGED**.
- **403/403 vitest tests across 46 files** (was 394/45 at Phase-10
  close); **+9 tests / +1 test file this phase** (Unit 11.2's API route
  tests).
- `pnpm validate-content` → **203 files** green (unchanged).
- `pnpm audit-content` → **0 errors / 6 warnings** (Q32-expected
  baseline since Phase 2; unchanged).
- `pnpm typecheck` clean. `pnpm build` clean (modulo one transient
  Windows `.next` chunk-not-found flake observed in Unit 11.4 build
  smoke; resolved by clean-rebuild; unrelated to Phase 11 changes).
- **5 visualizations live** (unchanged from Phase 4).
- **13 ADRs** (unchanged from Phase 9 close; Phases 10 + 11 added
  zero new ADRs).
- **Dependencies**: **+0 net in Phase 11** (last addition was Phase 9's
  5-package burst).
- **`lighthouserc.json`** enrols **19 URLs** (unchanged from Phase-8
  close).
- **OPEN_QUESTIONS state**: 19 resolved + 4 decided-as-lean + 30 open
  = **53 total entries** (was 51 at Phase-10 close; +2 from Q57 + Q58
  surfacing in Unit 11.6).
- **DB schema tables**: 6 (`user`, `account`, `session`,
  `verificationToken`, `watchlist`, `ratingChallenge`). Phase 11 added
  `ratingChallenge`.
- **Migrations**: 3 cumulative (drizzle-kit 0-indexed monotonic
  sequence — `0000_initial_auth`, `0001_watchlist`,
  `0002_rating_challenges`).
- **Middleware bundle**: **159 kB UNCHANGED** since Phase 9.

### `lighthouserc.json` final state

Phase 11 changes: **none**. URL count holds at **19** at Phase-11 close.
Same cohort-strategy as prior phases. The new auth-aware
problem-detail-page section + new profile-page section are observable
on existing URLs (`/en/problems/[any-slug]` + `/en/profile`); no new
enrolment needed.

### Phase-11 follow-ons that survive the gate (non-blocking)

Documented in detail in Unit 11.5's hygiene status pass (12 Class B +
4 Class A + 19 Class C items). Critical-path subset for Phase 12+
planning:

1. **Q57 — Curator review pipeline** (Class B item 1). The largest
   Phase-12+ architectural unblock. Closes the "what happens after
   submission" gap.
2. **Q58 — Challenge visibility to non-author users** (Class B item 2).
   Couples to Q57.
3. **Form-state preservation on validation error** (Class B item 3) —
   UX papercut; needs `useActionState` client island (~3-5 kB delta).
4. **Per-dimension dynamic input format** (Class B item 4) — couples
   to item 3.
5. **Withdraw-own-challenge UI** (Class B item 5) — small 2-3 unit
   sub-phase.
6. **Per-challenge detail page** (Class B item 6).
7. **Orphan-row cleanup script** for `ratingChallenge` + `watchlist`
   (Class B item 11) — single script handles both tables.
8. **Q54 + Q55 operational unblocks** (Class A items 1 + 2; carried
   since Phase 9). Phase-9-Phase-10-Phase-11 architectural surfaces
   are complete; only the operational env config blocks end-to-end
   smoke on Vercel.
9. **CI dummy `AUTH_SECRET`** (Class A item 3; carried).
10. **`pnpm db:migrate` doc for new contributors** (Class A item 4) —
    DB now has 3 migrations to run.

### Pre-existing follow-ons that survived Phase 11 (carryover)

Unchanged from Unit 10.5 acceptance gate:

- Orphan `components/domain-tile-grid/` deletion (curator
  authorization).
- `entries.json` content backfill on 8 problems (editorial + API).
- `<managingEditor>` on RSS feeds (Q33 / Q44; Q2 DNS).
- W3C RSS validator pass on `/api/v1/rss.xml` (Phase-3 carryover).
- W3C validator pass on Phase-5 digest endpoints.
- `pnpm clean-drafts` script (operational signal not yet met).
- Phase-2 ROR-ID + InstaDeep orphan (curator hygiene).
- `NEXT_PUBLIC_GISCUS_REPO_ID` + repo-Discussions enablement
  (Phase-6 Q47 operational gate).
- Real-API integration smoke for `lib/discussions/github-graphql.ts`
  (Q47 + `GITHUB_TOKEN`).
- HTML shell migration + `<html lang={locale}>` + SiteHeader under
  `NextIntlClientProvider` — STILL ON HOLD per parallel-session
  preservation signal.
- Fallback-hint UI for `didFallback === true`.
- `messages.{contributing,methodology,…}.*` chrome strings + FR bulk
  backfill + StatusPill localization + nav labels via
  `useTranslations` (Q51 curator-track horizon).
- Trailing-slash normalization for `NEXT_PUBLIC_SITE_URL`.
- Per-entry sitemap `lastModified` / `changeFrequency` / `priority`.
- Public profile page at `/[locale]/u/[handle]` (Phase-10 Class B
  item 1).
- Watchlist count display on `/problems` index (Phase-9 Class B item
  4).
- Multi-provider OAuth expansion (Phase-9 Class B item 8).
- First LHCI run validating auth-aware SiteHeader on `/en/`.
- Middleware-based auth-route protection (Phase-9 Class B item 12) —
  threshold still ≤ 1 protected page route (Phase 11's new API route
  is API-only, not a page route); deferral holds.

Documented in detail in Unit 11.5 Class C carryover catalog.

### Phase 12+ entry conditions

Per §12 cardinal rule: **explicit human sign-off required** at this
acceptance gate. Phase 11 closes the "second write-path" architectural
pattern + the honored-deferral pick from Phase-9 Unit 9.0 D-5.

**Phase 12+ candidate threads** (each overridable):

- **Curator review pipeline** (Q57): the highest-value Phase-12+
  thread. Lands the curator-side data layer + columns migration
  (`0003_rating_challenge_review`) + curator dashboard route
  (`/[locale]/curator/challenges`) + Phase 12+'s lean of status
  transitions limited to `submitted → under_review → accepted | rejected`.
  ~8-10 unit phase. Triggers ADR-0014 (curator review pipeline) +
  middleware-based auth-route protection (third protected route
  crosses the threshold). **Strongest claim by sequential
  thread-closure precedent**: Phase 11 introduced the submission
  surface; Phase 12 closes the review surface.

- **Subscriber-list (third-party email)** (Phase-5 D-4 punt): ~6
  units; decoupled from auth via third-party (Buttondown / Resend /
  ConvertKit / Loops); ADR-0014 (alternative path) for the provider
  choice. Couples to Q57's "email notifications when challenge is
  reviewed" follow-on.

- **Public profile page at `/[locale]/u/[handle]`** (Phase-10 Class B
  item 1): ~3-4 units; builds on Phase-10's profile route.

- **Multi-provider OAuth expansion** (Google / GitLab / email-link):
  ~3-4 units; needs follow-on ADR lifting ADR-0012 D-B's restriction.

- **HTML shell migration + Unit 8.4 unblock**: STILL ON HOLD per
  parallel-session preservation signal.

DB-migration trigger re-eval mandatory at Phase 12 kickoff per Unit
11.0 D-2 (procedural; trigger (a) already fired in Phase 9; trigger
(b) cold at ~1.656%).

### Cross-phase milestone

This commit closes the **Rating-challenge submission write-path** —
the second write-path in the project (first was watchlist in Phase 9
Unit 9.6). 8 commits + 0 new ADRs + 0 new dependencies + 0
client-bundle regressions (103 kB First Load JS preserved end-to-end)
+ 0 test regressions + 9 new tests + +2 OPEN_QUESTIONS (Q57 + Q58
surfaced; zero net promotions) + 1 new DB table (`ratingChallenge`)
+ 1 new migration (`0002_rating_challenges`) + 27 new i18n keys per
locale (54 total across EN + FR).

**§13 ledger remains CLOSED** (closed at Unit 9.9 with Phase 9
acceptance gate). Phase 11 is the **second NON-§13 phase**.

**Architectural pattern validation**: Phase 11 confirmed the
Phase-9-introduced write-path pattern generalizes. Both write-paths
(watchlist + rating-challenge) follow the same shape: auth-required
+ Drizzle-helpers + REST API route + server-action UI consumer +
i18n message namespace + profile-page surface (Phase 10 for
watchlist; Phase 11 for rating-challenge). Phase 12+ can confidently
build on this pattern.

### Phase-11 over-vs-under against the 11.0 plan

Unit 11.0 prep scoped an 8-unit phase (11.0–11.7). Actual: 8 units
shipped, **zero deferrals**, **zero scope drift**. Per-unit landings
matched the prep table exactly. The lone tactical adjustment was Unit
11.1's omission of a separate THINK doc (Phase-11.0 D-3 was detailed
enough to subsume it); CHANGELOG entries documented the THINK-doc
omission inline per the project's "THINK artifact: omitted" convention.

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 11.7 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 403/403 across 46 files unchanged (no test files
  touched in 11.7).
- `pnpm build` → ~590 prerendered pages + 3 dynamic API routes
  unchanged. First Load JS shared chunk = 103 kB unchanged. Middleware
  bundle = 159 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-11 close: marks Unit 11.7 as the acceptance gate; cites the
Phase-11 deliverable status table (every row ✓; zero deferrals);
emits the cross-phase state-at-HEAD (10 problems / etc.; ~590
prerendered pages + 3 dynamic API routes; 103 kB First Load JS; 403
tests across 46 files; 13 ADRs; 19 LHCI URLs; +0 new deps; 6 DB
tables; 3 migrations; 53 OQ entries); enumerates Phase-11 follow-ons
(Q57 curator review pipeline; Q58 challenge visibility; form-state
preservation; per-dimension dynamic input; withdraw UI; per-challenge
detail page; orphan-row cleanup; Q54 + Q55 + CI AUTH_SECRET + db:migrate
doc operational items); enumerates pre-existing carryovers; states
Phase 12+ entry condition (explicit sign-off per §12); notes the
architectural pattern validation: Phase 9's write-path shape
generalizes; both watchlist + rating-challenge follow the canonical
pattern.
