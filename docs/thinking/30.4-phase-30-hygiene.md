# 30.4 — Phase-30 hygiene + OPEN_QUESTIONS (+Q75 +Q76) + ADR-0013 D-F Phase-30 cross-ref note

Fifth Phase-30 unit; docs-only. Hygiene catalog + OPEN_QUESTIONS
promotion-pair + ADR-0013 cross-ref. Mirrors Phase-28's Unit 28.4
shape verbatim (Phase 28 surfaced Q73 + Q74 alongside ADR-0020;
Phase 30 surfaces Q75 + Q76 alongside ADR-0021).

## THINK

### Class A items: 0

Matches Phase-12 through Phase-29 well-scoped pattern; **nineteenth
consecutive phase** ships with 0 Class A items. Phase 30 shipped
Units 30.0 + 30.1 + 30.2 + 30.3 without deferrals.

### Class B status (post-Phase-30 delta)

- **B.1** Image-processing expansion: 3 carried (Q68 expansion /
  cropping UI / AVIF).
- **B.2** Phase-17 markdown subset expansion: 8 carried.
- **B.3** Phase-18 surface expansion: 3 + Q72 family (3 sibling-
  divergence candidates `reviewNotesSchema` + `rationaleSchema` +
  `actionRationaleSchema`) carried.
- **B.4** Phase-20 backfill operational: 3 carried.
- **B.5** Phase-21 cleanup operational: 2 carried.
- **B.6** + **B.7**: 0 (closed Phase 25).
- **B.8** Phase-24 transcode operational: 1 carried.
- **B.9** Operational + infrastructure: 8 carried (Q54 / Q55 /
  Q69 / Q73 / Q75 operational + CI dummy AUTH_SECRET + pnpm
  db:migrate doc + first LHCI run + 3 per-script operational
  docs).
- **B.10** Phase-26 detail-page: 2 carried (acceptedActionId link
  + curator-translated FR review).
- **B.11** Operational + i18n: 1 carried — i18n key count audit
  (now **153 per-locale**; was 142 at Phase 29 close; +11 Phase-30
  keys in new `email` namespace).
- **B.12** Phase-27 markdown-promotion: 0 carried (closed Phase
  29).
- **B.13** Phase-28 multi-provider: 4 carried (Q73 / Q74 /
  account-merge UI / 3rd+ provider).
- **B.14** Phase-29 wikilink rendering: 1 carried (Class B not
  Q-promoted).
- **B.15** Phase-30 subscriber-list (**NEWLY SURFACED**): **5
  items** —
  - Weekly digest scheduler infrastructure (Vercel Cron;
    Phase 31+).
  - Digest-send email template (Phase 31+; consumes existing
    `DigestPayload`).
  - DB-helper integration tests for `lib/subscribers/`
    (`createOrRefreshPendingSubscription` + `verifyByToken` +
    `unsubscribeByToken`; Phase-31+ B.20 candidate).
  - Stale verification-token cleanup job (Phase-31+; daily cron
    to clean rows where `status = pending_verification` AND
    `verificationTokenExpiresAt < now`).
  - Subscribe-endpoint rate limiting (Vercel Edge rate-limit
    config; Phase 31+ if spam emerges).

### Class C carryovers unchanged

All ~25 pre-existing Class C items persist (Q47 / Q54 / Q55 /
Q69 / Q73 / Q75 operational gates + Phase-2 ROR-ID orphan +
`<managingEditor>` RSS + W3C validator + `pnpm clean-drafts` +
LHCI first run + abandoned-blob cleanup script gap + etc.).

### Phase 29 → Phase 30 delta (final at pre-this-unit HEAD `ad390f3`)

- **ADRs**: 20 → **21** (+1 ADR-0021 subscriber-list email; **first
  new ADR since Phase 28** = 1-phase ADR-cycle interval; tightest
  gap since Phase 17→18 + 18→19; **ADR-0021 candidate slot closes
  at 2-phase age**).
- **Migrations**: 6 → **7** (+1 `0006_subscriber`; **breaks 13-
  phase 0-migration streak** Phase 17-29; **first non-zero-
  migration phase since Phase 16** = 14-phase gap).
- **DB tables**: 6 → **7** (+`subscriber`; **first new DB table
  since Phase 11** `ratingChallenge` = 19-phase gap; **third
  write-path** in project history after `watchlist` + `ratingChallenge`).
- **Env vars**: 9 → **11** (+`RESEND_API_KEY` + `EMAIL_FROM`;
  **second env-var-count change in 2 phases** — Phase 28: 7 → 9;
  Phase 30: 9 → 11; first such cluster since Phase 16-17).
- **Runtime deps**: 0 → **+1** (`resend@6.12.3`; **first new
  runtime dep since Phase 19** `sharp@0.34.5` = 11-phase gap;
  **first new runtime dep with new ADR-anchored module since
  Phase 19**).
- **Tests**: 627/56 → **667/60** (+40 / +4 files; Unit 30.2 added
  +19/+3 + Unit 30.3 added +21/+1; **first multi-file vitest delta
  in a single phase** since Phase 12 — Phase 28 added +1 file
  only, Phase 30 adds +4 across 2 code units).
- **First Load JS shared chunk**: **103 kB UNCHANGED** (39
  consecutive units at 103 kB; Phase 9-30 invariant preserved).
- **Middleware bundle**: **160 kB UNCHANGED** since Phase 12.
- **All affected page bundles UNCHANGED**: `/[locale]/digest` =
  108 kB (the page-bundle delta from the new `"use client"`
  `SubscribeForm` is 0 kB; the form is tree-shaken from the
  shared chunk; per-page bundle preserved at the existing 108 kB
  envelope).
- **Dynamic page+API routes**: 8 → **11** (+3 new subscribe
  routes).
- **i18n keys per locale**: 142 → **153** (+11 in new `email`
  namespace).
- **`lib/email/`**: 0 → **3 source files + 3 test files**.
- **`lib/subscribers/`**: 0 → **1 source file + 1 test file**.
- **API route count under `app/api/v1/`**: 7 → **8** (`subscribe`
  joins).
- **OPEN_QUESTIONS state**: 26 / 4 / 31 / 61 → 26 / 4 / **33** /
  **63** (+2 net; **Q75 + Q76 promotion pair** mirrors Phase-28's
  Q73 + Q74 shape).
- **APPEND-not-EDIT notes on ADR-0013**: 1 (Phase-11 existing) →
  **2** (+Phase-30 this unit; cross-ref to ADR-0021 D-B).
- **Patience-signal closures**: Phase 30 closes **Phase-5 D-4 at
  22+ phases** — single longest closure in project history; prior
  longest was Phase 28's Phase-9 B.8 at 17 phases.

### 16 Phase-30 architectural firsts (cumulative across the phase)

1. **First new ADR since Phase 28** (1-phase ADR-cycle interval;
   tightest gap in project history alongside Phase 17→18 + 18→19).
2. **ADR-0021 candidate slot claimed at 2-phase age**.
3. **First `lib/email/` infrastructure** in project history.
4. **First Resend SDK integration** (`resend@6.12.3`).
5. **First TSX server component refactored to plain template-
   literal string builder** (lessons-learned from Next.js App
   Router's `react-dom/server` import block).
6. **First `vi.hoisted` + class-shaped `vi.mock` pattern** in
   project history.
7. **First test-only export hook** (`__resetResendClientForTests`).
8. **First `lib/subscribers/` infrastructure**.
9. **First use of `crypto.timingSafeEqual`** (constant-time token
   comparison).
10. **First `index` + `uniqueIndex` Drizzle imports** (explicit
    indexes vs prior column-level UNIQUE / composite primaryKey).
11. **First multi-route API surface in a single unit** since
    Phase 9 (3 routes: subscribe + verify + unsubscribe).
12. **First HTML-string-returning GET routes** in project history.
13. **First `"use client"` component since Phase 16** (14-phase
    gap; `<SubscribeForm>`).
14. **First `_components/` private-directory inside an app-router
    page directory**.
15. **Phase-5 D-4 punt closed at 22+ phase carryover** — **single
    longest patience-signal closure in project history** (prior
    longest: Phase 28's Phase-9 B.8 at 17 phases).
16. **Q75 + Q76 promotion pair** mirrors Phase-28's Q73 + Q74
    shape (operational + architectural Q-pair coupled to a single
    ADR); **second consecutive new-ADR phase to surface an
    operational-plus-architectural Q pair** — establishes the
    ADR-cycle-coupled Q-promotion-pair shape as a recurring
    pattern.

### OPEN_QUESTIONS hygiene

**+Q75** (operational; Resend account + domain DKIM/SPF/DMARC +
RESEND_API_KEY + EMAIL_FROM provisioning; mirrors Q54 + Q73 shape
verbatim).

**+Q76** (architectural; per-user-account-based subscriptions;
ADR-0021 D-C Phase-31+ deferral; three resolution options
documented — Option A FK to users.id; Option B separate
`user_subscriptions` table; Option C indefinite anonymous email-
only).

**Q-tally**: 26 resolved + 4 lean + 31 open → 26 resolved + 4 lean
+ **33 open** = **63 total** (+2 net). **1-phase Q-tally freeze
broken at Phase 30** (Phase 29 = 1-phase freeze; Phase 30 breaks
cleanly via +Q75 +Q76; mirrors Phase-28's Q-tally-freeze break).

### ADR-0013 D-F Phase-30 cross-ref note appended

Per the APPEND-not-EDIT phase-tagged convention. **Second APPEND
note on ADR-0013** (Phase-11 existing — `ratingChallenge` first
write-path-beyond-watchlist + Phase-30 this unit — second new DB
table since Phase 11). Mirrors Phase-29 Unit 29.3's third APPEND
on ADR-0018 D-G shape. Documents the 19-phase gap + 13-phase
0-migration streak break + USER-STATE-only invariant preservation
via subscriber being user-derived data + `domainSubscriptions`-as-
text precedent following `problemSlug`-as-text.

### Smoke gates (docs-only)

`pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline since
Phase 2; unchanged through every Phase 3-29 unit). typecheck /
test / build untouched since no source files modified at this
unit.

### Files modified

- `OPEN_QUESTIONS.md` (+Q75 +Q76; ~50 lines appended after Q74).
- `docs/adr/0013-db-choice.md` (Phase-30 cross-ref note appended
  to D-F section; ~5 lines).
- CHANGELOG.md (Unit 30.4 entry).
- `docs/thinking/30.4-phase-30-hygiene.md` (this file).

### Parallel-curator awareness

This unit docs-only, no collision risk. No parallel-session
activity observed during Phase 30 work; working tree clean at
each unit boundary.
