# 7.10 — OPEN_QUESTIONS hygiene + ADR review (Phase 7 pre-close)

Seventh code unit of Phase 7 (counting code + docs units). Docs-only. Mirrors the Phase-5 (Unit 5.12) + Phase-6 (Unit 6.9) precedents: promote Phase-7 open questions whose leans have been realized in implementation; confirm decided-as-lean questions that the ADR pins but implementation hasn't yet exercised; scan ADRs for stale status or supersede markers.

Lands ahead of the natural acceptance-gate sequence (7.11) because (a) it's docs-only (zero collision risk with the parallel-curator session likely working on Unit 7.5 methodology FR pilot, or any other route work), (b) Q-promotions only depend on already-shipped units (7.1 / 7.4 / 7.8), (c) Unit 7.9 (Phase-7 hygiene status pass) typically follows Q-hygiene and the acceptance-gate follows both.

## THINK

### State of Phase-7 questions at HEAD (`365f764`)

The Phase-7 prep doc ([7.0 §F](7.0-phase-7-prep.md)) surfaced four Phase-7 questions:

| # | Title                                       | Status (HEAD) | Disposition this unit |
|---|---------------------------------------------|---------------|------------------------|
| Q50 | i18n runtime choice                       | **resolved** (Unit 7.1) | no change — already resolved by ADR-0011 D-A in Unit 7.1 |
| Q51 | Bilingual content backfill cadence       | **decided-as-lean** | **no promotion** — cadence is ongoing curator work; never an ADR-grade decision |
| Q52 | Translation provenance schema             | **decided-as-lean** | **promote to resolved** — Unit 7.4 realized the schema (`translation_source: s.enum(["human", "machine-assisted"])`); ADR-0011 D-G pinned the contract |
| Q53 | Curator authorship attribution per-locale | **decided-as-lean** | **promote to resolved** — ADR-0011 D-G explicitly pinned "`primary_curator` remains global"; matches the Unit 7.1 promotion precedent for Q50 (resolved on ADR-pin, not on bulk implementation) |

Phase-6 carryover already closed in Unit 7.8: **Q48** (talk-page indexing posture) — both halves resolved (link-half Unit 6.3, sitemap-half Unit 7.8).

### Q-by-Q reasoning

**Q50 (no change)**. Already resolved by Unit 7.1's ADR-0011 D-A. The status block in `OPEN_QUESTIONS.md` reads `decided · Resolved: Unit 7.1`; no edit needed in this hygiene pass.

**Q51 (no promotion)**. The cadence question is genuinely ongoing curator-editorial work. Phase-7 acceptance ships "infrastructure complete" (per the lean's own framing). The lean stays valid; promoting to `resolved` would require a misleading "resolved" date for what's an ongoing workstream. Stays `decided-as-lean` indefinitely; consider closing in a future phase only if a curator-process ADR formalizes the cadence.

**Q52 (promote to resolved)**. Realized in two places:
- **ADR-0011 D-G** (Unit 7.1) pinned the contract: required on translated files; absent on EN canonicals; enum of `human` / `machine-assisted`.
- **Unit 7.4** (`f315458`, parallel-session commit) implemented the schema field across 5 Velite collections + 2 canonical Zod-4 schemas (`lib/schemas/problem.ts` + `lib/schemas/paper.ts`); added `TRANSLATION_SOURCES` enum constant + post-transform refines enforcing FR-requires-source.

Both the contract pin and the implementation are live at HEAD. Status update: `decided-as-lean` → `resolved 2026-05-16 (Unit 7.4)`.

**Q53 (promote to resolved)**. ADR-0011 D-G explicitly pinned: "`editorial.primary_curator` remains **global** (not per-locale per Q53 lean)". The ADR is an architecturally load-bearing commitment; this matches the Q50 precedent where the ADR's runtime choice (D-A) resolved Q50 without requiring all the downstream implementation units (7.2–7.6) to finish first. No `lib/schemas/problem.ts` change was needed (the schema already has `primary_curator: z.string().min(1)` — a single string, not per-locale). Status update: `decided-as-lean` → `resolved 2026-05-16 (Unit 7.1)` (since the ADR is what pinned it; D-G predates this hygiene unit by 9 commits).

### ADR review pass

11 ADRs at HEAD (0001 through 0011); all `Status: accepted`; none superseded.

Phase-7 added one ADR: **0011 — i18n strategy** (Unit 7.1; `6c7a52d`). Confirmed:
- Status: `accepted`; date authored + accepted = `2026-05-16`.
- Deciders block notes Q7 (editorial governance) is still open; correct.
- Supersedes / superseded-by: both `—`. Correct.
- Cross-links to ADR-0001 / ADR-0002 / ADR-0010 / ADR-0008 / ADR-0009 verified during Unit 7.1 review.

No edits needed to any ADR in this hygiene pass.

### Scope (this unit)

Three small edits + this doc:

1. **`OPEN_QUESTIONS.md`** — promote Q52 + Q53 to `resolved`; no other changes.
2. **`docs/thinking/7.10-open-questions-hygiene.md`** (this doc).
3. **`CHANGELOG.md`** — Phase-7 Unit 7.10 entry.

### Scope NOT in this unit

- **No `MEMORY.md` / auto-memory edits** — auto-memory is per-user persistent state outside the repo; project-state changes don't touch it.
- **No ADR amendments** — no Phase-7 implementation surfaced gaps in ADR-0011 (the sibling-file pattern works as specified; the cycling-toggle UX shipped per D-F; the alternates-only-for-existing-shadows decision in Unit 7.8 follows ADR-0011 D-E mechanically).
- **No new ADRs** — no architectural decisions surfaced that aren't already covered by ADR-0011's seven decision blocks.
- **No promotion of Q51** (see reasoning above).
- **No new Q-surfacing** — the units shipped this session (7.6, 7.7, 7.8) operated entirely within ADR-0011's contract; no implementation choice was load-bearing enough to surface as a Q. The "html-lang vs content-language mismatch" risk flagged in Unit 7.7 is an axe-rule edge case, not a new Q.

### Constraints

- **OPEN_QUESTIONS is a shared singleton**. Per the file header: "concurrent curation runs must not write here directly. They drop ambiguities into `docs/open-questions-inbox/<RUN-ID>.md`". This unit is the *primary* session; writes are serialized through me. The parallel session (last seen committing Unit 7.4 = `f315458`) hasn't touched OPEN_QUESTIONS since.
- **No new file beyond this THINK doc** — minimal blast radius.

### Tradeoffs / defensible defaults flagged

1. **Promoting Q53 on ADR-pin alone** (no bulk implementation evidence) — matches the Q50 precedent. Defensible. If a future unit changes the curator schema to per-locale, the ADR would supersede first (Q53 reopens).
2. **Not promoting Q51** — keeps the cadence question open as an audit trail of "Phase 7 acceptance is infrastructure-complete, not content-complete." Useful provenance for future phases that may revisit FR content backfill.
3. **No ADR edits** — Phase 7 has been remarkably ADR-stable (one ADR, one Phase, zero amendments). Consistent with the Phase-6 single-ADR rhythm (ADR-0010 was the Phase-6 lone ADR; no amendments). Defensible defaults are holding.

### Smoke gates

Docs-only — `validate-content` / `typecheck` / `test` / `build` / `audit-content` all unaffected. Confirm by running the full suite anyway (cheap; catches incidental drift).

### CHANGELOG entry shape

Per-unit entry: promotes Q52 + Q53 to `resolved`; confirms Q50 already resolved (Unit 7.1) + Q51 stays `decided-as-lean` (ongoing curator cadence). ADR review pass — 11 ADRs accepted, none superseded; Phase 7 added ADR-0011 only. No new questions surfaced from Units 7.6 / 7.7 / 7.8; the units operated entirely within ADR-0011's contract.
