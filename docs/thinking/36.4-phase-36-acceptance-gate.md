# 36.4 — Phase 36 acceptance gate (Q64 per-user privacy opt-out realized; **24 ADRs**; 5 units; **first post-Q68-resolution longest-Q closure**; **first user-toggleable privacy surface**; **first public-data-invariant exception**; **first schema migration since Phase 33**; **3 consecutive net-Q-decrease phases**; **6 consecutive phases without new B category**; **10 ADR D-clauses with APPENDs total — milestone**; 32nd "Continue" override opportunity at Phase 36 → 37 boundary)

Fifth and final Phase-36 unit; docs-only. Closes Phase 36 (**twenty-
seventh NON-§13 phase**; Q64 per-user privacy opt-out realized via
new `users.profilePublic` boolean column + opt-out toggle UI on
`/[locale]/profile` + `getPublicProfileByHandle` filter + pure 404
on `/[locale]/u/{handle}` for opted-out users + ADR-0015 D-A
APPEND). Mirrors Phase-12 through Phase-35 acceptance-gate patterns.
**§13 ledger CLOSED at Unit 9.9** (carried unchanged through
Phases 10-36).

## THINK

### Phase 36 ledger

- **5 units shipped** (36.0 prep → 36.4 gate). **0 deferrals**;
  **0 scope drift**.
- **Twenty-fifth consecutive well-scoped phase**.
- **Zero new ADRs** (Phase 36 = first no-new-ADR phase since Phase
  34; alternating-pattern extends to 5 phases: Phase 32 no-new → 33
  new → 34 no-new → 35 new → 36 no-new). ADR count UNCHANGED at 24.
- **One ADR APPEND** (ADR-0015 D-A; first APPEND for that clause;
  22-phase source-to-APPEND gap). **10 ADR D-clauses with APPENDs
  total** post-Phase-36 — milestone count.
- **One schema migration shipped** (`0009_user_profile_public`;
  9th migration; **4th ALTER on `users`** and first since Phase
  16's `0005_user_image_override`; **3-phase migration gap** mirrors
  Phase 30 → 33 gap exactly).
- **One user-table column added** (`users.profilePublic boolean
  default true`).
- **Two new helpers in `lib/users/`** (`setProfilePublic` +
  `getPublicProfileByHandle` filter extension).
- **One new server action** (`updateProfilePrivacyAction`) on
  `/[locale]/profile`.
- **One new UI section** (privacy toggle on `/[locale]/profile`).
- **+7 i18n keys per locale** (161 → 168; first i18n addition since
  Phase 34 = 2-phase gap).
- **+3 tests / 0 file** cumulative across the phase (733/65 → 736/65;
  all in `lib/users/index.test.ts` existing file).

### Final smoke gates (all green at HEAD pre-this-unit `1b91498`)

- `pnpm typecheck` clean.
- `pnpm lint` clean.
- `pnpm test` → **736/736 across 65 vitest files** (+3 / 0 file Δ
  from Phase 35 close 733/65; all in Unit 36.1).
- `pnpm build` → ~659 prerendered pages + **13 dynamic page+API
  routes UNCHANGED**; **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-36 unit (**51st
  consecutive unit at 103 kB**); **middleware bundle = 160 kB
  UNCHANGED** since Phase 12; all page bundles UNCHANGED.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline since
  Phase 2; unchanged through every Phase 3-36 unit).

### Phase 35 → Phase 36 architectural firsts (cumulative across the phase; ~10 firsts)

1. **First post-Q68-resolution longest-Q closure** at 15+ phase
   carryover (Q64 was the longest after Q68 resolved Phase 35; Q64
   resolution Phase 36 closes the second-longest in 2 consecutive
   phases). **First 2-phase longest-Q-closure streak** in project
   history.
2. **First user-toggleable VISIBILITY surface** in project history.
   Phase 14-15 shipped user-controlled IDENTITY surface (displayName +
   bio); Phase 16 shipped user-controlled BINARY surface (avatar);
   Phase 36 ships user-controlled VISIBILITY surface (profilePublic).
   Identity → Binary → Visibility user-control surface-category
   lineage complete for Phase 14-16-36.
3. **First public-data-invariant exception** in project history
   (Phase 14 ADR-0015 D-A established the public-data invariant;
   Phase 36 establishes the user's right to opt out of that invariant
   for their own data — extension, not contradiction).
4. **First schema migration since Phase 33** (`0009_user_profile_public`
   = 9th migration; **3-phase migration gap** mirrors Phase 30 → 33
   gap exactly).
5. **First "completely-absent-from-OPEN_QUESTIONS Q-resolution"** in
   project history (Q64 was never even sub-bulleted in OPEN_QUESTIONS.md;
   Q68 expansion had 3 sub-bullet locations; Q64 has zero).
6. **First dual-`revalidatePath` server action** in project history
   (`updateProfilePrivacyAction` invalidates both `/profile` + `/u/[handle]`
   because the toggle affects both surfaces).
7. **First `formData.get(...) === "on"` checkbox-state extraction**
   in project history.
8. **First `Promise<void>` user-table write helper** in project
   history (`setProfilePublic` shape simplification because no
   validation step exists).
9. **First retroactive typecheck-fix carry-forward** in project
   history (Unit 35.2 moderation mocks shipped with latent TS errors
   that vitest didn't catch but `pnpm typecheck` did; fixed in Unit
   36.1).
10. **Tenth APPEND-pattern ADR D-clause cluster** — milestone count
    (ADR-0013 D-F + ADR-0018 D-G + ADR-0021 D-F + ADR-0022 D-D +
    ADR-0021 D-C + ADR-0023 D-H + ADR-0016 D-B + ADR-0017 D-H +
    ADR-0019 D-F + **ADR-0015 D-A**).

### Delta summary (Phase 35 → Phase 36)

- **+5 commits** in-phase (36.0 + 36.1 + 36.2 + 36.3 + this gate).
- **+0 ADRs** (ADR count UNCHANGED at 24; first no-new-ADR phase
  since Phase 34).
- **+1 migration** (9th migration; `0009_user_profile_public`;
  §5.7 trigger (b) FIRED Unit 36.1).
- **+1 DB column** (`users.profilePublic`; 4th ALTER on `users`).
- **+0 runtime deps**.
- **+0 env vars** (13 UNCHANGED; first no-env-var phase since
  Phase 34).
- **+7 i18n keys per locale** (161 → 168; first i18n addition
  since Phase 34 = 2-phase gap).
- **+3 / 0 file tests** (733/65 → 736/65; all in `lib/users/index.test.ts`).
- **+0 dynamic API routes** (13 UNCHANGED; no new routes; UI is
  server-action on existing `/profile` page).
- **+0 new code/ADR files** (extension within existing files +
  1 new migration SQL file + 1 auto-emitted snapshot).
- **+1 new server action** (`updateProfilePrivacyAction`).
- **+1 new UI section** (privacy toggle on `/profile`).
- **+1 new helper** (`setProfilePublic` in `lib/users/`).
- **+0 First Load JS Δ** (103 kB UNCHANGED end-to-end Phase 9-36;
  51st consecutive unit at 103 kB).
- **+0 middleware Δ** (160 kB UNCHANGED).
- **+0 net top-level Q tally Δ** (66 UNCHANGED; Q64 was never in
  OPEN_QUESTIONS.md so the top-level tally doesn't shift).
- **-1 Phase-37+ candidate** (11 → 10; **third consecutive net-
  decrease phase** Phase 34 + 35 + 36 = first 3-phase net-decrease
  streak in project history).
- **+0 Class B categories** (14 UNCHANGED; **sixth consecutive
  phase without new B category** Phase 31 + 32 + 33 + 34 + 35 +
  36; first such 6-phase run in project history).
- **+0 Class B items**.
- **+1 patience-signal-closing phase** (Q64 at 15+ phase carryover —
  longest open architectural pre-Phase-36).
- **+1 ADR APPEND** (ADR-0015 D-A; first APPEND for that clause;
  **10 ADR D-clauses with APPENDs total** post-Phase-36 — milestone).

### Surviving follow-ons (Class B; ~3 items across 14 categories)

Same set as Phase 35 close — Phase 36 did not address B catalog.
**B.15 item 5 carried** (subscribe-endpoint rate limiting; Phase
37+ if spam). 8 carried unchanged from Phase 35 close.

### Phase-37+ Q-candidates flagged (10 total; net-decrease from 11)

- 5 carried from Phase 27 (Q60 / Q61 / Q62 / Q65 / Q72; Q64 + Q68
  expansion both now resolved).
- 2 carried from Phase 28 (Q73 + Q74).
- 1 carried from Phase 30 (Q75).
- 2 carried from Phase 31 (Q77 + Q78).
- 0 from Phase 33-35.
- 0 new Phase 36.

**Slot net-decrease: 11 → 10 candidates**. Phase 37+ thread menu is
one item shorter than Phase 36 entry was; **third consecutive net-
decrease phase** (Phase 34 + Phase 35 + Phase 36 = first 3-phase
net-decrease streak in project history).

### DB-migration trigger re-eval at Phase 37 kickoff

- §5.7 trigger (a): FIRED Unit 9.6 carried.
- §5.7 trigger (b): **FIRED Unit 36.1** (9th migration shipped).
  Next thread Phase 37+ determines trigger (b) — most Phase-37+
  candidates do NOT require migration in their minimum form (Q78
  Profile A digest analytics needs new column for bounce tracking
  IF Resend webhook integration ships; Q72 markdown evolution
  doesn't; ADR-0025 concrete content-moderation provider may add
  persistent decision audit log = would fire trigger (b);
  account-merge UI ADR-0020 D-E may need user-merge tracking table).

### Risk surface at Phase 36 close

- **6 operational gates pending** UNCHANGED (Q54 GitHub OAuth + Q55
  Turso prod DB + Q69 Vercel Blob + Q73 Google OAuth + Q75 Resend
  domain + Q77 Cron secret).
- **2 architectural Phase-37+ candidates** carry forward (Q74 non-
  GitHub-curator identity; Q78 digest-send analytics).
- All other Phase-30-31-32-33-34-35 risk-surface items carry forward
  unchanged.

### Boundary statement (Phase 36 anti-scope)

NOT a granular per-surface opt-out (per-problem watchlist visibility
/ per-challenge submission visibility / subscription visibility all
Phase 37+ if signal); NOT friend-only profiles (Phase 37+); NOT
explicit-consent UX on first sign-in (Phase 37+); NOT opt-out audit
trail (Phase 37+); NOT bulk CSV opt-out (Phase 37+); NOT friendlier
"private profile" notice page (Phase 37+; pure 404 ships); NOT
ADR-0025 concrete content-moderation provider (Phase 37+ when
curator authors policy OR operational API gate fires); NOT Q78
digest-send analytics (waits for Q77 production signal; Phase 37+);
NOT Q79 Profile B full-edit UX or Profile C combined-curator-
analytics dashboard (Phase 37+); NOT account-deletion blob cleanup
(Phase 37+); NOT account-merge UI ADR-0020 D-E (Phase 37+); NOT
third+ provider expansion (Phase 37+); NOT markdown evolution ADR
(Phase 37+); NOT v2 methodology authoring (curator-track per
release-readiness audit §2.9); NOT SiteHeader avatar pill (Phase
37+); NOT subscribe-endpoint rate limiting B.15 item 5 (Phase 37+
if spam); NOT bounce/complaint webhook handling (Phase 37+ Q78 sub-
thread); NOT row-level lock for concurrent-cron race; NOT
`safeAuth()` extraction (Phase 37+ if 5th in-place copy surfaces);
NOT per-problem subscriptions extension (Phase 37+); NOT cross-
domain aggregated weekly summary (Phase 37+); NOT subscriber-list
export tooling (Phase 37+); NOT per-user-account-subscription audit
trail (Phase 37+); NOT Phase-35 ADR-0024 framework consumption
(Phase 37+ via ADR-0025 concrete provider); NOT pre-commit
typecheck hook addition (separate hygiene unit candidate; Phase
37+).

**This phase is the privacy-opt-out realization** for the per-user
identity model. `users.profilePublic` column ships, opt-out toggle
ships, `getPublicProfileByHandle` filter ships, pure 404 on
`/[locale]/u/{handle}` for opted-out users ships, ADR-0015 D-A
APPEND documents the public-data-invariant extension. Granular
per-surface opt-outs remain Phase 37+ candidates as UX-track work;
concrete content-moderation provider remains Phase 37+ when curator
authors policy.

### Parallel-curator awareness

Phase 36 shipped as a **single-session phase** — primary shipped all
5 units 36.0 → 36.4 in one coherent session. **Twentieth consecutive
single-session phase** (Phase 17-36). Phase 16 remains the lone
parallel-session phase. Working tree clean at each unit boundary; no
parallel-session activity observed during Phase 36 work.

### Phase 36 vs prior no-new-ADR phases

Phase 36 = **11th no-new-ADR phase** in project history (Phases
20-27 + 29 + 32 + 34 + 36 = 11 no-new-ADR phases). **9-phase
no-new-ADR run Phase 20-27 + 29 remains the longest**; Phase 36
extends the alternating "no-new → new-ADR → no-new → new-ADR" pattern
to 5 phases (Phase 32 no-new → 33 new → 34 no-new → 35 new → **36
no-new**) — **first 5-phase alternating pattern** in project history.

### Phase 37 entry conditions

Per §12 cardinal rule, **explicit human sign-off required**. **Thirty-
second "Continue" override opportunity** in Unit 37.0 prep. Thread
options for Phase 37+ (overridable per §12):

- **(a) ADR-0025 concrete content-moderation provider commitment**
  (Phase-35 framework consumption; curator-track-dependent: needs
  either curator-authored wordlist OR external API operational
  unblock; tightest 0-phase Q-carryover available; mirrors Phase 30
  → 31 tight-follow-on cadence).
- **D. Markdown evolution ADR** (Q72 + Phase-29 B.14 wikilink couple;
  ~3-5 units; ADR-0025 candidate; framework-only-pattern playbook
  reuse opportunity).
- **B. Q78 architectural digest-send analytics** (~3-5 units; still
  waits for first-week production cron signal from Q77).
- **L. Account-deletion blob cleanup** (~1-2 units; small scope;
  exercises Phase-33 ADR-0023 D-D cascade).
- **N. v2 methodology authoring** (curator-track per release-
  readiness audit §2.9 = primary blocker for §1 (d) success metric).
- **Q79 Profile B full-edit UX** or **Profile C combined-curator-
  analytics dashboard** (Phase 37+ UX-track without ADR).
- **J-other. Account-merge UI** (ADR-0020 D-E).
- **K-other. Third+ provider expansion** (ADR-0020 D-B amendment).
- **M. SiteHeader avatar pill** (Phase-14 carryover).
- **B.15 item 5. Subscribe-endpoint rate limiting** (~1-2 units;
  Phase 37+ if spam observed).
- **Phase-36-prep deferred candidate: pre-commit typecheck hook
  addition** (~1 unit; surfaced as Unit 36.1 recommendation;
  prevents future latent-TS-error commits like Unit 35.2 mock-shape).
- **Phase-36 deferred: subscribe route test file backfill** (~1-2
  units; gap noted in Unit 35.2 + 36.2).
- **Phase-36 deferred: `/profile` page-handler test coverage** for
  the privacy toggle render + submit-redirect path (~1-2 units; gap
  noted in Unit 36.2).

**Strongest non-just-surfaced patience signal**: post-Q64 resolution,
the longest open architectural is **Q72 markdown evolution** (Phase-
29 B.14 wikilink couple; multi-surface; ADR-0025 candidate; demand-
signal-first pattern eligible for framework-only-ADR reuse from
Phase-35 ADR-0024 precedent). **Q78** still waits for Q77 operational
signal. **Q72** = strongest single remaining patience signal at 7+
phase carryover (since Phase 29).

### Surfaces ready for Phase 37+ consumption

- `users.profilePublic` boolean column + `0009_user_profile_public`
  migration (Phase-36 schema).
- `setProfilePublic(userId, isPublic): Promise<void>` helper +
  `getPublicProfileByHandle` filter (Phase-36 helpers).
- `updateProfilePrivacyAction` server action shape (Phase-36 dual-
  revalidatePath pattern reusable for future opt-out toggles).
- Privacy toggle UI pattern (labeled checkbox + submit-on-change;
  no shadcn `Switch`; no client-side state) reusable for future per-
  surface opt-outs.
- 7 i18n keys per locale in `profile_edit.privacy_*` namespace
  (extendable for granular opt-outs).

### Phase 36 → Phase 37 sign-off requirement

Per §12 cardinal rule: explicit human sign-off required at Phase
boundaries. Phase 36 close + Phase 37 open is the **32nd "Continue"
override opportunity** in the project's unit-rhythm history. Phase
36 shipped a substantial user-visible privacy feature (column +
migration + UI + filter + 404 behavior + ADR APPEND); Phase 37
entry naturally weighs:

- (a) Operationalize ADR-0024 framework with concrete provider
  (ADR-0025 candidate; tightest carryover; curator-track-
  dependent).
- (b) Q72 markdown evolution as framework-only-ADR-pattern reuse
  (Phase-29 B.14 wikilink couple; multi-surface; defensible-default
  candidate).
- (c) Q78 digest-send analytics (still waits for Q77 operational
  signal).
- (d) Curator-track v2 methodology authoring (per release-
  readiness audit §2.9).
- (e) Operational-gate work (the 6 pending gates).

**Default recommendation: (b) Q72 markdown evolution** — reuses the
Phase-35 framework-only-ADR pattern in a multi-surface context
(wikilinks + tables + footnotes + images + …); demand-signal-first
concern dissolved by progressive-enhancement default (start with
existing safe-subset per ADR-0018; extensions opt-in per renderer
context). **Alternative: (a) ADR-0025 concrete provider** if user
prefers to operationalize the Phase-35 framework before extending
the framework-only pattern to new domains.
