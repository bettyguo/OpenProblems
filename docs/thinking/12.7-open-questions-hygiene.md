# 12.7 — OPEN_QUESTIONS hygiene + ADR review (Phase 12 pre-close)

Eighth Phase-12 unit; docs-only. Mirrors Unit 5.12 / 6.9 / 7.10 / 8.8 /
9.8 / 10.4 / 11.6 OQ-hygiene precedents. Scans the open-questions
ledger for Phase-12 promotions + reviews the 14 ADRs at HEAD for stale
status / supersede markers / prose-shift reconciliations. Lands ahead
of Unit 12.8 (acceptance gate); the gate cites the updated state.

## THINK

### OPEN_QUESTIONS scan

Phase 12 promotes **1 question** + surfaces **0 new questions**.

**Q57 promoted** `open` → `resolved 2026-05-16 (Unit 12.1)`. ADR-0014
pins the architectural surface (state machine; authz; COI; emission;
ALTER discipline; route shape). The lean documented in Unit 11.6 Q57
body became D-A through D-F in ADR-0014 verbatim — no surprises at
implementation time.

**Q58 stays open** per ADR-0014's explicit scope cap (Phase 13+ thread;
couples to Phase-10 Class B item 1 — public profile route). The Unit
11.6 Q58 body (counter / per-problem listing / public-profile-of-
challenges; status-gated visibility leaning toward `accepted` + `submitted`
public, `rejected` + `withdrawn` submitter-only) is still the current
lean. Phase 13+ may surface a follow-on ADR pinning Q58 once volume
justifies.

**No new Q-numbers surfaced.** Two potential Phase-12-internal
candidates were flagged in Unit 12.0 prep + Unit 12.6 hygiene
risk-surface enumeration (NOT promoted unless usage demands):

- **Q59 candidate** — rating-action YAML emission automation. Lean: CLI
  helper `pnpm emit-challenge-action <id>` Phase 13+. NOT surfaced this
  unit; flag for post-Phase-12 usage observation.
- **Q60 candidate** — curator authorization evolution beyond env-var
  allowlist (per-problem ACLs; `curatorRoles` DB table; multi-tier
  editorial-board). Lean: defer to Phase 13+ / 14+ alongside Q7
  resolution. NOT surfaced this unit.

**Q54 + Q55 stay open** as operational gates (carried since Phase 9).
Phase-12 surfaces inherit the same gates; Phase 12 adds no new
operational gates to Class A per Unit 12.6's hygiene catalog (the new
`LOP_CURATOR_LOGINS` env var is Phase-12-internal, NOT promoted to
operational Class A).

### Ledger state at HEAD (Status-field-tally; mechanically auditable)

- **20 resolved**: Q1, Q4, Q5, Q12, Q13, Q18, Q27, Q32, Q40, Q41, Q43,
  Q45, Q46, Q48, Q49, Q50, Q52, Q53, Q56, **Q57**.
- **4 decided-as-lean**: Q38, Q42, Q44, Q51.
- **29 open**: Q2, Q3, Q6, Q7, Q8, Q9, Q10, Q11, Q14, Q15, Q16, Q17,
  Q19, Q25, Q26, Q28, Q29, Q30, Q31, Q33, Q34, Q35, Q36, Q37, Q39, Q47,
  Q54, Q55, Q58.
- **Total: 53 entries** (was 53 at Phase-11 close).
- **Phase-12 delta**: +1 resolved (Q57); +0 decided-as-lean; -1 open
  (Q57 left); net **0 size change** (Q57 moved between buckets).

The two flagged Q59 + Q60 candidates are anticipated-not-surfaced —
deferred until Phase-13+ usage signals justify promotion. ADR-0014 has
absorbed the architectural surface that would otherwise have created
Q59 + Q60 prematurely; the candidates would surface at Phase-12
acceptance smoke OR mid-Phase-13 if real-world cadence reveals friction.

### ADR review

**14 ADRs at HEAD** (0001 – 0014). Phase 12 added **1 new ADR** (0014
in Unit 12.1). All 13 prior ADRs unchanged in body; status remains
`accepted` for the entire set.

**Per-ADR exercise status at Phase-12 close:**

- **ADR-0001 through ADR-0010**: unchanged in body; Phase 12 invoked
  none of them beyond their Phase-N+ established exercise patterns.
- **ADR-0004 (file-first / no-DB-for-content)**: REAFFIRMED by
  ADR-0014 D-D (manual YAML emission preserves file-first; rating-
  action YAMLs land in `content/problems/<slug>/ratings/` via
  out-of-band PR, not via API-side commit).
- **ADR-0005 (rating-action immutability)**: REAFFIRMED by ADR-0014
  D-A's terminal-status-non-reversible policy (`accepted` / `rejected`
  / `withdrawn` terminal; re-challenge = new row not reopen) +
  D-E's ALTER migration discipline (`0003_rating_challenge_review`
  ALTERs `ratingChallenge` ADDITIVELY; never edits applied migrations).
- **ADR-0011 D-A through D-G**: exercised by Phase-12's `messages.curator.*`
  (47 keys per locale; Unit 12.4) + `messages.profile.*` extensions (8
  keys per locale across Units 12.4 + 12.5). FR translations honor §3
  brand register. No supersede triggers.
- **ADR-0012 D-A through D-E**: exercised by Phase-12 curator dashboard
  + review API + profile-page withdraw form. D-D (redirect-to-provider
  full-page sign-in) preserved in both curator dashboard auth-check
  redirects + review-API 401 responses. D-E (`users.githubLogin`
  joining to file-system) consumed by `getLoginById` + `isCurator`
  curator-authz tier + `getCoIStatus` self-review COI check.
- **ADR-0013 D-A through D-F**: exercised by Phase-12's first ALTER
  migration. D-B (migration immutability) **extended** by ADR-0014
  D-E to cover ALTER migrations (NEVER edit `0002_rating_challenges`;
  column adds = new migration file `0003_*`). D-F (USER-STATE only)
  honored — 4 new columns on `ratingChallenge` are REVIEW-STATE
  which is USER-STATE-adjacent on the same row.
- **ADR-0014 D-A through D-F**: realized across Phase-12 implementation:
  - D-A state machine: codified in `lib/rating-challenges/index.ts`
    state-machine helpers (Unit 12.3); enforced via `reviewChallenge`
    + `withdrawChallenge` helpers; tested via 25 state-machine tests.
  - D-B env-var authz: `lib/auth/curator.ts` + `LOP_CURATOR_LOGINS`
    env var (Unit 12.3); 11 tests covering edge cases.
  - D-C COI policy: `lib/rating-challenges/coi.ts` (Unit 12.3); 7 tests
    covering self-review hard-block + primary-curator soft-warn +
    null-submitter edge.
  - D-D manual YAML emission: detail-page "Attach action YAML" form
    server-action (Unit 12.4); UI banner directs curator to commit
    YAML out-of-band; `acceptedActionId` set via form post-acceptance.
  - D-E ALTER migration discipline: `0003_rating_challenge_review`
    landed atomically with snapshot + journal (Unit 12.2); in-place
    SQL correction for FK `ON DELETE SET NULL` clause documented as
    anticipated edge case.
  - D-F route shape + auth tier: `/[locale]/curator/challenges` list +
    `/[locale]/curator/challenges/[id]` detail (Unit 12.4); page-local
    auth + curator-authz mirroring Phase-10 `/profile` pattern;
    middleware lift threshold still 2/3 protected page routes.

### Prose-shift reconciliations

**Zero** this unit. ADR-0014 was authored fresh in Unit 12.1; all
implementation matched the ADR verbatim. No prose-shift between ADR
intent and realized code surfaced (unlike Unit 9.8's two ADR edits for
ADR-0012 D-A pin convention + ADR-0013 D-E migration filename numbering).

### Q-promotions in the broader ledger

Q57 is the only Q-promotion this phase. The 18 resolved + 4 decided-as-
lean state at Phase-11 close becomes 19 resolved at Phase-12 mid-phase
(when Q57 promotes to resolved on ADR-0014 acceptance in Unit 12.1) and
**20 resolved at Phase-12 close** (this unit's commit).

### No new ADRs anticipated for Phase 12 acceptance

ADR-0014 is the lone Phase-12 ADR. Phase-12 surface is fully covered
by ADR-0014 + the existing 13-ADR set. No follow-on ADRs anticipated
at Unit 12.8 acceptance gate.

### Phase-13+ ADR-promotion candidates (flagged, not surfaced)

- **ADR-0015 candidate** — subscriber-list email provider (per Phase-5
  D-4 punt + ADR-0014 D-D's email-notifications cross-reference). Lean:
  Buttondown / Resend / ConvertKit / Loops. Phase 13+ when subscriber-
  list thread is chosen.
- **ADR-0016 candidate** — multi-provider OAuth (Google / GitLab /
  email-link); lifts ADR-0012 D-B's single-provider restriction. Phase
  13+/14+ when non-GitHub user-segment ask is observed.
- **ADR-0017 candidate** — full §8.6 24-mo collaborator COI enforcement
  + `curatorRoles` DB table (couples to Q7 resolution + Phase-12 Class
  B item 5 + 6). Phase 14+ alongside editorial-board promotion.

None of these are anticipated at Phase-12 acceptance; flagged here for
Phase-13+ planning continuity.

### Smoke gates (this unit)

Docs-only. Will run `pnpm audit-content` to confirm Q32 baseline
unchanged.

- File modified: `OPEN_QUESTIONS.md` (Q57 promoted; one entry edited).
- File added: `docs/thinking/12.7-open-questions-hygiene.md`.
- CHANGELOG: Unit 12.7 entry.
- `audit-content` expected: 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-12 OQ-hygiene: Q57 promoted to resolved (ADR-0014 acceptance);
Q58 stays open (Phase-13+ explicit per ADR-0014 scope cap); no new
Q-numbers surfaced (Q59 + Q60 candidates flagged but NOT promoted);
ledger state 20 resolved + 4 decided-as-lean + 29 open = 53 total
entries; ADR-0014 added (14 ADRs at HEAD; ADRs 0001-0013 unchanged in
body); no prose-shift reconciliations; ADRs 0004 + 0005 + 0011 +
0012 + 0013 reaffirmed / extended by ADR-0014. Phase-13+ candidates
flagged for ADR-0015 (subscriber-list email) + ADR-0016 (multi-provider
OAuth) + ADR-0017 (full §8.6 COI + `curatorRoles`).
