# 21.2 — Phase-21 hygiene status pass (0 Class A / ~9 Class B / Class C unchanged)

Third Phase-21 unit; docs-only. Mirrors Phase-13 through Phase-20
hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-21 cleanup: **0 items**

Phase-21's single code unit (21.1) shipped without deferrals.
The cleanup-orphan-blobs contract is fully realized in
`scripts/cleanup-orphan-blobs.ts`: 5-phase per-run pipeline
(enumerate → DB-snapshot → detect orphans → delete with
try/catch → summary) + parallel enumerate+snapshot via
`Promise.all` + `--dry-run` CLI flag + pathname-pattern filter.
Phase-21-deferred-to-Phase-22+ refinements (`--prefix` flag for
non-avatar blobs / `--verbose` flag for per-blob progress /
concurrency / streaming pagination) are explicit scope-cap
decisions made in Unit 21.0 D-3 + D-4 — NOT Class A.

**Phase-21 Class A count = 0** matches **Phase-12 through
Phase-20 well-scoped pattern — ten consecutive phases** ship
with 0 Class A items.

### Class B — Phase-21 follow-ons: **~9 items in 5 categories**

#### B.1 Image-processing expansion (4; carried from Phase 20 unchanged)

- **Q68 expansion — content moderation on uploaded images**
  (ADR-0019 D-F carryover). ~3-5 units; ADR-0020+ candidate.
  Phase 22+. **Phase-20 backfill pattern reusable** for
  retroactive content-moderation scan if scope includes
  backfill.
- **Cropping UI** (ADR-0019 D-F carryover). `sharp.extract()`
  inserted between `.rotate()` and `.toBuffer()` + client crop
  region selection. ~3-5 units. Phase 22+.
- **Server-side resizing** (ADR-0019 D-F carryover).
  `sharp.resize()` inserted between `.rotate()` and
  `.toBuffer()`. ~1-2 units. Phase 22+ if bandwidth/render
  signal.
- **WebP/AVIF format conversion** (ADR-0019 D-F carryover).
  `sharp.webp()` / `sharp.avif()`. ~1-2 units. Phase 22+ if
  storage cost signal.

**Phase 20 had 4 in B.1; Phase 21 added 0 and closed 0 in B.1**
(B.1 is image-processing expansion; cleanup-orphan-blobs is
storage hygiene, not image processing).

#### B.2 Phase-17 markdown subset expansion (8; carried unchanged)

8 items carried verbatim through Phase 17-20 hygiene units:
tables / footnotes / images / syntax-highlighted code blocks /
bio length expansion / `@mentions` / task-list interactivity /
live preview client boundary. Phase 21 added zero new markdown
subset items.

#### B.3 Phase-18 surface expansion (3 + Q72; carried unchanged)

3 items carried verbatim through Phase 18-20 hygiene units:
markdown rating-action `rationale`; methodology page markdown
unification; per-challenge detail page. **Q72 candidate**
(`reviewNotesSchema` divergence from `bioSchema`) also carried
unchanged.

#### B.4 Phase-20 backfill operational follow-ons (3; carried unchanged)

3 items carried verbatim from Phase-20 Unit 20.2:
- **`--user-id` flag** for surgical single-user backfill
  (`backfill-exif-strip`).
- **`--verbose` flag** for per-row progress + EXIF tag counts
  before/after (`backfill-exif-strip`).
- **Session-level dry-run sentinel** for `backfill-exif-strip`.

#### B.5 Phase-21 cleanup operational follow-ons (2; Phase-21 newly surfaced)

- **`--prefix` flag for non-avatar blob types** —
  `cleanup-orphan-blobs` is avatar-specific Phase 21 (pathname
  filter pins to `avatars/<userId>-<timestamp>(-backfill)?.<ext>`
  shapes). Phase 22+ when project ships non-avatar Vercel Blob
  surfaces (e.g., uploaded content per Q68 expansion +
  cropping UI saved-region cache + WebP transcoded variants),
  generalize via `--prefix` to scope cleanup per surface.
  ~0.5 units when promoted.
- **`--verbose` flag for per-blob progress** —
  `cleanup-orphan-blobs` Phase-21 minimal flag set is
  `--dry-run` only. Phase 22+ if curator demands diagnostic
  output (e.g., per-blob `uploadedAt` filtering window;
  per-blob size statistics). ~0.5 units when promoted.

#### B.6 Operational + infrastructure (7; +1 from Phase 20 for new cleanup script doc)

- Q69 operational gate (`BLOB_READ_WRITE_TOKEN`); Q54 + Q55
  operational gates.
- CI dummy `AUTH_SECRET`.
- `pnpm db:migrate` doc (6 migrations unchanged).
- First LHCI run (19 URLs unchanged).
- Middleware-based auth-route protection (threshold stays at 2;
  Phase 21 added zero protected routes).
- `pnpm backfill-exif-strip` operational doc (Phase-20 carryover).
- **`pnpm cleanup-orphan-blobs` operational doc** — Phase-21
  newly surfaced. Curator-track; should sit alongside
  `backfill-exif-strip` doc + `pnpm db:migrate` doc when
  Phase 22+ writes contributor / operator docs.

### Class C — pre-existing carryovers (unchanged from Phase 20 Unit 20.2)

All ~25 Class C items persist unchanged. The HTML shell migration
+ Unit 8.4 unblock remain ON HOLD per parallel-session
preservation signal.

### Phase-20 → Phase-21 delta

| Metric | Phase 20 close (`f4f498e`) | Phase 21 close pre-this (HEAD `01fc7c3`) | Δ |
|---|---|---|---|
| Commits | 5 | 2 + this hygiene + 21.3 + 21.4 (anticipated) | +5 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | **19** | **19** | **0** (Phase 21 ships no new ADR; **third phase since Phase 3 to close without new ADR** — Phase 18 + 20 + 21) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (**fifth consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 (**zero new env vars Phase 21**) |
| Runtime deps | (Phase-20 stack) | (Phase-20 stack) | 0 (**zero new deps Phase 21**) |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 564 / 54 files | **564 / 54 files** | **0** (Phase 21 ships no new vitest tests; **second consecutive zero-new-test phase** — Phase 20 + 21) |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** (per ADR-0018 D-F invariant extended; cleanup script is CLI not bundled) |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS resolved | 26 | 26 | 0 (anticipated 0 in Unit 21.3 — no new Q-promotions Phase 21) |
| OPEN_QUESTIONS open | 29 | 29 | 0 |
| OPEN_QUESTIONS lean | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 59 | 59 | 0 |
| i18n keys per locale | 127 | 127 | 0 |
| LHCI URLs | 19 | 19 | 0 |
| `lib/storage/` files | 2 | 2 | 0 |
| `scripts/` files | 7 | **8** | **+1** (`cleanup-orphan-blobs.ts`) |
| `package.json#scripts` entries | (+1: `backfill-exif-strip`) | (+1: `cleanup-orphan-blobs`) | +1 |
| Server-side image processing surfaces | 2 (`putAvatar` + backfill script) | 2 | 0 (cleanup script is storage-hygiene, not image-processing) |
| Privacy-by-default surfaces | 2 | 2 | 0 |
| Retroactive-privacy-correction surfaces | 1 | 1 | 0 |
| Storage-reconciliation surfaces | 0 | **1** | **+1** (first in project history) |
| Operational-script-keystone phases | 1 | **2** | **+1** (Phase 20 first; Phase 21 second — establishes pattern as recurring) |

### Architectural firsts shipped in Phase 21

1. **First storage-reconciliation surface** in project history —
   Phase 16-20 trusted storage layer for atomicity (delete-on-replace
   in `updateProfileImage` action; best-effort `del()` in Phase-20
   backfill); Phase 21 introduces explicit DB-vs-storage
   reconciliation (DB is source of truth; storage is the
   side-effect to reconcile).
2. **Second operational-script-keystone phase** in project history —
   Phase 20 was the first (`backfill-exif-strip.ts`); Phase 21
   second (`cleanup-orphan-blobs.ts`). **Establishes the pattern as
   recurring** (not a one-off Phase-20 anomaly). Suggests future
   phases will continue to ship operational scripts as keystones
   when previously-pinned ADR clauses or class-B-tracked
   operational follow-ons need implementation.
3. **Fifth consecutive 0-migration phase** (Phase 17 + 18 + 19 +
   20 + 21; 8 of 12 phases since DB landed are 0-migration:
   Phase 10 + 13 + 14 + 17 + 18 + 19 + 20 + 21).
4. **Fifth consecutive single-session phase** (Phase 17 + 18 + 19
   + 20 + 21; Phase 16 was the parallel-session phase). Demonstrates
   that the constitution + auto-memory + ADR ledger converge work
   regardless of session count given well-scoped phases.
5. **Third phase since Phase 3 to close without new ADR** (Phase
   18 first via ADR-0018 D-G inheritance; Phase 20 second via
   ADR-0019 D-E impl; Phase 21 third via class-B carryover close).
   **Broadens the precedent**: ADR-pinned Phase-N+ contracts AND
   class-B-tracked operational follow-ons both qualify for
   no-new-ADR phases.
6. **Second consecutive zero-new-dep/test/ADR phase** (Phase 20 +
   21). **Operational-script-keystone pattern correlates with
   zero-new-architectural-surface** — both Phase 20 and Phase 21
   ship entirely on existing primitives (`sharp` from Phase 19 +
   `@vercel/blob` from Phase 16 + drizzle-orm from Phase 9).
7. **First phase to combine `Promise.all`-parallel read-only
   operations** in an operational script — Unit 21.1's
   `enumerateAvatarBlobs()` + `snapshotInUseUrls()` run
   concurrently via `Promise.all` because both are idempotent +
   side-effect-free. Phase-20 backfill script was serial-only.

### Phase-22+ Q-candidates flagged

Carryovers from Phase 20 (8 candidates carried; 0 promoted Phase
21; 0 new Phase 21):

- **Q59** (CLI emit-challenge-action) — carried Phase-12 through
  Phase-21 = **10 phases**.
- **Q60** / **Q61** / **Q62** / **Q64** / **Q65**.
- **Q68 expansion** (content moderation on uploaded images;
  ADR-0019 D-F integration point + Phase-20 backfill pattern
  reusable for content-moderation backfill).
- **Q72** (`reviewNotesSchema` divergence).

Total **8 Phase-22+ Q-candidates** carrying (unchanged from
Phase 20 close).

### Phase-22+ ADR slot reservations

ADR-0020 candidate slot remains open after **8-phase ADR cycle
without multi-provider-OAuth claim** (Phase-15 ADR-0016 +
Phase-16 ADR-0017 + Phase-17 ADR-0018 + Phase-18 NO-new-ADR +
Phase-19 ADR-0019 + Phase-20 NO-new-ADR + Phase-21 NO-new-ADR =
4 ADR slots since Phase-15 + 4 cycles without multi-provider
claim):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried **11 phases** — strongest architectural "patience
  signal" growing each cycle without claim).
- **Image-processing API content moderation** (Q68 expansion;
  ADR-0019 D-F inheritance compose pattern).
- **Markdown subset extensions ADR** (Phase-17 D-H + Q72
  candidate).
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  **15+ phases** — strongest overall "patience signal" growing).
- **Per-challenge detail page** (Phase-11 + 13 carryover; may
  not need new ADR).

### Two consecutive operational-script-keystone phases pattern

Phase 20 and Phase 21 share an unusually similar shape:

| Property | Phase 20 (backfill-exif-strip) | Phase 21 (cleanup-orphan-blobs) |
|---|---|---|
| Units | 5 | 5 |
| Code units | 1 | 1 |
| New ADRs | 0 | 0 |
| New deps | 0 | 0 |
| New env vars | 0 | 0 |
| New vitest tests | 0 | 0 |
| New migrations | 0 | 0 |
| New UI affordances | 0 | 0 |
| New i18n keys | 0 | 0 |
| New `scripts/` files | 1 | 1 |
| Closes carryover | ADR-0019 D-E | Phase-16 + Phase-20 (class-B) |
| Continues precedent | (established Phase 20) | (continues Phase 20) |

This is **the tightest two-phase cadence in project history**.
Demonstrates that operational-script-keystone phases compress
faster than UI/runtime-keystone phases because they ship on
existing primitives with no architectural surface to author or
validate.

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/21.2-phase-21-hygiene.md` (this).
- CHANGELOG: Unit 21.2 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 21.2 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-20 well-scoped pattern (ten
consecutive); ~9 Class B items in 6 categories (4 image-
processing expansion carryovers from Phase 20 B.1; 8 Phase-17
markdown subset carryovers; 3 Phase-18 surface carryovers + Q72;
3 Phase-20 backfill operational follow-ons carried; 2 Phase-21
cleanup operational follow-ons newly surfaced; 7 operational +
1 new cleanup script doc); Class C carryovers unchanged; Phase
20 → 21 delta (0 ADR Δ; 0 migration Δ; 0 runtime dep Δ; 0 test
Δ; 0 i18n Δ; 0 bundle Δ; +1 `scripts/` file; +1
storage-reconciliation surface; +1 operational-script-keystone
phase counter); 7 Phase-21 architectural firsts (incl.
storage-reconciliation surface + 2nd operational-script-
keystone phase + 5th consecutive 0-migration + 5th consecutive
single-session + 3rd phase without new ADR + 2nd consecutive
zero-new-dep/test/ADR phase + first `Promise.all`-parallel
operational script); 8 Phase-22+ Q-candidates carry (unchanged
from Phase 20; Q59 carried 10 phases); ADR-0020 candidate slot
remains open after 8-phase cycle without multi-provider-OAuth
claim (Phase-9 Class B item 8 now carried 11 phases); two
consecutive operational-script-keystone phases pattern noted
(tightest two-phase cadence in project history).
