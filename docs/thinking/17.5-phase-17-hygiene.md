# 17.5 — Phase-17 hygiene status pass (0 Class A / 17 Class B / Class C unchanged; ADR-0019+ candidates flagged)

Sixth Phase-17 unit; docs-only. Mirrors Phase-13 Unit 13.4 +
Phase-14 Unit 14.6 + Phase-15 Unit 15.6 + Phase-16 Unit 16.6
hygiene catalog patterns. Catalogs what's in-flight after Phase
17's three code units (17.2 + 17.3 + 17.4) and what survives as
follow-ons or carryovers heading into Units 17.6 (OPEN_QUESTIONS
hygiene) + 17.7 (acceptance gate).

## THINK

### Class A — in-flight Phase-17 cleanup: **0 items**

Phase-17's three code units shipped without deferrals. All eight
ADR-0018 D-clauses are either code-realized (D-A library; D-B
subset; D-C heading demotion; D-D URL allow-list; D-E render
path on BOTH `/u/{handle}` + `/profile`; D-F server-side-only) or
text-only deferrals (D-G inheritance contract; D-H Phase-18+
deferrals).

**Phase-17 Class A count = 0** matches **Phase-12 + Phase-13 +
Phase-14 + Phase-15 + Phase-16's well-scoped pattern** — five
consecutive 9-unit-shaped phases (Phase 17 ships 8 units;
slightly shorter due to no DB migration scope) ship with 0 Class
A items.

The /profile read-mode preview was anticipated as a Phase-17
Class A in-flight follow-on after Unit 17.3 deferred it. Unit
17.4 closed it explicitly + added the 3 new i18n keys
(`bio_preview_heading` + `bio_preview_aria_label` + `bio_hint`
text update) per atomic-i18n discipline; ADR-0018 D-E now fully
realized across both integration points.

### Class B — Phase-17 follow-ons: **17 items** in 3 categories

#### B.1 Markdown subset expansion (8)

- **Tables (GFM)** — ADR-0018 D-B + D-H deferral. Phase 18+ if
  signal demands; schema extension to `bioSchema.tagNames`
  adding `table` / `thead` / `tbody` / `tr` / `th` / `td`.
- **Footnotes (GFM)** — ADR-0018 D-B + D-H deferral. Phase 18+ if
  signal demands; needs `remark-footnote` config.
- **Images** (`![alt](url)`) — ADR-0018 D-B + D-H deferral. Phase
  18+; couples to image-URL sanitization (Vercel Blob URL
  pattern + external URL allowlist from Phase 16 work; reuses
  existing patterns).
- **Syntax-highlighted code blocks** — ADR-0018 D-H deferral.
  Phase 18+; separate dep (Prism / Shiki / `highlight.js`; ~80-
  300 kB transitive); Shiki already shipped in project for
  Velite MDX pipeline (could reuse).
- **Bio length expansion** beyond 280 chars — ADR-0017's bio cap
  preserved Phase 17; Phase 18+ if signal demands (500-1000
  chars).
- **`@mentions`** (linking @handle to `/u/{handle}`) — ADR-0018
  D-H deferral. Phase 18+ if signal demands; needs `@`-handle
  detection + `/u/{handle}` link rewrite + handle existence
  check.
- **Task-list interactivity** — ADR-0018 D-B Phase-17 ships
  read-only checkboxes; Phase 18+ if signal demands interactive
  task tracking.
- **Live-preview client boundary on edit form** — ADR-0018 D-H
  deferral. Phase 18+ if signal demands; **caveat**: breaks the
  103 kB First Load JS invariant (markdown deps server-only
  today; client-side preview would pull `react-markdown` or
  similar onto client bundle).

#### B.2 Surface expansion (4)

- **Markdown-rendered curator review notes** (`ratingChallenge.reviewNotes`)
  — Phase-15 Class B B.2 item 5 carryover; ADR-0018 D-G
  inheritance contract documents the pattern (new
  `renderReviewNotesMarkdown` sibling helper + `reviewNotesSchema`
  derived schema). ~1-2 units when promoted.
- **Markdown in rating-action `rationale`** — Phase-18+ if curator
  demand; same inheritance pattern.
- **Methodology page markdown** — long-term; Velite's MDX pipeline
  handles `/methodology` content today (separate compilation
  path); inheriting `lib/markdown/` would unify USER-STATE
  markdown + content-file MDX rendering but is a larger
  architectural decision.
- **Per-user schema configurability** — ADR-0018 D-H deferral.
  Defer; single schema for Phase 17.

#### B.3 Operational + infrastructure (5)

- **Q69 operational gate** (`BLOB_READ_WRITE_TOKEN` provisioning)
  — Phase-16 carryover. Architecture complete; deployment unblock
  pending curator action via Vercel dashboard + `vercel env pull`.
  Parallel to **Q54 + Q55**.
- **Q54 + Q55 carryovers** (Phase-9 operational gates) —
  unchanged from prior phases.
- **CI dummy `AUTH_SECRET`** — Phase-9 Class A item 3 carryover.
- **`pnpm db:migrate` doc** for new contributors — `/contributing`
  v1.2 carryover. Now **6 migrations** to run on first setup
  (unchanged from Phase 16).
- **First LHCI run** validating Phase-14/15/16/17 auth-aware
  surfaces. 19 LHCI URLs unchanged (Phase 17 added zero new
  protected page routes; threshold stays at 2; markdown
  rendering is rendered server-side without affecting auth
  policy).
- **Middleware-based auth-route protection threshold** stays at
  2. Phase 17 added zero protected page routes; bio markdown
  rendering inherits Phase-15 `/profile` protected status
  (single protected route alongside curator dashboard).

### Class C — pre-existing carryovers (unchanged from Phase 16 Unit 16.6)

All ~25 Class C items from Phase 16's hygiene catalog persist
unchanged. Notable items:

- **Orphan `components/domain-tile-grid/` deletion** (curator
  authorization).
- **`entries.json` backfill on 8 problems**.
- **W3C feed validator passes** (Phase-3/5/6/8 carryover).
- **HTML shell migration + Unit 8.4 unblock** STILL ON HOLD per
  parallel-session preservation signal.
- **Abandoned-blob cleanup script** (Phase-16 Class B; orphans
  tolerated until script lands; Phase 17 added zero blobs).
- **External URL allowlist composability** (ADR-0017 Option 2;
  Phase-16 deferral).
- **SiteHeader avatar pill / avatar-dropdown** (Phase-14 Class
  B; Phase-16 forward-compat'd `getUserMetadataById` with
  imageOverride; Phase 17 added zero changes).
- **EXIF stripping** (Q70 candidate from Phase 16; couples to
  Q68 expansion for image content moderation).
- **GFM tables / footnotes / images / syntax-highlighting** —
  Phase-17 newly added to deferral catalog per ADR-0018 D-H.
- **Mobile-nav variant + form-state preservation** — Phase-14/15
  carryovers.
- **Rate-limiting on review API** — Phase-12 carryover.

### Phase-16 → Phase-17 delta

| Metric | Phase 16 close (`2ea957b`) | Phase 17 close pre-this (HEAD `96ea82c`) | Δ |
|---|---|---|---|
| Commits | 9 | 4 + this hygiene + 17.6 + 17.7 (anticipated) | +7 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | 17 | **18** | **+1** (ADR-0018) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (first phase since Phase 9 to ship zero migrations) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-16 stack) | **+8 net** (`unified` + `remark-parse` + `remark-gfm` + `remark-rehype` + `rehype-sanitize` + `rehype-stringify` + `unist-util-visit` + `@types/hast`; ~120 kB transitive server-only) | +8 |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 519 / 53 files | **552 / 54 files** | **+33 / +1** |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS open count | 30 | 30 (Q66 promotion in Unit 17.6 will leave open at 30 net: -1 promoted, 0 newly opened) | 0 anticipated |
| OPEN_QUESTIONS resolved count | 23 | 23 (Q66 promotes to 24 in Unit 17.6) | 0 (anticipated +1 in 17.6) |
| OPEN_QUESTIONS decided-as-lean count | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 57 | 57 (anticipated +1 in 17.6) | 0 |
| i18n keys per locale | 124 | **127** | **+3** (`bio_preview_heading` + `bio_preview_aria_label` + `bio_hint` text update) |
| LHCI URLs | 19 | 19 | 0 |
| `lib/markdown/` files | 0 | **3** | **+3** (`index.ts` + `sanitize-schema.ts` + `index.test.ts`) |
| XSS-audit surfaces | 0 | **1** | **+1** (`renderBioMarkdown` pipeline) |
| `dangerouslySetInnerHTML` surfaces | 0 | **2** | **+2** (/u/{handle} bio + /profile bio preview) |

### Architectural firsts shipped in Phase 17

1. **First markdown processing pipeline in project history** —
   no prior code path interprets markdown (Velite's MDX
   pipeline handles content files via separate compilation, not
   USER-STATE render at request time).
2. **First XSS-audit surface in project history** — sanitization
   schema (`bioSchema`) + URL allow-list (`rehypeStripUnsafeHrefs`
   defense-in-depth plugin) + multi-stage pipeline boundary
   (parse → transform → sanitize → stringify).
3. **First three-step pipeline boundary** — `unified` shape
   names stages explicitly; Phase-18+ markdown surfaces inherit
   via sibling helpers per ADR-0018 D-G.
4. **First `dangerouslySetInnerHTML` surface in project
   history** (two total in Phase 17: `/u/{handle}` bio + `/profile`
   bio preview); XSS-safe by ADR-0018 sanitization. Pattern:
   comment block above the surface citing the sanitization-
   pipeline-is-trusted invariant.
5. **First phase since Phase 9 to ship zero migrations**
   (Phase 11 + 12 + 15 + 16 each shipped 1; Phase 10 + 13 + 14
   shipped 0; Phase 17 is the second 0-migration phase since the
   DB landed).
6. **First new runtime dep cluster since Phase 16** (~120 kB
   server-only; 8 new packages); 1-phase dependency-discipline
   interval.
7. **First i18n key TEXT UPDATE** (`bio_hint` phrasing changed
   from "Plain text (no markdown)." to "Markdown supported
   (bold, italic, links, lists, code).") since the i18n
   namespace was first added in Phase 7. Phase-10 through Phase-
   16 all ADD-only patterns; Phase 17 introduces the first
   "doc-correction" update.
8. **First two-surface markdown render consistency** — same
   prose styling utilities consumed across `/u/{handle}` public
   surface + `/profile` private read-mode preview.

### Phase-17+ Q-candidates flagged but NOT promoted

Eight candidates stay flagged for Phase-18+ kickoff (carry from
prior phases + Phase-17 deferrals):

- **Q59 candidate** (CLI `pnpm emit-challenge-action <id>`) —
  carried Phase-12/13/14/15/16/17; smallest scope.
- **Q60 candidate** (curator authz evolution beyond env-var) —
  couples to editorial-board (Q7).
- **Q61 candidate** (submitter anonymity option) — Phase-13
  carryover.
- **Q62 candidate** (rejection-rationale public visibility) —
  Phase-13 carryover.
- **Q64 candidate** (per-user privacy opt-out) — Phase-14
  carryover; ~3 units.
- **Q65 candidate** (per-curator activity feed) — Phase-14
  carryover.
- **Q68 expansion** (content moderation on uploaded images
  alongside bio text) — Phase-15/16 carryover/expansion.
- **Q70 candidate** (EXIF stripping on uploaded images) —
  Phase-16 carryover.

### Phase-18+ ADR slot reservations

- **ADR-0019 candidate** options (in rough decreasing strength):
  - Multi-provider OAuth expansion (Phase-9 Class B item 8
    carried 8 phases; previously ADR-0016 / 0017 / 0018
    candidate — slots claimed by Phase 15 + 16 + 17 respectively).
  - Image-transcoding pipeline (Q70 EXIF + Q68 expansion) —
    `sharp` vs external service.
  - Content moderation API (Q68 + Q68 expansion) — OpenAI
    moderation / Perspective / custom regex+wordlist.
  - Markdown subset extensions (tables / footnotes / images /
    syntax-highlighting) — schema-extension ADR if Phase-17
    deployment surfaces signal.
  - Multi-surface markdown render (curator review notes /
    rating-action rationale) — composes Phase-17 `lib/markdown/`
    pattern.

### Phase-17 architectural inheritance shape

Phase 17 establishes the **`lib/markdown/` module pattern** that
Phase-18+ markdown surfaces inherit:

```
lib/markdown/
├── index.ts                  ← renderBioMarkdown (Phase 17)
│                                renderReviewNotesMarkdown (Phase 18+)
│                                renderRationaleMarkdown (Phase 18+)
├── sanitize-schema.ts        ← bioSchema (Phase 17)
│                                reviewNotesSchema (Phase 18+; sibling)
│                                rationaleSchema (Phase 18+; sibling)
└── index.test.ts             ← 33 tests Phase 17
                                Phase-18+ surfaces extend test file
                                or add sibling test files
```

Phase-17 D-G inheritance contract: each surface gets its own
helper + schema per ADR-0018 D-G (NOT a single shared
`renderMarkdown` + `defaultSchema`). Audit boundary stays
explicit per surface.

### Smoke gates

Docs-only — no source files modified beyond CHANGELOG +
OPEN_QUESTIONS in subsequent unit + this THINK doc + the
hygiene CHANGELOG entry.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  since Phase 2; unchanged through every Phase 3-17 unit).
- File added: `docs/thinking/17.5-phase-17-hygiene.md` (this).
- CHANGELOG: Unit 17.5 entry pinning the catalog + deltas.
- typecheck / test / build untouched.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 17.5 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12+13+14+15+16 well-scoped pattern (five
consecutive); 17 Class B items in 3 categories (8 markdown
subset expansion + 4 surface expansion + 5 operational +
infrastructure); Class C carryovers unchanged from Phase 16
Unit 16.6; Phase 16 → 17 delta table (18 ADRs / 6 migrations /
10 users columns / 552 tests / 0 bundle regression / +8 deps /
+3 i18n keys per locale / +3 lib/markdown files / +1 XSS-audit
surface / +2 dangerouslySetInnerHTML surfaces); 8 Phase-17
architectural firsts; 8 Phase-18+ Q-candidates flagged; ADR-0019+
slot options enumerated; `lib/markdown/` inheritance shape
documented.
