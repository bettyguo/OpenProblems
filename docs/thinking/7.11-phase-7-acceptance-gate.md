# 7.11 — Phase 7 acceptance gate (Bilingual rendering thread)

Phase-7 closing unit. Mirrors Units 1.12 / 2.13 / 3.13 / 4.13 / 5.13 / 6.10. Verifies every §13 Phase-6+ thread-deliverable that this phase pulled (the **Bilingual rendering** thread per Unit 7.0 D-1) is operational locally at HEAD, emits the cross-phase roll-up, and lists Phase-7 follow-ons that survive into Phase 8+.

## THINK

### §13 Phase-6+ scope clarification

`MASTER_PROMPT.md` §13 names four thread candidates — Discussions, read+write API with token auth, bilingual rendering (FR primary), Montréal-adjacent surfaces — without an enumerated cross-phase contract. Phase 6 (Unit 6.0 D-1) pulled **Discussions** as the first thread and closed it at the Unit 6.10 acceptance gate. Phase 7 (Unit 7.0 D-1) pulled **Bilingual rendering** as the second thread; this acceptance gate verifies that thread's deliverable surface. Auth + subscriber-list + monetization stay deferred for future open-ended phases.

### §13 Bilingual thread — local pass status

| Surface                                                                                | Phase-7 unit | Status |
|----------------------------------------------------------------------------------------|--------------|--------|
| ADR-0011 pins i18n strategy (next-intl + sub-path routing + sibling-file storage)      | Unit 7.1     | ✓ Accepted 2026-05-16; D-A through D-G cover runtime / routing / storage / fallback / slug-strategy / locale-toggle UI / translation-provenance. |
| `next-intl@^3.x` runtime + `lib/i18n/` infrastructure + seed messages                  | Unit 7.2     | ✓ `lib/i18n/routing.ts` (locales source-of-truth) + `lib/i18n/request.ts` (per-locale switch loader) + `messages/{en,fr}.json` seed catalogues + `next.config.ts` plugin wrap; 9 vitest tests. |
| `app/[locale]/` layout + about-page wiring proof                                       | Unit 7.3     | ✓ `app/[locale]/layout.tsx` + `app/[locale]/about/page.tsx` (2 SSG pages: `/en/about` + `/fr/about`); coexists with bare `/about`. Bulk-migration of other 21 routes deferred to Unit 7.3a. |
| Velite sibling-file pattern + content-schema plumbing (5 collections)                  | Unit 7.4     | ✓ `lang` derived from `.fr` infix; `translation_source` enum required on translated files; canonical Zod schemas (`problem.ts` + `paper.ts`) mirror via `TranslationSourceSchema`. |
| `/methodology` FR pilot (first end-to-end sibling-file consumer)                       | Unit 7.5     | ✓ `content/methodology/v1.fr.mdx` (machine-assisted, curator-reviewable) + `lib/i18n/load-localized.ts` resolver + `app/[locale]/methodology/{page,[version]/page}.tsx`; 4 new SSG pages (`/en/methodology` + `/fr/methodology` + per-version × 2 locales). |
| `/methodology` LHCI enrolment + sitemap locale-alternates                              | Unit 7.5a    | ✓ `lighthouserc.json` 16 → **18** URLs; `lib/sitemap/build-sitemap.ts` adds alternates to `/methodology` + per-version entries; 2 new sitemap-test assertions. |
| `components/locale-toggle/` site-header UI                                             | Unit 7.6     | ✓ `"use client"` toggle next to `ThemeToggle`; pathname-based locale detection; renders only on `/[locale]/...` routes (fail-closed on bare paths); 11 vitest tests. |
| `lighthouserc.json` enrolment for `[locale]/about` pilot URLs                          | Unit 7.7     | ✓ 14 → 16 URLs (added `/en/about` + `/fr/about`); inherits canonical perf/a11y/seo ≥ 0.95 gates; first LHCI run lands on first PR with these commits. |
| `app/sitemap.ts` + Q48 sitemap-half closure                                            | Unit 7.8     | ✓ `lib/sitemap/build-sitemap.ts` enumerates 80+ URLs; locale alternates on `/about`; Q48 closed (link-half from Unit 6.3/6.5 + sitemap-half here). |
| `app/robots.ts` (sister convention to `app/sitemap.ts`)                                | Unit 7.8a    | ✓ Permissive baseline (`User-agent: *` / `Allow: /`) + `Sitemap:` pointer + `Host:` directive; reuses `SITE` constant; 3 vitest tests. |
| Phase-7 hygiene status pass (Class A in-flight + Class B follow-ons + Class C carry)   | Unit 7.9     | ✓ Catalogs Unit 7.3a as in-flight (Class A); enumerates 11 Class B follow-ons + 10 Class C carryovers from prior phases. |
| OPEN_QUESTIONS hygiene + ADR review (Phase 7 pre-close)                                | Unit 7.10    | ✓ Q52 + Q53 promoted to `resolved 2026-05-16`; Q51 stays `decided-as-lean` (curator-track cadence is an audit trail, not a gate); 11 ADRs reviewed; none superseded. |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: `/en/about` + `/fr/about` enrolled in Unit 7.7; `/en/methodology` + `/fr/methodology` enrolled in Unit 7.5a. **Documented risk** (Unit 7.7 / Unit 7.9 Class B): `app/layout.tsx` sets `<html lang="en">` statically while `/fr/*` renders FR content; axe's `html-has-lang` rule may flag content-vs-attribute mismatch on first CI run. One-line fix in `app/[locale]/layout.tsx`; absorbed into Unit 7.3a or a focused follow-on if CI signals.
- **W3C feed validator on the per-domain digest + `/api/v1/rss.xml`**: deferred to first preview deploy + Q47 — same Q27-class pattern as prior phases. Unchanged from Phase-6 acceptance.
- **Visual-regression baselines** for `/en/about`, `/fr/about`, `/en/methodology`, `/fr/methodology` × 2 themes × N viewports — deferred Phase-7 follow-on; no Playwright spec changes this phase. Future PR can pass `playwright test --update-snapshots`.
- **No auto-merge** (ADR-0009): Phase 7 did NOT add any LLM-drafting paths. `content/methodology/v1.fr.mdx` is marked `translation_source: machine-assisted` per ADR-0011 D-G — an honesty signal, not an LLM-CLI artefact; the file was curator-reviewable and applied via the standard git-apply flow.
- **File-first / no DB** (ADR-0004): held. FR content lives as `.fr` siblings next to EN canonicals. DB-migration trigger re-eval at HEAD: `tar -czf .velite/` jitter still ~1.4% of 5 MB threshold (203 content files; sibling-file FR backfill would scale to ~400 at full rollout, still under the `> 600` files trigger).
- **First Load JS shared chunk = 103 kB**: held through every Phase-7 unit. `next-intl` server-side modules don't bundle into page chunks; `NextIntlClientProvider` adds weight only when downstream client components actually call `useTranslations` (LocaleToggle uses `usePathname` deliberately to avoid the provider-scope dependency).

### Phase-7 unit summary (12 units shipped — Bilingual thread)

| Unit  | Commit    | Title                                                                                           |
| ----- | --------- | ----------------------------------------------------------------------------------------------- |
| 7.0   | `b8bf23c` | Phase 7 prep (Bilingual-first thread; 12-unit breakdown + DB-trigger 1.433% re-eval)            |
| 7.1   | `6c7a52d` | ADR-0011 — i18n strategy (next-intl + sub-path + sibling-file)                                  |
| 7.2   | `c9d8215` | `next-intl` runtime install + `lib/i18n/` infrastructure + seed messages                        |
| 7.3   | `20319ac` | `app/[locale]/` layout + about-page wiring proof (deliberate scope cut)                         |
| 7.4   | `f315458` | Velite sibling-file pattern + content-schema plumbing                                           |
| 7.5   | `c5e5c7f` | Methodology FR pilot (first end-to-end sibling-file consumer)                                   |
| 7.5a  | `f8d92ec` | Methodology FR pilot follow-ons (LHCI enrolment + sitemap locale-alternates)                    |
| 7.6   | `d05fe97` | `components/locale-toggle/` site-header UI                                                      |
| 7.7   | `bd2d535` | `lighthouserc.json` enrolment for the `[locale]/about` pilot URLs                               |
| 7.8   | `365f764` | `app/sitemap.ts` + Q48 sitemap-half closure (Phase-6 carryover)                                 |
| 7.8a  | `e6a8da8` | `app/robots.ts` (sister convention to `app/sitemap.ts`)                                         |
| 7.9   | `e9efb6b` | Phase-7 hygiene status pass (Class A in-flight + Class B + Class C deferrals)                   |
| 7.10  | `6c8593a` | OPEN_QUESTIONS hygiene + ADR review (Q52 + Q53 → resolved)                                      |
| 7.11  | _this_    | Phase 7 acceptance gate                                                                         |

Total: **13 commits** comprising **12 numbered units** (7.0 – 7.10) + **1 follow-on unit** (7.5a) + **this gate** (7.11). Matches the 11-unit Phase-6 cadence and is tighter than the 14-unit Phase 0-5 cadence; Unit 7.0 had predicted "12 units (7.0 – 7.11)" — 7.5a was an unplanned follow-on, 7.3a was scope-cut to in-flight, so unit-count parity holds within ±1.

Non-linear landing order documented at the time: 7.3 → 7.4 → 7.6 → 7.7 → 7.8 → 7.10 → 7.9 → 7.8a → 7.5 → 7.5a (per the parallel-curator workflow surfaced in `reference_parallel_curator.md` auto-memory; both sessions checked `git log` + `git status` before each unit, yielded on collision).

### State at HEAD (Unit 7.11)

- **Content unchanged from Phase-6 close at content-count level**, plus **1 new FR sibling MDX**: `content/methodology/v1.fr.mdx` (Unit 7.5). All other counts as at Phase-6 close: 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors / 14 institutions / 20 rating actions / 4 issue templates / methodology v1 (EN + **FR**) / contributing v1.0 + v1.1 / 2 `entries.json` files. **Phase 7 added infrastructure + 1 FR content file**, not bulk content backfill (per Q51 lean — curator-track work in parallel).
- **New Phase-7 surfaces** (relative to Phase-6 close at `bb8f816`):
  - 2 SSG pilot pages: `/en/about` + `/fr/about` (Unit 7.3).
  - 4 SSG methodology pages: `/en/methodology` + `/fr/methodology` + `/en/methodology/v1.0.0` + `/fr/methodology/v1.0.0` (Unit 7.5).
  - 1 sitemap surface: `/sitemap.xml` (Unit 7.8) with locale alternates on `/about` + `/methodology` + per-version methodology entries (Unit 7.5a).
  - 1 robots surface: `/robots.txt` (Unit 7.8a).
  - 1 site-header control: `LocaleToggle` (Unit 7.6) — renders on `[locale]/...` routes only until Unit 7.3a's bulk migration surfaces it site-wide.
  - 1 Velite-collection augmentation: `lang` derive + `translation_source` enum on 5 collections (Unit 7.4).
  - 2 i18n helpers: `lib/i18n/locale-filename.ts` (Unit 7.4) + `lib/i18n/load-localized.ts` (Unit 7.5).
  - 1 sitemap helper: `lib/sitemap/build-sitemap.ts` (Unit 7.8; extended in 7.5a).
- **341 prerendered pages** (was 333 at Phase-6 close → 335 after Unit 7.3 +2 pilot pages → 336 after Unit 7.8 +`/sitemap.xml` → 337 after Unit 7.8a +`/robots.txt` → 341 after Unit 7.5 +4 methodology pages). Audit: 333 + 2 + 1 + 1 + 4 = 341 ✓.
- **First Load JS shared chunk = 103 kB UNCHANGED** through every Phase-7 unit. The LocaleToggle component falls below the 1 kB reporting threshold; methodology FR routes are SSG with server-side resolution; sitemap + robots are server-only. `next-intl`'s server-side import surface bundles into route chunks (≤ 213 B per `/[locale]/...` route in build output).
- **383/383 vitest tests across 44 files** (was 323/38 at Phase-5 close → 323/38 at Phase-6 close; Phase 7 added **+60 tests across +6 files**): `lib/i18n/routing.test.ts` (9 in Unit 7.2) + `lib/i18n/locale-filename.test.ts` (9 in Unit 7.4) + `lib/schemas/problem.test.ts` (+3 in Unit 7.4) + `lib/schemas/paper.test.ts` (+2 in Unit 7.4) + `components/locale-toggle/index.test.tsx` (11 in Unit 7.6) + `lib/sitemap/build-sitemap.test.ts` (13 in Unit 7.8) + `lib/sitemap/build-sitemap.test.ts` (+2 in Unit 7.5a) + `lib/i18n/load-localized.test.ts` (8 in Unit 7.5) + `app/robots.test.ts` (3 in Unit 7.8a). Total deltas: 9 + 9 + 3 + 2 + 11 + 13 + 2 + 8 + 3 = **60** ✓.
- `pnpm validate-content` → **203 files** green (unchanged; MDX is not validated by `validate-content` — `content/methodology/v1.fr.mdx` flows through Velite's refine instead).
- `pnpm audit-content` → **0 errors / 6 warnings** (Q32-expected baseline since Phase 2; unchanged).
- `pnpm typecheck` clean. `pnpm build` clean compile (~3s).
- **5 visualizations live** (unchanged from Phase 4): RatingRadar, SaturationCurve, MoversBoard, RatingHistoryStream, DomainMap.
- **11 ADRs** (added ADR-0011 in Phase 7): 0001-0010 unchanged + **0011 (i18n strategy — next-intl + sub-path + sibling-file)** new.
- **1 new dependency in Phase 7**: `next-intl@^3.x` (pnpm resolved `3.26.5`). Pure JS; no postinstall; no `pnpm-workspace.yaml` `allowBuilds` change needed. ADR-0011 D-A version-pin held against pnpm's auto-pick of 4.x via ADR-immutability discipline.
- **`lighthouserc.json`** enrols **18 URLs** (was 14 at Phase-6 close; +2 from Unit 7.7 for `/en/about` + `/fr/about`; +2 from Unit 7.5a for `/en/methodology` + `/fr/methodology`).
- **OPEN_QUESTIONS state**: 4 new Phase-7 questions surfaced (Q50-Q53); all 4 retired this phase. **Q50** closed by ADR-0011 D-A in Unit 7.1; **Q52** closed by Unit 7.4 (schema implementation); **Q53** closed by Unit 7.1 (ADR-0011 D-G pin); **Q51** stays `decided-as-lean` as an audit trail of "Phase 7 acceptance is infrastructure-complete, not content-complete." Q48 (Phase-6 partial) closed by Unit 7.8 (sitemap-half). Phase-7 net ledger movement: 5 resolutions (Q48 + Q50 + Q52 + Q53; Q51 stays lean).

### `lighthouserc.json` final state

Phase 7 added 4 URLs: `/en/about` + `/fr/about` (Unit 7.7) + `/en/methodology` + `/fr/methodology` (Unit 7.5a). **18 URLs total.** Same cohort-strategy as prior phases — one representative URL per route-template per locale.

### Phase-7 follow-ons that survive the gate (non-blocking)

Same Q27-class pattern as prior phases — CI / deployed-URL artifacts must refresh on the first PR. Phase-7 specifics overlay the canonical list.

1. **Unit 7.3a (bulk page migration + middleware)** — 22-file structural change scope-cut from Unit 7.3. Surfaces every existing route under `app/[locale]/<route>/page.tsx`, lands `middleware.ts` with `localePrefix: "always"` per ADR-0011 D-B, and updates link-generators (`lib/digest/build-digest.ts` + `TALK_PATHNAME_REGEX` + `lighthouserc.json` URL prefixes). **Held over from Phase 7**: the Bilingual thread is **infrastructure-complete without it** (the pilot routes prove the pipeline end-to-end), but the §13 "FR primary" goal is only **partially realized** in terms of route coverage. Phase 8's first unit may choose to land 7.3a as a prerequisite OR defer it to a curator-track follow-on. **No Phase-8 dependency** is gated on 7.3a today.
2. **First LHCI run for the 4 new pilot URLs** — `/en/about`, `/fr/about`, `/en/methodology`, `/fr/methodology` enrolled but unrun until first PR. Documented `html-has-lang` axe-rule risk on FR pages (Unit 7.7 CHANGELOG flag).
3. **Visual-regression baselines** for the 4 pilot pages × 2 themes × N viewports — `chromium-win32` baselines not captured this phase (no Playwright spec changes). Future PR can pass `playwright test --update-snapshots`.
4. **`<html lang={locale}>` fix** in `app/[locale]/layout.tsx` — Unit 7.7's documented axe-rule risk; one-line fix; lands when CI flags it or when 7.3a moves SiteHeader under `[locale]`.
5. **`SITE` constant extraction** into `lib/site-url.ts` — currently 3 hardcoded call sites (`lib/digest/rss.ts`, `app/api/v1/rss.xml/route.ts`, `lib/sitemap/build-sitemap.ts`); threshold for extraction is 5+ per Unit 7.8 follow-on.
6. **`LOCALE_ALTERNATE_ROUTES` table extraction** in `lib/sitemap/build-sitemap.ts` — currently 2 entries (`/about` + `/methodology`); threshold for extraction is 5+ per Unit 7.5a follow-on.
7. **`scripts/validate-content.ts` glob extension** — currently uses LITERAL filename lookups (`path.join(problemDir, "problem.yaml")`); will need extension when first `*.fr.yaml` content lands (only `*.fr.mdx` exists today from Unit 7.5).
8. **Per-entry `lastModified` / `changeFrequency` / `priority`** on sitemap entries — observation-driven if Lighthouse SEO flags omissions (Google deprecated reliance on the latter two in 2023).
9. **Locale alternates beyond `/about` + `/methodology`** in sitemap — depends on Unit 7.3a expanding `[locale]/<route>` coverage; sitemap-test "no alternates without [locale] shadow" invariant blocks speculative additions.
10. **`app/[locale]/page.tsx` home page** — `/en/` + `/fr/` 404 at HEAD; depends on Unit 7.3a.
11. **`NEXT_LOCALE` cookie writes** from middleware — depends on Unit 7.3a middleware.
12. **`useTranslations` for LocaleToggle aria-label** — currently hardcoded string pair (`"Passer au français"` / `"Switch to English"`); depends on Unit 7.3a moving SiteHeader under `NextIntlClientProvider`.
13. **Bilingual content backfill** (problem.yaml + paper.yaml + per-problem MDX surfaces + contributing v1.0/v1.1) — Q51 lean: curator-track in parallel; not a Phase-7 deliverable.
14. **Real-API integration smoke for `lib/discussions/github-graphql.ts`** — Phase-6 carryover; gated on Q47 + `GITHUB_TOKEN` in CI.
15. **W3C feed validator pass** against deployed `/api/v1/rss.xml` + `/api/v1/digest/<slug>` — Phase-3 / Phase-5 / Phase-6 carryover compound; first preview deploy + Q47.

Documented in detail in Unit 7.9's hygiene status pass.

### Pre-existing follow-ons that survived Phase 7 (carryover from prior phases)

Unchanged from Unit 6.10 acceptance gate (Phase-6 close):

- Orphan `components/domain-tile-grid/` deletion — gated on explicit "delete the orphan tile-grid" curator authorization (destructive-action policy blocks unauthorized).
- `entries.json` content backfill on 8 problems — gated on curator-driven session with `ANTHROPIC_API_KEY`.
- `<managingEditor>` on RSS feeds (Q33 / Q44) — gated on Q2 (DNS / project email).
- W3C RSS validator pass on `/api/v1/rss.xml` — Phase-3 carryover; first preview deploy.
- W3C validator pass on Phase-5 digest endpoints — Phase-5 carryover; first preview deploy.
- `pnpm clean-drafts` script — operational signal not yet met (`drafts/` < 100 stale files locally).
- Phase-2 ROR-ID + InstaDeep orphan — curator-driven hygiene; unchanged.
- `NEXT_PUBLIC_GISCUS_REPO_ID` env wiring + repo-Discussions enablement — Phase-6 Q47 operational gate; unchanged.

Documented in detail in Unit 7.9 Class C carryover catalog.

### Phase 8 entry conditions

Per §12 cardinal rule: **explicit human sign-off required**. §13 Phase 6+ scope is open-ended; Phase 6 pulled the Discussions thread (closed); Phase 7 pulled the Bilingual rendering thread (infrastructure-complete; content backfill curator-track). Phase 8+ candidate threads remain:

- **Auth thread**: Clerk vs NextAuth.js v5 vs GitHub OAuth direct. **MANDATORY DB-migration trigger flip** on the first write-path unit per Units 4.12 / 5.10 / 6.0 D-2 / 7.0 D-2. Unit 6.0 alternative-thread table sketched this as a 14+ unit phase. Breaks the Phase-4 "no user accounts" pact at the chosen-provider-commits unit.
- **Read+write API with token auth**: requires auth thread foundation; couples to Phase 3's read-API maturity. Phase 8 candidate after auth.
- **Subscriber-list thread**: completes the Phase-5 D-4 punt; coupled to auth OR third-party (Buttondown / Resend / ConvertKit). Phase 8+ candidate.
- **Monetization thread**: premature without auth + API + read-API maturity. Phase 9+ candidate.
- **Unit 7.3a (bulk page migration + middleware)** — could land as Phase-8 Unit 8.0 if Phase 8's first thread depends on full `/[locale]/` route coverage; otherwise stays a curator-track follow-on. Defer-acceptable.

DB-migration trigger re-eval mandatory at Phase 8 kickoff per Unit 7.0 D-2. Stays cold throughout Phase 7 (+1 FR MDX added 6 KB to `.velite/methodology.json`; trigger still ~1.4% of 5 MB threshold). Flips immediately if Phase 8 picks the auth thread.

### Cross-phase milestone

This commit closes the **Bilingual rendering thread** of Phase 7 to the level Unit 7.0 D-1 + ADR-0011 scoped it: **infrastructure-complete and pilot-validated** (1 FR content surface end-to-end; 2 SSG locale pilots; sitemap + LHCI + locale-toggle UI infrastructure; sibling-file Velite plumbing; English-canonical-slug routing; `translation_source` provenance schema enforced). Content backfill across the remaining ~200 EN files is curator-track per Q51 (decided-as-lean); the §13 ledger entry reads "Bilingual: started — infrastructure shipped Phase 7; FR content backfill ongoing." The other §13 Phase-6+ threads (auth, read+write API, subscriber list, monetization) remain available for future open-ended phases.

13 commits + 1 ADR + 1 new dependency + 0 client-bundle regressions (103 kB First Load JS preserved end-to-end) + 0 test regressions + 60 new tests + 5 OPEN_QUESTIONS movements (4 Phase-7 resolutions + 1 Phase-6 carryover closed).

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 7.11 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 383/383 across 44 files unchanged (no test files touched in 7.11).
- `pnpm build` → 341 prerendered pages unchanged. First Load JS shared chunk = 103 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-7 close: marks Unit 7.11 as the acceptance gate; cites the §13 Bilingual deliverable status table (one row per Phase-7 surface); emits the cross-phase state-at-HEAD (10 problems / 30 papers / etc.; 341 routes; 103 kB; 383 tests; 11 ADRs; 18 LHCI URLs; 1 new dep); enumerates Phase-7 follow-ons (Unit 7.3a bulk migration, first LHCI run, visual baselines, `<html lang>` fix, SITE extraction, LOCALE_ALTERNATE_ROUTES extraction, validate-content glob extension, lastModified hints, locale-alternates expansion, `app/[locale]/page.tsx` home, NEXT_LOCALE cookie, useTranslations for LocaleToggle aria, bilingual content backfill); enumerates pre-existing carryovers from prior phases; states Phase-8 entry condition (explicit sign-off per §12); notes the DB-migration trigger re-eval status at HEAD (~1.4%; cold).
