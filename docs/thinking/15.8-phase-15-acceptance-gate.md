# 15.8 — Phase 15 acceptance gate (Q63 closure; 16 ADRs total; second ALTER migration)

Ninth and final Phase-15 unit; docs-only. Closes Phase 15 (**sixth
NON-§13 phase**; Q63 promotion). Mirrors Phase-12 Unit 12.8 +
Phase-13 Unit 13.6 + Phase-14 Unit 14.8 acceptance-gate patterns:
ledger of deliverables; smoke-gate results; surviving follow-ons;
Phase-16+ entry conditions.

> **§12 sign-off**: Phase 16 sign-off **pending** at the end of this
> unit. The "Continue" override path is available (eleventh
> invocation would set the precedent at Phase 5 → 6 / Phase 6 → 7 /
> Phase 7 → 8 / Phase 8 → 9 / Phase 9 → 10 / Phase 10 → 11 / Phase 11
> → 12 / Phase 12 → 13 / Phase 13 → 14 / Phase 14 → 15 → **Phase 15
> → 16**).

## THINK

### Phase 15 ledger

**9 units shipped** (15.0 prep → 15.8 gate) across HEAD `34290d7`
(Phase 14 close) → HEAD `0620cba` (Phase 15 close pre-this-unit).

| Unit | Title | Type | Status |
|---|---|---|---|
| 15.0 | Phase 15 prep | docs | shipped `3d6e7f9` |
| 15.1 | ADR-0016 user-editable profile fields | docs | shipped `7414b46` |
| 15.2 | DB migration 0004 + schema edit | code | shipped `a4d18af` |
| 15.3 | `lib/users/` edit helpers + validation + tests | code | shipped `8e50bc5` |
| 15.4 | `/profile` edit form + `messages.profile_edit.*` | code | shipped `6ba9f6d` |
| 15.5 | Public consumption fallback chain + bio section | code | shipped `17e2065` |
| 15.6 | Phase-15 hygiene status pass | docs | shipped `cdf528e` |
| 15.7 | OPEN_QUESTIONS hygiene + ADR review (Q63 resolved) | docs | shipped `0620cba` |
| 15.8 | **Phase 15 acceptance gate** (this unit) | gate | this commit |

**0 deferrals**; **0 scope drift**; matches Phase-12 + Phase-13 +
Phase-14 well-scoped pattern.

### Q63 closure summary

Q63 (user-editable profile fields) was a Phase-14-anticipated
candidate that promoted + resolved in Unit 15.7 simultaneously, same
shape as Phase-12 Q57 + Phase-13 Q58. Closes the four-phase
honored-deferral lineage:

- Phase-10 Class B item 2 (user-editable display name; carried).
- Phase-14 Class B item 1 (user-editable fields per ADR-0015 D-C).
- Phase-14 Unit 14.8 acceptance-gate "Q63 candidate" surface flag.

ADR-0016 pins seven concrete contracts (D-A field set; D-B
validation; D-C edit surface route shape; D-D server-action; D-E
fallback chain; D-F bio formatting; D-G image override Phase-16+
deferral). All five code-realizable D-clauses (D-A through D-F)
shipped; D-G is text-only deferral.

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`0620cba`):

- **`pnpm typecheck`** → clean (`tsc --noEmit` no errors).
- **`pnpm test`** → **497/497 across 52 vitest files**. **+17 tests**
  from Phase 14 close (480/52); all in `lib/users/index.test.ts`
  from Unit 15.3.
- **`pnpm build`** → ~593 prerendered pages + **7 dynamic page+API
  routes** unchanged (`ƒ /api/auth/[...nextauth]` + `ƒ /api/v1/watchlist/[slug]`
  + `ƒ /api/v1/rating-challenges` + `ƒ /api/v1/rating-challenges/[id]/review`
  + `ƒ /[locale]/problems/[slug]/challenges` + `ƒ /[locale]/u/[handle]`
  + `ƒ /[locale]/u/[handle]/challenges`). **First Load JS shared
  chunk = 103 kB UNCHANGED** end-to-end through every Phase 9-15
  unit. **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` bundle = 108 kB; `/u/{handle}` = 108 kB.
- **`pnpm audit-content`** → **0 errors / 6 warnings** (Q32 baseline
  since Phase 2; unchanged through every Phase 3-15 unit).

### Architectural firsts shipped in Phase 15

1. **First user-controlled writes surface for `users` table**.
   Phase 9 established `users` row creation via Auth.js v5
   `events.linkAccount` (auth-side metadata population from OAuth
   profile); Phase 15 adds the first **user-controlled** write
   path — distinct concern. Pattern establishes inheritance shape
   for Phase-16+ identity-edit surfaces (image override; opt-out
   toggle; markdown bio).
2. **Second ALTER migration in project history**. First was
   Phase-12 `0003_rating_challenge_review` (FK edge case required
   manual SQL correction per ADR-0014 D-E discipline). Phase 15's
   `0004_user_profile_fields` shipped clean — drizzle-kit's
   nullable text ALTER emission is reliable.
3. **First sibling-helper-in-`lib/auth/`** (`getUserMetadataById`
   alongside `getLoginById`). Pattern preserves backward compat
   for 6 existing callers + tests while extending capability for
   the new SiteHeader use case.
4. **First bi-state search-param banner pattern** (`?saved=1` for
   success, `?error=<encoded>` for validation failure). Phase-11's
   `?challenge=submitted` was single-state; Phase 15 establishes
   the bi-state convention.

### Delta summary (Phase 14 → Phase 15)

| Metric | Phase 14 close (`34290d7`) | Phase 15 close (HEAD `0620cba`) | Δ |
|---|---|---|---|
| Commits | 9 | 8 + this gate | +8 |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | 15 | **16** | **+1** (ADR-0016) |
| DB tables | 6 | 6 | 0 |
| Migrations | 4 | **5** | **+1** (`0004_user_profile_fields`; **second ALTER**) |
| Env vars | 6 | 6 | 0 |
| `users` columns | 7 | **9** | **+2** (`displayName`, `bio`) |
| Tests | 480 / 52 files | **497 / 52 files** | **+17 / 0** |
| Prerendered pages | ~593 | ~593 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 108 kB | **108 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS open count | 28 | 28 | 0 |
| OPEN_QUESTIONS resolved count | 21 | **22** | **+1** (Q63) |
| OPEN_QUESTIONS decided-as-lean count | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 53 | **54** | **+1** |
| i18n keys per locale | 100 | **113** | **+13** (12 `profile_edit.*` + 1 `public_profile.bio_aria_label`) |
| LHCI URLs | 19 | 19 | 0 |
| Phase-16+ Q candidates flagged | Q63 / Q64 / Q65 | **Q64 / Q65 / Q66 / Q67 / Q68** | +3 / -1 |
| ADR-0017+ candidate slots reserved | (ADR-0016 candidate carryover) | **ADR-0017 candidate** (image storage); **ADR-0018+ candidates** (multi-provider OAuth carryover) | (slot renumbered) |

### Phase-16+ entry conditions

**Sign-off pending** per §12 cardinal rule. Sign-off paths to
consider (in rough decreasing strength):

1. **Q67 promotion** — image override / avatar upload (~4-5 units;
   surfaces **ADR-0017** for storage choice). Natural Phase 15 →
   16 progression; closes the largest remaining identity-surface
   gap. Phase-15 explicitly anticipated this as Phase-16+.
2. **Q66 promotion** — markdown rendering in bio (~3 units; new
   remark/rehype/sanitize pipeline ADR). Smaller scope; couples to
   reviewNotes markdown rendering (Phase-15 Class B B.2 item 10).
3. **Q64 promotion** — per-user privacy opt-out (~3 units). Pre-
   emptive (no observed demand signal); smallest among ADR-0015-
   deferred candidates.
4. **Q65 promotion** — per-curator activity feed (~4-5 units).
   Curator-niche; ADR-0015 D-E expansion.
5. **Q59 promotion** — CLI `pnpm emit-challenge-action <id>` (~2-3
   units). Phase-12+13+14+15 carryover; smallest scope; closes
   Phase-12 D-D manual emission friction.
6. **Multi-provider OAuth expansion** (~3-4 units; **ADR-0018
   candidate**). Phase-9 Class B item 8; no observed non-GitHub
   user-segment ask.
7. **Subscriber-list email** (Phase-5 D-4 punt; ~6 units; **ADR-0018
   candidate alternative**). Now deferred 10+ phases; opens
   `lib/email/` infrastructure.
8. **Q68 promotion** — content moderation on bio text. Phase-16+ if
   abuse signals accumulate; not yet observed.
9. **HTML shell migration** (~2-3 units; **STILL ON HOLD** per
   parallel-session preservation signal since Phase 8 D-2).
10. **Monetization** (Phase 17+ candidate; premature without observed
    traffic).

### Smoke gates (this unit)

Docs-only — no code modified.

- File added: `docs/thinking/15.8-phase-15-acceptance-gate.md`.
- CHANGELOG: Phase 15 closing summary entry.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline; unchanged).

### Parallel-curator awareness

- This unit is **docs-only**, no collision risk.
- Phase-15 closes cleanly with 0 Class A items + 16 Class B items +
  unchanged Class C carryovers (per Unit 15.6 catalog).

### CHANGELOG entry shape

Unit 15.8 entry: **Phase 15 acceptance gate**. Cites the 9-unit
ledger (15.0 prep → 15.8 gate); reports smoke-gate results (497/52
tests / 103 kB First Load JS / 160 kB middleware / 0 bundle delta
/ 0 errors / 6 warnings); names the 4 architectural firsts (first
user-controlled writes to `users`; second ALTER migration; first
sibling helper in `lib/auth/`; first bi-state search-param banner);
delta summary table (16 ADRs / 5 migrations / 22 resolved / 54 OQ
total / 113 i18n keys per locale / +17 tests / 0 bundle regression);
**Phase 16 sign-off pending** per §12 cardinal rule; flags 10
candidate Phase-16+ threads (Q67 image override is the strongest
honored-deferral pick alongside ADR-0017 candidate for storage
choice).
