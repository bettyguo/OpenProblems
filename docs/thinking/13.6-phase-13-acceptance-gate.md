# 13.6 — Phase 13 acceptance gate (Q58 public visibility surface; community-feedback loop fully closed)

Phase-13 closing unit. Mirrors Units 1.12/2.13/3.13/4.13/5.13/6.10/
7.11/8.9/9.9/10.5/11.7/12.8. Verifies every deliverable Phase 13
pulled (Q58 public visibility per ADR-0014 D-? + Unit 11.6 Q58 lean +
Unit 13.0 D-3) is operational locally at HEAD, emits the cross-phase
roll-up, and lists Phase-13 follow-ons that survive into Phase 14+.

## THINK

### Scope clarification

Phase 13 is the **fourth NON-§13 phase** (Phase 10 first; Phase 11
second; Phase 12 third; Phase 13 fourth). The §13 ledger has been
**CLOSED since Unit 9.9**. Phase 13's "Q58 public visibility surface"
closes the Phase-11-surfaced Q58 architectural question and **closes
the community-feedback loop from end to end**:

- Phase 9: auth + write-path foundation (watchlist).
- Phase 10: profile page (signed-in user state surface).
- Phase 11: submission half of community feedback (submitter writes).
- Phase 12: review half of community feedback (curator transitions).
- **Phase 13**: read-side public surface (community + visitors read the
  results).

Together: §3.1 "ratings are revisable" + §8.6 COI + community-feedback
loop are now all operational at the code layer, and the loop is fully
visible to unauthenticated visitors per the per-status policy.

### Phase-13 deliverables — local pass status

| Surface | Phase-13 unit | Status |
|---|---|---|
| Phase 13 prep THINK doc + 7-unit breakdown + procedural DB-trigger re-eval | Unit 13.0 | ✓ `docs/thinking/13.0-phase-13-prep.md`; first-thread = Q58; eighth "Continue" override invocation. |
| `lib/rating-challenges/` public-visibility helpers (`PUBLIC_CHALLENGE_STATUSES` const + `PublicChallengeStatus` type + `isPublicChallengeStatus` predicate + `PublicChallengeRow` interface + `getPublicChallengesByProblem` LEFT-JOIN helper + `getAcceptedChallengeCountByProblem`) + 11 tests | Unit 13.1 | ✓ pure-function half tested (11 tests: 4 const-shape + 6 predicate + 1 partition invariant); DB-helper half exercised at request time by Unit 13.2 + Unit 13.3 consumers. |
| Counter UI on problem detail page (section 8b; open + accepted counts via ICU plurals; "View all →" link) + `messages.public_challenges.*` namespace (16 keys per locale; pre-added upfront for atomic discipline) | Unit 13.2 | ✓ first read-side public surface for USER-STATE content; renders unauthenticated; section gated on `totalPublicChallengeCount > 0`. |
| Per-problem listing route at `/[locale]/problems/[slug]/challenges` (`force-dynamic`; no auth gate; per-status visibility server-side filtered; dense list with submitter login + date + dimension + proposed value + color-coded status pill + 200-char rationale + acceptedActionId reference) + submitter-login privacy note in `messages.rating_challenge.description` (EN + FR) | Unit 13.3 | ✓ first read-side public page route for USER-STATE content; route registered as ƒ Dynamic; 1.91 kB / 108 kB First Load JS. |
| Phase-13 hygiene status pass (Class A/B/C catalog) | Unit 13.4 | ✓ 4 carried Class A + 8 Phase-13-specific Class B + 19 Class C carryovers. Smaller Class B than Phase-12's 14 (read-only surface). |
| OPEN_QUESTIONS hygiene + ADR review (Q58 → resolved; 14 ADRs UNCHANGED in body; zero new ADRs Phase 13) | Unit 13.5 | ✓ Q58 promoted; Q61 + Q62 candidates flagged but NOT promoted; ADRs 0004 + 0005 + 0011 + 0012 + 0013 + 0014 reaffirmed or exercised. |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: 19 URLs UNCHANGED
  in `lighthouserc.json`. New `/[locale]/problems/[slug]/challenges`
  route could be enrolled into LHCI in Phase 14+; deferred until first
  deploy + Q54 + Q55 ops unblock.
- **W3C feed validator on `/api/v1/rss.xml` + `/api/v1/digest/<slug>`**:
  Phase-3/5/6/8 compound deferral; first preview deploy + Q47 unblock.
- **Visual-regression baselines** for `/problems/[slug]/challenges`
  deferred Phase-13 follow-on; no Playwright spec changes this phase.
- **No auto-merge** (ADR-0009): Phase 13 added zero LLM-translated
  content; all surfaces are code or curator-typed translation strings.
- **File-first / no DB** (ADR-0004): REAFFIRMED. Phase 13 ships read-
  only surface; zero new tables / columns / migrations. Content stays
  file-first.
- **First Load JS shared chunk = 103 kB**: **HELD** through every
  Phase-13 unit. All Phase-13 surfaces server-rendered (counter +
  listing); zero client-bundle delta.
- **Middleware bundle = 160 kB**: **UNCHANGED** since Phase 12 close.

### Phase-13 unit summary (7 commits — fourth NON-§13 phase)

| Unit | Commit | Title |
|---|---|---|
| 13.0 | `68e631d` | Phase 13 prep (Q58 public visibility; honored-deferral pick) |
| 13.1 | `ff48a24` | Public-visibility helpers + tests |
| 13.2 | `320e327` | Counter UI on problem detail page + messages.public_challenges.* |
| 13.3 | `212111b` | Per-problem listing route + privacy note |
| 13.4 | `f4e07e9` | Phase-13 hygiene status pass (Class A/B/C catalog) |
| 13.5 | `60dac5f` | OPEN_QUESTIONS hygiene + ADR review |
| 13.6 | _this_ | Phase 13 acceptance gate |

Total: **7 numbered units, all shipped**. Matches Unit 13.0 prep's
7-unit breakdown exactly. **Zero deferrals**, **zero scope drift**
across the phase.

### State at HEAD (Unit 13.6)

- **Content** (no count change vs Phase-8 close; Phases 9 + 10 + 11 +
  12 + 13 added no content):
  - 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors
    / 14 institutions / 20 rating actions / 4 issue templates /
    methodology v1 (EN + FR) / contributing v1.0 (EN) + v1.1 (EN + FR)
    / 2 `entries.json` files.
  - **203 schema-validated content files + 36 raw MDX = 239 total raw
    content files UNCHANGED**.
- **New Phase-13 code surfaces** (relative to Phase-12 close at
  `201825f`):
  - `lib/rating-challenges/index.ts` (edit; Unit 13.1) — 3 new exports
    (`PUBLIC_CHALLENGE_STATUSES`, `PublicChallengeStatus`,
    `PublicChallengeRow`) + 3 new helpers (`isPublicChallengeStatus`,
    `getPublicChallengesByProblem`, `getAcceptedChallengeCountByProblem`).
  - `lib/rating-challenges/public-visibility.test.ts` (Unit 13.1) —
    11 pure-function tests.
  - `app/[locale]/problems/[slug]/page.tsx` (edit; Unit 13.2) — new
    section 8b counter + `tPC` translator binding + parallel-fetch
    counts.
  - `app/[locale]/problems/[slug]/challenges/page.tsx` (Unit 13.3) —
    new public listing route.
  - `messages/{en,fr}.json` (edits; Units 13.2 + 13.3) — +16 keys per
    locale in `public_challenges.*` namespace + 1 edit per locale on
    `rating_challenge.description` (privacy note); +17 keys per locale;
    +34 total across EN + FR.
  - 3 new THINK docs (13.0 + 13.4 + 13.5 + 13.6 = 4 actually).
- **Routes**: ~592 → **~593 prerendered pages** (counter section 8b
  injects into existing problem detail page; listing route is `force-
  dynamic` so doesn't add to prerendered count) + **5 dynamic page +
  API routes** (4 dynamic API + 1 new dynamic page route: `ƒ
  /[locale]/problems/[slug]/challenges`).
- **Tests**: **467/467 across 51 vitest files** (was 456/50 at Phase-12
  close; +11 tests / +1 file in Phase 13 from Unit 13.1 pure-function
  tests).
- `pnpm validate-content` → **203 files** green (unchanged).
- `pnpm audit-content` → **0 errors / 6 warnings** (Q32 baseline since
  Phase 2; unchanged).
- `pnpm typecheck` clean.
- `pnpm build` clean; **First Load JS shared chunk = 103 kB UNCHANGED**;
  middleware bundle = 160 kB UNCHANGED.
- **5 visualizations live** (unchanged from Phase 4).
- **14 ADRs** UNCHANGED (Phase 13 added zero new ADRs; Q58 lean
  absorbed directly into implementation without ADR-class promotion).
- **Dependencies**: **+0 net in Phase 13** (Phase-9/10/11/12 stack
  stable).
- **`lighthouserc.json`** enrols **19 URLs** UNCHANGED.
- **OPEN_QUESTIONS state**: 21 resolved + 4 decided-as-lean + 28 open
  = **53 total entries** (was 53 at Phase-12 close; +1 promotion Q58 →
  resolved; -1 open net 0 size change).
- **DB schema tables**: **6 UNCHANGED**. **Columns UNCHANGED** (Phase
  13 read-only).
- **Migrations**: **4 UNCHANGED**.
- **Env contract**: **UNCHANGED** (no new env vars; `LOP_CURATOR_LOGINS`
  carried from Phase 12).
- **New code layers**: `lib/rating-challenges/` extensions + new public
  page route at `/[locale]/problems/[slug]/challenges`.

### `lighthouserc.json` final state

Phase 13 changes: **none**. URL count holds at **19** at Phase-13 close.
New public listing route at `/[locale]/problems/[slug]/challenges`
could be enrolled into LHCI in Phase 14+; deferred until first preview
deploy + Q54+Q55 ops unblock (when real challenges exist to render the
listing meaningfully).

### Phase-13 follow-ons that survive the gate (non-blocking)

Documented in detail in Unit 13.4's hygiene status pass (8 Class B + 4
Class A + 19 Class C items). Critical-path subset for Phase 14+
planning:

1. **Public profile route at `/[locale]/u/[handle]`** (Class B item 1).
   Largest remaining Q58-adjacent surface; couples to Q58 lean #3 +
   Phase-10 Class B item 1 + Phase-12 Class B item 12. **Phase-14
   candidate**; would surface **ADR-0015 (per-user privacy model)**.
2. **Submitter anonymity option (Q61 candidate)** (Class B item 5).
   Schema flag for opt-out from public attribution. Phase 14+; needs
   ADR.
3. **Rejection-rationale public visibility (Q62 candidate)** (Class B
   item 6). Phase 14+ policy decision.
4. **CLI helper `pnpm emit-challenge-action <id>`** (Q59 candidate;
   Class C item 19; Phase-12 Class B item 1 carryover). Phase 14+ if
   usage-friction signal demands.
5. **Per-problem listing URL search-param sort** (Class B item 2).
   Phase 14+.
6. **Per-challenge public detail page** at
   `/[locale]/problems/[slug]/challenges/[id]` (Class B item 3). Phase
   14+; from Phase-11 Class B item 6 + Phase-12 Class B item 8.
7. **Per-problem listing pagination** (Class B item 7). LIMIT 50
   silently caps the list; Phase 14+ via URL `?offset=N`.
8. **Subscriber-list email** (Class C item; Phase-5 D-4 punt + Phase-12
   Class B item 7). Phase 14+; **ADR-0015 candidate** (alternative path).
9. **Q54 + Q55 operational unblocks** (Class A items 1 + 2; carried
   since Phase 9).
10. **CI dummy `AUTH_SECRET`** (Class A item 3; carried).
11. **`pnpm db:migrate` doc for new contributors** (Class A item 4) —
    DB has 4 migrations.

### Pre-existing follow-ons that survived Phase 13 (carryover)

Unchanged from Unit 12.8 acceptance gate (19 Class C items per Unit
13.4 catalog). Notable:

- HTML shell migration + `<html lang={locale}>` + SiteHeader under
  `NextIntlClientProvider` — STILL ON HOLD per parallel-session
  preservation signal.
- W3C feed validator pass on `/api/v1/rss.xml` (Phase-3 carryover).
- `<managingEditor>` on RSS feeds (Q33/Q44; Q2 DNS gate).
- `pnpm clean-drafts` script.
- Phase-2 ROR-ID + InstaDeep orphan.
- Chrome strings + FR bulk backfill + StatusPill localization + nav
  labels (Q51 curator-track horizon).
- Trailing-slash normalization for `NEXT_PUBLIC_SITE_URL`.
- Per-entry sitemap hints.
- Watchlist count display on `/problems` index.
- Multi-provider OAuth expansion (Phase-9 Class B item 8; **ADR-0016
  candidate**).
- Real-API integration smoke for `lib/discussions/github-graphql.ts`
  (Q47 + `GITHUB_TOKEN`).

### Phase 14+ entry conditions

Per §12 cardinal rule: **explicit human sign-off required** at this
acceptance gate. Phase 13 closes the Q58 public visibility surface +
finishes the community-feedback loop from end to end.

**Phase 14+ candidate threads** (each overridable):

- **Public profile page at `/[locale]/u/[handle]`** (~4-5 units).
  Largest remaining honored-deferral pick (Phase-10 Class B item 1 +
  Phase-12 Class B item 12 + Phase-13 Class B item 1; Q58 lean #3
  natural completion). Surfaces **ADR-0015 (per-user privacy model)**.
- **Subscriber-list email** (~6 units). Phase-5 D-4 punt closure;
  opens `lib/email/` infrastructure. **ADR-0015 candidate
  (alternative path)** — whichever thread Phase 14 picks claims ADR-0015.
- **CLI `pnpm emit-challenge-action <id>`** (~2-3 units; **Q59
  promotion** if usage demands). Smallest scope; closes Phase-12 D-D
  manual emission friction.
- **Multi-provider OAuth expansion** (~3-4 units; **ADR-0016 candidate**).
- **Full §8.6 24-mo collaborator COI check** (~3-5 units; **ADR-0017
  candidate** alongside `curatorRoles` DB table).
- **HTML shell migration + Unit 8.4 unblock** — STILL ON HOLD per
  parallel-session preservation signal.
- **Monetization** — premature without observed user traffic. Phase
  15+ candidate.

DB-migration trigger re-eval mandatory at Phase 14 kickoff per Unit
13.0 D-2 cascade (procedural; trigger (a) already fired Phase 9;
trigger (b) cold).

### Cross-phase milestone

This commit closes the **Q58 public visibility surface** + **finishes
the community-feedback loop end-to-end**. 7 commits + 0 new ADRs + 0
new dependencies + 0 client-bundle regressions (103 kB First Load JS
preserved end-to-end) + 0 test regressions + 11 new tests + +1
OPEN_QUESTIONS promotion (Q58 resolved) + 1 new dynamic page route +
0 new DB columns / migrations / env vars + 34 new i18n keys + 1
counter section on existing problem detail page.

**§13 ledger remains CLOSED** (closed at Unit 9.9). Phase 13 is the
**fourth NON-§13 phase**.

**Community-feedback loop closed end-to-end**. The §3.1 "ratings are
revisable" + §8.6 COI mandates are now operational at the code layer
AND visible to unauthenticated visitors per the per-status visibility
policy. Phase 11 (submission) + Phase 12 (review) + Phase 13 (public
read-side) together complete the architectural surface that
MASTER_PROMPT.md envisioned for the community-feedback dimension of
the platform.

**Three architectural patterns validated by Phase 13**:

1. **First read-side public surface for USER-STATE content** — sets
   the pattern Phase 14+ public profile route will inherit.
2. **First status-gated visibility policy** — per-status partition
   between public + submitter-only enforces editorial-decision privacy
   while preserving editorial-record transparency.
3. **Atomic-i18n pre-add discipline** — Unit 13.2 pre-added all
   listing-page keys upfront so Unit 13.3 consumed them without
   further messages edits. Mirrors Phase-11 Unit 11.3 + Phase-12 Unit
   12.4 patterns; useful future-pattern for any multi-unit feature
   spanning UI surfaces.

### Phase-13 over-vs-under against the 13.0 plan

Unit 13.0 prep scoped a 7-unit phase (13.0–13.6). Actual: **7 units
shipped**, **zero deferrals**, **zero scope drift**. Per-unit landings
matched the prep table exactly. ADR-class promotion explicitly avoided
(Q58 lean documented across Unit 11.6 + Unit 12.0 D-10 + Unit 13.0 D-3
was sufficient to absorb the implementation without ADR-0015 surfacing).

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 13.6 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 467/467 across 51 files unchanged (no test files
  touched in 13.6).
- `pnpm build` → ~593 prerendered pages + 5 dynamic page+API routes
  unchanged. First Load JS shared chunk = 103 kB unchanged. Middleware
  bundle = 160 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-13 close: marks Unit 13.6 as the acceptance gate; cites the
Phase-13 deliverable status table (every row ✓; zero deferrals); emits
the cross-phase state-at-HEAD (10 problems / etc.; ~593 prerendered
pages + 5 dynamic routes; 103 kB First Load JS; 467 tests across 51
files; 14 ADRs; 19 LHCI URLs; +0 new deps; 6 DB tables; 4 migrations;
53 OQ entries); enumerates Phase-13 follow-ons (public profile / Q61
anonymity / Q62 rejection visibility / Q59 CLI helper / URL sort /
per-challenge detail / pagination / subscriber-list / Q54+Q55+CI
AUTH_SECRET+db:migrate doc); enumerates pre-existing carryovers; states
Phase 14+ entry condition (explicit sign-off per §12); notes the
**community-feedback loop closure end-to-end** (Phase 11 submission +
Phase 12 review + Phase 13 public read-side = §3.1 + §8.6
architectural mandate fully operational + publicly visible per
per-status policy); flags ADR-0015 (per-user privacy OR subscriber-list)
+ ADR-0016 (multi-provider OAuth) + ADR-0017 (full §8.6 COI +
`curatorRoles`) as Phase-14+ candidates.
