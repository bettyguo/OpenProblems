# 37.4 — Phase 37 acceptance gate

Phase 37 closes at HEAD `1908f53` (Unit 37.3 ship; Q72 sub-
bullet resolution). This unit ships the acceptance gate +
boundary statement + Phase-38-entry-condition documentation
per §12 cardinal rule + Phase-12-through-36 acceptance-gate
patterns.

## THINK

### Phase 37 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 37.0 | Phase 37 prep | docs | `ea72a0f` |
| 37.1 | `lib/markdown/extensions/` framework + ADR-0018 D-G APPEND | code+APPEND | `55e7ba9` |
| 37.2 | Registry integration in 4 markdown helpers + 10 tests | code | `6ca6fdf` |
| 37.3 | Phase-37 hygiene + Q72 sub-bullet resolution | docs | `1908f53` |
| 37.4 | Phase 37 acceptance gate (this unit) | gate | (current) |

**5 units; mirrors Phase-35 5-unit framework-only shape.** No
deferred units.

### Q72 markdown schema-divergence framework — REALIZED

Phase-37-prep D-1 first-thread recommendation realized at
HEAD `1908f53`:

- `lib/markdown/extensions/` framework module (5 files;
  Phase-35 `lib/moderation/` shape verbatim).
- `MarkdownExtensionRegistry` + `MarkdownExtensionSet` +
  `MarkdownSurface` types over **4 surfaces** (path-corrected
  from prep doc's 3-surface anticipation: bio + reviewNotes
  + rationale + actionRationale).
- `getExtensionRegistry()` lazy-singleton factory +
  `__resetRegistryForTests` / `__setRegistryForTests` test
  hooks.
- 4 lazy processor caches in `lib/markdown/index.ts`
  (`getBioProcessor()` + 3 siblings) + `buildProcessor()`
  helper folding extensions per APPEND-D-C + APPEND-D-D
  semantics + `__resetMarkdownCachesForTests()` hook.
- ADR-0018 D-G APPEND documents framework + 7 sub-decisions
  (APPEND-D-A through APPEND-D-G; +~95 lines).
- 10 new framework-integration tests verifying default-empty
  parity + rehype-plugin-after-default + schema-override-
  replace semantics.
- Q72 sub-bullet resolution at OPEN_QUESTIONS.md line 573
  (APPEND-not-EDIT pattern; sub-bullet-only resolution same
  shape as Q64 Phase 36; second consecutive in project
  history).

### Phase 36 → Phase 37 delta

| Metric | Phase 36 close | Phase 37 close | Δ |
|---|---|---|---|
| Tests | 736 / 65 files | **755 / 67 files** | +19 / +2 |
| ADRs | 24 | **24** | 0 |
| ADR D-clauses with APPENDs | 10 | **11** (ADR-0018 D-G now has 4 APPENDs — project first) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 13 | 13 | 0 |
| i18n keys per locale | 168 | 168 | 0 |
| Dynamic page+API routes | 13 | 13 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** (28 resolved + 4 lean + 34 open) | 0 |
| Phase-37+ candidate count | 10 | **9** (Q72 framework removed) | −1 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/` files | 3 | **8** (3 existing + 5 new `extensions/`) | +5 |

**Zero-cost-default discipline preserved end-to-end**: Phase
37 added zero runtime deps, zero env vars, zero migrations,
zero new operational gates, zero new ADRs, zero new i18n keys.
Framework's existence at default empty registry =
Phase-18/27/29 baseline.

### Architectural firsts in Phase 37

1. **First framework-only-ADR pattern reuse in a new domain**
   — Phase 35 established framework-only via ADR-0024 in the
   content-moderation domain; Phase 37 validates the pattern's
   reusability in the markdown-rendering domain. Phase-37-prep
   D-1 rationale 1 realized.
2. **First ADR D-clause with FOUR APPENDs in project history**
   — ADR-0018 D-G now carries Phase 18 reviewNotes + Phase 27
   rationale + Phase 29 actionRationale + **Phase 37
   framework** APPENDs. Prior maximum was 3 APPENDs on a
   single D-clause (same D-clause, prior count).
3. **Fourth consecutive longest-Q-closure phase** (Phase 34
   Q79 + Phase 35 Q68 + Phase 36 Q64 + Phase 37 Q72 = **first
   4-phase longest-Q-closure streak in project history**).
4. **Twenty-eighth NON-§13 phase**.
5. **Second no-new-ADR phase in 2 phases** (Phase 36 + 37;
   extends alternating-pattern to 6 phases: Phase 32 no-new
   → 33 new → 34 no-new → 35 new → 36 no-new → **37 no-new**).
6. **Fourth consecutive net-Q-decrease phase** (Phase 34 + 35
   + 36 + 37 = first 4-phase net-decrease streak in project
   history; Phase 37 Q72-resolution drops Phase-37+ candidate
   count 10 → 9).
7. **Seventh consecutive phase without new B category**
   (Phase 31 + 32 + 33 + 34 + 35 + 36 + 37 = first such
   7-phase run in project history).
8. **First lazy-processor-singleton pattern in `lib/markdown/`**
   — Phase 17 through Phase 29 used module-level `const
   xxxProcessor = unified()...` eager singletons; Phase 37
   Unit 37.2 converts to lazy `let xxxProcessor: Built |
   null = null` + getter pattern for registry-swappability.
9. **Second consecutive sub-bullet-only Q-resolution** (Phase
   36 Q64 + Phase 37 Q72; never before two in a row).
10. **18-phase Q72-surfaced-to-framework-realized carryover**
    (Phase 18 → Phase 37; longest cumulative Q72-family
    patience signal; second-longest Q-family carryover after
    Q68 expansion's 22+ phase Phase-35 closure).
11. **First "framework-only resolution" of a sibling-
    divergence Q-family** — the Q72 family's 3 sibling-
    divergence candidates (reviewNotesSchema +
    rationaleSchema + actionRationaleSchema divergence from
    bioSchema) all collapse to a single framework-consumption
    pattern Phase-38+.
12. **53rd-through-56th consecutive 103 kB First Load JS unit
    in project history** (Phase 9 Unit 9.5 through Phase 37
    Unit 37.4 inclusive); zero client-bundle regression
    end-to-end across the entire `lib/markdown/extensions/`
    framework.

### Phase 37 → Phase 38 entry conditions

Per §12 cardinal rule, Phase 38 entry requires **explicit
human sign-off**. This session has shipped the Phase 37
acceptance gate; Phase 38 entry waits for `"Continue"`
override OR explicit phase-38 thread direction.

**Recommended Phase 38 first-thread candidates** (carried
forward from Phase-37-prep alternative-threads table; refined
by Phase-37-as-shipped state):

| Rank | Thread | Units | Why | Why not |
|---|---|---|---|---|
| 1 | **First concrete Phase-37-framework extension consumer** (e.g., GFM tables per-reviewNotes via `schemaOverrides`, OR wikilinks per-bio via `rehypePlugins`) | 1-2 | Tightest validation of Phase-37 framework end-to-end; exercises the registry's env-var dispatch path (first Phase-38+ dispatch arm); proves zero-rework consumption claim from APPEND-D-G | Needs scope decision on WHICH extension first; user-direction-likely; no autonomous default obvious among the 12 deferred candidates |
| 2 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal post-Q72 closure; exercises Phase-35 framework end-to-end | Requires curator content authorship OR external-API operational unblock; not autonomously-tractable |
| 3 | **Pre-commit typecheck hook addition** | 1 | Closes Unit-36.1-surfaced gap; Phase-37 lints-pass-but-typecheck-fail risk persists | Tiny; arguably not a full phase |
| 4 | **Q78 digest-send analytics** | 3-5 | Tightest 2-phase Q-carryover (Q78 surfaced Phase 31) | Waits for Q77 production signal |
| 5 | **Account-deletion blob cleanup** | 1-2 | Closes ADR-0023 D-D Phase-33-deferral | Conditional on account-delete UI (none exists yet) |
| 6 | **`safeAuth()` extraction** | 1 | Closes Phase-7 D-8 5+-copy threshold | Still 4 copies; not yet at threshold |
| 7 | **Subscribe route + `/profile` test backfill** | 1-2 | Closes Unit-35.2 + 36.2 noted gaps | Not user-visible value |
| 8 | **N. v2 methodology authoring** | curator | Primary blocker for §1 (d) success metric | Curator-track-heavy; needs human direction |
| 9 | **Q79 Profile B / C** | 2-3 each | UX-track without ADR | Need user-feedback signal |

**Operational gates still pending** (6 unchanged from Phase
36 close): Q54 (GitHub OAuth) + Q55 (Turso prod DB) + Q69
(Vercel Blob) + Q73 (Google OAuth) + Q75 (Resend domain) +
Q77 (Cron secret). All curator-track; all block production
deploy but NOT phase work.

### Smoke gates (Phase 37 close)

- `pnpm typecheck` — strict; passes.
- `pnpm test` — **755 / 67 files** passing (was 736 / 65 at
  Phase 36 close; +19 tests / +2 files across Units 37.1
  (+9 / +2) + 37.2 (+10 / 0)).
- `pnpm audit-content` — 0 errors / 6 warnings (Q32 baseline
  since Phase 2; UNCHANGED through every Phase 3-37 unit; 35
  consecutive phases).
- First Load JS shared chunk — **103 kB UNCHANGED** end-to-
  end Phase 9 Unit 9.5 → Phase 37 Unit 37.4 (**56 consecutive
  units**).
- Middleware bundle — **160 kB UNCHANGED** since Phase 12.

### Anti-scope (Phase 38+ deferrals; carried forward)

NOT concrete extension implementations (wikilinks / GFM
tables / GFM footnotes / images / @mentions / tel-URLs / `<a
title>` / syntax highlighting / configurable schema per-user
— all Phase 38+ one-off additions per ADR-0018 D-G APPEND
F-list); NOT per-curator extension preferences (Phase 38+
DB-backed registry override); NOT curator-facing extension-
enable UI (Phase 38+); NOT live preview in edit form (Phase
38+ caveat: breaks 103 kB invariant); NOT ADR-0025 concrete
content-moderation provider (Phase 38+); NOT Q78 digest-send
analytics (waits for Q77); NOT account-deletion blob cleanup
(Phase 38+); NOT v2 methodology authoring (curator-track-
heavy); all other Phase-30-through-36 deferrals carry
forward.

### Boundary statement

**Phase 37 CLOSED at HEAD `<this-unit>` after 5 units (37.0
prep + 37.1 framework + 37.2 integration + 37.3 hygiene +
37.4 gate). Q72 markdown schema-divergence framework
realized via ADR-0018 D-G framework-only-pattern reuse from
Phase 35 ADR-0024. Twenty-eighth NON-§13 phase. Fourth
consecutive longest-Q-closure phase (Q79 + Q68 + Q64 +
Q72). Eleventh APPEND-pattern ADR D-clause cluster; first
ADR D-clause with 4 APPENDs (ADR-0018 D-G). 755 tests
across 67 vitest files (+19 / +2 vs Phase 36). 24 ADRs
UNCHANGED. 7 DB tables UNCHANGED. 9 migrations UNCHANGED.
13 env vars UNCHANGED. 168 i18n keys per locale UNCHANGED.
First Load JS 103 kB UNCHANGED (56 consecutive units).
Middleware 160 kB UNCHANGED. OPEN_QUESTIONS top-level
Q-count 66 UNCHANGED (28 resolved + 4 lean + 34 open).
Phase-37+ candidate count 10 → 9 (4th consecutive net-Q-
decrease phase). Zero new runtime deps / env vars / ADRs /
migrations / i18n keys / operational gates. Phase 38
entry requires explicit human sign-off per §12.**

## ITERATE

Reviewer-mode checklist for the gate unit:

- [x] All Phase-37 architectural firsts enumerated.
- [x] Phase 36 → 37 delta table complete.
- [x] Phase 38 entry conditions documented.
- [x] Anti-scope carries forward.
- [x] Smoke gates pass.
- [x] Boundary statement crisp.
- [x] STOP at Phase 38 boundary per §12 (this unit ships the
      gate; no Phase 38.0 prep written).
