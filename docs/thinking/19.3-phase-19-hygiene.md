# 19.3 — Phase-19 hygiene status pass (0 Class A / ~11 Class B / Class C unchanged)

Fourth Phase-19 unit; docs-only. Mirrors Phase-13 Unit 13.4 +
Phase-14 Unit 14.6 + Phase-15 Unit 15.6 + Phase-16 Unit 16.6 +
Phase-17 Unit 17.5 + Phase-18 Unit 18.4 hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-19 cleanup: **0 items**

Phase-19's single code unit (19.2) shipped without deferrals. All
six ADR-0019 D-clauses are either code-realized (D-A library
choice `sharp@0.34.5`; D-B EXIF allow-list strip-all-default; D-C
pipeline placement `lib/storage/putAvatar` server-side; D-D
auto-rotation `.rotate()` no-args) or text-only deferrals (D-E
backwards-compat no-retroactive Phase 19; D-F Phase-20+
inheritance contract for Q68 expansion / cropping / resizing /
format conversion).

**Phase-19 Class A count = 0** matches **Phase-12 through
Phase-18 well-scoped pattern — eight consecutive phases** ship
with 0 Class A items.

### Class B — Phase-19 follow-ons: **~11 items in 3 categories**

#### B.1 Image-processing expansion (5; Phase-19 surfaced)

- **EXIF backfill script** — `scripts/backfill-exif-strip.ts`
  per ADR-0019 D-E deferral. Iterates existing Phase 16-18
  `users.imageOverride` URLs; downloads each blob; `sharp(buffer).rotate().toBuffer()`
  → re-uploads stripped buffer → updates DB column → deletes
  original. Manual curator invocation; CLI dry-run flag; ~1-2
  units when promoted. Phase 20+.
- **Q68 expansion — content moderation on uploaded images**
  (ADR-0017 D-H + ADR-0019 D-F carryover) — server-side
  moderation API integration (OpenAI moderation / Perspective /
  custom regex+wordlist) inserted between `.rotate()` and
  `.toBuffer()`. ~3-4 units; ADR-0020+ candidate. Phase 20+.
- **Cropping UI** (ADR-0017 D-H + ADR-0019 D-F carryover) —
  `sharp.extract({ left, top, width, height })` inserted between
  `.rotate()` and `.toBuffer()`. Client component for crop
  region selection. ~3-5 units. Phase 20+.
- **Server-side resizing** (ADR-0017 D-H + ADR-0019 D-F
  carryover) — `sharp.resize(256, 256, { fit: "cover" })`
  inserted between `.rotate()` and `.toBuffer()`. ~1-2 units.
  Phase 20+ if observation shows bandwidth/render-time concern.
- **WebP/AVIF format conversion** (ADR-0019 D-F carryover) —
  `sharp.webp({ quality: 85 })` or `sharp.avif()` inserted
  between `.rotate()` and `.toBuffer()`. ~1-2 units. Phase 20+
  if observation shows storage cost concern.

#### B.2 Phase-17 markdown subset expansion (8; carried unchanged)

8 items carried verbatim from Phase 17 Unit 17.5 + Phase 18 Unit
18.4: tables / footnotes / images / syntax-highlighted code
blocks / bio length expansion / `@mentions` / task-list
interactivity / live preview client boundary. Phase 19 added
zero new markdown subset items.

#### B.3 Phase-18 surface expansion (3; carried unchanged)

3 items carried verbatim from Phase 18 Unit 18.4: markdown
rating-action `rationale` (couples to per-challenge detail page);
methodology page markdown unification (Velite MDX vs
`lib/markdown/`); per-challenge detail page (Phase-11 + 13
carryover). **Q72 candidate** (`reviewNotesSchema` divergence
from `bioSchema`) also carried from Phase 18.

#### B.4 Operational + infrastructure (5; unchanged from Phase 17/18)

- Q69 operational gate (`BLOB_READ_WRITE_TOKEN`); Q54 + Q55
  operational gates.
- CI dummy `AUTH_SECRET`.
- `pnpm db:migrate` doc (6 migrations unchanged from Phase 18).
- First LHCI run (19 URLs unchanged).
- Middleware-based auth-route protection (threshold stays at 2;
  Phase 19 added zero protected routes).

### Class C — pre-existing carryovers (unchanged from Phase 18 Unit 18.4)

All ~25 Class C items persist unchanged.

### Phase-18 → Phase-19 delta

| Metric | Phase 18 close (`4915406`) | Phase 19 close pre-this (HEAD `9ac2a23`) | Δ |
|---|---|---|---|
| Commits | 7 | 3 + this hygiene + 19.4 + 19.5 (anticipated) | +6 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | 18 | **19** | **+1** (ADR-0019) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (**third consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-18 stack) | **+1 net** (`sharp@0.34.5` direct dep; was transitive via `next/image` per Phase-0 `pnpm-workspace.yaml` `allowBuilds.sharp: true`) | +1 |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 562 / 54 files | **564 / 54 files** | **+2 / 0** |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** (per ADR-0019 extends ADR-0018 D-F invariant) |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS resolved | 25 | 25 (Q70 promotes in Unit 19.4 → 26) | 0 (anticipated +1) |
| OPEN_QUESTIONS open | 30 | 30 (-Q70 promoted to resolved in 19.4) | -1 anticipated |
| OPEN_QUESTIONS lean | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 59 | 59 (Q70 was already in file as `open`; promotion just status-changes, doesn't add) | 0 (mechanical Q-count) |
| i18n keys per locale | 127 | 127 | 0 |
| LHCI URLs | 19 | 19 | 0 |
| `lib/storage/` files | 2 | 2 (extended in-place) | 0 |
| Server-side image processing surfaces | 0 | **1** | **+1** |
| Privacy-by-default surfaces | 0 | **1** | **+1** |

### Architectural firsts shipped in Phase 19

1. **First server-side image processing surface** in project
   history — Phase 9-18 stored images as-is via Vercel Blob;
   Phase 19 transcodes (re-encodes without EXIF) before upload.
2. **First explicit privacy-by-default surface** in project
   history — Phase 16 imageOverride shipped privacy-by-omission;
   Phase 19 closes the privacy gap intentionally (conservative
   posture for PII surfaces).
3. **Third consecutive 0-migration phase** (Phase 17 + 18 + 19; 6
   of 10 phases since DB landed are 0-migration: Phase 10 + 13 +
   14 + 17 + 18 + 19).
4. **ADR-0019 claims slot after 5-phase ADR cycle without
   multi-provider-OAuth claim** — Phase-9 Class B item 8 carried
   9 phases becomes ADR-0020+ candidate; strongest architectural
   patience signal unclaimed for a sixth ADR cycle.
5. **First inverted-allow-list pattern** in project history (vs
   bioSchema/reviewNotesSchema explicit-allow-list approach) —
   `sharp.toBuffer()`'s strip-everything default leveraged
   instead of explicit per-tag allow/deny. Security-correct
   defaults via library choice.
6. **First single-code-unit-per-phase realization of multiple
   D-clauses** — Phase 17 took 2 code units (17.2 module + 17.3
   surface); Phase 18 took 3 code units (18.1 module + 18.2
   curator + 18.3 /profile); Phase 19 compresses D-A through D-D
   into one code unit (19.2) because narrower scope (single
   consumer surface; no UI affordance; no DB schema; no i18n).
7. **First "explicit-dep promotion" of transitive dep** — `sharp`
   was previously transitively available via `next/image` per
   Phase-0 `pnpm-workspace.yaml` `allowBuilds.sharp: true`
   configuration; Phase 19 promotes it to direct runtime dep for
   stability across future `next` updates.

### Phase-19+ Q-candidates flagged

Carryovers from Phase 18 (9 candidates carried; -1 promoted +0 new):

- **Q59** (CLI emit-challenge-action) — carried Phase-12 through
  Phase-19 = **8 phases**.
- **Q60** (curator authz evolution).
- **Q61** (submitter anonymity option).
- **Q62** (rejection-rationale public visibility).
- **Q64** (per-user privacy opt-out).
- **Q65** (per-curator activity feed).
- **Q68 expansion** (content moderation on uploaded images;
  Phase-19 D-F inheritance contract documents the
  `.rotate()` ↔ `.toBuffer()` integration point).
- **Q72** (`reviewNotesSchema` divergence from `bioSchema`;
  Phase 18 newly flagged).
- **Q70 PROMOTES to resolved in Unit 19.4** — Phase-16 carryover
  closed by ADR-0019.

Total **8 Phase-19+ Q-candidates** carrying after Q70 promotion.

### Phase-20+ ADR slot reservations

ADR-0019 claimed Phase 19. **ADR-0020 candidate slot open after
6-phase ADR cycle without multi-provider-OAuth claim** (Phase-15
ADR-0016 + Phase-16 ADR-0017 + Phase-17 ADR-0018 + Phase-18
NO-new-ADR + Phase-19 ADR-0019 = 5 ADR slots since Phase-15 +
1 cycle without multi-provider claim):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried 9 phases — strongest architectural "patience signal").
- **Image-processing API content moderation** (Q68 expansion;
  ADR-0019 D-F inheritance compose pattern).
- **Markdown subset extensions** (Phase-17 D-H + Q72 candidate).
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  13+ phases — strongest overall "patience signal").
- **Per-challenge detail page** (Phase-11 + 13 carryover; may not
  need new ADR).

### Phase-19 single-code-unit pattern

Phase 19 achieved a tighter cadence than Phase 17/18: **single
code unit (19.2) realized D-A through D-D of ADR-0019**. This
contrasts:

- Phase 17: 2 code units (17.2 module + 17.3 surface).
- Phase 18: 3 code units (18.1 module + 18.2 curator + 18.3
  /profile).
- Phase 19: 1 code unit (19.2).

The compression is enabled by Phase-19's narrower scope (single
consumer surface; no UI affordance; no DB schema; no i18n
strings). Phase-20+ image-processing expansion (Q68 / cropping /
resizing / WebP / EXIF backfill) will each be smaller
~1-3-unit phases inheriting the Phase-19 integration point.

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/19.3-phase-19-hygiene.md` (this).
- CHANGELOG: Unit 19.3 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 19.3 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-18 well-scoped pattern (eight
consecutive); ~11 Class B items in 4 categories (5 image-
processing expansion incl. EXIF backfill + Q68 + cropping +
resizing + WebP; 8 Phase-17 markdown subset carryovers; 3
Phase-18 surface expansion carryovers; 5 operational); Class C
carryovers unchanged; Phase 18 → 19 delta (+1 ADR; 0 migrations;
+1 runtime dep `sharp`; +2 tests; 0 i18n keys; 0 bundle delta;
+1 server-side image processing surface; +1 privacy-by-default
surface); 7 Phase-19 architectural firsts (incl. server-side
image processing + privacy-by-default + 3rd consecutive
0-migration + inverted-allow-list pattern + single-code-unit-
per-phase realization + explicit-dep promotion); 8 Phase-19+
Q-candidates carry; ADR-0020 candidate slot open after 6-phase
cycle without multi-provider-OAuth claim.
