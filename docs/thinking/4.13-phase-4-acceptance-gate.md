# 4.13 — Phase 4 acceptance gate

## THINK

Phase 4 closing unit. Mirrors Unit 1.12 (Phase 1 gate), Unit 2.13 (Phase 2 gate), Unit 3.13 (Phase 3 gate). Verifies every §13 Phase-4 acceptance criterion locally and emits the cross-phase roll-up.

### §13 Phase-4 deliverables — local pass status

1. **`DomainMap` (force graph) on `/` and `/domains`.** ✓ Unit 4.2 shipped the SSR-only force-graph viz with deterministic-layout SVG render + a11y plumbing (`role="img"` + `<desc>`). Unit 4.3 wired it into `/domains` via `<ChartTableSwitch>` with `<DomainMapTable>` fallback. Unit 4.4 wired it into `/` with 5 navigation chips above it. Both routes ship `○ Static` at small sizes; **First Load JS shared chunk stays 103 kB** across all Phase-4 work.
2. **GitHub issue templates** for new-problem, new-paper, leaderboard-entry, rating-challenge. ✓ Units 4.7–4.10 ship all 4 as form-based YAML at `.github/ISSUE_TEMPLATE/*.yml`. Each carries the project-correct GitHub URLs (pinned to `github.com/bettyguo/OpenProblems` via `git remote -v`), `[<Type>] ` title prefixes, `<type>` + `needs-triage` labels.
3. **`/contributing` page** rendering the full editorial workflow. ✓ Unit 4.5 shipped the versioned MDX (`content/contributing/v1.mdx`), Unit 4.6 wired the Velite collection + the page composition mirroring `/methodology`. Routes `/contributing` and `/contributing/v1.0.0` both live.
4. **(Conditional) DB migration.** ✓ Unit 4.12 produced the standalone trigger-evaluation note. `gzip(.velite snapshot) = 69,182 bytes` (~67.6 KB, ~1.32% of the 5 MB §12 threshold). Auth trigger also negative — Phase-4 workflow is GitHub-mediated (issue templates + PR review), no first-party auth per §5.8. **DB migration deferred to Phase 5** per the §12 contemplated path.

### §13 cross-phase criteria (the universal contract)

- **Lighthouse a11y ≥ 95 on new pages**. ✓ `lighthouserc.json` updated this unit to include `/contributing` + `/domains`; was already covering `/` from Unit 1.12 and `/domains/[any]` from Phase-1. The DomainMap a11y plumbing (`role="img"` + `aria-describedby` → `<desc>` enumerating all visible nodes by composite rating) inherits the Phase-3 viz pattern. CI cohort runs against the deployed URL set; first PR with this commit is the canonical signal (same Q27 pattern as prior phases).
- **Visual-regression baselines** for new pages × 2 themes × N viewports. Local `chromium-win32` baselines NOT re-captured in this commit (no Playwright spec changes). Same Q27-class follow-on as Phase-1 / Phase-2 / Phase-3 closure — a future PR can pass `playwright test --update-snapshots` against `/`, `/domains`, `/contributing`, `/contributing/v1.0.0`.
- **All charts have table-fallback toggles** — closed in Unit 3.12 (Phase 3 acceptance criterion); Phase-4 DomainMap inherits via Unit 4.2's `<DomainMapTable>` sibling, wired in Units 4.3 / 4.4.
- **Issue-template smoke-test** (manual): open each template in the GitHub UI on a non-main branch and verify field rendering + title prefix. **Cannot be automated** per Unit 4.0's plan; documented as a human-run checklist on this commit's PR.

### Phase-4 unit summary (14 units shipped)

| Unit | Commit    | Title                                                              |
|------|-----------|--------------------------------------------------------------------|
| 4.0  | `8ef0e18` | Phase 4 prep (THINK + 14-unit breakdown + DB-migration eval)       |
| 4.1  | `82194b3` | Install `d3-force` + `d3-selection`                                |
| 4.2  | `be29236` | `components/viz/DomainMap/` scaffold (§11 catalog item 4)          |
| 4.3  | `e3c2623` | `/domains` page swaps tile grid for DomainMap                      |
| 4.4  | `0569135` | `/` landing page swaps tile grid for DomainMap + chips             |
| 4.5  | `1c83f61` | `content/contributing/v1.mdx`                                      |
| 4.6  | `abd3c07` | `/contributing` page composition + Velite collection               |
| 4.7  | `de9460b` | Issue template: new-problem                                        |
| 4.8  | `1954b1d` | Issue template: new-paper                                          |
| 4.9  | `636da83` | Issue template: leaderboard-entry                                  |
| 4.10 | `81e4459` | Issue template: rating-challenge                                   |
| 4.11 | `25801f4` | ADR-0007 (DomainMap rendering + D3 import policy)                  |
| 4.12 | `1261aca` | DB-migration trigger evaluation note                               |
| 4.13 | _this_    | Phase 4 acceptance gate                                            |

### State at HEAD (Unit 4.13)

- 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors / 14 institutions / 20 rating actions / 4 issue templates / 1 `/contributing` MDX.
- **313 prerendered pages** (`Generating static pages (313/313)` from `pnpm build`). Note: prior CHANGELOG entries reported "198 routes" / "200 routes" — those were route-PATTERN counts; this is the expanded-page count. Number unchanged in spirit from Phase-3 close + 2 from `/contributing` + 1 from `/domains`-index becoming a brushable surface (no new SSG path; same `/domains` index path).
- **First Load JS shared chunk 103 kB** — UNCHANGED throughout Phase 4. d3-force runs server-side; the SVG payload is static markup; d3-selection ships in deps from Unit 4.1 but is unused server-side (tree-shaken from client bundle).
- **199/199 vitest tests across 29 files** (was 171/25 at Phase-3 close — +28 tests from Unit 4.2's DomainMap suite, Unit 4.3's build-domain-map.test.ts, and the parallel session's Storybook-as-snapshot tests).
- `pnpm validate-content` → **203 files green** (Velite validates the new `/contributing` MDX at build; YAML-vs-Zod cross-validation in `scripts/validate-content.ts` is unchanged per Q31).
- `pnpm audit-content` → **0 errors / 6 warnings** (the same Q32-expected `related-problems-symmetry` warnings since Phase 2).
- `pnpm typecheck` → clean.
- `pnpm build` → clean compile (3.0s), 313 prerendered pages, First Load JS shared chunk 103 kB.
- 5 visualizations live: RatingRadar (Phase 1), SaturationCurve, MoversBoard, RatingHistoryStream (Phase 3), **DomainMap (Phase 4)**.
- 7 ADRs: 0001 (Next.js), 0002 (Velite), 0003 (Zod), 0004 (file-first), 0005 (rating-action immutability), 0006 (saturation N/A), **0007 (DomainMap rendering + D3 import policy)**.
- 3 OPEN_QUESTIONS threads closed this phase: **Q38** (filter-chip URL persistence, decided-as-lean confirmed), **Q40** (ADR-0007 scope, decided), and Q40 implicitly. Q37 / Q39 remain open (issue-template field schemas may evolve; DomainMap mobile-viewport a11y to be re-evaluated post-baselines).

### `lighthouserc.json` updated this commit

URLs added: `/domains` (the brushable-DomainMap index from Unit 4.3) and `/contributing` (the curator-workflow page from Unit 4.6). Final URL list: 12 entries (was 10 after Unit 3.13a). CI cohort runs against the deployed URLs on first PR.

### Phase 5 entry conditions

Per §12 cardinal rule (mirror of all prior phase boundaries): **explicit human sign-off required**. Phase 5 deliverables per §13:
- arXiv ingestion helper (CLI in `scripts/`) producing draft paper YAML for curator review.
- LLM-assisted extraction of candidate leaderboard entries from a paper PDF (with mandatory human-review diff; no auto-merge).
- Email/RSS digest: per-domain weekly summary.

The §12 DB-migration trigger re-evaluation (Phase 5 mandatory per Unit 4.12) is the first item to revisit. The auth-trigger flips the moment a Phase-5 write-path lands.

### Cross-phase milestone

This commit closes Phase 4 in its entirety as authored in Unit 4.0. The 14-unit breakdown shipped end-to-end with one re-scoping deviation: drag interactivity (Unit 4.0 D-6 #3) deferred from Unit 4.2 to Phase 5 — captured as a known trade-off in ADR-0007.

### Phase-4 follow-ons that are NOT gate-blocking but real

- **W3C RSS validator pass against deployed `/api/v1/rss.xml`** — Phase-3 follow-on, still pending first preview deploy.
- **Visual-regression baselines** for `/`, `/domains`, `/contributing`, `/contributing/v1.0.0` × 2 themes × N viewports — Phase-4 follow-on, captured locally on next baseline pass.
- **Manual issue-template smoke** in the GitHub UI — one-time check the first time this commit's PR is opened.
- **Orphan `components/domain-tile-grid/`** — unused since Units 4.3 / 4.4 swapped to DomainMap. Phase-5 hygiene cleanup candidate (deletion gated on explicit authorization per the destructive-action rule).
- **`entries.json` content pass** on the 8 problems that still lack curator-authored entries — Phase-2/Phase-5 content backlog.
- **`docs/SESSION_HANDOFF_phase3_close.md`** — the prior-session resume artifact. The parallel session's staged `.gitignore` change adds it to the ignore list; ships when they commit.

### Smoke gates (this unit)

- `pnpm typecheck` — clean.
- `pnpm test` — 199/199 across 29 files.
- `pnpm validate-content` — 203 files.
- `pnpm audit-content` — 0 errors / 6 Q32-expected warnings.
- `pnpm build` — 313 prerendered pages, First Load JS 103 kB shared chunk.
- `lighthouserc.json` — URL list extended to 12 entries.

### CHANGELOG entry shape

Closing entry for Phase 4: roll-up table of all 14 units, state-at-HEAD summary, §13 deliverable status, Phase-5 entry conditions, follow-ons that survive the gate.
