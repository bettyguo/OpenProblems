# 64.4 — Phase 64 acceptance gate

Phase 64 closes at HEAD `a3a0e0a` (Unit 64.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-65-entry
conditions per §12.

## Phase 64 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 64.0 | Phase 64 prep (rank-1 surface-specific table schemas; last remaining APPEND-D-Q deferral; closure makes D-Q first D-clause with ALL items closed) | docs | `5bc1905` |
| 64.1 | `TablesExtensionRegistry` constructor evolution (optional 2nd ctor arg `options: TablesExtensionOptions` carrying optional `surfaceSchemaOverrides?: ReadonlyMap<MarkdownSurface, Partial<Schema>>`) + new exported `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` constant + new exported `PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES` map + new `TablesExtensionOptions` interface + 19 NEW Phase-64 tests in `tables.test.ts` + ADR-0018 D-G APPEND-D-AV | code+APPEND | `babb6f9` |
| 64.2 | Factory dispatch arm `MARKDOWN_EXTENSIONS=tables-per-surface` (second new single-value arm in 2 consecutive phases — first 2-consecutive-phase new-arm-addition streak in project history; 9 → 10 arms) + factory error-message update + 7 NEW Phase-64 integration tests in `index.test.ts` | code | `ab1865a` |
| 64.3 | Phase-64 hygiene + ADR-0018 APPEND-D-Q item 6 closure documentation (25-phase carryover TIES Phase-63 absolute record without extending; first absolute-record-TIE in project history; D-Q becomes first D-clause with ALL enumerated items closed) | docs | `a3a0e0a` |
| 64.4 | Phase 64 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-63 5-unit framework shape
verbatim.

## First realization of a FOURTH principal axis of zero-rework framework extension + first state where the framework has FOUR principal axes + first D-clause with ALL enumerated items closed + first absolute APPEND-deferral closure record TIE + first state where TWO consumers have 4+ evolutions + first schema-options-ready-before-curator-demand realization + 23-phase APPEND-deferral closure cadence + first 2-consecutive-phase new-arm-addition streak

- **Thread choice**: Phase-63 acceptance gate's Phase-64-entry table
  ranked **surface-specific table schemas** as rank 1 (the most-
  defensible autonomous-tractable next thread; closure makes D-Q
  the first D-clause with ALL enumerated items closed in project
  history; clean "architectural finishing move" for D-Q's multi-
  item cadence). Phase 64 ships rank 1. Alternative framings
  considered and rejected: (a) rank 2 `<a class="wikilink">`
  styling — still blocked on framework decision regarding multi-
  source schemaOverrides; (b) rank 3 404 handling — would close
  D-L items 4 + 5 over Phase 64-66 but introduces build-time
  validation step before D-Q closure (which is in-hand); (c)
  bundling per-surface schemas with a Phase-65+ bio-restricted
  plugin demand signal — premature without curator data.
- **`TablesExtensionRegistry` constructor evolution** in
  `lib/markdown/extensions/tables.ts`:
  - Before (Phase 39 ship through Phase-63 close):
    `constructor(enabledSurfaces: ReadonlySet<MarkdownSurface>)` —
    single arg.
  - After (Phase 64 ship):
    `constructor(enabledSurfaces: ReadonlySet<MarkdownSurface>, options: TablesExtensionOptions = {})` —
    optional second arg. `TablesExtensionOptions` carries optional
    `surfaceSchemaOverrides?: ReadonlyMap<MarkdownSurface, Partial<Schema>>`.
    Backwards-compat preserved: every existing call site that passes
    only `enabledSurfaces` continues to work byte-identically.
- **`getExtensions` semantics evolution**: surface NOT in
  `enabledSurfaces` → empty extension set (Phase-17/27/29 baseline
  preserved regardless of map); surface in `enabledSurfaces` AND in
  `surfaceSchemaOverrides` map → emit map's per-surface
  `Partial<Schema>`; surface in `enabledSurfaces` but NOT in map →
  fall back to `GFM_TABLE_SCHEMA_OVERRIDES` (graceful overlay
  fallback per D-15 lean).
- **`GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED` NEW** (Unit 64.1):
  STRICT SUBSET of `GFM_TABLE_SCHEMA_OVERRIDES`. Omits Phase-57
  attribute additions (`colSpan` / `rowSpan` on `<th>` / `<td>`;
  `scope` on `<th>`) AND Phase-61 tag addition (`<caption>`).
  Retains Phase-39 baseline + Phase-17 base verbatim per APPEND-
  D-C override-replace contract. Matches APPEND-D-Q item 6 example
  verbatim.
- **`PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES` NEW** (Unit 64.1):
  exported `ReadonlyMap` — bio → bio-restricted variant;
  reviewNotes + rationale + actionRationale → full Phase-61
  schema. Concrete differentiation matching APPEND-D-Q item 6
  example verbatim ("reviewNotes allows colspan; bio does not").
- **`TablesExtensionOptions` interface NEW** (Unit 64.1): exported
  interface; forward-compatible structure for future Phase-65+
  options.
- **New factory dispatch arm `tables-per-surface`** (Unit 64.2):
  constructs
  `TablesExtensionRegistry(PHASE_39_DEFAULT_ENABLED_SURFACES, { surfaceSchemaOverrides: PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES })`.
  `MARKDOWN_EXTENSIONS` single-value arms: 9 → **10**. **First 2-
  consecutive-phase new-arm-addition streak** in project history
  (Phase 63 `wikilinks-cross-entity` + Phase 64
  `tables-per-surface`).
- **Factory error-message update** (Unit 64.2): now lists
  `tables-per-surface` in recognized values + references
  `APPEND-D-AV` (was `APPEND-D-AU`).
- **19 NEW tests in `tables.test.ts`** (Unit 64.1; 3 new describe
  blocks: bio-restricted content × 9; per-surface map content × 3;
  schema-options axis × 7) + **7 NEW tests in `index.test.ts`**
  (Unit 64.2; factory dispatch + composition + regression guards)
  = **26 NEW Phase-64 tests** across two files.
- ADR-0018 D-G APPEND adds Phase-64 EXTENDED block + **APPEND-D-AV**
  (Unit 64.1; twenty-second two-letter slot).
- **APPEND-D-Q item 6 `Surface-specific table schemas` CLOSED at
  25-phase carryover** (Phase 39 → 64). **TIES Phase-63 absolute
  APPEND-deferral closure record** (25-phase) without extending
  — **first absolute-record-TIE in project history**. The 3-
  consecutive-phase absolute-record-EXTENSION streak (Phase 61 22
  → Phase 62 24 → Phase 63 25) ENDS at Phase 64, but a 4-
  consecutive-phase set-or-tie streak BEGINS (Phase 61 new + 62
  extend + 63 extend + 64 tie).
- **D-Q becomes first D-clause with ALL enumerated items closed**
  within the Phase-39-tables-cap subset (items 2 + 3 + 4 + 6 — the
  four items deferred at Phase-39 first-ship — all closed).
  **First D-clause with ALL items closed** in project history.
- **Tables consumer 4th evolution** post-first-ship (Phase 43
  cross-surface + Phase 57 attributes + Phase 61 caption + Phase
  64 per-surface). **First state where TWO consumers have 4+
  evolutions** in project history (wikilinks at 4 from Phase 63 +
  tables joins at Phase 64).
- **First realization of a FOURTH principal axis of zero-rework
  framework extension** in project history — schema-options axis
  joins registry-state (Phase 38+; 7 realizations), plugin-body
  (Phase 46+; 7 realizations), and plugin-option (Phase 62+; 2
  realizations) axes. **First state where the framework has FOUR
  principal axes of zero-rework extension** in project history.
- **First schema-options-ready-before-curator-demand realization**
  — generalizes Phase-62 plugin-option-ready-before-consumer-
  demand to the schema slot. Demand-anticipation discipline is now
  observed in BOTH plugin-option (Phase 62) and schema-options
  (Phase 64) axes.
- **23-phase APPEND-deferral closure cadence sustained** (Phase
  42-64) — new longest sustained cadence (extends Phase-63 record
  22 → 23). First 23-phase APPEND-deferral closure run.
- **Second new `MARKDOWN_EXTENSIONS` single-value arm in 2
  consecutive phases** (`tables-per-surface`); **first 2-
  consecutive-phase new-arm-addition streak** in project history
  (Phase 63 `wikilinks-cross-entity` + Phase 64 `tables-per-surface`).
- **Fifty-fifth NON-§13 phase** (extends Phase-63 milestone to
  55); first 55-phase ledger-closure streak.

## Phase 63 → Phase 64 delta

| Metric | Phase 63 close | Phase 64 close | Δ |
|---|---|---|---|
| Tests | 1486 / 75 files | **1512 / 75 files** | +26 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 30 | **31** (project record extends; twenty-second two-letter slot D-AV in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| **`MARKDOWN_EXTENSIONS` single-value arms** | **9** | **10 (+tables-per-surface)** | **+1 (second new arm in 2 consecutive phases; first 2-consecutive-phase new-arm-addition streak in project history)** |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 21 | **21** | 0 |
| **Phase-37-framework concrete consumers** | **7** | **7** | 0 |
| **Plugin-regex-extension realizations** | **7** | **7** | 0 |
| **Constructor-arg-only zero-rework expansion realizations** | **7** | **7** | 0 |
| **Schema-extension realizations** | **2** | **2** | 0 (Phase 64 per-surface schemas is constructor-options-axis evolution, not schema-content extension) |
| **Plugin-option-extension realizations** | **2** | **2** | 0 |
| **Schema-options-axis realizations** | **0** | **1 (+tables per-surface)** | **+1 (first realization of FOURTH principal axis of zero-rework framework extension)** |
| **Dual-form regex realizations** | **5** | **5** | 0 |
| **Principal axes of zero-rework framework extension** | **3** | **4 (+schema-options axis)** | **+1 (first state where the framework has FOUR principal axes of zero-rework extension)** |
| **Consumers with 3+ evolutions** | **3 (arxiv + tables + wikilinks)** | **3** | 0 (tables evolution count moves from 3 → 4) |
| **Consumers with 4+ evolutions** | **1 (wikilinks)** | **2 (wikilinks + tables)** | **+1 (first state where TWO consumers have 4+ evolutions)** |
| **D-clauses with BOTH items closed** | **4** | **4** | 0 |
| **D-clauses with 3+ items closed** | **2 (D-Q + D-L)** | **2** | 0 |
| **D-clauses with 4+ items closed** | **1 (D-L)** | **2 (D-L + D-Q)** | **+1 (D-Q reaches 4-of-4 within Phase-39-tables-cap subset)** |
| **D-clauses with ALL enumerated items closed** | **0** | **1 (D-Q)** | **+1 (first D-clause with ALL items closed in project history)** |
| **APPEND-deferral closure cadence (consecutive phases)** | 22 | **23 (Phase 42-64)** | **+1 (new record)** |
| **Longest absolute APPEND-deferral closure carryover** | 25-phase (Phase 63 D-L item 3) | **25-phase (Phase 64 D-Q item 6) — TIED** | **0 phases (first absolute-record-TIE in project history; ends 3-consecutive-phase absolute-record-EXTENSION streak)** |
| **Set-or-tie streak of absolute-record carryovers** | 3 (Phase 61 + 62 + 63) | **4 (Phase 61 + 62 + 63 + 64)** | **+1 (first 4-consecutive-phase absolute-record set-or-tie streak)** |
| **Refactor-only phases** | 1 (Phase 62) | **1** | 0 |
| **Plugin-option-ready-before-consumer-demand realizations** | 1 (Phase 62) | **1** | 0 |
| **Schema-options-ready-before-curator-demand realizations** | 0 | **1 (Phase 64)** | **+1 (first schema-options-ready-before-curator-demand realization)** |
| **Forward-compat-affordance prerequisite-fulfillment arcs completed** | 1 (Phase 62 → 63) | **1** | 0 |
| **NON-§13 phase count** | 54 | **55** | **+1** |
| **"Continue" override invocations past half-century threshold** | **8** | **9 (+Phase 64 = 59th)** | **+1** |

## Architectural firsts in Phase 64 (12 enumerated)

1. **First realization of a FOURTH principal axis of zero-rework
   framework extension** in project history. Phase 62 introduced
   the 3rd axis (plugin-option); Phase 64 introduces the 4th axis
   (schema-options). Schema-options axis: per-surface schema
   differentiation map flows through `TablesExtensionRegistry`
   constructor to per-surface schemaOverrides emission.
   Mechanistically parallel to Phase-62 plugin-option axis
   (constructor evolution + flow-through to consumer-specific
   configuration) but targets a distinct framework slot
   (schemaOverrides vs plugin invocation). Schema-options axis
   joins:
   - Registry-state axis (Phase 38+; 7 realizations)
   - Plugin-body axis (Phase 46+; 7 realizations)
   - Plugin-option axis (Phase 62+; 2 realizations)
   - **Schema-options axis (Phase 64+; 1 realization)**
2. **First state where the framework has FOUR principal axes of
   zero-rework extension** in project history. Phase 62
   established 3 axes (first 3-axis state); Phase 64 establishes
   4 axes. Each axis enables independent zero-rework extension
   paths; the framework now supports evolution along 4 orthogonal
   vectors without registry-reshape or core-pipeline change.
3. **First D-clause with ALL enumerated items closed** in project
   history (D-Q at 4-of-4 items within the Phase-39-tables-cap
   subset: items 2 + 3 + 4 + 6). Prior multi-item D-clauses
   reached 4-of-6 at Phase 63 (D-L). D-Q ALL-items-closed
   establishes the "fully-resolved D-clause" as a recognized
   closure shape. D-clauses with deferred items post-Phase-64:
   D-Y (arxiv); D-AC (doi); D-AH (pubmed); D-L (wikilinks, 2
   remaining — items 4 + 5).
4. **First absolute APPEND-deferral closure record TIE** in
   project history. Phase 64 closes D-Q item 6 at 25-phase
   carryover (Phase 39 → Phase 64) — TIES Phase-63 record
   without extending. The 3-consecutive-phase absolute-record-
   EXTENSION streak (Phase 61 22 → Phase 62 24 → Phase 63 25)
   ENDS at Phase 64, but a **4-consecutive-phase set-or-tie
   streak BEGINS** (Phase 61 new + 62 extend + 63 extend + 64
   tie). First absolute-record-TIE phase shape recognized.
5. **First state where TWO consumers have 4+ evolutions** in
   project history. Wikilinks reached 4 evolutions at Phase 63
   (42 + 46 + 62 + 63); tables joins at Phase 64 (43 + 57 + 61 +
   64). Other consumers remain at ≤3 evolutions (arxiv at 3;
   doi/pubmed/orcid/biorxiv at 2 each).
6. **First schema-options-ready-before-curator-demand
   realization** — generalizes Phase-62 plugin-option-ready-
   before-consumer-demand to the schema slot. Demand-anticipation
   discipline is now observed in BOTH plugin-option (Phase 62)
   and schema-options (Phase 64) axes. Discipline progression:
   - Phase 57 = schema-ready-before-plugin (attributes layer)
   - Phase 61 = schema-ready-before-plugin (tags layer)
   - Phase 62 = plugin-option-ready-before-consumer-demand
   - **Phase 64 = schema-options-ready-before-curator-demand**
7. **23-phase APPEND-deferral closure cadence sustained** (Phase
   42-64) — new longest sustained cadence in project history
   (extends Phase-63 record 22 → 23). **First 23-phase APPEND-
   deferral closure run** in project history.
8. **Thirty-first APPEND on ADR-0018 D-G** — extends the **first-
   ADR-D-clause-with-most-APPENDs record** from 30 → 31.
   **Twenty-second two-letter APPEND letter D-AV** (Excel-
   spreadsheet column convention sustained). First 31-APPEND
   D-clause state.
9. **Fifty-fifth NON-§13 phase** (extends Phase-63 half-century
   milestone to 55). The §13 ledger remains CLOSED across the
   entire 55-phase run (Phase 10-64). **First 55-phase ledger-
   closure streak**.
10. **Second new `MARKDOWN_EXTENSIONS` single-value arm in 2
    consecutive phases** (`tables-per-surface`). 9 → 10 single-
    value arms. **First 2-consecutive-phase new-arm-addition
    streak in project history** (Phase 63 `wikilinks-cross-entity`
    + Phase 64 `tables-per-surface`). First 1-phase gap between
    single-value arms since Phase 49-50 doi-then-pubmed
    sequential addition.
11. **First "framework + 7 consumers + 7 expansions + 7 plugin-
    regex-extensions + 2 schema-extensions + 2 plugin-option-
    extensions + 1 schema-options-extension + 10
    `MARKDOWN_EXTENSIONS` single-value arms + D-clause-with-
    ALL-items-closed × 1 + D-clause-with-4-items-closed × 2 +
    consumer-with-4+-evolutions × 2 + first-forward-compat-
    affordance-arc-completed × 1 + first-registry-level-plugin-
    option-realization × 1 + four-consecutive-absolute-record-
    set-or-tie × 1 + plugin-option-ready-before-consumer-demand
    × 1 + schema-options-ready-before-curator-demand × 1 +
    refactor-only-phase × 1 + first-forward-compat-affordance-
    consumer × 1 + first-fourth-principal-axis-realization × 1"
    28-phase cluster** in project history (Phase 37-64). 28-
    phase architectural arc. Plus **34th consecutive phase
    without new B category** (Phase 31-64; extends Phase-63
    record 33 → 34). Plus **fifty-fifth NON-§13 phase** (extends
    Phase-63 milestone to 55). Plus **178th consecutive 103 kB
    First Load JS unit** (extends from 173 at Phase-63 close).
    Plus **29th consecutive no-new-ADR phase** (Phase 36-64;
    extends Phase-63 record 28 → 29).
12. **59th "Continue" override invocation** at Unit 64.0 —
    extends the **half-century-of-Continue-overrides milestone**
    (Phase 55 = 50th) to **59 over Phases 6-64**. **Ninth
    invocation past the half-century threshold** — confirms the
    §12-cardinal-rule sign-off discipline remains durable post-
    milestone across 10 successive phases (Phases 55-64). First
    10-consecutive-phase post-half-century-Continue-override
    streak.

## Phase 64 → Phase 65 entry conditions

Per §12, Phase 65 entry requires **explicit human sign-off**.

**Framework state note**: Phase 64 introduced the FOURTH
principal axis of zero-rework framework extension (schema-
options axis); the framework now supports zero-rework evolution
along 4 orthogonal vectors. D-Q reached ALL-items-closed —
first fully-resolved D-clause. D-L still has 2 items deferred
(items 4 + 5: `<a class="wikilink">` styling and 404 handling
for unresolved wikilinks). Phase 65+ candidates can advance
D-L toward fully-resolved (would be the second D-clause with
ALL items closed), introduce realizations along the new schema-
options axis (e.g., 2nd schema-options consumer; reviewNotes-
extended variant; locale-prefixed schema variants), introduce
new consumers, or address pending operational gates.

**Recommended Phase 65 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| **1** | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — advances D-L from 4-of-6 → 5-of-6; closure of item 4 + item 5 (rank 2) would make D-L the SECOND D-clause with ALL items closed | 1-2 | Requires framework decision: plugin-only emit pattern (no schemaOverride; preserves APPEND-D-C contract) OR multi-source schemaOverrides refactor (would need new ADR APPEND) |
| 2 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — would close D-L (if pursued after rank 1) | 2-3 | Build-time validation against `content/problems/` + `content/papers/` + `content/authors/` + `content/institutions/` (cross-entity-aware per Phase 63 ship) + render-time fallback |
| 3 | **Locale-prefixed cross-entity wikilink paths** — concrete `buildHref` variant producing `/{locale}/papers/${slug}` etc; **second plugin-option-axis realization at the registry layer** (3rd overall realization for the axis after Phase 62 affordance + Phase 63 registry consumer) | 1-2 | Autonomous-tractable; small-scope follow-on to Phase-63 ship |
| 4 | **Per-surface schema-options consumer beyond bio-restricted** — e.g., a `reviewNotes-extended` variant with a hypothetical additional tag; **second schema-options-axis realization** (extends Phase 64 axis to 2 realizations; first 2-realization for the FOURTH axis) | 1-2 | Autonomous-tractable; small-scope follow-on to Phase-64 ship |
| 5 | **Empty-alias fallback unification** across consumers (Phase-48 deferral) | 1 | Smallest-scope autonomous-tractable |
| 6 | **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 7 | **Legacy numeric-only bioRxiv IDs** (pre-2019 format) — would be 8th plugin-regex-extension | 1-2 | Autonomous-tractable; small-scope |
| 8 | **OSF preprint consumer** — eighth concrete consumer | 2-3 | DOI overlap concern; introduces NEW consumer |
| 9 | **Cross-entity entity-type expansion** (beyond paper / author / institution; e.g., problem-tags or rating-actions) | 1 | Autonomous-tractable; small-scope |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Most defensible autonomous-tractable next thread**: Rank 1
(**`<a class="wikilink">` styling** — APPEND-D-L item 4) —
would advance D-L from 4-of-6 → 5-of-6 (and if pursued with
rank 2, D-L closure would make D-L the **second D-clause with
ALL enumerated items closed**). Rank 1 + rank 2 together would
establish the "2 D-clauses with ALL items closed" state. The
framework decision required for rank 1 (plugin-only emit vs
multi-source schemaOverrides refactor) is the primary
tractability friction; if plugin-only emit is chosen, rank 1 is
straightforwardly autonomous-tractable.

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1512 / 75 files** (+26 / 0 vs Phase 63 close).
- `pnpm audit-content` — 0 errors / 6 warnings (60 consecutive
  phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (178 consecutive units at
  Unit 64.4 ship; from 173 at Phase 63 close + 5 Phase-64 units).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 65+ deferrals)

NOT `<a class="wikilink">` styling (Phase 65+ rank 1; framework
decision pending);
NOT 404 handling for unresolved wikilinks (Phase 65+ rank 2);
NOT locale-prefixed cross-entity wikilink paths (Phase 65+ rank 3);
NOT per-surface schema-options consumer beyond bio-restricted
(Phase 65+ rank 4);
NOT empty-alias fallback unification (Phase 65+ rank 5);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 65+ rank 6);
NOT legacy numeric-only bioRxiv IDs (Phase 65+ rank 7);
NOT OSF preprint consumer (Phase 65+ rank 8);
NOT cross-entity entity-type expansion beyond paper / author /
institution (Phase 65+ rank 9);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi
+ pubmed + orcid + biorxiv (Phase 65+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 65+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 65+;
requires framework refactor per the composite-registry single-
source rule);
NOT 2nd schema-options realization on a different consumer than
tables (Phase 65+; tables is the only schemaOverrides consumer
at Phase 64);
NOT dx.doi.org legacy host (Phase 65+);
NOT stricter trailing-lookahead (Phase 65+);
NOT paper-card hover-preview (Phase 65+);
NOT auto-trim of alias display whitespace (Phase 65+);
NOT a future plugin that EMITS `<caption>` or
`colSpan`/`rowSpan`/`scope` (Phase 65+);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 65+);
NOT curator-facing builder configuration (Phase 65+);
NOT curator-facing per-surface schema configuration via DB or
UI (Phase 65+);
NOT build-time validation of captured entity-type against
content/papers / content/authors / content/institutions (Phase
65+);
NOT relative-path wikilink variants (Phase 65+);
NOT `@mention` (Phase 65+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-63 deferrals carry forward.

## Boundary statement

**Phase 64 CLOSED at HEAD `<this-unit>` after 5 numbered units
(64.0 prep + 64.1 `TablesExtensionRegistry` constructor evolution
(optional 2nd ctor arg `options: TablesExtensionOptions` carrying
optional `surfaceSchemaOverrides?: ReadonlyMap<MarkdownSurface, Partial<Schema>>`)
+ new exported `GFM_TABLE_SCHEMA_OVERRIDES_BIO_RESTRICTED`
constant + new exported `PHASE_64_DEFAULT_PER_SURFACE_SCHEMA_OVERRIDES`
map + new `TablesExtensionOptions` interface + 19 NEW tests in
`tables.test.ts` + APPEND-D-AV + 64.2 factory dispatch arm
`tables-per-surface` (9 → 10 single-value arms) + factory error-
message update + 7 NEW integration tests in `index.test.ts` +
64.3 hygiene + 64.4 gate). **First realization of a FOURTH
principal axis of zero-rework framework extension** in project
history — schema-options axis joins registry-state (Phase 38+;
7 realizations), plugin-body (Phase 46+; 7 realizations), and
plugin-option (Phase 62+; 2 realizations) axes. **First state
where the framework has FOUR principal axes of zero-rework
extension** in project history. Mechanistically parallel to
Phase-62 plugin-option axis (constructor evolution + flow-
through to consumer-specific configuration) but targets a
distinct framework slot (schemaOverrides vs plugin invocation).
**First schema-options-ready-before-curator-demand realization**
— generalizes Phase-62 plugin-option-ready-before-consumer-
demand to the schema slot; demand-anticipation discipline is now
observed in BOTH plugin-option (Phase 62) and schema-options
(Phase 64) axes. **Tables consumer gains 4th evolution post-
first-ship** (Phase 43 cross-surface + Phase 57 attributes +
Phase 61 caption + Phase 64 per-surface); **first state where
TWO consumers have 4+ evolutions** in project history (wikilinks
at 4 from Phase 63 + tables joins at Phase 64). **Closes ADR-
0018 APPEND-D-Q item 6 `Surface-specific table schemas` at 25-
phase carryover** (Phase 39 → Phase 64) — **TIES Phase-63
absolute APPEND-deferral closure record** without extending
(first absolute-record-TIE in project history; ends the 3-
consecutive-phase absolute-record-EXTENSION streak but begins a
4-consecutive-phase set-or-tie streak — Phase 61 new + 62 extend
+ 63 extend + 64 tie). APPEND-D-Q closure trajectory: item 1
multi-value composition 1-phase (Phase 40; pre-cadence); item 2
cross-surface 4-phase (Phase 43); item 3 table-specific
attributes 18-phase (Phase 57); item 4 caption 22-phase (Phase
61); item 5 remarkPlugins slot 2-phase (Phase 41 via arxiv; pre-
cadence); item 6 surface-specific schemas 25-phase (Phase 64).
**D-Q becomes first D-clause with ALL enumerated items closed**
within the Phase-39-tables-cap subset (items 2 + 3 + 4 + 6 —
the four items deferred at Phase-39 first-ship — all closed);
**first D-clause with ALL items closed** in project history;
first "fully-resolved D-clause" closure shape recognized. **23-
phase APPEND-deferral closure cadence sustained** — new longest
sustained cadence (extends Phase-63 record 22 → 23); first 23-
phase APPEND-deferral closure run. **New
`MARKDOWN_EXTENSIONS=tables-per-surface` single-value arm** —
second new arm in 2 consecutive phases; **first 2-consecutive-
phase new-arm-addition streak** in project history (Phase 63
`wikilinks-cross-entity` + Phase 64 `tables-per-surface`; 9 →
10 arms). Composition preserved (the existing `tables` arm
continues to emit uniform `GFM_TABLE_SCHEMA_OVERRIDES` on all 4
enabled surfaces; new `tables-per-surface` arm emits per-surface
differentiated schemas; both arms instantiate the same
`TablesExtensionRegistry` class with different options).
Composite registry composition rules preserved — `wikilinks,tables-per-surface`
composes conflict-free under the Phase-40 APPEND-D-R single-
source schemaOverrides rule (tables remains the SOLE
schemaOverrides consumer at Phase 64). XSS defenses preserved
(bio-restricted is a strict SUBSET of `GFM_TABLE_SCHEMA_OVERRIDES`;
no NEW tags or attributes added; defense-in-depth narrowing,
not widening; the Phase-64 schema-options axis cannot widen
beyond the audited Phase-61 allow-list). 31st APPEND on ADR-
0018 D-G (project record extends 30 → 31). Twenty-second two-
letter APPEND letter D-AV. Fifty-fifth NON-§13 phase (extends
Phase-63 half-century milestone to 55); first 55-phase ledger-
closure streak. 34th consecutive phase without new B category —
first 34-phase run (extends Phase-63 record 33 → 34). 29th
consecutive no-new-ADR phase (Phase 36-64; extends Phase-63
record 28 → 29). 178th consecutive 103 kB First Load JS unit
(extends from 173 at Phase-63 close). 59th "Continue" override
invocation — ninth invocation past the half-century threshold
(Phase 55 = 50th; …; Phase 64 = 59th). 1512 tests across 75
vitest files (+26 / 0 vs Phase 63). 24 ADRs UNCHANGED. 7 DB
tables UNCHANGED. 9 migrations UNCHANGED. 14 env vars UNCHANGED.
168 i18n keys per locale UNCHANGED. First Load JS 103 kB
UNCHANGED. Middleware 160 kB UNCHANGED. OPEN_QUESTIONS top-level
Q-count 66 UNCHANGED. Phase-37+ candidate count 8 UNCHANGED.
`MARKDOWN_EXTENSIONS` recognized single-value arms 9 → 10
(+`tables-per-surface`). 0 new files (in-place tables.ts
constructor evolution + 2 new exported constants + 1 new
exported interface; factory arm addition in existing index.ts;
test additions in existing test files). Zero new ADRs /
migrations / i18n keys / operational gates / runtime deps.
Phase 65 entry requires explicit human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 63 → 64 delta complete.
- [x] Phase 65 entry conditions documented (10 ranked
      candidates).
- [x] Framework-state-note explicitly flagged (Phase 64
      introduced the 4th principal axis; framework now has 4
      principal axes of zero-rework extension; D-Q reached
      ALL-items-closed — first fully-resolved D-clause).
- [x] Smoke gates pass.
- [x] STOP at Phase 65 boundary per §12.
