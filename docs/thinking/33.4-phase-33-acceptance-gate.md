# 33.4 — Phase 33 acceptance gate (per-user-account subscriptions realized via ADR-0023 Option A; **23 ADRs**; 5 units; closes ADR-0021 D-C Phase-31+ anticipation at 2-phase Q-carryover; **subscriber-list-email arc completes Phase 30 → 31 → 32 → 33 = 4-phase complete-feature pair** — first such arc in project history; first ADR to close an explicit prior ADR's Phase-N+1 anticipation at tight follow-on; third migration on the `subscriber` table within 4 phases; first migration-cluster-resume-after-1-phase-gap; first 5-unit new-ADR phase; 29th "Continue" override opportunity at Phase 33 → 34 boundary)

Fifth and final Phase-33 unit; docs-only. Closes Phase 33 (**twenty-
fourth NON-§13 phase**; per-user-account subscriptions realized via
new ADR-0023 Option A FK extension + `subscriber.userId` column +
`decideSubscriberUserId` pure helper + subscribe-route `safeAuth()`
integration + cascade-on-user-delete constraint + Q76 status flip to
resolved + Q79 promotion + 2 ADR APPENDs). Mirrors Phase-12 through
Phase-32 acceptance-gate patterns. **§13 ledger CLOSED at Unit 9.9**
(carried unchanged through Phases 10-32; Phase 33 inferred-not-§13
like its twenty-three predecessors).

## THINK

### Phase 33 ledger

**5 units shipped** (33.0 prep → 33.4 gate). **0 deferrals**; **0
scope drift**. **Twenty-second consecutive well-scoped phase**.
**First 5-unit new-ADR phase in project history** (Phase 19 + 28 +
30 + 31 = all 6-unit new-ADR phases; Phase 33 smaller code scope =
single column extension + single route update + no new module + no
new template + no new UI surface justifies the 5-unit shape).

| Unit | Title | Type | Commit |
|---|---|---|---|
| 33.0 | Phase 33 prep | docs | `84ad696` |
| 33.1 | ADR-0023 per-user-account subscriptions | docs | `e655520` |
| 33.2 | subscriber.userId FK + migration + subscribe-route + tests | code | `05f2149` |
| 33.3 | hygiene + Q76 resolved + Q79 + 2 ADR APPENDs | docs | `e87ac73` |
| 33.4 | Phase 33 acceptance gate | docs | this commit |

### Final smoke gates (all green at HEAD pre-this-unit `e87ac73`)

- `pnpm typecheck` clean.
- `pnpm lint` clean.
- `pnpm test` → **718/718 across 63 vitest files** (+6 / +0 file Δ
  from Phase 32 close 712/63; Unit 33.2 added all +6/+0 in existing
  `lib/subscribers/index.test.ts`).
- `pnpm build` → ~659 prerendered pages + **13 dynamic page+API
  routes** UNCHANGED (Phase 33 modifies existing `/api/v1/subscribe`
  route; no new routes); **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-33 unit (**44th
  consecutive unit at 103 kB**); **middleware bundle = 160 kB
  UNCHANGED** since Phase 12; all page bundles UNCHANGED.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline since
  Phase 2; unchanged through every Phase 3-33 unit).

### Phase 32 → Phase 33 architectural firsts (cumulative across the phase; ~17 firsts)

1. **First new ADR since Phase 31** (Phase 32 = no-new-ADR) = **2-phase
   ADR-cycle gap**; first 2-phase gap since Phase 19 → 28 = 9-phase
   gap (shorter than Phase 19-28 cycle).
2. **ADR-0023 candidate slot claimed at 2-phase age** (Phase 32 gap).
3. **First ADR to close an explicit prior ADR's Phase-N+1 anticipation
   at tight follow-on** (ADR-0021 D-C → ADR-0023 at 3-phase elapsed;
   mirrors ADR-0020 D-B partial-supersession pattern but for
   anticipation-vs-supersession).
4. **First "complete-feature 4-phase pair" in project history**
   (Phase 30 + 31 + 32 + 33 = subscriber-list-email full delivery arc:
   foundation + scheduler + cleanup + authenticated-linkage).
5. **First 5-unit new-ADR phase in project history** (Phase 19 + 28 +
   30 + 31 = 6-unit; Phase 33's smaller code scope = 5-unit;
   **establishes 5-unit shape as new-ADR-with-small-code-scope rhythm**).
6. **Third consecutive new-ADR phase to ship 8 D-clauses** (Phase 30 +
   31 + 33 all 8 D-clauses; **confirms 8-clause shape as stable
   new-ADR-phase ADR shape**).
7. **First non-zero-migration phase 3 of 4 phases** (Phase 30 + 31 +
   33; Phase 32 gap).
8. **Third migration on the `subscriber` table within 4 phases**
   (Phase 30 create + Phase 31 `+lastDigestSentAt` + Phase 33
   `+userId`) — **most active table-extension cluster in project
   history**.
9. **First "migration-cluster-resume-after-1-phase-gap"** in project
   history (Phase 30 + 31 cluster + Phase 32 gap + Phase 33 resume;
   mirrors Phase 15-16 cluster shape with post-cluster-pause extension).
10. **First FK column added to a project-owned table via ALTER** (prior
    FK columns were created at table-introduction time).
11. **First manual SQL edit to a drizzle-kit-generated migration** to
    add `ON DELETE CASCADE` clause.
12. **First `decideSubscriberUserId` 10-case decision-table pure
    helper** in project history (3-branch pure helper covers all 10
    D-E table cases via composition).
13. **First fourth in-place `safeAuth()` copy** per ADR-0023 Negative
    consequences extraction-threshold pattern (Phase-7 D-8 established
    "5+ copies"; current count = 4 after Phase 33; extraction = Phase
    34+ candidate if 5th copy surfaces).
14. **First subscribe-route `auth()` integration** (Phase 30 subscribe
    route was auth-unaware; Phase 33 reads session for userId-population).
15. **First Q-status-flip phase since Phase 30** (Q76 → resolved;
    Phase 31 + 32 hygiene units only added new Qs without flipping
    existing ones; 3-phase gap).
16. **First ADR-D-clause to be the source of TWO different APPENDs**
    (ADR-0021 D-F Phase 31 + ADR-0021 D-C Phase 33).
17. **Third APPEND on ADR-0013 D-F** — ties ADR-0018 D-G at 3 APPENDs
    (joint-most-extended ADR D-clause in project history).
18. **Two D-clauses with 3+ APPENDs in project history** (ADR-0013 D-F
    + ADR-0018 D-G).
19. **Fifth APPEND-pattern ADR D-clause cluster** in project history
    (ADR-0021 D-C; mirrors Phase-31 + 32 APPEND-shipping rhythm).
20. **Three consecutive APPEND-shipping phases** (Phase 31 + 32 + 33)
    — first such triple in project history.
21. **First "Q76 → Q79" Q-promotion-chain** in project history (parent-
    ADR-resolves-Q-then-deferral-surfaces-as-Q pattern; mirrors
    Phase-15 Q63 → Q66/Q67/Q68 but tighter at single promotion).
22. **Three consecutive phases without new B category** (Phase 31 +
    32 + 33) — first such triple-phase since Phase 9.

### Delta summary (Phase 32 → Phase 33)

- **Commits**: 5 (4 + this gate).
- **ADRs**: 22 → **23** (+1; ADR-0023).
- **Migrations**: 8 → **9** (+1; `0008_subscriber_user_id.sql`; third
  in 4 phases; third on `subscriber` table).
- **DB tables**: 7 UNCHANGED (column extension on existing `subscriber`
  table).
- **Runtime dependencies**: 0 added Phase 33 (server-only stack
  reused).
- **Env vars**: 12 UNCHANGED.
- **i18n keys per locale**: 153 UNCHANGED (no new keys Phase 33).
- **Tests**: 712/63 → **718/63** (+6 / +0 file; all from Unit 33.2
  `decideSubscriberUserId` tests in existing
  `lib/subscribers/index.test.ts`).
- **Dynamic page+API routes**: 13 UNCHANGED (Phase 33 modifies
  existing `/api/v1/subscribe` route).
- **Source files in `lib/subscribers/`**: 1 source UNCHANGED + 0 test
  files added (extension within existing source: +1 export
  `decideSubscriberUserId` + extended signature on
  `createOrRefreshPendingSubscription`; extension within existing test
  file: +6 tests).
- **First Load JS shared chunk**: **103 kB UNCHANGED** end-to-end
  through every Phase 9-33 unit (**44th consecutive unit at 103 kB**).
- **Middleware bundle**: 160 kB UNCHANGED since Phase 12.
- **Open questions**: 65 → **66 total** (+1 net; Q79 promoted;
  Q76 status flip from `open` to `resolved` — total tally
  shifts: 27 resolved + 4 lean + 35 open = 66).
- **Class B categories**: 14 UNCHANGED (third consecutive phase
  without new B category Phase 31 + 32 + 33; first such triple since
  Phase 9).
- **Class B items**: ~3 UNCHANGED (Phase 33 does NOT touch B.15
  catalog; closes Q76 architectural follow-on separately).
- **Patience-signal closures**: +1 (Q76 architectural carryover
  resolved at 2-phase Q-carryover via ADR-0023; closes ADR-0021 D-C
  Phase-31+ anticipation explicitly).

### Surviving follow-ons (Class B; ~3 items across 14 categories)

Same set as Phase 32 close — Phase 33 did not address B.15 catalog
directly:

| Category | Item | Notes |
|---|---|---|
| B.15 (Phase-30 subscriber-list) | 5. Subscribe-endpoint rate limiting | Phase 34+ if spam |

Plus 8 carried unchanged from Phase 32 close (B.1 + B.2 + B.3 + B.4 +
B.5 + B.8 + B.9 + B.10 + B.11 + B.13 + B.14 — same set carrying since
Phase 30 close).

**B.15 close-rate summary**: still 4 of 5 addressed by Phase 32 close
(80%); item 5 carried Phase 34+.

### Phase-34+ Q-candidates flagged (13 total)

7 carried from Phase 27 (Q60 / Q61 / Q62 / Q64 / Q65 / Q68 expansion /
Q72) + 2 carried from Phase 28 (Q73 + Q74) + 1 carried from Phase 30
(Q75; Q76 resolved Phase 33) + 2 carried from Phase 31 (Q77 + Q78) +
**1 NEW Phase 33 (Q79)**. **Slot rotation**: Q76 closed, Q79 opened —
total Phase-34+ candidate count UNCHANGED at 13.

### DB-migration trigger re-eval at Phase 34 kickoff

- §5.7 trigger (a): FIRED Unit 9.6 carried.
- §5.7 trigger (b): **FIRED Phase 33** (`subscriber.userId` FK column);
  cold again at Phase 34 entry unless next thread requires migration.
  Migration cadence post-Phase-33: Phase 30 + 31 + 33 = 3 migrations
  in 4 phases (Phase 32 gap); first "migration-cluster-resume-after-
  1-phase-gap" shape established.

### Risk surface at Phase 33 close

**6 operational gates pending** UNCHANGED (Phase 33 added 0
operational gates; reuses existing Q54 + Q55 + Q69 + Q73 + Q75 + Q77
infrastructure):
- Q54 GitHub OAuth registration.
- Q55 Turso production DB.
- Q69 Vercel Blob token.
- Q73 Google OAuth registration.
- Q75 Resend account + domain DKIM/SPF/DMARC.
- Q77 Vercel Cron `CRON_SECRET` + first-week monitoring.

**3 architectural Phase-34+ candidates pending** (Q76 resolved Phase
33; Q79 newly promoted; total stays at 3):
- Q74 non-GitHub users as curators.
- Q78 digest-send analytics + observability.
- **Q79 NEW** "manage my subscriptions" UX page.

**Cross-user re-subscribe collision** documented as known limitation
in ADR-0023 D-F (conservative-preserve posture; Phase 34+ may revise
if user-merge signal emerges).

**`safeAuth()` 4th in-place copy** Phase 33 — Phase-7 D-8 extraction-
threshold pattern preserved (5+ copies = extraction); current count =
4 after Phase 33; extraction = Phase 34+ candidate if 5th copy surfaces.

All other Phase-30-31-32 risk-surface items carry forward unchanged.

### Boundary statement (Phase 33 anti-scope)

NOT a new ADR-0024 (slot stays open; carries to Phase 34+); NOT
production cron operation (Q77 operational; carries); NOT digest-send
analytics / Resend webhook integration (Q78 architectural; Phase 34+);
NOT "manage my subscriptions" UX page (Q79 Phase 34+); NOT curator
analytics dashboard (Phase 34+ per ADR-0023 D-H); NOT per-problem
subscriptions extension (Phase 34+ Q76-sibling-expansion per ADR-0023
D-H); NOT subscription-preference editing UX (Phase 34+); NOT
cross-domain aggregated weekly summary (Phase 34+ per ADR-0022 D-H);
NOT subscriber-list export tooling (Phase 34+); NOT user-merge UI
(Phase 34+; revisits ADR-0023 D-F); NOT account-deletion flow (Phase
34+; uses the ADR-0023 D-D cascade but the delete path itself is not
in Phase 33 scope); NOT per-user-account-subscription audit trail
(Phase 34+); NOT soft-delete vs hard-delete revisit (Phase 34+); NOT
content moderation Q68 expansion (Phase 34+ if user-base signals);
NOT Q64 per-user privacy opt-out (Phase 34+); NOT account-merge UI
ADR-0020 D-E (Phase 34+); NOT third+ provider expansion ADR-0020 D-B
amendment (Phase 34+); NOT markdown evolution ADR (Phase 34+); NOT v2
methodology authoring (curator-track); NOT account-deletion blob
cleanup (Phase 34+); NOT SiteHeader avatar pill (Phase 34+); NOT
subscribe-endpoint rate limiting B.15 item 5 (Phase 34+ if spam); NOT
bounce/complaint webhook handling (Phase 34+ Q78 sub-thread); NOT
row-level lock for concurrent-cron race (Phase 34+ documented as known
limitation in ADR-0022); NOT cleanup-cron schedule promotion (daily
vs weekly deferred Phase 34+ if accumulation signal); NOT `safeAuth()`
extraction (Phase 34+ if 5th in-place copy surfaces per Phase-7 D-8
threshold).

**This phase is the authenticated-user-linkage realization** for the
subscriber-list-email feature — the FK column ships, the auto-populate
logic ships, the cascade-on-user-delete constraint ships, the
`decideSubscriberUserId` pure helper realizing D-E/D-F/D-G semantics
ships, the test surface ships. **Subscriber-list-email arc completes
Phase 30 → 31 → 32 → 33 = first 4-phase complete-feature pair in
project history**. UX surfaces ("manage my subscriptions" Q79 +
curator analytics dashboard) await Phase 34+.

### Parallel-curator awareness

Phase 33 shipped as a **single-session phase** — primary shipped all
5 units 33.0 → 33.4 in one coherent session. **Seventeen consecutive
single-session phases** (Phase 17-33). Phase 16 remains the lone
parallel-session phase. Working tree clean at each unit boundary;
no parallel-session activity observed during Phase 33 work.

### Phase 33 vs prior new-ADR phases

Phase 33 = **fifth new-ADR phase since Phase 19** (Phase 19 + 28 +
30 + 31 + 33). **First 5-unit new-ADR phase** (prior 4 = all 6-unit).
Smaller code scope justifies the unit-count reduction:

| Phase | ADR | Units | Code scope |
|---|---|---|---|
| 19 | ADR-0019 image-transcoding | 6 | extends existing `lib/storage/putAvatar` |
| 28 | ADR-0020 multi-provider OAuth | 6 | extends existing `lib/auth/` |
| 30 | ADR-0021 subscriber-list email foundation | 6 | +2 new `lib/` modules + 1 new DB table + 1 migration + 3 new API routes + 1 new UI component + 4 new test files + 2 new env vars + new i18n namespace + 1 new runtime dep |
| 31 | ADR-0022 weekly digest scheduler | 6 | +1 new `lib/` module + 1 new template + 1 selector extension + 1 new top-level config file + 1 new API route + 1 column extension migration + 1 new env var |
| **33** | **ADR-0023 per-user-account subscriptions** | **5** | **+1 column extension migration + extended existing helper + extended existing route + 6 new tests in existing test file** |

**Phase 33 = lightest new-ADR phase in project history by code-scope
metrics**; the 5-unit shape reflects this. Confirms 5-unit shape as
the new-ADR-with-existing-module-extension-only rhythm; future ADRs
that purely extend existing modules without introducing new ones may
follow this rhythm.

**Subscriber-list-email arc summary** (Phase 30 + 31 + 32 + 33 = 4
phases; 21 units total):
- Phase 30 = 6 units (foundation).
- Phase 31 = 6 units (scheduler).
- Phase 32 = 4 units (cleanup; no new ADR).
- Phase 33 = 5 units (authenticated-linkage).
- **Total: 21 units across 4 phases delivering: ADR-0021 + ADR-0022
  + ADR-0023 + `lib/email/` + `lib/subscribers/` + `lib/cron/` + 1 DB
  table + 3 migrations + 5 API routes + 1 UI component + 11 i18n keys
  + 1 new runtime dep + 3 new env vars + 5 new operational/architectural
  Qs surfaced (Q75 + Q76 + Q77 + Q78 + Q79; Q76 resolved Phase 33).**
- **First 4-phase complete-feature arc** in project history; future
  multi-phase feature arcs have this 21-unit / 3-ADR / 4-phase template
  as a reference shape.

### Phase 34 entry conditions

Per §12 cardinal rule, **explicit human sign-off required**. **Twenty-
ninth "Continue" override opportunity** in Unit 34.0 prep when that
unit is authored. Thread options for Phase 34+ (overridable per §12):

- **A. Q68 expansion content moderation** (~3-5 units; ADR-0024
  candidate; 12+ phase carryover — **strongest non-just-surfaced
  patience signal**; natural next-step after the subscriber-list-email
  4-phase arc completes).
- **B. Q78 architectural digest-send analytics + observability**
  (~3-5 units; ADR-0024 candidate; **still waits for first-week
  production cron signal from Q77** before Profile A vs B vs C
  decision).
- **C. Q79 "manage my subscriptions" UX page** (~1-2 units Profile A
  read-only or ~3-4 units Profile B full-edit; ADR-0024 candidate or
  UX-track without ADR; just surfaced Phase 33 = 0-phase carryover).
- **D. Markdown evolution ADR** (Q72 + Phase-29 B.14 wikilink couple;
  ~3-5 units).
- **G. Q64 per-user privacy opt-out** (~3 units; **15+ phase
  carryover — longest open architectural question**).
- **J-other. Account-merge UI** (ADR-0020 D-E; ~2-3 units; 6-phase
  age).
- **K-other. Third+ provider expansion** (ADR-0020 D-B amendment).
- **L. Account-deletion blob cleanup** (Phase-16 carryover) — now
  also enables the ADR-0023 D-D cascade-on-user-delete to actually
  fire (Phase 33 ships the constraint; Phase 34+ delete flow would
  exercise it).
- **M. SiteHeader avatar pill** (Phase-14 carryover).
- **N. v2 methodology authoring** (curator-track).
- **B.15 item 5. Subscribe-endpoint rate limiting** (~1-2 units;
  Phase 34+ if spam observed).

**Q68 expansion** remains the **strongest non-just-surfaced patience
signal** at 12+ phases — the natural next-step thread for Phase 34+
after the subscriber-list-email 4-phase arc completes. **Q79** is
the tightest 0-phase-carryover (just surfaced) but Profile A is
small (1-2 units) and could be a tight follow-on phase. **Q78** still
waits for Q77 operational signal. **Q64** remains longest-carrying
open architectural question at 15+ phases.

### Surfaces ready for Phase 34+ consumption

Phase 30 + 31 + 32 + 33 together establish a complete subscriber-list-
email feature surface ready for Phase 34+:

- **`subscriber` table** (12 columns including `userId` FK Phase 33) —
  extendable with Phase-34+ candidates: `bounceCount` (Q78 Profile A);
  `preferredSendDay` (ADR-0022 D-H); `lastDigestOpenedAt` (analytics);
  per-problem subscription column (Q76-sibling-expansion).
- **`lib/subscribers/`** (3 exports + extended helper; pure/DB split
  pattern) — extendable for "manage my subscriptions" (Q79) +
  curator analytics queries + bulk-import tooling.
- **`lib/cron/`** (2 source files Phase 32) — extendable for retry
  queue (Q78 Profile C) + bounce-handling cron (Q78 Profile A).
- **`app/api/v1/subscribe/route.ts`** (Phase-33 `safeAuth()` integrated)
  — extendable for `safeAuth()` 5th in-place copy threshold (extraction-
  trigger).
- **`app/api/v1/cron/`** (2 routes Phase 32) — extendable for 3rd+
  cron jobs reusing `checkCronAuth`.
- **`vercel.json` `crons` array** (2 entries Phase 32) — extends
  additively for future scheduled jobs.
- **ADR-0023 D-H deferral catalog** (~9 items) — Phase-34+ candidate
  menu for the subscriber-list-email feature evolution.
- **Q79 (just promoted)** — Phase-34+ candidate UX surface; Profile
  A/B/C decision waits for signed-in user feedback.

### Phase 33 → Phase 34 sign-off requirement

Per `MASTER_PROMPT.md` §12 cardinal rule: **explicit human sign-off
required** before Phase 34 work begins. **29th "Continue" override
opportunity** at this boundary.

> Phase 33 is COMPLETE. All gates green. **Subscriber-list-email
> 4-phase arc completes Phase 30 → 31 → 32 → 33** = first complete-
> feature 4-phase pair in project history. Surface this acceptance
> gate + boundary statement to the curator and **WAIT for explicit
> "Continue" override** before authoring Unit 34.0 (Phase 34 prep) —
> OR — for the curator to redirect to a different thread / pause the
> project / close-Phase-N-without-Phase-34-kickoff / signal that the
> subscriber-list-email feature arc completion is a natural pause
> point for human review of the entire 21-unit feature delivery
> before continuing.
