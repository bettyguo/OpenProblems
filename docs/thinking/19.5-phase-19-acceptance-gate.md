# 19.5 — Phase 19 acceptance gate (Q70 closure; 19 ADRs total; first server-side image processing + first explicit privacy-by-default)

Sixth and final Phase-19 unit; docs-only. Closes Phase 19
(**tenth NON-§13 phase**; Q70 EXIF stripping resolved). Mirrors
Phase-12 / 13 / 14 / 15 / 16 / 17 / 18 acceptance-gate patterns.

> **§12 sign-off**: Phase 20 sign-off **pending** at end of unit.
> "Continue" override path available (**fifteenth invocation**
> opportunity).

## THINK

### Phase 19 ledger

**6 units shipped** (19.0 prep → 19.5 gate). **0 deferrals**;
**0 scope drift**. **Eighth consecutive well-scoped phase**.
Smallest phase since Phase 13 (which shipped 7 units).

| Unit | Title | Type | Status |
|---|---|---|---|
| 19.0 | Phase 19 prep | docs | shipped `8de6ed3` |
| 19.1 | ADR-0019 image-transcoding pipeline | docs | shipped `24e54dc` |
| 19.2 | `lib/storage/putAvatar` extension (`sharp` integration) | code | shipped `9ac2a23` |
| 19.3 | Phase-19 hygiene status pass | docs | shipped `727b61e` |
| 19.4 | OPEN_QUESTIONS hygiene + ADR review (Q70 resolved) | docs | shipped `466e9d3` |
| 19.5 | **Phase 19 acceptance gate** (this unit) | gate | this commit |

### Q70 closure summary

Q70 (EXIF stripping on uploaded images) promoted from `open` to
`resolved` in Unit 19.4 — **first STATUS-CHANGE-ONLY promotion**
since Phase-12 Q57 + Phase-13 Q58 + Phase-15 Q63 + Phase-16 Q67 +
Phase-17 Q66 + Phase-18 Q71 all used same-unit add-and-resolve
pattern. Q70 was Phase-16-added carryover with 3-phase wait
before resolution.

ADR-0019 pins six concrete contracts (D-A library `sharp`; D-B
EXIF allow-list strip-all-default; D-C pipeline placement
`lib/storage/putAvatar` server-side; D-D auto-rotation
preservation; D-E backwards-compat no-retroactive Phase 19; D-F
Phase-20+ inheritance contract for Q68 expansion / cropping /
resizing / format conversion).

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`466e9d3`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **564/564 across 54 vitest files**. +2 / 0
  from Phase 18 close (562/54); both new tests from Unit 19.2
  (sharp integration shape verification).
- **`pnpm build`** → ~659 prerendered pages + 7 dynamic page+API
  routes UNCHANGED. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-19 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` = 117 kB UNCHANGED. `/u/{handle}` = 108 kB
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32 baseline
  since Phase 2; unchanged through every Phase 3-17 unit).

### Architectural firsts shipped in Phase 19

1. **First server-side image processing surface** in project
   history.
2. **First explicit privacy-by-default surface** (Phase 16
   imageOverride shipped privacy-by-omission; Phase 19 closes gap).
3. **Third consecutive 0-migration phase** (Phase 17 + 18 + 19;
   6 of 10 phases since DB landed).
4. **ADR-0019 claims slot after 5-phase ADR cycle without
   multi-provider-OAuth claim**.
5. **First inverted-allow-list pattern** (vs Phase-17/18 explicit-
   allow-list `bioSchema`/`reviewNotesSchema`).
6. **First single-code-unit-per-phase realization of multiple
   D-clauses** — Phase 17 took 2; Phase 18 took 3; Phase 19
   compresses D-A through D-D into one code unit (19.2).
7. **First "explicit-dep promotion" of transitive dep** — `sharp`
   promoted from transitive (`next/image`) to direct runtime dep.
8. **First STATUS-CHANGE-ONLY Q-promotion** (Q70 via Unit 19.4;
   vs 6 prior phases of same-unit add-and-resolve).

### Delta summary (Phase 18 → Phase 19)

| Metric | Phase 18 close (`4915406`) | Phase 19 close (HEAD `466e9d3`) | Δ |
|---|---|---|---|
| Commits | 7 | 5 + this gate | +6 |
| ADRs | 18 | **19** | **+1** (ADR-0019) |
| Migrations | 6 | **6** | **0** (third consecutive 0-migration phase) |
| Runtime deps | (Phase-18 stack) | **+1 net** (`sharp@0.34.5` direct; first explicit-dep promotion of transitive dep) | +1 |
| Tests | 562 / 54 files | **564 / 54 files** | +2 / 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` + `/u/{handle}` bundles | 117 + 108 kB | 117 + 108 kB | 0 |
| OPEN_QUESTIONS resolved | 25 | **26** | +1 (Q70) |
| OPEN_QUESTIONS open | 30 | **29** | -1 (Q70 promoted out) |
| OPEN_QUESTIONS total | 59 | **59** | **0** (mechanical Q-count unchanged for status-change-only promotion) |
| i18n keys per locale | 127 | 127 | 0 |
| Server-side image processing surfaces | 0 | **1** | +1 |
| Privacy-by-default surfaces | 0 | **1** | +1 |

### Surviving follow-ons

**Class B — ~11 items** (Phase-19 5 image-processing expansion +
8 Phase-17 markdown carryovers + 3 Phase-18 surface carryovers +
5 operational).

**Class C** — pre-existing carryovers unchanged.

### Phase-20+ Q-candidates flagged

**8 candidates carry** (9 Phase-18 minus Q70 promoted):

- Q59 (CLI emit-challenge-action) — carried Phase-12 through
  Phase-19 = **8 phases**.
- Q60 / Q61 / Q62 / Q64 / Q65.
- Q68 expansion (content moderation on uploaded images;
  ADR-0019 D-F integration point now documented).
- Q72 (`reviewNotesSchema` divergence).

### Phase 20+ entry conditions

Per §12 cardinal rule. **Fifteenth "Continue" override** opportunity
in Unit 20.0 prep. Phase 20+ thread options (all inferred-not-§13):

- **A. Q68 expansion content moderation** — ADR-0020 candidate
  for moderation API; ADR-0019 D-F inheritance contract already
  documents `.rotate()` ↔ `.toBuffer()` integration point. ~3-4
  units.
- **B. EXIF backfill script** (per ADR-0019 D-E) — Phase-19
  carryover; manual curator invocation; CLI dry-run. ~1-2 units.
- **C. Cropping UI** (per ADR-0019 D-F) — `sharp.extract()`
  inserted between `.rotate()` and `.toBuffer()` + client crop
  region selection. ~3-5 units.
- **D. Server-side resizing + WebP/AVIF format conversion** (per
  ADR-0019 D-F) — `sharp.resize().webp()` insertion. ~1-2 units
  if observation shows bandwidth/render-time concern.
- **E. Q72 markdown subset extensions** — `reviewNotesSchema`
  divergence (tables / footnotes for curator reviewNotes). ~2-3
  units.
- **F. Per-challenge detail page** (Phase-11 + 13 carryover) —
  couples to Phase-18 line-clamp UX. ~2-3 units.
- **G. Markdown rating-action `rationale`** — couples to (F).
  ~2-3 units.
- **H. Q64 per-user privacy opt-out** — ADR-0015 D-D Phase-15+
  deferral. ~3 units.
- **I. Multi-provider OAuth expansion** — Phase-9 Class B item 8
  carried **9 phases**. **ADR-0020 candidate**. ~3-4 units.
- **J. Subscriber-list email** — Phase-5 D-4 punt carried **13+
  phases**. ~6 units.
- **K. Q59 CLI emit-challenge-action** — carried **8 phases**.
  Smallest reasonable scope.

**DB-migration trigger re-eval at Phase 20 kickoff**: trigger (a)
FIRED Unit 9.6; trigger (b) cold. Phase 20 migration count
depends on chosen thread.

### Risk surface at HEAD `466e9d3`

1. **Three operational gates** (Q54 / Q55 / Q69) pending curator.
2. **Orphan blob accumulation tolerated** until backfill +
   cleanup scripts land.
3. **No content moderation on uploaded images** (Q68 expansion
   deferred).
4. **No backwards-compat EXIF strip** for Phase 16-18 avatars
   (Phase-20+ backfill script candidate).
5. **No live-preview in edit form** (Phase 17 + 18 deferral).
6. **Sharp cold-start cost** (~100 ms per first invocation;
   amortized subsequent calls).

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token (Q69), NOT FR content backfill (Q51
curator-track), NOT moderation / cropping / resizing / WebP /
EXIF backfill (Phase 20+), NOT destructive cleanup (Class C
still gated). This unit is the **closure**, not the **unblock**.

### Parallel-curator awareness

Phase 19 shipped as a **single-session phase** — primary shipped
all 6 units 19.0 → 19.5 in one coherent session. Matches Phase
17 + 18 single-session pattern (vs Phase 16's 8-of-9 parallel-
session pattern). **Three consecutive single-session phases**
demonstrates that well-scoped phases converge regardless of
session count.

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline unchanged).
- File added: `docs/thinking/19.5-phase-19-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 19.5 entry.

### CHANGELOG entry shape

Unit 19.5 entry: declares Phase 19 closed (**tenth NON-§13
phase**; 6 units; 0 deferrals; 0 scope drift; eighth consecutive
well-scoped phase); cites Q70 closure (first STATUS-CHANGE-ONLY
promotion since 6 phases of same-unit add-and-resolve); 8
Phase-19 architectural firsts; Phase 18 → 19 delta table; Class
B follow-ons (~11 items in 4 categories); 8 Phase-20+
Q-candidates; ADR-0020 candidate slot open after 6-phase cycle
without multi-provider-OAuth claim; Phase 20+ thread options
(strongest patience signals: Phase-9 Class B item 8 = 9 phases
multi-provider OAuth, Phase-5 D-4 punt = 13+ phases subscriber-
list email); fifteenth "Continue" override opportunity per §12;
three consecutive single-session phases (Phase 17 + 18 + 19).
