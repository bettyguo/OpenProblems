# 18.6 — Phase 18 acceptance gate (Q71 closure; 18 ADRs unchanged; first phase since Phase 3 to close a Q without new ADR)

Seventh and final Phase-18 unit; docs-only. Closes Phase 18
(**ninth NON-§13 phase**; Q71 multi-surface markdown render via
ADR-0018 D-G inheritance). Mirrors Phase-12 / 13 / 14 / 15 / 16 /
17 acceptance-gate patterns.

> **§12 sign-off**: Phase 19 sign-off **pending** at the end of
> this unit. The "Continue" override path is available
> (**fourteenth invocation** opportunity).

## THINK

### Phase 18 ledger

**7 units shipped** (18.0 prep → 18.6 gate) across HEAD `ad951e4`
(Phase 17 close) → HEAD `85dee1f` (Phase 18 close pre-this-unit).

| Unit | Title | Type | Status |
|---|---|---|---|
| 18.0 | Phase 18 prep | docs | shipped `e764e21` |
| 18.1 | `lib/markdown/` extension (`renderReviewNotesMarkdown` + `reviewNotesSchema` + tests) | code | shipped `a66c5ff` |
| 18.2 | `/curator/challenges/[id]` reviewNotes markdown (FULL render) | code | shipped `b4d6842` |
| 18.3 | `/profile` own-challenges listing reviewNotes markdown + CSS `line-clamp-3` | code | shipped `bf3acb4` |
| 18.4 | Phase-18 hygiene status pass | docs | shipped `29e98b5` |
| 18.5 | OPEN_QUESTIONS hygiene + ADR review (Q71 resolved) | docs | shipped `85dee1f` |
| 18.6 | **Phase 18 acceptance gate** (this unit) | gate | this commit |

**0 deferrals**; **0 scope drift**; matches Phase-12 through
Phase-17 well-scoped pattern. **Seventh consecutive well-scoped
phase** shipping clean.

### Q71 closure summary

Q71 (multi-surface markdown render via ADR-0018 D-G inheritance)
promoted + resolved in Unit 18.5 simultaneously — **sixth
invocation** of the "added + resolved in same unit" pattern.
Closes the **seven-reference convergence**:

- Phase-15 Class B B.2 item 5 (markdown-rendered curator review
  notes carryover).
- Phase-15 Unit 15.6 + 15.7 + 15.8.
- Phase-17 ADR-0018 D-G (inheritance contract documented).
- Phase-17 Unit 17.5 B.2 (surface expansion item carried).
- Phase-18 Unit 18.0 (anticipation).

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`85dee1f`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **562/562 across 54 vitest files**. +10 / 0
  from Phase 17 close (552/54); all from Unit 18.1's sibling
  helper + schema-parity tests.
- **`pnpm build`** → ~659 prerendered pages + 7 dynamic page+API
  routes UNCHANGED. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-18 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` = 117 kB UNCHANGED. `/u/{handle}` = 108 kB UNCHANGED.
- **`pnpm audit-content`** → **0 errors / 6 warnings** (Q32
  baseline since Phase 2; unchanged through every Phase 3-17
  unit).

### Architectural firsts shipped in Phase 18

1. **First ADR-0018 D-G inheritance exercise** — sibling helper
   + sibling schema pattern realized at second exercise of the
   contract.
2. **First reviewNotes markdown surface** — Phase-12 plain-text
   rendering upgraded across both consumer surfaces.
3. **Second consecutive 0-migration phase** (Phase 17 + 18; 5 of
   9 phases since DB landed).
4. **First 4-surface markdown render consistency** — single 14-
   class prose-styling vocabulary across all 4 markdown surfaces.
5. **First CSS `line-clamp` visual truncation** — Tailwind v4
   built-in utility; replaces Phase-12's `truncateRationale()`
   source-truncation pattern for markdown-incompatible field.
6. **First XSS-audit schema-parity pattern** — 3 parity tests
   assert `reviewNotesSchema` ≡ `bioSchema` Phase-18; Phase-19+
   divergence requires explicit parity-test removal.
7. **First "rename refactor" code unit** — `bioProcessor` rename
   per Unit 18.0 D-8; zero behavior change.
8. **First phase since Phase 3 to close a Q-candidate without a
   new ADR** — Phase 3 closed Q18 via ADR-0006; Phase 4-17 each
   closed Q via new ADR; Phase 18 closes Q71 via ADR-0018 D-G
   inheritance contract realization (no architectural decision
   surface remaining open).

### Delta summary (Phase 17 → Phase 18)

| Metric | Phase 17 close (`ad951e4`) | Phase 18 close (HEAD `85dee1f`) | Δ |
|---|---|---|---|
| Commits | 8 | 6 + this gate | +7 |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | 18 | **18** | **0** (first phase since Phase 3 to close a Q without new ADR) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (second consecutive 0-migration phase) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-17 stack) | (Phase-17 stack) | **0** |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 552 / 54 files | **562 / 54 files** | **+10 / 0** |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS open count | 30 | 30 | 0 |
| OPEN_QUESTIONS resolved count | 24 | **25** | **+1** (Q71) |
| OPEN_QUESTIONS decided-as-lean count | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 58 | **59** | **+1** |
| i18n keys per locale | 127 | 127 | **0** |
| LHCI URLs | 19 | 19 | 0 |
| `lib/markdown/` files | 3 | 3 | 0 (extended; no file count change) |
| XSS-audit surfaces | 1 | **2** | **+1** (`renderReviewNotesMarkdown`) |
| `dangerouslySetInnerHTML` surfaces | 2 | **4** | **+2** (`/curator/challenges/[id]` + `/profile` own-challenges) |
| Markdown render-consistency surfaces | 2 | **4** | **+2** |
| Schema-parity test surfaces | 0 | **1** | **+1** (3 parity tests) |

### Surviving follow-ons (from Unit 18.4)

**Class B — ~12 items** in 3 categories:

- **B.1 Markdown subset expansion (9)**: 8 Phase-17 carryovers +
  Phase-18 newly flagged Q72 (`reviewNotesSchema` divergence).
- **B.2 Surface expansion (3)**: markdown rationale; methodology
  unification; per-challenge detail page.
- **B.3 Operational + infrastructure (5)**: Q69 / Q54 / Q55 +
  CI AUTH_SECRET + db:migrate doc + LHCI + middleware (unchanged
  from Phase 17).

**Class C** — pre-existing carryovers unchanged from Phase 17.

### Phase-18+ Q-candidates flagged but NOT promoted

**9 candidates carry** (8 from Phase 17 + 1 Phase-18 new):

- Q59 (CLI emit-challenge-action) — carried Phase-12 through
  Phase-18 = **7 phases**.
- Q60 / Q61 / Q62 / Q64 / Q65 / Q68-expansion / Q70 — Phase 17
  carryovers.
- **Q72** (`reviewNotesSchema` divergence from `bioSchema` — markdown
  subset extensions for curator reviewNotes; tables / footnotes /
  extended subset; 4000-char field justifies tabular COI
  comparisons + structured editorial reasoning) — Phase-18 newly
  flagged.

### Phase 19+ entry conditions

Per §12 cardinal rule, the next phase needs explicit human
sign-off (or "Continue" override per the thirteen-phase
precedent; fourteenth invocation in Unit 19.0 prep). Phase 19+
thread options ALL inferred-not-§13 (§13 ledger remains CLOSED
since Phase 9):

- **A. Q70 promotion — EXIF stripping** (~2-3 units; ADR-0019
  candidate for image-transcoding pipeline). Strongest privacy
  signal carried from Phase 16.
- **B. Q68 expansion promotion — content moderation** (~3-4
  units; ADR-0019+ candidate for moderation API).
- **C. Q72 promotion — markdown subset extensions** (~2-3 units;
  schema-extension ADR; tables / footnotes for curator reviewNotes
  or bio).
- **D. Per-challenge detail page** (Phase-11 + 13 carryover;
  couples to Phase-18 line-clamp UX — user clicks through for
  full markdown rendering; ~2-3 units; may not need new ADR).
- **E. Markdown rating-action `rationale`** (Phase-11 plain-text
  field; couples to existing `truncateRationale()` helper;
  promotion would need analogous CSS line-clamp + markdown render
  swap; couples to per-challenge detail page; ~2-3 units).
- **F. Q64 promotion — per-user privacy opt-out** (~3 units;
  ADR-0015 D-D Phase-15+ deferral).
- **G. Q65 promotion — per-curator activity feed** (~4-5 units).
- **H. Q59 promotion — CLI emit-challenge-action** (~2-3 units;
  carried 7 phases).
- **I. Multi-provider OAuth expansion** (~3-4 units; ADR-0019+
  candidate). Phase-9 Class B item 8 carried 9 phases —
  **strongest architectural "patience signal"**.
- **J. Subscriber-list email** (~6 units; Phase-5 D-4 punt
  carried 13+ phases — **strongest overall "patience signal"**).
- **K. SiteHeader avatar pill / avatar-dropdown menu**.
- **L. Full §8.6 24-mo collaborator COI check** + **HTML shell
  migration STILL ON HOLD** + **monetization Phase 19+/20+**.

**DB-migration trigger re-eval at Phase 19 kickoff** (mandatory):
trigger (a) FIRED Unit 9.6; trigger (b) cold. Phase 19 migration
count depends on chosen thread.

### Risk surface at HEAD `85dee1f`

1. **Three operational gates** (Q54 / Q55 / Q69) pending curator
   action.
2. **Orphan blob accumulation tolerated** until Phase-16 Class B
   cleanup script lands.
3. **Middleware bundle 160 kB** unchanged.
4. **`/profile` bundle 117 kB** unchanged from Phase 16.
5. **No live-preview in edit form** — Phase-17 + 18 ship server-
   side render-after-save only.
6. **`@tailwindcss/typography` not used** — arbitrary-variant
   utility classes used instead; Phase-19+ may swap if richer
   prose surfaces demand.
7. **No content moderation on bio text or reviewNotes** (Q68 +
   Q68 expansion deferred).
8. **No EXIF stripping** (Q70 deferred).

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token (Q69), NOT FR content backfill (Q51
curator-track), NOT EXIF / moderation / cropping (Phase 19+),
NOT markdown subset extensions (Phase 19+), NOT destructive
cleanup (Class C still gated). This unit is the **closure**,
not the **unblock**.

### Parallel-curator awareness

This unit is docs-only — no collision risk. Phase 18 shipped
as a **single-session phase** — primary shipped all 7 units 18.0
→ 18.6 in one coherent session (matching Phase 17's single-
session pattern; vs Phase 16's 8-of-9 parallel-session pattern).

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline unchanged).
- File added: `docs/thinking/18.6-phase-18-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 18.6 entry.

### CHANGELOG entry shape

Unit 18.6 entry: declares Phase 18 closed (**ninth NON-§13
phase**; 7 units; 0 deferrals; 0 scope drift; seventh consecutive
well-scoped phase); cites Q71 closure + ADR-0018 D-G inheritance
realization (first phase since Phase 3 to close Q without new
ADR); consolidates 8 Phase-18 architectural firsts; presents
Phase 17 → 18 delta table; enumerates Class B + Class C
follow-ons; lists 12 Phase-19+ Q-candidates + thread options
(strongest patience signals: Phase-9 Class B item 8 = 9 phases
multi-provider OAuth, Phase-5 D-4 punt = 13+ phases subscriber-
list email); surfaces Phase-19 sign-off requirement (fourteenth
"Continue" override opportunity per §12 cardinal rule); notes
Phase 18 single-session shipping pattern continuation.
