# 25.4 — Phase 25 acceptance gate (4 operational follow-ons closed across Phase 22+23+24; 19 ADRs unchanged; sixth consecutive operational-script-keystone phase; largest Class-B catch-up to date)

Fifth and final Phase-25 unit; docs-only. Closes Phase 25
(**sixteenth NON-§13 phase**; 4 operational follow-ons bundled
across Phase 22+23+24 surfaces). Mirrors Phase-12 through
Phase-24 acceptance-gate patterns.

> **§12 sign-off**: Phase 26 sign-off **pending** at end of unit.
> "Continue" override path available (**twenty-first invocation**
> opportunity).

## THINK

### Phase 25 ledger

**5 units shipped** (25.0 prep → 25.4 gate). **0 deferrals**;
**0 scope drift**. **Fourteenth consecutive well-scoped phase**.
**Matches Phase 20-24 5-unit shape** — six consecutive 5-unit
operational-script-keystone phases.

| Unit | Title | Type | Status |
|---|---|---|---|
| 25.0 | Phase 25 prep | docs | shipped `081bcfd` |
| 25.1 | 4 operational follow-ons bundled (+7 tests) | code | shipped `37e5740` |
| 25.2 | Phase-25 hygiene status pass | docs | shipped `1a1bb59` |
| 25.3 | OPEN_QUESTIONS hygiene + ADR review (0 edits) | docs | shipped `6780803` |
| 25.4 | **Phase 25 acceptance gate** (this unit) | gate | this commit |

### Phase-25 closure summary

**4 operational follow-ons CLOSED across 3 prior phases** in
Unit 25.1:
- **Phase-22 B.6 item 3** (methodology-version dynamic read):
  new `readMethodologyVersion({ contentRoot })` helper reads
  `content/methodology/v*.mdx` frontmatter (Phase-26+ v2
  forward-compat).
- **Phase-22 B.6 item 4** (batch-emit-multiple): new
  `--all-under-review` flag on `emit-challenge-action.ts`
  processes all `status=under_review` challenges in one
  invocation; per-row try/catch + summary.
- **Phase-23 B.7** (`--no-prior-fill`): new opt-out flag
  forces Phase-22 placeholder behavior.
- **Phase-24 B.8 item 1** (`--user-id`): new flag on
  `backfill-resize-webp.ts` scopes retroactive resize to a
  single user.

**Largest Class-B catch-up phase to date** — 4 items closed in
one code unit (Phase 22+23 closed 1+2 each; Phase 24 closed 2).
**Sixth consecutive operational-script-keystone phase** (Phase
20-25). **Pattern very entrenched at 6 phases**.

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`6780803`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **592/592 across 55 vitest files**. +7 / 0
  Δ from Phase 24 close (585/55).
- **`pnpm build`** → ~659 prerendered pages + 7 dynamic page+API
  routes UNCHANGED. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-25 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` = 117 kB UNCHANGED. `/u/{handle}` = 108 kB
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32
  baseline since Phase 2; unchanged through every Phase 3-25
  unit).
- **`pnpm lint`** clean.

### Architectural firsts shipped in Phase 25

1. **Sixth consecutive operational-script-keystone phase**
   (Phase 20-25); **pattern very entrenched at 6 phases**.
2. **Ninth consecutive 0-migration phase** (Phase 17-25; 12
   of 16 phases since DB landed are 0-migration).
3. **Ninth consecutive single-session phase** (Phase 17-25).
4. **Seventh phase since Phase 3 to close without new ADR**
   (Phase 18 + 20 + 21 + 22 + 23 + 24 + 25; **first six-phase
   no-new-ADR streak**).
5. **First phase to close follow-ons across 3 prior phases in
   one bundle** (Phase-22 B.6 items 3+4 + Phase-23 B.7 +
   Phase-24 B.8 item 1).
6. **First phase to add CLI flags across two distinct scripts
   in one code unit** (`emit-challenge-action.ts` +
   `backfill-resize-webp.ts`).
7. **First phase to extend the filesystem-read pattern to a
   new content-tree subdir** (Phase 23 read `problems/*/ratings/`;
   Phase 25 reads `methodology/v*.mdx`).
8. **First lib helper with explicit Phase-26+ forward-compat
   intent** (`readMethodologyVersion` anticipates v2
   methodology landing).
9. **Largest Class-B catch-up phase to date** (4 items closed
   in one code unit).
10. **First phase where `parseArgs` validates mutual exclusion**
    (`<challenge-id>` vs `--all-under-review`).
11. **First extract-and-share refactor in an operational-
    script-keystone phase** (`scaffoldOne()` +
    `validateChallenge()` helpers).
12. **First six-phase Q-tally freeze** (Phase 20-25; mechanical
    `Status:`-field count UNCHANGED).
13. **Second hygiene unit in the streak with zero edits**
    (Phase 21.3 first; Phase 25.3 second). Convention's three
    branches (OQ-pinned → annotate OQ; ADR-pinned → annotate
    ADR; neither → no annotation) now all exercised in the
    streak.

### Delta summary (Phase 24 → Phase 25)

| Metric | Phase 24 close (`1f0cb08`) | Phase 25 close (HEAD `6780803`) | Δ |
|---|---|---|---|
| Commits | 5 | 4 + this gate | +5 |
| ADRs | **19** | **19** | **0** (**sixth consecutive no-new-ADR phase** = first six-phase streak; **seventh phase since Phase 3** to close without new ADR) |
| Migrations | 6 | **6** | **0** (**ninth consecutive 0-migration phase**) |
| Runtime deps | (Phase-24 stack) | (Phase-24 stack unchanged) | **0** |
| Env vars | 7 | 7 | **0** |
| Tests | 585 / 55 files | **592 / 55 files** | **+7 / 0** |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` + `/u/{handle}` bundles | 117 + 108 kB | 117 + 108 kB | 0 |
| OPEN_QUESTIONS resolved | 26 | **26** | **0** (**first six-phase Q-tally freeze** — Phase 20-25) |
| OPEN_QUESTIONS open | 29 | **29** | **0** |
| OPEN_QUESTIONS total | 59 | **59** | **0** |
| i18n keys per locale | 127 | 127 | 0 |
| `lib/rating-challenges/` files | 5 (+ 4 .test.ts) | 5 (+ 4 .test.ts) | **0** (in-place edits) |
| `scripts/` files | 10 | 10 | **0** (in-place edits to 2 scripts) |
| `package.json#scripts` entries | (+1: `backfill-resize-webp`) | (unchanged) | 0 |
| YAML-scaffolding surfaces | 1 | 1 | 0 |
| Filesystem-read surfaces (in `lib/`) | 1 | **2** | **+1** (`readMethodologyVersion`) |
| Operational-script-keystone phases | 5 | **6** | **+1** (Phase 20-25) |
| Items closed (cumulative across streak) | 7 | **11** | **+4** (Phase 25 alone) |

### Surviving follow-ons

**Class B — ~6 items in 9 categories** (Phase-24 Class B
unchanged minus 4 closed Phase 25; B.6 + B.7 fully closed; B.8
1 of 2 closed):

- **B.1 Image-processing expansion (3 carried unchanged)**:
  Q68 expansion; cropping UI; AVIF format conversion.
- **B.2 Phase-17 markdown subset expansion (8 carried unchanged)**.
- **B.3 Phase-18 surface expansion (3 carried + Q72)**.
- **B.4 Phase-20 backfill operational follow-ons (3 carried)**.
- **B.5 Phase-21 cleanup operational follow-ons (2 carried)**.
- **B.6 Phase-22 scaffold operational follow-ons (0 carried;
  2 CLOSED Phase 25)**.
- **B.7 Phase-23 scaffold operational follow-ons (0 carried;
  1 CLOSED Phase 25)**.
- **B.8 Phase-24 transcode operational follow-ons (1 carried;
  1 CLOSED Phase 25)**: AVATAR_SIZE / WEBP_QUALITY tuning.
- **B.9 Operational + infrastructure (9 carried unchanged)**.

**Class C** — pre-existing carryovers unchanged.

### Phase-26+ Q-candidates flagged

**7 candidates carry** (unchanged from Phase 22-25):

- Q60 (curator authz evolution).
- Q61 (submitter anonymity).
- Q62 (rejection-rationale public visibility).
- Q64 (per-user privacy opt-out; ADR-0015 D-D).
- Q65 (per-curator activity feed; ADR-0015 D-E).
- Q68 expansion (content moderation; ADR-0019 D-F row 1).
- Q72 (`reviewNotesSchema` divergence).

### Phase 26+ entry conditions

Per §12 cardinal rule. **Twenty-first "Continue" override
opportunity** in Unit 26.0 prep. Phase 26+ thread options (all
inferred-not-§13):

- **A. Q68 expansion content moderation** — ADR-0020
  candidate; ADR-0019 D-F row 1; could be **seventh consecutive
  operational-script-keystone** if scope tightens. ~3-5 units.
- **B. Cropping UI** (per ADR-0019 D-F row 2) — ~3-5 units;
  client dep.
- **C-other. AVIF format conversion** (per ADR-0019 D-F row
  4 half) — ~2-3 units; needs ADR amendment for format-choice
  design (drop WebP universal support OR `<picture>` element +
  dual storage).
- **D. Q72 markdown subset extensions** — ~2-3 units.
- **E. Per-challenge detail page** — ~3-4 units; **first
  user-facing UX since Phase 18**.
- **F. Markdown rating-action `rationale`** — ~2-3 units;
  couples to (E).
- **G. Q64 per-user privacy opt-out** — ~3 units; breaks
  9-consecutive 0-migration streak.
- **H. Multi-provider OAuth expansion** — Phase-9 Class B item
  8 carried **15 phases**. **ADR-0020 candidate**. ~3-4 units.
  Strongest architectural patience signal at right scope.
- **I. Subscriber-list email** — Phase-5 D-4 punt carried
  **19+ phases**. ~6 units. Strongest overall patience signal.
- **K-other. Remaining operational tail** — Phase-20 B.4 (3)
  + Phase-21 B.5 (2) + Phase-24 B.8 item 2 (AVATAR_SIZE
  tuning) = ~6 small items; could be a **seventh consecutive
  operational-script-keystone** if bundled. ~2.5 units.
- **L. Account-deletion blob cleanup** — Phase-16 carryover;
  conditional on account-delete path.
- **M. SiteHeader avatar pill** — Phase-14 Class B; UI minor.
- **N. v2 methodology authoring** — operational
  (curator-track); not code-shipping. Would exercise Phase-25's
  `readMethodologyVersion` forward-compat path.

**DB-migration trigger re-eval at Phase 26 kickoff**: trigger (a)
FIRED Unit 9.6; trigger (b) cold.

### Risk surface at HEAD `6780803`

1. **Three operational gates** (Q54 / Q55 / Q69) pending
   curator.
2. **Methodology-version dynamic read fallback risk** — if
   `content/methodology/v*.mdx` frontmatter format drifts
   (e.g., YAML multi-line `version: |\n  ...`), regex fails
   silently → fallback to `"1.0.0"` constant. Mitigation:
   defensive fallback; Phase 26+ if frontmatter shape changes.
3. **Batch-emit error handling**: per-row try/catch isolates
   failures, but partial-batch scaffolding could leave drafts
   for succeeded challenges + omit drafts for failed ones —
   curator must inspect summary + manually re-run individual
   failures.
4. **No content moderation on uploaded images** (Q68 expansion
   deferred).
5. **No live-preview in edit form** (Phase 17+18 deferral).
6. **AVIF support deferred** (ADR-0019 D-F row 4 half;
   needs ADR amendment).

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token (Q69), NOT FR content backfill
(Q51 curator-track), NOT moderation / cropping / AVIF (Phase
26+), NOT live curator workflow run, NOT v2 methodology
authoring (curator-track). This unit is the **closure**, not the
**unblock**.

### Parallel-curator awareness

Phase 25 shipped as a **single-session phase** — primary
shipped all 5 units 25.0 → 25.4 in one coherent session.
**Nine consecutive single-session phases** (Phase 17-25).
Phase 16 remains the lone parallel-session phase.

### Six consecutive operational-script-keystone phases pattern

| Property | P20 | P21 | P22 | P23 | P24 | P25 |
|---|---|---|---|---|---|---|
| Units | 5 | 5 | 5 | 5 | 5 | 5 |
| Code units | 1 | 1 | 1 | 1 | 1 | 1 |
| New ADRs | 0 | 0 | 0 | 0 | 0 | 0 |
| New deps | 0 | 0 | 0 | 0 | 0 | 0 |
| New env vars | 0 | 0 | 0 | 0 | 0 | 0 |
| New vitest tests | 0 | 0 | +11 | +10 | 0 (5 in-place) | +7 |
| New test files | 0 | 0 | +1 | 0 | 0 | 0 |
| New `scripts/` files | 1 | 1 | 1 | 0 | 1 | 0 |
| New `lib/` modules | 0 | 0 | +1 | 0 | 0 | 0 |
| Dead-code removed | 0 | 0 | 0 | 0 | +1 | 0 |
| Refactor (extract helpers) | 0 | 0 | 0 | 0 | 0 | +1 |
| Items closed in phase | 1 | 1 | 1 | 2 | 2 | **4** |
| Hygiene-unit edits | OQ annot | none | ADR annot | ADR annot | ADR annot | none |

**Pattern very entrenched.** Phase 25 contributes the **largest
single-phase Class-B catch-up** (4 items) and the **second
zero-edits hygiene unit** in the streak (Phase 21 was first).
Convention's three annotation branches all exercised across
Phase 20-25.

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings.
- File added: `docs/thinking/25.4-phase-25-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 25.4 entry.

### CHANGELOG entry shape

Unit 25.4 entry: declares Phase 25 closed (**sixteenth NON-§13
phase**; 5 units; 0 deferrals; 0 scope drift; fourteenth
consecutive well-scoped phase); cites **4 operational
follow-ons closed across Phase 22+23+24 surfaces** via Unit
25.1 (Phase-22 B.6 items 3+4 + Phase-23 B.7 + Phase-24 B.8
item 1); **largest Class-B catch-up phase to date**; **sixth
consecutive operational-script-keystone phase**; **first
six-phase no-new-ADR streak**; **first six-phase Q-tally
freeze**; 13 Phase-25 architectural firsts; Phase 24 → 25
delta table (0 ADR Δ; 0 migration Δ; 0 runtime dep Δ; 0 env
var Δ; +7 tests / 0 file Δ; 0 bundle Δ; 0 `lib/` / `scripts/`
file Δ — pure in-place edits; +1 filesystem-read surface in
`lib/rating-challenges/`; +1 operational-script-keystone phase
counter); Class B follow-ons (~6 items in 9 categories; B.6 +
B.7 fully closed; B.8 1 of 2 closed); 7 Phase-26+ Q-candidates
carry unchanged; ADR-0020 candidate slot open after **12-phase
ADR cycle without multi-provider-OAuth claim** (Phase-9 Class B
item 8 carried **15 phases**); Phase 26+ thread options
(strongest patience signals: Phase-9 Class B item 8 = 15 phases
multi-provider OAuth; Phase-5 D-4 punt = 19+ phases subscriber-
list email; remaining operational tail ~2.5 units bundled for
7th-consecutive-keystone); twenty-first "Continue" override
opportunity per §12; nine consecutive single-session phases;
ninth consecutive 0-migration phase; six consecutive
operational-script-keystone phases pattern table shows Phase 25
contributes **4-items-closed-in-one-phase** (largest catch-up)
and **second zero-edits hygiene unit** as new dimensions.
