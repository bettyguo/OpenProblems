# 37.3 — Phase 37 hygiene: Q72 sub-bullet resolution + Q-status reconciliation

Phase 37 unit per `docs/thinking/37.0-phase-37-prep.md` (HEAD
`6ca6fdf` = Unit 37.2 ship). Docs-only hygiene unit:
documents Q72 sub-bullet resolution in OPEN_QUESTIONS.md;
reconciles Q-status. No source files touched; no schema
change; no test additions.

## THINK

### Problem restatement

Q72 sub-bullet resolution + Q-status reconciliation per
Phase-37-prep D-8 deferral. Mirrors Phase-36 Unit 36.3 +
Phase-35 Unit 35.3 hygiene shapes.

### Inputs

- HEAD `6ca6fdf` (Unit 37.2 ship; framework integrated; 755/67
  tests).
- `OPEN_QUESTIONS.md` — 66 top-level Qs (28 resolved + 4 lean
  + 34 open) UNCHANGED from Phase 36. Q72 candidate appears
  at line 573 as a sub-bullet under Q71 (resolved Phase 18).
- Q72 references in OPEN_QUESTIONS.md (3 mentions):
  - Line 560 (Q71 resolved status — describes Q72 candidate
    as Phase-19+ if-curator-demand future work; HISTORICAL,
    describes the Phase-18-as-shipped state).
  - Line 568 (Q71 implementation notes — schema-parity-by-
    explicit-shallow-copy decision; references "Q72 candidate"
    as the divergence path needing surface-specific schema
    authoring + parity-test removal; HISTORICAL).
  - Line 573 (Q71 future-candidate sub-bullet — ACTIVE Q72
    candidate description; this is the locus to update for
    Phase-37 realization).

### Constraints

- Q72 is **sub-bullet only** in OPEN_QUESTIONS.md (no top-level
  `## Q72` header). Same shape as Q64 (sub-bullet-absent
  resolution Phase 36). Q72 resolution is APPEND-not-EDIT to
  the line 573 sub-bullet describing the active candidate.
- 66 top-level Q-count UNCHANGED post-resolution (Q72 is not
  a top-level Q; its resolution doesn't change the count).
- Phase-37+ candidate count: was 10 going into Phase 37;
  after Q72 framework resolution, drops to 9 (fourth
  consecutive net-decrease phase: Phase 34 + 35 + 36 + 37 =
  first 4-phase net-decrease streak in project history).
- Historical Q72 references (lines 560, 568) describe Phase-
  18-as-shipped state — should NOT edit. Update only the
  active candidate sub-bullet at line 573.

### Alternatives considered

**Option 1: APPEND resolution note to line 573 (chosen)** —
adds a "**REALIZED Phase 37 Unit 37.1-2**" prefix or postscript
to the existing Q72 candidate sub-bullet describing how the
framework realizes the candidate. Historical references at
560/568 stay intact.

**Option 2: Promote Q72 to top-level `## Q72.` (rejected)** —
would require renumbering nothing (Q73 already exists) but
adds an OPEN_QUESTIONS.md top-level Q only to immediately mark
it resolved. **Why rejected**: matches the Q64 shape from
Phase 36 (sub-bullet-only resolution; never promoted to top-
level Q). Avoids Q-tally surprise (66 → 67 → 67 in a single
phase is noise).

**Option 3: Edit lines 560/568 to mark Q72 as resolved
(rejected)** — those lines describe the Phase-18-as-shipped
state; editing them to reference Phase-37 realization would
make the historical record inaccurate. **Why rejected**:
audit-trail discipline — historical Q-references stay frozen
as snapshots of when each Q-candidate was named.

### Edge cases & failure modes

1. **OPEN_QUESTIONS.md merge conflicts with parallel curators**
   — file is shared singleton per the parallel-curator
   workflow contract. Unit 37.3 modifies one sub-bullet
   inside a Q71 entry. LOW collision absent another markdown
   thread editing Q71.
2. **Q-tally surprise**: Phase-37 changelog and Phase-37 gate
   summary both reference Q-tally; ensure 28 resolved + 4
   lean + 34 open = 66 UNCHANGED. Phase-37+ candidate count
   drops 10 → 9 (Q72 resolved as framework).
3. **Cross-reference rot**: ADR-0018 D-G APPEND (Unit 37.1)
   references Q72 candidate as a sub-bullet; OPEN_QUESTIONS.md
   line 573 (updated this unit) must reference back to ADR-0018
   D-G APPEND for symmetry.

### Open questions

None new this unit.

### Decision

Edit OPEN_QUESTIONS.md line 573 sub-bullet only:

```markdown
- **Q72 candidate (FRAMEWORK REALIZED Phase 37 Unit 37.1-2)**
  — Phase-37 ships the `MarkdownExtensionRegistry` framework
  in `lib/markdown/extensions/` (5 files; Phase-35
  `lib/moderation/` shape verbatim) integrated into all 4
  markdown helpers per ADR-0018 D-G APPEND. The framework
  REPLACES the "needs surface-specific schema authoring +
  parity-test removal + audit comment" mechanism described
  here: per-surface schema overrides ship as
  `MarkdownExtensionSet.schemaOverrides` diffs via the
  registry, with per-surface `remarkPlugins` / `rehypePlugins`
  for parse-time + post-sanitize extensions. The `reviewNotesSchema`
  divergence path now requires only (a) authoring a new
  registry implementation with non-empty `schemaOverrides` for
  the `"reviewNotes"` surface and (b) wiring the new registry
  into `getExtensionRegistry()` via an env-var dispatch arm
  — no `lib/markdown/index.ts` edit needed. Default
  `DefaultExtensionRegistry` keeps Phase-18 parity. Phase-19+
  → Phase-37 = 19-phase carryover from Q72 surfacing to
  framework realization (longest cumulative Q72-family
  patience signal in project history). Phase-37+ concrete
  extensions (tables per-reviewNotes; footnotes per-rationale;
  wikilinks per-bio + per-actionRationale; etc.) deferred per
  ADR-0018 D-G APPEND F-list (each is an independent 1-2-unit
  commit).
```

Leave lines 560 + 568 untouched (historical Phase-18 record).

CHANGELOG entry under Phase 37 documenting the hygiene unit
+ Q72 sub-bullet resolution + Phase-37+ candidate count
update (10 → 9; fourth consecutive net-decrease).

### Smoke gates (this unit)

Docs-only — no source files modified.

- File edited: `OPEN_QUESTIONS.md` (one sub-bullet).
- Files added: `docs/thinking/37.3-phase-37-hygiene.md` +
  CHANGELOG entry.
- `audit-content`: 0 errors / 6 warnings (Q32 baseline
  UNCHANGED).
- Typecheck / test / build untouched (no source change).

### Parallel-curator awareness

`OPEN_QUESTIONS.md` is the shared-singleton file per
`docs/CURATION_PROMPT.md`. Unit 37.3 edits ONE sub-bullet
inside the Q71 entry; LOW collision absent another concurrent
curator session touching Q71. Working tree at HEAD `6ca6fdf`
clean at unit start.

## ITERATE

Reviewer-mode checklist:

- [x] Claim sourced (Q72 resolution is realization of
      framework shipped Units 37.1 + 37.2).
- [x] Statistical artifacts N/A.
- [x] No regression (docs-only).
- [x] Failure modes documented.
- [x] Cross-reference symmetry: ADR-0018 D-G APPEND ↔
      OPEN_QUESTIONS.md line 573.
- [x] No impl leakage.
- [x] No Lighthouse / bundle / a11y regression.
