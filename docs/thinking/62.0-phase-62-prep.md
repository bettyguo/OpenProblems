# 62.0 — Phase 62 prep (**plugin parameterization for wikilink-href-builder** on `rehypeResolveWikilinks` — adds optional `buildHref?: (slug: string) => string` plugin option with default-fallback `(slug) => "/problems/${slug}"`; **first framework-refactor-only phase in project history** — first phase shape that ships zero new realization of any existing phase-shape pattern AND zero new consumer behavior, purely a framework-affordance addition; **third principal axis of zero-rework framework extension introduced** — plugin-option axis joins registry-state axis (Phase 38+) and plugin-body axis (Phase 46+); closes ADR-0018 APPEND-D-L item 6 `Plugin parameterization` at **24-phase carryover** (Phase 38 → Phase 62) — **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED** (extends Phase-61 22-phase record by 2 phases — second consecutive phase to set the absolute-record); **D-L becomes second D-clause with 3-of-6 enumerated items closed** (items 1 + 2 + 6 closed); **first state where TWO D-clauses have 3+ items closed each** (D-Q at Phase 61 + D-L at Phase 62); APPEND-D-AT twentieth two-letter slot; anticipated 5 units; **57th "Continue" override** — seventh invocation past half-century threshold; **fifty-third NON-§13 phase**; prerequisite for Phase 63 cross-entity wikilinks (APPEND-D-L item 3))

> **Thread choice note (Phase-61 gate rank 1)**: Phase 61
> acceptance gate's Phase-62-entry table ranked **plugin
> parameterization for wikilink-href-builder** as rank 1 (the
> most-defensible autonomous-tractable next thread; prerequisite
> for Phase 63 cross-entity wikilinks at rank 2). Phase 62
> ships rank 1 as a standalone refactor-only phase. The
> alternative framing — **bundle the refactor INTO Phase 63
> cross-entity wikilinks as a single 3-5 unit phase** — was
> considered and rejected on two grounds: (a) it conflates two
> architectural concerns (the parameterization is independently
> valuable as a framework affordance even if cross-entity
> wikilinks slip), and (b) it would deny the project its
> **first refactor-only phase** as a recognized phase shape.
> Shipping plugin parameterization as a standalone phase
> establishes the **refactor-only phase shape** as a first-
> class framework-evolution shape, complementing the existing
> registry-state-evolution + plugin-body-evolution + schema-
> extension shapes.

Phase 62 kickoff per `MASTER_PROMPT.md` §12 cardinal rule. Phase 61
closed at HEAD `b5e5cf4` (Unit 61.4 acceptance gate + README/
PROGRESS_SUMMARY refresh; fifty-second NON-§13 phase; 5
numbered units; **second realization of Phase-57-derived schema-
extension phase-shape pattern**; first 2-realization for that
pattern; first state where schema-extension is observed twice
within the same consumer (tables); tables consumer gains 3rd
evolution post-first-ship; first state where TWO consumers have
3+ evolutions each; first phase to advance framework beyond
Phase-60 capability ceiling; schema-ready-before-plugin extended
from attributes to tags; **NEW LONGEST ABSOLUTE APPEND-DEFERRAL
CLOSURE EVER OBSERVED** at 22-phase carryover; **D-Q becomes
first D-clause with 3-of-4 enumerated items closed**; 20-phase
APPEND-deferral closure cadence; 28th D-G APPEND record extends;
nineteenth two-letter APPEND letter D-AS; 56th "Continue"
override). This unit is docs-only.

> **Sign-off note**: §12 phase-boundary sign-off granted via the
> **57th "Continue" override** invocation (over Phases 6-62).
> **Seventh invocation past the half-century threshold** (Phase
> 55 = 50th; Phase 56 = 51st; Phase 57 = 52nd; Phase 58 = 53rd;
> Phase 59 = 54th; Phase 60 = 55th; Phase 61 = 56th; Phase 62 =
> 57th). User selected Rank 1 via `AskUserQuestion` AND issued
> a literal "Continue" message, per Phase-45-boundary discipline.
> First-thread D-1 recommendation overridable into Unit 62.1.

> **§13 ledger status (carried)**: still **CLOSED**. Phase 62 is
> the **fifty-third NON-§13 phase** (extends Phase-61
> half-century milestone to 53).

## THINK

### Phase 61 → Phase 62 baseline (HEAD = `b5e5cf4`; clean tree)

- **Tests**: **1452 / 75 vitest files** (+12 / 0 vs Phase 60
  close).
- **ADRs**: **24** (Phase 61 added 0 new ADRs; 26 consecutive
  no-new-ADR phases since Phase 36 — Phase 62 anticipates
  **27**).
- **ADR-0018 D-G**: **28 APPENDs** (project record). Single-
  letter slot CONSUMED through Z; two-letter slots IN USE since
  Phase 43 D-AA … Phase 61 D-AS; Phase 62 takes **D-AT**
  (twentieth two-letter slot).
- **First Load JS shared chunk**: **103 kB UNCHANGED** through
  every Phase 9-61 unit (**164 consecutive units** at Unit 61.4
  ship; 100-unit threshold crossed at Phase 48 Unit 48.3; Phase
  62 anticipates **168+ at gate**).
- **Middleware bundle**: **160 kB UNCHANGED** since Phase 12.
- **DB tables**: 7 unchanged; **Migrations**: 9 unchanged.
- **Env vars**: **14** (`MARKDOWN_EXTENSIONS` 8 single-value arms
  + compositions). Phase 62 anticipates **14 unchanged** (plugin
  parameterization is plugin-internal signature evolution; no
  new env-var arm).
- **`lib/markdown/extensions/` files**: **21** UNCHANGED.
- **Phase-37-framework concrete consumers**: **7**. Phase 62
  keeps count at **7** (plugin parameterization is plugin-
  signature evolution on existing wikilinks consumer; no new
  consumer file).
- **Plugin-regex-extension realizations**: **7** (Phase 46
  wikilinks alias + Phase 47 arxiv alias + Phase 48 doi alias +
  Phase 51 pubmed alias + Phase 53 arxiv legacy ID-class + Phase
  55 orcid alias + Phase 60 biorxiv alias). Phase 62 anticipates
  **7 UNCHANGED** (Phase 62 evolves plugin OPTION signature, not
  plugin BODY regex).
- **Constructor-arg-only zero-rework expansion realizations**:
  **7** (Phase 42 + 43 + 44 + 49 + 52 + 56 + 59). Phase 62
  anticipates **7 UNCHANGED**.
- **Schema-extension realizations**: **2** (Phase 57 tables
  attributes + Phase 61 tables caption). Phase 62 anticipates
  **2 UNCHANGED**.
- **Plugin-option-axis realizations**: **0** Phase 61 close.
  Phase 62 anticipates **1** (Phase 62 wikilinks `buildHref`) —
  **first realization of plugin-option-axis evolution as a
  named framework-extension axis in project history**.
- **Dual-form regex realizations**: **5** (arxiv + doi + pubmed +
  orcid + biorxiv). Phase 62 anticipates **5 UNCHANGED**.
- **Plugin-regex-extensions on `remarkPlugins` consumers**: **5**.
  Phase 62 anticipates **5 UNCHANGED**.
- **Surfaces with sextuple-alias (6 alias-syntax consumers)**:
  **4** (all surfaces; Phase 60 ship). Phase 62 anticipates **4
  UNCHANGED**.
- **Per-consumer default-enabled-surfaces** (Phase 62 starting
  state — all 7 consumers at all 4 surfaces post-Phase-59 cross-
  surface generalization):
  - wikilinks: 4 (Phase 42); alias-capable (Phase 46);
    **Phase 62 anticipated: plugin-option-axis introduced
    (buildHref)**.
  - tables: 4 (Phase 43); schema-extension attributes (Phase
    57) + caption (Phase 61).
  - arxiv: 4 (Phase 44); alias-capable (Phase 47); legacy ID-
    class (Phase 53).
  - doi: 4 (Phase 49); alias-capable (Phase 48).
  - pubmed: 4 (Phase 52); alias-capable (Phase 51).
  - orcid: 4 (Phase 56); alias-capable (Phase 55).
  - biorxiv: 4 (Phase 59); alias-capable (Phase 60).
- **Wikilinks consumer evolutions**: **2** (Phase 42 cross-
  surface; Phase 46 alias). Phase 62 anticipates **3** (+plugin
  parameterization) — **first 3-evolution-on-wikilinks state in
  project history**; wikilinks becomes the third consumer with
  3+ evolutions (arxiv via Phase 44 + 47 + 53; tables via Phase
  43 + 57 + 61; wikilinks joins at Phase 62). **First state
  where THREE consumers have 3+ evolutions each** in project
  history.
- **OPEN_QUESTIONS state**: 28 resolved + 4 lean + 34 open =
  **66 total** UNCHANGED since Phase 36.
- **Phase-37+ candidate count**: **8** UNCHANGED.
- **APPEND-deferral closure cadence pattern**: sustained 20
  phases (Phase 42-61) — longest in project history. Phase 62
  continues with **Phase-38 APPEND-D-L item 6 (Plugin
  parameterization)** (Phase 38 D-L enumerated plugin
  parameterization as a deferral; Phase 62 closes at 24-phase
  carryover).
- **Cadence shape**: Phase 62 is the **first realization of the
  plugin-option-axis evolution as a named framework-extension
  axis**. Carryover for D-L items: item 1 cross-surface 4-phase
  (Phase 42); item 2 alias 8-phase (Phase 46); item 6 plugin
  parameterization **24-phase** (Phase 62) — **NEW LONGEST
  ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED** (extends
  Phase-61 22-phase record by 2 phases). **Second consecutive
  phase to set the absolute-record** — first 2-consecutive-
  phase absolute-record-extension streak in project history.
- **Working tree at session start**: clean (Phase 61 close
  committed at `b5e5cf4` including README/PROGRESS_SUMMARY
  refresh).

### D-1. First-thread recommendation: **plugin parameterization for `rehypeResolveWikilinks` via `buildHref` option**

Closes:

1. **ADR-0018 APPEND-D-L item 6** (Plugin parameterization —
   Phase 38 enumerated as a deferral at first-ship). Phase 62
   closes at **24-phase carryover** (Phase 38 → Phase 62).
   **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER
   OBSERVED** — extends Phase-61 22-phase record (Phase 39 →
   61 D-Q item 4 `<caption>` element) by 2 phases. First
   sub-24-phase record was Phase-61 at 22-phase; Phase 62 sets
   new 24-phase record. **Second consecutive phase to set the
   absolute-record** in project history.
2. **Phase-61-gate Phase-62-candidate rank 1** ("Plugin
   parameterization for wikilink-href-builder (APPEND-D-L item
   6) — refactor `rehypeResolveWikilinks` plugin to accept
   entity-type parameter; prerequisite for cross-entity
   wikilinks").

**Why plugin parameterization now** (Phase-61 advance beyond
Phase-60 capability ceiling + Phase-63 cross-entity wikilinks
prerequisite + first refactor-only phase shape):

- **First framework-refactor-only phase in project history**.
  Phase 38-61 each shipped at least one of: a new realization
  of an existing phase-shape pattern (e.g., Phase 56 6th
  constructor-arg-only realization), a new consumer (e.g.,
  Phase 58 biorxiv), a new slot kind (e.g., Phase 39 first
  schemaOverrides), a new phase-shape pattern (e.g., Phase 57
  schema-extension), or an APPEND-deferral closure on a non-
  refactor item. **Phase 62 ships ONLY a plugin-signature
  refactor** — the `WikilinkExtensionRegistry` continues to
  call `rehypeResolveWikilinks` without options (default
  fallback preserves Phase-38-through-Phase-61 behavior
  verbatim). **First-of-its-kind phase shape in project
  history**.
- **Third principal axis of zero-rework framework extension
  introduced**. Pre-Phase-62, the framework supported two
  principal axes of zero-rework extension: (a) **registry-
  state axis** (Phase 38+; constructor-arg evolution of
  `PHASE_NN_DEFAULT_ENABLED_SURFACES` — 7 realizations Phase
  42 + 43 + 44 + 49 + 52 + 56 + 59), (b) **plugin-body axis**
  (Phase 46+; in-body regex evolution — 7 realizations Phase
  46 + 47 + 48 + 51 + 53 + 55 + 60). Phase 62 introduces
  **plugin-option axis** as a third axis: plugin-signature
  evolution that callers exercise via tuple-form
  `.use([plugin, options])` invocation. **First state where
  the framework has three principal axes of zero-rework
  extension** in project history.
- **Prerequisite for Phase 63 cross-entity wikilinks
  (APPEND-D-L item 3)**. Phase 63 cross-entity wikilinks
  (`[[paper-id|display]]` → `/papers/{id}`; `[[author-slug]]`
  → `/authors/{slug}`; `[[institution-slug]]` → `/institutions/
  {slug}`) requires entity-type disambiguation, which depends
  on the `buildHref` plugin option introduced by Phase 62. The
  Phase 38 original deferral text explicitly chained the two:
  "Phase 39+ cross-entity expansion would parameterize via
  plugin options (`{ buildHref: (slug) => string }`)". Phase
  62 ships the parameterization affordance; Phase 63 (if
  pursued) consumes the affordance via cross-entity URL
  builder logic. Phase 62 is **independently valuable** even
  if Phase 63 slips — the affordance enables future builder
  variants (e.g., locale-prefixed paths `/{locale}/problems/${slug}`,
  curator-overridable href builders, or relative-path
  variants for embedded-component contexts) without any
  Phase 62 follow-on requirement.
- **D-L becomes second D-clause with 3-of-6 items closed**.
  Pre-Phase-62: D-L has 2 items closed (item 1 cross-surface
  Phase 42; item 2 alias Phase 46). Post-Phase-62: 3 items
  closed (items 1, 2, 6). **D-L joins D-Q as the second D-
  clause with 3+ items closed** in project history (D-Q at
  Phase 61 with items 1 + 3 + 4 closed). **First state where
  TWO D-clauses have 3+ items closed each** in project
  history. Confirms 3-item depth as a recognized framework-
  evolution shape for multi-item D-clauses.
- **Closes APPEND-D-L item 6 at 24-phase carryover** — **NEW
  LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED**
  (extends Phase-61 22-phase record by 2 phases). Phase-38
  was the wikilinks first-ship phase that enumerated D-L
  items 1 (cross-surface), 2 (alias), 3 (cross-entity), 4
  (`<a class="wikilink">` styling), 5 (404 handling), 6
  (plugin parameterization) as deferrals. Closure timeline:
  - Item 1 (cross-surface) → Phase 42 = 4-phase carryover.
  - Item 2 (alias) → Phase 46 = 8-phase carryover.
  - Item 6 (plugin parameterization) → Phase 62 = **24-phase
    carryover (new record)**.
  - Items 3, 4, 5 → still deferred.
- **21-phase APPEND-deferral closure cadence sustained**
  (extends Phase-61 record 20 → 21). **First 21-phase APPEND-
  deferral closure run** in project history.
- **Wikilinks consumer gains 3rd evolution post-first-ship**.
  Pre-Phase-62: wikilinks has 2 evolutions (Phase 42 cross-
  surface; Phase 46 alias). Post-Phase-62: 3 evolutions
  (Phase 42 + 46 + 62). **Wikilinks becomes the third
  consumer with 3+ evolutions** in the framework. **First
  state where THREE consumers have 3+ evolutions each** in
  project history (arxiv was first via Phase 44 + 47 + 53;
  tables was second via Phase 43 + 57 + 61; wikilinks joins
  at Phase 62).
- **No new runtime dep**: in-place signature evolution on
  existing `rehypeResolveWikilinks` plugin.
- **No concrete demand signal beyond Phase 63 prerequisite**:
  This is a deliberate framework-affordance addition, not a
  curator-demand-driven feature. The Phase 38 D-L item 6
  deferral text explicitly framed plugin parameterization as
  cross-entity-prerequisite infrastructure. Phase 62 ships
  the affordance to unblock Phase 63 cross-entity wikilinks.
  **First phase to ship a framework affordance ahead of
  curator demand signal** — mirrors Phase-57 / Phase-61
  schema-ready-before-plugin discipline ONE LAYER UP (plugin-
  option-ready-before-consumer-demand).

**Path A** (no new ADR; APPEND ADR-0018 D-G) chosen per Phase-
37 through 61 D-9 precedent. ADR-0018 D-G gains its **29th
APPEND** — extends the record from 28 → 29. **Twentieth two-
letter APPEND letter D-AT** (after Phase-43 D-AA … Phase-61
D-AS).

### D-2. DB-migration trigger re-evaluation

Phase 62 anticipates **0 new migrations**. Pure plugin-
signature evolution in a TypeScript module + test additions.

### D-3. Plugin parameterization shape anticipated (Unit 62.1)

D-A. **`rehypeResolveWikilinks` signature evolution**:

```ts
// Before (Phase 38 ship through Phase-61 close):
export const rehypeResolveWikilinks: Plugin<[], Root> = () => (tree) => {
  // ...
  newNodes.push({
    type: "element",
    tagName: "a",
    properties: { href: `/problems/${slug}` },  // hardcoded
    children: [{ type: "text", value: display }],
  });
  // ...
};

// After (Phase 62 ship):
export interface ResolveWikilinksOptions {
  /**
   * Optional href-builder. Receives the kebab-case slug
   * captured from `[[slug]]` or `[[slug|display]]` syntax;
   * returns the absolute or relative URL string to use as the
   * emitted `<a href="...">` value.
   *
   * Defaults to `(slug) => `/problems/${slug}`` (the Phase
   * 38-61 hardcoded shape). Future Phase 63 cross-entity
   * wikilinks consumer (APPEND-D-L item 3) will pass a builder
   * that inspects slug shape / prefix to route per entity-type
   * (paper / author / institution / problem).
   *
   * **XSS-safety contract**: The builder MUST return a
   * relative-path or known-safe-host URL. The captured slug
   * is constrained to `[a-z0-9-]+` per APPEND-D-I; builders
   * that interpolate the slug into a URL preserve the
   * XSS-safety contract automatically. Builders that produce
   * absolute URLs to untrusted hosts would bypass the Phase-37
   * framework's `rehypeStripUnsafeHrefs` step (which runs
   * BEFORE this plugin per APPEND-D-D); curator-facing builder
   * configuration is therefore a **Phase 63+** concern, NOT a
   * Phase 62 affordance.
   */
  buildHref?: (slug: string) => string;
}

export const rehypeResolveWikilinks: Plugin<[ResolveWikilinksOptions?], Root> =
  (options = {}) => (tree) => {
    const buildHref = options.buildHref ?? ((slug: string) => `/problems/${slug}`);
    // ...
    newNodes.push({
      type: "element",
      tagName: "a",
      properties: { href: buildHref(slug) },  // parameterized
      children: [{ type: "text", value: display }],
    });
    // ...
  };
```

**Default-fallback preserves Phase 38-61 behavior verbatim**:
when `WikilinkExtensionRegistry.getExtensions(...)` returns
`{ rehypePlugins: [rehypeResolveWikilinks] }` (no tuple-form
options), the plugin invocation `() => (tree) => ...` receives
`options = {}` and the `buildHref ?? default` coalescing
selects the Phase-38 hardcoded shape. **Zero behavior change
for any existing call site**.

D-B. **`WikilinkExtensionRegistry` class + `PHASE_38_DEFAULT_ENABLED_SURFACES`
constant + factory dispatch arm — UNCHANGED**. The registry
continues to return `rehypePlugins: [rehypeResolveWikilinks]`
(bare plugin, no tuple form, no options passed). Default
behavior preserved. **Phase 62 is plugin-signature-only
evolution** — does NOT touch the registry, the constant, the
factory, or the composite-registry composition. Mirrors the
Phase-46 "plugin-body evolution without touching registry
state" discipline ONE LAYER UP at the plugin-signature level.

D-C. **Pipeline / composition invariants preserved**:
- `unified().use(remarkParse).use(...).use(rehypeResolveWikilinks)`
  (bare invocation; Phase-38-style) continues to work without
  any caller change.
- `unified().use(...).use(rehypeResolveWikilinks, { buildHref })`
  (tuple-form invocation; Phase-63-ready) is now supported by
  the signature.
- `CompositeExtensionRegistry` composition is unchanged
  (rehypePlugins concatenation per APPEND-D-R; plugin-option
  axis is per-plugin-instance, not composition-level).
- `rehype-sanitize` schema is unchanged (no new tags, no new
  attributes, no schema impact whatsoever).
- `rehypeStripUnsafeHrefs` runs BEFORE wikilinks per
  APPEND-D-D — Phase 62 does NOT alter plugin ordering;
  curator-controllable href builders that produce untrusted
  hosts would need a Phase-63+ design that either pre-strips
  via additional defense-in-depth OR restricts builder return
  values via type-narrowed return.

D-D. **Tests to update / add** (Unit 62.1, ~7 NEW tests in
`wikilinks.test.ts`):

1. **Plugin invoked without options → default `/problems/{slug}`
   href (backwards-compat)**: existing Phase-38-through-Phase-46
   call shape continues to produce the Phase-38 hardcoded href;
   regression guard for the default-fallback path.
2. **Plugin invoked with custom `buildHref` → custom href in
   emitted `<a>`**: passing `{ buildHref: (slug) => `/papers/
   ${slug}` }` produces `<a href="/papers/some-slug">`.
3. **Custom `buildHref` invoked once per slug**: spy on the
   builder function; `[[a]] and [[b]]` invokes builder twice
   (once per match); no double-invocation per match.
4. **Custom `buildHref` orthogonal to alias-syntax**:
   `[[slug|display]]` with custom `buildHref` produces the
   custom href AND preserves the alias display text.
5. **Custom `buildHref` returning empty string → href is empty
   string** (no special handling; trusts builder); documents
   the contract.
6. **Custom `buildHref` that depends on slug content** (e.g.,
   conditional path based on slug prefix): builder receives
   exact `[a-z0-9-]+` capture as input.
7. **TypeScript signature exported** (`ResolveWikilinksOptions`
   interface): import + structural-type assertion that the
   shape is `{ buildHref?: (slug: string) => string }`.

D-E. **`wikilinks.test.ts` integration-level test** (Unit 62.2,
~3-5 NEW tests):

1. **`describe("Phase-62 plugin parameterization: WikilinkExtensionRegistry default-call")`**:
   the framework continues to invoke `rehypeResolveWikilinks`
   without options via `WikilinkExtensionRegistry.getExtensions`;
   end-to-end behavior matches Phase 46-61 verbatim on every
   surface (regression guard for the registry-level
   integration).
2. **`describe("Phase-62 plugin parameterization: tuple-form invocation hypothesis")`**:
   demonstrate that a future Phase-63 cross-entity consumer
   COULD construct a non-default `WikilinkExtensionRegistry`
   variant by composing `rehypePlugins: [[rehypeResolveWikilinks,
   { buildHref }]]` via tuple form; assert the unified pipeline
   accepts the tuple form without runtime error and produces
   the expected non-default href.
3. **`describe("Phase-62 plugin parameterization: default-fallback equals Phase-38 hardcode")`**:
   assert that the default-fallback `(slug) => `/problems/
   ${slug}`` is byte-identical to the Phase-38 hardcoded shape
   via direct invocation (no plugin involvement).

D-F. **Composition matrix Phase 62**: plugin parameterization
is plugin-signature evolution with zero behavioral effect
under the current `WikilinkExtensionRegistry` default-call
shape. **Composition matrix UNCHANGED** at Phase 62 default:

| Surface | rehypePlugins | schemaOverrides | remarkPlugins |
|---|---|---|---|
| `bio` | wikilinks (default-call; `buildHref` defaults to `/problems/${slug}`) | tables (schema-extended Phase 57 + 61) | [arxiv, doi, pubmed, orcid, biorxiv] |
| `reviewNotes` | wikilinks (default-call) | tables (schema-extended Phase 57 + 61) | [arxiv, doi, pubmed, orcid, biorxiv] |
| `rationale` | wikilinks (default-call) | tables (schema-extended Phase 57 + 61) | [arxiv, doi, pubmed, orcid, biorxiv] |
| `actionRationale` | wikilinks (default-call) | tables (schema-extended Phase 57 + 61) | [arxiv, doi, pubmed, orcid, biorxiv] |

D-G. **APPEND letter sequence: D-AT (twentieth two-letter
slot)**. Phase 43 D-AA … Phase 61 D-AS; Phase 62 D-AT.

D-H. **Phase 63+ deferrals** (Phase-62 plugin-parameterization
scope cap):

- **Cross-entity wikilinks** (APPEND-D-L item 3) — Phase 63+
  rank 1 (now unblocked by Phase 62 plugin parameterization).
- **`<a class="wikilink">` styling** (APPEND-D-L item 4) —
  Phase 63+ rank 2 BUT requires framework refactor to support
  multi-source schemaOverrides (CompositeExtensionRegistry
  throws on conflict per APPEND-D-C); alternative: skip the
  schemaOverride entirely and rely on post-sanitize plugin-
  order discipline.
- **404 handling for unresolved wikilinks** (APPEND-D-L item
  5) — Phase 63+.
- **Surface-specific table schemas** (APPEND-D-Q item 6 — last
  remaining D-Q deferral) — Phase 63+. Closure would complete
  D-Q (4-of-4 items closed; first D-clause with ALL items
  closed in project history).
- **Empty-alias fallback unification** across consumers —
  Phase 63+.
- **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
  prefix** — Phase 63+.
- **Legacy numeric-only bioRxiv IDs** — Phase 63+.
- **dx.doi.org legacy host parsing** (APPEND-D-AC carries) —
  Phase 63+.
- **Stricter trailing-lookahead for trailing-period DOIs**
  (APPEND-D-AC carries) — Phase 63+.
- **Paper-card hover-preview** (APPEND-D-Y item 6 carries) —
  Phase 63+.
- **Auto-trim of alias display whitespace** — Phase 63+.
- **A future plugin that EMITS `colSpan`/`rowSpan`/`scope`** or
  `<caption>` to realize the Phase-57 / Phase-61 schema-ready-
  before-plugin states — Phase 63+.
- **OSF preprint consumer** — eighth concrete consumer —
  Phase 63+.
- **6th-or-later `remarkPlugins` consumer beyond arxiv + doi
  + pubmed + orcid + biorxiv** — Phase 63+.
- **2nd `rehypePlugins` consumer beyond wikilinks** — Phase
  63+.
- **2nd `schemaOverrides` consumer beyond tables** — Phase
  63+ (requires framework refactor per the composite-registry
  single-source rule).
- **3rd regex evolution on `remarkLinkArxivIds`** — Phase
  63+.
- **ADR-0025 concrete content-moderation provider** — Phase
  63+; not autonomous-tractable.

### Provisional unit breakdown (62.0 … 62.4)

| Unit | Title | Type |
|---|---|---|
| 62.0 | **Phase 62 prep** (this unit) | docs |
| 62.1 | `rehypeResolveWikilinks` signature evolution + `ResolveWikilinksOptions` interface + ~7 NEW `wikilinks.test.ts` plugin-option tests + ADR-0018 D-G APPEND-D-AT | code+APPEND |
| 62.2 | 3-5 NEW integration-level tests in `wikilinks.test.ts` for `WikilinkExtensionRegistry` default-call + tuple-form invocation hypothesis + default-fallback byte-identity | code |
| 62.3 | Phase-62 hygiene + ADR-0018 APPEND-D-L item 6 closure documentation (24-phase carryover; NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE RECORD; D-L becomes second D-clause with 3-of-6 items closed; first state where TWO D-clauses have 3+ items closed each) | docs |
| 62.4 | **Phase 62 acceptance gate** | gate |

Total: **5 units**. Mirrors Phase 41-61 5-unit framework shape
verbatim.

### Phase-62-blocking decisions deferred to per-unit implementation

- **D-8** (Unit 62.1): Plugin-option interface naming —
  `ResolveWikilinksOptions` (Phase-46-style: noun describing
  the operation; mirrors `Schema` from `rehype-sanitize` /
  `Options` from `unified` plugin conventions). Lean = **`ResolveWikilinksOptions`**.
- **D-9** (Unit 62.1): `buildHref` parameter type —
  `(slug: string) => string` (pure synchronous function;
  receives the captured slug only, not the display string or
  match index). Lean = **synchronous string→string function;
  slug only**.
- **D-10** (Unit 62.1): Default-fallback placement — inline
  null-coalescing inside the plugin closure
  (`options.buildHref ?? ((slug) => `/problems/${slug}`)`);
  alternative is a hoisted `const DEFAULT_BUILD_HREF` —
  preferred to keep the default colocated with the inline
  comment that documents Phase-38 verbatim preservation. Lean
  = **hoisted `const DEFAULT_BUILD_HREF`** for testability
  (Unit 62.2 D-E.3 imports it for byte-identity assertion).
- **D-11** (Unit 62.2): Tuple-form integration test approach —
  manually construct a one-off registry variant via
  `WikilinkExtensionRegistry` subclass OR direct `unified()
  .use([rehypeResolveWikilinks, { buildHref }])` invocation
  bypassing the registry. Lean = **direct unified invocation**
  (registry subclassing is over-engineered for a hypothesis
  test; Phase 63 will introduce the real cross-entity
  registry variant).
- **D-12** (Unit 62.1): APPEND-D-AT naming — Excel convention
  continues. Lean = **continue Excel**.

### Smoke gates (this unit)

Docs-only — no source files modified.

- File added: `docs/thinking/62.0-phase-62-prep.md`.
- CHANGELOG: open `### Phase 62` section + Unit 62.0 entry.
- `pnpm audit-content`: 0 errors / 6 warnings UNCHANGED.
- `pnpm test`: 1452/75 UNCHANGED.
- First Load JS / middleware: UNCHANGED.

### Parallel-curator awareness

This unit docs-only; no collision. Phase 62 collision risk:

- 62.1: MODIFIED `lib/markdown/extensions/wikilinks.ts`
  (signature evolution + `ResolveWikilinksOptions` interface
  + default-fallback constant) + MODIFIED
  `lib/markdown/extensions/wikilinks.test.ts` (~7 NEW tests) +
  MODIFIED `docs/adr/0018-markdown-sanitization.md`
  (APPEND-D-AT).
- 62.2: MODIFIED `lib/markdown/extensions/wikilinks.test.ts`
  (3-5 NEW integration-level tests; same file as Unit 62.1).
- 62.3 / 62.4: docs hygiene + gate; parallel-safe.

### Alternative threads (overridable into Unit 62.1)

| Thread | Why tempting | Why deferred | Units |
|---|---|---|---|
| **Surface-specific table schemas** (APPEND-D-Q item 6) | Closure would make D-Q the first D-clause with ALL items closed; "architectural finishing move" | Rank 3 in Phase-61 gate; defensible but does not unblock Phase 63 cross-entity; safer to ship plugin parameterization first | 1-2 |
| **Cross-entity wikilinks** (APPEND-D-L item 3) | Phase-61 gate rank 2 | Depends on plugin parameterization (rank 1); should ship Phase 62 prerequisite first | 2-3 |
| **`<a class="wikilink">` styling** | Phase-61 gate rank 4 | Requires framework refactor (CompositeExtensionRegistry single-source schemaOverrides rule); deferred until either single-class-attribute pattern accepted as plugin-only emit OR framework supports multi-source schemaOverrides | 1-2 |
| **404 handling for unresolved wikilinks** | Phase-61 gate rank 5 | Build-time validation step; Phase 63+. | 2-3 |
| **Empty-alias fallback unification** | Phase-48 deferral; smallest scope | Phase 63+. | 1 |
| **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** | Phase-61 gate rank 7 | Phase 63+. | 1-2 |
| **Legacy numeric-only bioRxiv IDs** | Phase-61 gate rank 8; mirrors Phase-53 shape | Phase 63+. | 1-2 |
| **OSF preprint consumer** | Phase-61 gate rank 9 | DOI overlap concern; Phase 63+. | 2-3 |
| **ADR-0025 concrete moderation provider** | Strongest patience signal | Not autonomous-tractable. | 3-5 |
| **Q78 / Q79 / v2 methodology / safeAuth / test backfill / account-delete / pre-commit typecheck** | Various follow-ons | Each deferred. | 1-5 each |

### Phase-62 anti-scope (boundary statement preview for Unit 62.4)

NOT cross-entity wikilinks (Phase 63+; depends on Phase 62
plugin parameterization);
NOT `<a class="wikilink">` styling (Phase 63+; deferred pending
framework decision on single-class-attribute pattern);
NOT 404 handling for unresolved wikilinks (Phase 63+);
NOT surface-specific table schemas (Phase 63+; last remaining
D-Q item);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 63+);
NOT legacy numeric-only bioRxiv IDs (Phase 63+);
NOT dx.doi.org legacy host (Phase 63+);
NOT stricter trailing-lookahead (Phase 63+);
NOT paper-card hover-preview (Phase 63+);
NOT auto-trim of alias display whitespace (Phase 63+);
NOT empty-alias-as-slug-fallback (Phase 63+);
NOT empty-alias-as-bare-arxiv-fallback (Phase 63+);
NOT empty-alias-as-bare-doi-fallback / empty-alias unification
(Phase 63+);
NOT empty-alias-as-bare-pubmed-fallback (Phase 63+);
NOT empty-alias-as-bare-orcid-fallback (Phase 63+);
NOT empty-alias-as-bare-biorxiv-fallback (Phase 63+);
NOT a future plugin that EMITS `<caption>` or
`colSpan`/`rowSpan`/`scope` (Phase 63+);
NOT `@mention` resolution (Phase 63+);
NOT OSF preprint consumer (Phase 63+);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi
+ pubmed + orcid + biorxiv (Phase 63+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 63+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 63+;
requires framework refactor);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 63+);
NOT curator-facing builder configuration (Phase 63+; would
require XSS-audit per-builder + locale-aware path discipline);
NOT locale-prefixed wikilink paths (Phase 63+; would be a
specific `buildHref` value, not a framework change);
NOT relative-path wikilink variants (Phase 63+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-61 deferrals carry forward.

**This phase is the first framework-refactor-only phase in
project history** + **first realization of the plugin-option
axis as a third principal axis of zero-rework framework
extension** + **first phase to ship a framework affordance
ahead of curator demand signal** (Phase 38 D-L item 6
deferral closed as cross-entity-prerequisite infrastructure).
**Generalizes the framework's zero-rework-extension surface
from two axes (registry-state + plugin-body) to three axes
(registry-state + plugin-body + plugin-option)**.

### Anticipated architectural firsts in Phase 62 (12)

1. **First framework-refactor-only phase in project history**.
   Phase 38-61 each shipped at least one of: a new realization
   of an existing phase-shape pattern, a new consumer, a new
   slot kind, a new phase-shape pattern, OR an APPEND-deferral
   closure on a non-refactor item. Phase 62 ships ONLY a
   plugin-signature refactor that preserves Phase-38-through-
   Phase-61 behavior verbatim under default-call invocation.
   **First-of-its-kind phase shape** in project history.
2. **Third principal axis of zero-rework framework extension
   introduced**. Pre-Phase-62: 2 axes (registry-state axis
   Phase 38+ with 7 realizations; plugin-body axis Phase 46+
   with 7 realizations). Post-Phase-62: 3 axes (registry-state
   + plugin-body + **plugin-option** with 1 realization Phase
   62 wikilinks `buildHref`). **First state where the framework
   has three principal axes of zero-rework extension** in
   project history.
3. **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER
   OBSERVED** at **24-phase carryover** (Phase 38 → Phase
   62). Extends Phase-61 22-phase record by 2 phases. APPEND-
   D-L closure trajectory: item 1 cross-surface 4-phase
   (Phase 42); item 2 alias 8-phase (Phase 46); item 6
   plugin parameterization **24-phase** (Phase 62). **Second
   consecutive phase to set the absolute-record** — first
   2-consecutive-phase absolute-record-extension streak in
   project history.
4. **D-L becomes second D-clause with 3-of-6 enumerated items
   closed** within the cadence (items 1 + 2 + 6 closed; items
   3 + 4 + 5 remaining). **First state where TWO D-clauses
   have 3+ items closed each** in project history (D-Q at
   Phase 61 with items 1 + 3 + 4 closed; D-L at Phase 62
   with items 1 + 2 + 6 closed). Confirms 3-item depth as a
   recognized framework-evolution shape for multi-item D-
   clauses.
5. **21-phase APPEND-deferral closure cadence sustained**
   (Phase 42-62) — new longest sustained cadence in project
   history (extends Phase-61 record 20 → 21). **First 21-
   phase APPEND-deferral closure run** in project history.
6. **Wikilinks consumer gains 3rd evolution post-first-ship**
   (Phase 42 cross-surface + Phase 46 alias + Phase 62 plugin
   parameterization). **First state where THREE consumers
   have 3+ evolutions each** in project history (arxiv was
   first via Phase 44 + 47 + 53; tables was second via Phase
   43 + 57 + 61; wikilinks joins at Phase 62). Confirms the
   3-evolution depth as a generalized framework-evolution
   shape across the consumer roster.
7. **Twenty-first prep-/APPEND-doc-level deferral closed by
   a later phase**: Phase 42 → 38 D-L item 1; Phase 43 → 39
   D-Q item 2; Phase 44 → 41 D-Y item 1; Phase 45 → 41 D-Y
   item 4; Phase 46 → 38 D-L item 2; Phase 47 → 41 D-Y item
   5; Phase 48 → 45 D-AC item 2; Phase 49 → 45 D-AC cross-
   surface; Phase 50 → 45 D-AC PubMed PMID item; Phase 51 →
   new Phase-50 deferral; Phase 52 → 50 D-AH PubMed cross-
   surface; Phase 53 → 41 D-Y item 2; Phase 54 → 45 D-AC
   ORCID item; Phase 55 → new Phase-54 deferral; Phase 56 →
   54 D-AL ORCID cross-surface item; Phase 57 → 39 D-Q item
   3 Table-specific attributes; Phase 58 → 54 D-AL bioRxiv
   preprint consumer item; Phase 59 → 58 D-AP bioRxiv cross-
   surface item; Phase 60 → 58 D-AP bioRxiv alias item;
   Phase 61 → 39 D-Q item 4 `<caption>` element; **Phase 62
   → 38 D-L item 6 Plugin parameterization**.
   **APPEND-deferral closure cadence sustained 21 phases** —
   **new longest sustained cadence in project history**
   (extends Phase-61 record 20 → 21).
8. **Twenty-ninth APPEND on ADR-0018 D-G** — extends the
   **first-ADR-D-clause-with-most-APPENDs record** from 28 →
   29. **Twentieth two-letter APPEND letter D-AT** (Excel-
   spreadsheet column convention sustained).
9. **Fifty-third NON-§13 phase** (extends Phase-61 half-
   century milestone to 53). The §13 ledger remains CLOSED
   across the entire 53-phase run (Phase 10-62). **First 53-
   phase ledger-closure streak**.
10. **First phase to ship a framework affordance ahead of
    curator demand signal**. Phase 57 / Phase 61 schema-
    ready-before-plugin shipped schema state ahead of any
    pipeline emitting the schema'd content (forward-
    compatibility within a single value). Phase 62 ships a
    plugin-option signature affordance ahead of any consumer
    using the option (forward-compatibility within plugin
    signature). **Plugin-option-ready-before-consumer-demand**
    discipline established — generalizes the schema-ready-
    before-plugin discipline ONE LAYER UP to plugin
    signature level.
11. **First "framework + 7 consumers + 7 expansions + 7
    plugin-regex-extensions + 2 schema-extensions + 1
    plugin-option-extension + 5-consumer same-slot × 4
    surfaces + 7-consumer composition × 4 surfaces + alias
    × 6 + dual-form × 5 + sextuple-alias × 4 + cross-
    surface-first × 1 + axis-asymmetry-resolved × 1 +
    ALL-7-consumers-fully-extended × 1 + schema-ready-
    before-plugin × 2 + plugin-option-ready-before-consumer-
    demand × 1 + 4-D-clauses-both-items-closed + D-clause-
    with-3-items-closed × 2 + 3-consumers-with-3+-evolutions
    × 1 + three-principal-axes-of-zero-rework-extension × 1
    + refactor-only-phase × 1" 26-phase cluster** in project
    history (Phase 37-62). 26-phase architectural arc. Plus
    **32nd consecutive phase without new B category** —
    Phase 31-62 = first 32-phase run (extends Phase-61
    record 31 → 32). Plus **fifty-third NON-§13 phase**
    (extends Phase-61 milestone to 53). Plus **168th
    consecutive 103 kB First Load JS unit** (extends from
    164 at Phase-61 close). Plus **27th consecutive no-new-
    ADR phase** (Phase 36-62; extends Phase-61 record 26 →
    27).
12. **57th "Continue" override invocation** at Unit 62.0 —
    extends the **half-century-of-Continue-overrides
    milestone** (Phase 55 = 50th) to **57 over Phases
    6-62**. **Seventh invocation past the half-century
    threshold** — confirms the §12-cardinal-rule sign-off
    discipline remains durable post-milestone across 8
    successive phases.

### Open questions newly surfacing

Anticipated: 0 new opens. ADR-0018 APPEND-D-L item 6 is the
LOCUS of Phase 62 closure.

## DESIGN — not applicable

This unit is docs-only.

## CODE — not applicable

Same.

## ITERATE — gates

- [x] Phase 61 close baseline documented.
- [x] D-1 first-thread recommendation framed (rank 1 from
      Phase-61 gate).
- [x] Plugin-parameterization shape sketched (D-3).
- [x] Backwards-compatibility contract documented (default-
      fallback preserves Phase 38-61 verbatim).
- [x] Anti-scope enumerated.
- [x] Anticipated architectural firsts enumerated (12).
- [x] Provisional unit breakdown (5 units).
- [x] Parallel-curator awareness restated.
- [x] Per-unit decisions D-8..D-12 lean-noted.
- [x] No new ADRs (Path A per Phase-37-through-61 precedent).
- [x] No new OPEN_QUESTIONS entries.
- [x] Smoke gates green (docs-only).
- [ ] Unit 62.0 ships at commit `<this unit>`; Unit 62.1
      authors the `rehypeResolveWikilinks` signature evolution
      + `ResolveWikilinksOptions` interface + ~7 NEW plugin-
      option tests + APPEND-D-AT.
