# 28.3 — Phase-28 hygiene status pass (0 Class A / ~5 Class B / Class C unchanged; seventeen consecutive well-scoped phases; Phase-9 Class B item 8 CLOSED at 17-phase carryover)

Fourth Phase-28 unit; docs-only. Mirrors Phase-13 through Phase-27
hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-28 cleanup: **0 items**

Phase 28 shipped without deferrals through Unit 28.2. ADR-0020
(Unit 28.1) + multi-provider OAuth implementation (Unit 28.2;
across 8 files: `lib/auth/index.ts` + 2 new `lib/auth/link-account.*`
files + `components/auth-control/index.tsx` + `.env.example` + 2
i18n locale files + CHANGELOG) + 11 new vitest tests landed
cleanly. **Phase-28 Class A count = 0** matches Phase-12 through
Phase-27 well-scoped pattern — **seventeen consecutive phases**
ship with 0 Class A items.

### Class B — Phase-28 follow-ons: **~5 items in 12 categories**

#### B.1 Image-processing expansion (3 carried unchanged)

- Q68 expansion content moderation on uploaded images.
- Cropping UI.
- AVIF format conversion.

#### B.2 Phase-17 markdown subset expansion (8 carried unchanged)

#### B.3 Phase-18 surface expansion (3 + Q72 + rationale-Q72-analogue carried unchanged)

#### B.4 Phase-20 backfill operational follow-ons (3 carried unchanged)

#### B.5 Phase-21 cleanup operational follow-ons (2 carried unchanged)

#### B.6 + B.7 (0 carried; closed Phase 25)

#### B.8 Phase-24 transcode operational follow-ons (1 carried unchanged)

AVATAR_SIZE / WEBP_QUALITY tuning.

#### B.9 Operational + infrastructure (**8 carried; 1 CLOSED Phase 28**)

- **~~Multi-provider OAuth expansion~~** — **CLOSED Phase 28
  Unit 28.1 + 28.2**. Phase-9 Class B item 8 carried **17 phases**
  (strongest non-multi-phase architectural patience signal in
  project history at Phase 28 entry) is now resolved via ADR-0020
  partial-supersession of ADR-0012 D-B + Google provider integration
  in `lib/auth/index.ts` + per-provider sign-in UI in
  `<AuthControl>` + Q73 operational gate surfaced + Q74 architectural
  candidate surfaced (non-GitHub-curator eligibility). 8 of 9
  Phase-9 Class B operational + infrastructure items still
  carrying (Q54/Q55/Q69 operational gates; CI dummy `AUTH_SECRET`;
  `pnpm db:migrate` doc; first LHCI run; middleware-based
  auth-route protection; `pnpm backfill-exif-strip` operational
  doc; `pnpm cleanup-orphan-blobs` operational doc; `pnpm
  emit-challenge-action` operational doc — note Q73 lands as
  **B.13** newly surfaced under Phase-28 multi-provider operational
  block).

#### B.10 Phase-26 detail-page follow-ons (2 carried unchanged)

- acceptedActionId link target carried.
- Curator-translated FR review carried.

#### B.11 Operational + i18n maintenance (1 carried; updated)

i18n key count audit (now **142** per-locale; was 140 at Phase 27
close; +2 Phase-28 `auth.sign_in_with_github` + `auth.sign_in_with_google`).

#### B.12 Phase-27 markdown-promotion follow-ons (1 carried unchanged)

Rating-action `rationale` markdown promotion (separate from
rating-challenge `rationale` shipped Phase 27).

#### B.13 Phase-28 multi-provider follow-ons (**Phase-28 newly surfaced**)

Four newly surfaced items couple to the multi-provider OAuth
realization:

1. **Q73 Google OAuth app registration** — operational gate;
   mirrors Q54 shape. Curator registers Google Cloud Console
   OAuth app (production + local dev); populates
   `AUTH_GOOGLE_ID` + `AUTH_GOOGLE_SECRET`. Phase 29+ if curator
   provisions ahead of deploy.
2. **Q74 non-GitHub users as curators** — architectural; Phase
   29+ candidate if curator demand widens. Would require
   non-GitHub editorial-identity field on users OR username-
   claim mechanism OR widened `editorial.primary_curator`
   schema. ADR-0020 D-D preserves the GitHub-only gate; this
   item tracks future opening of that surface.
3. **Account-merge UI for same-email cross-provider users** —
   ADR-0020 D-E deferral. Phase 29+ if curator-reported
   confusion. ~2-3 units.
4. **Third+ provider expansion** — GitLab / email-link /
   passkeys candidates. Each requires ADR-0020 amendment or
   new ADR per D-B. Phase 30+ if observed demand.

### Class C — pre-existing carryovers (unchanged)

All ~25 Class C items persist unchanged (Q47 / Q54 / Q55 / Q69 +
operational gates + Phase-2 ROR-ID orphan + `<managingEditor>` on
RSS + W3C validator passes + `pnpm clean-drafts` + LHCI first
run + abandoned-blob cleanup script gap + etc.).

### Phase-27 → Phase-28 delta

| Metric | Phase 27 close (`a34a38d`) | Phase 28 close pre-this (HEAD `6219ccc`) | Δ |
|---|---|---|---|
| Commits | 5 | 3 + this hygiene + 28.4 + 28.5 (anticipated) | +6 anticipated total |
| ADRs | **19** | **20** | **+1** (ADR-0020 multi-provider OAuth; **first new ADR since Phase 19**; **eight-phase no-new-ADR streak ends** Phase 18 + 20-27 = 8 phases) |
| Migrations | 6 | **6** | **0** (**twelfth consecutive 0-migration phase**) |
| Env vars | 7 | **9** | **+2** (`AUTH_GOOGLE_ID` + `AUTH_GOOGLE_SECRET`; first env-var-count change since Phase 16 Q69 = **12 phases gap**) |
| Runtime deps | (Phase-27 stack) | (Phase-27 stack) | 0 (`next-auth/providers/google` is part of the existing `next-auth@5.0.0-beta.31` distribution; no new top-level dep) |
| Tests | 605 / 55 files | **616 / 56 files** | **+11 / +1** (+8 `extractGithubLogin` cases + 3 `PROVIDER_IDS` cases; first new test file in `lib/auth/` since Phase-12 Unit 12.3 `curator.test.ts` = **16 phases gap**) |
| First Load JS (shared) | 103 kB | **103 kB** | **0** (37th consecutive unit at 103 kB; Phase 9-28) |
| Middleware bundle | 160 kB | **160 kB** | **0** (since Phase 12) |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` / `/u/{handle}/challenges` / `/u/{handle}/challenges/{id}` / `/problems/{slug}/challenges` bundles | 108 kB each | **108 kB each** | **0** |
| Dynamic page+API routes | 8 | **8** | **0** (Google sign-in reuses `/api/auth/[...nextauth]`) |
| OPEN_QUESTIONS resolved/open/lean/total | 26 / 29 / 4 / 59 | 26 / 29 / 4 / 59 | 0 (Q73 + Q74 land Unit 28.4; **Q-tally freeze breaks at 8 phases** Phase 20-27) |
| i18n keys per locale | 140 | **142** | **+2** (`auth.sign_in_with_github` + `auth.sign_in_with_google`; first `auth` namespace addition since Phase-9 Unit 9.5 = **16 phases gap**) |
| `lib/auth/` files | 3 (`index.ts` + `curator.ts` + `curator.test.ts`) | **5** (added `link-account.ts` + `link-account.test.ts`) | **+2** (first new `lib/auth/` source file since Phase-12; first new `lib/auth/` test file since Phase-12 = 16 phases gap) |
| OAuth providers configured | 1 (GitHub) | **2** (GitHub + Google) | **+1** (closes Phase-9 Class B item 8) |
| User-facing UX phases since Phase 18 | 2 (Phase 26 + 27) | **2** (Phase 28 is auth-infrastructure phase — signed-in user surface unchanged Phase 28; signed-out surface gains a button) | 0 |
| Patience-signal-closing phases since Phase 19 | 0 | **1** (Phase 28 closes 17-phase Phase-9 Class B item 8) | **+1** (first such phase since Phase 19) |
| `lib/auth/index.ts` LOC | 65 | **76** | **+11** |
| `components/auth-control/index.tsx` LOC | 95 | **122** | **+27** |
| `.env.example` blocks | 4 (Phase-5 LLM + Phase-6 Discussions + Phase-8 i18n + Phase-9 Auth + Phase-9 DB) | **5** (Phase-28 Auth multi-provider inserted before Phase-9 DB) | **+1** |

### Architectural firsts shipped in Phase 28 (pre-this-unit at `6219ccc`)

1. **First new ADR since Phase 19** — ADR-0019 was the last;
   Phase 28 adds ADR-0020. 10-phase no-new-ADR gap closes.
2. **First architectural patience-signal phase since Phase 19** —
   Phase 20-27 closed operational + UX follow-ons (low
   architectural surface change); Phase 28 returns to
   architectural-commitment shape.
3. **First time an ADR D-clause is partially superseded by a
   follow-on ADR** rather than amended via implementation note —
   ADR-0020 supersedes ADR-0012 D-B (single-provider restriction
   lifted); ADR-0012's D-A / D-C / D-D / D-E preserved.
4. **Eight-phase no-new-ADR streak ends** (Phase 18 + 20-27 = 8
   phases; Phase 28 = new-ADR phase). Prior longest = 5 phases at
   Phase 23 close.
5. **Eight-phase Q-tally freeze ends** (Phase 20-27 = 8 phases;
   Phase 28 Unit 28.4 lands +Q73 + +Q74 = +2 open / 0 net change
   to total cap until Q-promotions in future phases).
6. **First time the project ships exactly 2 OAuth providers** —
   Phase 9-27 shipped GitHub-only; Phase 28 = GitHub + Google.
7. **First env-var-count change since Phase 16** (Q69
   `BLOB_READ_WRITE_TOKEN` added Phase 16; 12 phases gap).
   Phase 28: 7 → 9 (+`AUTH_GOOGLE_ID` + `AUTH_GOOGLE_SECRET`).
8. **First pure-function-extraction from `lib/auth/index.ts`** —
   `link-account.ts` extracts the `linkAccount` event helper +
   `PROVIDER_IDS` constant from the NextAuth runtime module
   (which can't be unit-tested due to module-load DB-adapter
   initialization). Mirrors Phase-22+23 `scaffold.ts` pure-function-
   extraction precedent applied to a Phase-9 surface.
9. **First multi-button signed-out UI in `<AuthControl>`** —
   Phase-9 single-button shape preserved through 18 NON-§13
   phases. Phase 28 introduces per-provider buttons stacked
   mobile + `sm:` side-by-side per ADR-0020 D-F.
10. **First new test file in `lib/auth/` since Phase-12** — 16
    phases gap. `link-account.test.ts` joins `curator.test.ts`
    as a pure-function-helper test sibling.
11. **First i18n-key addition to the `auth` namespace since
    Phase-9** — 16 phases unchanged; +2 keys
    (`sign_in_with_github` + `sign_in_with_google`).
12. **`PROVIDER_IDS` constant** establishes single-source-of-
    truth for provider count + ordering. Sign-in UI iterates the
    constant directly; ADR-0020 D-B/D-F invariants enforced
    through the data structure rather than parallel
    documentation.
13. **Twelfth consecutive 0-migration phase** (Phase 17-28; 15
    of 19 phases since DB landed).
14. **Twelfth consecutive single-session phase** anticipated
    (Phase 17-28; no parallel-session activity observed during
    Phase 28 work).
15. **Phase-28 closes the strongest non-multi-phase
    architectural patience signal** — Phase-9 Class B item 8
    carried 17 phases at entry; 18 phases at close.

### Phase-29+ Q-candidates flagged

Carryovers from Phase 27 (7 candidates) + **2 NEW Phase-28
surfaces** anticipated landing Unit 28.4:

- **Q60** / **Q61** / **Q62** / **Q64** / **Q65** / **Q68
  expansion** / **Q72** (carried unchanged from Phase 27).
- **Q73** (NEW Phase 28 operational) — Google OAuth app
  registration; mirrors Q54 shape.
- **Q74** (NEW Phase 28 architectural) — non-GitHub users as
  curators; ADR-0020 D-D Phase 29+ candidate.

### Phase-29+ ADR slot reservations

**ADR-0021 candidate slot opens** (Phase 28 claimed ADR-0020).
Strongest remaining candidates:

- **Image-processing API content moderation** (Q68 + Q68
  expansion).
- **Markdown subset extensions ADR** (Phase-17 D-H + Q72 + Phase-27
  Q72-analogue for rationale).
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  **21+ phases** — strongest overall patience signal growing
  each cycle now that the multi-provider OAuth carryover has
  closed).
- **Account-merge UI** (ADR-0020 D-E Phase 29+ candidate).
- **Third+ provider expansion ADR amendment** (ADR-0020 D-B Phase
  29+ candidate).

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/28.3-phase-28-hygiene.md` (this).
- CHANGELOG: Unit 28.3 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk. No parallel-session
activity observed during Phase 28 work; working tree clean at
each unit boundary.

### CHANGELOG entry shape

Unit 28.3 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-27 well-scoped pattern
(**seventeen consecutive**); ~5 Class B items in 12 categories
(3 image-processing carried; 8 markdown carried; 3 Phase-18 +
Q72 + rationale-Q72-analogue carried; 3 Phase-20 carried; 2
Phase-21 carried; 0 for B.6 + B.7; 1 Phase-24 carried; **B.9 8
of 9 operational carried; 1 CLOSED Phase 28 = multi-provider
OAuth expansion = Phase-9 Class B item 8 17-phase carryover
closed**; 2 Phase-26 carried; 1 Phase-26 i18n audit updated to
142 per-locale; 1 Phase-27 markdown-promotion carried; **B.13 4
NEW Phase-28 surfaced**: Q73 operational + Q74 architectural +
account-merge UI Phase 29+ + third+ provider expansion); Class
C carryovers unchanged; Phase 27 → 28 delta (**+1 ADR Δ — first
since Phase 19**; 0 migration Δ; 0 runtime dep Δ; **+2 env vars
Δ — first since Phase 16**; +11 tests / **+1 test file Δ —
first new `lib/auth/` test file since Phase 12 = 16-phase gap**;
**+2 i18n keys per locale Δ — first `auth` namespace addition
since Phase 9 = 16-phase gap**; 0 bundle Δ; **+1 OAuth provider
Δ — first multi-provider state**; **+2 `lib/auth/` source files
Δ — first new since Phase 12**; +1 `.env.example` block);
**15 Phase-28 architectural firsts** (incl. **first new ADR
since Phase 19**, **first patience-signal-closing phase since
Phase 19**, **first ADR D-clause partial-supersession by
follow-on ADR**, **first pure-function-extraction from
`lib/auth/index.ts`**, **first multi-button signed-out UI in
`<AuthControl>`**, **first multi-provider state**); 9 Phase-29+
Q-candidates carry (7 from Phase 27 + Q73 + Q74 new Phase 28);
ADR-0021 candidate slot opens after Phase 28 claimed ADR-0020;
strongest remaining patience signals: Phase-5 D-4 subscriber-list
email (21+ phases); Q68 expansion / Q72 / markdown extensions
ADRs; account-merge UI + third+ provider ADR-0020 amendment
candidates.
