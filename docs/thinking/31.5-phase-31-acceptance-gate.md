# 31.5 — Phase 31 acceptance gate (weekly digest scheduler realized; **22 ADRs**; 6 units; closes Phase-30 B.15 item 1 at 1-phase carryover; first Vercel Cron + first `vercel.json` + first scheduled API endpoint; second consecutive migration phase = first cluster since Phase 15-16; fourth consecutive new-ADR phase at 6 units; 27th "Continue" override opportunity at Phase 31 → 32 boundary)

Sixth and final Phase-31 unit; docs-only. Closes Phase 31 (**twenty-
second NON-§13 phase**; weekly digest scheduler + send template
realized via new ADR-0022 + Vercel Cron infrastructure + new
`/api/v1/cron/digest-send` route + `vercel.json` config + `subscriber.lastDigestSentAt`
column extension migration + new `lib/cron/` + new `lib/email/templates/digest.tsx`
+ `lib/subscribers/` selector extension + Q77 operational gate +
Q78 architectural candidate). Mirrors Phase-12 through Phase-30
acceptance-gate patterns. **§13 ledger CLOSED at Unit 9.9** (carried
unchanged through Phases 10-30; Phase 31 inferred-not-§13 like its
twenty-one predecessors).

## THINK

### Phase 31 ledger

**6 units shipped** (31.0 prep → 31.5 gate). **0 deferrals**; **0
scope drift**. **Twentieth consecutive well-scoped phase**.

| Unit | Title | Type | Commit |
|---|---|---|---|
| 31.0 | Phase 31 prep | docs | `4a159a9` |
| 31.1 | ADR-0022 weekly digest scheduler | docs | `1662f31` |
| 31.2 | digest template + subscriber selector | code | `221ce59` |
| 31.3 | cron route + vercel.json + migration | code | `cc20793` |
| 31.4 | hygiene + Q77 + Q78 + ADR-0021 cross-ref | docs | `8ca8780` |
| 31.5 | Phase 31 acceptance gate | docs | this commit |

### Final smoke gates (all green at HEAD pre-this-unit `8ca8780`)

- `pnpm typecheck` clean.
- `pnpm lint` clean.
- `pnpm test` → **707/707 across 62 vitest files** (+40 / +2 file Δ
  from Phase 30 close 667/60; Unit 31.2 added +17/+1 + Unit 31.3 added
  +23/+1).
- `pnpm build` → ~659 prerendered pages + **12 dynamic page+API
  routes** (was 11; +1 `/api/v1/cron/digest-send` at 183 B / 103 kB
  First Load JS); **First Load JS shared chunk = 103 kB UNCHANGED**
  end-to-end through every Phase 9-31 unit (**42nd consecutive unit at
  103 kB**); **middleware bundle = 160 kB UNCHANGED** since Phase 12;
  all page bundles UNCHANGED.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline since
  Phase 2; unchanged through every Phase 3-31 unit).

### Phase 30 → Phase 31 architectural firsts (cumulative across the phase; ~15 firsts)

1. **First new ADR since Phase 30** (1-phase ADR-cycle interval; ties
   Phase 17→18 + 18→19 + 28→30 + 30→31 = **five 1-phase ADR-cycle
   intervals**; confirms 1-phase cadence as a recurring shape).
2. **ADR-0022 candidate slot claimed at 1-phase age**.
3. **First Vercel Cron infrastructure** in project history.
4. **First scheduled-trigger API endpoint** in project history.
5. **First `app/api/v1/cron/` route prefix** in project history.
6. **First `vercel.json` top-level config file** in project history.
7. **First `lib/cron/` infrastructure module** in project history.
8. **First production transactional email send loop** (Phase 30
   shipped verification + welcome single-recipient-per-click path;
   Phase 31 ships recurring multi-row loop bounded by Resend
   3,000/month free-tier ceiling).
9. **First multi-row INSERT-or-UPDATE loop bounded by external
   rate-limit ceiling** in project history.
10. **First migration that extends an existing table from a prior
    phase** (`subscriber` Phase 30 → `lastDigestSentAt` Phase 31; prior
    new tables were always net-new in their introducing phase).
11. **First non-zero-migration phase 2 phases in a row** = **first
    migration cluster since Phase 15-16** (Phase-15 `displayName`+`bio`
    → Phase-16 `imageOverride` precedent shape).
12. **First constant-time HTTP header comparison** (`checkCronAuth`
    via `crypto.timingSafeEqual` for `Bearer` token check; Phase-30
    `safeCompareTokens` was for DB-token URL-path comparison).
13. **First fully-dep-injected loop driver with vi.fn-based test
    surface** (`runDigestSend` 8-key `DigestSendDeps` interface; tests
    inject 8 mocks; production code path exercised end-to-end without
    touching SQLite or Resend — replaces B.20 DB-helper-deferred scope
    for the digest-send path).
14. **First ADR D-clause to receive a Phase-N+1 APPEND at 1-phase age**
    (ADR-0021 D-F → Phase-31 APPEND via Unit 31.4; tightest source-to-
    APPEND gap in project history; prior APPENDs spanned 5+ phases
    between source and APPEND).
15. **First time the same `DigestPayload` shape is consumed by two
    render targets** (Phase-5 RSS XML side + Phase-31 email HTML side);
    type-level contract preservation across two output formats.
16. **First fourth consecutive new-ADR phase at 6 units** (Phase 19 + 28
    + 30 + 31; confirms 6-unit shape stability for new-ADR phases).
17. **First phase since Phase 9 to NOT add a new Class B category**
    (Phase 9-30 each added at least one new B category surfaced by
    that phase's work; Phase 31 = pure follow-on phase consuming
    Phase-30 B.15 surface; the only B-class movement is item-status
    changes within B.15).
18. **First 2-phase same-shape Q-promotion streak** (Phase 30
    +Q75+Q76 = 1 operational + 1 architectural; Phase 31 +Q77+Q78 =
    identical 1+1 shape).
19. **Third consecutive new-ADR phase to surface an operational-plus-
    architectural Q-pair coupled to a single ADR** (Phase 28
    ADR-0020 → Q73 + Q74; Phase 30 ADR-0021 → Q75 + Q76; Phase 31
    ADR-0022 → Q77 + Q78) — confirms ADR-cycle-coupled Q-promotion-
    pair shape as stable recurring pattern.
20. **Third APPEND-pattern ADR D-clause cluster** (ADR-0013 D-F =
    2 APPENDs Phase 11 + Phase 30; ADR-0018 D-G = 3 APPENDs Phase 22 +
    27 + 29; ADR-0021 D-F = 1 APPEND Phase 31).

### Delta summary (Phase 30 → Phase 31)

- **Commits**: 6 (5 + this gate).
- **ADRs**: 21 → **22** (+1; ADR-0022).
- **Migrations**: 7 → **8** (+1; `0007_subscriber_last_digest_sent_at`;
  second consecutive migration phase = first cluster since Phase 15-16).
- **DB tables**: 7 UNCHANGED (column extension on existing `subscriber`
  table, not new table).
- **Runtime dependencies**: 0 added Phase 31 (server-only Next.js +
  Drizzle + Resend carry forward; no React Email / MJML / event-
  driven runtime).
- **Env vars**: 11 → **12** (+1; `CRON_SECRET`).
- **i18n keys per locale**: 153 UNCHANGED (no new `email` keys Phase 31;
  digest template + cron route are English-only per ADR-0022 D-H FR-
  digest-content deferral).
- **Tests**: 667/60 → **707/62** (+40 / +2 file; Unit 31.2 +17/+1 +
  Unit 31.3 +23/+1).
- **Dynamic page+API routes**: 11 → **12** (+1; `/api/v1/cron/digest-send`).
- **Source files in `lib/email/templates/`**: 3 (Phase-30 verification
  + welcome) → **4** (+digest); test files **6** (+digest.test.tsx).
- **Source files in `lib/subscribers/`**: 1 UNCHANGED (extension within
  existing file; +2 new exports + 6 new tests).
- **Source files in `lib/cron/`**: 0 → **2** (NEW directory; +digest-send.ts
  source + digest-send.test.ts; **first `lib/cron/`** in project history).
- **Top-level config files**: +1 `vercel.json` (NEW; **first
  `vercel.json`** in project history).
- **First Load JS shared chunk**: **103 kB UNCHANGED** end-to-end
  through every Phase 9-31 unit (**42nd consecutive unit at 103 kB**).
- **Middleware bundle**: 160 kB UNCHANGED since Phase 12.
- **Open questions**: 26 resolved + 4 lean + 33 open → 26 resolved + 4
  lean + **35 open** = **65 total** (+2 net; Q77 + Q78).
- **Class B categories**: 14 UNCHANGED (Phase 31 = first phase since
  Phase 9 to NOT add a new B category).
- **Class B items**: ~5 → ~4 (B.15 item 1 + item 2 CLOSED; item 3
  partial close; items 4 + 5 carried).
- **Patience-signal closures**: +1 (B.15 item 1 closed at 1-phase
  carryover — tightest follow-on cadence; mirrors Phase 21/23/25/27/29).

### Surviving follow-ons (Class B; ~4 items across 14 categories)

| Category | Item | Notes |
|---|---|---|
| B.15 (Phase-30 subscriber-list) | 4. Stale verification-token cleanup job | Phase 32+ (second cron entry in `vercel.json`) |
| B.15 (Phase-30 subscriber-list) | 5. Subscribe-endpoint rate limiting | Phase 32+ if spam |

Plus 8 Class B items carried unchanged from Phase 30 close (B.1 + B.2
+ B.3 + B.4 + B.5 + B.8 + B.9 + B.10 + B.11 + B.13 + B.14).

### Phase-32+ Q-candidates flagged (13 total)

7 carried from Phase 27 (Q60 / Q61 / Q62 / Q64 / Q65 / Q68 expansion /
Q72) + 2 carried from Phase 28 (Q73 + Q74) + 2 carried from Phase 30
(Q75 + Q76) + **2 NEW Phase 31 (Q77 + Q78)**.

### DB-migration trigger re-eval at Phase 32 kickoff

- §5.7 trigger (a): FIRED Unit 9.6 carried.
- §5.7 trigger (b): FIRED Phase 30 (subscriber table) + Phase 31
  (`lastDigestSentAt` column extension) — **2-phase fire streak**;
  cold again at Phase 32 entry unless next thread requires migration.

### Risk surface at Phase 31 close

**5 operational gates pending** (unchanged from Phase 30):
- Q54 GitHub OAuth registration.
- Q55 Turso production DB.
- Q69 Vercel Blob token.
- Q73 Google OAuth registration.
- Q75 Resend account + domain DKIM/SPF/DMARC.

**+1 NEW operational gate Phase 31**:
- **Q77 Vercel Cron setup + `CRON_SECRET` provisioning** — cron route
  returns 401 + `missing_cron_secret_env` reason until resolved; **no
  weekly digest sends until provisioned**.

**Total operational gates pending at Phase 31 close: 6** (was 5 at
Phase 30 close).

**3 architectural Phase-32+ candidates pending**:
- Q74 non-GitHub users as curators.
- Q76 per-user-account-based subscriptions.
- **Q78 NEW Phase 31** digest-send analytics + observability (6
  candidate sub-threads enumerated; 3 resolution profiles documented;
  Phase-31 lean = Profile A Resend-webhook-first).

**Resend free-tier ceiling**: 3,000/month × 4 weekly = ~750 subscribers
maximum on free tier. **Q75 monitoring sub-step** + **Q78 architectural
candidate** address this; production cron route response surfaces
`sent: N` for observability per ADR-0022 D-G.

**Cron at-least-once delivery semantics**: documented as known
limitation in ADR-0022 Negative consequences; theoretical double-send
race window under concurrent cron invocations mitigated by SQLite-
serialized per-row UPDATE on `lastDigestSentAt`; row-level lock pattern
deferred Phase 32+ per ADR-0022 D-H.

All other Phase-30 risk-surface items carry forward unchanged.

### Boundary statement (Phase 31 anti-scope)

NOT the new ADR-0023 (slot stays open); NOT production cron operation
(Q77 operational; Phase 31+); NOT digest-send analytics / Resend
webhook integration (Q78 architectural; Phase 32+); NOT retry queue
for transient send failures (Phase 32+ per ADR-0022 D-H); NOT per-
subscriber preferred send-day (Phase 32+); NOT digest content
customization (Phase 32+); NOT React Email / MJML template adoption
(Phase 32+ if template count grows beyond ~5); NOT FR translation of
digest content (Phase 32+ curator-track); NOT per-problem subscription
(Phase 32+ Q76 expansion); NOT authenticated `users.id` link on
subscriber rows (Phase 32+ Q76); NOT cross-domain aggregated weekly
summary (Phase 32+); NOT admin / curator UI for digest analytics
(Phase 32+); NOT manual / curator-triggered "send now" override
(Phase 32+); NOT Resend tier upgrade automation (Phase 32+); NOT
row-level lock for concurrent-cron-invocation race (Phase 32+;
documented as known limitation); NOT bounce / complaint webhook
handling (Phase 32+); NOT stale verification-token cleanup job
(Phase 32+ B.15 item 4); NOT subscribe-endpoint rate limiting (Phase
32+ B.15 item 5).

This phase is the **production send-loop realization** for the Phase-30
subscriber-list email foundation — the cron route ships, the template
ships, the migration ships, the auth contract ships, the idempotency
column ships, the test surface ships. Production cron operation awaits
Q77 operational provisioning (curator-track: `vercel env` CLI + first-
week monitoring per Q77 documentation).

### Parallel-curator awareness

Phase 31 shipped as a **single-session phase** — primary shipped all 6
units 31.0 → 31.5 in one coherent session. **Fifteen consecutive
single-session phases** (Phase 17-31). Phase 16 remains the lone
parallel-session phase. Working tree clean at each unit boundary; no
parallel-session activity observed during Phase 31 work.

### Phase 31 vs prior new-ADR phases

Phase 31 = **fourth 6-unit new-ADR phase** (Phase 19 + 28 + 30 + 31
all 6 units; new-ADR + migration / module-introduction scope).
**6-unit shape is the stable new-ADR-phase rhythm** confirmed across
4 consecutive new-ADR phases.

Phase comparison (new-ADR phases, lightest → heaviest):
- **Phase 19** (lightest): ADR-0019 image-transcoding via `sharp`;
  extends existing `lib/storage/putAvatar`; no new module + no new
  table + no new migration + no new env var + minimal test delta.
- **Phase 28** (mid-weight): ADR-0020 multi-provider OAuth; extends
  existing `lib/auth/`; no new module + no new table + no new
  migration + +2 env vars + moderate test delta.
- **Phase 30** (heaviest until Phase 31): ADR-0021 subscriber-list
  email foundation; +2 new `lib/` modules (`lib/email/` + `lib/subscribers/`)
  + 1 new DB table (`subscriber`) + 1 migration + 3 new API routes
  + 1 new UI component + 4 new test files + 2 new env vars + new
  i18n namespace + 1 new runtime dep + new operational gate (Q75).
- **Phase 31** (NEW upper-bound): ADR-0022 weekly digest scheduler;
  +1 new `lib/` module (`lib/cron/`) + 1 new template (`lib/email/templates/digest.tsx`)
  + 1 selector extension (`lib/subscribers/getVerifiedSubscribersForDomain`)
  + 1 new top-level config file (`vercel.json` — first in project
  history) + 1 new API route (cron) + 1 column extension migration
  + 1 new env var (`CRON_SECRET`) + new operational gate (Q77) +
  new architectural Q (Q78) + 5-surface composition realized
  (`lib/digest/build-digest` Phase 5 + `lib/email/sendEmail` Phase 30
  + `lib/subscribers/` Phase 30 + `lib/email/templates/digest` Phase
  31 + `lib/cron/digest-send` Phase 31). **Phase 31 ties Phase 30 as
  heaviest new-ADR phase** (different shape: Phase 30 = new-module-
  heavy + new-table; Phase 31 = composition-heavy + first-Vercel-
  infrastructure). Together Phase 30 + 31 = the **subscriber-list
  email full-stack realization** — foundation phase + production-send
  phase pair = 12 units across 2 phases delivering: ADR-0021 +
  ADR-0022 + `lib/email/` + `lib/subscribers/` + `lib/cron/` + 1 DB
  table + 2 migrations + 4 API routes + 1 UI component + 11 i18n
  keys + 1 new runtime dep + 3 new env vars + 4 new operational/
  architectural Qs.

### Phase 32 entry conditions

Per §12 cardinal rule, **explicit human sign-off required**. **Twenty-
seventh "Continue" override opportunity** in Unit 32.0 prep when that
unit is authored. Thread options for Phase 32+ (overridable per §12):

- **A. Q68 expansion content moderation** (~3-5 units; ADR-0023
  candidate; 10+ phase carryover — strongest non-just-surfaced
  patience signal).
- **B. Q78 architectural Phase-32+ digest-send analytics + observability**
  (~3-5 units; ADR-0023 candidate; Profile A Resend-webhook-first
  lean per Unit 31.4; tightest 1-phase Q-carryover but waits for
  first-week production cron signal).
- **C. Q76 architectural Phase-32+ per-user-account subscriptions**
  (~2-4 units; ADR-0023 candidate; 1-phase Q-carryover; couples to
  authenticated-user flow Phase 32+).
- **D. Markdown evolution ADR** (Q72 + Phase-29 B.14 wikilink couple;
  ~3-5 units).
- **G. Q64 per-user privacy opt-out** (~3 units; 14+ phase carryover).
- **J-other. Account-merge UI** (ADR-0020 D-E; ~2-3 units; 4-phase
  age).
- **K-other. Third+ provider expansion** (ADR-0020 D-B amendment).
- **L. Account-deletion blob cleanup** (Phase-16 carryover).
- **M. SiteHeader avatar pill** (Phase-14 carryover).
- **N. v2 methodology authoring**.
- **B.15 item 4. Stale-verification-token cleanup job** (~1-2 units;
  Phase 32+ B item; second `vercel.json` cron entry).

**B.15 item 1 + 2 CLOSED Phase 31** at tightest 1-phase follow-on
cadence; **B.15 item 3 partial close** via Unit 31.3's dep-injection
coverage. **Q78** is the natural next-step at 1-phase Q-carryover
mirroring the Phase 21/23/25/27/29/31 tight-follow-on cadence; but
waits for first-week production cron signal to inform the Profile A vs
B vs C decision (per Q78 documentation). **Q68 expansion** is the next-
strongest non-just-surfaced patience signal at 10+ phases. **Q64** is
the longest-carrying open architectural question at 14+ phases.

### Surfaces ready for Phase 32+ consumption

Phase 31 + 30 together establish a rich surface for Phase 32+:

- **`lib/cron/digest-send`** Phase 31 — extendable for second cron job
  (stale-token cleanup B.15 item 4 + retry queue Q78 Profile C).
- **`lib/email/templates/`** Phase 30 + 31 (3 templates: verification +
  welcome + digest) — adding 4th template (e.g., reset-subscription,
  digest-summary) extends sibling-pattern.
- **`subscriber` table** Phase 30 + 31 (12 columns) — extendable with
  `userId` FK Phase 32+ Q76 + `bounceCount` Phase 32+ Q78 + per-row
  preferred-send-day Phase 32+ D-H.
- **`vercel.json` `crons` array** Phase 31 — extends with second entry
  for stale-token cleanup or other scheduled jobs.
- **`CRON_SECRET` env var** Phase 31 — shared secret for ALL future
  cron endpoints (not per-cron-job; single secret for the project's
  cron surface).
- **`runDigestSend` orchestrator pattern** Phase 31 — fully-dep-
  injected loop driver shape extends to future scheduled-job
  orchestrators (stale-token cleanup, bounce-handling, etc.).

### Phase 31 → Phase 32 sign-off requirement

Per `MASTER_PROMPT.md` §12 cardinal rule: **explicit human sign-off
required** before Phase 32 work begins. **27th "Continue" override
opportunity** at this boundary.

> Phase 31 is COMPLETE. All gates green. Surface this acceptance gate
> + boundary statement to the curator and **WAIT for explicit
> "Continue" override** before authoring Unit 32.0 (Phase 32 prep) —
> OR — for the curator to redirect to a different thread / pause the
> project / close-Phase-N-without-Phase-32-kickoff.
