# 8.7 — Phase-8 hygiene status pass

Seventh Phase-8 unit. Docs-only. Mirrors the Phase-5 (Unit 5.11), Phase-6 (Unit 6.8), and Phase-7 (Unit 7.9) hygiene passes. Catalogs **Class A** in-flight Phase-8 items, **Class B** Phase-8-specific follow-ons that survive the phase, and **Class C** carryovers from prior phases. Lands before Unit 8.8 (OPEN_QUESTIONS hygiene) and 8.9 (acceptance gate); the gate's "follow-ons that survive" section will pull directly from this catalog.

## THINK

### Class A — In-flight Phase-8 items (1)

- **Unit 8.4 — `useTranslations` for LocaleToggle aria-label**. **Deferred indefinitely** at Unit 8.5 commit time. Depends on SiteHeader being rendered under `NextIntlClientProvider`, which depends on the HTML shell migration (moving `<html>` / `<body>` / fonts / `ThemeProvider` / `SiteHeader` from `app/layout.tsx` into `app/[locale]/layout.tsx`). The shell migration was originally planned for Unit 8.1 but **dropped mid-flight** when a parallel session signaled (via system reminder) that the existing `app/layout.tsx`-owns-`<html>` structure was intentional. Without the shell migration, Unit 8.4 has no tractable scope (`useTranslations` would throw at render time outside the provider). Unit 8.5's CHANGELOG entry inline-documented the deferral; this unit re-surfaces it as Class A so the acceptance gate (8.9) carries it forward. **Unblock**: explicit curator authorization to move the HTML shell.

### Class B — Phase-8-specific follow-ons (12)

Items that survive Phase 8 because they're either scope-creep (out of the Phase-8 thread's "rollout completion" framing), waiting on an architectural unblock that wasn't granted this phase, or tactical hygiene whose threshold hasn't been crossed.

1. **HTML shell migration into `app/[locale]/layout.tsx`** — the architectural unblock for Unit 8.4. Closes Unit 7.7's documented `html-has-lang` axe-rule risk. Survives Phase 8 by parallel-session signal preservation.
2. **`<html lang={locale}>`** on the root layout (consequence of 1).
3. **SiteHeader moves under `NextIntlClientProvider`** (consequence of 1; unblocks `useTranslations` in Phase-7's hardcoded aria-label strings).
4. **Fallback-hint UI for `didFallback === true`** — `lib/i18n/load-localized.ts` from Unit 7.5 returns the flag, but no consumer renders the "this page is not yet translated" hint banner today. ADR-0011 D-D pinned the hint copy; deferred since Unit 7.5; survived Unit 8.6 (`/contributing/v1.0.0` triggers `didFallback` but renders silently). Class B follow-on; future curator-track or a focused i18n-UI unit.
5. **`content/contributing/v1.0.fr.mdx`** (older-version FR translation). Curator-track per Q51 lean; users hitting `/fr/contributing` (default landing) already get the FR via v1.1; v1.0 is a historical snapshot.
6. **Bulk content backfill** — translating problem.yaml + paper.yaml + per-problem MDX (`background.fr.mdx`, `definition.fr.mdx`, `history.fr.mdx` × 10 problems = 30+ MDX files) + 30 paper YAMLs + 4 issue-template forms. Q51 curator-track horizon; not a phase-level deliverable.
7. **`messages.{contributing,methodology,…}.*` chrome strings** — breadcrumb labels ("Contributing", "Methodology", "Other versions:"), section headings, "← back to" links, citation block labels. Hardcoded EN today across most pages (only the home page got the full i18n treatment in Unit 8.2). Per-page i18n string extraction is a focused unit per surface; deferred.
8. **`StatusPill` localization** — enum labels (open / partially-solved / converging / solved / retired) display in EN today. Phase-8 scope didn't cover component-side enum mappings; deferred.
9. **`components/recently-rated/` empty-state copy** — "No rating actions yet — see /contributing." Component-side; multiple call sites; deferred.
10. **Site-header nav labels** via `useTranslations` — `messages.nav.*` keys exist (from Unit 7.2) but `SiteHeader` uses hardcoded EN strings ("Domains", "Problems", "Methodology", "Trending"). Blocks on 8.4 (SiteHeader-under-provider).
11. **Trailing-slash normalization for `NEXT_PUBLIC_SITE_URL`** — if a curator sets `NEXT_PUBLIC_SITE_URL=https://example.com/`, the resulting concatenation `${SITE}/about` produces `https://example.com//about` (double slash). Unit 8.5 documented this in the `lib/site-url.ts` comment but didn't add a runtime guard. Defer until first observed footgun.
12. **Per-entry sitemap `lastModified` / `changeFrequency` / `priority`** — observation-driven per Unit 7.8 follow-on. Google deprecated reliance on the latter two in 2023; no Lighthouse SEO signal flagging the omission yet. Deferred.

Additional Phase-7 follow-ons that **continue to survive**:

- **First LHCI run for the 5 new locale-pilot URLs**: `/en/about`, `/fr/about`, `/en/methodology`, `/fr/methodology`, `/fr/contributing`. Unit 7.7 enrolled `[locale]/about`; Unit 7.5a enrolled `[locale]/methodology`; Unit 8.6 enrolled `/fr/contributing`. URL count holds at 19. Real LHCI run lands on first PR with the unit-8.x commits. Documented `html-has-lang` axe-rule risk on FR pages (Unit 7.7 flag) survives — same root cause as Class B item 1.
- **Visual-regression baselines** for the locale-pilot pages — `chromium-win32` baselines not captured this phase (no Playwright spec changes). Future PR can pass `playwright test --update-snapshots`.

### Class C — Carryover deferrals (unchanged from Unit 7.9)

These are pre-Phase-8 deferrals that survived this phase. No status change.

1. **Orphan `components/domain-tile-grid/` deletion** — verified still orphan at HEAD; deletion gated on explicit curator authorization per destructive-action policy.
2. **`entries.json` backfill on 8 problems** — still 2/10 have entries; curator-driven with `ANTHROPIC_API_KEY` per Phase-5 D-3.
3. **`pnpm clean-drafts` script** — operational signal not yet met (`drafts/` < 100 stale files locally).
4. **`<managingEditor>` on RSS feeds** (Q33 / Q44) — coupled to Q2 (DNS / project email).
5. **Phase-2 ROR-ID + InstaDeep orphan** — curator-driven hygiene; unchanged.
6. **W3C feed validator pass** against deployed `/api/v1/rss.xml` + `/api/v1/digest/<slug>` — Phase-3 / Phase-5 / Phase-6 / Phase-8 compound; first preview deploy + Q47 unblock.
7. **`/digest` + `/problems/[slug]/talk` Playwright baselines** — Phase-6 carryover; visual baselines.
8. **Real-API integration smoke for `lib/discussions/github-graphql.ts`** — Q47 + `GITHUB_TOKEN` in CI.
9. **`NEXT_PUBLIC_GISCUS_REPO_ID` env wiring + repo Discussions enablement** — Phase-6 Q47 operational gate.

### Phase-8 surface delta vs Phase-7 close

- **Routes**: 341 → ~590 (+~245 from locale-doubling). Every page route lives under `/[locale]/...` per Unit 8.1's `localePrefix: "always"`.
- **Tests**: 383 → **388** (+5 net: +60 in Phase 7's bulk-migration tests minus zero in Phase-8 (Unit 8.1 net was 60 - 56 = 4 retained from existing pages; Unit 8.5 net +4; Unit 8.2 / 8.3 / 8.6 no test deltas).
  - Wait — let me re-check. At Phase-7 close: 383 tests. After Unit 8.1's TALK_PATHNAME_REGEX refactor: +1 net (existing 4 cases refactored into 3 + 1 new locale-prefix coverage = +1). After Unit 8.5: +4 net (sitemap test rewrite). 383 + 1 + 4 = **388**. ✓
- **First Load JS shared chunk**: **103 kB UNCHANGED** through every Phase-8 unit. The `next-intl` Link wrapper, middleware bundle (51.8 kB server-side), and resolveLocalized refactors add zero net bytes to the shared chunk.
- **ADRs**: 11 (unchanged; no ADRs added in Phase 8).
- **Dependencies**: no new dependencies in Phase 8.
- **LHCI URLs**: 18 → **19** (+1 from Unit 8.6 enrolment of `/fr/contributing`).
- **Content**: +1 FR MDX (`content/contributing/v1.1.fr.mdx` from Unit 8.6). Total content files: 203 schema-validated + 36 raw MDX (was 35 at Phase-7 close; +1) = 239 raw content files. Still well under the 600-file DB-trigger threshold.

### Phase-8 unit ordering (actual landing order)

| Unit | Title                                                              | Commit    |
| ---- | ------------------------------------------------------------------ | --------- |
| 8.0  | Phase 8 prep                                                       | `0ee1ad9` |
| 8.1  | Bulk page migration + middleware (`localePrefix: "always"`)        | `31ea2d5` |
| 8.2  | Home-page FR hero translation + `messages.home.*`                  | `defb122` |
| 8.3  | Explicit `NEXT_LOCALE` cookie config on middleware                 | `5e2b509` |
| 8.4  | **DEFERRED** (blocked on HTML shell migration)                     | —         |
| 8.5  | `lib/site-url.ts` extraction + universal sitemap alternates        | `90bd3c3` |
| 8.6  | `/contributing` FR pilot (second sibling-file consumer)            | `bad59fa` |
| 8.7  | Phase-8 hygiene status pass (this unit)                            | —         |
| 8.8  | OPEN_QUESTIONS hygiene + ADR review                                | —         |
| 8.9  | Phase 8 acceptance gate                                            | —         |

Total Phase-8 commits: **7 code/docs units** (8.0 – 8.3, 8.5, 8.6, this) + **2 closing units** (8.8 + 8.9). Unit 8.4 deferred indefinitely. Numbering preserved (8.3 → 8.5 jump explicit; 8.4 acceptance-gate ledger records the deferral).

### Risk surface at HEAD

- **`html-has-lang` axe-rule mismatch** on `/fr/...` pages — surface inherited from Phase 7; survives Phase 8. Will surface on first CI Lighthouse run if axe is strict.
- **SiteHeader can't use `useTranslations`** — Class B item 10; nav labels stay hardcoded EN under `/fr/...` until 8.4 unblocks.
- **`/fr/contributing/v1.0.0` renders mixed-language page** (EN body, FR site-header chrome) — acceptable intermediate state per Q51 lean; surfaces an architectural seam between layout-chrome i18n and content-side FR backfill.

### Boundary statement

NOT the bulk migration (Unit 8.1 closed it), NOT the FR content backfill (Q51 curator-track horizon), NOT destructive cleanup (Class C item 1 still gated on curator authorization), NOT the acceptance gate (Unit 8.9 is the phase-close gate). This unit is the **catalog**, not the **resolution**.

### Smoke gates

Docs-only; unchanged from Unit 8.6 snapshot (388/388 tests; ~590 routes; 103 kB First Load JS; 19 LHCI URLs; 0 errors / 6 warnings audit).

- File added: this THINK doc.
- CHANGELOG: Unit 8.7 entry (Class A/B/C catalog).
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).
