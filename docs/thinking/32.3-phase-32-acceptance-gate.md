# 32.3 — Phase 32 acceptance gate (stale verification-token cleanup job realized; **22 ADRs**; 4 units; closes Phase-30 B.15 item 4 at 2-phase carryover; first NO-NEW-ADR phase since Phase 29; second `vercel.json` cron entry; validates Phase-31 cron infrastructure reusability claim; first 0-Q-promotion phase since Phase 29; 28th "Continue" override opportunity at Phase 32 → 33 boundary)

Fourth and final Phase-32 unit; docs-only. Closes Phase 32 (**twenty-
third NON-§13 phase**; stale verification-token cleanup job realized
via Phase-31 cron pattern reuse + new `/api/v1/cron/cleanup-stale-tokens`
route + second `vercel.json` cron entry + new `lib/cron/cleanup-stale-tokens.ts`
orchestrator + 5 new tests + ADR-0022 D-D APPEND note). Mirrors
Phase-12 through Phase-31 acceptance-gate patterns. **§13 ledger
CLOSED at Unit 9.9** (carried unchanged through Phases 10-31; Phase
32 inferred-not-§13 like its twenty-two predecessors).

## THINK

### Phase 32 ledger

**4 units shipped** (32.0 prep → 32.3 gate). **0 deferrals**; **0
scope drift**. **Twenty-first consecutive well-scoped phase**. **First
4-unit phase since Phase 29** = 3-phase gap; confirms 4-unit shape
as stable no-new-ADR phase rhythm.

| Unit | Title | Type | Commit |
|---|---|---|---|
| 32.0 | Phase 32 prep | docs | `337ecb8` |
| 32.1 | cleanup-stale-tokens cron + vercel.json | code | `e3a869a` |
| 32.2 | Phase-32 hygiene + ADR-0022 D-D APPEND | docs | `969fe24` |
| 32.3 | Phase 32 acceptance gate | docs | this commit |

### Final smoke gates (all green at HEAD pre-this-unit `969fe24`)

- `pnpm typecheck` clean.
- `pnpm lint` clean.
- `pnpm test` → **712/712 across 63 vitest files** (+5 / +1 file Δ
  from Phase 31 close 707/62; Unit 32.1 added all +5/+1).
- `pnpm build` → ~659 prerendered pages + **13 dynamic page+API
  routes** (was 12; +1 `/api/v1/cron/cleanup-stale-tokens` at 187 B /
  103 kB First Load JS); **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-32 unit (**43rd
  consecutive unit at 103 kB**); **middleware bundle = 160 kB
  UNCHANGED** since Phase 12; all page bundles UNCHANGED.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline since
  Phase 2; unchanged through every Phase 3-32 unit).

### Phase 31 → Phase 32 architectural firsts (cumulative across the phase; ~10 firsts)

1. **First NO-NEW-ADR phase since Phase 29** = **2-phase new-ADR
   streak break** (Phase 30 + 31); **4-phase new-ADR cluster break**
   (Phase 19 + 28 + 30 + 31).
2. **First reuse of Phase-31 `checkCronAuth` helper** — validates
   the extraction-for-multi-cron-reuse design intent documented in
   ADR-0022 D-D.
3. **First reuse of Phase-31 `runDigestSend` injectable-deps
   orchestrator shape** — `runCleanupStaleTokens` mirrors structure;
   confirms pattern scales additively to a second scheduled job.
4. **Second `app/api/v1/cron/` route prefix** in project history.
5. **Second `crons` entry in `vercel.json`** in project history.
6. **Second `lib/cron/` source file** in project history.
7. **First multi-cron auth pattern realized** — single shared
   secret across multiple scheduled endpoints; single shared
   header-check pure function.
8. **Returns to 0-migration pattern** after the 2-phase Phase-30 +
   31 cluster (mirrors Phase 17-29 long 0-migration runs after short
   Phase 15-16 cluster — confirms the "short cluster bracketed by
   long 0-migration run" recurring rhythm).
9. **First operational-follow-on phase since Phase 27** (Phase 27 =
   last "tight follow-on phase" with operational-only scope before
   the architectural-heavy Phase 28-31 run).
10. **First 4-unit phase since Phase 29** = 3-phase gap (confirms
    4-unit shape as stable no-new-ADR-phase rhythm).
11. **First 0-Q-promotion phase since Phase 29** — breaks 2-phase
    Q-promotion-pair streak (Phase 30 + 31 both +1op+1arch).
12. **First hygiene unit since Phase 17 to ship without
    OPEN_QUESTIONS edits** — confirms hygiene units do NOT always
    require Q-edits.
13. **Fourth APPEND-pattern ADR D-clause cluster** in project
    history (ADR-0022 D-D Phase-32 APPEND; ties ADR-0021 D-F's
    1-phase APPEND gap as tightest in project history).
14. **Two consecutive 1-phase-gap APPENDs** — Phase 30 → 31 ADR-0021
    D-F + Phase 31 → 32 ADR-0022 D-D; establishes tight-gap APPEND
    cadence as recurring shape.
15. **B.15 item 4 CLOSED at 2-phase carryover** — second-tightest
    follow-on cadence after Phase-31's 1-phase close of B.15 item 1.
16. **3 of 5 Phase-30 B.15 items closed within 2 phases of
    surfacing** — confirms B-item-as-follow-on-tracker pattern for
    tight 2-phase windows.

### Delta summary (Phase 31 → Phase 32)

- **Commits**: 4 (3 + this gate).
- **ADRs**: 22 UNCHANGED (Phase 32 = 0 new ADRs; first no-new-ADR
  phase since Phase 29).
- **Migrations**: 8 UNCHANGED (returns to 0-migration pattern after
  Phase 30 + 31 cluster).
- **DB tables**: 7 UNCHANGED.
- **Runtime dependencies**: 0 added Phase 32 (server-only stack
  reused).
- **Env vars**: 12 UNCHANGED (no new env var; reuses `CRON_SECRET`
  for both cron endpoints).
- **i18n keys per locale**: 153 UNCHANGED (no new keys Phase 32).
- **Tests**: 707/62 → **712/63** (+5 / +1 file; all from Unit 32.1
  `cleanup-stale-tokens.test.ts`).
- **Dynamic page+API routes**: 12 → **13** (+1;
  `/api/v1/cron/cleanup-stale-tokens`).
- **Source files in `lib/cron/`**: 1 → **2** (+`cleanup-stale-tokens.ts`)
  + test files 1 → **2** (+`cleanup-stale-tokens.test.ts`).
- **Source files in `app/api/v1/cron/`**: 1 → **2**.
- **`vercel.json` `crons` array entries**: 1 → **2**.
- **First Load JS shared chunk**: **103 kB UNCHANGED** end-to-end
  through every Phase 9-32 unit (**43rd consecutive unit at 103 kB**).
- **Middleware bundle**: 160 kB UNCHANGED since Phase 12.
- **Open questions**: 65 UNCHANGED (first 0-Q-promotion phase since
  Phase 29; breaks 2-phase Q-promotion-pair streak).
- **Class B categories**: 14 UNCHANGED (Phase 32 = first phase since
  Phase 9 to NOT add a new B category — same as Phase 31).
- **Class B items**: ~4 → ~3 (B.15 item 4 CLOSED; item 5 carried).
- **Patience-signal closures**: +1 (B.15 item 4 closed at 2-phase
  carryover — second-tightest follow-on cadence after Phase-31's
  1-phase close).

### Surviving follow-ons (Class B; ~3 items across 14 categories)

| Category | Item | Notes |
|---|---|---|
| B.15 (Phase-30 subscriber-list) | 5. Subscribe-endpoint rate limiting | Phase 33+ if spam |

Plus 8 Class B items carried unchanged from Phase 31 close (B.1 + B.2
+ B.3 + B.4 + B.5 + B.8 + B.9 + B.10 + B.11 + B.13 + B.14 — same set
carrying since Phase 30 close).

**B.15 close-rate summary**: 5 items surfaced Phase 30; 4 of 5
addressed by Phase 32 close (item 1 + 2 closed Phase 31 at 1-phase
carryover; item 3 partial close Phase 31; **item 4 closed Phase 32 at
2-phase carryover**); item 5 carried as Phase-33+ candidate. **80%
B.15 close-rate within 2 phases of surfacing** — confirms the
B-item-as-follow-on-tracker pattern works for tight follow-on windows.

### Phase-33+ Q-candidates flagged (13 total UNCHANGED)

7 carried from Phase 27 (Q60 / Q61 / Q62 / Q64 / Q65 / Q68 expansion /
Q72) + 2 carried from Phase 28 (Q73 + Q74) + 2 carried from Phase 30
(Q75 + Q76) + 2 carried from Phase 31 (Q77 + Q78) + **0 NEW Phase 32**.

### DB-migration trigger re-eval at Phase 33 kickoff

- §5.7 trigger (a): FIRED Unit 9.6 carried.
- §5.7 trigger (b): cold Phase 32 (returns to 0-migration pattern
  after Phase 30 + 31 cluster); next thread chosen Phase 33+ will
  determine trigger (b) re-evaluation (Q76 Option A would require
  migration; Q78 Profile A Resend-webhook-handling would require
  migration; Q64 per-user privacy opt-out would require migration;
  Q68 expansion content moderation would NOT require migration if
  done as policy + lightweight enforcement).

### Risk surface at Phase 32 close

**6 operational gates pending** (unchanged from Phase 31; Q77 covers
both cron endpoints — no new operational gate Phase 32):
- Q54 GitHub OAuth registration.
- Q55 Turso production DB.
- Q69 Vercel Blob token.
- Q73 Google OAuth registration.
- Q75 Resend account + domain DKIM/SPF/DMARC.
- Q77 Vercel Cron `CRON_SECRET` + first-week monitoring (Q77 now
  covers BOTH cron endpoints — single shared secret).

**3 architectural Phase-33+ candidates pending** (unchanged from Phase
31 close):
- Q74 non-GitHub users as curators.
- Q76 per-user-account-based subscriptions.
- Q78 digest-send analytics + observability.

All other Phase-30-31 risk-surface items carry forward unchanged.

### Boundary statement (Phase 32 anti-scope)

NOT a new ADR (ADR-0023 candidate slot stays open; carries to Phase
33+); NOT production cron operation (Q77 operational; covers both
endpoints); NOT digest-send analytics / Resend webhook integration
(Q78 architectural; Phase 33+); NOT retry queue for transient send
failures (Phase 33+ per ADR-0022 D-H); NOT per-subscriber preferred
send-day / digest content customization / React Email / MJML / FR
digest i18n / per-problem subscription / authenticated `users.id`
link / cross-domain aggregated summary / admin curator UI for digest
analytics / manual send-now override / Resend tier upgrade
automation / row-level lock for concurrent-cron race / bounce-
complaint webhook handling (all Phase 33+); NOT subscribe-endpoint
rate limiting (Phase 33+ B.15 item 5 if spam); NOT content moderation
Q68 expansion (Phase 33+ if user-base signals); NOT Q64 per-user
privacy opt-out (Phase 33+); NOT Q76 per-user-account subscriptions
(Phase 33+ third consecutive migration phase would be unprecedented);
NOT account-merge UI ADR-0020 D-E (Phase 33+); NOT third+ provider
expansion ADR-0020 D-B amendment (Phase 33+); NOT markdown evolution
ADR (Phase 33+); NOT v2 methodology authoring (curator-track); NOT
account-deletion blob cleanup (Phase 33+); NOT SiteHeader avatar pill
(Phase 33+); NOT cleanup-cron schedule promotion (daily vs weekly
deferred Phase 33+ if accumulation signal warrants).

**This phase is the operational-follow-on phase** that validates the
Phase-31 cron infrastructure's reusability claim. The cleanup cron
ships, the second `vercel.json` entry ships, the single-shared-secret
auth contract ships, the orchestrator pattern reuse is realized,
the test surface ships. Production cleanup operation awaits Q77
operational provisioning (same gate as Phase-31 digest-send; no
additional operational work Phase 32).

### Parallel-curator awareness

Phase 32 shipped as a **single-session phase** — primary shipped all
4 units 32.0 → 32.3 in one coherent session. **Sixteen consecutive
single-session phases** (Phase 17-32). Phase 16 remains the lone
parallel-session phase. Working tree clean at each unit boundary;
no parallel-session activity observed during Phase 32 work.

### Phase 32 vs prior no-new-ADR phases

Phase 32 = **eighth no-new-ADR phase** in project history (Phase 20
+ 21 + 22 + 23 + 24 + 25 + 26 + 27 + 29 = 9 prior; Phase 32 = the
10th — wait, let me recount). Looking back:

- Phase 19 = NEW-ADR (ADR-0019)
- Phase 20 = no-new-ADR
- Phase 21 = no-new-ADR
- Phase 22 = no-new-ADR
- Phase 23 = no-new-ADR
- Phase 24 = no-new-ADR
- Phase 25 = no-new-ADR
- Phase 26 = no-new-ADR
- Phase 27 = no-new-ADR
- Phase 28 = NEW-ADR (ADR-0020)
- Phase 29 = no-new-ADR
- Phase 30 = NEW-ADR (ADR-0021)
- Phase 31 = NEW-ADR (ADR-0022)
- Phase 32 = no-new-ADR

Phase 32 = **9th no-new-ADR phase** (Phases 20-27 + 29 = 9 prior;
Phase 32 = the 10th). **9-phase no-new-ADR run Phase 20-27 + 29** is
the longest such run in project history; Phase 32 = the next entry
in the no-new-ADR cadence. **First no-new-ADR phase since Phase 29**
= 2-phase gap (Phase 30 + 31 = 2-phase new-ADR mini-cluster bracketed
by Phase 29 + Phase 32 no-new-ADR phases). **The 2-phase new-ADR
mini-cluster shape** = first such mini-cluster in project history;
prior new-ADR-with-gap-1 events (Phase 17→18→19) were 3-phase
mini-clusters; Phase 28→30→31 = the second 3-phase mini-cluster
(if we count the 2-phase gap; otherwise Phase 30 + 31 is the first
2-phase mini-cluster).

Phase length comparison (no-new-ADR phases):
- **Phase 20-27 + 29** (Phase 23 + 24 + 25 + 26 + 27 + 29): 4-unit
  phases.
- **Phase 21 + 22**: 5-unit phases.
- **Phase 20**: heavier (5 units; Q57 + ADR-0014 D-D scoped).
- **Phase 32**: 4-unit (mirrors the Phase 23-27 + 29 stable rhythm).

### Phase 33 entry conditions

Per §12 cardinal rule, **explicit human sign-off required**. **Twenty-
eighth "Continue" override opportunity** in Unit 33.0 prep when that
unit is authored. Thread options for Phase 33+ (overridable per §12):

- **A. Q68 expansion content moderation** (~3-5 units; ADR-0023
  candidate; 11+ phase carryover at Phase 32 close — strongest
  non-just-surfaced patience signal).
- **B. Q78 architectural Phase-33+ digest-send analytics +
  observability** (~3-5 units; ADR-0023 candidate; Profile A
  Resend-webhook-first lean per Unit 31.4; **waits for first-week
  production cron signal from Q77** before Profile A vs B vs C
  decision).
- **C. Q76 architectural Phase-33+ per-user-account subscriptions**
  (~2-4 units; ADR-0023 candidate; 2-phase Q-carryover; **third
  migration phase would be unprecedented** — Phase 30 + 31 = 2-
  phase cluster; extending to Phase 33 = unprecedented 3-phase
  cluster; couples to authenticated-user flow).
- **D. Markdown evolution ADR** (Q72 + Phase-29 B.14 wikilink couple;
  ~3-5 units).
- **G. Q64 per-user privacy opt-out** (~3 units; **15+ phase
  carryover — longest open architectural question**).
- **J-other. Account-merge UI** (ADR-0020 D-E; ~2-3 units; 5-phase
  age).
- **K-other. Third+ provider expansion** (ADR-0020 D-B amendment).
- **L. Account-deletion blob cleanup** (Phase-16 carryover).
- **M. SiteHeader avatar pill** (Phase-14 carryover).
- **N. v2 methodology authoring** (curator-track).
- **B.15 item 5. Subscribe-endpoint rate limiting** (~1-2 units;
  Phase-33+ if spam observed).

**Q68 expansion** is the **strongest non-just-surfaced patience
signal** at 11+ phases — the natural next-step thread for Phase 33+
after the operational-follow-on Phase 32. Q78 + Q76 are tighter
Q-carryovers but each blocked by demand-signal-first concerns (Q78
waits for Q77 production signal; Q76 third migration phase
unprecedented).

### Surfaces ready for Phase 33+ consumption

Phase 31 + 32 together establish a multi-cron infrastructure surface
ready for Phase 33+:

- **`lib/cron/` directory** (2 source files Phase 32) — extends to
  3rd+ cron jobs (retry queue Q78 Profile C; bounce-handling Q78
  Profile A; etc.).
- **`vercel.json` `crons` array** (2 entries Phase 32) — extends
  additively for future scheduled jobs.
- **`checkCronAuth` shared helper** + **single `CRON_SECRET`** —
  validated by Phase 32 multi-cron reuse; 3rd+ cron jobs use the
  same pattern.
- **`runDigestSend`-shaped orchestrator pattern** — `runCleanupStaleTokens`
  confirmed Phase 32 = scales additively; 3rd+ orchestrators follow
  the injectable-deps + structured-result shape.
- **`app/api/v1/cron/` route prefix** — 2 routes Phase 32; thin
  POST-handler-delegating-to-lib pattern.

### Phase 32 → Phase 33 sign-off requirement

Per `MASTER_PROMPT.md` §12 cardinal rule: **explicit human sign-off
required** before Phase 33 work begins. **28th "Continue" override
opportunity** at this boundary.

> Phase 32 is COMPLETE. All gates green. Surface this acceptance gate
> + boundary statement to the curator and **WAIT for explicit
> "Continue" override** before authoring Unit 33.0 (Phase 33 prep) —
> OR — for the curator to redirect to a different thread / pause the
> project / close-Phase-N-without-Phase-33-kickoff.
