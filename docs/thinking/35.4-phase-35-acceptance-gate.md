# 35.4 — Phase 35 acceptance gate (Q68 expansion content moderation as framework-only ADR-0024 with NoopModerator default + 4-surface extension-point wiring; **24 ADRs**; 5 units; **first framework-only ADR in project history**; **first multi-surface extension-point wiring from a single ADR**; **first demand-signal-first-dissolution-by-zero-cost-default pattern**; **first sub-bullet-only Q resolution**; **first 3-APPEND single unit**; **5 consecutive phases shipping APPENDs**; 31st "Continue" override opportunity at Phase 35 → 36 boundary)

Fifth and final Phase-35 unit; docs-only. Closes Phase 35 (**twenty-
sixth NON-§13 phase**; Q68 expansion content moderation realized as
framework-only ADR-0024 with `NoopModerator` default + 4-surface
extension-point wiring + 3 ADR APPENDs + Q68 expansion sub-bullet
resolution at 3 deferral list locations). Mirrors Phase-12 through
Phase-34 acceptance-gate patterns. **§13 ledger CLOSED at Unit 9.9**
(carried unchanged through Phases 10-35).

## THINK

### Phase 35 ledger

- **5 units shipped** (35.0 prep → 35.4 gate). **0 deferrals**;
  **0 scope drift** (one path-correction at Unit 35.2 absorbed into
  ADR-0019 D-F APPEND without disrupting prep estimates).
- **Twenty-fourth consecutive well-scoped phase**.
- **One ADR shipped** (ADR-0024 content moderation; framework-only).
  ADR count 23 → 24.
- **Three ADR D-clauses APPENDed** (ADR-0016 D-B + ADR-0017 D-H +
  ADR-0019 D-F; all first APPENDs for those clauses).
- **One sub-bullet-only Q resolution** (Q68 expansion; updated at
  Q63 + Q67 + Q70 deferral list sub-bullets).
- **One new `lib/<module>/` directory** (`lib/moderation/` with 5
  files: types + noop + index + 2 test files).
- **Four user-content surfaces wired** (bio + avatar + rating-
  challenge + subscribe) with `getModerator()` extension-point
  integration.
- **One env-var added** (`MODERATION_PROVIDER` default `noop`); env-
  var count 12 → 13.
- **+11 tests / +2 test files** cumulative across the phase (722/63
  → 733/65); split: Unit 35.1 +8/+2 + Unit 35.2 +3/+0.

### Final smoke gates (all green at HEAD pre-this-unit `1694f72`)

- `pnpm typecheck` clean.
- `pnpm lint` clean.
- `pnpm test` → **733/733 across 65 vitest files** (+11 / +2 file Δ
  from Phase 34 close 722/63; Unit 35.1 added +8/+2 + Unit 35.2 added
  +3/+0).
- `pnpm build` → ~659 prerendered pages + **13 dynamic page+API
  routes UNCHANGED**; **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-35 unit (**48th
  consecutive unit at 103 kB**); **middleware bundle = 160 kB
  UNCHANGED** since Phase 12; all page bundles UNCHANGED.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline since
  Phase 2; unchanged through every Phase 3-35 unit + the 2 pre-
  Phase-35 housekeeping docs commits).

### Phase 34 → Phase 35 architectural firsts (cumulative across the phase; ~12 firsts)

1. **First framework-only ADR in project history** — ADR-0024
   commits to `ContentModerator` interface without provider in
   same ADR. Establishes playbook for future framework-first
   decisions (Q72 markdown evolution; future deferred-provider
   Qs).
2. **First multi-surface extension-point wiring from a single ADR**
   — Unit 35.2 wires 4 surfaces (bio + avatar + rating-challenge +
   subscribe) in one architectural unit vs accreted-via-copy
   pattern of `safeAuth()` (4 copies across 4 phases).
3. **First "demand-signal-first concern dissolved by zero-cost-
   default framework" pattern** — Q68 was deferred 12+ phases on
   "no users yet → no abuse signals"; noop default reduces pre-
   position cost to zero, dissolving the concern.
4. **First sub-bullet-only Q resolution** in project history — Q68
   expansion was never promoted to top-level `## Q68. ...` heading;
   resolution edits sub-bullets at Q63 + Q67 + Q70 deferral lists.
5. **First 3-APPEND single unit** in project history — ADR-0016 D-B
   + ADR-0017 D-H + ADR-0019 D-F all receive their first APPEND
   simultaneously in Unit 35.3.
6. **First `lib/<module>/` with no-op default** rather than
   committed provider (vs `lib/email/` Resend + `lib/cron/` Vercel
   Cron pattern).
7. **First throw-on-unknown-config factory pattern** — fail-loud
   on misconfiguration; mirrors Phase-9 `safeAuth()` posture for
   non-auth configs.
8. **First HTTP 422 moderation-refused response shape** in project
   history — distinguishes from 400 validation-failure + 401
   unauthenticated + 404 not-found.
9. **First "buffer double-read for moderation + storage" pattern**
   — justified by the 2 MB upload cap + microsecond overhead.
10. **First env-var-count increase since Phase 31** — Phase 32-34
    added zero env vars; Phase 35 adds `MODERATION_PROVIDER`.
    **13 env-var milestone**.
11. **First ADR Pros/Cons table with 4+ options** in project
    history — ADR-0024 enumerates framework-only + committed
    OpenAI + defer entirely + regex-wordlist-default with full
    Pros/Cons each.
12. **5 consecutive phases shipping at least one APPEND** (Phase
    31 + 32 + 33 + 34 + 35) — first 5-phase APPEND streak.

### Delta summary (Phase 34 → Phase 35)

- **+5 commits** in-phase (35.0 + 35.1 + 35.2 + 35.3 + this gate)
  + 2 pre-Phase-35 housekeeping commits (audit + coi.md).
- **+1 ADR** (ADR-0024 → 24 total).
- **+0 migrations** (9 UNCHANGED; framework-only code change).
- **+0 DB tables** (7 UNCHANGED).
- **+0 runtime deps** (no new SDK; `lib/moderation/` uses only
  Node builtins + project types).
- **+1 env var** (`MODERATION_PROVIDER` → 13 total; first since
  Phase 31).
- **+0 i18n keys** (161 per locale UNCHANGED; framework returns
  provider-specific reason strings rendered as-is).
- **+11 / +2 file tests** (722/63 → 733/65; all in `lib/moderation/`
  new files Unit 35.1 + per-surface refusal tests in existing
  files Unit 35.2).
- **+0 dynamic API routes** (13 UNCHANGED; no new routes; framework
  + integration only).
- **+5 new files** (`docs/adr/0024-content-moderation.md` + 5 files
  in `lib/moderation/` = 6 new code/ADR files + 5 new THINK docs
  35.0 + 35.1 + 35.2 + 35.3 + 35.4 — 11 new files total).
- **+1 new `lib/<module>/`** (`lib/moderation/` — third deferred-
  provider-pattern dir after `lib/email/` + `lib/cron/`).
- **+0 `/profile` page bundle delta** (UNCHANGED at 2.31 kB / 117
  kB First Load JS).
- **+0 First Load JS Δ** (103 kB UNCHANGED end-to-end Phase 9-35;
  48th consecutive unit at 103 kB).
- **+0 middleware Δ** (160 kB UNCHANGED).
- **-1 net Q tally Δ** (66 UNCHANGED top-level; Q68 expansion sub-
  bullet resolution doesn't shift top-level tally; **but Phase-36+
  candidate count drops 12 → 11** — second net-decrease phase in a
  row).
- **+0 Class B categories Δ** (14 UNCHANGED — **fifth consecutive
  phase without new B category** Phase 31 + 32 + 33 + 34 + 35;
  first such 5-phase run in project history).
- **+0 Class B items Δ**.
- **+1 patience-signal-closing phase** (Q68 expansion at 12+ phase
  carryover — strongest non-just-surfaced patience signal in the
  Phase-35+ candidate tally pre-resolution).
- **+3 ADR APPENDs** (ADR-0016 D-B + ADR-0017 D-H + ADR-0019 D-F;
  all first APPENDs for those clauses; **9 ADR D-clauses with
  APPENDs total** post-Phase-35 — milestone).

### Surviving follow-ons (Class B; ~3 items across 14 categories)

Same set as Phase 34 close — Phase 35 did not address B catalog.
**B.15 item 5 carried** (subscribe-endpoint rate limiting; Phase
36+ if spam). 8 carried unchanged from Phase 34 close.

### Phase-36+ Q-candidates flagged (11 total; net-decrease from 12)

- 6 carried from Phase 27 (Q60 / Q61 / Q62 / Q64 / Q65 / Q72; Q68
  expansion now resolved Phase 35).
- 2 carried from Phase 28 (Q73 + Q74).
- 1 carried from Phase 30 (Q75).
- 2 carried from Phase 31 (Q77 + Q78).
- 0 from Phase 33 (Q79 resolved Phase 34).
- 0 from Phase 34.
- 0 new Phase 35.

**Slot net-decrease: 12 → 11 candidates**. Phase 36+ thread menu
is one item shorter than Phase 35 entry was; **second consecutive
net-decrease phase** (Phase 34 + Phase 35 = first 2-phase net-
decrease streak).

### DB-migration trigger re-eval at Phase 36 kickoff

- §5.7 trigger (a): FIRED Unit 9.6 carried.
- §5.7 trigger (b): **COLD Phase 35** (no schema change; framework-
  only code). Next thread Phase 36+ determines trigger (b) — most
  Phase-36+ candidates do NOT require migration in their minimum
  form (Q78 Profile A digest analytics needs new column for bounce
  tracking IF Resend webhook integration ships; Q64 needs migration;
  Q72 markdown evolution doesn't; Q79 Profile B doesn't; concrete
  content-moderation provider Phase 36+ may add persistent
  moderation-decision audit log table = would fire trigger (b)).

### Risk surface at Phase 35 close

- **6 operational gates pending** UNCHANGED (Q54 GitHub OAuth +
  Q55 Turso prod DB + Q69 Vercel Blob + Q73 Google OAuth + Q75
  Resend domain + Q77 Cron secret).
- **2 architectural Phase-36+ candidates** carry forward (Q74 non-
  GitHub-curator identity; Q78 digest-send analytics).
- All other Phase-30-31-32-33-34 risk-surface items carry forward
  unchanged.

### Boundary statement (Phase 35 anti-scope)

NOT a provider commitment (OpenAI moderation / Perspective /
regex-wordlist / custom all Phase 36+ operational-API-gate); NOT
persistent moderation-decision audit log (Phase 36+ + new table +
migration); NOT appeal mechanism (Phase 36+ UX); NOT bulk-rescan
of historical content (Phase 36+ reuses Phase-20 backfill pattern);
NOT moderation-policy versioning (Phase 36+ curator-track); NOT
per-surface severity-threshold tuning (Phase 36+ once real-provider
data accumulates); NOT block-vs-warn UX divergence (Phase 36+
polish); NOT async-webhook moderation (Phase 36+ for image-deep-
classifier providers); NOT subscribe-route test file backfill
(Phase 36+ scope); NOT per-surface custom i18n-keyed UX (Phase 36+
polish per ADR-0024 D-H); NOT Q78 digest-send analytics (waits for
Q77 production signal; Phase 36+); NOT Q64 per-user privacy opt-
out (Phase 36+; longest open architectural); NOT Q79 Profile B
full-edit UX or Profile C combined-curator-analytics (Phase 36+
Q79-sibling-expansion); NOT account-deletion blob cleanup (Phase
36+); NOT account-merge UI ADR-0020 D-E (Phase 36+); NOT third+
provider expansion (Phase 36+); NOT markdown evolution ADR (Phase
36+); NOT v2 methodology authoring (curator-track per release-
readiness audit §2.9); NOT SiteHeader avatar pill (Phase 36+); NOT
subscribe-endpoint rate limiting B.15 item 5 (Phase 36+ if spam);
NOT bounce/complaint webhook handling (Phase 36+ Q78 sub-thread);
NOT row-level lock for concurrent-cron race; NOT `safeAuth()`
extraction (Phase 36+ if 5th in-place copy surfaces); NOT per-
problem subscriptions extension (Phase 36+ Q76-sibling-expansion);
NOT cross-domain aggregated weekly summary (Phase 36+); NOT
subscriber-list export tooling (Phase 36+); NOT per-user-account-
subscription audit trail (Phase 36+).

**This phase is the framework-only architectural commitment** for
content moderation. Interface ships; no-op default ships; four
extension points wire in; env-var gates ship; tests cover the
default + refusal-propagation paths. Provider commitments + audit
log + appeal + bulk-rescan all remain Phase 36+ candidates as
operational-API-gate + curator-track work.

### Parallel-curator awareness

Phase 35 shipped as a **single-session phase** — primary shipped
all 5 units 35.0 → 35.4 in one coherent session, plus 2 pre-Phase-
35 housekeeping docs commits (audit + coi.md). **Nineteen
consecutive single-session phases** (Phase 17-35). Phase 16 remains
the lone parallel-session phase. Working tree clean at each unit
boundary; no parallel-session activity observed during Phase 35
work.

### Phase 35 vs prior new-ADR phases

Phase 35 = **15th new-ADR phase** in project history (Phases 0-1
+ 5 + 6 + 9 + 11 + 12 + 13 + 15 + 16 + 18 + 19 + 28 + 30 + 31 + 33
+ 35 = 15+ new-ADR phases). **First framework-only ADR** distinguishes
ADR-0024 from prior provider-committed ADRs (ADR-0008 Anthropic;
ADR-0017 Vercel Blob; ADR-0021 Resend; ADR-0022 Vercel Cron;
ADR-0023 Option A FK). **First "alternating no-new-ADR with new-
ADR sandwiched" pattern extended** (Phase 32 no-new → Phase 33 new
→ Phase 34 no-new → Phase 35 new — 4-phase alternation; first such
4-phase alternation in project history).

### Phase 36 entry conditions

Per §12 cardinal rule, **explicit human sign-off required**.
**Thirty-first "Continue" override opportunity** in Unit 36.0 prep.
Thread options for Phase 36+ (overridable per §12):

- **B. Q78 architectural digest-send analytics** (~3-5 units;
  ADR-0025 candidate; **still waits for first-week production cron
  signal from Q77** which is still pending operationally).
- **D. Markdown evolution ADR** (Q72 + Phase-29 B.14 wikilink
  couple; ~3-5 units; ADR-0025 candidate).
- **G. Q64 per-user privacy opt-out** (~3 units; **15+ phase
  carryover — longest open architectural question post-Q68
  resolution**).
- **L. Account-deletion blob cleanup** (~1-2 units; small scope;
  exercises Phase-33 ADR-0023 D-D cascade).
- **N. v2 methodology authoring** (curator-track per release-
  readiness audit §2.9 = primary blocker for §1 (d) success metric).
- **Q79 Profile B full-edit UX** or **Profile C combined-curator-
  analytics dashboard** — both UX-track without ADR if Phase 35+
  feedback warrants.
- **First Phase-36+ ADR-0025 concrete content-moderation provider
  commitment** (e.g., regex-wordlist-default ship as the Phase-36
  exercise of the ADR-0024 framework). Tightest 0-phase Q-carryover
  available; mirrors Phase 30 → 31 → 32 tight-follow-on cadence
  but on the ADR-0024 framework rather than a subscriber-list-
  email arc.
- **J-other. Account-merge UI** (ADR-0020 D-E).
- **K-other. Third+ provider expansion** (ADR-0020 D-B amendment).
- **M. SiteHeader avatar pill** (Phase-14 carryover).
- **B.15 item 5. Subscribe-endpoint rate limiting** (~1-2 units;
  Phase 36+ if spam observed).

**Q64** = strongest remaining non-just-surfaced patience signal
post-Q68 resolution (15+ phase carryover, longest open architectural).
**Q78** still waits for Q77 operational signal. **Phase-36+ ADR-
0025 concrete content-moderation provider commitment** = tightest
0-phase Q-carryover available — natural ADR-0024 follow-on if user
chooses to operationalize the framework. **First post-Phase-35
ADR-0024 framework consumption** is the Phase-36 acceptance-gate
test of the framework-only-ADR architectural commitment.

### Surfaces ready for Phase 36+ consumption

- `lib/moderation/` (5 files; framework + noop default + factory).
- `ContentModerator` interface (`moderateText` + `moderateImage` +
  `ModerationContext` + `ModerationResult`); future providers
  implement this verbatim.
- `getModerator()` factory + dispatch table (extend with new
  `case "openai": return new OpenAIModerator()` arm for first
  Phase-36+ provider).
- `MODERATION_PROVIDER` env var (documented in `.env.example`;
  default `noop`; future values `openai` / `perspective` / `regex-
  wordlist` / `custom`).
- 4-surface integration points (bio-write + avatar-upload + rating-
  challenge + subscribe) all live; flipping the env-var to a real
  provider activates all 4 simultaneously.
- HTTP 422 moderation-refused JSON shape (`{ error: "moderation-
  refused", field, message }`) established; future UX consumers
  render the message verbatim.

### Phase 35 → Phase 36 sign-off requirement

Per §12 cardinal rule: explicit human sign-off required at Phase
boundaries. Phase 35 close + Phase 36 open is the **31st "Continue"
override opportunity** in the project's unit-rhythm history. Phase
35 shipped a substantial architectural commitment (framework-only
ADR + multi-surface wiring); Phase 36 entry naturally weighs:

- (a) Operationalize the ADR-0024 framework with a concrete
  provider (Phase-36 ADR-0025 candidate; tightest 0-phase Q-
  carryover; natural follow-on).
- (b) Pivot to Q64 per-user privacy opt-out (longest non-Q68
  patience signal at 15+ phases).
- (c) Pivot to Q78 digest-send analytics (still waits for Q77
  operational signal).
- (d) Curator-track v2 methodology authoring (per release-readiness
  audit §2.9; primary blocker for §1 (d) success metric).
- (e) Operational-gate work (the 6 pending gates: Q54 / Q55 / Q69
  / Q73 / Q75 / Q77).

Default recommendation: **(a) ADR-0025 concrete provider** — would
exercise the Phase-35 framework + demonstrate the framework-only-
ADR pattern's full lifecycle (interface → noop default → concrete
provider) within 2 phases. Alternative: **(b) Q64** if longest-
carrying signal preferred over tight-follow-on cadence.
