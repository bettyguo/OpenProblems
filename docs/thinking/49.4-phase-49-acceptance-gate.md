# 49.4 — Phase 49 acceptance gate

Phase 49 closes at HEAD `faafbe3` (Unit 49.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-50-entry
conditions per §12.

## Phase 49 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 49.0 | Phase 49 prep | docs | `0661f90` |
| 49.1 | `PHASE_45_DEFAULT_ENABLED_SURFACES` cross-surface expansion + dispatch tests + decoupling + ADR-0018 D-G APPEND-D-AG | code+APPEND | `d0c7857` |
| 49.2 | 19 NEW end-to-end Phase-49 tests + first all-4-surfaces triple-alias + same-slot composition | code | `cac8951` |
| 49.3 | Phase-49 hygiene + APPEND-D-AC cross-surface item closure documentation | docs | `faafbe3` |
| 49.4 | Phase 49 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-48 5-unit framework shapes
verbatim.

## Fourth realization of constructor-arg-only zero-rework expansion + first all-4-surfaces triple-alias + first all-4-surfaces same-slot composition

- `PHASE_45_DEFAULT_ENABLED_SURFACES` value evolved in-place
  from `new Set(["rationale"])` (Phase 45 ship) to `new
  Set(["bio", "reviewNotes", "rationale", "actionRationale"])`
  (Phase 49 ship). **Fourth real-consumer-expansion realization**
  of the "constructor-arg-only zero-rework expansion" property
  (Phase 42 wikilinks; Phase 43 tables; Phase 44 arxiv; Phase
  49 doi). **First 4-realization property in project history**.
  **Completes the per-consumer all-4-surfaces arc**.
- `DoiExtensionRegistry` class + factory dispatch arm +
  plugin body + regex **UNCHANGED**. Pure constructor-arg-only
  change.
- 3 dispatch tests in `lib/markdown/extensions/index.test.ts`
  flipped to reflect Phase-49 all-4-surfaces dispatch.
- 4 baseline test blocks in `lib/markdown/index.test.ts`
  decoupled with explicit `new Set(["rationale"])` constructor
  args to preserve Phase-45/46/47/48 baseline coverage.
  **Decoupled-baseline-block pattern** documented as load-
  bearing convention for future cross-surface-expansion phases.
- 19 NEW end-to-end tests in `lib/markdown/index.test.ts`
  across 3 describe blocks: default doi on all 4 surfaces (9);
  first all-4-surfaces triple-alias under 4-way composite (5);
  first all-4-surfaces same-slot composition (5).
- ADR-0018 D-G APPEND adds Phase-49 EXTENDED block + **APPEND-
  D-AG** (Unit 49.1; seventh two-letter APPEND letter).
- ADR-0018 APPEND-D-AC cross-surface item CLOSED at **4-phase
  carryover** (Phase 45 → Phase 49; Unit 49.3 documents).
  Matches Phase-38 → 42 + Phase-39 → 43 4-phase cadence
  verbatim. **First cross-surface-expansion closure since
  Phase 44**.
- **First D-clause with BOTH items closed within the closure
  cadence**. APPEND-D-AC carries 2 distinct deferral items
  (DOI alias syntax + DOI cross-surface expansion); Phase 48
  closed item 2; Phase 49 closes the cross-surface item.
  **First D-clause to have ALL its enumerated deferrals
  resolved through the cadence**. Sets the precedent for
  multi-item APPEND closure across consecutive phases.
- **First "all 4 surfaces have same-slot composition" state**
  in project history. Pre-Phase-49 only rationale had `[arxiv,
  doi]` in `remarkPlugins`; post-Phase-49 every surface does.
  Regex-disjointness-as-sole-defense discipline (Phase 48
  established) exercised on every surface in production
  default.
- **First "all 4 surfaces are triple-alias" state** in project
  history. Pre-Phase-49 only rationale was triple-alias (under
  4-way default); Phase 49 generalizes via doi cross-surface
  expansion. **First surface-with-3-alias-consumers cardinality
  of 4**.
- **Maximal multi-consumer all-surfaces composition under
  default dispatch** — defines the upper bound for the Phase-
  37-framework's current capabilities.

## Phase 48 → Phase 49 delta

| Metric | Phase 48 close | Phase 49 close | Δ |
|---|---|---|---|
| Tests | 1069 / 72 files | **1088 / 72 files** | +19 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 15 | **16** (project record extends; seventh two-letter slot D-AG in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 5 | 5 | 0 |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 15 | 15 | 0 |
| **Phase-37-framework concrete consumers** | **4** | **4** | 0 |
| **Constructor-arg-only zero-rework expansion realizations** | **3 (wikilinks + tables + arxiv)** | **4 (+doi)** | **+1** |
| **Surfaces with same-slot composition** | **1 (rationale)** | **4 (all surfaces)** | **+3** |
| **Surfaces with 3+ alias-syntax consumers (triple-alias)** | **1 (rationale)** | **4 (all surfaces)** | **+3** |
| **Component-surface-slot active positions under default** | **13** (12 single + 1 same-slot extra on rationale) | **16** (12 single + 4 same-slot extras on every surface) | **+3** |

## Architectural firsts in Phase 49 (12 enumerated)

1. **Fourth realization of the "constructor-arg-only zero-
   rework expansion" property**. Phase 42 wikilinks; Phase 43
   tables; Phase 44 arxiv; Phase 49 doi. **First 4-realization
   property in project history**. **Completes the per-consumer
   all-4-surfaces arc**.
2. **First "all 4 surfaces have same-slot composition" state**.
   The arxiv-vs-doi pair in `remarkPlugins` now active on every
   surface under 4-way default. Regex-disjointness-as-sole-
   defense discipline exercised everywhere.
3. **First "all 4 surfaces are triple-alias" state**. Rationale
   was triple-alias since Phase 48; Phase 49 generalizes via
   doi cross-surface expansion. **First surface-with-3-alias-
   consumers cardinality of 4**.
4. **First cross-surface-expansion closure since Phase 44**.
   The 4-phase non-cross-surface-expansion streak (Phase 45-
   48) ends at Phase 49.
5. **First D-clause with BOTH items closed within the closure
   cadence**. APPEND-D-AC's 2 deferrals both resolved (Phase
   48 closed item 2; Phase 49 closes cross-surface).
6. **APPEND-D-AC cross-surface item CLOSED at 4-phase
   carryover** (Phase 45 → 49). Matches Phase-38 → 42 + Phase-
   39 → 43 4-phase cadence verbatim. Restores standard cross-
   surface-expansion cadence after the Phase-41 → 44 3-phase
   outlier.
7. **Eighth prep-/APPEND-doc-level deferral closed by a later
   phase**. APPEND-deferral closure cadence sustained 8 phases.
8. **Fifth cross-surface-expansion APPEND-deferral closure**
   in the cadence (Phase 42 + 43 + 44 + 49).
9. **Sixteenth APPEND on ADR-0018 D-G** — extends first-ADR-D-
   clause-with-most-APPENDs record 15 → 16.
10. **Seventh two-letter APPEND letter D-AG** (Excel-spreadsheet
    column convention sustained).
11. **First "framework + 4 consumers + composition + 4
    expansions + same-slot composition × 4 + alias syntax × 3"
    13-phase cluster** in project history (Phase 37-49). 13-
    phase architectural arc. Plus **19th consecutive phase
    without new B category** — Phase 31-49 = first 19-phase
    run (extends Phase-48 record from 18 → 19). Plus
    **fortieth NON-§13 phase**. Plus **105th consecutive 103 kB
    First Load JS unit** (extends from 100 at Phase-48 close).
12. **Maximal multi-consumer all-surfaces composition under
    default dispatch** — defines the upper bound for the Phase-
    37-framework's current capabilities. 4 consumers × 4
    surfaces × 3 slots = 48 component-surface-slot positions;
    16 active under 4-way default (12 single-occupancy + 4
    same-slot extras). Future expansions (5th consumer or 4th-
    or-later surface) extend the dimensions, not the cell
    density.

## Phase 49 → Phase 50 entry conditions

Per §12, Phase 50 entry requires **explicit human sign-off**.

**Recommended Phase 50 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **PubMed PMID sibling consumer** (`pubmed:NNNNNNNN` / `pmid:NNNNNNNN`) — fifth concrete consumer; **first 3rd-`remarkPlugins` consumer** beyond arxiv + doi; tests whether the regex-disjointness-as-sole-defense discipline scales to 3 same-slot consumers; mirrors Phase-41/45 first-consumer shape | 2-3 | Autonomous-tractable; closes new Phase-46 deferral; opens the 5th-consumer arc |
| 2 | **Older-style category-prefixed arxiv IDs** (APPEND-D-Y item 2; e.g., `arxiv:math/0211159`) — regex extension on existing `remarkLinkArxivIds`; second regex evolution on arxiv plugin; demonstrates non-alias regex evolution | 1-2 | Autonomous-tractable; closes APPEND-D-Y item 2 at 9-phase carryover |
| 3 | **Table-specific attributes** (`colspan` / `rowspan` / `scope`; APPEND-D-Q item 3) — schema-override extension on existing `TablesExtensionRegistry`; first regex/schema-extension equivalent on `schemaOverrides` slot | 1-2 | XSS-audit-required; schema-extension only |
| 4 | **Cross-entity wikilinks** (APPEND-D-L item 3) — entity-type disambiguation + plugin parameterization | 2-3 | Requires APPEND-D-L item 6 plugin parameterization to land first |
| 5 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — couples with `schemaOverride` for `class` attribute on `<a>`; first single-consumer-multi-slot case | 1-2 | XSS audit per attribute value |
| 6 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 7 | **Bare arxiv / DOI IDs without prefix** (APPEND-D-Y item 3 + APPEND-D-AC) — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 8 | **Empty-alias unification across consumers** (new Phase-48 deferral) — unify bare-form lookahead terminator class across arxiv-doi | 1 | Smallest-scope autonomous-tractable; refines first observed dual-form behavior divergence |
| 9 | **Plugin parameterization for wikilink-href-builder** (APPEND-D-L item 6) — makes `/problems/${slug}` configurable per-consumer; gates rank 4 | 1-2 | Autonomous-tractable |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1088 / 72 files**.
- `pnpm audit-content` — 0 errors / 6 warnings (45 consecutive
  phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (106 consecutive units
  at Unit 49.4 ship; 100-unit threshold crossed at Phase 48
  Unit 48.3).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 50+ deferrals)

NOT PubMed PMID consumer (Phase 50+ rank 1);
NOT older-style category-prefixed arxiv IDs (Phase 50+ rank 2);
NOT table-specific attributes (Phase 50+ rank 3);
NOT cross-entity wikilinks (Phase 50+ rank 4);
NOT `<a class="wikilink">` styling (Phase 50+ rank 5);
NOT 404 handling (Phase 50+ rank 6);
NOT bare arxiv / DOI IDs without prefix (Phase 50+ rank 7);
NOT empty-alias unification (Phase 50+ rank 8);
NOT plugin parameterization (Phase 50+ rank 9);
NOT auto-trim of alias display whitespace (Phase 50+);
NOT empty-alias-as-slug-fallback (Phase 50+);
NOT empty-alias-as-bare-arxiv-fallback (Phase 50+);
NOT empty-alias-as-bare-doi-fallback (Phase 50+);
NOT paper-card hover-preview (Phase 50+);
NOT `<caption>` element (Phase 50+);
NOT surface-specific table schemas (Phase 50+);
NOT `@mention` resolution (Phase 50+);
NOT dx.doi.org legacy host (Phase 50+);
NOT stricter trailing-lookahead for trailing-period DOIs (Phase
50+);
NOT a 3rd-or-later `remarkPlugins` consumer beyond arxiv + doi
(Phase 50+ rank 1 ships PubMed PMID;
the 4th and beyond defer further);
NOT a 2nd `rehypePlugins` consumer beyond wikilinks (Phase 50+);
NOT a 2nd `schemaOverrides` consumer beyond tables (Phase 50+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-48 deferrals carry forward.

## Boundary statement

**Phase 49 CLOSED at HEAD `<this-unit>` after 5 numbered units
(49.0 prep + 49.1 cross-surface expansion + dispatch tests +
APPEND-D-AG + 49.2 19 NEW end-to-end tests + first all-4-
surfaces triple-alias + first all-4-surfaces same-slot composition
+ 49.3 hygiene + 49.4 gate). Fourth realization of the
"constructor-arg-only zero-rework expansion" property — completes
the per-consumer all-4-surfaces arc (Phase 42 wikilinks; Phase 43
tables; Phase 44 arxiv; Phase 49 doi). First 4-realization
property in project history. First "all 4 surfaces have same-slot
composition" state — the arxiv-vs-doi pair in `remarkPlugins` now
active on every surface under 4-way default; regex-disjointness-
as-sole-defense discipline (Phase 48 established) exercised
everywhere in production default. First "all 4 surfaces are
triple-alias" state — every surface carries wikilinks + arxiv +
doi aliases simultaneously under 4-way default. First surface-
with-3-alias-consumers cardinality of 4 in project history.
Maximal multi-consumer all-surfaces composition under default
dispatch achieved. First cross-surface-expansion closure since
Phase 44 — the 4-phase non-cross-surface-expansion streak (Phase
45-48) ends at Phase 49. First D-clause with BOTH items closed
within the closure cadence — APPEND-D-AC's 2 deferrals both
resolved (Phase 48 closed item 2; Phase 49 closes cross-surface);
sets the precedent for multi-item APPEND closure across phases.
Closes ADR-0018 APPEND-D-AC cross-surface item at 4-phase
carryover (Phase 45 → 49; matches Phase-38 → 42 + Phase-39 → 43
cadence verbatim). APPEND-deferral closure cadence sustained 8
phases (Phase 42-49). Fifth cross-surface-expansion APPEND-
deferral closure. Sixteenth APPEND on ADR-0018 D-G (project
record extends 15 → 16). Seventh two-letter APPEND letter D-AG.
Fortieth NON-§13 phase. First "framework + 4 consumers +
composition + 4 expansions + same-slot composition × 4 + alias
syntax × 3" 13-phase cluster in project history (Phase 37-49).
19th consecutive phase without new B category (first 19-phase
run; extends Phase-48 record 18 → 19). 105th consecutive 103 kB
First Load JS unit (extends from 100 at Phase-48 close). XSS line
of defense preserved. 1088 tests across 72 vitest files (+19/0
vs Phase 48). 24 ADRs UNCHANGED. 7 DB tables UNCHANGED. 9
migrations UNCHANGED. 14 env vars UNCHANGED. 168 i18n keys per
locale UNCHANGED. First Load JS 103 kB UNCHANGED. Middleware 160
kB UNCHANGED. OPEN_QUESTIONS top-level Q-count 66 UNCHANGED.
Phase-37+ candidate count 8 UNCHANGED. `MARKDOWN_EXTENSIONS`
recognized single-value arms 5 UNCHANGED. Zero new ADRs /
migrations / i18n keys / env vars / operational gates / runtime
deps / files. Phase 50 entry requires explicit human sign-off
per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 48 → 49 delta complete.
- [x] Phase 50 entry conditions documented (10 ranked
      candidates).
- [x] Smoke gates pass.
- [x] STOP at Phase 50 boundary per §12.
