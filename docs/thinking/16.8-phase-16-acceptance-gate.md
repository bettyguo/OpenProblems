# 16.8 — Phase 16 acceptance gate (Q67 closure; 17 ADRs total; third ALTER migration; first storage layer)

Ninth and final Phase-16 unit; docs-only. Closes Phase 16 (**seventh
NON-§13 phase**; Q67 promotion — image override / avatar upload).
Mirrors Phase-12 Unit 12.8 + Phase-13 Unit 13.6 + Phase-14 Unit 14.8
+ Phase-15 Unit 15.8 acceptance-gate patterns: ledger of deliverables;
smoke-gate results; surviving follow-ons; Phase-17+ entry conditions.

> **§12 sign-off**: Phase 17 sign-off **pending** at the end of this
> unit. The "Continue" override path is available (twelfth invocation
> would set the precedent at Phase 5 → 6 / Phase 6 → 7 / Phase 7 → 8
> / Phase 8 → 9 / Phase 9 → 10 / Phase 10 → 11 / Phase 11 → 12 /
> Phase 12 → 13 / Phase 13 → 14 / Phase 14 → 15 / Phase 15 → 16 →
> **Phase 16 → 17**).

## THINK

### Phase 16 ledger

**9 units shipped** (16.0 prep → 16.8 gate) across HEAD `7644a70`
(Phase 15 close) → HEAD `6844ad2` (Phase 16 close pre-this-unit).

| Unit | Title | Type | Status |
|---|---|---|---|
| 16.0 | Phase 16 prep | docs | shipped `bb180cf` |
| 16.1 | ADR-0017 image storage architecture | docs | shipped `6f12cd7` |
| 16.2 | DB migration 0005 + schema edit (third ALTER) | code | shipped `baff769` |
| 16.3 | `lib/storage/` + `lib/users/` image-override helpers + tests | code | shipped `9715a96` |
| 16.4 | `/profile` image upload form + multipart actions + i18n | code | shipped `1461f4e` |
| 16.5 | Public consumption avatar fallback + `getUserMetadataById` extension | code | shipped `3bb68c0` |
| 16.6 | Phase-16 hygiene status pass | docs | shipped `e099a08` |
| 16.7 | OPEN_QUESTIONS hygiene + ADR review (Q67 resolved; Q69 + Q70 flagged) | docs | shipped `6844ad2` |
| 16.8 | **Phase 16 acceptance gate** (this unit) | gate | this commit |

**0 deferrals**; **0 scope drift**; matches Phase-12 + Phase-13 +
Phase-14 + Phase-15 well-scoped pattern. Fifth consecutive
9-unit phase shipping clean.

### Q67 closure summary

Q67 (image override / avatar upload) was a Phase-15-anticipated
candidate that promoted + resolved in Unit 16.7 simultaneously,
same shape as Phase-12 Q57 + Phase-13 Q58 + Phase-15 Q63. Closes
the two-phase honored-deferral lineage:

- Phase-15 Class B item 2 (image override per ADR-0016 D-G).
- Phase-15 Unit 15.8 acceptance-gate "Q67 candidate — strongest
  honored-deferral pick" surface flag.

ADR-0017 pins eight concrete contracts (D-A storage architecture;
D-B upload pipeline scope; D-C edit surface route shape; D-D
server-action shape; D-E public consumption fallback chain; D-F
validation + sanitization model; D-G operational gating (Q69); D-H
Phase 17+ deferrals). All six code-realizable D-clauses (D-A
through D-F) shipped; D-G is operational (curator action); D-H is
text-only deferral.

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`6844ad2`):

- **`pnpm typecheck`** → clean (`tsc --noEmit` no errors).
- **`pnpm test`** → **519/519 across 53 vitest files**. **+22 tests
  / +1 file** from Phase 15 close (497/52); all from Unit 16.3
  (4 in new `lib/storage/index.test.ts` + 18 in
  `lib/users/index.test.ts`).
- **`pnpm build`** → ~659 prerendered pages + **10 dynamic
  page+API routes** unchanged. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-16 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  **`/profile` bundle = 117 kB (was 108 kB at Phase 15 close; +9 kB
  page-scoped for the `"use client"` boundary on
  `ProfileImageUploadField`)**. `/u/{handle}` = 108 kB UNCHANGED.
  All other routes UNCHANGED at their Phase 15-close sizes.
- **`pnpm audit-content`** → **0 errors / 6 warnings** (Q32
  baseline since Phase 2; unchanged through every Phase 3-16 unit).

### Architectural firsts shipped in Phase 16

1. **First binary storage layer in project history** (Vercel Blob
   alongside file-system content + Turso DB). New `lib/storage/`
   module pattern (~3 functions; thin wrapper; vendor-swap surface
   bounded) for Phase-17+ binary-asset inheritance.
2. **Third ALTER migration validates ADR-0014 D-E discipline at
   THIRD exercise** (`0005_user_image_override`; clean drizzle-kit
   emission on nullable text column). Discipline crystallized for
   Phase-17+ inheritance.
3. **First user-controlled BINARY write surface** in project
   history. Surface-category progression complete for Phase 9-16
   identity architecture (Phase 9 auth-side / Phase 11 challenge /
   Phase 15 text / **Phase 16 binary**).
4. **First new runtime dependency since Phase-9 auth stack**
   (`@vercel/blob@2.3.3`; ~30 kB server-only). 5-phase
   dependency-discipline interval (Phase 10-15 added zero new
   runtime deps).
5. **First `"use client"` boundary on `/profile`**
   (`ProfileImageUploadField` for `URL.createObjectURL` preview;
   ~50 lines; +9 kB page-scoped; shared chunk UNCHANGED at 103 kB).
6. **First multipart-form server action in project history**
   (`updateProfileImageAction` with `encType="multipart/form-data"`).
7. **First three-way `?saved=...` banner pattern** (`saved=1` text /
   `saved=image` upload / `saved=image-cleared` clear; extends
   Phase-15's bi-state precedent).
8. **First sibling-helper-extension-Phase-16** in
   `getUserMetadataById` — gains third field (`imageOverride`)
   forward-compat for Phase-14 Class B SiteHeader avatar-dropdown
   follow-on.

### Delta summary (Phase 15 → Phase 16)

| Metric | Phase 15 close (`7644a70`) | Phase 16 close (HEAD `6844ad2`) | Δ |
|---|---|---|---|
| Commits | 9 | 8 + this gate | +8 |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | 16 | **17** | **+1** (ADR-0017) |
| DB tables | 6 | 6 | 0 |
| Migrations | 5 | **6** | **+1** (`0005_user_image_override`; **third ALTER**) |
| Env vars | 6 | **7** | **+1** (`BLOB_READ_WRITE_TOKEN` Q69 gate) |
| Runtime deps | (Phase-15 stack) | (+`@vercel/blob@2.3.3`) | **+1** |
| Storage layers | 2 (FS content + Turso DB) | **3** (+Vercel Blob) | **+1** |
| `users` columns | 9 | **10** | **+1** (`imageOverride`) |
| Tests | 497 / 52 files | **519 / 53 files** | **+22 / +1** |
| Prerendered pages | ~593 | ~659 | (build-time variance — unchanged content) |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 108 kB | **117 kB** | **+9** (page-scoped use-client) |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS open count | 28 | **30** | **+2** (Q69 + Q70) |
| OPEN_QUESTIONS resolved count | 22 | **23** | **+1** (Q67) |
| OPEN_QUESTIONS decided-as-lean count | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 54 | **57** | **+3** |
| i18n keys per locale | 113 | **124** | **+11** (`profile_edit.image_*` ×11) |
| LHCI URLs | 19 | 19 | 0 |

### Surviving follow-ons (Class B + Class C; from Unit 16.6)

**Class B — 17 items**:

- **B.1 Image-override expansion (8)**: Q70 (EXIF stripping); Q68
  expansion (content moderation on uploaded images); cropping UI;
  server-side resizing/transcoding; multiple-avatars history;
  image dimensions check; abandoned-blob cleanup script; GIF
  support.
- **B.2 Operational + infrastructure (7)**: Q69 (`BLOB_READ_WRITE_TOKEN`);
  Q54 (GitHub OAuth registration); Q55 (Turso DB provisioning);
  CI dummy `AUTH_SECRET`; `pnpm db:migrate` doc (6 migrations);
  first LHCI run; middleware-based auth-route protection.
- **B.3 Surface expansion (2)**: external URL allowlist
  composability; SiteHeader avatar pill / avatar-dropdown menu.

**Class C — pre-existing carryovers** unchanged from Phase 15
(with Phase-16 column / migration / env / dep / storage-layer
deltas noted).

### Phase-17+ Q-candidates flagged but NOT promoted

Eight candidates stay flagged for Phase-17+ kickoff:
- **Q59 candidate** (CLI `pnpm emit-challenge-action <id>`) —
  carried Phase-12/13/14/15/16; smallest scope.
- **Q60 candidate** (curator authz evolution beyond env-var) —
  couples to editorial-board (Q7).
- **Q61 candidate** (submitter anonymity option) — Phase-13
  carryover; needs schema + ADR.
- **Q62 candidate** (rejection-rationale public visibility) —
  Phase-13 carryover; policy decision.
- **Q64 candidate** (per-user privacy opt-out) — Phase-14
  carryover; ~3 units.
- **Q65 candidate** (per-curator activity feed) — Phase-14
  carryover; ~4-5 units.
- **Q66 candidate** (markdown rendering in bio) — Phase-15
  carryover; ~3 units.
- **Q68 expansion** (content moderation on uploaded images
  alongside bio text) — Phase-15/16 carryover/expansion.

### Phase 17+ entry conditions

Per §12 cardinal rule, the next phase needs explicit human
sign-off (or "Continue" override per the eleven-phase precedent;
twelfth invocation in Unit 17.0 prep). Phase 17+ thread options
ALL inferred-not-§13 (§13 ledger remains CLOSED since Phase 9):

- **A. Q70 promotion — EXIF stripping** (~2-3 units; ADR-0018
  candidate for image-transcoding pipeline). Strongest privacy
  signal among Phase-16-surfaced candidates. Couples to Q68
  expansion (moderation API integration).
- **B. Q66 promotion — markdown rendering in bio** (~3 units;
  ADR-0018+ candidate for sanitization subset). Phase-15
  carryover.
- **C. Q64 promotion — per-user privacy opt-out** (~3 units;
  ADR-0015 D-D Phase-15+ deferral). Phase-14 carryover;
  pre-emptive.
- **D. Q65 promotion — per-curator activity feed** (~4-5 units;
  ADR-0015 D-E expansion). Phase-14 carryover.
- **E. Q59 promotion — CLI emit-challenge-action** (~2-3 units;
  smallest reasonable scope). Carried 5 phases.
- **F. Multi-provider OAuth expansion** (~3-4 units; ADR-0018+
  candidate). Phase-9 carryover.
- **G. Subscriber-list email** (~6 units; Phase-5 D-4 punt;
  ADR-0018+ candidate). Carried 11+ phases — strongest
  "patience signal".
- **H. Q68 expansion promotion — content moderation** (~3-4 units;
  ADR-0018+ candidate for moderation API). Phase-15/16
  carryover/expansion.
- **I. External URL allowlist composability** (ADR-0017 Option
  2 extension; ~3-5 units). Phase-16 alternative path.
- **J. SiteHeader avatar pill / avatar-dropdown menu**
  (Phase-14 Class B; Phase 16 forward-compat'd
  `getUserMetadataById` for this). Couples to mobile-nav variant.
- **K. Full §8.6 24-mo collaborator COI check** (~3-5 units;
  ADR-0014 D-C deferral).
- **L. Cropping UI / server-side resizing / multiple-avatars
  history / abandoned-blob cleanup script** — image-pipeline
  expansion follow-ons.
- **HTML shell migration** STILL ON HOLD per parallel-session
  preservation signal.
- **Monetization** premature without observed user traffic;
  Phase 18+ candidate.

**DB-migration trigger re-eval at Phase 17 kickoff** (mandatory
per Unit 14.0 D-2 cascade): trigger (a) FIRED Unit 9.6; trigger
(b) cold (~1.6% of 5 MB threshold; content count 239 unchanged).
Phase 17 migration count depends on chosen thread.

### Risk surface at HEAD `6844ad2`

1. **Three operational gates** (Q54 / Q55 / Q69) pending curator
   action. Architecture is complete; deployment unblock pending.
2. **Orphan blob accumulation tolerated** until Class B cleanup
   script lands.
3. **Middleware bundle 160 kB** unchanged (~15% of Vercel Edge's
   1 MB limit).
4. **`/profile` bundle +9 kB** from `"use client"` boundary.
   Acceptable; shared chunk UNCHANGED.
5. **No content moderation on uploaded images** (Q68 expansion;
   deferred).
6. **No EXIF stripping** (Q70; deferred).

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token provisioning (Q69), NOT FR content
backfill (Q51 curator-track), NOT image transcoding / cropping /
moderation (Phase 17+), NOT destructive cleanup (Class C still
gated). This unit is the **closure**, not the **unblock**.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### Smoke gates

Docs-only — no `validate-content` / `typecheck` / `test` /
`build` changes anticipated.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/16.8-phase-16-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 16.8 entry cites the 9-unit Phase-16 ledger +
  smoke-gate results + delta summary + surviving follow-ons +
  Phase 17+ entry conditions.

### CHANGELOG entry shape

Unit 16.8 entry: declares Phase 16 closed (**seventh NON-§13
phase**; 9 units; 0 deferrals; 0 scope drift); cites Q67 closure
+ ADR-0017 acceptance; consolidates 8 Phase-16 architectural
firsts; presents Phase 15 → 16 delta table; enumerates Class B
+ Class C follow-ons; lists Phase-17+ Q-candidates + thread
options; surfaces Phase-17 sign-off requirement (twelfth
"Continue" override opportunity per §12 cardinal rule).
