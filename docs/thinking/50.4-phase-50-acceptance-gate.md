# 50.4 — Phase 50 acceptance gate

Phase 50 closes at HEAD `06f3559` (Unit 50.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-51-entry
conditions per §12.

## Phase 50 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 50.0 | Phase 50 prep | docs | `a068990` |
| 50.1 | `pubmed.ts` + `pubmed.test.ts` (NEW) + factory dispatch arm + dispatch tests + ADR-0018 D-G APPEND-D-AH | code+APPEND | `68d84d5` |
| 50.2 | 22 NEW end-to-end Phase-50 tests + first 3-consumer same-slot + first 5-consumer composition | code | `270737f` |
| 50.3 | Phase-50 hygiene + APPEND-D-AC PubMed PMID item closure documentation | docs | `06f3559` |
| 50.4 | Phase 50 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-49 5-unit framework shapes
verbatim.

## Fifth concrete Phase-37-framework consumer + first 3-consumer same-slot composition + first 5-consumer composition

- NEW `lib/markdown/extensions/pubmed.ts` (`PUBMED_PATTERN =
  /\b(?:pubmed|pmid):(\d{1,9})\b/gi` + `remarkLinkPubmedIds`
  plugin + `PubmedExtensionRegistry` class +
  `PHASE_50_DEFAULT_ENABLED_SURFACES = Set(["rationale"])`).
- NEW `lib/markdown/extensions/pubmed.test.ts` (27 tests).
- `lib/markdown/extensions/index.ts` factory dispatch arm adds
  `"pubmed"` case + 7 NEW dispatch tests in
  `extensions/index.test.ts`.
- 22 NEW end-to-end tests in `lib/markdown/index.test.ts`
  across 3 describe blocks (Unit 50.2).
- ADR-0018 D-G APPEND adds Phase-50 EXTENDED block + **APPEND-
  D-AH** (Unit 50.1; eighth two-letter slot).
- ADR-0018 APPEND-D-AC PubMed PMID item CLOSED at **5-phase
  carryover** (Phase 45 → 50).
- **Fifth concrete Phase-37-framework consumer**.
- **First 3rd-`remarkPlugins` consumer** in project history.
- **First 3-consumer same-slot composition** under
  `MARKDOWN_EXTENSIONS=arxiv,doi,pubmed`.
- **First 5-consumer composition under default dispatch** under
  `MARKDOWN_EXTENSIONS=wikilinks,tables,arxiv,doi,pubmed` —
  **maximum-consumer-cardinality state** in project history.
- **First env-var single-value arm beyond 5** — 6th arm
  `pubmed`; first expansion of recognized-arms set since Phase
  45.
- **Regex-disjointness-as-sole-defense discipline scales from
  2 to 3 same-slot consumers** without architectural change.
  All three pairs (arxiv-vs-doi; arxiv-vs-pubmed; doi-vs-pubmed)
  are collision-free via regex character class disjointness
  alone. Plugin invocation order is immaterial for the triple.
- **First "first-ship after a 5-phase APPEND-deferral
  carryover"**.

## Phase 49 → Phase 50 delta

| Metric | Phase 49 close | Phase 50 close | Δ |
|---|---|---|---|
| Tests | 1088 / 72 files | **1144 / 73 files** | +56 / +1 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 16 | **17** (project record extends; eighth two-letter slot D-AH in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 5 | **6** (`pubmed` added) | **+1** |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 15 | **17** (+`pubmed.ts` + `pubmed.test.ts`) | **+2** |
| **Phase-37-framework concrete consumers** | **4** | **5 (+pubmed)** | **+1** |
| **Plugins in `remarkPlugins` slot** | **2 (arxiv + doi)** | **3 (+pubmed)** | **+1** |
| **Surfaces with N-consumer composition** | rationale: 4 (Phase 49); others: 4 | **rationale: 5 (max); others: 4** | **+1 (rationale)** |
| **3-consumer same-slot compositions** | **0** | **1 (rationale; arxiv+doi+pubmed)** | **+1** |

## Architectural firsts in Phase 50 (12 enumerated)

1. **Fifth concrete Phase-37-framework consumer**
   (`PubmedExtensionRegistry`). After Phase 50 the framework
   has 5 consumers across 3 slots; `remarkPlugins` slot hosts
   3 (arxiv + doi + pubmed); other 2 slots remain singleton.
2. **First 3rd-`remarkPlugins` consumer in project history**.
3. **First 3-consumer same-slot composition** under
   `MARKDOWN_EXTENSIONS=arxiv,doi,pubmed`. Tests whether the
   regex-disjointness-as-sole-defense discipline (Phase 48
   established for 2 same-slot consumers; Phase 49
   generalized to all 4 surfaces) scales to 3 same-slot
   consumers — **scales without architectural change**.
4. **First 5-consumer composition under default dispatch**
   under `MARKDOWN_EXTENSIONS=wikilinks,tables,arxiv,doi,pubmed`.
   Rationale carries 5 consumers across 3 slots. **Maximum-
   consumer-cardinality state** in project history.
5. **First env-var single-value arm beyond 5**.
   `MARKDOWN_EXTENSIONS` recognized single-value arms: 5 → 6
   (gains `pubmed`). **First expansion of the recognized-arms
   set since Phase 45** — 5-phase stability run for the arms
   set (Phase 45 → 50; longest single-arm-stability run
   observed).
6. **First "first-ship after a 5-phase APPEND-deferral
   carryover"**. Phase-41 arxiv first-ship had 0-phase
   carryover; Phase-45 doi first-ship had 4-phase carryover;
   Phase-50 pubmed first-ship has 5-phase carryover.
7. **APPEND-D-AC PubMed PMID item CLOSED at 5-phase carryover**
   (Phase 45 → 50). Slower than the Phase-41 → 45 4-phase doi
   carryover because intervening Phase 46-49 closed 4 other
   APPEND items.
8. **Ninth prep-/APPEND-doc-level deferral closed by a later
   phase** — APPEND-deferral closure cadence sustained 9
   phases. **Longest sustained closure cadence in project
   history**.
9. **Second non-cross-surface non-alias APPEND-deferral
   closure** in the cadence (Phase 45 doi sibling first;
   Phase 50 pubmed sibling second). **APPEND-D-AC second
   item closed by sibling-consumer introduction** — first
   closure for the "additional auto-link consumers" deferral
   text.
10. **Seventeenth APPEND on ADR-0018 D-G** — extends first-
    ADR-D-clause-with-most-APPENDs record 16 → 17.
11. **Eighth two-letter APPEND letter D-AH** (Excel-spreadsheet
    column convention sustained).
12. **First "framework + 5 consumers + composition + 4
    expansions + 3-consumer same-slot composition + alias
    syntax × 3" 14-phase cluster** in project history (Phase
    37-50). 14-phase architectural arc. Plus **20th
    consecutive phase without new B category** — Phase 31-50
    = first **2-decade no-new-B-category run** (extends
    Phase-49 record 19 → 20). Plus **forty-first NON-§13
    phase**. Plus **110th-or-greater consecutive 103 kB First
    Load JS unit** (106 at Phase-49 close + 5 Phase-50 units
    = 111 at Phase-50 close).

## Phase 50 → Phase 51 entry conditions

Per §12, Phase 51 entry requires **explicit human sign-off**.

**Recommended Phase 51 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **PubMed PMID alias syntax** (`[[pubmed:NNN\|display]]` / `[[pmid:NNN\|display]]`) — mirrors Phase-47 arxiv-alias + Phase-48 doi-alias dual-form regex extension verbatim on the pubmed plugin; **fourth realization of Phase-46 plugin-regex-extension phase-shape pattern**; smallest-scope autonomous-tractable thread | 1-2 | Autonomous-tractable; plugin-only regex extension; closes new Phase-50 deferral at 1-phase carryover (fastest closure possible) |
| 2 | **Older-style category-prefixed arxiv IDs** (APPEND-D-Y item 2; e.g., `arxiv:math/0211159`) — regex extension on existing `remarkLinkArxivIds`; closes APPEND-D-Y item 2 at 10-phase carryover | 1-2 | Autonomous-tractable; closes oldest open APPEND-D-Y item |
| 3 | **Table-specific attributes** (`colspan` / `rowspan` / `scope`; APPEND-D-Q item 3) — schema-override extension on existing `TablesExtensionRegistry`; first regex/schema-extension equivalent on `schemaOverrides` slot | 1-2 | XSS-audit-required; schema-extension only |
| 4 | **ORCID auto-link consumer** (`orcid:NNNN-NNNN-NNNN-NNNN`) — sixth concrete consumer; **first 4th-`remarkPlugins` consumer**; closes APPEND-D-AC third item (the "additional auto-link consumers" deferral text remains active for ORCID + bioRxiv + OSF) | 2-3 | Autonomous-tractable; mirrors Phase-41/45/50 first-consumer shape |
| 5 | **Cross-entity wikilinks** (APPEND-D-L item 3) — entity-type disambiguation + plugin parameterization | 2-3 | Requires APPEND-D-L item 6 plugin parameterization to land first |
| 6 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — couples with `schemaOverride` for `class` attribute on `<a>`; first single-consumer-multi-slot case | 1-2 | XSS audit per attribute value |
| 7 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 8 | **PubMed PMID cross-surface expansion** (new Phase-50 deferral) — constructor-arg value-only change in `PHASE_50_DEFAULT_ENABLED_SURFACES`; **fifth realization of "constructor-arg-only zero-rework expansion"** property | 1-2 | Autonomous-tractable; ~Phase 54 at 4-phase-gap cadence (Phase 50 → 54 anticipated) |
| 9 | **Empty-alias unification across consumers** (Phase-48 deferral) | 1 | Smallest-scope autonomous-tractable; refines first observed dual-form behavior divergence |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1144 / 73 files**.
- `pnpm audit-content` — 0 errors / 6 warnings (46 consecutive
  phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (111 consecutive units
  at Unit 50.4 ship; 100-unit threshold crossed at Phase 48
  Unit 48.3).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 51+ deferrals)

NOT PubMed PMID alias syntax (Phase 51+ rank 1);
NOT older-style category-prefixed arxiv IDs (Phase 51+ rank 2);
NOT table-specific attributes (Phase 51+ rank 3);
NOT ORCID consumer (Phase 51+ rank 4);
NOT cross-entity wikilinks (Phase 51+ rank 5);
NOT `<a class="wikilink">` styling (Phase 51+ rank 6);
NOT 404 handling (Phase 51+ rank 7);
NOT PubMed PMID cross-surface expansion (Phase 51+ rank 8;
~Phase 54 at 4-phase cadence);
NOT empty-alias unification (Phase 51+ rank 9);
NOT bioRxiv consumer (Phase 51+);
NOT OSF preprint consumer (Phase 51+);
NOT bare arxiv / DOI IDs without prefix (Phase 51+);
NOT dx.doi.org legacy host (Phase 51+);
NOT stricter trailing-lookahead for trailing-period DOIs (Phase
51+);
NOT paper-card hover-preview (Phase 51+);
NOT plugin parameterization (Phase 51+);
NOT auto-trim of alias display whitespace (Phase 51+);
NOT empty-alias-as-slug-fallback (Phase 51+);
NOT empty-alias-as-bare-arxiv-fallback (Phase 51+);
NOT empty-alias-as-bare-doi-fallback (Phase 51+);
NOT empty-alias-as-bare-pubmed-fallback (new Phase-50 deferral
candidate);
NOT `<caption>` element (Phase 51+);
NOT surface-specific table schemas (Phase 51+);
NOT `@mention` resolution (Phase 51+);
NOT a 4th-or-later `remarkPlugins` consumer beyond arxiv + doi
+ pubmed (Phase 51+ rank 4 ships ORCID as 4th candidate);
NOT a 2nd `rehypePlugins` consumer beyond wikilinks (Phase 51+);
NOT a 2nd `schemaOverrides` consumer beyond tables (Phase 51+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-49 deferrals carry forward.

## Boundary statement

**Phase 50 CLOSED at HEAD `<this-unit>` after 5 numbered units
(50.0 prep + 50.1 NEW pubmed consumer + factory dispatch arm +
APPEND-D-AH + 50.2 22 NEW end-to-end tests + first 3-consumer
same-slot + first 5-consumer composition + 50.3 hygiene + 50.4
gate). Fifth concrete Phase-37-framework consumer in project
history (`PubmedExtensionRegistry`). First 3rd-`remarkPlugins`
consumer beyond arxiv + doi. First 3-consumer same-slot
composition under default dispatch — the regex-disjointness-as-
sole-defense discipline (Phase 48 established for 2 same-slot
consumers; Phase 49 generalized to all 4 surfaces) scales from
2 to 3 same-slot consumers without architectural change. The
three regex character classes (arxiv `\d{4}\.\d{4,5}` lacks
`:`+`/`; doi `10.<reg>/<suffix>` requires `10.`+`/`; pubmed
`(?:pubmed|pmid):\d` requires literal prefix) are pairwise
disjoint — no string can match more than one — and plugin
invocation order is immaterial for the triple. First 5-
consumer composition under default dispatch — maximum-consumer-
cardinality state in project history (rationale: 5 consumers
across 3 slots; other 3 surfaces: 4 consumers; pubmed inactive
there per Phase-50 rationale-only default). First env-var
single-value arm beyond 5 — 6th arm `pubmed`; first expansion
of the recognized-arms set since Phase 45. First "first-ship
after a 5-phase APPEND-deferral carryover". Closes ADR-0018
APPEND-D-AC PubMed PMID item at 5-phase carryover (Phase 45 →
50). APPEND-deferral closure cadence sustained 9 phases —
longest sustained closure cadence in project history. Second
non-cross-surface non-alias APPEND-deferral closure. Seventeenth
APPEND on ADR-0018 D-G (project record extends 16 → 17). Eighth
two-letter APPEND letter D-AH. Forty-first NON-§13 phase. First
"framework + 5 consumers + composition + 4 expansions + 3-
consumer same-slot composition + alias syntax × 3" 14-phase
cluster in project history (Phase 37-50). 20th consecutive phase
without new B category — first 2-decade no-new-B-category run
(extends Phase-49 record 19 → 20). 111th consecutive 103 kB
First Load JS unit (extends from 106 at Phase-49 close). XSS
line of defense preserved. 1144 tests across 73 vitest files
(+56/+1 vs Phase 49; +27 from new pubmed.test.ts + 7 from new
dispatch tests + 22 from new end-to-end tests). 24 ADRs
UNCHANGED. 7 DB tables UNCHANGED. 9 migrations UNCHANGED. 14
env vars UNCHANGED. 168 i18n keys per locale UNCHANGED. First
Load JS 103 kB UNCHANGED. Middleware 160 kB UNCHANGED.
OPEN_QUESTIONS top-level Q-count 66 UNCHANGED. Phase-37+
candidate count 8 UNCHANGED. `MARKDOWN_EXTENSIONS` recognized
single-value arms 6 (+1 vs Phase 49 close). Zero new ADRs /
migrations / i18n keys / env vars / operational gates / runtime
deps. Phase 51 entry requires explicit human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 49 → 50 delta complete.
- [x] Phase 51 entry conditions documented (10 ranked
      candidates).
- [x] Smoke gates pass.
- [x] STOP at Phase 51 boundary per §12.
