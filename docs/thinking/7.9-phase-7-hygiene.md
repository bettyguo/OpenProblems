# 7.9 — Phase-7 hygiene status pass (deferrals + in-flight units)

Eighth unit of Phase 7 (out-of-order: lands ahead of the natural §F sequence because Unit 7.5 + Unit 7.3a are not yet shipped; both are in-flight rather than retired). Docs-only. Mirrors the Phase-5 (Unit 5.11) + Phase-6 (Unit 6.8) hygiene-pass framing: list deferred items, name the unlock condition, mark gate type. Adds a second class for Phase 7 — **in-flight units the primary session has not authored**.

## THINK

### Class A — In-flight Phase-7 units (not yet shipped at HEAD `6c8593a`)

These are Phase-7 §F deliverables that aren't done. Distinct from "deferred hygiene" because they're expected work, just not landed yet.

| Unit | Title                                              | Likely owner | Status |
|------|----------------------------------------------------|--------------|--------|
| 7.3a | Bulk page migration + middleware                   | Open         | Deferred from 7.3 (scope-cut; 22-file structural change; HIGH blast radius) |
| 7.5  | `/methodology/[locale]/v1.0.0` FR pilot            | Parallel session likely | Touched neither Velite nor route layer in primary-session commits since 7.3 |

### Class B — Phase-7-specific follow-ons (deferred from completed units)

Items flagged in the THINK docs / CHANGELOG of shipped Phase-7 units that didn't land in their parent unit.

| Item | Flagged in | Unlock condition | Gate type |
|------|------------|------------------|-----------|
| `<html lang={locale}>` on `app/[locale]/layout.tsx` (axe `html-has-lang` content-vs-attribute mismatch on `/fr/about`) | Unit 7.7 + 7.6 | First Lighthouse CI run flagging it OR absorbed into Unit 7.3a when SiteHeader moves under [locale] | Observation-driven (first PR) |
| `SITE` constant extraction into `lib/site-url.ts` | Unit 7.8 | 5+ call sites (currently 3: `lib/digest/rss.ts:16`, `app/api/v1/rss.xml/route.ts:27`, `lib/sitemap/build-sitemap.ts`) | Count threshold |
| `scripts/validate-content.ts` extension for `*.fr.yaml` literal-lookup → glob | Unit 7.4 | First `*.fr.yaml` content lands in `content/` | Content arrival |
| `app/robots.ts` (sister convention to `app/sitemap.ts`) | Unit 7.8 | A `User-Agent` / `Disallow` policy decision (AI-scraping posture; no Q surfaced yet) | Policy decision |
| Per-entry `lastModified` + `changeFrequency` + `priority` on sitemap | Unit 7.8 | Lighthouse SEO category flags it (modern crawlers treat omissions as hints) | Observation-driven |
| Locale alternates for routes beyond `/about` in sitemap | Unit 7.8 | Unit 7.3a expands `/[locale]/<route>` shadows | Depends on 7.3a |
| `app/[locale]/page.tsx` (home page under [locale]; `/en/` + `/fr/` currently 404) | Unit 7.3 | Unit 7.3a OR a deliberate `[locale]` home pilot | Depends on 7.3a |
| `NEXT_LOCALE` cookie writes from middleware | Unit 7.6 | Middleware lands in Unit 7.3a | Depends on 7.3a |
| `useTranslations` for LocaleToggle aria-label (currently hardcoded EN/FR strings) | Unit 7.6 | SiteHeader moves under NextIntlClientProvider in Unit 7.3a | Depends on 7.3a |
| Locale-toggle query-param / fragment preservation | Unit 7.6 | Any Phase-7+ route adds `?param=` or `#section` to a URL | Future need |
| Centralized translation-CLI (machine-assisted FR drafting) | ADR-0011 D-G + Unit 7.4 | Phase 7+ enhancement (per ADR-0009 D-F precedent — `verified: false` flow) | Phase 8+ |

### Class C — Carryover deferrals (pre-Phase-7; unchanged)

Verified at HEAD `6c8593a`. None unlocked by Phase-7 work.

| Item | Originally flagged | Carry-status | Unlock |
|------|---------------------|--------------|--------|
| Orphan `components/domain-tile-grid/` deletion | Unit 4.4 + 5.11 + 6.8 | Still orphan at HEAD (`grep -rn domain-tile-grid app components lib` finds only the self-reference) | Explicit curator authorization ("delete the orphan tile-grid") |
| `entries.json` backfill on 8 problems | Unit 5.11 + 6.8 | Still 2/10 problems have entries (hallucination-reduction + long-horizon-agent-reliability) | Curator session + `ANTHROPIC_API_KEY` + per-paper review |
| `pnpm clean-drafts` script | Unit 5.10 + 5.11 | `drafts/` directory doesn't exist at HEAD (no accumulated stale files) | `drafts/` > 100 stale files in any working tree |
| `<managingEditor>` on RSS feeds (Q44 / Q33) | Unit 5.11 + 6.8 | Coupled to Q2 | Q2 (DNS / project email) resolution |
| Phase-2 ROR-ID + InstaDeep orphan | Phase-4 hygiene list | Unchanged | Curator-driven session |
| W3C feed validator pass against deployed `/api/v1/rss.xml` + `/api/v1/digest/<slug>` URLs | Unit 5.11 + 6.8 | First preview deploy hasn't happened | First preview deploy |
| `/digest` + `/problems/[slug]/talk` Playwright `chromium-win32` baselines | Unit 6.8 | Manual `playwright test --update-snapshots` run | Manual run |
| Real-API smoke for Phase-6 GraphQL client (Unit 6.2) | Unit 6.8 | Q47 + `GITHUB_TOKEN` | Operational |
| `NEXT_PUBLIC_GISCUS_REPO_ID` env wiring | Unit 6.8 | Q47 resolution + curator runs giscus.app config UI | Operational |
| W3C feed validator pass against `/api/v1/digest/<slug>` with discussion items (post-6.6) | Unit 6.8 | Q47 + 1+ discussion with comments + first deploy | Compound |

### Phase-7 surface delta vs Phase-6 close

- **+1 ADR** (0011 — i18n strategy).
- **+1 sitemap surface** (`/sitemap.xml` from Unit 7.8).
- **+2 SSG pages** (`/en/about` + `/fr/about` from Unit 7.3).
- **+1 site-header control** (LocaleToggle from Unit 7.6; renders only on `[locale]/...` routes).
- **+2 LHCI URLs** (Unit 7.7).
- **+1 Velite-collection-augmentation** (Unit 7.4 — `lang` derive + `translation_source` enum across 5 collections).
- **+1 helper** (`lib/i18n/locale-filename.ts` from Unit 7.4).
- **+1 helper** (`lib/sitemap/build-sitemap.ts` from Unit 7.8).
- **+ Q closures**: Q48 (sitemap-half from Unit 7.8); Q52 + Q53 (from Unit 7.10 hygiene).
- **+ Q promotions**: Q50 (already done Unit 7.1).
- **Page count: 333 → 336** (+3 across Phase 7: +2 [locale]/about + 1 sitemap.xml).
- **Test count: 323 → 370** (+47 across Phase 7).
- **First Load JS shared chunk: 103 kB UNCHANGED** through every Phase-7 unit.

### Risk surface at HEAD

1. **`<html lang="en">` mismatch on `/fr/about`**: documented in Unit 7.7 CHANGELOG. axe's `html-has-lang` rule MAY flag content-vs-attribute mismatch on first PR; potential a11y dip below 0.95 threshold. One-line fix when it triggers.
2. **No middleware**: bare `/about`, `/methodology`, etc. coexist with `/en/about` + `/fr/about`. Three different about pages depending on URL. Acceptable intermediate state per Unit 7.3 / 7.6 deviation notes.
3. **Locale-toggle hides on bare paths**: by design (fail-closed). Most visitors don't see it until 7.3a expands the bilingual surface.
4. **`pnpm-workspace.yaml` allowedBuilds drift**: not checked this unit; should be flat (esbuild + sharp + unrs-resolver from Phase 5).

### What this unit IS NOT

- **Not the bulk migration**. Unit 7.3a is deferred work, not hygiene.
- **Not the methodology FR pilot**. Unit 7.5 is in-flight (likely parallel session).
- **Not destructive cleanup**. Orphan deletion still requires curator authorization per the Unit 4.4 / 5.11 / 6.8 policy.
- **Not the acceptance gate**. Unit 7.11 is the phase-close gate with its own deliverable-status table and §13 ledger review.

### Smoke gates

Docs-only — `validate-content` / `typecheck` / `test` / `build` / `audit-content` unchanged from Unit 7.10 snapshot (370/370 tests; 336 routes; 103 kB First Load JS).

### CHANGELOG entry shape

Per-unit entry: status pass on Phase-7 hygiene. Class A = 2 in-flight units (7.3a, 7.5); Class B = 11 Phase-7-specific follow-ons; Class C = 10 carryover deferrals (unchanged from 6.8). Phase-7 surface delta summary; risk-surface table; explicit "not 7.3a / 7.5 / 7.11" boundary statement.
