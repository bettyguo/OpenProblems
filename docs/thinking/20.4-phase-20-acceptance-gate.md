# 20.4 — Phase 20 acceptance gate (ADR-0019 D-E closure; 19 ADRs unchanged; first operational-script-only phase)

Fifth and final Phase-20 unit; docs-only. Closes Phase 20
(**eleventh NON-§13 phase**; ADR-0019 D-E backfill script
realized via `scripts/backfill-exif-strip.ts`). Mirrors Phase-12 /
13 / 14 / 15 / 16 / 17 / 18 / 19 acceptance-gate patterns.

> **§12 sign-off**: Phase 21 sign-off **pending** at end of unit.
> "Continue" override path available (**sixteenth invocation**
> opportunity).

## THINK

### Phase 20 ledger

**5 units shipped** (20.0 prep → 20.4 gate). **0 deferrals**;
**0 scope drift**. **Ninth consecutive well-scoped phase**.
**Smallest phase since Phase 13** (which shipped 7 units; Phase
17 shipped 8; Phase 18 shipped 7; Phase 19 shipped 6; Phase 20
ships **5**).

| Unit | Title | Type | Status |
|---|---|---|---|
| 20.0 | Phase 20 prep | docs | shipped `17817e0` |
| 20.1 | `scripts/backfill-exif-strip.ts` (ADR-0019 D-E impl) | code | shipped `3e37be1` |
| 20.2 | Phase-20 hygiene status pass | docs | shipped `7bcbcd0` |
| 20.3 | OPEN_QUESTIONS hygiene + ADR review (Q70 Phase-20 note) | docs | shipped `da195d1` |
| 20.4 | **Phase 20 acceptance gate** (this unit) | gate | this commit |

### Phase-20 closure summary

ADR-0019 D-E (backfill script Phase-20+ candidate) **REALIZED**
in Unit 20.1 via `scripts/backfill-exif-strip.ts` — the
retroactive EXIF-stripping CLI tool that closes the
no-retroactive-Phase-19 gap for Phase 16-18 avatars uploaded
before EXIF stripping shipped Phase 19.

Phase 20 is the **second phase since Phase 3 to close without
new ADR** (Phase 18 was first via ADR-0018 D-G inheritance
contract for Q71; Phase 20 second via ADR-0019 D-E
implementation). Establishes precedent: **when an ADR pins a
Phase-N+ contract explicitly, the implementation phase can ship
without a new ADR**.

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`da195d1`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **564/564 across 54 vitest files**. 0 / 0
  Δ from Phase 19 close (564/54); **first phase since DB
  landed to ship zero new vitest tests** (per Unit 20.0 D-7
  scope-cap decision — script correctness verified via manual
  `--dry-run` inspection).
- **`pnpm build`** → ~659 prerendered pages + 7 dynamic page+API
  routes UNCHANGED. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-20 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` = 117 kB UNCHANGED. `/u/{handle}` = 108 kB
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32 baseline
  since Phase 2; unchanged through every Phase 3-20 unit).
- **`pnpm lint`** clean.

### Architectural firsts shipped in Phase 20

1. **First retroactive-privacy-correction surface** in project
   history. Phase 19 shipped forward-only EXIF stripping (new
   uploads via `putAvatar`); Phase 20 closes the backwards-compat
   gap for Phase 16-18 avatars via
   `scripts/backfill-exif-strip.ts`. Privacy posture moves from
   "new uploads stripped" to "all uploads stripped (after manual
   curator run)".
2. **Fourth consecutive 0-migration phase** (Phase 17 + 18 + 19 +
   20; 7 of 11 phases since DB landed are 0-migration: Phase 10 +
   13 + 14 + 17 + 18 + 19 + 20).
3. **Fourth consecutive single-session phase** (Phase 17 + 18 +
   19 + 20; Phase 16 was the parallel-session phase). Demonstrates
   that well-scoped phases converge regardless of session count.
4. **Second phase since Phase 3 to close without new ADR** (Phase
   18 was first; Phase 20 second). Establishes precedent that
   ADR-pinned Phase-N+ implementation phases can ship without
   a new ADR.
5. **Smallest Phase shape since Phase 13** — **5 units** (was 6
   in Phase 19; 7 in Phase 18; 7 in Phase 13). Phase 20's scope
   compression vs Phase 19 because: no new ADR + no new vitest
   tests + no UI affordance + no new dep + no new env var.
6. **First operational-script-only phase** in project history —
   Phase 20 ships zero changes to `lib/` / `app/` / `components/`
   / `content/`; only `scripts/` + `package.json` + docs.
7. **First "zero new dep + zero new test + zero new ADR"
   well-scoped phase** — pure implementation of previously-pinned
   contract. Phase 20 ships entirely on existing architectural
   primitives (`sharp@0.34.5` direct dep from Phase 19 +
   `@vercel/blob` from Phase 16 + drizzle-orm from Phase 9).
8. **First hygiene unit (20.3) with zero Q-status movements**
   since Phase 16 Unit 16.7. Phase 17/18/19 each promoted 1 Q
   (Q66 / Q71 / Q70); Phase 20 promoted zero — pure implementation
   phase with no new architectural surface.

### Delta summary (Phase 19 → Phase 20)

| Metric | Phase 19 close (`87d11e6`) | Phase 20 close (HEAD `da195d1`) | Δ |
|---|---|---|---|
| Commits | 6 | 4 + this gate | +5 |
| ADRs | **19** | **19** | **0** (second phase since Phase 3 to close without new ADR) |
| Migrations | 6 | **6** | **0** (fourth consecutive 0-migration phase) |
| Runtime deps | (Phase-19 stack incl. `sharp@0.34.5`) | (Phase-19 stack unchanged) | **0** (first zero-new-dep well-scoped phase) |
| Tests | 564 / 54 files | **564 / 54 files** | **0** (first zero-new-test phase since DB landed) |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` + `/u/{handle}` bundles | 117 + 108 kB | 117 + 108 kB | 0 |
| OPEN_QUESTIONS resolved | 26 | **26** | **0** (first hygiene unit with 0 movements since Phase 16) |
| OPEN_QUESTIONS open | 29 | **29** | **0** |
| OPEN_QUESTIONS total | 59 | **59** | **0** |
| i18n keys per locale | 127 | 127 | 0 |
| `scripts/` files | 6 | **7** | **+1** (`backfill-exif-strip.ts`) |
| `package.json#scripts` entries | (existing) | (+1: `backfill-exif-strip`) | +1 |
| Server-side image processing surfaces | 1 (`putAvatar`) | **2** (`putAvatar` + backfill script) | +1 |
| Privacy-by-default surfaces | 1 (forward EXIF strip) | **2** (forward + retroactive) | +1 |
| Retroactive-privacy-correction surfaces | 0 | **1** | **+1** (first in project history) |

### Surviving follow-ons

**Class B — ~10 items in 5 categories** (Phase-19 Class B carried
minus EXIF backfill closed Unit 20.1 + 3 Phase-20 backfill
operational follow-ons + 1 operational doc):

- **B.1 Image-processing expansion (4)**: Q68 expansion content
  moderation; cropping UI; server-side resizing; WebP/AVIF
  format conversion. All ADR-0019 D-F carryovers.
- **B.2 Phase-17 markdown subset expansion (8 carried)**: tables
  / footnotes / images / syntax-highlighted code blocks / bio
  length expansion / `@mentions` / task-list interactivity /
  live preview client boundary.
- **B.3 Phase-18 surface expansion (3 carried + Q72)**: markdown
  rationale; methodology page markdown unification; per-challenge
  detail page; Q72 `reviewNotesSchema` divergence.
- **B.4 Phase-20 backfill operational follow-ons (3 newly
  surfaced)**: `--user-id` flag; `--verbose` flag; session-level
  dry-run sentinel.
- **B.5 Operational + infrastructure (6; +1 from Phase 19)**:
  Q69 / Q54 / Q55 operational gates; CI dummy `AUTH_SECRET`;
  `pnpm db:migrate` doc; first LHCI run; middleware-based
  auth-route protection; `pnpm backfill-exif-strip` operational
  documentation.

**Class C** — pre-existing carryovers unchanged.

### Phase-21+ Q-candidates flagged

**8 candidates carry** (unchanged from Phase 19; Phase 20 added
zero new and promoted zero):

- Q59 (CLI emit-challenge-action) — carried Phase-12 through
  Phase-20 = **9 phases**.
- Q60 / Q61 / Q62 / Q64 / Q65.
- Q68 expansion (content moderation on uploaded images;
  ADR-0019 D-F integration point + Phase-20 backfill pattern
  reusable for content-moderation backfill if Q68 expansion
  ships).
- Q72 (`reviewNotesSchema` divergence).

### Phase 21+ entry conditions

Per §12 cardinal rule. **Sixteenth "Continue" override
opportunity** in Unit 21.0 prep. Phase 21+ thread options (all
inferred-not-§13):

- **A. Q68 expansion content moderation** — ADR-0020 candidate
  for moderation API; ADR-0019 D-F inheritance contract already
  documents `.rotate()` ↔ `.toBuffer()` integration point;
  **Phase-20 backfill pattern reusable** if scope includes
  retroactive scan. ~3-5 units (4-5 if backfill included).
- **B. Cropping UI** (per ADR-0019 D-F) — `sharp.extract()`
  inserted between `.rotate()` and `.toBuffer()` + client crop
  region selection. ~3-5 units. Couples to lazy-loaded client
  dep to preserve 103 kB First Load JS invariant.
- **C. Server-side resizing + WebP/AVIF format conversion** (per
  ADR-0019 D-F) — `sharp.resize().webp()` insertion. ~1-2 units
  if observation shows bandwidth/render-time concern.
- **D. Q72 markdown subset extensions** — `reviewNotesSchema`
  divergence (tables / footnotes for curator reviewNotes). ~2-3
  units.
- **E. Per-challenge detail page** (Phase-11 + 13 carryover) —
  couples to Phase-18 line-clamp UX. ~2-3 units; may not need
  new ADR.
- **F. Markdown rating-action `rationale`** — couples to (E).
  ~2-3 units.
- **G. Q64 per-user privacy opt-out** — ADR-0015 D-D Phase-15+
  deferral. ~3 units; 4th ALTER migration (breaks 4-consecutive
  0-migration streak).
- **H. Multi-provider OAuth expansion** — Phase-9 Class B item
  8 carried **10 phases**. **ADR-0020 candidate**. ~3-4 units.
- **I. Subscriber-list email** — Phase-5 D-4 punt carried **14+
  phases**. ~6 units.
- **J. Q59 CLI emit-challenge-action** — carried **9 phases**.
  Smallest reasonable scope.
- **K. `scripts/cleanup-orphan-blobs.ts`** — Phase-16 carryover;
  another operational-script-keystone phase per Phase-20
  precedent. ~1-2 units. Tidies orphan Vercel Blob storage
  accumulated from delete-on-replace + backfill operations.

**DB-migration trigger re-eval at Phase 21 kickoff**: trigger (a)
FIRED Unit 9.6; trigger (b) cold. Phase 21 migration count
depends on chosen thread.

### Risk surface at HEAD `da195d1`

1. **Three operational gates** (Q54 / Q55 / Q69) pending curator.
2. **Backfill script untested against live data Phase 20** —
   manual curator `--dry-run` verification required before
   first execution run. Script logic exercises Phase-19-tested
   `sharp` pipeline + Phase-16-tested Vercel Blob primitives;
   orchestration logic verified via typecheck + lint only.
3. **Orphan blob accumulation tolerated** until cleanup script
   lands (Phase-16 carryover; Phase-20 backfill creates additional
   orphans on each run — old-URL `del()` is best-effort).
4. **No content moderation on uploaded images** (Q68 expansion
   deferred Phase 21+).
5. **No live-preview in edit form** (Phase 17 + 18 deferral).
6. **Sharp cold-start cost** (~100 ms per first invocation;
   amortized; Phase-20 backfill amortizes across batch).

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token (Q69), NOT FR content backfill (Q51
curator-track), NOT moderation / cropping / resizing / WebP
(Phase 21+), NOT live backfill run (curator operational task;
NOT a code-shipping unit), NOT destructive cleanup (Class C
still gated). This unit is the **closure**, not the **unblock**.

### Parallel-curator awareness

Phase 20 shipped as a **single-session phase** — primary shipped
all 5 units 20.0 → 20.4 in one coherent session. Matches Phase
17 + 18 + 19 single-session pattern. **Four consecutive
single-session phases** (Phase 17 + 18 + 19 + 20) demonstrate
that the constitution + auto-memory + ADR ledger converge work
regardless of session count given well-scoped phases.

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline unchanged).
- File added: `docs/thinking/20.4-phase-20-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 20.4 entry.

### CHANGELOG entry shape

Unit 20.4 entry: declares Phase 20 closed (**eleventh NON-§13
phase**; 5 units; 0 deferrals; 0 scope drift; ninth consecutive
well-scoped phase); cites ADR-0019 D-E closure via Unit 20.1
backfill script (**first retroactive-privacy-correction surface**
in project history; **first operational-script-only phase**;
**first zero-new-dep/test/ADR well-scoped phase**); 8 Phase-20
architectural firsts; Phase 19 → 20 delta table (0 ADR Δ; 0
migration Δ; 0 runtime dep Δ; 0 test Δ; 0 bundle Δ; +1 `scripts/`
file; +1 retroactive-privacy-correction surface); Class B
follow-ons (~10 items in 5 categories; 4 image-processing
expansion + 8 Phase-17 markdown carryovers + 3 Phase-18 surface
carryovers + Q72 + 3 Phase-20 backfill operational follow-ons +
6 operational); 8 Phase-21+ Q-candidates (Q59 9-phase carryover;
Q60-62; Q64-65; Q68 expansion; Q72); ADR-0020 candidate slot
open after **7-phase ADR cycle without multi-provider-OAuth
claim** (Phase-9 Class B item 8 carried 10 phases — strongest
architectural patience signal); Phase 21+ thread options
(strongest patience signals: Phase-9 Class B item 8 = 10 phases
multi-provider OAuth; Phase-5 D-4 punt = 14+ phases subscriber-
list email; Phase-12 D-D = 9 phases Q59 CLI); sixteenth
"Continue" override opportunity per §12; four consecutive
single-session phases (Phase 17 + 18 + 19 + 20); fourth
consecutive 0-migration phase.
