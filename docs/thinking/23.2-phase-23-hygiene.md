# 23.2 — Phase-23 hygiene status pass (0 Class A / ~8 Class B / Class C unchanged; twelve consecutive well-scoped phases)

Third Phase-23 unit; docs-only. Mirrors Phase-13 through Phase-22
hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-23 cleanup: **0 items**

Phase-23's single code unit (23.1) shipped without deferrals.
The prior-action auto-fill + signals_considered auto-gather
contract is realized in `lib/rating-challenges/scaffold.ts`
(`readPriorRatingAction` + `buildRatingActionYaml` priorAction
extension) + `scripts/emit-challenge-action.ts` (CLI
orchestration) + 10 new vitest tests. Phase-23-deferred
refinements (`--no-prior-fill` flag for opt-out) are explicit
scope-cap decisions made in Unit 23.0 D-11 — NOT Class A.

**Phase-23 Class A count = 0** matches Phase-12 through Phase-22
well-scoped pattern — **twelve consecutive phases** ship with 0
Class A items.

### Class B — Phase-23 follow-ons: **~8 items in 7 categories**

#### B.1 Image-processing expansion (4; carried from Phase 20 unchanged)

Q68 expansion content moderation; cropping UI; resizing;
WebP/AVIF.

#### B.2 Phase-17 markdown subset expansion (8; carried unchanged)

#### B.3 Phase-18 surface expansion (3 + Q72; carried unchanged)

#### B.4 Phase-20 backfill operational follow-ons (3; carried unchanged)

`--user-id` + `--verbose` + dry-run sentinel for
`backfill-exif-strip`.

#### B.5 Phase-21 cleanup operational follow-ons (2; carried unchanged)

`--prefix` + `--verbose` for `cleanup-orphan-blobs`.

#### B.6 Phase-22 scaffold operational follow-ons (**2 carried; 2 closed Phase 23**)

- ~~Prior-action auto-fill~~ — **CLOSED Phase 23 Unit 23.1**.
- ~~`signals_considered` auto-gather~~ — **CLOSED Phase 23 Unit
  23.1** (coupled with prior-action auto-fill per Unit 23.0
  D-1).
- Methodology-version dynamic read (~0.5 units) — carried;
  bundle Phase 24+ if/when methodology v2 lands.
- Batch-emit-multiple via `--all-under-review` (~0.5 units) —
  carried; Phase 24+ if curator demand.

#### B.7 Phase-23 scaffold operational follow-ons (1; Phase-23 newly surfaced)

- **`--no-prior-fill` opt-out flag** — Phase 23 ships auto-fill
  always-on. Curator may want a flag to force placeholder
  behavior (e.g., when prior data is suspect or curator
  explicitly wants to re-author OTHER 4 dimensions from scratch).
  ~0.5 units when promoted. Phase 24+ if curator demand.

#### B.8 Operational + infrastructure (8; unchanged from Phase 22)

Q69 / Q54 / Q55 operational gates; CI dummy `AUTH_SECRET`;
`pnpm db:migrate` doc; first LHCI run; middleware-based
auth-route protection; `pnpm backfill-exif-strip` operational
doc; `pnpm cleanup-orphan-blobs` operational doc; `pnpm
emit-challenge-action` operational doc.

### Class C — pre-existing carryovers (unchanged)

All ~25 Class C items persist unchanged.

### Phase-22 → Phase-23 delta

| Metric | Phase 22 close (`b353641`) | Phase 23 close pre-this (HEAD `18c44b2`) | Δ |
|---|---|---|---|
| Commits | 5 | 2 + this hygiene + 23.3 + 23.4 (anticipated) | +5 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | **19** | **19** | **0** (Phase 23 ships no new ADR; **fifth phase since Phase 3** — Phase 18 + 20 + 21 + 22 + 23; **four-phase no-new-ADR streak** — first ever) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (**seventh consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 (**zero new env vars Phase 23**) |
| Runtime deps | (Phase-22 stack) | (Phase-22 stack) | 0 (**zero new deps Phase 23**) |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 575 / 55 files | **585 / 55 files** | **+10 / 0** (**first phase to add tests without adding test files**; Phase 22 added +1 file via new `scaffold.test.ts`) |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS resolved | 26 | 26 | 0 (anticipated 0 in Unit 23.3) |
| OPEN_QUESTIONS open | 29 | 29 | 0 |
| OPEN_QUESTIONS lean | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 59 | 59 | 0 (**first four-phase Q-tally freeze anticipated** — Phase 20 + 21 + 22 + 23) |
| i18n keys per locale | 127 | 127 | 0 |
| LHCI URLs | 19 | 19 | 0 |
| `lib/storage/` files | 2 | 2 | 0 |
| `lib/rating-challenges/` files | 5 (+ 4 .test.ts) | 5 (+ 4 .test.ts) | **0** (extends existing `scaffold.ts` + `scaffold.test.ts` in-place) |
| `scripts/` files | 9 | 9 | **0** (extends existing `emit-challenge-action.ts` in-place) |
| `package.json#scripts` entries | (+1: `emit-challenge-action`) | (unchanged) | 0 |
| Server-side image processing surfaces | 2 | 2 | 0 |
| Privacy-by-default surfaces | 2 | 2 | 0 |
| Retroactive-privacy-correction surfaces | 1 | 1 | 0 |
| Storage-reconciliation surfaces | 1 | 1 | 0 |
| YAML-scaffolding surfaces | 1 | **1** (extended) | 0 (in-place extension to Phase-22 surface) |
| Filesystem-read surfaces (operational scripts) | 0 | **1** | **+1** (first in project history) |
| Operational-script-keystone phases | 3 | **4** | **+1** (Phase 20 + 21 + 22 + 23 = **pattern entrenched**) |

### Architectural firsts shipped in Phase 23

1. **First filesystem-read surface in an operational script** —
   Phase 20 (Vercel Blob put/del) + Phase 21 (Vercel Blob list +
   DB select) + Phase 22 (DB select) all stayed within DB/blob
   boundaries; Phase 23 adds `content/problems/<slug>/ratings/*.yaml`
   read via `fs/promises.readdir` + `readFile` + `parseYaml` +
   `RatingActionSchema.parse`.
2. **First script that consumes existing `content/` rather than
   producing into `drafts/`** — Phase 5's `extract-leaderboard`
   produces drafts from PDF, Phase 22's `emit-challenge-action`
   produces drafts from DB, Phase 23 enhances Phase-22 to also
   CONSUME existing content. Single script crosses both
   boundaries.
3. **Fourth consecutive operational-script-keystone phase** —
   Phase 20 + 21 + 22 + 23. **Pattern entrenched** (was
   "crystallized" at 3 phases; "entrenched" at 4 = recurring
   roadmap shape, not an episodic interlude).
4. **Seventh consecutive 0-migration phase** (Phase 17-23; 10
   of 14 phases since DB landed are 0-migration).
5. **Seventh consecutive single-session phase** (Phase 17-23).
6. **Fifth phase since Phase 3 to close without new ADR** (Phase
   18 + 20 + 21 + 22 + 23; **four-phase no-new-ADR streak** —
   first ever).
7. **First four-phase Q-tally freeze anticipated** (Phase 20 +
   21 + 22 + 23).
8. **Closes Phase-22 B.6 items in the next phase** — tightest
   follow-on-close cadence (Phase-22 surfaced; Phase-23 closed
   the two highest-value items in the same shape).
9. **First phase to add tests without adding test files** — Phase
   22 added +1 file (`scaffold.test.ts`); Phase 23 adds +10
   tests to the existing file in-place. Demonstrates the pure-
   function-extraction precedent (Phase 22) scales to follow-up
   extensions naturally.
10. **First in-test tempdir pattern** — Phase 23's
    `readPriorRatingAction` tests use `mkdtemp` + `afterEach`
    cleanup for fixture management. Phase-22 tests didn't need
    filesystem fixtures (pure-function string-in / string-out).
    Establishes the pattern for future operational scripts that
    need filesystem reads.
11. **First in-place code unit** — Phase 20 / 21 / 22 each added
    a new `scripts/*.ts` file; Phase 23's code unit (23.1)
    modifies existing files in-place (no new `scripts/` file or
    `lib/` module). Operational-script-keystone-phase shape
    accommodates both new-script and extend-existing-script
    scopes.

### Phase-23+ Q-candidates flagged

Carryovers from Phase 22 (7 candidates carried; 0 promoted Phase
23; 0 new Phase 23):

- **Q60** (curator authz evolution).
- **Q61** (submitter anonymity option).
- **Q62** (rejection-rationale public visibility).
- **Q64** (per-user privacy opt-out).
- **Q65** (per-curator activity feed).
- **Q68 expansion** (content moderation on uploaded images;
  ADR-0019 D-F integration point + Phase-20 backfill + Phase-21
  cleanup + Phase-23 prior-read patterns all reusable for
  retroactive moderation backfill).
- **Q72** (`reviewNotesSchema` divergence).

### Phase-24+ ADR slot reservations

**ADR-0020 candidate slot remains open after 10-phase ADR cycle
without multi-provider-OAuth claim** (Phase-15 ADR-0016 +
Phase-16 ADR-0017 + Phase-17 ADR-0018 + Phase-18 NO-new-ADR +
Phase-19 ADR-0019 + Phase-20 NO-new-ADR + Phase-21 NO-new-ADR +
Phase-22 NO-new-ADR + Phase-23 NO-new-ADR = 4 ADR slots since
Phase-15 + 6 cycles without multi-provider claim):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried **13 phases** — strongest architectural patience
  signal growing each cycle without claim).
- **Image-processing API content moderation** (Q68 expansion).
- **Markdown subset extensions ADR** (Phase-17 D-H + Q72) if
  signal demands.
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  **17+ phases** — strongest overall patience signal growing).
- **Per-challenge detail page** (Phase-11 + 13 carryover; may
  not need new ADR).

### Four consecutive operational-script-keystone phases pattern

| Property | P20 (backfill-exif) | P21 (cleanup-orphans) | P22 (emit-challenge) | P23 (auto-fill) |
|---|---|---|---|---|
| Units | 5 | 5 | 5 | 5 |
| Code units | 1 | 1 | 1 | 1 |
| New ADRs | 0 | 0 | 0 | 0 |
| New deps | 0 | 0 | 0 | 0 |
| New env vars | 0 | 0 | 0 | 0 |
| New vitest tests | 0 | 0 | +11 | **+10** |
| New `scripts/` files | 1 | 1 | 1 | **0 (in-place)** |
| New `lib/` modules | 0 | 0 | +1 | **0 (in-place)** |
| Closes carryover | ADR-0019 D-E | Phase-16+20 (class-B) | ADR-0014 D-D | Phase-22 B.6 items 1+2 |
| Carryover phase-count | 1 phase | 6 phases | 10 phases | **1 phase (tightest follow-on cadence)** |

**Pattern entrenched.** Phase 22 + 23 demonstrated the shape
accommodates pure-function-testable surfaces AND in-place
extensions to existing surfaces.

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/23.2-phase-23-hygiene.md` (this).
- CHANGELOG: Unit 23.2 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 23.2 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-22 well-scoped pattern (twelve
consecutive); ~8 Class B items in 8 categories (4 image-processing
carried; 8 markdown carried; 3 Phase-18 carried + Q72; 3
Phase-20 backfill carried; 2 Phase-21 cleanup carried; **B.6
items 1+2 CLOSED Phase 23** + 2 carried; **B.7 Phase-23
`--no-prior-fill` opt-out newly surfaced**; 8 operational +
infrastructure unchanged); Class C carryovers unchanged; Phase
22 → 23 delta (0 ADR Δ; 0 migration Δ; 0 dep Δ; 0 env var Δ;
**+10 tests / 0 file** — first phase to add tests without
adding test files; 0 bundle Δ; **0 `lib/` Δ + 0 `scripts/` Δ —
in-place extension** to Phase-22 surfaces; +1 filesystem-read
surface in operational script; +1 operational-script-keystone
phase counter = pattern **entrenched**); **11 Phase-23
architectural firsts** (incl. **first filesystem-read in
operational script**, **first content-consumer in script**,
**fourth consecutive operational-script-keystone**, **first
four-phase no-new-ADR streak**, **first four-phase Q-tally
freeze anticipated**, **first phase to add tests without test
files**, **first in-test tempdir pattern**, **first in-place
code unit**); **7 Phase-24+ Q-candidates** carry unchanged;
ADR-0020 candidate slot remains open after **10-phase ADR cycle
without multi-provider-OAuth claim** (Phase-9 Class B item 8
carried **13 phases**; Phase-5 D-4 carried **17+ phases**); four
consecutive operational-script-keystone phases pattern (Phase 20
+ 21 + 22 + 23) entrenched table.
