# 43.0 — Phase 43 prep (second cross-surface expansion of a Phase-37-framework consumer: tables expand from `reviewNotes`-only to all 4 markdown surfaces via constructor-arg change in `PHASE_39_DEFAULT_ENABLED_SURFACES`; mirrors Phase 42 wikilinks-expansion pattern verbatim; closes ADR-0018 APPEND-D-Q item 2 deferral at 4-phase carryover; first two-letter APPEND letter wrap (APPEND-D-AA after D-Z consumption Phase 42); anticipated 4-5 units; **38th "Continue" override**; **thirty-fourth NON-§13 phase**)

Phase 43 kickoff per `MASTER_PROMPT.md` §12 cardinal rule. Phase 42
closed at HEAD `99b4764` (Unit 42.4 acceptance gate; **thirty-
third NON-§13 phase**; 5 units; first cross-surface expansion
of a Phase-37 framework consumer realized via wikilinks
expansion to all 4 surfaces + first canonical same-surface
different-slot composition + first single-letter alphabet wrap
deferral). This unit is docs-only.

> **Sign-off note**: §12 requires explicit human sign-off at every
> phase boundary. Phase 43 sign-off was granted **via "Continue"
> override** (**thirty-eighth invocation** of this pattern). The
> first-thread D-1 recommendation is overridable into Unit 43.1.

> **§13 ledger status (carried)**: still **CLOSED**. Phase 43 is
> the **thirty-fourth NON-§13 phase**.

## THINK

### Phase 42 → Phase 43 baseline (HEAD = `99b4764`)

- **Tests**: **895/895 across 71 vitest files**.
- **ADRs**: **24** (Phase 42 added 0 new ADRs; ADR-0025 candidate
  slot still open since Phase 35).
- **ADR-0018 D-G**: **9 APPENDs** (project record; Phase 18 +
  27 + 29 + 37 + 38 + 39 + 40 + 41 + 42). Single-letter slot
  CONSUMED through Z at Phase 42 ship (APPEND-D-Z); Phase 43+
  wraps to two-letter form (D-AA + ...) per Phase-42-prep D-4
  Option β.
- **First Load JS shared chunk**: **103 kB UNCHANGED** through
  every Phase 9-42 unit (**76 consecutive units**).
- **Middleware bundle**: **160 kB UNCHANGED** since Phase 12.
- **DB tables**: 7 unchanged; **Migrations**: 9 unchanged.
- **Env vars**: **14** (`MARKDOWN_EXTENSIONS` recognized values:
  `default` + `wikilinks` + `tables` + `arxiv` + comma-separated
  compositions; 4 single-value arms; multi-value composition
  shape including 3-way).
- **`lib/markdown/extensions/` files**: **13** (5 framework +
  wikilinks.ts + wikilinks.test.ts + tables.ts + tables.test.ts
  + composite.ts + composite.test.ts + arxiv.ts +
  arxiv.test.ts).
- **Phase-37-framework concrete consumers**: **3**
  (`WikilinkExtensionRegistry` + `TablesExtensionRegistry` +
  `ArxivExtensionRegistry`) exercising **3 of 3 slots**.
- **Composition arity**: 3-way (validated end-to-end Phase 41).
- **Markdown surfaces enabled by ≥1 consumer**: **4 of 4**
  (Phase 42 closed the `bio` gap via wikilinks expansion;
  4-of-4 surface-coverage state achieved).
- **Per-consumer default-enabled-surfaces constants** (Phase
  43 starting state):
  - `PHASE_38_DEFAULT_ENABLED_SURFACES = Set(["bio", "reviewNotes", "rationale", "actionRationale"])` — wikilinks (4 surfaces; Phase 42 expansion).
  - `PHASE_39_DEFAULT_ENABLED_SURFACES = Set(["reviewNotes"])` — tables (1 surface; Phase 39 ship).
  - `PHASE_41_DEFAULT_ENABLED_SURFACES = Set(["rationale"])` — arxiv (1 surface; Phase 41 ship).
- **OPEN_QUESTIONS state**: 28 resolved + 4 lean + 34 open =
  **66 total** UNCHANGED since Phase 36.
- **Phase-37+ candidate count**: **8** UNCHANGED.
- **Working tree at session start**: clean (Phase 42 close
  committed at `99b4764`).

### D-1. First-thread recommendation: **tables cross-surface expansion to all 4 markdown surfaces via constructor-arg change in `PHASE_39_DEFAULT_ENABLED_SURFACES`**

Closes:

1. **ADR-0018 APPEND-D-Q item 2 deferral** (Phase 39 documented
   "Cross-surface table expansion — bio + rationale +
   actionRationale could enable tables; demand-signal-first;
   constructor-arg change with zero plugin or registry
   rework"). Phase 43 is the demand-signal-relaxed realization
   mirroring the Phase-42 wikilinks pattern verbatim.
2. **Phase-42-gate Phase-43-candidate rank 1** ("Tables
   cross-surface expansion (analogous Phase-43 candidate to
   Phase-42 wikilinks; constructor-arg-only change)").

**Why tables over arxiv** (the two remaining cross-surface-
expansion candidates after Phase 42):

- **APPEND-doc-letter older**: APPEND-D-Q (Phase 39) is older
  than APPEND-D-Y (Phase 41). Closing in age-order parallels
  the Phase-42 rationale (APPEND-D-L Phase-38 oldest of
  three).
- **Schema-augmentation surface coverage**: tables's
  `schemaOverrides` augments the per-surface sanitize allow-
  list with table tags. Pre-Phase-43 only `reviewNotes`
  rendered tables; post-Phase-43 ALL 4 surfaces render
  tables. This is the **first phase to put `schemaOverrides`
  on the `bio` + `rationale` + `actionRationale` surfaces**
  (those surfaces had base allow-list only Phase 18/27/29
  ship).
- **Composition test surface expansion**: Phase 42 introduced
  the first canonical same-surface different-slot composition
  (wikilinks rehype + tables schema on reviewNotes); Phase
  43 expansion creates **same-surface different-slot
  composition on ALL 4 surfaces** under the default
  `MARKDOWN_EXTENSIONS=wikilinks,tables` 2-way + 3-way
  configurations. Architecturally richer.

**Why not arxiv first**: arxiv expansion to all 4 surfaces is
the Phase-44+ candidate (analogous, but less architecturally
novel since arxiv's `remarkPlugins` slot doesn't change the
per-surface schema). Tables's schema-augmentation has more
downstream test surface to verify; arxiv's text-to-link
transform is more uniform across surfaces.

**Path A** (no new ADR; APPEND ADR-0018 D-G) chosen per
Phase-37 through 42 D-9 precedent. ADR-0018 D-G gains its
**10th APPEND** (record extends 9 → 10; **first two-letter
APPEND letter D-AA** in project history per Phase-42-prep
D-4 Option β). ADR-0025 candidate slot stays open.

### D-2. DB-migration trigger re-evaluation

Phase 43 anticipates **0 new migrations**. Pure constant-value
change in a TypeScript module; no schema change. §5.7 trigger
(a) FIRED Unit 9.6 carried; trigger (b) cold Phase 43.

### D-3. Cross-surface tables expansion shape anticipated (Unit 43.1)

D-A. **`PHASE_39_DEFAULT_ENABLED_SURFACES` value change**
(mirrors Phase-42 Unit 42.1 wikilinks expansion verbatim):

```ts
// Before (Phase 39 ship through Phase-42 close):
export const PHASE_39_DEFAULT_ENABLED_SURFACES: ReadonlySet<MarkdownSurface> =
  new Set(["reviewNotes"]);

// After (Phase 43 ship):
export const PHASE_39_DEFAULT_ENABLED_SURFACES: ReadonlySet<MarkdownSurface> =
  new Set(["bio", "reviewNotes", "rationale", "actionRationale"]);
```

**Name discipline preserved** per Phase-42 D-8 precedent —
constant NAME encodes WHEN it was introduced (Phase 39); VALUE
evolves Phase 43. Phase-42 establishes the audit-trail
convention; Phase 43 follows it.

D-B. **`TablesExtensionRegistry` class — UNCHANGED**. The
class accepts a `ReadonlySet<MarkdownSurface>` constructor arg;
per-surface dispatch in `getExtensions` is already generic.
Expanding the default constant requires zero plugin / registry
/ factory edits. Validates the **constructor-arg-only zero-
rework expansion property** for the SECOND framework consumer
(Phase 42 validated it for wikilinks).

D-C. **Factory dispatch arm — UNCHANGED**.
`getExtensionRegistry()` reads `PHASE_39_DEFAULT_ENABLED_SURFACES`
when constructing `TablesExtensionRegistry`; the change
propagates automatically.

D-D. **Tests to update / add** (mirrors Phase-42 Unit-42.1
shape):

1. **`tables.test.ts` — `PHASE_39_DEFAULT_ENABLED_SURFACES`
   assertions**: the 1 test asserting `Set(["reviewNotes"])`
   default + counts becomes a 4-surface assertion (mirrors
   Phase-42 Unit-42.1 wikilinks.test.ts update). UPDATE in
   place.
2. **`extensions/index.test.ts` — tables dispatch arm**: the
   existing test "TablesExtensionRegistry dispatch enables
   tables on reviewNotes only Phase 39" updated to "all 4
   surfaces Phase 43". UPDATE in place.
3. **`lib/markdown/index.test.ts` end-to-end tests**: the
   existing Phase-39 tables-consumer + Phase-40/41 composite
   blocks all use MANUAL constructor args (`new
   TablesExtensionRegistry(new Set(["reviewNotes"]))`) — those
   tests verify the consumer-class property under explicit
   single-surface enabled sets. PRESERVE the existing tests
   verbatim.
4. **NEW end-to-end tests**: add a new `describe("Phase-43
   tables default — all 4 surfaces under MARKDOWN_EXTENSIONS=tables")`
   block exercising the factory-driven Phase-43 default
   (tables on bio + rationale + actionRationale newly-enabled;
   reviewNotes Phase-39 baseline).
5. **NEW composition tests**: extend the `describe("Phase-42
   same-surface different-slot composition under default
   dispatch")` shape with Phase-43 expanded tables defaults
   — under 3-way `wikilinks,tables,arxiv` with both wikilinks
   AND tables enabled on all 4 surfaces, every surface has
   wikilinks(rehype) + tables(schema), and rationale also has
   arxiv(remark). All surfaces are same-surface different-
   slot composition cases.

D-E. **Composition matrix Phase 43 expansion** (Phase-43
ADR-0018 D-G APPEND-D-AA documents):

| Surface | `wikilinks,tables` (Phase 43 default) | `wikilinks,arxiv` | `wikilinks,tables,arxiv` (3-way) |
|---|---|---|---|
| `bio` | wikilinks(rehype) + tables(schema) | wikilinks(rehype) only | wikilinks(rehype) + tables(schema) |
| `reviewNotes` | wikilinks(rehype) + tables(schema) | wikilinks(rehype) only | wikilinks(rehype) + tables(schema) |
| `rationale` | wikilinks(rehype) + tables(schema) | wikilinks(rehype) + arxiv(remark) | wikilinks(rehype) + tables(schema) + arxiv(remark) |
| `actionRationale` | wikilinks(rehype) + tables(schema) | wikilinks(rehype) only | wikilinks(rehype) + tables(schema) |

**Pre-Phase-43 (Phase 42 close)**: only `reviewNotes` had
both wikilinks (rehype, Phase 42 expansion) + tables (schema,
Phase 39 default); the other 3 surfaces had wikilinks only.

**Post-Phase-43**: ALL 4 surfaces have wikilinks (rehype) +
tables (schema). The 3-way default with arxiv adds arxiv
(remark) to rationale only. **All surfaces remain conflict-
free** under APPEND-D-R because each surface has at most one
component per slot (wikilinks owns rehype; tables owns schema;
arxiv owns remark when present).

**First "all 4 surfaces with same-surface different-slot
composition" state** in project history Phase 43 ship.
Pre-Phase-43: only 1 surface (reviewNotes) had wikilinks +
tables co-rendering. Post-Phase-43: all 4 surfaces co-render.

D-F. **`PHASE_41_DEFAULT_ENABLED_SURFACES` UNCHANGED**: arxiv
stays `rationale`-only. Phase 43 scopes ONE consumer for
expansion (tables) to maintain unit-size discipline + preserve
audit trail per consumer + mirror Phase-42 per-phase-one-
expansion-consumer precedent. Phase 44+ may expand arxiv if
appropriate.

D-G. **APPEND letter sequence: first two-letter D-AA**. Phase
42 APPEND-D-Z consumed the last single-letter slot. Phase 43's
APPEND is **D-AA** (first two-letter form). Subsequent Phase
44+ APPENDs would be D-AB, D-AC, ... through D-AZ, then D-BA,
... (Excel-spreadsheet column convention; documented in
Phase-43 APPEND-D-AA itself for posterity).

D-H. **Phase 44+ deferrals** (explicit in Phase-43 ADR-0018
D-G APPEND-D-AA):

- **Arxiv cross-surface expansion** to bio + reviewNotes +
  actionRationale — Phase-41 APPEND-D-Y item 1 deferral
  carries forward to Phase 44+ candidate; analogous
  constructor-arg-only change.
- **Table-specific attributes** — `colspan` / `rowspan` /
  `scope` (APPEND-D-Q item 3) — Phase 44+ if demand surfaces.
- **`<caption>` element** — APPEND-D-Q item 4 — Phase 44+ if
  non-GFM extension surfaces.
- **Surface-specific table schemas** — APPEND-D-Q item 6 —
  Phase 44+ if cross-surface tables emerge with different
  requirements (e.g., bio with no colspan; reviewNotes with
  full table feature set).
- **Multi-anchor wikilink alias syntax** (APPEND-D-L item 2)
  — carries forward.
- **Cross-entity wikilinks** (APPEND-D-L item 3) — carries
  forward.
- **DOI sibling consumer** (APPEND-D-Y item 4) — Phase 44+
  candidate; first compositional same-slot case.
- **`@mention` consumer** (APPEND-D-Y item 7) — Phase 44+
  candidate; conditional on Q73 gate.

### Provisional unit breakdown (43.0 … 43.4)

| Unit | Title | Type |
|---|---|---|
| 43.0 | **Phase 43 prep** (this unit) | docs |
| 43.1 | `PHASE_39_DEFAULT_ENABLED_SURFACES` expansion to 4 surfaces + updated `tables.test.ts` + updated `extensions/index.test.ts` + ADR-0018 D-G APPEND-D-AA | code+APPEND |
| 43.2 | NEW end-to-end tests for tables on bio + rationale + actionRationale under `MARKDOWN_EXTENSIONS=tables` factory dispatch + composition tests verifying all 4 surfaces under 3-way `wikilinks,tables,arxiv` with expanded tables default | code |
| 43.3 | Phase-43 hygiene + ADR-0018 APPEND-D-Q item 2 closure documentation | docs |
| 43.4 | **Phase 43 acceptance gate** | gate |

Total: **5 units** anticipated. Mirrors Phase 42 unit shape
verbatim (Phase 42 was the first cross-surface expansion;
Phase 43 is the second; identical structure).

### Phase-43-blocking decisions deferred to per-unit implementation

- **D-8** (Unit 43.1): Constant naming — keep
  `PHASE_39_DEFAULT_ENABLED_SURFACES` vs rename. Lean = **keep
  Phase-39 name** (audit-trail-friendly; mirrors Phase-42 D-8
  precedent for `PHASE_38_DEFAULT_ENABLED_SURFACES`).
- **D-9** (Unit 43.1): Surface enumeration order — `MarkdownSurface`
  type-union order vs alphabetic. Lean = **`MarkdownSurface`
  type-union order** (mirrors Phase-42 D-9 precedent).
- **D-10** (Unit 43.2): Test-block placement — new `describe("Phase-43
  tables default — all 4 surfaces ...")` block. Lean = **new
  block** (mirrors Phase-42 D-10 precedent).
- **D-11** (Unit 43.1): APPEND-D-AA naming — Excel-spreadsheet
  column convention (A through Z; then AA, AB, ... AZ; then
  BA, BB, ...). Lean = **Excel convention** (most familiar
  letter-wrap pattern; documented in APPEND-D-AA body for
  posterity).

### Smoke gates (this unit)

Docs-only — no source files modified.

- File added: `docs/thinking/43.0-phase-43-prep.md`.
- CHANGELOG: open `### Phase 43` section + Unit 43.0 entry.
- `pnpm audit-content`: 0 errors / 6 warnings (Q32 baseline
  unchanged).
- `pnpm test`: 895/895 across 71 files UNCHANGED.
- First Load JS / middleware: UNCHANGED.

### Parallel-curator awareness

This unit docs-only; no collision. Phase 43 collision risk:

- 43.1: MODIFIED `lib/markdown/extensions/tables.ts` (one
  constant + docstring; LOW); MODIFIED `tables.test.ts` (one
  test); MODIFIED `extensions/index.test.ts` (one dispatch-
  test update); MODIFIED `docs/adr/0018-markdown-sanitization.md`
  (APPEND-D-AA).
- 43.2: MODIFIED `lib/markdown/index.test.ts` (new describe
  blocks; low collision absent another markdown end-to-end
  thread).
- 43.3 / 43.4: docs hygiene + gate; parallel-safe.

Pre-unit checks (`git log --oneline -10` + `git status --short`)
mandatory at every unit start per `feedback_unit_rhythm.md` +
`reference_parallel_curator.md`.

### Alternative threads (overridable into Unit 43.1)

| Thread | Why tempting | Why deferred | Units |
|---|---|---|---|
| **Arxiv cross-surface expansion** (instead of tables) | Equally trivial constructor-arg-only change | Tables has older APPEND deferral (APPEND-D-Q Phase 39 vs APPEND-D-Y Phase 41); closes deferrals in age order; Phase 44+ for arxiv. | 1-2 |
| **DOI sibling consumer** in `remarkPlugins` slot | First compositional same-slot case (two consumers using `remarkPlugins`); architecturally novel | Higher complexity than cross-surface expansion (new consumer authoring); Phase 44+. | 2-3 |
| **Multi-anchor wikilink alias syntax** `[[slug\|display]]` | APPEND-D-L item 2 deferral closes; plugin regex extension | Less architecturally novel than cross-surface expansion; Phase 44+. | 1-2 |
| **Cross-entity wikilinks** | APPEND-D-L item 3 deferral closes; new plugin parameterization | Requires entity-type disambiguation in syntax + plugin options; higher complexity; Phase 44+. | 2-3 |
| **ADR-0025 concrete moderation provider** | Strongest patience signal | Not autonomous-tractable. | 3-5 |
| **`@mention` consumer** | APPEND-D-Y item 4 | Conditional on Q73 gate. | 2-3 |
| **Pre-commit typecheck hook** | Closes Unit-36.1 gap | Tiny (~1 unit). | 1 |
| **Q78 / Q79 / v2 methodology / safeAuth / test backfill / account-delete** | Various follow-ons | Each deferred per existing rationale. | 1-5 each |

### Phase-43 anti-scope (boundary statement preview for Unit 43.4)

NOT arxiv cross-surface expansion (Phase 44+);
NOT DOI sibling consumer (Phase 44+);
NOT multi-anchor wikilink alias syntax (Phase 44+);
NOT cross-entity wikilinks (Phase 44+);
NOT table-specific attributes (`colspan` / `rowspan` /
`scope`; Phase 44+);
NOT `<caption>` element (Phase 44+);
NOT surface-specific table schemas (Phase 44+);
NOT plugin parameterization for wikilink-href-builder
(Phase 44+);
NOT `<a class="wikilink">` styling (Phase 44+);
NOT 404 handling for unresolved wikilinks (Phase 44+);
NOT `@mention` resolution (Phase 44+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-42 deferrals carry forward.

**This phase is the second cross-surface expansion of a
Phase-37-framework consumer**. Mirrors Phase 42 verbatim:
constant-value change; 3 existing tests updated; NEW
factory-dispatch + composition end-to-end tests; ADR-0018
APPEND-D-AA documenting the expansion + Phase-44+ deferrals;
hygiene + gate.

### Anticipated architectural firsts in Phase 43

1. **Second cross-surface expansion of a Phase-37-framework
   consumer**. Phase 42 expanded wikilinks; Phase 43 expands
   tables. Validates the **"constructor-arg-only zero-rework
   expansion" property for two consumers** (each Phase
   38/39/41 consumer documented this property in its APPEND;
   Phase 42 + 43 each ship a realization).
2. **First two-letter APPEND letter in ADR-0018 D-G**:
   APPEND-D-AA (after APPEND-D-Z Phase 42). Validates the
   Phase-42-prep D-4 Option β alphabet-wrap convention with
   Excel-spreadsheet column lettering. Phase 43 is the first
   phase where the wrap takes effect.
3. **First "schemaOverrides on bio + rationale +
   actionRationale" production state**. Pre-Phase-43: those
   three surfaces had base allow-list only (Phase 18/27/29
   ship). Post-Phase-43: all three surfaces get tables-
   augmented allow-list via tables's `schemaOverrides`. First
   `schemaOverrides`-on-bio surface in project history.
4. **First "all 4 surfaces with same-surface different-slot
   composition" state under default dispatch**. Pre-Phase-43:
   only `reviewNotes` had wikilinks(rehype) + tables(schema)
   co-rendering under `MARKDOWN_EXTENSIONS=wikilinks,tables`.
   Post-Phase-43: ALL 4 surfaces co-render under same composition.
5. **Tenth APPEND on ADR-0018 D-G** — extends the **first-
   ADR-D-clause-with-most-APPENDs record** from 9 → 10
   (Phase 18 + 27 + 29 + 37 + 38 + 39 + 40 + 41 + 42 + **43**).
6. **First "framework + 3 consumers + composition + 2
   expansions" 7-phase cluster** in project history (Phase
   37 + 38 + 39 + 40 + 41 + 42 + 43). Seven-phase
   architectural arc: framework definition + 3 consumers +
   composition + first expansion + second expansion.
7. **13th consecutive phase without new B category** —
   Phase 31-43 = first **13-phase run** in project history
   (extends Phase-42 record from 12 → 13).
8. **77th-through-80th consecutive 103 kB First Load JS
   units** carry forward (Phase 43 has 5 units; 43.0 prep
   docs-only doesn't increment; 43.1-43.4 each increment =
   +4).
9. **Thirty-fourth NON-§13 phase**.
10. **Second prep-/APPEND-doc-level deferral closed by value-
    only change in a later phase**. Phase 38 APPEND-D-L item
    1 closed Phase 42; Phase 39 APPEND-D-Q item 2 closes
    Phase 43. **4-phase gap** for both (Phase 38 → 42; Phase
    39 → 43). Establishes a **closure-cadence pattern**:
    one APPEND-deferral resolved per phase, oldest first.

### Open questions newly surfacing

Anticipated: 0 new opens. ADR-0018 APPEND-D-Q item 2 (cross-
surface table expansion deferral) is the LOCUS of Phase 43
closure — that's the resolution site, not OPEN_QUESTIONS.md.
