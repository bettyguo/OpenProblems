# 10.4 — OPEN_QUESTIONS hygiene + ADR review (Phase 10 pre-close)

Fourth Phase-10 unit. Docs-only. Mirrors Unit 5.12 / 6.9 / 7.10 / 8.8 /
9.8 OQ-hygiene precedents. Scans the open-questions ledger for Phase-10
promotions and reviews the 13 ADRs at HEAD for stale status / supersede
markers.

## THINK

### OPEN_QUESTIONS scan

**Phase 10 surfaced 0 new questions.** ADR-0012 (auth provider) and
ADR-0013 (DB choice) covered every Phase-10 implementation decision;
per-unit D-decisions in Unit 10.0 prep + the per-unit implementations
were all tactical-not-architectural. No new Q-numbers needed.

**Phase 10 promotions** — none net. Phase-10 work intersected with
several open Q-numbers but didn't change their status:

| Q   | Phase-10 touch                                                        | Status                                |
| --- | --------------------------------------------------------------------- | ------------------------------------- |
| Q54 | Profile page joins the OAuth-app-registration exposure (`auth()` called inside the route handler; sign-in surface still routes through the same `/api/auth/signin/github` flow). | `open (operational)` — unchanged.     |
| Q55 | Profile page reads `users.githubLogin` from production DB — adds a per-request DB read alongside the Phase-9 session read. Vercel Turso provisioning still gates production. | `open (operational)` — unchanged.     |
| Q56 | `watchlist` table read by `getWatchedSlugs` — exercised in production by profile-page render. Already resolved in Phase 9 (Unit 9.6); no re-classification. | `resolved 2026-05-16 (Unit 9.6)` — unchanged. |
| Q47 | No Discussions-surface change in Phase 10. | `open (operational)` — unchanged.     |

**No other Q-numbers** touched. Ledger entry count: **51** (unchanged
from Phase 9 close).

### Ledger state at HEAD (post-10.3; will be post-10.4 after this commit)

Counts derived directly from the file's `Status:` lines (mechanical
auditable per Unit 9.8 convention):

| Bucket                  | Count | Change from Phase 9 close |
| ----------------------- | ----: | ------------------------- |
| Resolved / decided       | **19** | 0 (Q56 promoted in Unit 9.6 holds)                                                                          |
| Decided-as-lean          |  **4** | 0 (Q38 / Q42 / Q44 / Q51 unchanged)                                                                          |
| Open                     | **28** | 0 (Q54 / Q55 / Q47 stay open; no new Qs surfaced)                                                            |
| **Total**                | **51** | 0                                                                                                              |

Phase 10 is the first phase to surface ZERO new questions. Consistent
with the consolidation framing (Unit 10.0 D-1): Phase 10 lit up
Phase-9 surfaces without introducing new architectural decisions.

### ADR review

**13 ADRs at HEAD** (0001 – 0013). Phase 10 added **zero new ADRs**;
none of the existing ADRs needed prose edits or supersede markers.
ADR-0004 + ADR-0005 + ADR-0011 + ADR-0012 + ADR-0013 are the active
ones for Phase-10 implementation; all hold without amendment.

| ADR  | Title                                                      | Status     | Phase-10 touch                                                                              |
| ---- | ---------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------- |
| 0001 | Next.js App Router                                         | accepted   | No change. Profile page is a server component under the App Router.                          |
| 0002 | Velite                                                     | accepted   | No change.                                                                                  |
| 0003 | Zod 4                                                      | accepted   | No change.                                                                                  |
| 0004 | File-first; no DB through Phase 3                          | accepted   | **Reaffirmed**. Profile page reads file-first content (`problems` from `#site/content`) + USER-STATE DB (`watchlist` + `users.githubLogin`). The separation holds. |
| 0005 | Rating-action immutability                                 | accepted   | No change. Migrations still immutable; no new migration in Phase 10.                         |
| 0006 | Saturation N/A encoding                                    | accepted   | No change.                                                                                  |
| 0007 | DomainMap rendering + D3 import policy                     | accepted   | No change.                                                                                  |
| 0008 | LLM provider = Anthropic + cost-governance                 | accepted   | No change.                                                                                  |
| 0009 | Human-review diff format                                   | accepted   | No change. Phase 10 added no LLM-translated content.                                         |
| 0010 | Discussions backend (Giscus + GraphQL)                     | accepted   | No change.                                                                                  |
| 0011 | i18n strategy (next-intl + sub-path + sibling-file)        | accepted   | **Phase-10 exercise**: `messages.profile.*` (6 keys EN + FR) consumes the sibling-file pattern; profile page renders via `getTranslations()` server-side. No supersede triggers. |
| 0012 | Auth provider: NextAuth.js v5 + GitHub OAuth                | accepted   | **Phase-10 exercise**: profile page is the first protected route consuming `auth()` + `signOut`. D-A through D-E all hold. No supersede triggers. |
| 0013 | Database: Turso (libSQL/SQLite) + Drizzle ORM              | accepted   | **Phase-10 exercise**: profile page reads `users.githubLogin` via Drizzle SELECT + reuses `lib/watchlist/` helpers. D-A through D-F all hold. No supersede triggers. |

**No new ADRs surfaced**; no prose edits needed.

The Phase-9 Class B item 12 ("auth-aware route protection") partially
resolved in Phase 10 doesn't require an ADR — the
server-component-level protection pattern is a tactical implementation,
not an architectural commitment. The middleware-based lift IF it
ever happens would warrant ADR-0014, but that's Phase 11+ scope.

### Cross-phase ledger update

After Unit 10.4:
- **19 resolved** (unchanged from Phase 9 close).
- **4 decided-as-lean** (unchanged).
- **28 open** (unchanged).
- Total entries: **51** (unchanged).

### Phase-10 OQ + ADR delta vs Phase-9 close

- New questions: **0**.
- Promotions: **0**.
- Status changes: **0**.
- New ADRs: **0**.
- ADR prose edits: **0**.
- Supersede markers: **0**.

Phase 10 is the **first zero-architectural-surface phase** in the
project's history. Consistent with the "consolidation" framing
(Unit 10.0 D-1).

### Smoke gates

Docs-only; unchanged from Unit 10.3 snapshot (394/394 tests; ~590
prerendered pages + 2 dynamic API routes + 2 profile route entries;
103 kB First Load JS; middleware 159 kB; 19 LHCI URLs; 0 errors / 6
warnings audit).

- File added: this THINK doc.
- File edited: none.
- CHANGELOG: Unit 10.4 entry.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).
