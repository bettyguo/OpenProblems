# 24.2 — Phase-24 hygiene status pass (0 Class A / ~9 Class B / Class C unchanged; thirteen consecutive well-scoped phases)

Third Phase-24 unit; docs-only. Mirrors Phase-13 through Phase-23
hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-24 cleanup: **0 items**

Phase-24's single code unit (24.1) shipped without deferrals.
Forward-path (`putAvatar` resize+WebP) + backfill script
(`scripts/backfill-resize-webp.ts`) + storage test mock updates
all realized in one code unit. Phase-24-deferred refinements
(AVIF support; AVATAR_SIZE / WEBP_QUALITY tuning if signal
demands; `--user-id` flag on backfill) are explicit scope-cap
decisions per Unit 24.0 D-9 through D-14 — NOT Class A.

**Phase-24 Class A count = 0** matches Phase-12 through
Phase-23 well-scoped pattern — **thirteen consecutive phases**
ship with 0 Class A items.

### Class B — Phase-24 follow-ons: **~9 items in 9 categories**

#### B.1 Image-processing expansion (3 carried; **1 closed Phase 24**)

- Q68 expansion content moderation (ADR-0019 D-F).
- Cropping UI (ADR-0019 D-F).
- ~~Server-side resizing~~ — **CLOSED Phase 24 Unit 24.1**.
- ~~WebP/AVIF format conversion (WebP half)~~ — **CLOSED Phase
  24 Unit 24.1** (AVIF half carried).
- AVIF format conversion — carried; Phase 25+ if bandwidth
  signal demands AVIF on top of WebP.

#### B.2 Phase-17 markdown subset expansion (8; carried unchanged)

#### B.3 Phase-18 surface expansion (3 + Q72; carried unchanged)

#### B.4 Phase-20 backfill operational follow-ons (3; carried unchanged)

`--user-id` + `--verbose` + dry-run sentinel for
`backfill-exif-strip`.

#### B.5 Phase-21 cleanup operational follow-ons (2; carried unchanged)

`--prefix` + `--verbose` for `cleanup-orphan-blobs`.

#### B.6 Phase-22 scaffold operational follow-ons (2; carried unchanged)

Methodology-version dynamic read; batch-emit-multiple for
`emit-challenge-action`.

#### B.7 Phase-23 scaffold operational follow-ons (1; carried unchanged)

`--no-prior-fill` opt-out flag for `emit-challenge-action`.

#### B.8 Phase-24 transcode operational follow-ons (2; Phase-24 newly surfaced)

- **`--user-id` flag for `backfill-resize-webp`** — Phase 24
  ships `--dry-run` only flag. Phase 25+ if curator demands
  per-user surgical re-resize (e.g., user reports broken avatar
  after upload; curator re-runs backfill for that one user).
  ~0.5 units when promoted.
- **AVATAR_SIZE / WEBP_QUALITY tuning** — Phase 24 hardcodes
  512px square @ quality 85. Phase 25+ if hi-DPI signal demands
  768/1024 OR if observed quality issue from quality 85 (e.g.,
  banding on gradient-heavy avatars). ~0.5 units when promoted.

#### B.9 Operational + infrastructure (9; +1 from Phase 23 for new backfill-resize-webp doc)

- Q69 / Q54 / Q55 operational gates.
- CI dummy `AUTH_SECRET`.
- `pnpm db:migrate` doc.
- First LHCI run.
- Middleware-based auth-route protection.
- `pnpm backfill-exif-strip` operational doc.
- `pnpm cleanup-orphan-blobs` operational doc.
- `pnpm emit-challenge-action` operational doc.
- **`pnpm backfill-resize-webp` operational doc** — Phase-24
  newly surfaced.

### Class C — pre-existing carryovers (unchanged from Phase 23)

All ~25 Class C items persist unchanged.

### Phase-23 → Phase-24 delta

| Metric | Phase 23 close (`225d97f`) | Phase 24 close pre-this (HEAD `dbab227`) | Δ |
|---|---|---|---|
| Commits | 5 | 2 + this hygiene + 24.3 + 24.4 (anticipated) | +5 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | **19** | **19** | **0** (Phase 24 ships no new ADR; **sixth phase since Phase 3** — Phase 18 + 20 + 21 + 22 + 23 + 24; **five-phase no-new-ADR streak** — first ever) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (**eighth consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-23 stack) | (Phase-23 stack) | 0 (**zero new deps Phase 24**) |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 585 / 55 files | **585 / 55 files** | **0** (5 existing tests modified in-place; 0 net new) |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS resolved | 26 | 26 | 0 (anticipated 0 in Unit 24.3) |
| OPEN_QUESTIONS open | 29 | 29 | 0 |
| OPEN_QUESTIONS lean | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 59 | 59 | 0 (**fifth consecutive Q-tally freeze anticipated** — Phase 20-24) |
| i18n keys per locale | 127 | 127 | 0 |
| LHCI URLs | 19 | 19 | 0 |
| `lib/storage/` files | 2 | 2 | **0** (in-place edit; both files modified) |
| `scripts/` files | 9 | **10** | **+1** (`backfill-resize-webp.ts`) |
| `package.json#scripts` entries | (+1: `emit-challenge-action`) | (+1: `backfill-resize-webp`) | +1 |
| Server-side image processing surfaces | 2 | **3** | **+1** (`putAvatar` upgrade + new backfill-resize-webp) |
| Retroactive-privacy-correction surfaces | 1 | **2** | **+1** (Phase-20 EXIF backfill + Phase-24 resize-WebP backfill) |
| YAML-scaffolding surfaces | 1 | 1 | 0 |
| Filesystem-read surfaces (operational scripts) | 1 | 1 | 0 |
| Bandwidth-optimization surfaces | 0 | **1** | **+1** (first in project history) |
| Operational-script-keystone phases | 4 | **5** | **+1** (Phase 20-24 = pattern crystallized further) |

### Architectural firsts shipped in Phase 24

1. **First bandwidth-optimization surface** in project history —
   Phase 16 stored avatars as-is; Phase 19 added EXIF strip
   (privacy, not size); Phase 20 backfilled EXIF strip. Phase
   24 introduces explicit bandwidth optimization via resize +
   WebP transcoding. Smaller files = lower bandwidth + storage
   cost + render time.
2. **Fifth consecutive operational-script-keystone phase** —
   Phase 20 + 21 + 22 + 23 + 24. **Pattern stable at 5 phases**.
3. **Eighth consecutive 0-migration phase** (Phase 17-24; 11
   of 15 phases since DB landed are 0-migration).
4. **Eighth consecutive single-session phase** (Phase 17-24).
5. **Sixth phase since Phase 3 to close without new ADR** (Phase
   18 + 20 + 21 + 22 + 23 + 24; **first five-phase no-new-ADR
   streak**).
6. **First concrete user-value-at-deploy** in the operational-
   script-keystone streak — Phase 20-23 closures shipped
   curator-workflow improvements (EXIF privacy backfill / orphan
   storage hygiene / curator scaffold tooling / scaffold
   auto-fill); Phase 24 ships measurable post-deploy effect on
   shipped content (smaller avatar files + bandwidth + render
   savings).
7. **First Phase to edit `lib/storage/` since Phase 19** —
   first-edit-after-five-phases of the storage layer. Demonstrates
   that even long-stable surfaces can be revisited cleanly when
   ADR D-clause carryovers are realized.
8. **First operational-script-keystone phase to ship 0 net new
   tests** — Phase 20+21 shipped 0 new tests (live-infrastructure
   scripts); Phase 22+23 shipped +11/+10 tests (pure-function
   helpers); Phase 24 modifies 5 existing tests in-place
   without adding net new tests. Demonstrates the pattern
   accommodates all three test-delta shapes.
9. **First phase to ship two operational scripts producing
   different storage-key suffixes** — Phase 20 ships
   `-backfill.<ext>`; Phase 24 ships `-resize.webp`. The two
   backfill scripts can coexist + run in either order; output
   suffixes distinguish them in Vercel Blob console.
10. **First phase to remove dead code** — `inferExt()` helper
    in `lib/storage/index.ts` was load-bearing for Phase 16-23
    but becomes dead after Phase 24 (output is always `.webp`
    regardless of input MIME). Phase 24 removes it. Demonstrates
    that operational-script-keystone phases can also include
    code cleanup when refactoring opportunities surface
    naturally.

### Phase-24+ Q-candidates flagged

Carryovers from Phase 23 (7 candidates carried; 0 promoted Phase
24; 0 new Phase 24):

- **Q60** (curator authz evolution).
- **Q61** (submitter anonymity option).
- **Q62** (rejection-rationale public visibility).
- **Q64** (per-user privacy opt-out).
- **Q65** (per-curator activity feed).
- **Q68 expansion** (content moderation; Phase 24 confirms the
  ADR-0019 D-F integration point is well-trodden — moderation
  API integration would follow the same `sharp(buf).rotate()...toBuffer()`
  pipeline shape, with moderation as a between-step).
- **Q72** (`reviewNotesSchema` divergence).

### Phase-25+ ADR slot reservations

**ADR-0020 candidate slot remains open after 11-phase ADR cycle
without multi-provider-OAuth claim** (Phase-15 ADR-0016 +
Phase-16 ADR-0017 + Phase-17 ADR-0018 + Phase-18 NO-new-ADR +
Phase-19 ADR-0019 + Phase-20 NO-new-ADR + Phase-21 NO-new-ADR +
Phase-22 NO-new-ADR + Phase-23 NO-new-ADR + Phase-24 NO-new-ADR =
4 ADR slots since Phase-15 + 7 cycles without multi-provider
claim):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried **14 phases** — strongest architectural patience
  signal growing each cycle).
- **Image-processing API content moderation** (Q68 expansion).
- **Markdown subset extensions ADR** (Phase-17 D-H + Q72).
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  **18+ phases** — strongest overall patience signal growing).

### Five consecutive operational-script-keystone phases pattern

| Property | P20 (backfill-exif) | P21 (cleanup-orphans) | P22 (emit-challenge) | P23 (auto-fill) | P24 (resize+WebP) |
|---|---|---|---|---|---|
| Units | 5 | 5 | 5 | 5 | 5 |
| Code units | 1 | 1 | 1 | 1 | 1 |
| New ADRs | 0 | 0 | 0 | 0 | 0 |
| New deps | 0 | 0 | 0 | 0 | 0 |
| New env vars | 0 | 0 | 0 | 0 | 0 |
| New vitest tests | 0 | 0 | +11 | +10 | **0 (in-place modify)** |
| New test files | 0 | 0 | +1 | 0 | **0** |
| New `scripts/` files | 1 | 1 | 1 | 0 (in-place) | **1** |
| New `lib/` modules | 0 | 0 | +1 | 0 (in-place) | **0 (in-place edit)** |
| Closes carryover | ADR-0019 D-E | Phase-16+20 (class-B) | ADR-0014 D-D | Phase-22 B.6 1+2 | **ADR-0019 D-F** |
| Carryover phase-count | 1 phase | 6 phases | 10 phases | 1 phase | **5 phases** |
| User-value-at-deploy | curator privacy | curator hygiene | curator scaffolder | curator scaffolder | **bandwidth + storage + render** |

**Pattern stable at 5 phases**. Phase 24 is the first to ship
**concrete user-value-at-deploy** — extends the pattern from
"curator-workflow tooling" to "operational-script-keystone
phases can also deliver user-facing value when the right
carryover is closed."

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/24.2-phase-24-hygiene.md` (this).
- CHANGELOG: Unit 24.2 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 24.2 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-23 well-scoped pattern
(**thirteen consecutive**); ~9 Class B items in 9 categories
(3 carried in B.1 image-processing expansion + **2 closed Phase
24** — server-side resizing + WebP format conversion; 8 markdown
carried; 3 Phase-18 carried + Q72; 3 Phase-20 backfill carried;
2 Phase-21 cleanup carried; 2 Phase-22 scaffold carried; 1
Phase-23 scaffold carried; **2 Phase-24 transcode operational
newly surfaced** — `--user-id` flag + AVATAR_SIZE/WEBP_QUALITY
tuning; 9 operational + infrastructure with `pnpm
backfill-resize-webp` operational doc newly surfaced); Class C
carryovers unchanged; Phase 23 → 24 delta (0 ADR Δ; 0 migration
Δ; 0 dep Δ; 0 env var Δ; **0 net test Δ — 5 existing modified
in-place**; 0 bundle Δ; 0 `lib/` file Δ — in-place edit; +1
`scripts/` file; +1 server-side image processing surface; +1
retroactive-privacy-correction surface (now 2); +1
bandwidth-optimization surface (first in project history); +1
operational-script-keystone phase counter); **10 Phase-24
architectural firsts** (incl. **first bandwidth-optimization
surface**, **fifth consecutive operational-script-keystone
phase**, **eighth consecutive 0-migration phase**, **sixth phase
since Phase 3 without new ADR = first five-phase no-new-ADR
streak**, **first concrete user-value-at-deploy in operational-
script-keystone streak**, **first edit to `lib/storage/` since
Phase 19**, **first operational-script-keystone to ship 0 net
new tests**, **first phase shipping two operational scripts
with distinct storage-key suffixes**, **first phase to remove
dead code**); 7 Phase-25+ Q-candidates carry unchanged; ADR-0020
candidate slot remains open after **11-phase ADR cycle without
multi-provider-OAuth claim** (Phase-9 Class B item 8 carried
**14 phases**; Phase-5 D-4 carried **18+ phases**); five
consecutive operational-script-keystone phases pattern table
shows Phase 24 introduces **user-value-at-deploy** column —
extends the pattern from curator-tooling to user-facing-value.
