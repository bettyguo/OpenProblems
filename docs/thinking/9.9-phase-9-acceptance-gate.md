# 9.9 — Phase 9 acceptance gate (Auth + read+write API thread — final §13 Phase-6+ thread)

Phase-9 closing unit. Mirrors Units 1.12 / 2.13 / 3.13 / 4.13 / 5.13 /
6.10 / 7.11 / 8.9. Verifies every §13 deliverable Phase 9 pulled (the
**Auth + read+write API** thread per Unit 9.0 D-1) is operational
locally at HEAD, emits the cross-phase roll-up, and lists Phase-9
follow-ons that survive into Phase 10+.

**This unit closes the §13 Phase-6+ enumeration.** Phase 6 closed
Discussions (Unit 6.10). Phase 7 + 8 closed Bilingual rendering (Units
7.11 + 8.9). **Phase 9 closes Auth + read+write API** (this gate).
After sign-off, the §13 ledger has no remaining Phase-6+ deliverables;
Phase 10+ thread options are inferred-not-§13 (subscriber-list,
monetization, content backfill, HTML shell migration).

## THINK

### §13 Phase-6+ scope clarification

`MASTER_PROMPT.md` §13 names three Phase-6+ threads:

| Thread                                  | Closing unit | Status at HEAD |
| --------------------------------------- | ------------ | -------------- |
| Discussions / talk pages (GitHub Discussions integration) | Unit 6.10 | ✓ Closed Phase 6. |
| Bilingual rendering (FR primary candidate) | Unit 7.11 + Unit 8.9 | ✓ Closed Phase 7 + 8 (infrastructure + route coverage + 2 content surfaces translated; Q51 remaining curator-track). |
| **Read+write API with token auth**       | **Unit 9.9 (this)** | **✓ Closed Phase 9** — auth foundation + ONE write-path (watchlist toggle) operational locally + architecturally complete + Q54 + Q55 operational gates surfaced + Q56 resolved. |

**§13 Phase-6+ enumeration is complete after sign-off on this gate.**
Phase 10+ thread options:
- Subscriber-list (Phase-5 D-4 punt completion; not §13);
- Monetization (premature without API + auth maturity; not §13);
- HTML shell migration / Unit 8.4 unblock (Phase-8 follow-on; not §13);
- Content backfill (Q51 curator-track horizon; not §13);
- Rating-challenge submission write-path (Phase-9 deferred per Unit 9.0 D-5; second write-path; could land as a small Phase-10 expansion of the Phase-9 auth surface).

### §13 Auth + read+write API thread — local pass status

| Surface                                                                                          | Phase-9 unit | Status |
| ------------------------------------------------------------------------------------------------ | ------------ | ------ |
| **ADR-0012** — Auth provider pinned (NextAuth.js v5 + GitHub OAuth + Drizzle adapter + DB-backed sessions + redirect-to-provider UX) | Unit 9.1     | ✓ Accepted; D-A through D-E all exercised in Units 9.3-9.6. |
| **ADR-0013** — DB engine pinned (Turso/libSQL + Drizzle ORM + USER-STATE-only DB; content stays file-first per ADR-0004) | Unit 9.2     | ✓ Accepted; D-A through D-F all exercised in Units 9.3 + 9.6; D-E migration numbering reconciled to drizzle-kit's 0-indexed convention in Unit 9.8. |
| `lib/db/schema.ts` + `lib/db/index.ts` + `drizzle.config.ts` (Drizzle scaffold + env-fallback client) | Unit 9.3     | ✓ 4 NextAuth canonical tables + `githubLogin` text-unique column on `user`; local-dev `file:./local.db` + production `libsql://...turso.io`. |
| `lib/db/migrations/0000_initial_auth.sql` (first migration; NextAuth canonical schema + `githubLogin`) | Unit 9.3     | ✓ 4 tables; 2 FKs with cascade on user delete; 2 unique indexes (`user.email`, `user.githubLogin`). |
| `lib/auth/index.ts` (NextAuth.js v5 wrapper + Drizzle adapter + GitHub provider + DB session strategy + `trustHost`) | Unit 9.4     | ✓ Exports `{ auth, handlers, signIn, signOut }`; provider invoked WITHOUT args (Auth.js v5 auto-discovers `AUTH_GITHUB_ID` + `AUTH_GITHUB_SECRET` per the canonical convention). |
| `app/api/auth/[...nextauth]/route.ts` (Auth.js v5 route handler re-export)                       | Unit 9.4     | ✓ Dynamic ƒ; `export const { GET, POST } = handlers;`. |
| `middleware.ts` composition (`auth((req) => intlMiddleware(req))` — Auth.js outer + next-intl inner) | Unit 9.5     | ✓ Auth-aware redirects get session context for free; next-intl handles locale prefix; matcher excludes `/api/...`. Middleware bundle 159 kB (was 51.8 kB; +~107 kB from Auth.js + Drizzle adapter; server-side; ~15% of Vercel Edge's 1 MB budget). |
| `components/auth-control/index.tsx` (SiteHeader sign-in / sign-out via Auth.js v5 server-action `<form>` pattern) | Unit 9.5     | ✓ Server-rendered + server-action driven; zero client JS; mirrors `ThemeToggle` / `LocaleToggle` `h-9` sizing. |
| `safeAuth()` defensive wrapper in `SiteHeader`                                                   | Unit 9.5     | ✓ Try/catch returning `null` on any `auth()` failure (no-DB CI / fresh clone / transient outage); SSG-safe. |
| `messages.auth.*` namespace (EN + FR; 3 keys: `sign_in` / `sign_out` / `signed_in_fallback`)     | Unit 9.5     | ✓ FR: "Se connecter" / "Se déconnecter" / "Connecté". |
| `lib/db/schema.ts` `watchlist` table (per Q56 resolution: composite PK `(userId, problemSlug)`; FK only on `userId`; `problemSlug` plain text) | Unit 9.6     | ✓ ADR-0013 D-F file-first-for-content preserved; orphan rows tolerated per intentional design. |
| `lib/db/migrations/0001_watchlist.sql` (second migration)                                        | Unit 9.6     | ✓ 1 table; 1 FK with cascade; 1 composite PK; createdAt default via `unixepoch() * 1000`. |
| `lib/auth/index.ts` `events.linkAccount` callback (populates `users.githubLogin` from GitHub OAuth profile on first sign-in) | Unit 9.6     | ✓ Joins DB user identity to file-system `editorial.primary_curator` per ADR-0012 D-E. |
| `lib/watchlist/index.ts` (Drizzle helpers: `isWatched` / `addToWatchlist` / `removeFromWatchlist`; idempotent) | Unit 9.6     | ✓ Auth-agnostic; API route + server actions call after their own session checks. |
| `app/api/v1/watchlist/[slug]/route.ts` (REST surface: POST adds, DELETE removes; auth-required; 401 / 404 / 200 exits) | Unit 9.6     | ✓ Dynamic ƒ; 6 tests across 2 verbs × 3 exit shapes. **§5.7 trigger (a) FIRES** on first POST. |
| `app/api/v1/watchlist/[slug]/route.test.ts` (6 tests)                                            | Unit 9.6     | ✓ Mocks `@/lib/auth` + `@/lib/watchlist`; no real DB or session required. |
| `components/watchlist-toggle/index.tsx` (per-problem star/unstar UI; 3 branches: signed-out / signed-in not-watched / signed-in watched) | Unit 9.6     | ✓ Server component; server-action `<form>` posts read slug from hidden FormData input + re-validate session inside the action; `revalidatePath` after each mutation. |
| `app/[locale]/problems/[slug]/page.tsx` inject `<WatchlistToggle slug={slug} />` in header card  | Unit 9.6     | ✓ Right of `StatusPill`, above "Last curated" line. |
| `messages.watchlist.*` namespace (EN + FR; 4 keys: `watch` / `watching` / `sign_in_prompt` / `aria_label`) | Unit 9.6     | ✓ FR: "Suivre" / "Suivi" / "Se connecter pour suivre" / aria-label. |
| Phase-9 hygiene status pass (Class A in-flight / Class B follow-ons / Class C carryovers)        | Unit 9.7     | ✓ 4 Class A (Q54 / Q55 / CI dummy `AUTH_SECRET` / `pnpm db:migrate` doc) + 12 Class B + 14 Class C. |
| OPEN_QUESTIONS hygiene + ADR review + prose-shift reconciliations                                 | Unit 9.8     | ✓ Q56 promoted resolved; Q54 + Q55 stay open operational; 13 ADRs reviewed; ADR-0012 D-A + ADR-0013 D-E prose-shifts reconciled inline. |

### §14 universal cross-phase contract

- **Lighthouse a11y / perf / SEO ≥ 95 on new pages**: 19 URLs enrolled in
  `lighthouserc.json` UNCHANGED through Phase 9 (no new locale-pilot
  surfaces in Phase 9; auth-aware SiteHeader is observable on all 19
  existing URLs; no enrolment change needed). **First LHCI run
  validating auth-aware SiteHeader on `/en/`** is a Class B follow-on
  (Unit 9.7 item 10); triggers on first preview-deploy PR with Phase-9
  commits. **Documented `html-has-lang` axe-rule risk** on `/fr/...`
  pages survives indefinitely (Phase-7 Unit 7.7 flag + Phase-8 Class
  B item 1).
- **W3C feed validator on `/api/v1/rss.xml` + `/api/v1/digest/<slug>`**:
  Phase-3/5/6/8/9 compound deferral; first preview deploy + Q47
  unblock.
- **Visual-regression baselines** for the auth-aware SiteHeader on
  problem detail pages — Phase-9 deferred follow-on; no Playwright spec
  changes this phase.
- **No auto-merge** (ADR-0009): Phase 9 added no LLM-translated content;
  all schema + auth code authored directly by curator.
- **File-first / no DB** (ADR-0004): held for CONTENT; ADR-0013 D-F
  reaffirms (DB stores USER-STATE only). **§5.7 trigger (a) FIRED in
  Unit 9.6** — first first-party DB write landed. Phase-4 "no user
  accounts" pact broken per the announced consequence (warned 5 phases
  in a row at Units 4.12 / 5.10 / 6.0 / 7.0 / 8.0 D-2).
- **First Load JS shared chunk = 103 kB**: held through every Phase-9
  unit. Auth + DB + watchlist surfaces are entirely server-side;
  sign-in / sign-out / star / unstar are server-actions; the auth-aware
  SiteHeader is async server component. Zero client-bundle delta.
- **Middleware bundle = 159 kB**: server-side; ~15% of Vercel Edge's
  1 MB budget. Monitor as multi-provider OAuth expansion (Class B
  item 8) lands in Phase 10+.

### Phase-9 unit summary (10 commits — Auth + read+write API thread)

| Unit | Commit    | Title                                                                                |
| ---- | --------- | ------------------------------------------------------------------------------------ |
| 9.0  | `3f42fdf` | Phase 9 prep (Auth + read+write API; final §13 thread)                               |
| 9.1  | `094a0a0` | ADR-0012 (Auth provider: NextAuth.js v5 + GitHub OAuth)                              |
| 9.2  | `98e3d52` | ADR-0013 (Database: Turso/libSQL + Drizzle ORM)                                       |
| 9.3  | `5eb0d8f` | DB scaffold (Drizzle + libsql + initial migration)                                    |
| 9.4  | `1370cdb` | NextAuth.js v5 install + `lib/auth/` wrapper                                          |
| 9.5  | `1bb2ede` | Middleware composition + auth-aware SiteHeader UI                                     |
| 9.6  | `b432f59` | **Watchlist write-path (§5.7 trigger (a) fires; Q56 resolved)**                       |
| 9.7  | `86f46a2` | Phase-9 hygiene status pass (Class A / B / C catalog)                                 |
| 9.8  | `c668cbb` | OPEN_QUESTIONS hygiene + ADR review (Q56 promoted; ADR prose-shifts reconciled)       |
| 9.9  | _this_    | Phase 9 acceptance gate                                                               |

Total: **10 numbered units, all shipped**. No deferrals (contrast with
Phase 8's Unit 8.4). Matches Unit 9.0 prep's 10-unit breakdown
(9.0-9.9).

### State at HEAD (Unit 9.9)

- **Content** (no change vs Phase-8 close):
  - 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors / 14 institutions / 20 rating actions / 4 issue templates / methodology v1 (EN + FR) / contributing v1.0 (EN) + v1.1 (EN + FR) / 2 `entries.json` files.
  - **203 schema-validated content files**; **36 raw MDX files**. Both unchanged from Phase-8 close.
- **New Phase-9 surfaces** (relative to Phase-8 close at `c41cf31`):
  - **2 new ADRs**: ADR-0012 (auth provider) + ADR-0013 (DB choice). ADR index extends 11 → 13.
  - **New DB layer**: `lib/db/schema.ts` (5 tables — `user`, `account`, `session`, `verificationToken`, `watchlist`) + `lib/db/index.ts` (env-fallback Drizzle client) + `drizzle.config.ts` (drizzle-kit config) + 2 migrations (`0000_initial_auth` + `0001_watchlist`).
  - **New auth layer**: `lib/auth/index.ts` (NextAuth.js v5 + Drizzle adapter + GitHub provider + `events.linkAccount`) + `app/api/auth/[...nextauth]/route.ts` (handler re-export).
  - **New watchlist layer**: `lib/watchlist/index.ts` (Drizzle helpers) + `app/api/v1/watchlist/[slug]/route.ts` (REST POST/DELETE) + `route.test.ts` (6 tests).
  - **New auth-aware UI**: `components/auth-control/` (signed-in/out in SiteHeader) + `components/watchlist-toggle/` (star/unstar on problem pages).
  - **Middleware composition**: `auth((req) => intlMiddleware(req))` — server-side bundle 159 kB (was 51.8 kB; +~107 kB).
  - **`messages/{en,fr}.json`**: `auth.*` + `watchlist.*` namespaces (3 + 4 = 7 new keys per locale).
  - **`.env.example`**: documents 5 new env vars (`AUTH_GITHUB_ID` + `AUTH_GITHUB_SECRET` + `AUTH_SECRET` + `TURSO_DATABASE_URL` + `TURSO_AUTH_TOKEN`).
  - **`.gitignore`**: adds 4 patterns (`local.db`, `local.db-*`, `.env.local`, `.env.*.local`).
- **~590 prerendered pages + 2 new dynamic API routes** (`ƒ /api/auth/[...nextauth]` + `ƒ /api/v1/watchlist/[slug]`). Page-route count unchanged. First Load JS shared chunk = **103 kB UNCHANGED**.
- **394/394 vitest tests across 45 files** (was 388/44 at Phase-8 close); **+6 tests this phase**: all from Unit 9.6's watchlist API route tests. +1 test file (`app/api/v1/watchlist/[slug]/route.test.ts`).
- `pnpm validate-content` → **203 files** green (unchanged).
- `pnpm audit-content` → **0 errors / 6 warnings** (Q32-expected baseline since Phase 2; unchanged).
- `pnpm typecheck` clean. `pnpm build` clean (~4-6s compile; per-unit variance).
- **5 visualizations live** (unchanged from Phase 4).
- **13 ADRs** (was 11 at Phase 8 close; +2 in Phase 9).
- **+5 new dependencies in Phase 9** (was 0 in Phase 8): `next-auth@5.0.0-beta.31` (exact-pin per beta convention) + `@auth/drizzle-adapter@1.11.2` + `drizzle-orm@0.45.2` + `@libsql/client@0.17.3` (all runtime) + `drizzle-kit@0.31.10` (devDep).
- **`lighthouserc.json`** enrols **19 URLs** (unchanged from Phase-8 close; no new locale-pilot surfaces in Phase 9).
- **OPEN_QUESTIONS state** (per Unit 9.8 mechanical tally):
  - **19 resolved** (Q1, Q4, Q5, Q12, Q13, Q18, Q27, Q32, Q40, Q41, Q43, Q45, Q46, Q48, Q49, Q50, Q52, Q53, **Q56**).
  - **4 decided-as-lean** (Q38, Q42, Q44, Q51).
  - **28 open** (Q2, Q3, Q6, Q7, Q8, Q9, Q10, Q11, Q14, Q15, Q16, Q17, Q19, Q25, Q26, Q28, Q29, Q30, Q31, Q33, Q34, Q35, Q36, Q37, Q39, Q47, **Q54**, **Q55**).
  - **Total: 51 entries** (was 48 at Phase-8 close under mechanical-tally convention; +3 surfaced via Q54-Q56 in Phase 9; +1 promotion via Q56 in Unit 9.6).

### Phase-9 follow-ons that survive the gate (non-blocking)

Same Q27-class pattern as prior phases. Phase-9 specifics overlay the
canonical list (full enumeration in Unit 9.7):

**Class A (4 — in-flight operational)**:
1. **Q54** — GitHub OAuth app registration (register app under `bettyguo` org; add `AUTH_GITHUB_ID` + `AUTH_GITHUB_SECRET` + `AUTH_SECRET` to Vercel env; sign-in surfaces `/api/auth/error?error=Configuration` until done).
2. **Q55** — Turso production DB provisioning (`turso db create` + `turso db tokens create`; add `TURSO_DATABASE_URL` + `TURSO_AUTH_TOKEN` to Vercel env; Vercel's read-only filesystem breaks `file:./local.db` fallback in production).
3. **CI dummy `AUTH_SECRET`** for build smoke (Auth.js v5 throws at module-load if unset; `safeAuth()` doesn't catch initialization errors).
4. **`pnpm db:migrate` doc for new contributors** in `/contributing` v1.2.

**Class B (12 — Phase-9-specific follow-ons)**: see Unit 9.7 — profile page; rating-challenge submission write-path; email notifications; watchlist count on `/problems` index; bulk-import/clear UI; orphan-row cleanup script; rate-limiting; multi-provider OAuth; `createUser` vs `linkAccount` documentation; first LHCI run; OAuth callback URL stability (Q2 DNS); auth-aware route protection.

**Class C (14 — carryovers from prior phases)**: see Unit 9.7 — orphan `components/domain-tile-grid/`; `entries.json` backfill; `pnpm clean-drafts`; `<managingEditor>` on RSS; Phase-2 ROR-ID + InstaDeep orphan; W3C feed validator; Playwright baselines; real-API discussions smoke; `NEXT_PUBLIC_GISCUS_REPO_ID` enablement; **HTML shell migration + Unit 8.4 unblock STILL ON HOLD per parallel-session signal**; fallback-hint UI; chrome strings + FR backfill + StatusPill localization + nav labels; trailing-slash normalization; per-entry sitemap hints.

Documented in detail in Unit 9.7's hygiene status pass.

### Phase 10 entry conditions

Per §12 cardinal rule: **explicit human sign-off required**.

**§13 ledger CLOSED at this gate.** All three Phase-6+ threads
(Discussions, Bilingual, Auth+API) have shipped. There is no
"next-§13-thread" candidate to pull. Phase 10+ thread options are
ALL inferred-not-§13:

1. **Subscriber-list (third-party email provider)** — Phase-5 D-4
   punt completion. ~6 units. Decoupled from auth (third-party
   service holds the subscriber list; site posts new entries via API).
   Provider choice ADR (Buttondown / Resend / ConvertKit / Loops)
   + `lib/email/` wrapper + subscription endpoint + digest delivery
   scheduler.
2. **Rating-challenge submission write-path** — Phase-9 Unit 9.0 D-5
   explicit deferral. Second write-path beyond watchlist. ~6 units.
   Draft + curator review pipeline + form on problem detail page +
   `rating_challenges` Drizzle table.
3. **Profile page + Phase-9 UI polish** — `/[locale]/profile` or
   `/[locale]/u/[handle]` showing watched problems + sign-out + future
   per-user preferences. ~4-5 units.
4. **HTML shell migration + Unit 8.4 unblock** — Phase-8 follow-on
   STILL ON HOLD per parallel-session preservation signal. Re-attempt
   only with explicit curator authorization.
5. **Monetization thread** — premature without auth + API + observed
   user traffic. Phase 11+ candidate.
6. **Multi-provider OAuth expansion** (Google / GitLab / email-link)
   — ADR-0012 D-B forbidden in Phase 9; Phase-10+ follow-on ADR or
   explicit Q-promotion required.
7. **Q51 curator-track bulk FR content backfill** — orthogonal to
   architectural phases; could land as a long-running curator-track
   thread in parallel with any of the above.

**DB-migration trigger re-eval** mandatory at Phase 10 kickoff per
procedural cascade from Units 4.12 / 5.10 / 6.0 / 7.0 / 8.0 / 9.0 D-2.
At HEAD: §5.7 trigger (a) ALREADY FIRED in Unit 9.6 (this phase);
trigger (b) still cold (~1.6% of 5 MB threshold). The re-eval at
Phase 10 kickoff is now a documentation-only formality; the DB exists
and is in active use.

### Cross-phase milestone

This commit closes the **Auth + read+write API thread** of Phase 9
— the **final §13 Phase-6+ thread**. §13 ledger is fully closed.

The thread shipped at scope cap: **auth foundation + ONE write-path
(watchlist) operational locally + architecturally complete + Q54-Q55
operational gates surfaced + Q56 resolved**. Per Unit 9.0 D-5:
rating-challenge submission deferred to Phase 10; multi-provider
OAuth forbidden in Phase 9; profile page deferred. Scope discipline
preserved.

10 commits (9.0-9.9; no deferrals) + 2 new ADRs (0012 + 0013) + 5 new
dependencies + 0 client-bundle regressions (103 kB First Load JS
preserved end-to-end through every Phase-9 unit) + 0 test regressions
+ 6 new tests + 0 OPEN_QUESTIONS net resolved-vs-still-open movement
on the architectural axis (Q56 promoted; Q54 + Q55 added but they're
operational gates, not architectural ambiguities).

**Phase-9 firsts (project-wide)**:
- First first-party DB.
- First first-party write-path.
- First time the §5.7 DB-migration trigger fired (deferred 5 phases).
- First time `users` and `sessions` exist as project state.
- First multi-runtime composition in `middleware.ts` (Auth.js outer +
  next-intl inner).
- First exact-pinned dependency in `dependencies` (`next-auth@5.0.0-beta.31`
  per beta-tag convention; future ADR-pin-revisit anticipated).

### Phase-9 over-vs-under against the 9.0 plan

Unit 9.0 prep scoped a 10-unit phase (9.0-9.9). Actual: **10 units
shipped + 0 deferred**. Scope drift this phase: **none**. Phase 9
stayed exactly within the 9.0 prep envelope.

Surfaces that emerged within scope:
- `events.linkAccount` chosen over `events.createUser` for
  `users.githubLogin` population (Unit 9.6 design decision; documented
  in 9.6 THINK doc + 9.7 Class B item 9 for future multi-provider
  context).
- Drizzle-kit 0-indexed migration naming (off-by-one vs ADR-0013 D-E
  prose; reconciled inline by Unit 9.8).
- Auth.js v5 beta exact-pin convention (vs ADR-0012 D-A's `^5.x`
  guidance; reconciled inline by Unit 9.8).
- `safeAuth()` defensive wrapper pattern in `SiteHeader` (Unit 9.5)
  re-applied in `WatchlistToggle` (Unit 9.6) — a small idiom that
  emerged organically.
- Parallel session shipped Unit 9.5 verbatim outside the primary
  session's window (documented in 9.7 + project memory's
  parallel-curator workflow).

### Smoke gates (this unit + final cross-cut)

- Files added: this THINK doc.
- CHANGELOG: Unit 9.9 entry (acceptance gate summary).
- `pnpm validate-content` → 203 unchanged.
- `pnpm typecheck` clean.
- `pnpm test` → 394/394 across 45 files unchanged (no test files
  touched in 9.9).
- `pnpm build` → ~590 prerendered pages + 2 dynamic API routes
  unchanged. First Load JS shared chunk = 103 kB unchanged.
  Middleware bundle 159 kB unchanged.
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline).

### CHANGELOG entry shape

Phase-9 close: marks Unit 9.9 as the acceptance gate; cites the §13
Auth + read+write API thread deliverable status table (one row per
Phase-9 surface; no DEFERRED rows); emits the cross-phase
state-at-HEAD (10 problems / etc.; ~590 routes + 2 API routes; 103 kB;
394 tests; 13 ADRs; 19 LHCI URLs; +5 deps; middleware 159 kB);
enumerates Phase-9 follow-ons (Class A operational gates Q54 / Q55 /
CI `AUTH_SECRET` / db:migrate doc + Class B + Class C); states
**§13 ledger CLOSED**; states Phase 10 entry condition (explicit
sign-off per §12; Phase 10+ thread options ALL inferred-not-§13;
re-eval procedural-only since the DB already exists).
