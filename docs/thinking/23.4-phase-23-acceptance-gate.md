# 23.4 — Phase 23 acceptance gate (prior-action auto-fill realization; 19 ADRs unchanged; fourth consecutive operational-script-keystone phase = pattern entrenched)

Fifth and final Phase-23 unit; docs-only. Closes Phase 23
(**fourteenth NON-§13 phase**; prior-action auto-fill +
signals_considered auto-gather realized via Phase-22 B.6 items
1+2 closure). Mirrors Phase-12 through Phase-22 acceptance-gate
patterns.

> **§12 sign-off**: Phase 24 sign-off **pending** at end of unit.
> "Continue" override path available (**nineteenth invocation**
> opportunity).

## THINK

### Phase 23 ledger

**5 units shipped** (23.0 prep → 23.4 gate). **0 deferrals**;
**0 scope drift**. **Twelfth consecutive well-scoped phase**.
**Matches Phase 20 + 21 + 22 5-unit shape** — four consecutive
5-unit operational-script-keystone phases.

| Unit | Title | Type | Status |
|---|---|---|---|
| 23.0 | Phase 23 prep | docs | shipped `68eed9a` |
| 23.1 | prior-action auto-fill + signals seeding (+10 tests) | code | shipped `18c44b2` |
| 23.2 | Phase-23 hygiene status pass | docs | shipped `a795d6d` |
| 23.3 | ADR-0014 D-D Phase-23 enhancement note | docs | shipped `5478cf7` |
| 23.4 | **Phase 23 acceptance gate** (this unit) | gate | this commit |

### Phase-23 closure summary

**Phase-22 B.6 items 1+2 CLOSED** in Unit 23.1 via
`readPriorRatingAction` helper + `buildRatingActionYaml`
priorAction extension + CLI orchestration. Prior-action
auto-fill copies 4 OTHER dimensions + `prior_action` filename +
`watchlist` from prior; signals_considered seeds from prior +
appends placeholder. Phase-22 null-fallback placeholder behavior
preserved as regression guard. **Tightest follow-on-close
cadence in project history** — Phase-22 surfaced; Phase-23
closed the two highest-value items in the immediately-next phase
in the same operational-script-keystone shape.

Phase 23 is the **fifth phase since Phase 3 to close without
new ADR** (Phase 18 + 20 + 21 + 22 + 23; **first four-phase
no-new-ADR streak**) and the **fourth consecutive operational-
script-keystone phase** (Phase 20 + 21 + 22 + 23). **Pattern
entrenched**: well-scoped operational-script keystones now stack
to a recurring roadmap shape, not an episodic interlude.

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`5478cf7`):

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **585/585 across 55 vitest files**. +10 / 0
  Δ from Phase 22 close (575/55); **first phase to add tests
  without adding test files**.
- **`pnpm build`** → ~659 prerendered pages + 7 dynamic page+API
  routes UNCHANGED. **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-23 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` = 117 kB UNCHANGED. `/u/{handle}` = 108 kB
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32
  baseline since Phase 2; unchanged through every Phase 3-23
  unit).
- **`pnpm lint`** clean.

### Architectural firsts shipped in Phase 23

1. **First filesystem-read surface in an operational script** —
   Phase 20+21+22 stayed within DB/blob boundaries; Phase 23
   adds `content/problems/<slug>/ratings/*.yaml` read via
   `fs/promises.readdir` + `readFile` + `parseYaml` +
   `RatingActionSchema.parse`.
2. **First script that consumes existing `content/` rather than
   only producing into `drafts/`** — single script crosses both
   boundaries (produces drafts from DB + consumes prior content
   to auto-fill the draft).
3. **Fourth consecutive operational-script-keystone phase** —
   Phase 20 + 21 + 22 + 23. **Pattern entrenched** (was
   "crystallized" at 3 phases).
4. **Seventh consecutive 0-migration phase** (Phase 17-23; 10
   of 14 phases since DB landed are 0-migration).
5. **Seventh consecutive single-session phase** (Phase 17-23).
6. **Fifth phase since Phase 3 to close without new ADR** (Phase
   18 + 20 + 21 + 22 + 23; **first four-phase no-new-ADR
   streak**).
7. **First four-phase Q-tally freeze** (Phase 20 + 21 + 22 +
   23).
8. **Tightest follow-on-close cadence in project history** —
   Phase-22 surfaced B.6 items 1+2; Phase-23 closed them in the
   immediately-next phase. Prior tightest was Phase 19 → 20
   (ADR-0019 D-E surfaced + closed across consecutive phases).
9. **First phase to add tests without adding test files** —
   Phase 22 added +1 file (`scaffold.test.ts`); Phase 23 adds
   +10 tests to the existing file in-place.
10. **First in-test tempdir pattern** —
    `mkdtemp(path.join(tmpdir(), "scaffold-test-"))` +
    `afterEach` cleanup. Establishes pattern for future
    operational scripts that need filesystem reads.
11. **First in-place code unit** — Phase 20 / 21 / 22 each
    added a new `scripts/*.ts` file; Phase 23's code unit
    modifies existing files in-place (no new `scripts/` or
    `lib/` files). Pattern accommodates both new-script and
    extend-existing-script scopes.
12. **Implementation-note convention codified** — ADRs amended
    via APPEND-not-EDIT phase-tagged notes (mirrors ADR-0005
    immutability ethos applied to ADR D-clause amendments).

### Delta summary (Phase 22 → Phase 23)

| Metric | Phase 22 close (`b353641`) | Phase 23 close (HEAD `5478cf7`) | Δ |
|---|---|---|---|
| Commits | 5 | 4 + this gate | +5 |
| ADRs | **19** | **19** | **0** (1 amended with both Phase-22 + Phase-23 notes on D-D; **fourth consecutive no-new-ADR phase** = first four-phase no-new-ADR streak; **fifth phase since Phase 3** to close without new ADR) |
| Migrations | 6 | **6** | **0** (**seventh consecutive 0-migration phase**) |
| Runtime deps | (Phase-22 stack) | (Phase-22 stack unchanged) | **0** |
| Env vars | 7 | 7 | **0** |
| Tests | 575 / 55 files | **585 / 55 files** | **+10 / 0** (**first phase to add tests without adding test files**) |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` + `/u/{handle}` bundles | 117 + 108 kB | 117 + 108 kB | 0 |
| OPEN_QUESTIONS resolved | 26 | **26** | **0** (**first four-phase Q-tally freeze** — Phase 20 + 21 + 22 + 23) |
| OPEN_QUESTIONS open | 29 | **29** | **0** |
| OPEN_QUESTIONS total | 59 | **59** | **0** |
| i18n keys per locale | 127 | 127 | 0 |
| `lib/rating-challenges/` files | 5 (+ 4 .test.ts) | 5 (+ 4 .test.ts) | **0** (in-place extension to `scaffold.ts` + `scaffold.test.ts`) |
| `scripts/` files | 9 | 9 | **0** (in-place extension to `emit-challenge-action.ts`) |
| `package.json#scripts` entries | (+1: `emit-challenge-action`) | (unchanged) | 0 |
| YAML-scaffolding surfaces | 1 | 1 | 0 (Phase-22 surface extended in-place) |
| Filesystem-read surfaces (operational scripts) | 0 | **1** | **+1** (first in project history) |
| Operational-script-keystone phases | 3 | **4** | **+1** (Phase 20 + 21 + 22 + 23 = **pattern entrenched**) |

### Surviving follow-ons

**Class B — ~8 items in 8 categories** (Phase-22 Class B carried
minus B.6 items 1+2 closed Phase 23; + 1 Phase-23 B.7 newly
surfaced):

- **B.1 Image-processing expansion (4 carried unchanged)**:
  Q68 expansion; cropping UI; resizing; WebP/AVIF.
- **B.2 Phase-17 markdown subset expansion (8 carried unchanged)**.
- **B.3 Phase-18 surface expansion (3 carried + Q72)**.
- **B.4 Phase-20 backfill operational follow-ons (3 carried)**.
- **B.5 Phase-21 cleanup operational follow-ons (2 carried)**.
- **B.6 Phase-22 scaffold operational follow-ons (2 carried
  after Phase-23 closed items 1+2)**: methodology-version
  dynamic read (~0.5 units); batch-emit-multiple (~0.5 units).
- **B.7 Phase-23 scaffold operational follow-ons (1 newly
  surfaced)**: `--no-prior-fill` opt-out flag (~0.5 units;
  Phase 24+ if curator demand for force-placeholder behavior
  when prior data is suspect).
- **B.8 Operational + infrastructure (8 carried unchanged)**:
  Q69 / Q54 / Q55 operational gates; CI dummy `AUTH_SECRET`;
  `pnpm db:migrate` doc; first LHCI run; middleware-based
  auth-route protection; `pnpm backfill-exif-strip` operational
  doc; `pnpm cleanup-orphan-blobs` operational doc; `pnpm
  emit-challenge-action` operational doc.

**Class C** — pre-existing carryovers unchanged.

### Phase-24+ Q-candidates flagged

**7 candidates carry** (unchanged from Phase 22 + Phase 23):

- Q60 (curator authz evolution).
- Q61 (submitter anonymity).
- Q62 (rejection-rationale public visibility).
- Q64 (per-user privacy opt-out; ADR-0015 D-D).
- Q65 (per-curator activity feed; ADR-0015 D-E).
- Q68 expansion (content moderation; ADR-0019 D-F + Phase-20+21
  backfill/cleanup + Phase-23 prior-read patterns all reusable
  for retroactive moderation backfill).
- Q72 (`reviewNotesSchema` divergence).

### Phase 24+ entry conditions

Per §12 cardinal rule. **Nineteenth "Continue" override
opportunity** in Unit 24.0 prep. Phase 24+ thread options (all
inferred-not-§13):

- **A. Q68 expansion content moderation** — ADR-0020 candidate;
  could be **fifth consecutive operational-script-keystone
  phase** if scope = moderation API + retroactive backfill scan.
  ~3-5 units.
- **B. Cropping UI** (per ADR-0019 D-F) — ~3-5 units; client
  dep.
- **C. Server-side resizing + WebP/AVIF** (per ADR-0019 D-F) —
  ~1-2 units; may be operational-script-keystone if scope =
  backfill.
- **D. Q72 markdown subset extensions** — ~2-3 units.
- **E. Per-challenge detail page** — ~2-3 units; may not need
  new ADR.
- **F. Markdown rating-action `rationale`** — ~2-3 units;
  couples to (E).
- **G. Q64 per-user privacy opt-out** — ~3 units; breaks
  7-consecutive 0-migration streak.
- **H. Multi-provider OAuth expansion** — Phase-9 Class B item
  8 carried **13 phases**. **ADR-0020 candidate**. ~3-4 units.
  Strongest architectural patience signal at right scope —
  pre-emptive for non-existent user base at current project
  state (Q54/Q55/Q69 operational gates still pending).
- **I. Subscriber-list email** — Phase-5 D-4 punt carried **17+
  phases**. ~6 units. Strongest overall patience signal.
- **K-other. Phase-22 + Phase-23 scaffold operational
  follow-ons** (3 items combined: methodology-version dynamic
  read + batch-emit-multiple + `--no-prior-fill` flag) — ~1.5
  units combined; smallest reasonable scope. **Fifth
  consecutive operational-script-keystone phase** if bundled.
- **L. Account-deletion blob cleanup** — Phase-16 carryover;
  conditional on Phase 24+ shipping account-delete path.
- **M. SiteHeader avatar pill** — Phase-14 Class B; UI minor.

**DB-migration trigger re-eval at Phase 24 kickoff**: trigger (a)
FIRED Unit 9.6; trigger (b) cold. Phase 24 migration count
depends on chosen thread.

### Risk surface at HEAD `5478cf7`

1. **Three operational gates** (Q54 / Q55 / Q69) pending
   curator.
2. **Scaffolder prior-action auto-fill relies on filesystem
   read** — `content/problems/<slug>/ratings/` must exist for
   the auto-fill path to fire. Phase-23 graceful-fallback: when
   directory doesn't exist OR all priors malformed, returns
   null and scaffolder falls back to Phase-22 placeholder
   behavior. Mitigation: stderr log on parse failure; curator
   reviews stdout for auto-fill confirmation message.
4. **No content moderation on uploaded images** (Q68 expansion
   deferred).
5. **No live-preview in edit form** (Phase 17 + 18 deferral).
6. **Sharp cold-start cost** (~100 ms; Phase-19 carryover).
7. **Scaffolder live-tested only against in-test tempdir
   fixtures** — vitest unit tests cover the pure functions;
   live DB ↔ filesystem round-trip verified manually by curator
   on first invocation.

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token (Q69), NOT FR content backfill
(Q51 curator-track), NOT moderation / cropping / resizing /
WebP (Phase 24+), NOT live curator workflow run (curator
operational task; NOT a code-shipping unit), NOT destructive
cleanup. This unit is the **closure**, not the **unblock**.

### Parallel-curator awareness

Phase 23 shipped as a **single-session phase** — primary
shipped all 5 units 23.0 → 23.4 in one coherent session. **Seven
consecutive single-session phases** (Phase 17 + 18 + 19 + 20 +
21 + 22 + 23). Phase 16 remains the lone parallel-session phase.

### Four consecutive operational-script-keystone phases pattern entrenched

| Property | P20 (backfill-exif) | P21 (cleanup-orphans) | P22 (emit-challenge) | P23 (auto-fill) |
|---|---|---|---|---|
| Units | 5 | 5 | 5 | 5 |
| Code units | 1 | 1 | 1 | 1 |
| New ADRs | 0 | 0 | 0 | 0 |
| New deps | 0 | 0 | 0 | 0 |
| New env vars | 0 | 0 | 0 | 0 |
| New vitest tests | 0 | 0 | +11 | +10 |
| New test files | 0 | 0 | +1 | **0 (in-place)** |
| New `scripts/` files | 1 | 1 | 1 | **0 (in-place)** |
| New `lib/` modules | 0 | 0 | +1 | **0 (in-place)** |
| Closes carryover | ADR-0019 D-E | Phase-16+20 (class-B) | ADR-0014 D-D | Phase-22 B.6 items 1+2 |
| Carryover phase-count | 1 phase | 6 phases | 10 phases | **1 phase (tightest)** |

**Pattern entrenched.** Phase 22 + 23 demonstrated the shape
accommodates both pure-function-testable surfaces AND in-place
extensions to existing surfaces. Phase 23 is the **most compact
phase shape** to date (matches Phase 20+21's "no new lib/" with
Phase 22's "+10 tests" — best of both).

### Smoke gates

Docs-only.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/23.4-phase-23-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 23.4 entry.

### CHANGELOG entry shape

Unit 23.4 entry: declares Phase 23 closed (**fourteenth NON-§13
phase**; 5 units; 0 deferrals; 0 scope drift; twelfth
consecutive well-scoped phase); cites **Phase-22 B.6 items 1+2
realization** via Unit 23.1 (prior-action auto-fill +
signals_considered auto-gather); **first filesystem-read
surface in operational script**; **fourth consecutive
operational-script-keystone phase = pattern entrenched**;
**tightest follow-on-close cadence in project history**;
**fifth phase since Phase 3 to close without new ADR** =
**first four-phase no-new-ADR streak**; 12 Phase-23 architectural
firsts; Phase 22 → 23 delta table (0 ADR Δ; 0 migration Δ; 0
runtime dep Δ; 0 env var Δ; +10 tests / 0 file = first phase to
add tests without test files; 0 bundle Δ; 0 `lib/`/`scripts/`
file Δ — pure in-place extension; +1 filesystem-read surface;
+1 operational-script-keystone phase counter = pattern
entrenched); Class B follow-ons (~8 items in 8 categories;
B.6 items 1+2 CLOSED + 1 newly surfaced B.7 `--no-prior-fill`);
7 Phase-24+ Q-candidates (unchanged from Phase 22 + 23);
ADR-0020 candidate slot open after **10-phase ADR cycle without
multi-provider-OAuth claim** (Phase-9 Class B item 8 carried
**13 phases**); Phase 24+ thread options (strongest patience
signals: Phase-9 Class B item 8 = 13 phases multi-provider
OAuth; Phase-5 D-4 punt = 17+ phases subscriber-list email);
nineteenth "Continue" override opportunity per §12; seven
consecutive single-session phases; seventh consecutive
0-migration phase; four consecutive operational-script-keystone
phases pattern entrenched table.
