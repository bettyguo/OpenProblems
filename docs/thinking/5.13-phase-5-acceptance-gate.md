# 5.13 — Phase 5 acceptance gate

Phase-5 closing unit. Mirrors Unit 1.12 / 2.13 / 3.13 / 4.13. Verifies every §13 Phase-5 deliverable locally + emits the cross-phase roll-up.

## THINK

### §13 Phase-5 deliverables — local pass status

1. **arXiv ingestion helper (CLI in `scripts/`) producing draft paper YAML for curator review.** ✓ Unit 5.3 ships `scripts/ingest-arxiv.ts`. Default Sonnet 4.6. Drafts to `drafts/5.3-<ts>-<id>.diff` + `.meta.json` audit sidecar. Curator-mediated apply via `git apply` per ADR-0009. CLI smoke `pnpm ingest-arxiv --help` ✓.
2. **LLM-assisted extraction of candidate leaderboard entries from a paper PDF (must produce a human-review diff; no auto-merge).** ✓ Unit 5.5 ships `scripts/extract-leaderboard.ts`. Default Opus 4.7. Drafts to `drafts/5.5-<ts>-<id>-<slug>.diff` + `.meta.json`. Merge layer forces `verified: false` per Q43 / ADR-0009 D-F. CLI smoke `pnpm extract-leaderboard --help` ✓.
3. **Email\RSS digest: per-domain weekly summary.** ✓ Unit 5.7 (`buildDigest` builder) + Unit 5.8 (`/api/v1/digest/[domain]` RSS endpoint, 5 prerendered feeds — one per taxonomy domain) + Unit 5.9 (`/digest` HTML hub with auto-discovery `<link rel="alternate">` tags). Email deferred to Phase 6+ per Unit 5.0 D-4 (auth-trigger flips on subscriber list); Phase 5 ships RSS-only per the lean.

### §13 cross-phase criteria (universal contract)

- **Lighthouse a11y ≥ 95 on new pages**: `lighthouserc.json` extended this unit to **13 URLs** (was 12 after Unit 4.13). Added: `/digest` (the Unit 5.9 hub). The 5 per-domain `/api/v1/digest/<slug>` RSS endpoints are XML, not HTML — no Lighthouse coverage applies (they're tested via the W3C feed-validator follow-on).
- **W3C feed validator on the per-domain digest feeds** — same Q27-class follow-on as Unit 3.13's RSS validator pass. Defer to first preview deploy.
- **Visual-regression baselines** for `/digest` × 2 themes × N viewports — Phase-5 follow-on; future PR can pass `playwright test --update-snapshots`.
- **No auto-merge contract** (ADR-0009): both Phase-5 CLIs verified write to `drafts/` only; no `content/` mutations. Audit sidecars carry per-call cost + token + hash for spot-audit.

### Phase-5 unit summary (14 units shipped)

| Unit | Commit    | Title                                                                                                |
| ---- | --------- | ---------------------------------------------------------------------------------------------------- |
| 5.0  | `42fa01f` | Phase 5 prep (THINK + 14-unit breakdown + DB-migration re-eval)                                       |
| 5.1  | `5ccad5c` | ADR-0008 — LLM provider = Anthropic + cost-governance pact                                            |
| 5.2  | `f9d9a6d` | `lib/curate/arxiv-client.ts` (Atom API client + filesystem cache)                                     |
| 5.3  | `25fd29e` | `scripts/ingest-arxiv.ts` (LLM-drafting paper YAML CLI)                                               |
| 5.4  | `da50dbf` | `lib/curate/pdf-text.ts` (PDF text extraction)                                                        |
| 5.5  | `27e00e6` | `scripts/extract-leaderboard.ts` (LLM PDF→entries CLI)                                                |
| 5.6  | `655abdc` | ADR-0009 — Human-review diff format for LLM drafts                                                    |
| 5.7  | `6c33ed9` | `lib/digest/build-digest.ts` (per-domain weekly summary builder)                                      |
| 5.8  | `3a08fac` | `/api/v1/digest/[domain]` RSS endpoint                                                                |
| 5.9  | `4b9b562` | `/digest` HTML hub                                                                                    |
| 5.10 | `4ef69c2` | DB-migration trigger re-eval (Phase 5 close)                                                          |
| 5.11 | `f964ef4` | Phase-5 hygiene status pass (deferrals to Phase 6)                                                    |
| 5.12 | `0fa9743` | OPEN_QUESTIONS hygiene + ADR review                                                                   |
| 5.13 | _this_    | Phase 5 acceptance gate                                                                               |

### State at HEAD (Unit 5.13)

- 10 problems / 5 domains / ~12 subdomains / 30 papers / 126 authors / 14 institutions / 20 rating actions / 4 issue templates / 2 MDX docs (methodology + contributing) / **2 entries.json files** (hallucination-reduction + long-horizon-agent-reliability) / **2 LLM-using CLIs** (ingest-arxiv + extract-leaderboard) / **5 per-domain RSS digest endpoints** + `/digest` hub.
- **322 prerendered pages** (was 319 at Unit 5.9; the +3 includes the `/digest` hub + minor Next.js internal accounting drift across builds — the dominant additions in Phase 5 came from Unit 5.8's 5 digest feeds).
- **First Load JS shared chunk 103 kB UNCHANGED** throughout Phase 5. Every Phase-5 deliverable is server-side (Node CLIs + SSG endpoints + SSR pages); no client-bundle impact.
- **284/284 vitest tests across 36 files** (was 199/29 at Phase-4 close — +85 tests from arxiv-client (13) + paper-draft (15) + anthropic-wrapper (8) + pdf-text (8) + entry-draft (19) + build-digest (9) + rss-endpoint (13)).
- `pnpm validate-content` → 203 files green.
- `pnpm audit-content` → 0 errors / 6 warnings (same Q32-expected `related-problems-symmetry` set).
- `pnpm typecheck` clean. `pnpm build` clean compile (3.3s).
- **5 visualizations live** (unchanged from Phase 4): RatingRadar, SaturationCurve, MoversBoard, RatingHistoryStream, DomainMap.
- **9 ADRs** (added ADR-0008 + ADR-0009 in Phase 5): 0001 Next.js / 0002 Velite / 0003 Zod / 0004 file-first / 0005 rating-action immutability / 0006 saturation N/A / 0007 DomainMap rendering / **0008 LLM provider (Anthropic) + cost-governance pact** / **0009 human-review diff format**.
- **OPEN_QUESTIONS status**: 5 new Phase-5 questions surfaced + 1 newer (Q41–Q45). Q41 + Q43 + Q45 closed this phase. Q42 + Q44 promoted to `decided-as-lean`. Plus refined Q38 (Phase-4 carryover) framing.

### `lighthouserc.json` updated this commit

URL added: `/digest` (the Unit 5.9 hub). Final URL list: **13 entries** (was 12 after Unit 4.13). The 5 per-domain RSS endpoints are XML; Lighthouse a11y category doesn't meaningfully apply, so they aren't enrolled. W3C feed validator is the right gate for those — deferred to the first preview-deploy follow-on.

### Phase-5 follow-ons that survive the gate (non-blocking)

- **W3C feed validator pass** on the 5 deployed `/api/v1/digest/<slug>` URLs — same Q27-class pattern as Phase-3's `/api/v1/rss.xml` validation deferral.
- **Visual-regression baselines** for `/digest` × 2 themes × N viewports — future PR.
- **Orphan `components/domain-tile-grid/`** — still unimported at HEAD; deletion deferred to Phase-6 hygiene per Unit 5.11's destructive-action policy.
- **`entries.json` content backfill** on the 8 problems still without curator-authored entries — toolchain ready (Unit 5.5); editorial work deferred to a curator-driven session.
- **`/contributing` v1.x bump** documenting the LLM-assisted ingest workflow + ADR-0008 D-F conflict-of-interest disclosure.
- **`pnpm clean-drafts` script** — operational signal flagged in Unit 5.10 + 5.11.
- **`<managingEditor>` on RSS feeds** (Q44 / Q33) — gated on Q2 DNS resolution.
- **`docs/SESSION_HANDOFF_phase3_close.md`** — still untracked at HEAD (parallel session's staged `.gitignore` change from earlier in the session was unstaged at some point); cleanup candidate for a Phase-6 hygiene unit.

### Phase 6 entry conditions

Per §12 cardinal rule: **explicit human sign-off required**. §13 Phase-6+ scope is open-ended ("Discussions, API auth, monetization") — no enumerated deliverables. Phase-6 prep (Unit 6.0) would refine.

Re-eval triggers carried from Unit 5.10:
- Content scale 3× / `> 600` validated files / `> 1 MB` gzipped internal alarm.
- First Phase-6+ write-path lands (auth trigger flips).
- Phase 6 kickoff mandatory.
- Rating-action volume reaches 200.
- Drafts directory consistently > 100 stale files (operational hygiene signal, not migration trigger).

### Cross-phase milestone

This commit closes the Phase-5 plan in its entirety as authored in Unit 5.0. The 14-unit breakdown shipped end-to-end with three deviations, all documented:

1. **Email deliverable scoped out** of Phase 5 per Unit 5.0 D-4 — the §13 wording was "Email\RSS"; the lean read this as "RSS for sure; email-when-auth-lands". RSS-only ships.
2. **`[domain].xml/route.ts` → `[domain]/route.ts`** in Unit 5.8 — Next.js + Windows + git tooling friction with dotted-suffix dynamic segments. Documented in Q45.
3. **Hygiene unit 5.11 deferred** rather than executed — the orphan-tile-grid deletion needed explicit destructive-action authorization (caught by the harness in Unit 4.4); `entries.json` backfill is curator editorial work.

The intelligence-layer pipeline is operational: arXiv → metadata → LLM draft → human-review diff → curator apply. The digest pipeline is operational: rating actions + entries → per-domain weekly summary → RSS feed → HTML hub. The cost-governance pact (env key, dry-run, verbose, optional cap, audit sidecar) is in place from day zero.

### Smoke gates (this unit)

- `pnpm typecheck` — clean.
- `pnpm test` — 284/284 across 36 files.
- `pnpm validate-content` — 203 files.
- `pnpm audit-content` — 0 errors / 6 Q32-expected warnings.
- `pnpm build` — clean compile in 3.3s; **322 prerendered pages**; First Load JS shared chunk 103 kB.
- `pnpm ingest-arxiv --help` ✓.
- `pnpm extract-leaderboard --help` ✓.
- `lighthouserc.json` URL list extended to 13 entries.

### CHANGELOG entry shape

Phase 5 closing entry: roll-up table of all 14 units, state-at-HEAD summary, §13 deliverable status, follow-ons surviving the gate, Phase 6 entry conditions, deviation summary.
