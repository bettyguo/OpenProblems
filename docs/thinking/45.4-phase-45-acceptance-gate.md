# 45.4 — Phase 45 acceptance gate

Phase 45 closes at HEAD `fd60a75` (Unit 45.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-46-
entry conditions per §12.

## Phase 45 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 45.0 | Phase 45 prep | docs | `808e8d9` |
| 45.0a | README + 7 figures hero overhaul | docs (parallel session) | `7098cb2` |
| 45.1 | `lib/markdown/extensions/doi.ts` + `doi.test.ts` + factory `MARKDOWN_EXTENSIONS=doi` + ADR-0018 D-G APPEND-D-AC | code+APPEND | `32f2b18` |
| 45.1a | Figures polish + GitHub Pages-ready gallery | docs (parallel session) | (parallel commit) |
| 45.2 | 27 NEW end-to-end DOI + first same-slot composition + first 4-consumer composition tests | code | `d44be53` |
| 45.3 | Phase-45 hygiene + APPEND-D-Y item 4 closure documentation | docs | `fd60a75` |
| 45.4 | Phase 45 acceptance gate (this unit) | gate | (current) |

5 numbered units (45.0 through 45.4) + 2 parallel-session docs-
only sub-units (45.0a + 45.1a). Mirrors Phase 35 + 37 + 38 + 39
+ 40 + 41 + 42 + 43 + 44 5-unit framework shapes exactly for the
numbered sequence.

## Fourth concrete Phase-37-framework consumer realized + first compositional same-slot case

- `DoiExtensionRegistry` joins `WikilinkExtensionRegistry`
  (Phase 38) + `TablesExtensionRegistry` (Phase 39) +
  `ArxivExtensionRegistry` (Phase 41) as the **fourth concrete
  consumer**. **First second-consumer in any single framework
  slot** — DOI joins arxiv in `remarkPlugins`; `rehypePlugins`
  + `schemaOverrides` slots remain singleton Phase 45.
- New file `lib/markdown/extensions/doi.ts` ships
  `remarkLinkDoiIds()` function-declaration plugin +
  `DoiExtensionRegistry` class + `PHASE_45_DEFAULT_ENABLED_SURFACES = Set(["rationale"])`.
- New file `lib/markdown/extensions/doi.test.ts` ships 28 tests
  covering Crossref-spec regex correctness + class behavior +
  canonical doi.org host emission.
- `lib/markdown/extensions/index.ts` factory gains `case "doi"`
  arm; throw-error-message recognized-values list updated;
  JSDoc dispatch-arm-list updated.
- `lib/markdown/extensions/index.test.ts` gains 9 dispatch +
  composition tests including the **first same-slot composition
  test** (`arxiv,doi` → `[remarkLinkArxivIds, remarkLinkDoiIds]`
  in `remarkPlugins` on rationale).
- `lib/markdown/index.test.ts` gains 27 end-to-end tests across
  3 new describe blocks (Phase-45 doi default + Phase-45 first-
  same-slot composition + Phase-45 first-4-consumer composition).
- ADR-0018 D-G APPEND adds Phase-45 EXTENDED block + **APPEND-
  D-AC** (Unit 45.1; third two-letter APPEND letter after
  Phase-43 D-AA + Phase-44 D-AB).
- ADR-0018 APPEND-D-Y item 4 deferral CLOSED at **4-phase
  carryover** (Phase 41 → Phase 45; Unit 45.3 documents).
- **First compositional same-slot case in project history**.
  Under `MARKDOWN_EXTENSIONS=arxiv,doi` Phase-45 default the
  `remarkPlugins` slot on `rationale` carries
  `[remarkLinkArxivIds, remarkLinkDoiIds]` per APPEND-D-R
  "concatenated across components in registration order" rule.
- **First "two plugins active in the same slot on the same
  surface under default dispatch" state**.
- **First "4-consumer composition under default dispatch"
  state** via `MARKDOWN_EXTENSIONS=wikilinks,tables,arxiv,doi`
  (4-way): `rationale` carries 4 consumers across 3 slots;
  other 3 surfaces preserve the Phase-44 3-consumer baseline.

## Phase 44 → Phase 45 delta

| Metric | Phase 44 close | Phase 45 close | Δ |
|---|---|---|---|
| Tests | 921 / 71 files | **985 / 72 files** | +64 / +1 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 11 | **12** (project record extends; third two-letter slot D-AC in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 4 | **5** (+`doi`) | **+1** |
| i18n keys per locale | 168 | 168 | 0 |
| Dynamic page+API routes | 13 | 13 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 13 | **15** | **+2** (`doi.ts` + `doi.test.ts`) |
| **Phase-37-framework concrete consumers** | **3** | **4 (+`DoiExtensionRegistry`)** | **+1** |
| **Slots with ≥2 consumers (default dispatch)** | **0** | **1 (`remarkPlugins`: arxiv + doi)** | **+1** |
| **Component-surface-slot triples active under maximal default dispatch** | **12 (3 slots × 4 surfaces × 3 consumers)** | **13 (12 + 1 second-consumer-on-rationale-remark)** | **+1** |

## Architectural firsts in Phase 45 (12 enumerated)

1. **Fourth concrete Phase-37-framework consumer** —
   `DoiExtensionRegistry` joins wikilinks + tables + arxiv as
   the fourth real-consumer existence proof. **First second-
   consumer in any single framework slot**: DOI joins arxiv in
   `remarkPlugins`; `rehypePlugins` + `schemaOverrides` slots
   remain singleton.
2. **First compositional same-slot case in project history**.
   APPEND-D-R "concatenated across components in registration
   order" rule for `remarkPlugins` becomes live with two real
   consumers (previously trivially satisfied because each slot
   had exactly one consumer Phase 38-44).
3. **First "two plugins active in the same slot on the same
   surface under default dispatch" state**. Under
   `MARKDOWN_EXTENSIONS=arxiv,doi` Phase-45 default the
   `rationale` surface carries
   `[remarkLinkArxivIds, remarkLinkDoiIds]` in `remarkPlugins`.
4. **First "4-consumer composition under default dispatch"
   state**. Pre-Phase-45 max was 3-consumer (Phase-44
   `wikilinks,tables,arxiv`). Phase 45 adds DOI to bring
   4-consumer composition into default-dispatch range.
5. **APPEND-D-Y item 4 deferral CLOSED at 4-phase carryover**
   (Phase 41 → Phase 45; mirrors Phase 38→42 + 39→43 4-phase
   gaps). **First non-cross-surface-expansion APPEND-deferral
   closure** in the cadence — Phase 42-44 closures were all
   cross-surface-expansion items; Phase 45 closes a sibling-
   consumer-introduction item.
6. **APPEND-deferral closure cadence sustained 4 phases**
   (Phase 42 + 43 + 44 + 45). One deferral resolved per phase,
   oldest-applicable-first.
7. **Twelfth APPEND on ADR-0018 D-G** — extends the **first-
   ADR-D-clause-with-most-APPENDs record** from 11 → 12
   (Phase 18 + 27 + 29 + 37 + 38 + 39 + 40 + 41 + 42 + 43 +
   44 + **45**).
8. **Third two-letter APPEND letter D-AC** (after Phase-43
   D-AA + Phase-44 D-AB). Excel-spreadsheet column convention
   sustained.
9. **First "framework + 4 consumers + composition + 3
   expansions + same-slot composition" 9-phase cluster** in
   project history (Phase 37-45). Nine-phase architectural
   arc: framework definition + 4 consumers + composition
   infrastructure + 3 cross-surface expansions + first same-
   slot composition.
10. **15th consecutive phase without new B category** —
    Phase 31-45 = **first 15-phase run** in project history
    (extends Phase-44 record from 14 → 15).
11. **85th-through-88th consecutive 103 kB First Load JS
    units** (4 of 5 Phase-45 numbered units increment;
    Unit 45.0 prep does not increment per phase-prep
    precedent). Parallel-session docs-only sub-units (45.0a +
    45.1a) also docs-only.
12. **Fifth `MARKDOWN_EXTENSIONS` recognized single-value arm**
    — `default` / `wikilinks` / `tables` / `arxiv` / **`doi`**.
    **First env-var-recognized value addition since Phase 41**
    (Phase 42 + 43 + 44 were value-change phases, not value-
    addition phases).

## Phase 45 → Phase 46 entry conditions

Per §12, Phase 46 entry requires **explicit human sign-off**.

**Recommended Phase 46 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **Multi-anchor wikilink alias syntax** `[[slug\|display-text]]` (APPEND-D-L item 2) — plugin regex extension on existing `rehypeResolveWikilinks`; mirrors closest Phase-42-style "extend within existing consumer" pattern | 1-2 | Autonomous-tractable; plugin-only change; closes APPEND-D-L item 2 deferral at 8-phase carryover |
| 2 | **Cross-entity wikilinks** (`[[paper-id]]` / `[[author-slug]]` / `[[institution-slug]]`; APPEND-D-L item 3) — entity-type disambiguation + plugin parameterization | 2-3 | Autonomous-tractable; requires APPEND-D-L item 6 plugin parameterization to land first |
| 3 | **Table-specific attributes** (`colspan` / `rowspan` / `scope`; APPEND-D-Q item 3) — schema-override extension on existing `TablesExtensionRegistry` | 1-2 | XSS-audit-required; schema-extension only; closes APPEND-D-Q item 3 |
| 4 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — couples with `schemaOverride` for `class` attribute on `<a>`; first cross-slot consumer (wikilinks gains a `schemaOverrides` arm in addition to `rehypePlugins`) | 1-2 | XSS audit per attribute value; first single-consumer-multi-slot case |
| 5 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 6 | **DOI cross-surface expansion** to bio + reviewNotes + actionRationale (new APPEND-D-AC Phase-46+ deferral) — constructor-arg value-only change in `PHASE_45_DEFAULT_ENABLED_SURFACES` | 1-2 | Autonomous-tractable; **fourth realization of "constructor-arg-only zero-rework expansion"** property; mirrors Phase-44 arxiv expansion verbatim |
| 7 | **PubMed PMID sibling consumer** (`pubmed:NNNNNNNN` / `pmid:NNNNNNNN`) — fifth concrete consumer; second second-consumer-in-`remarkPlugins`-slot (joins arxiv + doi); plugin order may matter | 2-3 | Autonomous-tractable; mirrors Phase-41/45 first-consumer shape; closes a new APPEND-D-AC Phase-46+ deferral |
| 8 | **Older-style category-prefixed arxiv IDs** (APPEND-D-Y item 2) — regex extension on existing `remarkLinkArxivIds` | 1-2 | Autonomous-tractable; closes APPEND-D-Y item 2 |
| 9 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |
| 10 | **Q78 / Q79 / N. v2 methodology / safeAuth / test backfill / account-delete / pre-commit typecheck** | various | Each deferred per prior rationale |

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **985 / 72 files**.
- `pnpm audit-content` — 0 errors / 6 warnings (43 consecutive
  phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (88 consecutive units).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 46+ deferrals)

NOT DOI cross-surface expansion (Phase 46+ rank 6);
NOT DOI display-text alias syntax (Phase 46+);
NOT bare DOIs without `doi:` prefix (Phase 46+);
NOT dx.doi.org legacy host parsing (Phase 46+);
NOT stricter trailing-lookahead for legitimate trailing-period
DOIs (Phase 46+);
NOT older-style category-prefixed arxiv IDs (Phase 46+ rank 8);
NOT bare arxiv IDs without prefix (Phase 46+);
NOT arxiv display-text alias syntax (Phase 46+);
NOT paper-card hover-preview (Phase 46+);
NOT multi-anchor wikilink alias (Phase 46+ rank 1);
NOT cross-entity wikilinks (Phase 46+ rank 2);
NOT plugin parameterization for wikilink-href-builder (Phase
46+);
NOT `<a class="wikilink">` styling (Phase 46+ rank 4);
NOT 404 handling for unresolved wikilinks (Phase 46+ rank 5);
NOT table-specific attributes (Phase 46+ rank 3);
NOT `<caption>` element (Phase 46+);
NOT surface-specific table schemas (Phase 46+);
NOT `@mention` resolution (Phase 46+);
NOT PubMed PMID consumer (Phase 46+ rank 7);
NOT a 3rd-or-later `remarkPlugins` consumer beyond arxiv + doi
(Phase 46+);
NOT a 2nd `rehypePlugins` consumer beyond wikilinks (Phase 46+);
NOT a 2nd `schemaOverrides` consumer beyond tables (Phase 46+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-44 deferrals carry forward.

## Boundary statement

**Phase 45 CLOSED at HEAD `<this-unit>` after 5 numbered units +
2 parallel-session docs-only sub-units (45.0 prep + 45.0a README/
figures + 45.1 `lib/markdown/extensions/doi.ts` + `doi.test.ts`
+ factory dispatch + APPEND-D-AC + 45.1a figures polish + 45.2
27 NEW end-to-end DOI + first same-slot + first 4-consumer tests
+ 45.3 hygiene + 45.4 gate). Fourth concrete Phase-37-framework
consumer realized: DOI auto-link via new `DoiExtensionRegistry`
joining wikilinks + tables + arxiv. **First second-consumer in
any single framework slot** — DOI joins arxiv in `remarkPlugins`;
mirrors Phase-41 arxiv first-consumer ship shape verbatim. Closes
ADR-0018 APPEND-D-Y item 4 deferral at **4-phase carryover**
(Phase 41 → Phase 45). **First non-cross-surface-expansion
APPEND-deferral closure** in the Phase 42-43-44-45 closure
cadence (Phase 42-44 closed cross-surface-expansion items;
Phase 45 closes a sibling-consumer-introduction item). APPEND-
deferral closure cadence sustained 4 phases. **First
compositional same-slot case in project history** — APPEND-D-R
"concatenated across components in registration order" rule for
`remarkPlugins` becomes live with two real consumers (previously
trivially satisfied). Under `MARKDOWN_EXTENSIONS=arxiv,doi` the
`remarkPlugins` slot on `rationale` carries
`[remarkLinkArxivIds, remarkLinkDoiIds]`. **First "two plugins
active in the same slot on the same surface under default
dispatch" state** in project history. **First "4-consumer
composition under default dispatch" state** via
`MARKDOWN_EXTENSIONS=wikilinks,tables,arxiv,doi`. Twelfth APPEND
on ADR-0018 D-G (project record extends 11 → 12). **Third two-
letter APPEND letter D-AC** (Excel-spreadsheet column convention
sustained). Thirty-sixth NON-§13 phase. **First "framework + 4
consumers + composition + 3 expansions + same-slot composition"
9-phase cluster in project history** (Phase 37-45). Nine-phase
architectural arc: framework definition + 4 consumers +
composition infrastructure + 3 cross-surface expansions + first
same-slot composition. 15th consecutive phase without new B
category (first 15-phase run; extends Phase-44 record 14 → 15).
985 tests across 72 vitest files (+64 / +1 vs Phase 44). 24 ADRs
UNCHANGED. 7 DB tables UNCHANGED. 9 migrations UNCHANGED. 14 env
vars UNCHANGED. 168 i18n keys per locale UNCHANGED. First Load JS
103 kB UNCHANGED (88 consecutive units). Middleware 160 kB
UNCHANGED. OPEN_QUESTIONS top-level Q-count 66 UNCHANGED. Phase-
37+ candidate count 8 UNCHANGED. **Fifth `MARKDOWN_EXTENSIONS`
recognized single-value arm** — `default` / `wikilinks` /
`tables` / `arxiv` / **`doi`** (first env-var-recognized value
addition since Phase 41). Zero new ADRs / migrations / i18n keys
/ env vars / operational gates / runtime deps. Phase 46 entry
requires explicit human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 44 → 45 delta complete.
- [x] Phase 46 entry conditions documented (10 ranked
      candidates).
- [x] Smoke gates pass.
- [x] STOP at Phase 46 boundary per §12.
