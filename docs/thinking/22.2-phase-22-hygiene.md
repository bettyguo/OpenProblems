# 22.2 — Phase-22 hygiene status pass (0 Class A / ~10 Class B / Class C unchanged; eleven consecutive well-scoped phases)

Third Phase-22 unit; docs-only. Mirrors Phase-13 through Phase-21
hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-22 cleanup: **0 items**

Phase-22's single code unit (22.1) shipped without deferrals.
The ADR-0014 D-D emit-challenge-action contract is realized in
`scripts/emit-challenge-action.ts` + `lib/rating-challenges/scaffold.ts`
(+ tests at `lib/rating-challenges/scaffold.test.ts`). Phase-22-
deferred refinements (prior-action auto-fill / signals_considered
auto-gather / methodology-version dynamic read / `--out` flag /
batch-emit-multiple) are explicit scope-cap decisions made in
Unit 22.0 D-3 + D-4 — NOT Class A.

**Phase-22 Class A count = 0** matches Phase-12 through Phase-21
well-scoped pattern — **eleven consecutive phases** ship with 0
Class A items.

### Class B — Phase-22 follow-ons: **~10 items in 7 categories**

#### B.1 Image-processing expansion (4; carried from Phase 20 unchanged)

- Q68 expansion content moderation (ADR-0019 D-F).
- Cropping UI (ADR-0019 D-F).
- Server-side resizing (ADR-0019 D-F).
- WebP/AVIF format conversion (ADR-0019 D-F).

#### B.2 Phase-17 markdown subset expansion (8; carried unchanged)

8 items carried verbatim through Phase 17-21 hygiene units.

#### B.3 Phase-18 surface expansion (3 + Q72; carried unchanged)

Markdown rationale; methodology page markdown unification;
per-challenge detail page; Q72 reviewNotesSchema divergence.

#### B.4 Phase-20 backfill operational follow-ons (3; carried unchanged)

`--user-id` + `--verbose` + session-level dry-run sentinel for
`backfill-exif-strip`.

#### B.5 Phase-21 cleanup operational follow-ons (2; carried unchanged)

`--prefix` (non-avatar blob types) + `--verbose` (per-blob progress)
for `cleanup-orphan-blobs`.

#### B.6 Phase-22 scaffold operational follow-ons (4; Phase-22 newly surfaced)

- **Prior-action auto-fill** — read most recent prior rating-action
  YAML for the same problem (via Velite `#site/content` or direct
  filesystem read); copy its 4 unchanged dimensions' values +
  rationales into the scaffold; auto-set `prior_action` to its
  filename. Eliminates the "copy unchanged from prior action"
  manual-edit step. ~1 unit when promoted.
- **`signals_considered` auto-gather** — surface recent rating-action
  YAMLs' signals + paper citations + leaderboard movements as
  candidate signals; curator picks. ~1 unit when promoted (couples
  to prior-action auto-fill).
- **Methodology-version dynamic read** — Phase 22 hardcodes
  `"1.0.0"`; Phase 23+ may read from `lib/methodology/` helper if
  multiple methodology versions exist. ~0.5 units when promoted.
- **Batch-emit multiple challenges** — Phase 22 single-challenge
  per invocation; Phase 23+ may add `--all-under-review` to emit
  drafts for all challenges in `under_review` status. ~0.5 units
  when promoted.

#### B.7 Operational + infrastructure (8; +1 from Phase 21 for new emit-challenge-action doc)

- Q69 / Q54 / Q55 operational gates.
- CI dummy `AUTH_SECRET`.
- `pnpm db:migrate` doc.
- First LHCI run.
- Middleware-based auth-route protection (threshold stays at 2).
- `pnpm backfill-exif-strip` operational doc (Phase-20 carryover).
- `pnpm cleanup-orphan-blobs` operational doc (Phase-21 carryover).
- **`pnpm emit-challenge-action` operational doc** — Phase-22
  newly surfaced. Curator-track; should sit alongside other
  operational-script docs when Phase 23+ writes contributor /
  operator docs.

### Class C — pre-existing carryovers (unchanged from Phase 21)

All ~25 Class C items persist unchanged.

### Phase-21 → Phase-22 delta

| Metric | Phase 21 close (`a5ee9e1`) | Phase 22 close pre-this (HEAD `570e739`) | Δ |
|---|---|---|---|
| Commits | 5 | 2 + this hygiene + 22.3 + 22.4 (anticipated) | +5 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | **19** | **19** | **0** (Phase 22 ships no new ADR; **fourth phase since Phase 3** — Phase 18 + 20 + 21 + 22; **first three-phase no-new-ADR streak**) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (**sixth consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 (**zero new env vars Phase 22**) |
| Runtime deps | (Phase-21 stack) | (Phase-21 stack) | 0 (**zero new deps Phase 22**) |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 564 / 54 files | **575 / 55 files** | **+11 tests / +1 file** (**first phase since Phase 19 to add tests**; Phase 20 + 21 both shipped zero) |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** (cleanup script is CLI not bundled; scaffold lib is server-only) |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS resolved | 26 | 26 | 0 (anticipated 0 in Unit 22.3) |
| OPEN_QUESTIONS open | 29 | 29 | 0 |
| OPEN_QUESTIONS lean | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 59 | 59 | 0 (**first three-phase Q-tally freeze anticipated** — Phase 20 + 21 + 22) |
| i18n keys per locale | 127 | 127 | 0 |
| LHCI URLs | 19 | 19 | 0 |
| `lib/storage/` files | 2 | 2 | 0 |
| `lib/rating-challenges/` files | 4 (+ 3 .test.ts) | **5 (+ 4 .test.ts)** | **+1 + 1 test** (`scaffold.ts` + `scaffold.test.ts`) |
| `scripts/` files | 8 | **9** | **+1** (`emit-challenge-action.ts`) |
| `package.json#scripts` entries | (+1: `cleanup-orphan-blobs`) | (+1: `emit-challenge-action`) | +1 |
| Server-side image processing surfaces | 2 | 2 | 0 |
| Privacy-by-default surfaces | 2 | 2 | 0 |
| Retroactive-privacy-correction surfaces | 1 | 1 | 0 |
| Storage-reconciliation surfaces | 1 | 1 | 0 |
| YAML-scaffolding surfaces | 0 | **1** | **+1** (first in project history) |
| Operational-script-keystone phases | 2 | **3** | **+1** (Phase 20 + 21 + 22 = **pattern crystallized**) |

### Architectural firsts shipped in Phase 22

1. **First YAML-scaffolding surface** in project history —
   Phase 5 `extract-leaderboard` writes JSON diffs to `drafts/`;
   Phase 22 introduces YAML-scaffolding to `drafts/ratings/`
   targeting the content-tree shape directly.
2. **Third consecutive operational-script-keystone phase** —
   Phase 20 + 21 + 22. **Pattern crystallized**: well-scoped
   operational-script keystones now stack to a recurring rhythm.
3. **Sixth consecutive 0-migration phase** (Phase 17 + 18 + 19 +
   20 + 21 + 22; 9 of 13 phases since DB landed are 0-migration:
   Phase 10 + 13 + 14 + 17 + 18 + 19 + 20 + 21 + 22).
4. **Sixth consecutive single-session phase** (Phase 17-22).
5. **Fourth phase since Phase 3 to close without new ADR** (Phase
   18 + 20 + 21 + 22; **first three-phase no-new-ADR streak**).
6. **First double-digit ADR-D-clause carryover closed** —
   ADR-0014 D-D carried 10 phases (Phase 12 → Phase 21 close;
   realized Phase 22 Unit 22.1).
7. **First three-phase Q-tally freeze anticipated** (Phase 20 +
   21 + 22; both shipped 0 Q-promotions / 0 status changes per
   the operational-script-keystone pattern).
8. **First phase since Phase 19 to add tests** — Phase 20 + 21
   shipped zero new tests; Phase 22 ships **+11 tests / +1 file**
   because the YAML scaffolder is pure-function-testable (no DB
   / FS / network dependencies).
9. **First operational-script-keystone phase to ship vitest
   tests** — Phase 20 + 21 scripts depended on live infrastructure
   (Vercel Blob list+del / Turso DB); Phase 22 scaffolder is pure
   string-in / string-out → testable.
10. **Pure-function-extraction precedent**: Phase 22 promoted
    pure helpers (originally inline per D-9 lean) to
    `lib/rating-challenges/scaffold.ts` because the testing
    requirement made the inline factoring untenable (top-level-
    await CLI shell conflicts with vitest's import-time
    execution). Future operational scripts that have pure
    helpers + want tests follow this precedent: factor to
    `lib/<domain>/<helper>.ts` + co-located `.test.ts`.

### Phase-22+ Q-candidates flagged

Carryovers from Phase 21 (8 candidates carried; 0 promoted Phase
22; 0 new Phase 22):

- **Q59** (CLI emit-challenge-action) — **CLOSED Phase 22** as
  ADR-0014 D-D realization. Q59 was a "Possible Q59 candidate"
  mentioned only in ADR-0014 D-D body — never promoted to
  OPEN_QUESTIONS.md, so its closure does not affect the
  mechanical Q-tally (which counts only `## QN.`-prefixed
  entries).
- **Q60** / **Q61** / **Q62** / **Q64** / **Q65** / **Q68
  expansion** / **Q72** all carried unchanged.

**Net Phase-22+ Q-candidates carrying: 7 candidates** (was 8 at
Phase 21 close; Phase 22 closes Q59-candidate via ADR-0014 D-D
realization without promotion-and-resolution cycle).

### Phase-23+ ADR slot reservations

**ADR-0020 candidate slot remains open after 9-phase ADR cycle
without multi-provider-OAuth claim** (Phase-15 ADR-0016 +
Phase-16 ADR-0017 + Phase-17 ADR-0018 + Phase-18 NO-new-ADR +
Phase-19 ADR-0019 + Phase-20 NO-new-ADR + Phase-21 NO-new-ADR +
Phase-22 NO-new-ADR = 4 ADR slots since Phase-15 + 5 cycles
without multi-provider claim):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried **12 phases** — strongest architectural patience
  signal growing each cycle without claim).
- **Image-processing API content moderation** (Q68 expansion).
- **Markdown subset extensions ADR** (Phase-17 D-H + Q72) if
  signal demands.
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  **16+ phases** — strongest overall patience signal growing).
- **Per-challenge detail page** (Phase-11 + 13 carryover; may
  not need new ADR).

### Three consecutive operational-script-keystone phases pattern crystallized

| Property | Phase 20 (backfill-exif-strip) | Phase 21 (cleanup-orphan-blobs) | Phase 22 (emit-challenge-action) |
|---|---|---|---|
| Units | 5 | 5 | 5 |
| Code units | 1 | 1 | 1 |
| New ADRs | 0 | 0 | 0 |
| New deps | 0 | 0 | 0 |
| New env vars | 0 | 0 | 0 |
| New vitest tests | 0 | 0 | **+11** ← exception |
| New migrations | 0 | 0 | 0 |
| New UI affordances | 0 | 0 | 0 |
| New i18n keys | 0 | 0 | 0 |
| New `scripts/` files | 1 | 1 | 1 |
| New `lib/` modules | 0 | 0 | **+1** (`scaffold.ts`) ← exception |
| Closes carryover | ADR-0019 D-E | Phase-16 + Phase-20 | ADR-0014 D-D |
| Carryover phase-count | 1 phase | 6 phases (Phase-16) | **10 phases** (first double-digit) |

The Phase-22 +11 tests + new `lib/` module exceptions emerged
from a single property of Phase-22 work: the YAML scaffolder is
pure-function-testable, where Phase 20 + 21 scripts depended
on live infrastructure. The exceptions are scope-appropriate
adaptations of the pattern, not departures.

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/22.2-phase-22-hygiene.md` (this).
- CHANGELOG: Unit 22.2 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 22.2 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-21 well-scoped pattern (eleven
consecutive); ~10 Class B items in 7 categories (4 image-
processing expansion carried from Phase 20; 8 Phase-17 markdown
subset carried; 3 Phase-18 surface + Q72 carried; 3 Phase-20
backfill operational carried; 2 Phase-21 cleanup operational
carried; **4 Phase-22 scaffold operational newly surfaced** —
prior-action auto-fill + signals_considered auto-gather +
methodology-version dynamic read + batch-emit; 8 operational +
infrastructure with `pnpm emit-challenge-action` operational doc
newly surfaced); Class C carryovers unchanged; Phase 21 → 22
delta (0 ADR Δ; 0 migration Δ; 0 dep Δ; 0 env var Δ; **+11 tests
/ +1 test file** — first phase since Phase 19 to add tests; 0
bundle Δ; +1 `lib/` module + 1 test file; +1 `scripts/` file;
+1 YAML-scaffolding surface; +1 operational-script-keystone
phase counter); 10 Phase-22 architectural firsts (incl. **third
consecutive operational-script-keystone phase = pattern
crystallized**; **first double-digit ADR-D-clause carryover
closed** at 10 phases; **first phase since Phase 19 to add
tests**; **first operational-script-keystone phase to ship
vitest tests**; **pure-function-extraction precedent
established**); **7 Phase-23+ Q-candidates** carry (Q59-candidate
closed Phase 22 as ADR-0014 D-D realization without promotion-
and-resolution); ADR-0020 candidate slot remains open after
9-phase ADR cycle (Phase-9 Class B item 8 carried **12 phases**;
Phase-5 D-4 punt carried **16+ phases**); three consecutive
operational-script-keystone phases pattern crystallized table
(Phase 20 + 21 + 22 share 5 units / 1 code unit / 0 ADRs / deps
/ env vars / migrations / UI / i18n / shape; Phase 22 +11 tests
+ +1 `lib/` module are scope-appropriate exceptions for
pure-function-testable surfaces).
