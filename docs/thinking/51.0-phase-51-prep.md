# 51.0 — Phase 51 prep (PubMed PMID alias syntax `[[pubmed:NNN|display]]` / `[[pmid:NNN|display]]` via dual-form regex extension on existing `remarkLinkPubmedIds` plugin in `PubmedExtensionRegistry`; **fourth realization of Phase-46 plugin-regex-extension phase-shape pattern**; **third dual-form regex** in the framework; **third plugin-regex-extension on a `remarkPlugins` consumer**; closes new Phase-50 deferral at **1-phase carryover** — fastest APPEND-deferral closure ever observed; APPEND-D-AI ninth two-letter slot; anticipated 5 units; **46th "Continue" override**; **forty-second NON-§13 phase**)

Phase 51 kickoff per `MASTER_PROMPT.md` §12 cardinal rule. Phase 50
closed at HEAD `9f83be8` (Unit 50.4 acceptance gate; forty-first
NON-§13 phase; 5 numbered units; fifth concrete Phase-37-framework
consumer (PubMed PMID); first 3rd-`remarkPlugins` consumer; first
3-consumer same-slot composition; first 5-consumer composition
under default dispatch — maximum-consumer-cardinality state;
APPEND-deferral closure cadence sustained 9 phases). This unit
is docs-only.

> **Sign-off note**: §12 phase-boundary sign-off granted via the
> **46th "Continue" override** invocation (over Phases 6-51).
> First-thread D-1 recommendation overridable into Unit 51.1.

> **§13 ledger status (carried)**: still **CLOSED**. Phase 51 is
> the **forty-second NON-§13 phase**.

## THINK

### Phase 50 → Phase 51 baseline (HEAD = `9f83be8`; clean tree)

- **Tests**: **1144 / 73 vitest files**.
- **ADRs**: **24**; **ADR-0018 D-G APPENDs**: **17** (D-AA …
  D-AH used Phase 43-50). Phase 51 takes **D-AI** (ninth two-
  letter slot).
- **First Load JS shared chunk**: **103 kB** through every
  Phase 9-50 unit (**111 consecutive units**).
- **Middleware bundle**: **160 kB UNCHANGED** since Phase 12.
- **DB tables**: 7; **Migrations**: 9; **Env vars**: 14
  (`MARKDOWN_EXTENSIONS` 6 single-value arms).
- **`lib/markdown/extensions/` files**: 17. Phase 51 anticipates
  **17 UNCHANGED** (in-place edit of `pubmed.ts` +
  `pubmed.test.ts`).
- **Phase-37-framework concrete consumers**: 5. Phase 51 keeps
  count at **5** (regex extension on existing pubmed consumer).
- **Plugin-regex-extension realizations**: **3** (Phase 46
  wikilinks alias; Phase 47 arxiv alias; Phase 48 doi alias).
  Phase 51 anticipates **4** (+pubmed alias).
- **Dual-form regex realizations**: **2** (arxiv + doi). Phase
  51 anticipates **3** (+pubmed).
- **Plugin-regex-extension on `remarkPlugins` consumers**: **2**
  (arxiv + doi). Phase 51 anticipates **3** (+pubmed) — **all 3
  `remarkPlugins` consumers exhibit dual-form regex post-Phase
  51**.
- **Surfaces with alias-syntax consumers count**: rationale
  carries 3 alias-syntax consumers (wikilinks + arxiv + doi);
  Phase 51 anticipates **4 alias-syntax consumers on rationale**
  (wikilinks + arxiv + doi + pubmed) — **first quadruple-alias
  surface**.
- **OPEN_QUESTIONS state**: 66 UNCHANGED since Phase 36.
- **Phase-37+ candidate count**: 8 UNCHANGED.
- **APPEND-deferral closure cadence**: sustained 9 phases.
  Phase 51 continues with **new Phase-50 deferral**
  ("PubMed PMID display-text alias syntax — mirrors Phase-47
  arxiv-alias + Phase-48 doi-alias dual-form regex extension;
  Phase 51+ analogous extension"). **1-phase carryover** —
  **fastest APPEND-deferral closure ever observed** (beats
  the previous record of 3-phase Phase-44 cross-surface-arxiv
  + Phase-48 doi-alias).
- **Working tree at session start**: clean (Phase 50 close
  committed at `9f83be8`).

### D-1. First-thread recommendation: **PubMed PMID alias syntax via dual-form regex extension on `remarkLinkPubmedIds`**

Closes:

1. **New Phase-50 deferral** ("PubMed PMID display-text alias
   syntax `[[pubmed:NNN|display]]` / `[[pmid:NNN|display]]` —
   mirrors Phase-47 arxiv-alias + Phase-48 doi-alias dual-form
   regex extension; Phase 51+ analogous extension") at
   **1-phase carryover** (Phase 50 → Phase 51). **FASTEST
   APPEND-DEFERRAL CLOSURE EVER OBSERVED**. Cadence trajectory:
   Phase-46 wikilinks alias 8-phase; Phase-47 arxiv alias
   6-phase; Phase-48 doi alias 3-phase; Phase-51 pubmed alias
   **1-phase**. Each alias-syntax realization halves (or
   better) the architectural risk for the next.
2. **Phase-50-gate Phase-51-candidate rank 1**.

**Why PubMed PMID alias now** (immediately after Phase-50
pubmed first-ship):

- **Fourth realization of the Phase-46 plugin-regex-extension
  phase-shape pattern**. Phase 46 wikilinks (rehype); Phase
  47 arxiv (remark, first); Phase 48 doi (remark, second);
  Phase 51 pubmed (remark, third). **All 3 `remarkPlugins`
  consumers exhibit dual-form regex post-Phase 51**.
- **Third dual-form regex** in the framework. Pattern is now
  load-bearing vocabulary; sets the precedent that any new
  `remarkPlugins` consumer ships bare-first + alias-via-dual-
  form-extension as a separate phase.
- **First "alias-syntax on a non-bracketed-base consumer with
  prefix-alternation"**. Phase 47 arxiv had single prefix
  (`arxiv:`); Phase 48 doi had single prefix (`doi:`); Phase
  51 pubmed has prefix-alternation (`pubmed:` OR `pmid:`)
  inherited from Phase 50 ship. The bracketed alias form must
  accept BOTH prefixes — tests whether the dual-form pattern
  generalizes to alternation within the bracketed branch.
- **First "first-phase APPEND-deferral closure"** in project
  history. New record beats the prior 3-phase fastest (Phase
  44 + Phase 48). **Cadence acceleration validated** — the
  Phase-46 → Phase-47 → Phase-48 alias-syntax trajectory now
  extends with a 1-phase Phase-50 → Phase-51 step. Each new
  alias-syntax realization reduces the architectural risk for
  the next, accelerating the cadence toward immediate-
  successor-closure.
- **First "first-phase same-thread-direction phase boundary"**.
  Phase 50 was sibling-consumer introduction; Phase 51 is
  alias-syntax extension on the same consumer. **First
  consecutive-phase pair where the second phase extends the
  consumer the first phase introduced**. Pre-Phase-51 precedent
  was 4+ phases between consumer-introduction and alias-syntax:
  Phase 38 wikilinks → Phase 46 alias (8-phase gap); Phase 41
  arxiv → Phase 47 alias (6-phase gap); Phase 45 doi → Phase 48
  alias (3-phase gap); Phase 50 pubmed → Phase 51 alias
  (**1-phase gap; immediate-successor**).
- **No new runtime dep**: in-place regex extension.
- **Concrete demand signal**: same as Phase-46/47/48 alias —
  curator paper-citation prose benefits from human-readable
  citation display (e.g., `[[pubmed:12345678|Smith et al.
  2024]]`). PubMed PMID alias extends the alias-syntax
  coverage to the most-recent consumer.

**Path A** (no new ADR; APPEND ADR-0018 D-G) chosen per Phase-
37 through 50 D-9 precedent. ADR-0018 D-G gains its **18th
APPEND** — extends the record from 17 → 18. **Ninth two-letter
APPEND letter D-AI** (after Phase-43 D-AA … Phase-50 D-AH).

### D-2. DB-migration trigger re-evaluation

Phase 51 anticipates **0 new migrations**. Pure regex-extension
in a TypeScript module + test additions.

### D-3. PubMed alias regex shape anticipated (Unit 51.1)

D-A. **`PUBMED_PATTERN` regex evolution**:

```ts
// Before (Phase 50 ship):
const PUBMED_PATTERN = /\b(?:pubmed|pmid):(\d{1,9})\b/gi;

// After (Phase 51 ship):
const PUBMED_PATTERN =
  /\[\[(?:pubmed|pmid):(\d{1,9})(?:\|([^\]\n]+))?\]\]|\b(?:pubmed|pmid):(\d{1,9})\b/gi;
```

**Third dual-form regex** in the framework: alternation between
the bracketed form (priority) and the bare form (fallback).
Engine tries the bracketed alternative first; if it fails at
a given position, tries the bare alternative.

- **Bracketed form** `\[\[(?:pubmed|pmid):(\d{1,9})(?:\|([^\]\n]+))?\]\]`:
  - **Inner prefix-alternation** `(?:pubmed|pmid):` inherits
    Phase-50 dual-prefix support. **First dual-form regex with
    inner alternation inside the bracketed branch** in project
    history — Phase 47 arxiv + Phase 48 doi each had single-
    prefix bracketed forms.
  - Group 1 = PubMed ID (Phase-50 baseline preserved).
  - Group 2 = optional display text (`[^\]\n]+`; mirrors
    Phase-46/47/48 alias display class).
  - **No trailing-lookahead** — `]]` is the explicit
    terminator.
- **Bare form** `\b(?:pubmed|pmid):(\d{1,9})\b`:
  - Group 3 = PubMed ID (Phase-50 baseline preserved verbatim).
  - **Word-boundary anchors** preserved verbatim — no
    lookahead-vs-`\b` divergence (pubmed bare form had no
    lookahead at Phase-50 ship, unlike doi).

D-B. **`remarkLinkPubmedIds` plugin body update**: branch on
`isBracketed = match[0].startsWith("[[")`. Mirrors Phase-
47/Phase-48 body shape verbatim with `arxiv:`/`doi:` →
`(?:pubmed|pmid):` substitution + `https://pubmed.ncbi.nlm.nih.gov/${id}/`
URL form preserved + bracketed-without-alias drops `[[`+`]]`
while preserving source casing of BOTH the prefix variant
(`pubmed:` vs `pmid:`) AND any source-case mixing (`PubMed:`,
`PMID:`, etc.).

D-C. **`PubmedExtensionRegistry` class + factory dispatch arm
+ `PHASE_50_DEFAULT_ENABLED_SURFACES` constant — UNCHANGED**.
Phase 51 is plugin-internal regex extension.

D-D. **Tests to update / add** (Unit 51.1, ~12 NEW tests in
`pubmed.test.ts`):

1. Bracketed `[[pubmed:NNN|display]]` → `<a href=...>display</a>`.
2. Bracketed `[[pmid:NNN|display]]` (alternative prefix in
   alias form) → same canonical URL.
3. Bracketed without alias `[[pubmed:NNN]]` → verbatim ref.
4. Bracketed without alias preserves source casing of prefix
   variant (`[[PubMed:NNN]]` / `[[PMID:NNN]]`).
5. Backwards-compat: bare `pubmed:NNN` (Phase-50 baseline)
   still works.
6. Aliased + bare coexist in same paragraph.
7. Empty alias `[[pubmed:NNN|]]` falls through (likely
   fully-literal per Phase-48 doi divergence pattern; pubmed
   bare uses `\b` which behaves like arxiv — so the inner
   bare match SHOULD remain, making this case behave like
   Phase-47 arxiv rather than Phase-48 doi).
8. Display HTML-escapes via text-node rendering (XSS safety).
9. Bracketed with mixed-case prefix (`[[PMID:NNN|display]]`).
10. Multiple aliased pubmed refs in same paragraph.
11. Bracketed pubmed inside bold renders correctly.
12. Bracketed form with `pubmed:` prefix + `pmid:` prefix
    coexist in same paragraph (verify both bracketed-form
    prefix variants work in alternation).

D-E. **`lib/markdown/index.test.ts` — NEW end-to-end tests**
(Unit 51.2):

1. `describe("Phase-51 pubmed alias syntax — rationale surface under default dispatch")`
   block — alias renders correctly on rationale under
   `MARKDOWN_EXTENSIONS=pubmed`; non-rationale surfaces
   default-deny per Phase-50 rationale-only default.
2. `describe("Phase-51 pubmed alias under Phase-50 5-way composite")`
   block — wikilinks + arxiv + doi + pubmed alias all coexist
   on rationale under 5-way composite default. **First
   quadruple-alias surface** in project history.

D-F. **Composition matrix Phase 51**: alias is plugin-internal;
composition unchanged. Rationale post-Phase-51 carries 4 alias-
syntax consumers (wikilinks + arxiv + doi + pubmed). **First
quadruple-alias surface** (rationale-only at Phase 51 ship;
generalizes to all 4 surfaces if/when Phase ~54 pubmed cross-
surface expansion lands).

D-G. **APPEND letter sequence: D-AI (ninth two-letter slot)**.
Phase 43 D-AA … Phase 50 D-AH; Phase 51 D-AI.

D-H. **Phase 52+ deferrals** (Phase-51 pubmed-alias scope cap):

- **PubMed PMID cross-surface expansion** (Phase 50 deferral
  carries) — Phase ~54 at 4-phase-gap cadence.
- **Older-style category-prefixed arxiv IDs** (APPEND-D-Y item
  2 carries) — Phase 52+.
- **Bare arxiv / DOI / PubMed IDs without prefix** — Phase 52+.
- **dx.doi.org legacy host parsing** — Phase 52+.
- **ORCID auto-link consumer** — sixth concrete consumer;
  Phase 52+.
- **bioRxiv / OSF preprint consumers** — sixth or later
  concrete consumers; Phase 52+.
- **Cross-entity wikilinks** (APPEND-D-L item 3) — Phase 52+.
- **`<a class="wikilink">` styling** (APPEND-D-L item 4) —
  Phase 52+.
- **404 handling for unresolved wikilinks** (APPEND-D-L item 5)
  — Phase 52+.
- **Plugin parameterization for wikilink-href-builder**
  (APPEND-D-L item 6) — Phase 52+.
- **Auto-trim of alias display whitespace** — Phase 52+.
- **Empty-alias fallback unification** across consumers —
  Phase 52+.
- **Table-specific attributes** (APPEND-D-Q item 3) — Phase
  52+.
- **`<caption>` element** + **Surface-specific table schemas**
  (APPEND-D-Q items 4 + 6) — Phase 52+.
- **4th-or-later `remarkPlugins` consumer beyond arxiv + doi
  + pubmed** — Phase 52+.
- **2nd `rehypePlugins` consumer beyond wikilinks** — Phase
  52+.
- **2nd `schemaOverrides` consumer beyond tables** — Phase
  52+.

### Provisional unit breakdown (51.0 … 51.4)

| Unit | Title | Type |
|---|---|---|
| 51.0 | **Phase 51 prep** (this unit) | docs |
| 51.1 | `PUBMED_PATTERN` regex evolution to dual-form + plugin body update + ~12 NEW `pubmed.test.ts` tests + ADR-0018 D-G APPEND-D-AI | code+APPEND |
| 51.2 | NEW end-to-end pubmed alias tests for rationale + 5-way composite + first quadruple-alias surface | code |
| 51.3 | Phase-51 hygiene + new-Phase-50-deferral closure documentation + 1-phase carryover record | docs |
| 51.4 | **Phase 51 acceptance gate** | gate |

Total: **5 units**. Mirrors Phase 41-50 5-unit framework shape
verbatim.

### Anticipated architectural firsts in Phase 51 (12)

1. **Fourth realization of the Phase-46 plugin-regex-extension
   phase-shape pattern**. Phase 46 wikilinks (rehype); Phase
   47 arxiv (remark, 1st); Phase 48 doi (remark, 2nd); Phase
   51 pubmed (remark, 3rd). **All 3 `remarkPlugins` consumers
   exhibit dual-form regex post-Phase-51**.
2. **Third dual-form regex** in the framework.
3. **Third plugin-regex-extension on a `remarkPlugins`
   consumer** in project history.
4. **First dual-form regex with inner alternation inside the
   bracketed branch**. Phase 47 arxiv + Phase 48 doi had
   single-prefix bracketed forms; Phase 51 pubmed inherits
   the Phase-50 dual-prefix support (`pubmed:` OR `pmid:`)
   inside the bracketed branch.
5. **First "alias-syntax on a non-bracketed-base consumer
   with prefix-alternation"**.
6. **First "first-phase APPEND-deferral closure"** in project
   history. New Phase-50 deferral → Phase-51 realization at
   **1-phase carryover**. New fastest-closure record (beats
   prior 3-phase record from Phase 44 + Phase 48).
7. **First "immediate-successor same-thread-direction phase
   boundary"** — Phase 50 was sibling-consumer introduction;
   Phase 51 is alias-syntax extension on the same consumer.
   First consecutive-phase pair where the second phase
   extends the consumer the first phase introduced.
8. **Tenth prep-/APPEND-doc-level deferral closed by a later
   phase**. APPEND-deferral closure cadence sustained 10
   phases — longest ever (extends Phase-50 record 9 → 10).
9. **Third non-cross-surface-expansion APPEND-deferral
   closure** in the cadence (Phase 47 arxiv alias was first
   non-CSE on remarkPlugins; Phase 48 doi alias was second
   non-CSE on remarkPlugins; Phase 51 pubmed alias is third
   non-CSE on remarkPlugins). **First "three consecutive
   non-cross-surface-expansion closures on the same slot
   kind"**.
10. **18th APPEND on ADR-0018 D-G** — extends first-ADR-D-
    clause-with-most-APPENDs record 17 → 18.
11. **Ninth two-letter APPEND letter D-AI** (after Phase-43
    D-AA + … + Phase-50 D-AH).
12. **First quadruple-alias surface** in project history —
    rationale carries wikilinks alias (Phase 46) + arxiv
    alias (Phase 47) + doi alias (Phase 48) + pubmed alias
    (Phase 51) simultaneously under 5-way default. **First
    surface-with-4-alias-consumers cardinality of 1** (other
    surfaces remain triple-alias at Phase-49 baseline; pubmed
    rationale-only per Phase-50 default). Plus **21-phase no-
    new-B-category streak**; **forty-second NON-§13 phase**;
    **115+ consecutive 103 kB First Load JS unit**.

### Smoke gates (this unit)

Docs-only — no source files modified.

- File added: `docs/thinking/51.0-phase-51-prep.md`.
- CHANGELOG: open `### Phase 51` section + Unit 51.0 entry.
- `pnpm audit-content`: 0 errors / 6 warnings UNCHANGED.
- `pnpm test`: 1144/73 UNCHANGED.
- First Load JS / middleware: UNCHANGED.

### Parallel-curator awareness

This unit docs-only; no collision. Phase 51 collision risk:

- 51.1: MODIFIED `lib/markdown/extensions/pubmed.ts` +
  `pubmed.test.ts` + `docs/adr/0018-markdown-sanitization.md`.
- 51.2: MODIFIED `lib/markdown/index.test.ts`.
- 51.3 / 51.4: docs hygiene + gate; parallel-safe.

### Phase-51 anti-scope

NOT PubMed PMID cross-surface expansion (Phase 51+ rank 8 from
Phase-50 gate; Phase ~54 at 4-phase cadence);
NOT older-style category-prefixed arxiv IDs (Phase 52+);
NOT bare arxiv / DOI / PubMed IDs without prefix (Phase 52+);
NOT dx.doi.org legacy host (Phase 52+);
NOT ORCID consumer (Phase 52+);
NOT bioRxiv / OSF preprint consumers (Phase 52+);
NOT cross-entity wikilinks (Phase 52+);
NOT `<a class="wikilink">` styling (Phase 52+);
NOT 404 handling (Phase 52+);
NOT plugin parameterization (Phase 52+);
NOT auto-trim of alias display whitespace (Phase 52+);
NOT empty-alias fallback unification (Phase 52+);
NOT table-specific attributes (Phase 52+);
NOT `<caption>` element (Phase 52+);
NOT surface-specific table schemas (Phase 52+);
NOT `@mention` (Phase 52+);
NOT 4th-or-later `remarkPlugins` consumer beyond arxiv + doi
+ pubmed (Phase 52+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 52+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 52+);
NOT ADR-0025 concrete moderation provider;
NOT Q78/Q79/v2 methodology/pre-commit typecheck/safeAuth/test
backfill/account-deletion (each deferred);
all Phase-30-through-50 deferrals carry forward.

## DESIGN — not applicable

This unit is docs-only.

## CODE — not applicable

Same.

## ITERATE — gates

- [x] Phase 50 close baseline documented.
- [x] D-1 first-thread recommendation framed.
- [x] Dual-form regex shape with inner prefix-alternation
      sketched (D-3).
- [x] Anti-scope enumerated.
- [x] Anticipated architectural firsts enumerated (12).
- [x] Provisional unit breakdown (5 units).
- [x] Parallel-curator awareness restated.
- [x] No new ADRs (Path A per Phase-37-through-50 precedent).
- [x] No new OPEN_QUESTIONS entries.
- [x] Smoke gates green (docs-only).
- [ ] Unit 51.0 ships at commit `<this unit>`; Unit 51.1
      authors the regex evolution + plugin body update + tests
      + APPEND-D-AI.
