# 26.2 — Phase-26 hygiene status pass (0 Class A / ~7 Class B / Class C unchanged; fifteen consecutive well-scoped phases)

Third Phase-26 unit; docs-only. Mirrors Phase-13 through Phase-25
hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-26 cleanup: **0 items**

Phase-26's single code unit (26.1) shipped without deferrals.
Route + helper + i18n + tests all realized in one code unit
with +3 visibility-predicate tests. Phase-26-deferred refinements
(markdown rendering for `rationale` per thread F; listing-page
"View details" link from `/profile` + `/u/{handle}/challenges`;
linking `acceptedActionId` to a rendered rating-action page;
curator-translated FR keys) are explicit scope-cap decisions
per Unit 26.0 — NOT Class A. **Phase-26 Class A count = 0**
matches Phase-12 through Phase-25 well-scoped pattern —
**fifteen consecutive phases** ship with 0 Class A items.

### Class B — Phase-26 follow-ons: **~7 items in 11 categories**

#### B.1 Image-processing expansion (3; carried unchanged)

Q68 expansion content moderation; cropping UI; AVIF format
conversion.

#### B.2 Phase-17 markdown subset expansion (8; carried unchanged)

#### B.3 Phase-18 surface expansion (3 + Q72; carried unchanged)

#### B.4 Phase-20 backfill operational follow-ons (3; carried unchanged)

#### B.5 Phase-21 cleanup operational follow-ons (2; carried unchanged)

#### B.6 Phase-22 scaffold operational follow-ons (0 carried; closed Phase 25)

#### B.7 Phase-23 scaffold operational follow-ons (0 carried; closed Phase 25)

#### B.8 Phase-24 transcode operational follow-ons (1 carried)

AVATAR_SIZE / WEBP_QUALITY tuning.

#### B.9 Operational + infrastructure (9; carried unchanged)

#### B.10 Phase-26 detail-page follow-ons (4; Phase-26 newly surfaced)

- **Markdown rationale for rating-challenge** — Phase-11
  shipped `rationale` as plain-text (50-2000 chars,
  `whitespace-pre-wrap`). Detail page renders plain text per
  Phase-13 listing precedent. Phase-27+ if user demand for
  links / code / formatting in challenge rationale. ~2-3
  units; couples to Phase-18 markdown-render infrastructure
  (would extend `bioSchema` or introduce `rationaleSchema`).
- **Listing-page "View details" links** — Phase-26 ships the
  detail page; `/u/{handle}/challenges` + `/profile` +
  per-problem `/problems/{slug}/challenges` listings still
  show truncated rationale without a "View details" affordance.
  Phase-27+ if curator + user feedback prefer hard links over
  problem-link-then-click-through navigation. ~0.5 units;
  3-file edit (`/u/[handle]/challenges/page.tsx` +
  `/profile/page.tsx` + per-problem listing route).
- **`acceptedActionId` link target** — Phase-26 displays the
  filename as text. If the rating-action YAML is rendered at
  a public URL (e.g., `/problems/{slug}/ratings/{filename}`
  for the static-prerendered methodology context), the detail
  page could link the filename. Phase-27+ if such a route
  exists or is added. ~0.5 units.
- **Curator-translated FR i18n** — Phase-26 ships
  machine-translated FR keys per Phase-17/18 precedent. Curator
  review for quality drift could land Phase-27+. ~0.5 units
  (single-file edit; no code change).

#### B.11 Operational + i18n maintenance (Phase-26 newly surfaced)

- **i18n key count audit** — Phase 26 ships 12 new keys
  bringing per-locale count from 127 → 139. Methodology v1
  alignment (Phase-26+ if new methodology versions introduce
  related i18n shifts) tracked here.

### Class C — pre-existing carryovers (unchanged)

All ~25 Class C items persist unchanged.

### Phase-25 → Phase-26 delta

| Metric | Phase 25 close (`6127fb4`) | Phase 26 close pre-this (HEAD `c8d48c0`) | Δ |
|---|---|---|---|
| Commits | 5 | 2 + this hygiene + 26.3 + 26.4 (anticipated) | +5 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | **19** | **19** | **0** (**eighth phase since Phase 3** — Phase 18 + 20 + 21 + 22 + 23 + 24 + 25 + 26; **seven-phase no-new-ADR streak**) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (**tenth consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-25 stack) | (Phase-25 stack) | 0 (**zero new deps Phase 26**) |
| Tests | 592 / 55 files | **595 / 55 files** | **+3 / 0** |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | **8** | **+1** (new `ƒ /[locale]/u/[handle]/challenges/[id]`) |
| First Load JS (shared) | 103 kB | **103 kB** | **0** |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| `/u/{handle}/challenges/{id}` bundle | (did not exist) | **108 kB** | **+1 new route at 108 kB** |
| OPEN_QUESTIONS resolved | 26 | 26 | 0 (anticipated 0 in Unit 26.3) |
| OPEN_QUESTIONS open | 29 | 29 | 0 |
| OPEN_QUESTIONS total | 59 | 59 | 0 (**seventh consecutive Q-tally freeze anticipated** — Phase 20-26) |
| i18n keys per locale | 127 | **139** | **+12** (first i18n change since Phase 17 close = 8 phases) |
| `lib/rating-challenges/` files | 5 (+ 4 .test.ts) | 5 (+ 4 .test.ts) | **0** (in-place edit) |
| `scripts/` files | 10 | 10 | 0 |
| `app/[locale]/u/[handle]/challenges/` files | 1 page | **2** | **+1** (`[id]/page.tsx`) |
| Server-side image processing surfaces | 3 | 3 | 0 |
| Public profile-related routes | 2 (`/u/{handle}` + `/u/{handle}/challenges`) | **3** | **+1** (`/u/{handle}/challenges/{id}`) |
| Operational-script-keystone phases | 6 | **6** | **0** (streak BROKEN; Phase 26 is the first non-operational-script-keystone phase since Phase 19) |
| UI/UX phases since Phase 18 | 0 | **1** | **+1** (first since Phase 18) |

### Architectural firsts shipped in Phase 26

1. **Breaks the operational-script-keystone streak at 6 phases**.
   Phase 20-25 = 6 consecutive operational-script-keystones;
   Phase 26 = first non-operational-script-keystone phase since
   Phase 19. Pattern observed at 6 phases; cleanly broken without
   project disruption.
2. **First user-facing UX since Phase 18** — 8 phases of
   curator/server-only work; Phase 26 = first user-facing route
   addition since Phase 18.
3. **First new public page route since Phase 14** — 12 phases
   without new public route; Phase 26 = first new dynamic
   public route.
4. **First i18n key change since Phase 17 close** — 8 phases
   unchanged at 127 per locale; Phase 26 = first +12 keys
   (new `public_challenge_detail` namespace).
5. **Tenth consecutive 0-migration phase** (Phase 17-26; 13 of
   17 phases since DB landed are 0-migration).
6. **Tenth consecutive single-session phase** (Phase 17-26).
7. **Eighth phase since Phase 3 to close without new ADR** (Phase
   18 + 20 + 21 + 22 + 23 + 24 + 25 + 26; **seven-phase
   no-new-ADR streak**).
8. **First reuse of Phase-18 `renderReviewNotesMarkdown` outside
   the curator dashboard surface** — Phase-18 surfaced reviewNotes
   markdown rendering in `/curator/challenges/[id]` + `/profile`;
   Phase-26 adds third surface at `/u/{handle}/challenges/[id]`.
   Validates Phase-18 ADR-0018 D-G inheritance contract reuse at
   third call site.
9. **First page route to use drizzle-orm `alias()`** — Phase-26
   `getPublicChallengeDetailById` aliases `users` twice (submitter
   + reviewer) in a single query. Establishes the pattern for
   future routes that need same-table multi-join.
10. **First per-status reviewer-info gating** (server-side
    visibility filter in `getPublicChallengeDetailById` + pure
    `shouldShowReviewerInfo` predicate). Defense-in-depth pattern:
    SQL filter excludes private statuses; helper nullifies reviewer
    fields for non-accepted public statuses regardless of DB row
    state.

### Phase-26+ Q-candidates flagged

Carryovers from Phase 25 (7 candidates carried; 0 promoted Phase
26; 0 new Phase 26):

- **Q60** (curator authz evolution).
- **Q61** (submitter anonymity option).
- **Q62** (rejection-rationale public visibility).
- **Q64** (per-user privacy opt-out).
- **Q65** (per-curator activity feed).
- **Q68 expansion** (content moderation; ADR-0019 D-F row 1).
- **Q72** (`reviewNotesSchema` divergence).

### Phase-27+ ADR slot reservations

**ADR-0020 candidate slot remains open after 13-phase ADR cycle
without multi-provider-OAuth claim** (Phase-15 ADR-0016 +
Phase-16 ADR-0017 + Phase-17 ADR-0018 + Phase-18 NO-new-ADR +
Phase-19 ADR-0019 + Phase-20-26 NO-new-ADR):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried **16 phases** — strongest architectural patience
  signal growing each cycle).
- **Image-processing API content moderation** (Q68 + Q68
  expansion).
- **Markdown subset extensions ADR** (Phase-17 D-H + Q72;
  Phase 26 surfaces an explicit thread F candidate for
  markdown-promoted rationale).
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  **20+ phases** — strongest overall patience signal growing).

### Six-phase operational-script-keystone streak retrospective

| Property | P20 | P21 | P22 | P23 | P24 | P25 | P26 |
|---|---|---|---|---|---|---|---|
| Operational-script-keystone | yes | yes | yes | yes | yes | yes | **NO (UI/UX)** |
| Units | 5 | 5 | 5 | 5 | 5 | 5 | 5 |
| Code units | 1 | 1 | 1 | 1 | 1 | 1 | 1 |
| New ADRs | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| New deps | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| New env vars | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| New vitest tests | 0 | 0 | +11 | +10 | 0 (5 in-place) | +7 | +3 |
| New `scripts/` files | 1 | 1 | 1 | 0 | 1 | 0 | 0 |
| New `lib/` modules | 0 | 0 | +1 | 0 | 0 | 0 | 0 |
| New `app/` pages | 0 | 0 | 0 | 0 | 0 | 0 | **+1** |
| New i18n keys | 0 | 0 | 0 | 0 | 0 | 0 | **+12** |
| New dynamic routes | 0 | 0 | 0 | 0 | 0 | 0 | **+1** |
| Items closed | 1 | 1 | 1 | 2 | 2 | 4 | 2 |
| User-value-at-deploy | privacy backfill | storage hygiene | curator scaffolder | scaffolder auto-fill | bandwidth + storage + render | curator workflow ergonomics | **user-facing detail UX** |

**Streak broken cleanly at 6 phases.** Phase 26 inherits the
streak's invariants (5 units / 1 code unit / 0 new ADRs / 0
new deps / 0 new env vars / 0 new migrations) but adds new
dimensions (+1 page / +12 i18n keys / +1 dynamic route) that
the operational-script-keystone pattern didn't exercise.
Demonstrates the project's well-scoped-phase rhythm survives a
pattern shift without disruption.

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/26.2-phase-26-hygiene.md` (this).
- CHANGELOG: Unit 26.2 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 26.2 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-25 well-scoped pattern
(**fifteen consecutive**); ~7 Class B items in 11 categories
(3 image-processing carried; 8 markdown carried; 3 Phase-18 +
Q72 carried; 3 Phase-20 carried; 2 Phase-21 carried; 0 carried
for B.6 + B.7 (closed Phase 25); 1 Phase-24 carried;
**4 Phase-26 newly surfaced under B.10**: markdown rationale +
listing-page "View details" links + acceptedActionId link
target + curator-translated FR; **1 Phase-26 newly surfaced
under B.11**: i18n key count audit; 9 operational +
infrastructure unchanged); Class C carryovers unchanged; Phase
25 → 26 delta (0 ADR Δ; 0 migration Δ; 0 dep Δ; 0 env var Δ;
+3 tests / 0 file Δ; 0 First Load JS Δ; 0 middleware Δ; +1
dynamic route Δ; **+12 i18n keys / locale Δ** (first since
Phase 17 close = 8 phases); +1 `app/[locale]/u/[handle]/challenges/`
file (new `[id]/page.tsx`); +1 public profile-related route);
**10 Phase-26 architectural firsts** (incl. **breaks
operational-script-keystone streak at 6 phases**, **first
user-facing UX since Phase 18**, **first new public page route
since Phase 14**, **first i18n key change since Phase 17 close**,
**eighth phase since Phase 3 without new ADR = first
seven-phase no-new-ADR streak**, **first reuse of Phase-18
renderReviewNotesMarkdown outside curator dashboard surface**,
**first page route to use drizzle-orm alias()**, **first
per-status reviewer-info gating with defense-in-depth pattern**);
7 Phase-27+ Q-candidates carry unchanged; ADR-0020 candidate
slot remains open after **13-phase ADR cycle without
multi-provider-OAuth claim** (Phase-9 Class B item 8 carried
**16 phases**; Phase-5 D-4 carried **20+ phases**); six-phase
operational-script-keystone streak retrospective table shows
clean break at Phase 26 — well-scoped-phase rhythm survives
pattern shift without disruption.
