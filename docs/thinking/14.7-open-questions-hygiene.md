# 14.7 — OPEN_QUESTIONS hygiene + ADR review (Phase 14 pre-close)

Eighth Phase-14 unit; docs-only. Mirrors Unit 5.12 / 6.9 / 7.10 / 8.8 /
9.8 / 10.4 / 11.6 / 12.7 / 13.5 OQ-hygiene precedents. Scans the
OPEN_QUESTIONS ledger for Phase-14 promotions + reviews the 15 ADRs
at HEAD for stale status / supersede markers. Lands ahead of Unit
14.8 (Phase 14 acceptance gate); the gate cites the updated state.

## OPEN_QUESTIONS scan

Phase 14 surfaced **3 candidate questions** anticipated by ADR-0015's
deferral clauses but **NOT promoted** to formal `Status: open` ledger
entries this unit:

- **Q63 candidate** (user-editable profile fields) — ADR-0015 D-C
  deferral. Requires writes API + new DB columns (`users.displayName`,
  `users.bio`, possibly `users.imageOverride`) + new migration + XSS
  sanitization + possible Vercel-Blob image-upload ADR. **Phase 15+
  if usage feedback demands.**
- **Q64 candidate** (per-user privacy opt-out toggle) — ADR-0015 D-D
  deferral. Requires `users.profilePublic` boolean column + migration
  + writes surface + 404 response on `/u/{handle}` when opted-out.
  **Phase 15+ if signal appears.**
- **Q65 candidate** (per-curator activity feed / contribution
  timeline) — ADR-0015 D-E expansion. Requires `getCuratorActions(handle)`
  helper + new `/u/{handle}/curated` sub-route + rating-action history
  join. **Phase 15+ if per-curator surfaces accumulate usage.**

**Promotion deferral rationale**: candidate Q-numbers stay flagged in
CHANGELOG + ADR-0015 cross-references + Phase-14 hygiene catalog
(Unit 14.6 Class B items 1 / 2 / 3) but do NOT enter the formal
`OPEN_QUESTIONS.md` ledger until Phase 15+ kickoff actually decides
to pull them. Mirrors Phase-13 precedent (Q61 + Q62 candidates from
Unit 13.0 D-? remained flagged but unpromoted at Phase-13 close;
Unit 13.5 deferred promotion explicitly).

No reopenings, no new resolutions, no other Q-state changes this
phase.

## Ledger state at HEAD `0faa3e4` (mechanical `Status:`-field tally)

Derived from `Status:` lines in `OPEN_QUESTIONS.md` per Unit 9.8
canonical convention. Phase 14 delta vs Phase-13 close: **+0 net** (no
promotions; no surfacings to the ledger).

- **21 resolved**: Q1, Q4, Q5, Q12, Q13, Q18, Q27, Q32, Q40, Q41,
  Q43, Q45, Q46, Q48, Q49, Q50, Q52, Q53, Q56, **Q57**, **Q58**
  (last two from Phase 12 + Phase 13 closes respectively).
- **4 decided-as-lean**: Q38, Q42, Q44, Q51.
- **28 open**: Q2, Q3, Q6, Q7, Q8, Q9, Q10, Q11, Q14, Q15, Q16, Q17,
  Q19, Q25, Q26, Q28, Q29, Q30, Q31, Q33, Q34, Q35, Q36, Q37, Q39,
  Q47, Q54, Q55.
- **Total: 53 entries** UNCHANGED from Phase-13 close.

**Phase-14+ candidate Q-numbers flagged** (not in ledger): Q59 (CLI
emit-challenge-action; Phase-12 carryover), Q60 (curator authz
evolution; Phase-12 carryover), Q61 (anonymity option; Phase-13
carryover), Q62 (rejection-rationale visibility; Phase-13 carryover),
**Q63 (user-editable fields; Phase-14 new)**, **Q64 (privacy opt-out;
Phase-14 new)**, **Q65 (per-curator activity feed; Phase-14 new)**.

## ADR review

15 ADRs at HEAD (0001 – 0015). Phase 14 added **1 new ADR** (ADR-0015
in Unit 14.1). All 14 prior ADRs unchanged in body; status remains
`accepted` for the entire set.

### Phase-14 unit-level ADR exercise

- **ADR-0015 D-A through D-F all realized in Units 14.2 – 14.5**:
  - **D-A** (field partition): Unit 14.2 `PublicProfile` + `ProfileActivity`
    interfaces enforce shape at the type layer; Unit 14.3 + 14.4
    consume verbatim.
  - **D-B** (case-insensitive handle lookup; case-preserved URL):
    Unit 14.2 `getPublicProfileByHandle` uses `sql\`LOWER(...)\``;
    Unit 14.3 renders canonical case from `profile.githubLogin`;
    Unit 14.4 inherits via `getPublicProfileByHandle` call.
  - **D-C** (user-editable Phase-15+ deferral): no editing surface
    shipped Phase 14; Q63 candidate flagged.
  - **D-D** (privacy opt-out Phase-15+): no opt-out shipped; Q64
    candidate flagged.
  - **D-E** (curator-of-record case-sensitive): Unit 14.2
    `getCuratorOfRecordSlugs` uses `===` against YAML literal; Unit
    14.3 renders the badge.
  - **D-F** (SiteHeader integration): Unit 14.5 adds "Your profile"
    link → `/u/{login}` PUBLIC canonical; Unit 14.3 "Edit your
    profile" CTA → `/profile` EDIT mode.

### Prior ADR cross-references reaffirmed by Phase 14

- **ADR-0004** (file-first; no DB for content): Unit 14.2's
  `getCuratorOfRecordSlugs` scans `#site/content` problems —
  preserves file-system as source of truth for `editorial.primary_curator`
  attribution.
- **ADR-0005** (rating-action immutability): not exercised this
  phase (no rating-action writes); preserved.
- **ADR-0011** (i18n strategy; sibling-file convention): Unit 14.3
  `messages.public_profile.*` namespace follows the established
  sibling-file convention; FR translations idiomatic.
- **ADR-0012** (auth provider; `users.githubLogin` joining): Unit
  14.2's `getPublicProfileByHandle` + `getCuratorOfRecordSlugs` both
  exercise D-E (text-equal join); Unit 14.5's SiteHeader edit
  exercises D-D (signed-in branch UI).
- **ADR-0013** (DB choice; USER-STATE only): Unit 14.2's three
  helpers all read from USER-STATE tables (`users`, `watchlist`,
  `ratingChallenges`); preserves D-F.
- **ADR-0014** (curator review pipeline): mirrored on read side by
  ADR-0015 D-A's badge surface (curator-of-record visible publicly;
  curator = submitter blocked by ADR-0014 D-C is verifiable from the
  read side by visitors).

### Prose-shift reconciliations

**None this unit**. Phase 14 ships net-new architectural surface
(ADR-0015); no prose-shift candidates surfaced in the 14 prior ADRs
during Phase-14 work.

### No supersede markers added

ADR-0015 supersedes nothing. Status remains `accepted` across all 15
ADRs. No architectural shifts; no other ADRs touched.

## Smoke gates

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline since
  Phase 2).
- typecheck / test / build untouched since no source files modified.

## CHANGELOG entry shape

Unit 14.7 entry: confirms 21 resolved + 4 decided-as-lean + 28 open
= 53 total entries UNCHANGED from Phase-13 close (+0 net); flags Q63
+ Q64 + Q65 candidates from Phase 14 (joining Q59 + Q60 + Q61 + Q62
carryover candidates); confirms 15 ADRs all `accepted` (+1 in Phase
14: ADR-0015); no prose-shift reconciliations; ADR-0015 D-A through
D-F all unit-level exercised across Units 14.2 – 14.5; prior ADRs
0004 + 0005 + 0011 + 0012 + 0013 + 0014 cross-references reaffirmed.
