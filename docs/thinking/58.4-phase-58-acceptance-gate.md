# 58.4 — Phase 58 acceptance gate

Phase 58 closes at HEAD `b8eeb4d` (Unit 58.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-59-entry
conditions per §12.

## Phase 58 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 58.0 | Phase 58 prep | docs | `90942ab` |
| 58.1 | NEW `biorxiv.ts` + NEW `biorxiv.test.ts` (27 tests) + factory dispatch arm + 7 NEW dispatch tests + ADR-0018 D-G APPEND-D-AP | code+APPEND | `85fa298` |
| 58.2 | 18 NEW end-to-end Phase-58 tests on rationale (first 5-consumer same-slot + first 7-consumer composition) | code | `ba5b197` |
| 58.3 | Phase-58 hygiene + ADR-0018 APPEND-D-AL bioRxiv consumer item closure documentation (4-phase carryover) | docs | `b8eeb4d` |
| 58.4 | Phase 58 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-57 5-unit framework shape
verbatim.

## Seventh concrete consumer + first 5th-remarkPlugins consumer + first 5-consumer same-slot + first 7-consumer composition + regex-disjointness scales 4 → 5 + first 7-consumer cardinality + 17-phase APPEND-deferral closure cadence

- NEW `lib/markdown/extensions/biorxiv.ts` ships
  `BIORXIV_PATTERN = /\bbiorxiv:(\d{4}\.\d{2}\.\d{2}\.\d{6})(v\d+)?\b/gi`
  + `remarkLinkBiorxivIds` plugin emitting
  `https://www.biorxiv.org/content/10.1101/${id}${version}`
  canonical URLs + `BiorxivExtensionRegistry` class +
  `PHASE_58_DEFAULT_ENABLED_SURFACES = Set(["rationale"])`
  constant.
- NEW `lib/markdown/extensions/biorxiv.test.ts` ships 27
  tests (22 plugin + 5 class).
- Factory dispatch arm `case "biorxiv":` added to
  `lib/markdown/extensions/index.ts`; error message + example
  composite updated; future-extension reference updated to
  Phase 59+ / APPEND-D-AP.
- 7 NEW dispatch tests in
  `lib/markdown/extensions/index.test.ts` (Unit 58.1).
- 18 NEW end-to-end tests in `lib/markdown/index.test.ts`
  (Unit 58.2) across 3 describe blocks.
- ADR-0018 D-G APPEND adds Phase-58 EXTENDED block + **APPEND-
  D-AP** (Unit 58.1; sixteenth two-letter slot).
- **APPEND-D-AL bioRxiv preprint consumer item CLOSED at 4-
  phase carryover** (Phase 54 → 58). Standard consumer-first-
  ship gap.
- **Seventh concrete Phase-37-framework consumer** in project
  history (extends Phase-54 6-consumer record).
- **First 5th-`remarkPlugins` consumer** beyond arxiv + doi +
  pubmed + orcid.
- **Regex-disjointness-as-sole-defense discipline scales from
  4 to 5 same-slot consumers** without architectural change.
- **First 5-consumer same-slot composition** in project
  history (rationale only Phase-58 first-ship).
- **First 7-consumer composition under default dispatch** in
  project history. **New maximum-consumer-cardinality state**.
- **8th env-var single-value arm** for `MARKDOWN_EXTENSIONS`
  (adds `biorxiv`; first expansion since Phase 54 `orcid`).
- **APPEND-deferral closure cadence sustained 17 phases** —
  longest in project history (extends Phase-57 record
  16 → 17).

## Phase 57 → Phase 58 delta

| Metric | Phase 57 close | Phase 58 close | Δ |
|---|---|---|---|
| Tests | 1339 / 74 files | **1391 / 75 files** | +52 / +1 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 24 | **25** (project record extends; sixteenth two-letter slot D-AP in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 7 | **8** (+biorxiv) | **+1** |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 19 | **21** (+ `biorxiv.ts` + `biorxiv.test.ts`) | **+2** |
| **Phase-37-framework concrete consumers** | **6** | **7 (+biorxiv)** | **+1** |
| **`remarkPlugins` consumers** | **4 (arxiv + doi + pubmed + orcid)** | **5 (+biorxiv)** | **+1** |
| **Plugin-regex-extension realizations** | **6** | **6** | 0 |
| **Constructor-arg-only zero-rework expansion realizations** | **6** | **6** | 0 |
| **Schema-extension realizations** | **1** | **1** | 0 |
| **Consumer-first-ship closures in cadence** | **6** | **7 (+biorxiv)** | **+1** |
| **Maximum same-slot consumer cardinality** | **4 (all 4 surfaces; arxiv+doi+pubmed+orcid)** | **5 (rationale only; arxiv+doi+pubmed+orcid+biorxiv)** | **+1** |
| **Maximum default-dispatch consumer cardinality** | **6 (all 4 surfaces; Phase 56)** | **7 (rationale only; Phase 58)** | **+1** |
| **Surfaces saturated at maximum consumer-cardinality** | **4** | **1 (rationale; cross-surface deferred Phase 59+)** | **−3 (asymmetric state returns at higher cardinality)** |
| **Regex-disjointness same-slot scaling realizations** | **4 (Phase 48 → 50 → 54)** | **5 (+Phase 58)** | **+1 (4 consecutive scaling)** |
| **APPEND-deferral closure cadence (consecutive phases)** | 16 | **17 (Phase 42-58)** | **+1 (new record)** |
| **"Continue" override invocations past half-century threshold** | **2 (Phase 56 = 51st; Phase 57 = 52nd)** | **3 (+Phase 58 = 53rd)** | **+1** |

## Architectural firsts in Phase 58 (12 enumerated)

1. **Seventh concrete Phase-37-framework consumer** in project
   history. **First 7-consumer framework state** (extends
   Phase-54 6-consumer record). `BiorxivExtensionRegistry`.
2. **First 5th-`remarkPlugins` consumer** in project history
   (after arxiv Phase 41 + doi Phase 45 + pubmed Phase 50 +
   orcid Phase 54). **Regex-disjointness-as-sole-defense
   discipline scales from 4 to 5 same-slot consumers** without
   architectural change. **4 consecutive scaling realizations**
   (Phase 48 → 50 → 54 → 58).
3. **First 5-consumer same-slot composition** in project
   history (rationale only Phase-58 first-ship; cross-surface
   expansion deferred Phase 59+). All 10 pairs of 5 consumers
   pairwise collision-free via distinct literal prefixes
   (`arxiv:`, `doi:`, `pubmed:`/`pmid:`, `orcid:`,
   `biorxiv:`).
4. **First 7-consumer composition under default dispatch** in
   project history (rationale only under
   `wikilinks,tables,arxiv,doi,pubmed,orcid,biorxiv` Phase-58
   7-way default). **New maximum-consumer-cardinality state**.
   Mirrors Phase-54 introduction of 6-consumer cardinality at
   rationale-only.
5. **8th env-var single-value arm** for `MARKDOWN_EXTENSIONS`
   (adds `biorxiv`; first expansion of recognized-arms set
   since Phase 54 `orcid`). Mirrors the Phase-54 6-arm-to-7-
   arm expansion.
6. **Seventh consumer first-ship closure** in the cadence
   (Phase 38 wikilinks + Phase 39 tables + Phase 41 arxiv +
   Phase 45 doi + Phase 50 pubmed + Phase 54 orcid + Phase 58
   biorxiv). **Consumer-first-ship cadence stabilized at 4-
   to-5-phase gaps** across 4 successive sibling-consumer
   closures (doi 4-phase → pubmed 5-phase → orcid 4-phase →
   biorxiv 4-phase).
7. **Seventeenth prep-/APPEND-doc-level deferral closed by a
   later phase**. **APPEND-deferral closure cadence sustained
   17 phases — new longest sustained cadence in project
   history** (extends Phase-57 record 16 → 17). **First 17-
   phase APPEND-deferral closure run**.
8. **25th APPEND on ADR-0018 D-G** — extends the **first-
   ADR-D-clause-with-most-APPENDs record** from 24 → 25.
   **Sixteenth two-letter APPEND letter D-AP** (Excel-
   spreadsheet column convention sustained).
9. **First 7-consumer cardinality in the Phase-37-framework**
   in project history. 7 consumers × 4 surfaces × 3 slots =
   84 component-surface-slot positions (up from Phase-56
   maximum of 72).
10. **Maximal multi-consumer all-surfaces composition extended
    to 7-consumer + 5-consumer-same-slot cardinality** —
    defines the new upper bound for the Phase-37-framework's
    current capabilities. Pre-Phase-58 upper bound was 6-
    consumer + 4-consumer-same-slot on all surfaces (Phase-54
    + Phase-56). Phase 58 extends rationale to 7-consumer +
    5-consumer-same-slot; other 3 surfaces remain at 6-
    consumer + 4-consumer-same-slot (Phase-56 baseline
    preserved; cross-surface expansion deferred Phase 59+).
    **First state where rationale carries strictly more
    consumers than other surfaces** since Phase 56 (Phase-54
    introduced the asymmetric state; Phase-56 resolved it;
    Phase-58 re-introduces it at higher cardinality).
11. **First "framework + 7 consumers + 6 expansions + 6
    plugin-regex-extensions + 1 schema-extension + 5-consumer
    same-slot × 1 surface + 7-consumer composition × 1
    surface + 4-consumer same-slot × 4 surfaces + 6-consumer
    composition × 4 surfaces + alias syntax × 5 + dual-form
    regex × 4 + quintuple-alias × 4 + 5-consumer composition
    × 4 + inner-class disjunction × 1 + 2-evolutions on
    arxiv + all-3-slots-evolved" 22-phase cluster** in
    project history (Phase 37-58). 22-phase architectural arc.
    Plus **28th consecutive phase without new B category** —
    Phase 31-58 = first 28-phase run (extends Phase-57 record
    27 → 28). Plus **forty-ninth NON-§13 phase**. Plus
    **149th consecutive 103 kB First Load JS unit** (extends
    from 144 at Phase-57 close). Plus **23rd consecutive no-
    new-ADR phase** (Phase 36-58; extends Phase-57 record
    22 → 23).
12. **53rd "Continue" override invocation** at Unit 58.0 —
    **third invocation past the half-century threshold**
    (Phase 55 = 50th milestone; Phase 56 = 51st; Phase 57 =
    52nd; Phase 58 = 53rd). Confirms the §12-cardinal-rule
    sign-off discipline remains durable post-milestone across
    4 successive phases.

## Phase 58 → Phase 59 entry conditions

Per §12, Phase 59 entry requires **explicit human sign-off**.

**Recommended Phase 59 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **bioRxiv cross-surface expansion** to all 4 surfaces — constructor-arg value-only change in `PHASE_58_DEFAULT_ENABLED_SURFACES`; **seventh realization of "constructor-arg-only zero-rework expansion" property** (would extend Phase-56 6-realization record to 7); generalizes 7-consumer composition + 5-consumer same-slot state to all 4 surfaces; closes Phase-58 bioRxiv cross-surface deferral at 2-phase carryover (would tie Phase-52 + Phase-56 fastest cross-surface closure record at 2-phase) | 1-2 | Autonomous-tractable; mirrors Phase-44/49/52/56 cross-surface-expansion verbatim |
| 2 | **bioRxiv display-text alias syntax** `[[biorxiv:YYYY.MM.DD.NNNNNN\|display]]` — **seventh realization of Phase-46 plugin-regex-extension phase-shape pattern** (would extend Phase-55 6-realization record to 7); mirrors Phase-47 arxiv + Phase-48 doi + Phase-51 pubmed + Phase-55 orcid dual-form regex shape | 1-2 | Autonomous-tractable |
| 3 | **Cross-entity wikilinks** (APPEND-D-L item 3) — entity-type disambiguation + plugin parameterization | 2-3 | Requires APPEND-D-L item 6 plugin parameterization to land first |
| 4 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — couples with `schemaOverride` for `class` attribute on `<a>`; **second schema-extension realization** sibling to Phase 57 | 1-2 | XSS audit per attribute value |
| 5 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 6 | **Bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 7 | **Empty-alias fallback unification** across consumers (Phase-48 deferral carries) — refine arxiv/doi/pubmed/orcid/biorxiv empty-alias divergence | 1 | Smallest-scope autonomous-tractable |
| 8 | **OSF preprint consumer** — eighth concrete consumer alternative to bioRxiv | 2-3 | DOI overlap concern |
| 9 | **`<caption>` element** (APPEND-D-Q item 4) — second schema-extension realization on `schemaOverrides` slot kind; second remaining D-Q item | 1-2 | XSS audit minimal (text-only element) |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Most defensible autonomous-tractable next thread**: Rank 1
(bioRxiv cross-surface expansion). Mirrors Phase-56 ORCID
cross-surface expansion verbatim with `biorxiv` substituted.
Would create THREE 6-realization framework patterns coexisting
plus elevate constructor-arg-only-expansion to 7-realization
(first 7-realization for that pattern). OR rank 2 (bioRxiv
alias syntax) would create the **second "immediate-successor
same-thread-direction phase boundary"** equivalent on biorxiv
(after Phase-50 → 51 pubmed first-ship → alias; Phase-54 → 55
orcid first-ship → alias) at 1-phase carryover.

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1391 / 75 files** (+52 / +1 vs Phase 57
  close).
- `pnpm audit-content` — 0 errors / 6 warnings (54
  consecutive phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (149 consecutive units
  at Unit 58.4 ship).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 59+ deferrals)

NOT bioRxiv cross-surface expansion (Phase 59+ rank 1);
NOT bioRxiv alias syntax (Phase 59+ rank 2);
NOT cross-entity wikilinks (Phase 59+ rank 3);
NOT `<a class="wikilink">` styling (Phase 59+ rank 4);
NOT 404 handling (Phase 59+ rank 5);
NOT bare arxiv / DOI / PubMed / ORCID / bioRxiv IDs without
prefix (Phase 59+ rank 6);
NOT empty-alias fallback unification (Phase 59+ rank 7);
NOT OSF preprint consumer (Phase 59+ rank 8);
NOT `<caption>` element (Phase 59+ rank 9);
NOT 6th-or-later `remarkPlugins` consumer beyond arxiv + doi +
pubmed + orcid + biorxiv (Phase 59+);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 59+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 59+);
NOT legacy numeric-only bioRxiv IDs (Phase 59+);
NOT dx.doi.org legacy host (Phase 59+);
NOT stricter trailing-lookahead (Phase 59+);
NOT paper-card hover-preview (Phase 59+);
NOT plugin parameterization for wikilink-href-builder (Phase
59+);
NOT auto-trim of alias display whitespace (Phase 59+);
NOT surface-specific table schemas (Phase 59+; second-remaining
D-Q item);
NOT a future plugin that EMITS `colSpan`/`rowSpan`/`scope`
(Phase 59+);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 59+);
NOT `@mention` (Phase 59+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-57 deferrals carry forward.

## Boundary statement

**Phase 58 CLOSED at HEAD `<this-unit>` after 5 numbered units
(58.0 prep + 58.1 NEW `biorxiv.ts` + NEW `biorxiv.test.ts` (27
tests) + factory dispatch arm + 7 NEW dispatch tests + APPEND-
D-AP + 58.2 18 NEW end-to-end tests + 58.3 hygiene + 58.4
gate). Seventh concrete Phase-37-framework consumer in
project history: bioRxiv preprint auto-link via NEW
`BiorxivExtensionRegistry` + `MARKDOWN_EXTENSIONS=biorxiv`
env-var dispatch arm. First 7-consumer framework state
(extends Phase-54 6-consumer record). First 5th-`remarkPlugins`
consumer beyond arxiv + doi + pubmed + orcid. Regex-
disjointness-as-sole-defense discipline scales from 4 to 5
same-slot consumers without architectural change — all 10
pairs of 5 consumers pairwise collision-free via distinct
literal prefixes (`arxiv:`, `doi:`, `pubmed:`/`pmid:`,
`orcid:`, `biorxiv:`). 4 consecutive scaling realizations
(Phase 48 → 50 → 54 → 58). First 5-consumer same-slot
composition in project history under
`MARKDOWN_EXTENSIONS=arxiv,doi,pubmed,orcid,biorxiv` on
rationale (cross-surface expansion deferred Phase 59+). First
7-consumer composition under default dispatch in project
history under
`MARKDOWN_EXTENSIONS=wikilinks,tables,arxiv,doi,pubmed,orcid,biorxiv`
Phase-58 7-way default on rationale — new maximum-consumer-
cardinality state. 8th env-var single-value arm for
`MARKDOWN_EXTENSIONS` (adds `biorxiv`; first expansion of
recognized-arms set since Phase 54 `orcid`). Closes APPEND-
D-AL bioRxiv preprint consumer item at 4-phase carryover
(Phase 54 → 58). Standard consumer-first-ship gap. Consumer-
first-ship cadence stabilized at 4-to-5-phase gaps across 4
successive sibling-consumer closures (doi 4-phase → pubmed
5-phase → orcid 4-phase → biorxiv 4-phase). Seventh consumer
first-ship closure in the cadence. APPEND-deferral closure
cadence sustained 17 phases — new longest sustained cadence
in project history (extends Phase-57 record 16 → 17). First
17-phase APPEND-deferral closure run. Maximal multi-consumer
all-surfaces composition extended to 7-consumer + 5-consumer-
same-slot cardinality — defines the new upper bound. 25th
APPEND on ADR-0018 D-G (project record extends 24 → 25).
Sixteenth two-letter APPEND letter D-AP. Forty-ninth NON-§13
phase. First 7-consumer cardinality in the Phase-37-framework
(84 component-surface-slot positions; up from 72). First
"framework + 7 consumers + 6 expansions + 6 plugin-regex-
extensions + 1 schema-extension + 5-consumer same-slot × 1
surface + 7-consumer composition × 1 surface + 4-consumer
same-slot × 4 surfaces + 6-consumer composition × 4 surfaces
+ alias syntax × 5 + dual-form regex × 4 + quintuple-alias ×
4 + 5-consumer composition × 4 + inner-class disjunction × 1
+ 2-evolutions on arxiv + all-3-slots-evolved" 22-phase
cluster in project history (Phase 37-58). 28th consecutive
phase without new B category — first 28-phase run (extends
Phase-57 record 27 → 28). 23rd consecutive no-new-ADR phase
(Phase 36-58; extends Phase-57 record 22 → 23). 149th
consecutive 103 kB First Load JS unit (extends from 144 at
Phase-57 close). 53rd "Continue" override invocation — third
invocation past the half-century threshold (Phase 55 = 50th;
Phase 56 = 51st; Phase 57 = 52nd; Phase 58 = 53rd). XSS line
of defense preserved (bioRxiv plugin emits absolute https://
URLs only; no new tag-names; no event-handler attribute
paths). 1391 tests across 75 vitest files (+52/+1 vs Phase
57). 24 ADRs UNCHANGED. 7 DB tables UNCHANGED. 9 migrations
UNCHANGED. 14 env vars UNCHANGED. 168 i18n keys per locale
UNCHANGED. First Load JS 103 kB UNCHANGED. Middleware 160 kB
UNCHANGED. OPEN_QUESTIONS top-level Q-count 66 UNCHANGED.
Phase-37+ candidate count 8 UNCHANGED. `MARKDOWN_EXTENSIONS`
recognized single-value arms 7 → 8 (+`biorxiv`). 2 new files
(`biorxiv.ts` + `biorxiv.test.ts`). Zero new ADRs / migrations
/ i18n keys / operational gates / runtime deps. Phase 59
entry requires explicit human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 57 → 58 delta complete.
- [x] Phase 59 entry conditions documented (10 ranked
      candidates).
- [x] Smoke gates pass.
- [x] STOP at Phase 59 boundary per §12.
