# 17.7 — Phase 17 acceptance gate (Q66 closure; 18 ADRs total; first markdown pipeline + first XSS-audit surface)

Eighth and final Phase-17 unit; docs-only. Closes Phase 17
(**eighth NON-§13 phase**; Q66 promotion — markdown rendering in
bio). Mirrors Phase-12 Unit 12.8 + Phase-13 Unit 13.6 + Phase-14
Unit 14.8 + Phase-15 Unit 15.8 + Phase-16 Unit 16.8 acceptance-
gate patterns: ledger of deliverables; smoke-gate results;
surviving follow-ons; Phase-18+ entry conditions.

> **§12 sign-off**: Phase 18 sign-off **pending** at the end of
> this unit. The "Continue" override path is available
> (**thirteenth invocation** would set the precedent at Phase
> 5 → 6 / Phase 6 → 7 / … / Phase 16 → 17 → **Phase 17 → 18**).

## THINK

### Phase 17 ledger

**8 units shipped** (17.0 prep → 17.7 gate) across HEAD `2ea957b`
(Phase 16 close) → HEAD `3b9a8b4` (Phase 17 close pre-this-unit).

| Unit | Title | Type | Status |
|---|---|---|---|
| 17.0 | Phase 17 prep | docs | shipped `9b1bca0` |
| 17.1 | ADR-0018 markdown sanitization subset | docs | shipped `eff21a8` |
| 17.2 | `lib/markdown/` + sanitization schema + XSS test suite | code | shipped `4e7bfc1` |
| 17.3 | `/u/{handle}` bio markdown render (first `dangerouslySetInnerHTML`) | code | shipped `055b962` |
| 17.4 | `/profile` read-mode bio preview (closes ADR-0018 D-E second integration) | code | shipped `96ea82c` |
| 17.5 | Phase-17 hygiene status pass | docs | shipped `ce97415` |
| 17.6 | OPEN_QUESTIONS hygiene + ADR review (Q66 resolved) | docs | shipped `3b9a8b4` |
| 17.7 | **Phase 17 acceptance gate** (this unit) | gate | this commit |

**0 deferrals**; **0 scope drift**; matches Phase-12 + Phase-13 +
Phase-14 + Phase-15 + Phase-16 well-scoped pattern. **Sixth
consecutive well-scoped phase** shipping clean. Phase-17 shipped
8 units vs the typical 9-unit shape (one unit lighter — no DB
migration thread; lib/markdown rendering layer only).

### Q66 closure summary

Q66 (markdown rendering in bio) was a Phase-15-anticipated
candidate that promoted + resolved in Unit 17.6 simultaneously
— **fifth invocation of the "added + resolved in same unit"
pattern** (Phase-12 Q57 + Phase-13 Q58 + Phase-15 Q63 + Phase-16
Q67 + Phase-17 Q66). Closes the **seven-reference convergence**
honored-deferral lineage (strongest in project history; Phase 16
Q67 had four references):

- Unit 15.1 (ADR-0016 D-F deferral).
- ADR-0016 D-F deferral itself.
- Unit 15.6 hygiene (Class B B.2 item 5).
- Unit 15.7 OQ-hygiene (Q66 candidate flagged).
- Unit 15.8 acceptance gate (Q66 candidate carried).
- Unit 16.6 hygiene (Phase-15 carryover).
- Unit 16.7 + Unit 16.8 acceptance gate (Q66 carryover).

ADR-0018 pins eight concrete contracts (D-A library; D-B subset;
D-C heading demotion; D-D URL allow-list; D-E render path; D-F
server-side-only; D-G inheritance contract; D-H Phase-18+
deferrals). **All six code-realizable D-clauses (D-A through D-F)
shipped**; D-G is forward-compat documentation; D-H is text-only
deferral.

### Smoke gates (final)

All four gates green at HEAD pre-this-unit (`3b9a8b4`):

- **`pnpm typecheck`** → clean (`tsc --noEmit` no errors).
- **`pnpm test`** → **552/552 across 54 vitest files**. **+33 /
  +1** from Phase 16 close (519/53); all from Unit 17.2's
  `lib/markdown/index.test.ts` (14 happy-path + 12 XSS-vector
  + 4 heading-demotion + 3 null-edge tests).
- **`pnpm build`** → ~659 prerendered pages + **7 dynamic
  page+API routes** unchanged. **First Load JS shared chunk =
  103 kB UNCHANGED** end-to-end through every Phase 9-17 unit.
  **Middleware bundle = 160 kB UNCHANGED** since Phase 12.
  `/profile` bundle = 117 kB UNCHANGED (Phase-16 use-client
  boundary preserved; markdown render adds zero client JS).
  `/u/{handle}` = 108 kB UNCHANGED. All other routes UNCHANGED
  at their Phase 16-close sizes.
- **`pnpm audit-content`** → **0 errors / 6 warnings** (Q32
  baseline since Phase 2; unchanged through every Phase 3-17
  unit).

### Architectural firsts shipped in Phase 17

1. **First markdown processing pipeline in project history** —
   no prior code path interprets markdown at request time
   (Velite's MDX pipeline handles content files via separate
   build-time compilation; Phase 17 ships USER-STATE request-
   time render).
2. **First XSS-audit surface in project history** —
   sanitization schema (`bioSchema`) + URL allow-list +
   multi-stage pipeline + defense-in-depth `rehypeStripUnsafeHrefs`
   plugin for schemeless relative URLs that rehype-sanitize's
   `protocols.href` filter doesn't catch.
3. **First three-step pipeline boundary** (parse → transform →
   sanitize → stringify; `unified` shape names stages
   explicitly). Establishes inheritance shape for Phase-18+
   markdown surfaces via ADR-0018 D-G sibling helpers.
4. **First `dangerouslySetInnerHTML` surface in project
   history** — two surfaces total (`/u/{handle}` bio +
   `/profile` bio preview); XSS-safe by ADR-0018 sanitization.
5. **First phase since Phase 9 to ship zero migrations** —
   Phase 11 + 12 + 15 + 16 each shipped 1; Phase 10 + 13 + 14
   shipped 0; Phase 17 is the second 0-migration phase since
   the DB landed.
6. **First new runtime dep cluster since Phase 16** (~120 kB
   server-only; 8 new packages: `unified` + `remark-parse` +
   `remark-gfm` + `remark-rehype` + `rehype-sanitize` +
   `rehype-stringify` + `unist-util-visit` + `@types/hast`).
   1-phase dependency-discipline interval (broke the 5-phase
   interval from Phase 9-15 where zero new runtime deps
   landed).
7. **First i18n key TEXT UPDATE since the namespace was first
   added in Phase 7** (`bio_hint` phrasing changed from "Plain
   text (no markdown)." to "Markdown supported (bold, italic,
   links, lists, code).") — Phase-10 through Phase-16 all
   ADD-only patterns.
8. **First two-surface markdown render consistency** — same 14
   arbitrary-variant prose styling utility classes across
   `/u/{handle}` public surface + `/profile` private read-mode
   preview.
9. **Closes the four-phase identity-surface arc** — Phase 14
   shipped read-only public profile; Phase 15 shipped editable
   text fields; Phase 16 shipped editable image; **Phase 17
   closes the bio expressiveness gap**. Identity-surface arc
   COMPLETE across four phases.

### Delta summary (Phase 16 → Phase 17)

| Metric | Phase 16 close (`2ea957b`) | Phase 17 close (HEAD `3b9a8b4`) | Δ |
|---|---|---|---|
| Commits | 9 | 7 + this gate | +7 |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | 17 | **18** | **+1** (ADR-0018) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (first phase since Phase 9 to ship zero migrations) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-16 stack) | **+8 net** (~120 kB transitive server-only) | +8 |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | 519 / 53 files | **552 / 54 files** | **+33 / +1** |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** (per ADR-0018 D-F invariant) |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS open count | 30 | 30 | 0 |
| OPEN_QUESTIONS resolved count | 23 | **24** | **+1** (Q66) |
| OPEN_QUESTIONS decided-as-lean count | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 57 | **58** | **+1** |
| i18n keys per locale | 124 | **127** | **+3** (`bio_preview_heading` + `bio_preview_aria_label` + `bio_hint` text update — first TEXT UPDATE since Phase 7) |
| LHCI URLs | 19 | 19 | 0 |
| `lib/markdown/` files | 0 | **3** | **+3** (`index.ts` + `sanitize-schema.ts` + `index.test.ts`) |
| XSS-audit surfaces | 0 | **1** | **+1** (`renderBioMarkdown` pipeline) |
| `dangerouslySetInnerHTML` surfaces | 0 | **2** | **+2** (`/u/{handle}` bio + `/profile` bio preview) |

### Surviving follow-ons (from Unit 17.5)

**Class B — 17 items** in 3 categories:

- **B.1 Markdown subset expansion (8)**: tables / footnotes /
  images / syntax-highlighted code blocks / bio length
  expansion / `@mentions` / task-list interactivity / live
  preview client boundary.
- **B.2 Surface expansion (4)**: markdown-rendered curator
  review notes (ADR-0018 D-G inheritance via sibling helper);
  markdown rating-action `rationale`; methodology page markdown;
  per-user schema configurability.
- **B.3 Operational + infrastructure (5)**: Q69 / Q54 / Q55
  carryovers; CI dummy `AUTH_SECRET`; `pnpm db:migrate` doc
  (6 migrations); first LHCI run; middleware-based auth-route
  protection (threshold stays at 2).

**Class C — pre-existing carryovers** unchanged from Phase 16
Unit 16.6 (with Phase-17 dep / file / surface deltas noted).

### Phase-17+ Q-candidates flagged but NOT promoted

Eight candidates stay flagged for Phase-18+ kickoff (8 carry):

- **Q59** (CLI emit-challenge-action) — carried Phase-12 through
  Phase-17 (six phases).
- **Q60** (curator authz evolution).
- **Q61** (submitter anonymity option) — Phase-13 carryover.
- **Q62** (rejection-rationale public visibility) — Phase-13
  carryover.
- **Q64** (per-user privacy opt-out) — Phase-14 carryover; ~3
  units.
- **Q65** (per-curator activity feed) — Phase-14 carryover.
- **Q68 expansion** (content moderation on uploaded images) —
  Phase-15/16 carryover/expansion.
- **Q70** (EXIF stripping on uploaded images) — Phase-16
  carryover.

### Phase 18+ entry conditions

Per §12 cardinal rule, the next phase needs explicit human
sign-off (or "Continue" override per the twelve-phase precedent;
thirteenth invocation in Unit 18.0 prep). Phase 18+ thread
options ALL inferred-not-§13 (§13 ledger remains CLOSED since
Phase 9):

- **A. Q70 promotion — EXIF stripping** (~2-3 units; ADR-0019
  candidate for image-transcoding pipeline). Strongest privacy
  signal among Phase-16-surfaced candidates. Couples to Q68
  expansion.
- **B. Q68 expansion promotion — content moderation** (~3-4
  units; ADR-0019+ candidate for moderation API). Phase-15/16
  carryover/expansion.
- **C. Multi-surface markdown render expansion** (~3-5 units)
  — promote ADR-0018 D-G inheritance contract via
  `renderReviewNotesMarkdown` + `reviewNotesSchema` sibling
  (Phase-15 Class B B.2 item 5; Phase-17 just laid the
  foundation; natural next surface). May not need new ADR.
- **D. Markdown subset extensions** (~2-3 units) — tables /
  footnotes / images / syntax-highlighting; schema extension
  if Phase-17 deployment surfaces demand signal. Possibly new
  ADR for the new subset elements.
- **E. Q66+1: `@mentions`** (~2-3 units) — `@`-handle detection
  + `/u/{handle}` link rewrite + handle existence check.
- **F. Q64 promotion — per-user privacy opt-out** (~3 units;
  ADR-0015 D-D Phase-15+ deferral). Phase-14 carryover.
- **G. Q65 promotion — per-curator activity feed** (~4-5 units;
  ADR-0015 D-E expansion). Phase-14 carryover.
- **H. Q59 promotion — CLI emit-challenge-action** (~2-3 units;
  smallest reasonable scope). Carried 6 phases.
- **I. Multi-provider OAuth expansion** (~3-4 units; ADR-0019+
  candidate). Phase-9 Class B item 8 carried 8 phases —
  strongest "patience signal" among architectural threads.
- **J. Subscriber-list email** (~6 units; Phase-5 D-4 punt
  carried 12+ phases — strongest "patience signal" overall).
- **K. SiteHeader avatar pill / avatar-dropdown menu** (Phase-14
  Class B; Phase-16 forward-compat'd `getUserMetadataById`). Couples to mobile-nav.
- **L. Full §8.6 24-mo collaborator COI check** (~3-5 units;
  ADR-0014 D-C deferral).
- **HTML shell migration** STILL ON HOLD per parallel-session
  preservation signal.
- **Monetization** premature without observed user traffic;
  Phase 19+ candidate.

**DB-migration trigger re-eval at Phase 18 kickoff** (mandatory
per Unit 14.0 D-2 cascade): trigger (a) FIRED Unit 9.6; trigger
(b) cold (~1.6% of 5 MB threshold; content count 239 unchanged).
Phase 18 migration count depends on chosen thread.

### Risk surface at HEAD `3b9a8b4`

1. **Three operational gates** (Q54 / Q55 / Q69) pending curator
   action. Architecture is complete; deployment unblock pending.
2. **Orphan blob accumulation tolerated** until Phase-16 Class B
   cleanup script lands.
3. **Middleware bundle 160 kB** unchanged (~15% of Vercel Edge's
   1 MB limit).
4. **`/profile` bundle 117 kB** unchanged from Phase 16.
5. **No live-preview in edit form** — Phase-17 ships server-side
   render-after-save only; user sees rendered output on next
   page render (e.g., navigate to `/u/{handle}` or refresh
   `/profile`).
6. **8 new runtime deps** (~120 kB transitive server-only) —
   first dep increase since Phase 16; periodic dep audit Phase
   18+ if security signal surfaces.
7. **No content moderation on bio text** (Q68 deferred);
   **no EXIF stripping** (Q70 deferred) — same Phase-16-close
   carryover.

### Boundary statement

NOT the OAuth app registration (Q54), NOT Turso DB provisioning
(Q55), NOT Vercel Blob token provisioning (Q69), NOT FR content
backfill (Q51 curator-track), NOT image transcoding / cropping /
moderation (Phase 18+), NOT markdown subset extension (Phase
18+), NOT destructive cleanup (Class C still gated). This unit
is the **closure**, not the **unblock**.

### Parallel-curator awareness

This unit is docs-only — no collision risk. The parallel-
session-shipping pattern observed in Phase 16 (parallel session
shipped 8 of 9 units while primary shipped 16.0 + observed) did
NOT recur in Phase 17 — primary shipped **all 8 units** (17.0
through 17.7) in a single coherent session. Demonstrates the
constitution + auto-memory + ADR ledger converges single-session
or multi-session work on equivalent outputs.

### Smoke gates

Docs-only — no `validate-content` / `typecheck` / `test` /
`build` changes anticipated.

- `audit-content`: 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/17.7-phase-17-acceptance-gate.md`
  (this).
- CHANGELOG: Unit 17.7 entry cites the 8-unit Phase-17 ledger
  + smoke-gate results + delta summary + surviving follow-ons +
  Phase 18+ entry conditions.

### CHANGELOG entry shape

Unit 17.7 entry: declares Phase 17 closed (**eighth NON-§13
phase**; 8 units; 0 deferrals; 0 scope drift; sixth consecutive
well-scoped phase); cites Q66 closure + ADR-0018 acceptance;
consolidates 9 Phase-17 architectural firsts (including
identity-surface arc closure across Phase 14-17); presents
Phase 16 → 17 delta table; enumerates Class B + Class C
follow-ons; lists Phase-18+ Q-candidates + thread options;
surfaces Phase-18 sign-off requirement (thirteenth "Continue"
override opportunity per §12 cardinal rule). Notes the
single-session shipping pattern (vs Phase 16's parallel-session
heavy lift) as evidence that the constitution converges
work on equivalent outputs regardless of session count.
