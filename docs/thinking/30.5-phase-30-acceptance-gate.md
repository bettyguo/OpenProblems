# 30.5 — Phase 30 acceptance gate (subscriber-list email foundation realized via ADR-0021; 21 ADRs; closes Phase-5 D-4 at 22+ phase carryover — single longest patience-signal closure in project history; 6-unit shape matches Phase 19 + 28)

Sixth and final Phase-30 unit; docs-only. Closes Phase 30
(**twenty-first NON-§13 phase**; subscriber-list email foundation
realized via new ADR-0021 + Resend provider integration in
`lib/email/` + new `subscriber` DB table + 3 API routes + subscribe
form on `/digest` + Q75 operational gate + Q76 architectural
candidate). Mirrors Phase-12 through Phase-29 acceptance-gate
patterns. **Second 6-unit new-ADR phase since Phase 19** (Phase 19
+ 28 + 30); confirms the **6-unit new-ADR rhythm** as a stable
shape when migration + new module + multi-route surface land in
one phase.

> **§12 sign-off**: Phase 31 sign-off **pending** at end of unit.
> "Continue" override path available (**twenty-sixth invocation**
> opportunity).

## THINK

### Phase 30 ledger

**6 units shipped** (30.0 prep → 30.5 gate). **0 deferrals**;
**0 scope drift**. **Nineteenth consecutive well-scoped phase**.
**Resumes the 6-unit new-ADR phase shape** after Phase 29's 5-unit
no-new-ADR shape; **third 6-unit phase in project history** (Phase
19 + 28 + 30 = all 3 new-ADR phases with migration / module-
introduction scope).

| Unit | Title | Type | Status |
|---|---|---|---|
| 30.0 | Phase 30 prep | docs | shipped `e05a843` |
| 30.1 | ADR-0021 subscriber-list email | docs | shipped `0484ec5` |
| 30.2 | `lib/email/` + Resend + 2 templates + 19 tests | code | shipped `f2f5d4d` |
| 30.3 | subscriber schema + migration + 3 routes + form + i18n + 21 tests | code | shipped `ad390f3` |
| 30.4 | Phase-30 hygiene + Q75 + Q76 + ADR-0013 cross-ref | docs | shipped `f2341af` |
| 30.5 | **Phase 30 acceptance gate** (this unit) | gate | this commit |

### Phase-30 closure summary

**Phase-5 D-4 punt CLOSED at 22+ phase carryover** — **single
longest patience-signal closure in project history** (prior
longest: Phase 28's Phase-9 Class B item 8 at 17 phases). Closed
via:

- **ADR-0021** (Unit 30.1): 8 D-clauses pin Resend provider +
  `subscriber` data model + per-domain subscription scope +
  double opt-in 24h-expiry verification + single-click soft-
  delete unsubscribe + verify+welcome templates Phase 30 only +
  `EMAIL_FROM` sender identity + Phase 31+ deferrals.
- **`lib/email/index.ts`** (Unit 30.2): Resend SDK wrapper with
  lazy-init client + structured failure cases + graceful-
  degradation per ADR-0021 D-G.
- **`lib/email/templates/verification.tsx` + `welcome.tsx`** (Unit
  30.2; refactored to plain string builders Unit 30.3): 2
  transactional email templates with HTML-escape defense-in-depth.
- **`subscriber` table** in `lib/db/schema.ts` (Unit 30.3): 11
  columns + 3 indexes; migration `0006_subscriber` breaks 13-
  phase 0-migration streak.
- **`lib/subscribers/index.ts`** (Unit 30.3): pure + DB helpers
  with `crypto.timingSafeEqual` token comparison; first use of
  constant-time comparison in project history.
- **3 new API routes** under `app/api/v1/subscribe/*` (Unit 30.3):
  POST subscribe + GET verify + GET unsubscribe with inline HTML
  confirmation pages.
- **`<SubscribeForm>`** under `app/[locale]/digest/_components/`
  (Unit 30.3): first `"use client"` component since Phase 16 and
  first `_components/` private-directory in project history.
- **`messages/{en,fr}.json`** (Unit 30.3): 11 new `email`
  namespace keys per locale.
- **`.env.example`** (Unit 30.3): Phase-30 Subscriber-list email
  block adds `RESEND_API_KEY` + `EMAIL_FROM`.
- **OPEN_QUESTIONS Q75 + Q76** (Unit 30.4): operational + architectural
  Q-pair coupled to ADR-0021 (second consecutive new-ADR phase
  with this shape after Phase 28's Q73 + Q74).
- **ADR-0013 D-F APPEND note** (Unit 30.4): second-new-DB-table-
  since-Phase-11 cross-ref.

### Final smoke gates (all green at HEAD pre-this-unit `f2341af`)

- **`pnpm typecheck`** clean.
- **`pnpm test`** → **667/667 across 60 vitest files** (+40 / +4
  files Δ from Phase 29 close 627/56; Unit 30.2 added +19/+3 +
  Unit 30.3 added +21/+1).
- **`pnpm build`** → ~659 prerendered pages + **11 dynamic
  page+API routes** (was 8; +3 = `/api/v1/subscribe` + `/verify/[token]`
  + `/unsubscribe/[token]`). **First Load JS shared chunk = 103 kB
  UNCHANGED** end-to-end through every Phase 9-30 unit (39
  consecutive units; Phase 9-30 invariant preserved). **Middleware
  bundle = 160 kB UNCHANGED** since Phase 12. All page bundles
  UNCHANGED.
- **`pnpm audit-content`** → 0 errors / 6 warnings (Q32 baseline
  since Phase 2; unchanged through every Phase 3-30 unit).
- **`pnpm lint`** clean.

### 16+ Phase-30 architectural firsts (cumulative)

1. **First new ADR since Phase 28** = 1-phase ADR-cycle interval
   (tightest gap in project history with Phase 17→18 + 18→19).
2. **ADR-0021 candidate slot claimed at 2-phase age**.
3. **First `lib/email/` infrastructure** in project history.
4. **First Resend SDK integration** (`resend@6.12.3`).
5. **First TSX server component refactored to plain template-
   literal string builder** (Next.js App Router `react-dom/server`
   import block).
6. **First `vi.hoisted` + class-shaped `vi.mock` pattern**.
7. **First test-only export hook** (`__resetResendClientForTests`).
8. **First `lib/subscribers/` infrastructure**.
9. **First use of `crypto.timingSafeEqual`** (constant-time token
   comparison).
10. **First `index` + `uniqueIndex` Drizzle imports** (explicit
    indexes vs prior column-level UNIQUE / composite primaryKey).
11. **First multi-route API surface in a single unit** since
    Phase 9 (3 routes: subscribe + verify + unsubscribe).
12. **First HTML-string-returning GET routes** (verify +
    unsubscribe).
13. **First `"use client"` component since Phase 16** (14-phase
    gap; `<SubscribeForm>`).
14. **First `_components/` private-directory inside an app-router
    page directory**.
15. **Phase-5 D-4 punt closed at 22+ phase carryover** — **single
    longest patience-signal closure in project history**.
16. **Q75 + Q76 promotion pair** mirrors Phase-28's Q73 + Q74
    shape; **second consecutive new-ADR phase to surface an
    operational-plus-architectural Q pair coupled to a single
    ADR** — establishes the ADR-cycle-coupled Q-promotion-pair
    shape as a recurring pattern.
17. **First non-zero-migration phase since Phase 16** = **14-phase
    gap**.
18. **First new DB table since Phase 11** `ratingChallenge` =
    **19-phase gap**.
19. **Breaks 13-phase 0-migration streak** (Phase 17-29).
20. **First new runtime dep since Phase 19** `sharp` = **11-phase
    gap** (`resend@6.12.3`).
21. **First multi-file vitest delta in a single phase** since
    Phase 12 (+4 files = `lib/email/index.test.ts` +
    `templates/verification.test.tsx` + `templates/welcome.test.tsx`
    + `lib/subscribers/index.test.ts`).
22. **Second env-var-count change in 2 phases** (Phase 28: 7 → 9;
    Phase 30: 9 → 11; **first such cluster since Phase 16-17**).
23. **First new i18n namespace since Phase 26** `public_challenge_detail` =
    **4-phase gap** (Phase 30 `email` namespace).
24. **Second APPEND note on ADR-0013 D-F** (Phase 11 + Phase 30;
    first D-F clause with 2 APPEND notes).
25. **First `lib/<table>/` module introduction since Phase 11**
    `lib/rating-challenges/` = **19-phase gap** (`lib/subscribers/`).
26. **Phase 30 = third 6-unit new-ADR phase** (Phase 19 + 28 + 30);
    **stable rhythm confirmed** for new-ADR + migration + new
    module phases.

### Delta summary (Phase 29 → Phase 30)

- +6 commits (5 + this gate).
- **+1 ADR** (20 → 21; ADR-0021 subscriber-list email).
- **+1 migration** (6 → 7; `0006_subscriber`; breaks 13-phase
  0-migration streak).
- **+1 DB table** (6 → 7; `subscriber`; first new DB table since
  Phase 11 = 19-phase gap).
- **+1 runtime dep** (`resend@6.12.3`; first since Phase 19 = 11-
  phase gap).
- **+2 env vars** (9 → 11; `RESEND_API_KEY` + `EMAIL_FROM`).
- **+11 i18n keys per locale** (142 → 153; new `email` namespace).
- **+40 / +4 file tests** (627/56 → 667/60).
- **+3 dynamic API routes** (8 → 11; subscribe + verify +
  unsubscribe).
- **+3 source files in `lib/email/`** + **+3 test files**.
- **+1 source file in `lib/subscribers/`** + **+1 test file**.
- **+1 `_components/` directory** under `app/[locale]/digest/`.
- **0 First Load JS Δ** (103 kB UNCHANGED end-to-end Phase 9-30).
- **0 middleware Δ** (160 kB UNCHANGED since Phase 12).
- **0 page-bundle Δ**.
- **+2 Q opens** (61 → 63; Q75 + Q76).
- **+1 Class B category** (B.15 Phase-30 subscriber-list newly
  surfaced with 5 items).
- **+1 patience-signal-closing phase** (Phase-5 D-4 closed at 22+
  phases — single longest closure in project history).

### Surviving follow-ons (Class B; ~5 items across 14 categories)

**B.15 Phase-30 NEW 5 items surfaced**: weekly digest scheduler
(Phase 31+) + digest-send template (Phase 31+) + DB-helper
integration tests (Phase-31+ B.20) + stale-token cleanup job +
subscribe-endpoint rate limiting.

Carried unchanged from Phase 29 close: B.1 (3) + B.2 (8) + B.3
(3 + Q72 family) + B.4 (3) + B.5 (2) + B.8 (1) + B.9 (8;
operational gates + CI dummy AUTH_SECRET + pnpm db:migrate doc +
LHCI + 3 per-script docs) + B.10 (2) + B.11 (1; now **153
per-locale**) + B.13 (4; Phase-28) + B.14 (1; wikilink).

### Phase-31+ Q-candidates flagged (11 total)

7 carried from Phase 27 (Q60 / Q61 / Q62 / Q64 / Q65 / Q68
expansion / Q72) + 2 carried from Phase 28 (Q73 + Q74) + **2 NEW
Phase 30** (Q75 + Q76).

### Phase-31+ ADR slot reservations

**ADR-0022 candidate slot opens** (Phase 30 claimed ADR-0021).
Strongest remaining candidates at Phase 31 entry:

- **Weekly digest scheduler ADR** (B.15 item 1; Phase 31+
  natural-follow-on to Phase-30 foundation; couples to Vercel
  Cron + ADR-0021 D-F deferred digest-send template).
- **Q68 expansion content moderation** on uploaded images
  (ADR-0019 D-F row 1; 10+ phase carryover).
- **Markdown evolution ADR** coupling Phase-17 D-H + Q72 family
  + Phase-29 B.14 wikilink into one architectural scope.
- **Account-merge UI** (ADR-0020 D-E candidate; 3-phase age).
- **Third+ OAuth provider expansion** (ADR-0020 D-B amendment;
  3-phase age).
- **Q64 per-user privacy opt-out** (ADR-0015 D-D; 14-phase
  carryover).

### Risk surface at Phase 30 close

- **5 operational gates pending** at Phase 30 close: Q54 (GitHub
  OAuth registration) + Q55 (Turso production DB provisioning) +
  Q69 (Vercel Blob token) + Q73 (Google OAuth registration) +
  **Q75 (NEW; Resend account + domain DKIM/SPF/DMARC)**.
- **Subscribe form returns `email.send_unavailable` until Q75
  closed** — Phase-30 foundation works architecturally but no
  emails are sent until production Resend setup completes.
- **Weekly digest send is NOT shipped Phase 30** (deferred to
  Phase 31+ B.15 item 1 scheduler infrastructure). Subscribers
  who opt in Phase 30 receive welcome email but no digest emails
  until Phase 31+.
- **3,000/month Resend free-tier ceiling** bounds future
  subscriber-list size at ~750 (4 weekly sends × 750 = 3,000);
  Q75 operational follow-on includes tier-monitoring sub-step.
- **`resend@6.12.3` exact-version pin** — first new runtime dep
  since Phase 19; carries forward Phase 31+.
- **All other Phase-29 risk-surface items unchanged**.

### Boundary statement

NOT the new ADR-0022 (slot stays open); NOT scheduler / weekly
digest send (B.15 Phase 31+); NOT per-user-account subscriptions
(Q76 Phase 31+); NOT per-problem subscriptions (B.15 Phase 31+);
NOT React Email / MJML templates (Phase 31+ if template count
grows); NOT FR-translated email content (Phase 31+ curator-track);
NOT rate-limiting the subscribe endpoint (Phase 31+ if spam);
NOT operational Resend account / domain provisioning (Q75
operational); NOT stale-token cleanup job (B.15 Phase 31+); NOT
bulk-unsubscribe / "unsubscribe from all" (Phase 31+); NOT
admin/curator UI for subscriber management (Phase 31+); NOT
content moderation Q68 expansion (deferred); NOT account-merge UI
(Phase-28 follow-on; deferred). This phase is the **architectural
foundation** for transactional email delivery — the surface is
ready, the operational provisioning is the next step (Q75) and
the user-facing send loop awaits scheduler infrastructure Phase
31+.

### Parallel-curator awareness

Phase 30 shipped as a **single-session phase** — primary shipped
all 6 units 30.0 → 30.5 in one coherent session. **Fourteen
consecutive single-session phases** (Phase 17-30). Phase 16
remains the lone parallel-session phase. Working tree clean at
each unit boundary; no parallel-session activity observed during
Phase 30 work.

### Phase 30 vs prior new-ADR phases

Phase 30 = **third 6-unit new-ADR phase** (Phase 19 + 28 + 30
all 6 units; new-ADR + migration / module-introduction scope).
The **6-unit shape is the stable new-ADR-phase rhythm** when the
phase introduces:
- New ADR.
- New module (`lib/email/` + `lib/subscribers/`).
- New DB table + migration.
- Multi-route API surface.
- Multi-locale i18n addition.
- Q-promotion pair coupled to the ADR.

Phase 19 was lighter (single new file + migration); Phase 28 was
mid-weight (single-module + 1 file delta + 0 migration); Phase 30
is **heaviest 6-unit new-ADR phase** (2 new `lib/` modules + 1 new
DB table + 1 migration + 3 new API routes + 1 new UI component +
4 new test files + new env vars + new i18n namespace + new runtime
dep). **Phase 30 establishes the upper-bound shape for the 6-unit
new-ADR rhythm**; future phases of similar scope have a clear
template.

### Phase 31 entry conditions

Per §12 cardinal rule, **explicit human sign-off required**.
**Twenty-sixth "Continue" override opportunity** in Unit 31.0
prep. Thread options:

- **B.15 item 1: Weekly digest scheduler + send template**
  (~3-5 units; Phase-30 foundation natural follow-on; ADR-0022
  candidate; closes the foundation→production-send gap; mirrors
  Phase 21/23/25/27/29 tight-follow-on close pattern at 1-phase
  age).
- **Q68 expansion content moderation** (~3-5 units; ADR-0022
  candidate; 10+ phase carryover).
- **Markdown evolution ADR** coupling Q72 family + Phase-29 B.14
  (~3-5 units).
- **Account-merge UI** (ADR-0020 D-E; ~2-3 units; 3-phase age).
- **Q64 per-user privacy opt-out** (~3 units; 14-phase carryover;
  breaks 0-migration streak resumed Phase 31+).
- Other Class B / minor scopes.

### Files modified

- CHANGELOG.md (Unit 30.5 entry).
- `docs/thinking/30.5-phase-30-acceptance-gate.md` (this file).
