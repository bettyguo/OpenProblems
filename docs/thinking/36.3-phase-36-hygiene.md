# 36.3 — Phase-36 hygiene + 1 ADR APPEND (ADR-0015 D-A; first APPEND for that clause; tenth ADR D-clause cluster with APPENDs) + 0 OPEN_QUESTIONS edits (Q64 was never in OPEN_QUESTIONS.md at all — even more sub-bullet than Q68 expansion)

Fourth Phase-36 unit; docs-only. Single ADR APPEND + zero
OPEN_QUESTIONS edits + zero Class A items. Tighter than Phase 35's
hygiene unit (which shipped 3 ADR APPENDs + Q68 expansion sub-bullet
updates in 3 places).

## THINK

### Why only 1 ADR APPEND

Q64 was anticipated in ADR-0016 D-A as a passing mention ("opt-out
toggle Q64 Phase 16+") in a list of subsequent identity-edit
surfaces — not a load-bearing D-clause commitment. The
architecturally load-bearing surface is ADR-0015 D-A's public-data
invariant; Phase 36's opt-out is an EXTENSION of that invariant
(the user's right to opt out of public-data exposure for their own
data). ADR-0015 D-A APPEND is the right home.

ADR-0016 D-A's passing mention doesn't warrant a separate APPEND —
adding one would be ceremony. The mention itself stays valid (the
pattern claim was accurate; Phase 36's toggle inherits the
validation + server-action + inline-edit pattern from Phase 15
verbatim).

### Why 0 OPEN_QUESTIONS edits

**Q64 was never in OPEN_QUESTIONS.md at all.** Searched the file
exhaustively at Phase-36 entry — no `Q64` substring matches. Q64 is
referenced in:

- `docs/adr/0016-user-editable-profile-fields.md` D-A (passing
  mention in subsequent-surfaces list).
- `docs/adr/0023-per-user-account-subscriptions.md` Option 4 (Phase-
  35 thread-alternatives enumeration).
- Various phase-prep + acceptance-gate THINK docs (35.0 + 35.4 +
  34.3 + 34.0 + 33.4 + 33.0 + 32.0 + 31.0 + 30.0 + ...).
- `docs/release-readiness-audit.md`.

None of these are OPEN_QUESTIONS.md. Q64 is the "even more sub-
bullet" candidate than Q68 expansion: at least Q68 had sub-bullet
mentions in OPEN_QUESTIONS.md (3 locations under Q63 + Q67 + Q70);
Q64 has zero. First such "completely-absent-from-OPEN_QUESTIONS
Q-resolution" in project history.

The Q-tally update:

- **Q-tally** (mechanical `Status:`-field tally per Unit 9.8
  convention): 28 resolved + 4 lean + 34 open = **66 total**
  UNCHANGED. Phase 36 doesn't shift the top-level tally; Q64
  wasn't even on the ledger.
- **Phase-37+ candidate count** drops 11 → 10 (Q64 was on the
  candidate list per Phase-35-gate enumeration; closing it
  decreases the candidate count). **Third consecutive net-
  candidate-decrease phase** (Phase 34 + 35 + 36 = first 3-phase
  net-decrease streak in project history).

### ADR-0015 D-A APPEND content shape

Following the established APPEND-not-EDIT convention with "EXTENDED
Phase N Unit N.M — ..." prose:

- Documents the realization (column + UI + filter).
- Documents the public-data-invariant **extension, not
  contradiction** framing (key architectural claim).
- Enumerates Phase-37+ deferrals verbatim from Phase-36-prep D-F.
- Cross-refs Phase-36-prep D-H (pure 404 rationale).
- Counts as the **first public-data-invariant exception in
  project history**.
- Counts as the **tenth APPEND-pattern ADR D-clause cluster** in
  project history (milestone).

### Class B status (post-Phase-36 delta)

~3 across 14 categories UNCHANGED from Phase 35 close. Phase 36
does NOT touch B catalog directly. **Six consecutive phases without
new B category** (Phase 31 + 32 + 33 + 34 + 35 + 36) — extends the
Phase-31-onwards quintuple-phase streak to sextuple. First such
6-phase run in project history.

### Phase-36-specific architectural firsts shipped in this unit

- **First public-data-invariant exception** in project history
  (ADR-0015 D-A APPEND extends the invariant with the user's
  right to opt out).
- **Tenth APPEND-pattern ADR D-clause cluster** — milestone count.
- **First "completely-absent-from-OPEN_QUESTIONS Q-resolution"** in
  project history (Q64 was never even sub-bulleted; Q68 expansion
  had 3 sub-bullet locations; Q64 has zero).
- **Third consecutive net-candidate-decrease phase** (Phase 34 +
  35 + 36; Phase-37+ candidate count drops 13 → 12 → 11 → 10) =
  first 3-phase net-decrease streak in project history.
- **Six consecutive phases without new B category** (Phase 31 + 32
  + 33 + 34 + 35 + 36) = first such 6-phase run.

### Smoke gates (this unit)

Docs-only — no source files modified.

- Files modified: `docs/adr/0015-per-user-privacy-model.md` (+1
  APPEND paragraph after D-A's "Email never surfaces" prose);
  `CHANGELOG.md` (this entry); `docs/thinking/36.3-phase-36-hygiene.md`
  (NEW; THINK artifact).
- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- typecheck / test / build untouched since no source files
  modified.

### Parallel-curator awareness

This unit modifies 1 ADR + CHANGELOG + new THINK doc. Parallel-
safe — docs only. Working tree clean at unit boundary.

### Open questions newly surfacing

Anticipated: 0. No new opens; no new lean; no new resolved (Q64
sub-bullet resolution doesn't appear in OPEN_QUESTIONS.md tally).
