# 20.2 — Phase-20 hygiene status pass (0 Class A / ~10 Class B / Class C unchanged)

Third Phase-20 unit; docs-only. Mirrors Phase-13 Unit 13.4 +
Phase-14 Unit 14.6 + Phase-15 Unit 15.6 + Phase-16 Unit 16.6 +
Phase-17 Unit 17.5 + Phase-18 Unit 18.4 + Phase-19 Unit 19.3
hygiene catalog patterns.

## THINK

### Class A — in-flight Phase-20 cleanup: **0 items**

Phase-20's single code unit (20.1) shipped without deferrals.
The ADR-0019 D-E backfill contract is fully realized in
`scripts/backfill-exif-strip.ts`: per-row 5-phase pipeline
(fetch → sharp.rotate().toBuffer() → put → UPDATE → del) +
`--dry-run` CLI flag + URL-pattern filter + per-row try/catch.
Phase-20-deferred-to-Phase-21+ refinements (`--user-id` flag /
`--verbose` flag / concurrency / rate limiting / session-level
dry-run sentinel) are all explicit scope-cap decisions made in
Unit 20.0 D-3 + D-4 — NOT Class A.

**Phase-20 Class A count = 0** matches **Phase-12 through
Phase-19 well-scoped pattern — nine consecutive phases** ship
with 0 Class A items.

### Class B — Phase-20 follow-ons: **~10 items in 4 categories**

#### B.1 Image-processing expansion (4; carried from Phase 19 B.1 minus EXIF backfill = closed Unit 20.1)

- **Q68 expansion — content moderation on uploaded images**
  (ADR-0017 D-H + ADR-0019 D-F carryover) — server-side
  moderation API integration (OpenAI moderation / Perspective /
  custom regex+wordlist) inserted between `.rotate()` and
  `.toBuffer()`. ~3-4 units; ADR-0020+ candidate. Phase 21+.
- **Cropping UI** (ADR-0017 D-H + ADR-0019 D-F carryover) —
  `sharp.extract({ left, top, width, height })` inserted between
  `.rotate()` and `.toBuffer()`. Client component for crop
  region selection. ~3-5 units. Phase 21+.
- **Server-side resizing** (ADR-0017 D-H + ADR-0019 D-F
  carryover) — `sharp.resize(256, 256, { fit: "cover" })`
  inserted between `.rotate()` and `.toBuffer()`. ~1-2 units.
  Phase 21+ if observation shows bandwidth/render-time concern.
- **WebP/AVIF format conversion** (ADR-0019 D-F carryover) —
  `sharp.webp({ quality: 85 })` or `sharp.avif()` inserted
  between `.rotate()` and `.toBuffer()`. ~1-2 units. Phase 21+
  if observation shows storage cost concern.

**Phase-19 B.1 had 5 items; Phase 20 closed 1** (EXIF backfill
script via Unit 20.1); 4 carry to Phase 21+.

#### B.2 Phase-17 markdown subset expansion (8; carried unchanged)

8 items carried verbatim from Phase 17 Unit 17.5 + Phase 18 Unit
18.4 + Phase 19 Unit 19.3: tables / footnotes / images /
syntax-highlighted code blocks / bio length expansion /
`@mentions` / task-list interactivity / live preview client
boundary. Phase 20 added zero new markdown subset items.

#### B.3 Phase-18 surface expansion (3; carried unchanged)

3 items carried verbatim from Phase 18 Unit 18.4 + Phase 19 Unit
19.3: markdown rating-action `rationale` (couples to per-challenge
detail page); methodology page markdown unification (Velite MDX
vs `lib/markdown/`); per-challenge detail page (Phase-11 + 13
carryover). **Q72 candidate** (`reviewNotesSchema` divergence
from `bioSchema`) also carried from Phase 18.

#### B.4 Phase-20 backfill operational follow-ons (3; Phase-20 surfaced)

- **`--user-id` flag for surgical single-user backfill** —
  Phase-20 D-4 deferral. Useful for user-reported privacy
  retraction (curator runs `pnpm backfill-exif-strip --user-id=<id>`
  in response to a single user's privacy request). ~0.5 units
  when promoted. Phase 21+.
- **`--verbose` flag for per-row progress + EXIF tag counts
  before/after** — Phase-20 D-4 deferral. Diagnostic when bulk
  runs surface unexpected behavior. ~0.5 units when promoted.
  Phase 21+.
- **Session-level dry-run sentinel** — Phase-20 D-6 deferral.
  Script-header comment documents "run --dry-run first"; Phase
  21+ refinement adds a stronger guard (e.g., require explicit
  `--no-dry-run` flag or refuse first execution without a
  preceding `--dry-run` invocation in operator's shell history).
  ~0.5 units when promoted. Phase 21+.

#### B.5 Operational + infrastructure (5; unchanged from Phase 17/18/19)

- Q69 operational gate (`BLOB_READ_WRITE_TOKEN`); Q54 + Q55
  operational gates.
- CI dummy `AUTH_SECRET`.
- `pnpm db:migrate` doc (6 migrations unchanged from Phase 19).
- First LHCI run (19 URLs unchanged).
- Middleware-based auth-route protection (threshold stays at 2;
  Phase 20 added zero protected routes).
- **`pnpm backfill-exif-strip` operational documentation** —
  curator-track; should sit alongside `pnpm db:migrate` doc when
  Phase 21+ writes contributor / operator docs.

### Class C — pre-existing carryovers (unchanged from Phase 19 Unit 19.3)

All ~25 Class C items persist unchanged. The HTML shell migration
+ Unit 8.4 unblock remain ON HOLD per parallel-session
preservation signal.

### Phase-19 → Phase-20 delta

| Metric | Phase 19 close (`87d11e6`) | Phase 20 close pre-this (HEAD `3e37be1`) | Δ |
|---|---|---|---|
| Commits | 6 | 2 + this hygiene + 20.3 + 20.4 (anticipated) | +5 anticipated total |
| Content files (validated) | 203 | 203 | 0 |
| ADRs | **19** | **19** | **0** (Phase 20 ships no new ADR; second phase since Phase 3 to close without new ADR — first was Phase 18) |
| DB tables | 6 | 6 | 0 |
| Migrations | 6 | **6** | **0** (**fourth consecutive 0-migration phase**) |
| Env vars | 7 | 7 | 0 |
| Runtime deps | (Phase-19 stack incl. `sharp@0.34.5`) | (Phase-19 stack) | 0 (**zero new deps Phase 20**) |
| Storage layers | 3 | 3 | 0 |
| `users` columns | 10 | 10 | 0 |
| Tests | **564 / 54 files** | **564 / 54 files** | **0** (Phase 20 ships no new vitest tests per D-7) |
| Prerendered pages | ~659 | ~659 | 0 |
| Dynamic page+API routes | 7 | 7 | 0 |
| First Load JS (shared) | 103 kB | **103 kB** | **0** (per ADR-0018 D-F invariant extended; backfill script is CLI not bundled) |
| Middleware bundle | 160 kB | **160 kB** | **0** |
| `/profile` bundle | 117 kB | **117 kB** | **0** |
| `/u/{handle}` bundle | 108 kB | **108 kB** | **0** |
| OPEN_QUESTIONS resolved | 26 | 26 | 0 (anticipated 0 in Unit 20.3 — no new Q-promotions Phase 20) |
| OPEN_QUESTIONS open | 29 | 29 | 0 |
| OPEN_QUESTIONS lean | 4 | 4 | 0 |
| OPEN_QUESTIONS total | 59 | 59 | 0 |
| i18n keys per locale | 127 | 127 | 0 |
| LHCI URLs | 19 | 19 | 0 |
| `lib/storage/` files | 2 | 2 | 0 |
| `scripts/` files | 6 | **7** | **+1** (`backfill-exif-strip.ts`) |
| `package.json#scripts` entries | (existing) | (+1: `backfill-exif-strip`) | +1 |
| Server-side image processing surfaces | 1 (`putAvatar`) | **2** (`putAvatar` + backfill script) | **+1** |
| Privacy-by-default surfaces | 1 (Phase 19 EXIF on new uploads) | **2** (Phase 19 forward + Phase 20 retroactive) | **+1** |
| Retroactive-privacy-correction surfaces | 0 | **1** | **+1** |

### Architectural firsts shipped in Phase 20

1. **First retroactive-privacy-correction surface** in project
   history — Phase 19 shipped forward-only EXIF stripping (new
   uploads only via `putAvatar`); Phase 20 closes the
   backwards-compat gap for Phase 16-18 avatars via
   `scripts/backfill-exif-strip.ts`. Privacy posture moves from
   "new uploads stripped" to "all uploads stripped (after manual
   curator run)".
2. **Fourth consecutive 0-migration phase** (Phase 17 + 18 + 19 +
   20; 7 of 11 phases since DB landed are 0-migration: Phase 10 +
   13 + 14 + 17 + 18 + 19 + 20).
3. **Fourth consecutive single-session phase** (Phase 17 + 18 +
   19 + 20; Phase 16 was the parallel-session phase). Demonstrates
   that the constitution + auto-memory + ADR ledger converge work
   regardless of session count given well-scoped phases.
4. **Second phase since Phase 3 to close without new ADR** (Phase
   18 was first via ADR-0018 D-G inheritance contract for Q71;
   Phase 20 second via ADR-0019 D-E backfill implementation).
   Establishes precedent: when an ADR pins a Phase-N+ contract
   explicitly, the implementation phase can ship without a new
   ADR.
5. **Smallest Phase shape since Phase 13** — 5 units (was 6 in
   Phase 19; 7 in Phase 18; 7 in Phase 13). Phase 20's scope
   compression vs Phase 19 because: no new ADR + no new vitest
   tests + no UI affordance + no new dep + no new env var.
6. **First operational-script-only phase** in project history —
   Phase 20 ships zero changes to `lib/` / `app/` / `components/`
   /  `content/`; only `scripts/` + `package.json` + docs.
   Mirrors `extract-leaderboard` / `ingest-arxiv` /
   `cross-link-audit` / `validate-content` operational script
   posture; first time a phase keystone is an operational script.
7. **First "zero new dep + zero new test + zero new ADR"
   well-scoped phase** — Phase 20 ships entirely on existing
   architectural primitives (`sharp@0.34.5` direct dep from
   Phase 19 + `@vercel/blob` from Phase 16 + drizzle-orm from
   Phase 9). **Pure implementation of previously-pinned
   contract**.

### Phase-20+ Q-candidates flagged

Carryovers from Phase 19 (8 candidates carried; 0 promoted; 0 new):

- **Q59** (CLI emit-challenge-action) — carried Phase-12 through
  Phase-20 = **9 phases**.
- **Q60** (curator authz evolution).
- **Q61** (submitter anonymity option).
- **Q62** (rejection-rationale public visibility).
- **Q64** (per-user privacy opt-out).
- **Q65** (per-curator activity feed).
- **Q68 expansion** (content moderation on uploaded images;
  ADR-0019 D-F integration point documented).
- **Q72** (`reviewNotesSchema` divergence from `bioSchema`;
  Phase 18 newly flagged).

Total **8 Phase-21+ Q-candidates** carrying (unchanged from
Phase 19 close — Phase 20 added zero new and promoted zero).

### Phase-21+ ADR slot reservations

ADR-0020 candidate slot remains open after **7-phase ADR cycle
without multi-provider-OAuth claim** (Phase-15 ADR-0016 + Phase-16
ADR-0017 + Phase-17 ADR-0018 + Phase-18 NO-new-ADR + Phase-19
ADR-0019 + Phase-20 NO-new-ADR = 4 ADR slots since Phase-15 +
3 cycles without multi-provider claim):

- **Multi-provider OAuth expansion** (Phase-9 Class B item 8
  carried **10 phases** — strongest architectural "patience
  signal" growing).
- **Image-processing API content moderation** (Q68 expansion;
  ADR-0019 D-F inheritance compose pattern).
- **Markdown subset extensions** (Phase-17 D-H + Q72 candidate).
- **Subscriber-list email provider** (Phase-5 D-4 punt carried
  **14+ phases** — strongest overall "patience signal" growing).
- **Per-challenge detail page** (Phase-11 + 13 carryover; may not
  need new ADR).

### Phase-20 operational-script-keystone pattern

Phase 20 establishes a new phase-keystone pattern: **operational
script as primary deliverable**. Previous phases shipped:

- **Production-runtime keystones**: routes / components / server
  actions / API endpoints. Phase 1-19.
- **Operational scripts shipped as side deliverables**: Phase 2
  `validate-content`; Phase 2 `cross-link-audit`; Phase 5
  `ingest-arxiv` + `extract-leaderboard`. None of these were
  phase keystones — they were tooling alongside content/runtime
  work.
- **Phase 20**: `backfill-exif-strip.ts` is the keystone — sole
  code deliverable. No new runtime surface; pure operational
  retroactive fix.

This pattern likely repeats in future phases when previously-pinned
ADR clauses need operational implementation (e.g., `cleanup-orphan-blobs.ts`
per Phase-16 carryover).

### Smoke gates

Docs-only.

- `pnpm audit-content` → 0 errors / 6 warnings (Q32 baseline
  unchanged).
- File added: `docs/thinking/20.2-phase-20-hygiene.md` (this).
- CHANGELOG: Unit 20.2 entry.

### Parallel-curator awareness

This unit is docs-only — no collision risk.

### CHANGELOG entry shape

Unit 20.2 entry: docs-only hygiene catalog; 0 Class A items
matching Phase-12 through Phase-19 well-scoped pattern (nine
consecutive); ~10 Class B items in 5 categories (4 image-
processing expansion carryovers from Phase 19 B.1 minus EXIF
backfill closed Unit 20.1; 8 Phase-17 markdown subset
carryovers; 3 Phase-18 surface carryovers + Q72; 3 Phase-20
backfill operational follow-ons newly surfaced; 5 operational +
1 new operational doc); Class C carryovers unchanged; Phase 19
→ 20 delta (0 ADR Δ; 0 migration Δ; 0 runtime dep Δ; 0 test Δ;
0 i18n Δ; 0 bundle Δ; +1 `scripts/` file; +1 server-side image
processing surface; +1 privacy-by-default surface; +1
retroactive-privacy-correction surface); 7 Phase-20
architectural firsts (incl. retroactive-privacy-correction +
4th consecutive 0-migration + 4th consecutive single-session +
2nd phase since Phase 3 without new ADR + smallest Phase shape
since Phase 13 + first operational-script-only phase + first
zero-new-dep/test/ADR well-scoped phase); 8 Phase-21+
Q-candidates carry (unchanged from Phase 19; 0 promoted Phase
20); ADR-0020 candidate slot remains open after 7-phase cycle
without multi-provider-OAuth claim (Phase-9 Class B item 8 now
carried 10 phases).
