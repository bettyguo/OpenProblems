# 57.4 — Phase 57 acceptance gate

Phase 57 closes at HEAD `dc2b4ce` (Unit 57.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-58-entry
conditions per §12.

## Phase 57 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 57.0 | Phase 57 prep | docs | `091f078` |
| 57.1 | `GFM_TABLE_SCHEMA_OVERRIDES` schema-extension (colSpan + rowSpan + scope on th + td) + parity assertion extension + 9 NEW schema-isolation tests + ADR-0018 D-G APPEND-D-AO | code+APPEND | `b6b9bc3` |
| 57.2 | 6 NEW multi-attribute + multi-cell + regression-guard schema-isolation edge-case tests | code | `0d8de26` |
| 57.3 | Phase-57 hygiene + ADR-0018 APPEND-D-Q item 3 closure documentation (18-phase carryover; new longest absolute APPEND-deferral closure record) | docs | `dc2b4ce` |
| 57.4 | Phase 57 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-56 5-unit framework shape
verbatim.

## First schema-override extension on `schemaOverrides` slot kind + first schema-extension phase-shape + first all-3-slots-evolved state + first schema-ready-before-plugin pattern + new longest absolute APPEND-deferral closure (18-phase carryover; beats Phase-53 12-phase record)

- `GFM_TABLE_SCHEMA_OVERRIDES.attributes` evolved from `th: [["align", ...]]`
  + `td: [["align", ...]]` (Phase 39 ship; UNCHANGED through Phase
  56) to `th: [["align", ...], "colSpan", "rowSpan", ["scope", "row", "col", "rowgroup", "colgroup"]]`
  + `td: [["align", ...], "colSpan", "rowSpan"]` (Phase 57 ship).
  Schema-extension is constant-VALUE evolution; `TablesExtensionRegistry`
  class + factory dispatch arm + `PHASE_39_DEFAULT_ENABLED_SURFACES`
  UNCHANGED.
- Schema entries use **PROPERTY names** (camelCase JSX-style per
  `property-information`) rather than HTML attribute names —
  `colSpan` / `rowSpan` are the property names emitted into HAST;
  `rehype-stringify` converts them to HTML attribute form
  (`colspan="N"` / `rowspan="N"`) at compile time.
- **9 NEW schema-isolation tests** in `tables.test.ts` (Unit
  57.1) + **6 NEW multi-attribute + multi-cell edge-case tests**
  (Unit 57.2). Tests exercise the schema directly via manual
  HAST tree construction + `unified().use(rehypeSanitize,
  GFM_TABLE_SCHEMA_OVERRIDES).use(rehypeStringify)` pipeline
  (zero new deps).
- ADR-0018 D-G APPEND adds Phase-57 EXTENDED block + **APPEND-
  D-AO** (Unit 57.1; fifteenth two-letter slot).
- **APPEND-D-Q item 3 Table-specific attributes CLOSED at 18-
  phase carryover** (Phase 39 → 57) — **NEW LONGEST ABSOLUTE
  APPEND-DEFERRAL CLOSURE EVER OBSERVED** (beats prior 12-phase
  Phase 41 → 53 D-Y item 2 record).
- **First schema-override extension on the `schemaOverrides`
  slot kind** in project history.
- **First "schema-extension" phase-shape pattern** — sibling
  to the established plugin-regex-extension pattern (6
  realizations).
- **First evolution of `GFM_TABLE_SCHEMA_OVERRIDES` since
  Phase 39 ship** — 18-phase constant-stability streak ends.
- **First state where all 3 framework slot kinds have been
  evolved post-Phase-39** — `remarkPlugins` 5× + `rehypePlugins`
  1× + `schemaOverrides` 1× = all 3 slot kinds exercised.
- **First "schema-ready-before-plugin" pattern** in project
  history.
- **APPEND-deferral closure cadence sustained 16 phases** —
  longest in project history.

## Phase 56 → Phase 57 delta

| Metric | Phase 56 close | Phase 57 close | Δ |
|---|---|---|---|
| Tests | 1324 / 74 files | **1339 / 74 files** | +15 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 23 | **24** (project record extends; fifteenth two-letter slot D-AO in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 7 | 7 | 0 |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 19 | 19 | 0 |
| **Phase-37-framework concrete consumers** | **6** | **6** | 0 |
| **Plugin-regex-extension realizations** | **6** | **6** | 0 |
| **Constructor-arg-only zero-rework expansion realizations** | **6** | **6** | 0 |
| **Schema-extension realizations** | **0** | **1 (tables Phase 57)** | **+1 (new pattern)** |
| **Framework slot kinds evolved post-Phase-39** | **2 (remarkPlugins + rehypePlugins)** | **3 (+schemaOverrides)** | **+1 (first all-3-slots-evolved state)** |
| **Coexisting 6-realization framework patterns** | **2** | **2** | 0 |
| **First-realization framework patterns introduced this phase** | **0** | **1 (schema-extension)** | **+1** |
| **`GFM_TABLE_SCHEMA_OVERRIDES` value stability streak (phases)** | **18 (Phase 39 → 56)** | **0 (broken at Phase 57)** | **−18 (longest constant-stability streak ends)** |
| **APPEND-D-Q items closed** | **3 of 6** (items 1, 2, 5) | **4 of 6** (+item 3) | **+1** |
| **Longest absolute APPEND-deferral closure ever observed (phases)** | **12 (Phase 41 → 53)** | **18 (Phase 39 → 57)** | **+6 (new record)** |
| **APPEND-deferral closure cadence (consecutive phases)** | 15 | **16 (Phase 42-57)** | **+1 (new record)** |
| **"Continue" override invocations past half-century threshold** | **1 (Phase 56 = 51st)** | **2 (+Phase 57 = 52nd)** | **+1** |

## Architectural firsts in Phase 57 (12 enumerated)

1. **First schema-override extension on the `schemaOverrides`
   slot kind** in project history. Pre-Phase-57 the framework
   had 6 plugin-regex-extension realizations across 2 slot
   kinds (`remarkPlugins` × 5: Phase 47 + 48 + 51 + 53 + 55;
   `rehypePlugins` × 1: Phase 46). The third framework slot
   kind (`schemaOverrides`) had been ARCHITECTURALLY-PRESENT
   but EVOLUTION-DORMANT for 18 phases (Phase 39 → Phase 56).
   Phase 57 activates it.
2. **First "schema-extension" phase-shape pattern** in
   project history. Sibling to the established plugin-regex-
   extension pattern (6 realizations). The two patterns differ
   in slot kind (regex-body vs schema-shape) and validation
   discipline (regex-disjointness vs attribute-value-allow-
   list-tuple). Together they confirm the consumer-extension
   property is slot-kind-agnostic.
3. **First evolution of `GFM_TABLE_SCHEMA_OVERRIDES` since
   Phase 39 ship** in project history. The constant had been
   UNCHANGED for 18 phases (Phase 39 → Phase 56) — **longest
   constant-stability streak in framework history**. Phase 57
   evolves the VALUE while preserving the name per Phase-
   42/43/44/49/52/56 D-8 precedent generalized to schema
   constants.
4. **First state where all 3 framework slot kinds have been
   evolved post-Phase-39** in project history. `remarkPlugins`:
   5 evolutions (arxiv Phase 47 + 53; doi Phase 48; pubmed
   Phase 51; orcid Phase 55). `rehypePlugins`: 1 evolution
   (wikilinks Phase 46). `schemaOverrides`: 1 evolution
   (tables Phase 57). **First all-3-slots-evolved state**.
5. **NEW LONGEST ABSOLUTE APPEND-DEFERRAL CLOSURE EVER
   OBSERVED** at **18-phase carryover** (Phase 39 → 57; beats
   prior 12-phase Phase 41 → 53 D-Y item 2 record by 6
   phases). **First 18-phase APPEND-deferral closure** in the
   project's phase taxonomy. The carryover gap reflects the
   demand-signal-first deferral discipline at maximum
   patience.
6. **Third APPEND-D-Q deferral closure** (Phase 40 closed item
   1 multi-value composition; Phase 43 closed item 2 cross-
   surface; Phase 41 closed item 5 `remarkPlugins`-consumer-
   candidate via arxiv first-ship; Phase 57 closes item 3
   table-specific attributes). D-Q gains its **4th closed
   item of 6**; 2 items remain open (item 4 `<caption>` + item
   6 surface-specific schemas). **D-Q is the first non-D-G +
   non-D-Y D-clause to have 4+ items closed through the
   closure cadence**.
7. **First "non-cross-surface non-alias non-sibling non-regex-
   evolution APPEND-deferral closure"** in the cadence. Phase
   42 + 43 + 44 + 49 + 52 + 56 were cross-surface; Phase 46 +
   47 + 48 + 51 + 55 were alias-syntax plugin-regex-extensions;
   Phase 53 was inner-class disjunction plugin-regex-extension;
   Phase 45 + 50 + 54 were sibling-consumer first-ship. Phase
   57 is none of these — **the first schema-extension closure**.
   **First closure on the `schemaOverrides` slot kind**.
8. **First "schema-ready-before-plugin" pattern** in project
   history. Pipeline-emission caveat: `remark-gfm` does NOT
   emit span/scope (GFM table syntax has no span/scope
   markup); `remark-rehype` runs with `allowDangerousHtml:
   false`, stripping raw HTML at the MDAST → HAST boundary.
   **No current Phase-37-framework pipeline emits these
   attributes** — schema-extension prepares the framework for
   hypothetical future plugin authors (MultiMarkdown-table
   plugin, HTML-table-pass-through plugin, custom remark
   transformer via `mdast.data.hProperties`). Schema-isolation
   tests exercise the new allow-list entries directly via
   manual HAST tree construction; first phase-shape that
   establishes a defensive-infrastructure pattern.
9. **Sixteenth prep-/APPEND-doc-level deferral closed by a
   later phase**. **APPEND-deferral closure cadence sustained
   16 phases — new longest sustained cadence in project
   history** (extends Phase-56 record 15 → 16). **First 16-
   phase APPEND-deferral closure run**.
10. **24th APPEND on ADR-0018 D-G** — extends the **first-
    ADR-D-clause-with-most-APPENDs record** from 23 → 24.
    **Fifteenth two-letter APPEND letter D-AO** (Excel-
    spreadsheet column convention sustained; D-AP + ... +
    D-AZ would carry Phase 58+ at this cadence; after D-AZ
    rolls to D-BA at Phase 70+).
11. **First "first-realization architectural phase-shape"
    after the 6 alias-syntax / cross-surface phase-shapes
    have saturated**. Phase 38-56 closure cadence covered 4
    phase-shape patterns: consumer-first-ship (6
    realizations), constructor-arg-only-expansion (6), plugin-
    regex-extension (6), and sibling-thread (Phase 49 + 50 +
    54). Phase 57 INTRODUCES a fifth phase-shape (schema-
    extension) at its first realization — **first new phase-
    shape introduced since the plugin-regex-extension pattern
    Phase 46** (11-phase gap; Phase 46 → 57). Also: **first
    "framework + 6 consumers + 6 expansions + 6 plugin-regex-
    extensions + 1 schema-extension + 4-consumer same-slot ×
    4 surfaces + 6-consumer composition × 4 surfaces + alias
    syntax × 5 + dual-form regex × 4 + quintuple-alias × 4 +
    5-consumer composition × 4 + inner-class disjunction × 1
    + 2-evolutions on arxiv + all-3-slots-evolved" 21-phase
    cluster** in project history (Phase 37-57). 21-phase
    architectural arc. Plus **27th consecutive phase without
    new B category** — Phase 31-57 (extends Phase-56 record
    26 → 27). Plus **forty-eighth NON-§13 phase**. Plus
    **144th consecutive 103 kB First Load JS unit** (extends
    from 139 at Phase-56 close). Plus **22nd consecutive no-
    new-ADR phase** (Phase 36-57; extends Phase-56 record
    21 → 22).
12. **52nd "Continue" override invocation** at Unit 57.0 —
    **second invocation past the half-century threshold**
    (Phase 55 = 50th milestone; Phase 56 = 51st; Phase 57 =
    52nd). Confirms the §12-cardinal-rule sign-off discipline
    remains durable post-milestone.

## Phase 57 → Phase 58 entry conditions

Per §12, Phase 58 entry requires **explicit human sign-off**.

**Recommended Phase 58 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **bioRxiv preprint consumer** (`biorxiv:` / DOI-overlap with doi consumer) — **seventh concrete consumer**; first 5th-`remarkPlugins` consumer; tests whether regex-disjointness scales from 4 to 5 same-slot consumers; real user-facing demand-signal | 2-3 | DOI overlap concern requires regex disambiguation |
| 2 | **Cross-entity wikilinks** (APPEND-D-L item 3) — entity-type disambiguation + plugin parameterization | 2-3 | Requires APPEND-D-L item 6 plugin parameterization to land first |
| 3 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — couples with `schemaOverride` for `class` attribute on `<a>`; first single-consumer-multi-slot case | 1-2 | XSS audit per attribute value; would be **second schema-extension realization** sibling to Phase 57 |
| 4 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable; introduces build-time validation step |
| 5 | **Bare arxiv / DOI / PubMed / ORCID IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 6 | **Empty-alias fallback unification** across consumers (Phase-48 deferral carries) — refine arxiv/doi/pubmed/orcid empty-alias divergence | 1 | Smallest-scope autonomous-tractable |
| 7 | **OSF preprint consumer** — seventh or later concrete consumer alternative to bioRxiv | 2-3 | DOI overlap concern |
| 8 | **Plugin parameterization for wikilink-href-builder** (APPEND-D-L item 6) — enables Rank 2 cross-entity-wikilinks downstream | 1-2 | Mechanical refactor; autonomous-tractable |
| 9 | **`<caption>` element** (APPEND-D-Q item 4) — second schema-extension realization on `schemaOverrides` slot kind; first within-D-Q sibling closure post-Phase-57 | 1-2 | XSS audit minimal (text-only element) |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1339 / 74 files** (+15 / 0 vs Phase 56
  close).
- `pnpm audit-content` — 0 errors / 6 warnings (53
  consecutive phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (144 consecutive units
  at Unit 57.4 ship).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 58+ deferrals)

NOT bioRxiv preprint consumer (Phase 58+ rank 1);
NOT cross-entity wikilinks (Phase 58+ rank 2);
NOT `<a class="wikilink">` styling (Phase 58+ rank 3);
NOT 404 handling (Phase 58+ rank 4);
NOT bare arxiv / DOI / PubMed / ORCID IDs without prefix (Phase
58+ rank 5);
NOT empty-alias fallback unification (Phase 58+ rank 6);
NOT OSF preprint consumer (Phase 58+ rank 7);
NOT plugin parameterization for wikilink-href-builder (Phase
58+ rank 8);
NOT `<caption>` element (Phase 58+ rank 9);
NOT a future plugin that EMITS `colSpan`/`rowSpan`/`scope` to
realize the Phase-57 schema-ready-before-plugin state (Phase
58+);
NOT 5th-or-later `remarkPlugins` consumer beyond arxiv + doi +
pubmed + orcid (Phase 58+ rank 1 ships bioRxiv as 5th
candidate);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 58+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 58+);
NOT dx.doi.org legacy host (Phase 58+);
NOT stricter trailing-lookahead (Phase 58+);
NOT paper-card hover-preview (Phase 58+);
NOT auto-trim of alias display whitespace (Phase 58+);
NOT surface-specific table schemas (Phase 58+; second-remaining
D-Q item);
NOT `@mention` (Phase 58+);
NOT 3rd regex evolution on `remarkLinkArxivIds` (Phase 58+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-56 deferrals carry forward.

## Boundary statement

**Phase 57 CLOSED at HEAD `<this-unit>` after 5 numbered units
(57.0 prep + 57.1 schema-extension constant-value evolution +
parity assertion extension + 9 NEW schema-isolation tests +
APPEND-D-AO + 57.2 6 NEW multi-attribute + edge-case +
regression-guard schema-isolation tests + 57.3 hygiene + 57.4
gate). First schema-override extension on the `schemaOverrides`
slot kind in project history: GFM table cells gain `colSpan`
/ `rowSpan` (name-only allow on `<th>` and `<td>`) and `scope`
(literal-enum restricted to `row` / `col` / `rowgroup` /
`colgroup` on `<th>`) attribute allow-list entries via in-
place value evolution of `GFM_TABLE_SCHEMA_OVERRIDES.attributes`.
First "schema-extension" phase-shape pattern in project history
— sibling to the established plugin-regex-extension pattern
(6 realizations: Phase 46 + 47 + 48 + 51 + 53 + 55).
Demonstrates that consumer-extension is slot-kind-agnostic: a
Phase-37-framework consumer can evolve its `remarkPlugins` body
(Phase 46-55) OR its `schemaOverrides` shape (Phase 57+)
without architectural rework. First evolution of
`GFM_TABLE_SCHEMA_OVERRIDES` since Phase 39 ship in project
history — 18-phase constant-stability streak ends; constant
NAME preserved per Phase-42/43/44/49/52/56 D-8 precedent
generalized to schema constants. First state where all 3
framework slot kinds have been evolved post-Phase-39
(`remarkPlugins` 5× + `rehypePlugins` 1× + `schemaOverrides`
1× = all 3/3 framework slot kinds exercised). Closes APPEND-
D-Q item 3 at 18-phase carryover (Phase 39 → 57). NEW LONGEST
ABSOLUTE APPEND-DEFERRAL CLOSURE EVER OBSERVED (beats prior
12-phase Phase 41 → 53 D-Y item 2 record by 6 phases). First
18-phase APPEND-deferral closure in the project's phase
taxonomy. Third APPEND-D-Q deferral closure (D-Q gains 4th
closed item of 6; 2 items remain open: item 4 `<caption>` +
item 6 surface-specific schemas). D-Q is the first non-D-G +
non-D-Y D-clause to have 4+ items closed through the closure
cadence. First "non-cross-surface non-alias non-sibling non-
regex-evolution APPEND-deferral closure" in the cadence —
Phase 57 is the first schema-extension closure; first closure
on the `schemaOverrides` slot kind. First "schema-ready-
before-plugin" pattern in project history — pipeline-emission
caveat documented: `remark-gfm` doesn't emit span/scope;
`remark-rehype` with `allowDangerousHtml: false` strips raw
HTML; schema-extension prepares for future plugin authors.
APPEND-deferral closure cadence sustained 16 phases — new
longest sustained cadence in project history (extends Phase-
56 record 15 → 16). First 16-phase APPEND-deferral closure
run. First "first-realization architectural phase-shape"
after the 6 alias-syntax / cross-surface phase-shapes have
saturated — Phase 57 introduces a fifth framework phase-shape
(schema-extension) at its first realization; first new phase-
shape introduced since the plugin-regex-extension pattern
Phase 46 (11-phase gap). 24th APPEND on ADR-0018 D-G (project
record extends 23 → 24). Fifteenth two-letter APPEND letter
D-AO. Forty-eighth NON-§13 phase. First "framework + 6
consumers + 6 expansions + 6 plugin-regex-extensions + 1
schema-extension + 4-consumer same-slot × 4 surfaces + 6-
consumer composition × 4 surfaces + alias syntax × 5 + dual-
form regex × 4 + quintuple-alias × 4 + 5-consumer composition
× 4 + inner-class disjunction × 1 + 2-evolutions on arxiv +
all-3-slots-evolved" 21-phase cluster in project history
(Phase 37-57). 27th consecutive phase without new B category
— first 27-phase run (extends Phase-56 record 26 → 27). 22nd
consecutive no-new-ADR phase (Phase 36-57; extends Phase-56
record 21 → 22). 144th consecutive 103 kB First Load JS unit
(extends from 139 at Phase-56 close). 52nd "Continue"
override invocation — second invocation past the half-century
threshold (over Phases 6-57). XSS line of defense preserved
at new attribute frontier (APPEND-D-O boundary extended;
regression-guard tests validate no attribute allow-list
widening to event-handlers or styles). 1339 tests across 74
vitest files (+15/0 vs Phase 56). 24 ADRs UNCHANGED. 7 DB
tables UNCHANGED. 9 migrations UNCHANGED. 14 env vars
UNCHANGED. 168 i18n keys per locale UNCHANGED. First Load JS
103 kB UNCHANGED. Middleware 160 kB UNCHANGED. OPEN_QUESTIONS
top-level Q-count 66 UNCHANGED. Phase-37+ candidate count 8
UNCHANGED. `MARKDOWN_EXTENSIONS` recognized single-value arms
7 UNCHANGED. Zero new ADRs / migrations / i18n keys / env
vars / operational gates / runtime deps / files. Phase 58
entry requires explicit human sign-off per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 56 → 57 delta complete.
- [x] Phase 58 entry conditions documented (10 ranked
      candidates).
- [x] Smoke gates pass.
- [x] STOP at Phase 58 boundary per §12.
