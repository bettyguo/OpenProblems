# 42.4 — Phase 42 acceptance gate

Phase 42 closes at HEAD `7efe161` (Unit 42.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-43-
entry conditions per §12.

## Phase 42 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 42.0 | Phase 42 prep | docs | `7f086ec` |
| 42.1 | `PHASE_38_DEFAULT_ENABLED_SURFACES` → 4 surfaces + tests + APPEND-D-Z | code+APPEND | `c7baa48` |
| 42.2 | 12 NEW factory-dispatch + composition tests | code | `e907d9c` |
| 42.3 | Phase-42 hygiene + APPEND-D-L item 1 + Phase-29 D-G closures | docs | `7efe161` |
| 42.4 | Phase 42 acceptance gate (this unit) | gate | (current) |

5 units; mirrors Phase 35 + 37 + 38 + 39 + 40 + 41 5-unit
framework shapes exactly.

## First cross-surface expansion of a Phase-37 framework consumer realized end-to-end

- `PHASE_38_DEFAULT_ENABLED_SURFACES` value evolved from
  `Set(["actionRationale"])` (Phase 38 ship through Phase-41
  close) to `Set(["bio", "reviewNotes", "rationale",
  "actionRationale"])` (Phase 42 ship; Unit 42.1).
- Constant NAME preserved per prep-doc D-8 audit-trail
  discipline (Phase 38 = introduction phase encoded in name;
  VALUE evolves Phase 42).
- 3 existing test assertions updated in `wikilinks.test.ts`
  + `extensions/index.test.ts` (Unit 42.1).
- ADR-0018 D-G APPEND adds Phase-42 EXTENDED block + APPEND-
  D-Z (Unit 42.1; expansion shape + composition matrix update
  + Phase 43+ deferrals). **9th APPEND on ADR-0018 D-G**
  (project record extends 8 → 9). **APPEND-D-Z consumes the
  last single-letter slot**; Phase 43+ APPEND letters wrap
  to two-letter form (D-AA + ...).
- 12 NEW factory-dispatch + composition end-to-end tests in
  `lib/markdown/index.test.ts` (Unit 42.2): 7 Phase-42
  wikilinks-default tests + 5 same-surface different-slot
  composition tests.
- ADR-0018 APPEND-D-L item 1 deferral CLOSED at 4-phase
  carryover (Phase 38 → Phase 42; second prep-/APPEND-doc-
  level deferral closed by later phase).
- Phase-29 D-G REALIZED-block cross-surface deferral CLOSED
  at 13-phase carryover (Phase 29 → Phase 42; Phase 38 closed
  actionRationale-only single-surface scope; Phase 42 closes
  all-surfaces scope).
- First "all 4 markdown surfaces enabled by ≥1 consumer"
  state achieved.
- First "constructor-arg-only zero-rework expansion" property
  validated end-to-end.

## Phase 41 → Phase 42 delta

| Metric | Phase 41 close | Phase 42 close | Δ |
|---|---|---|---|
| Tests | 883 / 71 files | **895 / 71 files** | +12 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 8 | **9** (project record extends; single-letter slot consumed through Z) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 4 | 4 | 0 |
| i18n keys per locale | 168 | 168 | 0 |
| Dynamic page+API routes | 13 | 13 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 13 | 13 | 0 |
| Phase-37-framework concrete consumers | 3 | 3 | 0 |
| Framework slots exercised by real consumer | 3 of 3 | **3 of 3** | 0 |
| Markdown surfaces enabled by ≥1 consumer | 3 of 4 | **4 of 4 (+`bio`)** | +1 |
| Multi-value composition arity | 3-way | 3-way | 0 |
| Wikilinks default-enabled surfaces | 1 (`actionRationale`) | **4 (all)** | +3 |

## Architectural firsts in Phase 42 (12 enumerated)

1. **First cross-surface expansion of a Phase-37-framework
   consumer**. Phase 38/39/41 each shipped a consumer with
   `Set(["one-surface"])` default; Phase 42 expands the
   wikilinks consumer to all 4 surfaces via constructor-arg
   value-only change. Validates the "constructor-arg-only
   with zero plugin/registry rework" property each Phase
   38/39/41 consumer documented in its APPEND.
2. **First Phase-N APPEND deferral closed by Phase-(N+M)
   value-only change** — Phase-38 APPEND-D-L item 1 closes
   Phase 42 via a single-constant-value change. **4-phase
   gap** (Phase 38 → 42). Second prep-/APPEND-doc-level
   deferral closed by a later phase (first was Phase-40
   closure of Phase-38-prep D-11 multi-value composition
   deferral; both Phase-38 deferrals closed by Phase-40+
   phases).
3. **First "all 4 markdown surfaces enabled by ≥1 consumer"
   state** in project history. Pre-Phase-42: only 3 surfaces
   had any consumer (Phase 38 actionRationale + Phase 39
   reviewNotes + Phase 41 rationale = disjoint sets). Phase
   42 closes the `bio` gap — wikilinks becomes the first
   consumer to enable `bio` for any extension. **4-of-4
   surface-coverage state achieved.**
4. **First "constructor-arg-only zero-rework expansion"
   property validated end-to-end** in project history.
   Wikilinks consumer expansion: 1 constant-value change +
   3 test-assertion updates + 1 ADR APPEND + 12 NEW
   factory-dispatch + composition tests. Zero plugin /
   class / factory edits. **Framework + plugin code stable**
   since Phase 37/38/39/40/41 ship commits.
5. **First canonical same-surface different-slot composition
   case under default dispatch** in project history. Phase
   41's 3-way example was disjoint-surface (wikilinks
   actionRationale + tables reviewNotes + arxiv rationale =
   3 disjoint surfaces); Phase 42 wikilinks-on-all-4 + tables-
   on-reviewNotes creates the first SAME-surface multi-
   consumer dispatch where two consumers contribute to the
   same surface via distinct slots. APPEND-D-R "at most one
   component per slot per surface" rule still applies; the
   composition is conflict-free.
6. **Phase-29 D-G REALIZED-block cross-surface deferral
   CLOSED at 13-phase carryover** (Phase 29 → Phase 42).
   Phase 38 closed the actionRationale-only single-surface
   scope; Phase 42 closes the all-surfaces scope.
7. **Ninth APPEND on ADR-0018 D-G** — extends the **first-
   ADR-D-clause-with-most-APPENDs record** from 8 → 9
   (Phase 18 + 27 + 29 + 37 + 38 + 39 + 40 + 41 + **42**).
8. **First "framework + 3 consumers + composition + 1
   expansion" 6-phase cluster** in project history (Phase
   37 + 38 + 39 + 40 + 41 + 42). Six-phase architectural
   arc: framework definition (37) + consumer (38) + consumer
   (39) + composition (40) + third consumer realizing 3-of-3
   + 3-way composition (41) + first cross-surface expansion
   (42).
9. **First single-letter alphabet wrap deferral** in ADR-
   0018 D-G APPEND letters — APPEND-D-Z (Unit 42.1) consumed
   the last single-letter slot (A through Z = 26 letters);
   Phase 43+ APPEND letters wrap to two-letter form (D-AA +
   ...) per prep-doc D-4 Option β.
10. **12th consecutive phase without new B category** —
    Phase 31-42 = **first 12-phase run** in project history
    (extends Phase-41 record from 11 → 12).
11. **73rd-through-76th consecutive 103 kB First Load JS
    units** in project history (Phase 9 Unit 9.5 → Phase 42
    Unit 42.4 inclusive). 4 of the 5 Phase-42 units
    incremented the streak; Unit 42.0 prep did not
    increment per phase-prep precedent.
12. **Thirty-third NON-§13 phase** (§13 ledger CLOSED at
    Unit 9.9; carried unchanged through Phases 10-42).

## Phase 42 → Phase 43 entry conditions

Per §12, Phase 43 entry requires **explicit human sign-off**.

**Recommended Phase 43 first-thread candidates** (11 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **Tables cross-surface expansion** to bio + rationale + actionRationale (analogous Phase-43 candidate to Phase-42 wikilinks expansion; constructor-arg-only change) | 1-2 | Autonomous-tractable; mirrors Phase-42 pattern; zero current content evidence (demand-signal-first relaxed for property validation per Phase-41-gate rank-1 framing) |
| 2 | **Arxiv cross-surface expansion** to bio + reviewNotes + actionRationale | 1-2 | Autonomous-tractable; mirrors Phase-42 pattern; zero current content evidence |
| 3 | **DOI sibling consumer** in `remarkPlugins` slot (`doi:10.<reg>/<id>` → `https://doi.org/10.<reg>/<id>`) — first compositional same-slot case (two consumers both using `remarkPlugins`) | 2-3 | Autonomous-tractable; new consumer authoring; first time the framework needs to handle 2 plugins in the same slot under composition |
| 4 | **Multi-anchor wikilink alias syntax** `[[slug\|display-text]]` (APPEND-D-L item 2) | 1-2 | Autonomous-tractable; plugin regex extension; first plugin-level capability addition under the framework |
| 5 | **Cross-entity wikilinks** (`[[paper-id]]` / `[[author-slug]]` — APPEND-D-L item 3) | 2-3 | Requires entity-type disambiguation + plugin parameterization (APPEND-D-L item 6); more architectural surface |
| 6 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |
| 7 | **`@mention` resolution consumer** in `remarkPlugins` slot | 2-3 | Conditional on Q73 gate (Google OAuth) |
| 8 | **Pre-commit typecheck hook** | 1 | Tiny; arguably not a full phase |
| 9 | **Q78 / Q79 / N. v2 methodology / safeAuth / test backfill / account-delete** | various | Each deferred per prior rationale |

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **895 / 71 files**.
- `pnpm audit-content` — 0 errors / 6 warnings (40 consecutive
  phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (76 consecutive units).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 43+ deferrals)

NOT tables cross-surface expansion (Phase 43+);
NOT arxiv cross-surface expansion (Phase 43+ or later);
NOT DOI sibling consumer (Phase 43+);
NOT multi-anchor wikilink alias syntax `[[slug|display]]`
(Phase 43+);
NOT cross-entity wikilinks (`[[paper-id]]` / `[[author-slug]]`;
Phase 43+);
NOT plugin parameterization for wikilink-href-builder (Phase
43+);
NOT `<a class="wikilink">` styling (Phase 43+);
NOT 404 handling for unresolved wikilinks (Phase 43+);
NOT `@mention` resolution (Phase 43+);
NOT same-slot composition with two consumers (Phase 43+
when 2nd remarkPlugins or 2nd rehypePlugins consumer ships);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-41 deferrals carry forward.

## Boundary statement

**Phase 42 CLOSED at HEAD `<this-unit>` after 5 units (42.0
prep + 42.1 `PHASE_38_DEFAULT_ENABLED_SURFACES` expansion +
APPEND-D-Z + 42.2 12 NEW factory-dispatch + composition tests
+ 42.3 hygiene + 42.4 gate). First cross-surface expansion of
a Phase-37-framework consumer realized: wikilinks expand
from `actionRationale`-only to all 4 markdown surfaces via
constructor-arg value-only change in
`PHASE_38_DEFAULT_ENABLED_SURFACES`. Closes ADR-0018 APPEND-
D-L item 1 deferral at 4-phase carryover (second prep-/
APPEND-doc-level deferral closed by later phase). Closes
Phase-29 D-G REALIZED-block cross-surface deferral at 13-
phase carryover. First "all 4 markdown surfaces enabled by
≥1 consumer" state in project history (wikilinks becomes
the first consumer to enable `bio`). First "constructor-arg-
only zero-rework expansion" property validated end-to-end
(zero plugin / class / factory edits). First canonical same-
surface different-slot composition case under default
dispatch in project history (wikilinks rehype + tables
schema on reviewNotes under `wikilinks,tables`; conflict-
free per APPEND-D-R). Thirty-third NON-§13 phase. First
"framework + 3 consumers + composition + 1 expansion"
6-phase cluster in project history. Ninth APPEND on ADR-
0018 D-G (project record extends 8 → 9). First single-
letter alphabet wrap deferral in ADR-0018 D-G APPEND letters
(D-A through D-Z consumed at Phase 42 ship; Phase 43+ wraps
to D-AA+). 12th consecutive phase without new B category
(first 12-phase run). 895 tests across 71 vitest files (+12
/ 0 vs Phase 41). 24 ADRs UNCHANGED. 7 DB tables UNCHANGED.
9 migrations UNCHANGED. 14 env vars UNCHANGED. 168 i18n
keys per locale UNCHANGED. First Load JS 103 kB UNCHANGED
(76 consecutive units). Middleware 160 kB UNCHANGED.
OPEN_QUESTIONS top-level Q-count 66 UNCHANGED. Phase-37+
candidate count 8 UNCHANGED. Zero new ADRs / migrations /
i18n keys / env vars / operational gates / runtime deps /
files. Phase 43 entry requires explicit human sign-off per
§12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 41 → 42 delta complete.
- [x] Phase 43 entry conditions documented.
- [x] Smoke gates pass.
- [x] STOP at Phase 43 boundary per §12.
