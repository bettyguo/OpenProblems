# 51.4 — Phase 51 acceptance gate

Phase 51 closes at HEAD `7b377f9` (Unit 51.3 ship). This unit
ships the acceptance gate + boundary statement + Phase-52-entry
conditions per §12.

## Phase 51 — what shipped

| Unit | Title | Type | HEAD |
|---|---|---|---|
| 51.0 | Phase 51 prep | docs | `01e692d` |
| 51.1 | `PUBMED_PATTERN` dual-form regex + plugin body + 13 NEW alias tests + ADR-0018 D-G APPEND-D-AI | code+APPEND | `7da5832` |
| 51.2 | 17 NEW end-to-end Phase-51 tests + first quadruple-alias surface | code | `c78e922` |
| 51.3 | Phase-51 hygiene + new Phase-50 deferral closure documentation | docs | `7b377f9` |
| 51.4 | Phase 51 acceptance gate (this unit) | gate | (current) |

5 numbered units. Mirrors Phase 41-50 5-unit framework shape
verbatim.

## Fourth realization of Phase-46 phase-shape + third dual-form regex + first quadruple-alias surface + fastest-closure record

- `PUBMED_PATTERN` regex evolved in-place from
  `/\b(?:pubmed|pmid):(\d{1,9})\b/gi` (Phase 50 ship) to
  `/\[\[(?:pubmed|pmid):(\d{1,9})(?:\|([^\]\n]+))?\]\]|\b(?:pubmed|pmid):(\d{1,9})\b/gi`
  (Phase 51 ship). **Third dual-form regex** in the framework.
  **First "dual-form regex with inner alternation inside the
  bracketed branch"** — the `(?:pubmed|pmid):` alternation
  appears in BOTH alternatives, inheriting Phase-50 dual-prefix
  support.
- `remarkLinkPubmedIds` plugin body branches on `isBracketed`.
  Three display rules mirror Phase-47/Phase-48 body verbatim
  with prefix-alternation source-casing preservation.
- `PubmedExtensionRegistry` class + factory dispatch arm +
  `PHASE_50_DEFAULT_ENABLED_SURFACES` constant **UNCHANGED**.
- 13 NEW tests in `pubmed.test.ts` (28 → 41 plugin-level tests
  + 6 class-level UNCHANGED = 47 total in file).
- 17 NEW end-to-end tests in `lib/markdown/index.test.ts`
  across 2 describe blocks.
- ADR-0018 D-G APPEND adds Phase-51 EXTENDED block + **APPEND-
  D-AI** (Unit 51.1; ninth two-letter APPEND letter).
- New Phase-50 deferral CLOSED at **1-phase carryover** —
  **fastest APPEND-deferral closure ever observed**.
- **Fourth realization of the Phase-46 plugin-regex-extension
  phase-shape pattern** — all 3 `remarkPlugins` consumers
  exhibit dual-form regex post-Phase 51.
- **First quadruple-alias surface** in project history
  (rationale; wikilinks + arxiv + doi + pubmed aliases
  simultaneously under 5-way default).
- **First "immediate-successor same-thread-direction phase
  boundary"** — Phase 50 introduced pubmed; Phase 51 extends
  pubmed with alias-syntax.

## Phase 50 → Phase 51 delta

| Metric | Phase 50 close | Phase 51 close | Δ |
|---|---|---|---|
| Tests | 1144 / 73 files | **1174 / 73 files** | +30 / 0 |
| ADRs | 24 | **24** | 0 |
| ADR-0018 D-G APPEND count | 17 | **18** (project record extends; ninth two-letter slot D-AI in use) | +1 |
| DB tables | 7 | 7 | 0 |
| Migrations | 9 | 9 | 0 |
| Env vars | 14 | 14 | 0 |
| `MARKDOWN_EXTENSIONS` single-value arms | 6 | 6 | 0 |
| i18n keys per locale | 168 | 168 | 0 |
| First Load JS shared chunk | 103 kB | **103 kB** | 0 |
| Middleware bundle | 160 kB | 160 kB | 0 |
| OPEN_QUESTIONS top-level Q-count | 66 | **66** | 0 |
| Phase-37+ candidate count | 8 | **8** | 0 |
| Runtime deps added | — | **0** | 0 |
| `lib/markdown/extensions/` files | 17 | 17 | 0 |
| **Phase-37-framework concrete consumers** | **5** | **5** | 0 |
| **Plugin-regex-extension realizations** | **3 (wikilinks + arxiv + doi)** | **4 (+pubmed)** | **+1** |
| **Dual-form regex realizations** | **2 (arxiv + doi)** | **3 (+pubmed)** | **+1** |
| **`remarkPlugins` consumers with dual-form regex** | **2 of 3** | **3 of 3** | **+1** |
| **Surfaces with N+ alias-syntax consumers (max cardinality)** | **3 (triple-alias; rationale at Phase 50)** | **4 (quadruple-alias; rationale at Phase 51)** | **+1** |
| **Fastest APPEND-deferral closure (phases)** | 3 (Phase 44 + Phase 48 tied) | **1 (Phase 51)** | **-2 phases (new record)** |

## Architectural firsts in Phase 51 (12 enumerated)

1. **Fourth realization of the Phase-46 plugin-regex-extension
   phase-shape pattern**. Phase 46 wikilinks; Phase 47 arxiv;
   Phase 48 doi; Phase 51 pubmed. **All 3 `remarkPlugins`
   consumers exhibit dual-form regex post-Phase 51** — first
   state where every consumer in the largest-cardinality slot
   has been extended with alias-syntax.
2. **Third dual-form regex** in the framework.
3. **Third plugin-regex-extension on a `remarkPlugins`
   consumer** in project history.
4. **First "dual-form regex with inner alternation inside the
   bracketed branch"**. Validates that the dual-form pattern
   generalizes to inner alternation; sets precedent for future
   consumers with multiple equivalent prefix variants.
5. **First "first-phase APPEND-deferral closure"** in project
   history — 1-phase carryover (Phase 50 → 51). **Fastest
   APPEND-deferral closure ever observed** (beats prior 3-
   phase record from Phase 44 + Phase 48).
6. **First "immediate-successor same-thread-direction phase
   boundary"** — Phase 50 introduced pubmed; Phase 51 extends
   pubmed with alias-syntax. First consecutive-phase pair
   where the second phase extends the consumer the first
   phase introduced.
7. **Tenth prep-/APPEND-doc-level deferral closed by a later
   phase** — APPEND-deferral closure cadence sustained 10
   phases. **Longest sustained cadence in project history**.
   **First 10-phase APPEND-deferral closure run**.
8. **Third non-cross-surface-expansion APPEND-deferral
   closure on the `remarkPlugins` slot kind**. **First "three
   consecutive non-CSE closures on the same slot kind"** —
   all three `remarkPlugins` alias-syntax extensions resolved
   within the cadence (Phase 47 arxiv + Phase 48 doi + Phase
   51 pubmed).
9. **Eighteenth APPEND on ADR-0018 D-G** — extends first-
   ADR-D-clause-with-most-APPENDs record 17 → 18.
10. **Ninth two-letter APPEND letter D-AI** (Excel-spreadsheet
    column convention sustained).
11. **First quadruple-alias surface** in project history —
    rationale carries wikilinks + arxiv + doi + pubmed aliases
    simultaneously under 5-way default. **First surface-with-
    4-alias-consumers cardinality of 1**.
12. **First "framework + 5 consumers + composition + 4
    expansions + 3-consumer same-slot composition + alias
    syntax × 4 (3 remark + 1 rehype) + dual-form regex × 3"
    15-phase cluster** in project history (Phase 37-51). 15-
    phase architectural arc. Plus **21st consecutive phase
    without new B category** — first 21-phase run (extends
    Phase-50 record 20 → 21). Plus **forty-second NON-§13
    phase**. Plus **115th consecutive 103 kB First Load JS
    unit** (extends from 111 at Phase-50 close).

## Phase 51 → Phase 52 entry conditions

Per §12, Phase 52 entry requires **explicit human sign-off**.

**Recommended Phase 52 first-thread candidates** (10 ranked):

| Rank | Thread | Units | Tractability |
|---|---|---|---|
| 1 | **PubMed PMID cross-surface expansion** to all 4 surfaces — constructor-arg value-only change in `PHASE_50_DEFAULT_ENABLED_SURFACES`; **fifth realization of "constructor-arg-only zero-rework expansion" property**; generalizes quadruple-alias state to all 4 surfaces; mirrors Phase-44 + Phase-49 expansion pattern; ~Phase 54 at 4-phase-gap cadence but rank-1 candidate at Phase 52 if priority shifts | 1-2 | Autonomous-tractable |
| 2 | **Older-style category-prefixed arxiv IDs** (APPEND-D-Y item 2) — regex extension on existing arxiv plugin; closes APPEND-D-Y item 2 at 11-phase carryover | 1-2 | Autonomous-tractable |
| 3 | **Table-specific attributes** (`colspan` / `rowspan` / `scope`; APPEND-D-Q item 3) — schema-override extension; first regex/schema-extension equivalent on `schemaOverrides` slot | 1-2 | XSS-audit-required |
| 4 | **ORCID auto-link consumer** (`orcid:NNNN-NNNN-NNNN-NNNN`) — sixth concrete consumer; **first 4th-`remarkPlugins` consumer**; closes APPEND-D-AC third item | 2-3 | Autonomous-tractable |
| 5 | **Cross-entity wikilinks** (APPEND-D-L item 3) — entity-type disambiguation + plugin parameterization | 2-3 | Requires APPEND-D-L item 6 plugin parameterization to land first |
| 6 | **`<a class="wikilink">` styling** (APPEND-D-L item 4) — couples with `schemaOverride` for `class` attribute on `<a>`; first single-consumer-multi-slot case | 1-2 | XSS audit per attribute value |
| 7 | **404 handling for unresolved wikilinks** (APPEND-D-L item 5) — build-time validation against `content/problems/` + render-time fallback | 2-3 | Autonomous-tractable |
| 8 | **Bare arxiv / DOI / PubMed IDs without prefix** — regex evolution; ambiguity-sensitive | 1-2 | Smaller-than-alias scope |
| 9 | **Empty-alias fallback unification across consumers** — Phase-48 deferral carries; refine the arxiv-vs-doi-vs-pubmed empty-alias behavior divergence | 1 | Smallest-scope autonomous-tractable |
| 10 | **ADR-0025 concrete content-moderation provider** | 3-5 | Strongest patience signal; NOT autonomous-tractable |

**Operational gates still pending** (6 UNCHANGED): Q54 + Q55 +
Q69 + Q73 + Q75 + Q77.

## Smoke gates

- `pnpm typecheck` — passes.
- `pnpm test` — **1174 / 73 files**.
- `pnpm audit-content` — 0 errors / 6 warnings (47 consecutive
  phases at Q32 baseline).
- First Load JS — **103 kB UNCHANGED** (116 consecutive units
  at Unit 51.4 ship).
- Middleware — **160 kB UNCHANGED**.

## Anti-scope (Phase 52+ deferrals)

NOT PubMed PMID cross-surface expansion (Phase 52+ rank 1);
NOT older-style category-prefixed arxiv IDs (Phase 52+ rank 2);
NOT table-specific attributes (Phase 52+ rank 3);
NOT ORCID consumer (Phase 52+ rank 4);
NOT cross-entity wikilinks (Phase 52+ rank 5);
NOT `<a class="wikilink">` styling (Phase 52+ rank 6);
NOT 404 handling (Phase 52+ rank 7);
NOT bare arxiv / DOI / PubMed IDs without prefix (Phase 52+
rank 8);
NOT empty-alias fallback unification (Phase 52+ rank 9);
NOT bioRxiv consumer (Phase 52+);
NOT OSF preprint consumer (Phase 52+);
NOT dx.doi.org legacy host (Phase 52+);
NOT stricter trailing-lookahead (Phase 52+);
NOT paper-card hover-preview (Phase 52+);
NOT plugin parameterization (Phase 52+);
NOT auto-trim of alias display whitespace (Phase 52+);
NOT `<caption>` element (Phase 52+);
NOT surface-specific table schemas (Phase 52+);
NOT `@mention` resolution (Phase 52+);
NOT 4th-or-later `remarkPlugins` consumer beyond arxiv + doi +
pubmed (Phase 52+ rank 4 would ship ORCID as 4th if elected);
NOT 2nd `rehypePlugins` consumer beyond wikilinks (Phase 52+);
NOT 2nd `schemaOverrides` consumer beyond tables (Phase 52+);
NOT ADR-0025 concrete moderation provider;
NOT Q78 / Q79 / v2 methodology / pre-commit typecheck /
safeAuth / test backfill / account-deletion (each deferred);
all Phase-30-through-50 deferrals carry forward.

## Boundary statement

**Phase 51 CLOSED at HEAD `<this-unit>` after 5 numbered units
(51.0 prep + 51.1 dual-form regex + plugin body + 13 NEW alias
tests + APPEND-D-AI + 51.2 17 NEW end-to-end alias tests + first
quadruple-alias surface + 51.3 hygiene + 51.4 gate). Fourth
realization of the Phase-46 plugin-regex-extension phase-shape
pattern: PubMed PMID alias syntax `[[pubmed:NNN|display]]` /
`[[pmid:NNN|display]]` via in-place dual-form regex extension on
`remarkLinkPubmedIds`. Third plugin-regex-extension on a
`remarkPlugins` consumer in project history (Phase 47 arxiv
first; Phase 48 doi second; Phase 51 pubmed third). All 3
`remarkPlugins` consumers exhibit dual-form regex post-Phase 51
— first state where every consumer in the largest-cardinality
slot has been extended with alias-syntax. Third dual-form regex
in the framework. First "dual-form regex with inner alternation
inside the bracketed branch" — the `(?:pubmed|pmid):`
alternation appears in both alternatives, inheriting Phase-50
dual-prefix support. First "first-phase APPEND-deferral closure"
in project history — closes new Phase-50 deferral at 1-phase
carryover (Phase 50 → 51). FASTEST APPEND-DEFERRAL CLOSURE EVER
OBSERVED (beats prior 3-phase record from Phase 44 + Phase 48).
Cadence trajectory for the four alias-syntax extensions: 8 → 6
→ 3 → 1 phases. First "immediate-successor same-thread-direction
phase boundary" — Phase 50 introduced pubmed; Phase 51 extends
pubmed with alias-syntax. First quadruple-alias surface in
project history — rationale carries wikilinks + arxiv + doi +
pubmed aliases simultaneously under 5-way default; first surface-
with-4-alias-consumers cardinality of 1. APPEND-deferral closure
cadence sustained 10 phases — longest sustained cadence in
project history. Third non-cross-surface-expansion closure on
the `remarkPlugins` slot kind — first "three consecutive non-
CSE closures on the same slot kind". 3-consumer same-slot
regex-disjointness discipline holds under dual-form extension
at 3-consumer cardinality. Eighteenth APPEND on ADR-0018 D-G
(project record extends 17 → 18). Ninth two-letter APPEND letter
D-AI. Forty-second NON-§13 phase. First "framework + 5 consumers
+ composition + 4 expansions + 3-consumer same-slot composition
+ alias syntax × 4 + dual-form regex × 3" 15-phase cluster
(Phase 37-51). 21st consecutive phase without new B category
— first 21-phase run (extends 20 → 21). 116th consecutive 103 kB
First Load JS unit at Unit 51.4 ship. XSS line of defense
preserved. 1174 tests across 73 vitest files (+30/0 vs Phase
50). 24 ADRs UNCHANGED. 7 DB tables UNCHANGED. 9 migrations
UNCHANGED. 14 env vars UNCHANGED. 168 i18n keys per locale
UNCHANGED. First Load JS 103 kB UNCHANGED. Middleware 160 kB
UNCHANGED. OPEN_QUESTIONS top-level Q-count 66 UNCHANGED.
Phase-37+ candidate count 8 UNCHANGED. `MARKDOWN_EXTENSIONS`
recognized single-value arms 6 UNCHANGED. Zero new ADRs /
migrations / i18n keys / env vars / operational gates / runtime
deps / files. Phase 52 entry requires explicit human sign-off
per §12.**

## ITERATE

- [x] Architectural firsts enumerated (12).
- [x] Phase 50 → 51 delta complete.
- [x] Phase 52 entry conditions documented (10 ranked
      candidates).
- [x] Smoke gates pass.
- [x] STOP at Phase 52 boundary per §12.
