# --- Node / pnpm / npm / yarn ---
node_modules/
.pnpm-store/
.npm/
.yarn/
.pnp.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
yarn.lock
# (pnpm-lock.yaml is committed once Unit 0.2 lands.)

# --- Next.js ---
.next/
out/
next-env.d.ts

# --- TypeScript ---
*.tsbuildinfo

# --- Turbo / Vercel ---
.turbo/
.vercel/

# --- Test / coverage ---
coverage/
.nyc_output/
junit.xml

# --- Playwright ---
playwright-report/
test-results/
blob-report/
playwright/.cache/

# --- Storybook ---
storybook-static/

# --- Lighthouse CI ---
.lighthouseci/

# --- Build / dist ---
dist/
build/
*.tgz

# --- Velite (generated content snapshot; rebuilt by `pnpm velite` / `pnpm build` / `pnpm dev`) ---
.velite/

# --- Env files (do commit .env.example) ---
.env
.env.local
.env.*.local
!.env.example

# --- OS / editor / IDE ---
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
*.swp
*.swo

# --- Misc ---
.cache/
.parcel-cache/
.eslintcache
.stylelintcache

*storybook.log

# --- Phase-5 curator-script caches + drafts (Unit 5.0 D-3 / D-7) ---
.arxiv-cache/
.pdf-cache/
.llm-spend.log
drafts/
