import { StubPage } from "@/components/ui/stub-page";

export default function RatingsFeedPage() {
  return (
    <StubPage
      title="Rating actions"
      description="Public rating-action log (HTML view). RSS and JSON variants live under /api/v1/. Phase 3."
    />
  );
}
